# The optional semantic layer (tools/semantic.py), installed into
# tools/.venv-semantic by tools/install_semantic.sh. Pinned to the versions
# the layer was built and measured with, transitive dependencies included, so
# an install is the same install on every machine. Everything ships as a
# prebuilt wheel — install_semantic.sh passes --only-binary=:all: — and there
# is no torch, no onnx and nothing to compile. model2vec's own graph is:
#   model2vec -> numpy, tokenizers, safetensors, joblib, jinja2, tqdm
#   tokenizers -> huggingface_hub -> httpx, hf-xet, filelock, fsspec, ...
model2vec==0.9.0
numpy==2.5.2
tokenizers==0.23.1
safetensors==0.8.0
huggingface_hub==1.29.0
hf-xet==1.6.0
joblib==1.6.0
Jinja2==3.1.6
MarkupSafe==3.0.3
tqdm==4.70.0
cloudpickle==3.1.2
click==8.5.0
filelock==3.32.5
fsspec==2026.7.0
packaging==26.3
PyYAML==6.0.3
typing_extensions==4.16.0
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.14.2
idna==3.19
certifi==2026.7.22

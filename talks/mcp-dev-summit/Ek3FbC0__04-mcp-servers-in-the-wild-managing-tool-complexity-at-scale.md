---
id: Ek3FbC0__04
title: "MCP Servers in the Wild: Managing Tool Complexity at Scale - Arnav Balyan, Concierge AI"
slug: mcp-servers-in-the-wild-managing-tool-complexity-at-scale
conference: mcp-dev-summit
conference_name: "MCP Dev Summit"
category: "AI engineering & agents"
edition: "MCP Dev Summit NA 2026"
year: 2026
speakers: ["Arnav Balyan"]
channel: "Agentic AI Foundation"
duration_min: 18
published_at: 2026-04-13T23:17:10Z
video_id: Ek3FbC0__04
youtube_url: https://www.youtube.com/watch?v=Ek3FbC0__04
tags: []
transcript: true
---

# MCP Servers in the Wild: Managing Tool Complexity at Scale - Arnav Balyan, Concierge AI

**Arnav Balyan**

`MCP Dev Summit` · `MCP Dev Summit NA 2026` · `2026` · `18 min`

[Watch the recording](https://www.youtube.com/watch?v=Ek3FbC0__04) · [Conference site](https://events.linuxfoundation.org/mcp-dev-summit-north-america/)

## Description

MCP Servers in the Wild: Managing Tool Complexity at Scale - Arnav Balyan, Concierge AI

As MCPs are adopted at scale, certain patterns start to emerge:
1. Increase in wrong tool calls
2. increase in token usage, semantic loss
3. agents skipping the required tool call sequence.

For high stakes domains such as finance and infrastructure, this leads to reliability and compliance risk.

This talk presents a new MCP server design pattern for addressing this class of problems called: "progressive tool exposure".
Rather than assuming static tools, the MCP server actively controls which tools are visible to the agent at each point in execution. This framework ensures, tools are refreshed, scoped, and changed as the agent progresses through an MCP workflow. This allows the server to direct the agent’s action space, guide execution order, and enforce "runbooks" without changing server/ agent capability. This design pattern also tracks state ensuring backtracking of tool calls reverts the server side state, making agent behaviour as transactional on the MCP server.

We show how such practices reduce invalid tool calls, lower inference costs, and improve determinism for tool heavy systems.

## Transcript

*3,489 words · source: supa (en, exact timings)*

**[0:00](https://www.youtube.com/watch?v=Ek3FbC0__04&t=0s)** Hey, so thanks for joining. I know it's Friday towards the end of the summit, so we'll keep it short. So, uh, I'll discuss how you can manage MCP servers in the wild. Especially as tool complexities grow, as the number of tools increase, there are new problems and new challenges that emerge. So, just a quick overview at the ecosystem. MCP is growing rapidly. I have no doubt you've seen similar graphs throughout uh yesterday and today. The total number of downloads every month increasing rapidly. We see over 130 million plus downloads in February of 26. Right? So really strong adoption 250 times increase since the early release in uh November of 24 and the open source repos that depend on MCP also rapidly increasing right so there is this debate about hey MCP versus CLI is MCP dead we strongly believe and the data points to

**[0:50](https://www.youtube.com/watch?v=Ek3FbC0__04&t=50s)** it as well that MCP is only growing and there's no signs of stopping right that being said the protocol is evolving and there's new challenges and the new use cases that come up especially we see two main problems as you go beyond 20 tools. The first is that tool descriptions and tool schemas are really large, right? So, as you go beyond a number of tools, you start to saturate the model's context window. And the graph on the lower right side, you'll see the number of tools on the x-axis and the total tokens they use on the y-axis. And you'll see beyond 550 tools, you hit the 200k context window. And this is reasonably large size descriptions. You see even bigger tool descriptions and tool schemas in the wild. So there's a hard limit beyond which you can't exceed the number of tools. So how do we scale this? That's the first problem. The

**[1:38](https://www.youtube.com/watch?v=Ek3FbC0__04&t=98s)** second problem is as we go forward the nature of use cases are converting from analytical use cases to systems that change the state right a tool that alters the DB. So previously user queries used to be like hey find me hotels in the New York area. Now user queries are book me a hotel in the New York area. Right? So as you go towards these use cases, make a payment for me, order groceries for me, order a cab for me, failure becomes really hard to reverse and the cost of failure is really high. So translating this to MCP essentially means that the agent needs to know which tool to call and when to call it. So calling the right tool at the right time is important. And out of hundreds of tools, you don't want agents to guess the tool rather the server can give its own opinions. So these are the two main problems that we start to see

**[2:25](https://www.youtube.com/watch?v=Ek3FbC0__04&t=145s)** as your tool complexity increases as you go beyond 20 tools. The first is really large descriptions, really large schemas. And the second is servers need to give their own opinions when the right tool should be called. So to solve for this, there's new paradigms and new patterns that we start to see in the last few months that we've seen that come up. Progressive tool disclosure, code execution, plan-based execution, some of which you may have seen in other parts of the conference. But there's different ways to implement that and I'll talk about how you can use these for your own enterprises. Bring down reliability uh bring down inference time, improve reliability, right? So the first use case is that we have really large tool descriptions and number of tools and the contract of MCP is straightforward. Right? The client just does a list tool. The server has to return back all the tools, right? If

**[3:13](https://www.youtube.com/watch?v=Ek3FbC0__04&t=193s)** your server is exposing 100 tools, the client always seals all the 100 tools, right? So the idea is that instead of exposing all the tools as a flat list in in the list tool response, you can essentially convert them into tool groups and you can categorize that hey these are similar tool groups that similar set of tools. Let's just call it a tool group. And now the model sees these tool groups instead of actual tools. So you reduce the number of tools from 100 plus tools into just five groups. And on the graph you'll see on the left side the LLM sees all the tools. On the right side, we've actually categorized them and exposing these tools as essentially five groups. And now the model sees just these five groups. And it can then invoke one of these tool groups which exposes new tools, right? So you've essentially categorized them into groups. And you

**[4:01](https://www.youtube.com/watch?v=Ek3FbC0__04&t=241s)** can do this n levels. So you can have tool groups within tool groups within tool groups. So you convert this listbased structure into a tree that the agent can now navigate and it can decide which tools to call and eventually reach the right tool set. But this is hard to do over MCP because MCP over the wire just gives a clean protocol. You do a list tool, the server has to return all the tools. How do you expose a tree over MCP or or over the wire? So for this we intelligently use tool refresh notifications. So whenever a tool group is invoked, a tool group is a special tool. It's an artificial tool. Uh, and when a tool group is invoked, the server sends a tool update notification to the client that hey, something has changed on the server side and you need to do a list tool again. And now when the client receives the notification, it does a list tool again and this time the server

**[4:49](https://www.youtube.com/watch?v=Ek3FbC0__04&t=289s)** returns back a completely different set of tools which are the actual set tools held within a tool group. Right? So you can imagine when the client does this repeatedly over time it gets access to different tools and instead of seeing hundreds of tools at the same time it can self-discover which tools it wants to see dynamically based on the user query right so this is a really powerful pattern if you have large number of tools you want to reduce the tool complexity tool space you can have the LM do this right we can actually go one step deeper and we can connect tool groups right we can create workflows out of tool groups and introduce dependencies This is essentially useful for use cases where you have a particular expectation of how your server should be used. For example, for an e-commerce workflow, you want the agent to first login, then browse, then probably pay uh probably

**[5:38](https://www.youtube.com/watch?v=Ek3FbC0__04&t=338s)** check out and then pay, right? You don't want the agent to check out before adding items to the cart. Right? So for these dependencies, if you as a server owner have these hard dependencies, you're dealing with highstake scenarios where you don't want the agent to be guessing tool calls. rather the server should be giving these opinions on its own. You can connect these tool groups and now the agent first sees tool group one but it does not see other tool groups. It only sees tool group 2 3 4 once tool group one has actually been invoked. Right? So the server keeps a track that hey the model has done something on tool group one. Then then the agent can decide to transition to the next tool group and only then are the other tools visible. Right? So the server is putting its own expectations and opinions on how the client sees the tools. The client is not randomly guessing things, right? So in this example, the model always sees only one

**[6:26](https://www.youtube.com/watch?v=Ek3FbC0__04&t=386s)** of these tool groups out of the four blocks and it can choose to navigate back and forth between different tool groups which refreshes the context window, right? So it never sees the entire entire graph, only sees one node at a time, right? And each node has a particular group of tools. So it still has some autonomy, right? you can still decide between all the tools that are within a group what to do but essentially give server owners really powerful patterns to represent their dependencies and and their graphs right so with this you can create workflows that are natively served through MCP over the wire instead of you know having a skills file that says step one do this step two do this step three do this hoping to the gods that the model follows it uh in this the server puts its own expectations we can actually go one step deeper and we can connect tools with memory. Right? So with this a model

**[7:18](https://www.youtube.com/watch?v=Ek3FbC0__04&t=438s)** can initially do hey state set user language preference is equal to English. Now all the other tools are connected to shared memory and they know that the user prefers English instead of the model having to send similar information across multiple tools. So these are mostly stateful servers where you've created dependencies between your tool groups. you've connected them to common shared memory and now you've created AI native apps that agents can navigate, interact and transact with all over MCP. Uh under the hood everything uses the tool update notifications that the server very intelligently sends and in response to those it keeps on dynamically refreshing the list tool output. So this is great if you want to reduce your tool complexity, right? Especially if you're not a power user, the first first slide that we discussed, this is essentially helpful where you

**[8:05](https://www.youtube.com/watch?v=Ek3FbC0__04&t=485s)** could just represent a basic tool tree instead of a tool list, right? But this just solves one problem which is that you have large amount of tool descriptions and tool schemas. You want to reduce that. But there's another problem with MCP that the tool outputs themselves are really large. Right? If you're dealing with a notion server that returns a 200page doc, now the model sees the entire 200page doc in its context window. it can't uh a choose to avoid it. Right? So with this the second pattern is codebased execution and the idea is that instead of returning the entire tool response to the LM's context window, let's just represent that in a variable, right? And have the model write Python code or TypeScript code through a sandbox. And this helps with the both the problems. Uh you essentially convert hundreds of tools

**[8:52](https://www.youtube.com/watch?v=Ek3FbC0__04&t=532s)** into just two tools or one tool depending on how you're implementing this. Uh there's different flavors to it. And I'll talk about how you can do this with one tool. It's much more simpler that way. But essentially the LM sees a code execution sandbox that's a single tool. It sends a Python string to the sandbox, executes code, and the output std out and stdrer is something that the LM can see, right? And all the tools are available as importable stubs in the sandbox and you also get access to a runtime object. And now the LM can write code on really large uh tools, right? really uh tools that return really large outputs. So in this example you'll see the LM first prints the two tools. It says hey give me the schema of notion search give me the schema of two sheets append and it does a print on it. So first call the LMC is the entire

**[9:41](https://www.youtube.com/watch?v=Ek3FbC0__04&t=581s)** description. Then the agent actually writes hey pages is equal to await tools.notion search right so that 200page doc is now in the pages variable instead of the lm seeing it. And now the LM can write code that hey do sorting searching on it do different kinds of Pythonic operations on it reliably and then send this refined output to another tool. Without this the LM would have to rely on its own token generation abilities to do sorting searching and reliably send massive outputs to the next tool and you're never sure whether it actually did it right because it can hallucinate UU IDs and partial data. With code, it's always guaranteed because uh the code the the Python sandbox is enforcing data guarantees. So you're always sure that this pre-processed output is exactly sent to the next tool. Right? So this is helpful

**[10:28](https://www.youtube.com/watch?v=Ek3FbC0__04&t=628s)** when you want to reduce the tool outputs and you also want to reduce the tool descriptions. But with this there are some drawbacks because you're still exposing a sandbox over your server. So if you have hundreds of clients, you're now having to maintain hundreds of sandboxes. That's usually expensive, resource intensive as well. Further, the LLM is writing arbitrary code on your sandbox which is not always safe. You can network isolate the the sandbox and always have the tools stub make the actual network call which makes things a bit safer. But still the LM is arbitrary writing code. So there's another way to represent your tools which solves both these problems which is plan-based execution. And the idea is that the LM can now actually write a JSON execution plan that represents how your tools are orchestrated. Right? So on a server

**[11:18](https://www.youtube.com/watch?v=Ek3FbC0__04&t=678s)** that's exposing hundreds of tools, the LM writes a plan like this that says step one is this particular tool. I want to send these parameters to step one. Step two is this tool. Step three is this tool. And the beauty of it is in step three, it can actually re-reference outputs of previous tools. So it can say dollar ref get me the output of tool number two and a ref output of tool22.details.sku right? So it can actually index to particular variables in a tool output and send this plan to the server. The server sees this plan and executes this and tools can be represented as a async tools as well. So you the model can say hey step three is actually a sync I don't care about the output just get it to me at the end or wait for the dependency to complete. Right? So with this as well you have hundreds of tools. You convert it into two tools. The first

**[12:06](https://www.youtube.com/watch?v=Ek3FbC0__04&t=726s)** is a tool search tool. The second is a JSON execution tool. Right? You can and and one more pattern that we see is tool search tool which is the most common right and the idea is that you have hundreds of tools. Let's just convert them to two tools. The first is a search tool. Second is the execute tool. The search tool takes in a search string arbitrary search string. Runs BM25 or some vector search under the hood. Returns the top K tools that match. And then the execute tool takes one of these the the tools that the LM sends and executes and returns back the output. So tool search is the most common that we see. But usually it makes sense for clients and LLM providers to do it rather than you as a server owner because if a client has 10 different servers all 10 different servers expose their own tool search tool makes things inefficient. Now what we are seeing is Enthropic, Charg, OpenAI, they they have

**[12:56](https://www.youtube.com/watch?v=Ek3FbC0__04&t=776s)** tool search tool on the client side as a global proxy to all the servers. But still if you have tools in an enterprise, you manage the client, you manage the server. If you have low number of servers, it still makes sense for server owners to do it on the server side. So this is great, right? These are different patterns in which you can represent your tools. You can you're essentially adding one level of abstraction over MCP and now intelligently orchestrating these tools. But when does it make sense for you to use one of these tool patterns and one of these design patterns? So to answer this question, we did a study. We took over 50 plus MCP servers in the wild, GitHub, Slack, Copilot, uh Chat GPT, and uh Calendar, Gmail, a couple of them, and we created tasks, abstract tasks. tasks like hey go to Slack find this bug

**[13:44](https://www.youtube.com/watch?v=Ek3FbC0__04&t=824s)** that the user has reported go to notion check our knowledge base and finally raise a GitHub PR to fix it and this is highly verifiable because eventually the model creates a GitHub PR which the system can systematically check that an actual PR was created and then we compare after representing the servers in different modes how each server did and the the highle observations that we got to is that if you have under 22 tools. Plain mode is the best for your use case. You don't want to move away. You don't want to go away from the standardization have the maintenance overhead. If you have over 50 tools uh and your tools don't have dependencies between each other, these are stateless tools. One tool does not care what has happened. Search is better. Uh and if you have dependencies, planner code is the right mode for you. And we'll be

**[14:32](https://www.youtube.com/watch?v=Ek3FbC0__04&t=872s)** publishing more numbers after this conference. But this is a preview of the numbers that we saw based on the study. uh and the graph on the lower right right side will show you the completion time. So we essentially track the time to complete the reliability rate the token usage across all these modes. So if you see at the time to complete plane mode is actually the fastest right because it does not have to do one more level of lookup do metadata discovery it has all the tokens available to it all the tool and the descriptions available to it. So plane is the fastest followed by staged which is progressive disclosure followed by code followed by search right search is the slowest because there's almost two times the lookups that the LM has to do. So time to complete plane is the fastest. If you look at the total token usage it's completely opposite. It's an inverse relation where you'll see plane is the

**[15:21](https://www.youtube.com/watch?v=Ek3FbC0__04&t=921s)** uses the maximum number of tokens because it always sees all the descriptions all the time. uh followed by staged followed by search followed by code right code is the most efficient in terms of token usage for your use cases you may want it depends completely on the use case which mode what what parameters you prefer whether you prefer time to complete whether you prefer token usage mostly we see people prefer token usage uh because few thousand tokens take up a couple of dollars and when this happens repeatedly uh leads to massive cost savings so uh that being said there are different patterns different patterns may work for your use cases if you're beyond 20 plus tools. Uh and these are some of the observations that we saw. Some other areas that we see are emerging is especially UI and charge GBD apps generative UI where the

**[16:10](https://www.youtube.com/watch?v=Ek3FbC0__04&t=970s)** LLM can write UI code on the fly and create a presentation layer for tool outputs and render personalized UIs. Right? If you like the color blue, you'll see a blue colored widget instead of someone else liking the color red who sees a red color widget. So that's one of the challenges that we'll be solving going forward. And distribution is another area of focus where you create an app but you actually give it distributed to your servers uh to your users because once you've created the app the next step is to let your customers know about your app and iterate on it. So we see that loop broken in a lot of cases. people create their servers and uh usually it's forgotten and users have no reliable way of discovering the servers. So concurge SDK is something that we're building in the open source. It's an open source initiative we launched a

**[16:56](https://www.youtube.com/watch?v=Ek3FbC0__04&t=1016s)** couple of months back and the idea is that hey we are at a point where tool complexities are increasing. You you want users to convert their tools into powerful applications. How do you do this? So this is an open source project that allows you to convert your servers into one of these representations. Uh with one line of code you can convert your server into code mode, into search mode, progressive disclosure and a lot more. And the framework evaluates your servers for you, gives you a score how your server is behaving, optimizes tool descriptions for you and then you can convert that to a powerful representation. So you can if you want to work on the frontier of MCP or you want to get involved, you can check out the GitHub and always happy to collaborate and talk more. Thank you so much.

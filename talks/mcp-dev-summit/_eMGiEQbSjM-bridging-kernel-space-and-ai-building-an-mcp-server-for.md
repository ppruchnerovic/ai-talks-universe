---
id: _eMGiEQbSjM
title: "Bridging Kernel Space and AI: Building an MCP Server for Linux Scheduler Observabil... Daniel Hodges"
slug: bridging-kernel-space-and-ai-building-an-mcp-server-for
conference: mcp-dev-summit
conference_name: "MCP Dev Summit"
category: "AI engineering & agents"
edition: "MCP Dev Summit NA 2026"
year: 2026
speakers: []
channel: "Agentic AI Foundation"
duration_min: 30
published_at: 2026-04-13T23:19:13Z
video_id: _eMGiEQbSjM
youtube_url: https://www.youtube.com/watch?v=_eMGiEQbSjM
tags: []
transcript: true
---

# Bridging Kernel Space and AI: Building an MCP Server for Linux Scheduler Observabil... Daniel Hodges

**Speaker not identified**

`MCP Dev Summit` · `MCP Dev Summit NA 2026` · `2026` · `30 min`

[Watch the recording](https://www.youtube.com/watch?v=_eMGiEQbSjM) · [Conference site](https://events.linuxfoundation.org/mcp-dev-summit-north-america/)

## Description

Bridging Kernel Space and AI: Building an MCP Server for Linux Scheduler Observability - Daniel Hodges, Meta

The Model Context Protocol enables AI assistants to interface with external tools and data sources, but most examples focus on high-level APIs and databases. This talk explores building a production MCP server that exposes low-level Linux kernel observability data to AI assistants, enabling natural language debugging of complex systems.

`scxtop` is an observability tool for Linux's new sched_ext extensible scheduler framework (https://github.com/sched-ext/scx/tree/main/tools/scxtop). By implementing MCP, it allows developers to ask questions like "Why is my application experiencing high scheduling latency?" and receive AI-driven analysis that correlates kernel tracing data, hardware topology, performance counters, and scheduler internals.

## Transcript

*4,629 words · source: supa (en, exact timings)*

**[0:00](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=0s)** Perf events. Um, and this lets you do some really powerful analyses that uh, you know, you just couldn't do otherwise. Uh, MCP also makes this data conversational. So you can ask questions like why is this process stalling uh, for or stalling for 50 milliseconds. So we have a really nice useful human interface. Um but some of the problems that this also helps solve is BPF tooling has a really steep learning curve. Uh if you haven't written BPF before, it can be very painful and you have to understand the data uh the data model and the execution model in the kernel to be able to write BPF code. It also unlocks hypothesisdriven investigation uh where AIs are actually really good at taking really uh wide

**[0:52](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=52s)** dimensional data and to be able to ask questions and uh do complex analysis over it uh in a similar way that like a performance engineer would. So what is VPF? Um just as a really quick one slide um it's basically it runs inside the kernel. It's a jitted runtime. It's verified when BPF programs are loaded so it can't crash your kernel and uh you get basically near native execution with the JIT. Uh it also has access to what are called core data structures where you compile once and run anywhere. So it's able to run across different kernel versions and still work the same. Uh and our key insight is BPF lets you add that custom instrumentation to a running kernel without writing

**[1:40](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=100s)** kernel modules or even rebooting. So what's our plan? We want to build a BPF MCP tool. Um we're going to talk about SCXTOP which is auler MCP tool and some of the uh uniqueness about that. and we'll talk about some of the problems you'll encounter when writing BPF tools and some best practices. So, for building a simple MCP uh tool, we're going to start really simple with a single trace point uh one ring buffer. A ring buffer is basically how you pull events out of BPF and into user space. We'll have five different tools and we're going to use JSON, synchronous, standard IO. Um, and for this example, we're going to

**[2:28](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=148s)** use lib bpfrs. Uh, and there's a lot of different frameworks that you can use. Uh, but for our example, we're just using this one direction. And there are some like because this is a simple example we're going to walk through. You know, we're not going to cover permissions and security. And also, uh, as part of that security model, uh, you can have, uh, AI agents generate VPF bite code and load that into the kernel. That's a very dangerous thing to do. So, uh, we're just going to keep it simple. So, for building our MCP tool, uh, there's a reference if you want to follow along. It's on my GitHub at Hodgeges DS and BPF-mcp example. And for this, we're not going to cover things like complex BPF event types, BPF map types, task storage,

**[3:18](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=198s)** stack traces, or sampling. We'll get to some of those later in the talk. Uh, but our example is very simple. So, we have a five-step process for building our uh simple tool. So, first we need to define our BPF events. Then, we're going to write a BPF program that uses those events. We're going to set up ring buffers to communicate from kernel space to user space. Then, we're going to do aggregation of the data and expose those as MCP tools. So for defining BPF events, we just have a really simple event that we're hooking into. This is the SCD switch event. Uh and this event happens every time a process changes from one process to another in the scheduler. Uh it fires this event. So we want to capture that

**[4:07](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=247s)** and understand what's happening. Uh so we have a very simple strct where you know we have our event we have an event type which will be important later in the talk when we talk about how to collect multiple events and then just some common fields like PID uh task name things like that. So for our BPF program we also need the ring buffer. The ring buffer is the uh interface from the BPF kernel space to user space. And it's just defined like this as a simple strruct um with a name called events. For our BPF program, we'll have event handling. Uh there's a couple interesting things here to unpack. The at the very top there's a yellow highlighted section of code. It's

**[4:56](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=296s)** basically a macro that says we're using BTF to attach to this SC switch trace point and uh it's just a macro that attaches to that specific event. Then we have our program definition and here in white you can see where we set up our event and ring buffer. We reserve a entry from the ring ring buffer and in the green we just kind of fill out the fields in our strct using various BPF helper functions. And then finally in the white we submit the event. So that's basically a very simple BPF program. So on the user space side um we also need to set up the ring buffer so we can consume from it. Uh so I'm going to walk go through this pretty fast just to get

**[5:43](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=343s)** through all the slides but uh all this is on the GitHub if you want to follow. Um but anyways we'll lib bpfrs has this ring buffer builder. So we build the ring buffer builder. We add a call back uh that basically runs every time it consumes from the ring buffer. Um and in green you can see we're switching on our event type and then processing the event type where we're recording the SC switch. And lastly we have to consume from the ring buffer. Uh so this is also rust by the way if you're not familiar. Um but uh basically with our ring buffer consumption we're launching a background thread where we uh pull from that using e-le and then we just consume from the ring buffer.

**[6:32](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=392s)** So now we need to aggregate the data. So basically we're transforming our kernel events into structured user space data. uh and in this we can also have a uh we can parse our event from rust which gets generated um and we can also aggregate that into a stat strct. So aggregation can take very uh various types of form. For our example, we're just going to aggregate in user space. Um, and we'll there's a helper method that resets all the stacks, but basically we're just using a hashmap to store all our events. Um, and here's the record switch implementation where it's basically just updating the map every time and keeping

**[7:20](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=440s)** track of uh when a one process wakes another one up. Uh, finally, we need to expose this via MCP. Uh so we have our basic simple JSON of our different endpoints for the MCP server such as getuler stats, enable collection, disable collection. Um and this allows our MCP tool to be discovered. And one last final detail for the MCP server is we want it so that uh because BPF programs vary in complexity and can have different amounts of overhead uh we want a enable and a disable method. And so what this does is uh it basically loads

**[8:09](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=489s)** the BPF program uh when that tool is called. So the tool gets called it can run and then when the tool is the disable uh collection tool is called it uh unloads the program. Uh so you can see in green where basically the call to the attach of the skitch program happens that code path is basically saying like the BPF program has been loaded successfully. uh otherwise you know we hit the red uh state where you know if it's already attached we fail out or we just error if we couldn't attach. So to recap our simple uh MCP server we want to follow our five steps of defining BPF events writing our BPF

**[8:58](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=538s)** program uh setting up ring buffers doing aggregation and exposing that as MCP tools. But this simple example leads to uh many interesting questions of you know what if we need to handle security right uh we may not want uh to just load thing have AI load BPF programs that can do you know nefarious things right um we also h want to discuss like should this just be a tool versus MCP uh so some of the problems with having it be a CLI is you need implicit permissions to be able to run the CLI. And if you're running it as root, then maybe that's not great versus exposing it as MCP where you can have different protocols such as HTTP uh

**[9:47](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=587s)** to have a secure uh MCP server running. Um and the other thing we want to know is can this pattern scale to large machines and large number of events. Uh, and the other question we want to think about is would this work for large deployments? Could you deploy this on every machine in your fleet or is this more for debugging? So, let's switch gears a little bit and talk about uh an actual MCP tool. Uh, so this tool is kind of built around SCKEDex. Uh so sketex if you're not familiar is a way to build a scheduler in BPF and you load theuler into the kernel and it makes all uh scheduling decisions for you. So with this we

**[10:36](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=636s)** started out with a lot of observability with BPF trace scripts but eventually it got to the point where maintaining all sorts of just BPF trace scripts got kind of tedious. uh so that's why I started working on SCX top which is a way to aggregate those events and try to make sense of it so the architecture of it is there's both the kernel side which has various uh BPF programs I think it hooks into like 15 or 20 um and then on user space uh there's a couple different ways that it works it has a TUI mode which is kind of like a top uh it's built in Ratatouille uh it has a MCP mode which allows you to connect to uh you know agents or other things like that and it

**[11:25](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=685s)** also has a trace mode to generate trace data. Uh we'll talk about trace data more in a little bit. Uh but the really interesting thing here is there's a event processing layer that decoupled the TUI the MCP and the trace mode. So as you're consuming events off the ring buffer, they go through a generic uh event dispatch layer. Uh and that allows it to scale pretty well. There's also multiple ring buffers. So one of the things that we noticed is that when you're collecting a large number of events, let's say on a machine with several hundred cores, uh we'd start losing events because a single ring buffer just couldn't keep up. So we had to shard those out. So you could do

**[12:12](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=732s)** sharding based on things like L3 cache, numa node, things like that. Uh but it all comes down to like how many events you're pulling off of uh the ring buffers. So here's just kind of an uh example of one of the tuy views where you can see it's just basically like a top-like program. Uh but it also has some more advanced 2y views where you can see detailed stats. Um so this is auler view where you can see different things. Uh different cues are called DSQs and you can see the uh the hex over here of the different dispatch cues. and then aggregations of Q latency um based on P50, P90, P99 etc. and Q depth and then

**[13:02](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=782s)** sort of uh process views where you can analyze and see which processes are experiencing the most uh scheduling latency. It can also generate traces. So depending on the type of data that you're collecting, you can also generate peretto. Uh, Peretto is a uh, it's basically a whole uh, tracing and profiling framework that's developed by Google where you turn those events into Protobuff events and then you can load it into the Proetto UI and be able to run complex SQL queries and also do very interesting analyses um, using you know the Proetto UI. So this just is a example of some of the

**[13:51](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=831s)** scheduling data where you can see per CPU views of what things were scheduled and for how long. Uh so SCXtop also has a MCP analysis where it can do a lot of different or it has many different tools. uh but you can sort of ask questions like you know which cues are h have the most delay or uh which processes are experience seeing the most delay. Uh so in this example we can see different uh processes being exposed. We can see how many times they're context switching, what's their average latency, what's their max latency. Uh so if you really care about performance of your application, it can be a pretty powerful

**[14:39](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=879s)** tool. Um and it could also tell you tell you which cues are being hit. So here's a brief overview of the different MCP tools. I'm not going to cover all of them just because they're a lot. Um but they're kind of broken down into different categories. So it can do dynamic runtime profiling where you can start and stop profiling. It can also do live analysis of peretto data. Um also just you know loading peretto traces. So it has a mode where it will take a trace or and then be able to load it into its process memory and then also do analysis across that trace by doing uh generic queries just like you would would in the uh peretto UI or CLI to do aggregations

**[15:30](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=930s)** of P99s or you know P50s whatever you really want. Um it also has some exposure of hardware and topology and different events that are available on the system. So one thing that uh AIS really need to be aware of are like what events can I actually profile. Uh so it's important that it has tools that allow you to do you know these different types of things. So one problem that we have when we had our simple example that we needed to solve with uh SCXtop was how do you deal with multiple events coming through the same ring buffer and I kind of talked about the type earlier. So basically we have a generic BPF event strruct for

**[16:20](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=980s)** SCXTOP and one of the first fields is the type so that it can be parsed in user space. Um and then we have a set of common fields here such as size, time stamp or CPU. And then basically our events are just one big union. So you can see we have fork, exec, uh sked, switch, perf, sample event, etc. Um so that's basically the way that if you're building a BPF MCP tool and want to have different messages over the same ring buffer, you can just use this pattern. Another problem we had that I alluded to was uh high frequency events. When you have uh just uh SC switch, it can do many tens of thousands or hundreds of thousand events per se second and that

**[17:08](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=1028s)** generates a lot of data. Uh so depending on the type of data that you're doing, you may want something like a per CPU counter. Um, and without going into all the different BPF map types, uh, here's an example of a per CPU counter. So, we just have like a generic stats counter here in the white on the left. Uh, and in green we can see it's a map type per CPU array. And that's really cheap if you're just collecting counts of events that happen. If you don't want to know like specific details and just counts, um, it's a highly effective way of doing it. Uh and then on the left side we can see in the green where we load up our BPF skeleton. We uh we can look up per CPU data for uh the stats map that we have defined. Um and this code is just

**[17:59](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=1079s)** you know very specific for this use case but once again if you're implementing this in whatever language it'll be not too different. So, we also have the problem of if you're tracking a individual task. Um, let's say you're interested in like what's the latency of uh, you know, my Chrome tab when I'm watching YouTube. I want it to be scheduled so the audio doesn't crack or things like that, right? So, we need a way to t uh to track per task. And for that, we use this special map type called uh task storage. And basically what it allows you to do is have a uh basically allocate an event that gets stored on the task struck uh and you can update it and do various things like that. So

**[18:50](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=1130s)** that's very useful if you're trying to uh get a full trace of like when a task wait wakes up until it gets scheduled or when it goes through various uh events. you can use a uh test storage map. Another problem that we kind of ran into was PID is not a stable interface. So if you're just trying to track different events and store a PID into a BPF map and then do something based on that PID later, uh the PID may have changed because the PID life cycle is shorter than the actual task struck existing. Uh so a trick we had to do with that was basically uh we take the task pointer convert it to a U64 and then use that to

**[19:38](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=1178s)** store into our maps as a stable uh way of updating or tracking an individual task and that's useful if you're doing aggregations across like task uh but not doing task specific storage. Another problem we had to solve is what happens if you want to do dynamic tracing. You may not want to have a uh BPF program for every single possible trace point. You may want to select that dynamically. Um so on the left here we can see we have a static K probe uh for BPF SCEX reg which is when auler gets registered. Uh and you can see we have a special type in green. uh we give it a time stamp, do the ring buff, that's all good. But on the right

**[20:26](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=1226s)** side here, we have a generic K probe. And what a generic K probe allows you to do is from user space say I want to attach to this specific event. And the really powerful thing about that is you don't have to know the event ahead of time on the BPF side. uh and we handle that with this uh BPF funk IP uh ctx which basically gives you the function pointer and we set that on the event and then we can use that to look up the function address in user space. Uh on the rust side, it's also just uh we have this attach k probe function where we can say I want to attach to TCP send message, right? Um and it's just a pretty easy interface to use. Another problem we had is if you want to

**[21:17](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=1277s)** call into BPF from user space, uh there's this API called BPF prog run or test run. It originally was used for testing purposes, but you can kind of hijack it to use uh it to run any BPF event. Uh so on the BPF side, you can set up a SIS call and u here we call SCEX top init. And for this we're just initializing a bunch of timers and doing other setup that we need for uh our MCP server to work. And similarly on the rest side um there's a API where we uh first reference the SCX top init program and then we can call into it using the

**[22:05](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=1325s)** test run uh which libpf abstracts for us. There's also program input. So you can get like you can send data to BPF from user space and then do whatever you want with that. Uh it's not shown in this example though. So permissions is also a problem depending on what you're trying to do with BPF. So BPF needs either root or capsis BPF and maybe a number of other permissions depending on what your tools do and uh that may be far more permission than you want to give uh to your agent because that's a lot of permission. So when we're dealing with that uh another thing to consider is we have local instances and remote and where you

**[22:56](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=1376s)** collect that data becomes kind of important of uh sometimes it's better to just like run a tool to generate a trace or generate some dump of the data and then do your analysis on a machine with uh less permissions. Uh and this also is kind of an issue is that needs to be considered of if you have a continuous process that you're like monitoring your entire fleet or if it's more or if you're building a tool for like one-off debugging maybe it's okay to like you know launch it with pseudo and that's fine. Um so as I mentioned SCX top has a trace subcomand where it can just collect the peretto data and then you don't have to worry about permissions as much um or it's more isolated and that way for analysis you don't have to worry

**[23:46](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=1426s)** about permissions. Um another problem we kind of went through I'm going to try to speedrun so we uh make it in time for questions of event aggregations. Um, basically as I mentioned like ring buffer and having multiple of those is what you have to do to get it to scale on machines that just have a lot of events and a large number of uh CPUs. Um, you can do aggregation in BPF. Uh, BPF trace is a pretty common tool that uh it does that for you. Um, so that's another method as well. So to recap, um, for BPF, if you're scaling to large machines, you need ring buffer sharding, you want to do sampling

**[24:34](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=1474s)** where you can, uh, be able to control sampling through the life cycle of a task, which probably means you need task storage. Uh, if you want to do really high resolution timers, you probably want BPF timers for that. for uh sampling at very high resolution intervals. Uh you can use per CPU maps for high throughput data that needs to be collected and aggregated uh if you just care about counts and sys call programs allow you to uh basically call BPF from user space. So some of the best practices for MCP tools um basically know your BPF life cycle and have that be uh well defined. You don't want or depending on the tool you may want it to run uh consistently

**[25:24](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=1524s)** or you may want it to have a shortlived life. Uh the MCP tool should also have discoverability for the different events that it supports. So if you've ever used like Perf, you it has Perf list which lists out all the different Perf events or BPF trace L. Um but the danger of that is you also need to return like filtered data because it can blow out your context really uh quickly because there's so many events. Um, and also it's if you're trying to build tools that analyze BPF, uh, having some way of giving it topology awareness, a lot of times it can shell out to like LSCPU or things like that and, uh, get some of that data, but you can also build it into the MCP tool itself. Um, and then finally, having a data storage strategy

**[26:11](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=1571s)** that makes sense with the data that you're trying to collect and what you're trying to do. Uh so if you're doing uh uh trace analysis, Perfetto is pretty good. And for analysis tools, uh a lot of times you need to join with other data sets. So often you'll want to join it with like kernel source code or uh user space source code and you may want like actual PF data coming from your metrics data or hardware counters. So depending on where that lives and how that's structured, uh you may want your analysis tools to have access to all those. So it may mean making a really big uh set of MC tools or just having a bunch of separate MCP tools. Um

**[27:03](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=1623s)** and okay so how do we generalize this to solve pretty much any problem that you would face or your company faces? Um I think there's a lot of opportunities for uh security uh observability debugging networking. There's just so many areas that BPF uh can just give you insight that you know there's not a lot of tools right now that are kind of built for AIS to use. Um so like SCXTOP is just a very niche thing for understanding BPF schedulers but you know this could be generalized for almost anything. Um and then just some of the considerations of making sure you understand the data model. um the

**[27:51](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=1671s)** security and how you want the tool to work. Um all right, I left 2 minutes. Sorry. Any questions? And I have a bunch of stickers if you want at I'll have them in the back uh afterwards. So question >> I know this whole talk was about kernel space. I'm just curious if you know anybody who's doing something similar for like a JVM kind of environment with taking like bread dumps and so forth like doing that kind of analysis. >> Um I'm not personally aware because I don't work on JVM stuff but I think the same lessons could apply uh for like JVM thread scheduling uh things like that. It the JVM does have a lot of like

**[28:41](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=1721s)** points where you can uh observe it. So I think that's a really cool idea. Yeah. I have a question about your sort of end to end workflow here. So like where are you kind of coming from like at a starting point like what are you asking your agent to do? Is this like are you asking to do this sort of like pinpoint like I want to do this for federal analysis or like did you have some other problem that you were starting from and like what is the whole kind of thing? >> Yeah. So like I work on the BPF schedulers themselves and I also work on doing deployments of those for specific services. Uh so sometimes I need to understand like why a scheduler is not performing as well as another scheduler. And so for me if I have a way where I can feed in theuler source code and a bunch of raw performance data then I can

**[29:30](https://www.youtube.com/watch?v=_eMGiEQbSjM&t=1770s)** understand like why is theuler working in this specific manner and what does it mean when I see these specific uh perf events. >> Yeah. Cool. All right. If no other questions, thank you for your time.

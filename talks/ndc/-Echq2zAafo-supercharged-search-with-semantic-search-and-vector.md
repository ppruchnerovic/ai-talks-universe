---
id: -Echq2zAafo
title: "Supercharged Search with Semantic Search and Vector Embeddings - Giorgi Dalakishvili"
slug: supercharged-search-with-semantic-search-and-vector
conference: ndc
conference_name: "NDC Conferences"
category: "Software dev with AI tracks"
edition: "NDC"
year: 2026
speakers: ["Giorgi Dalakishvili"]
channel: "NDC Conferences"
duration_min: 55
published_at: 2026-01-19T11:20:18Z
video_id: -Echq2zAafo
youtube_url: https://www.youtube.com/watch?v=-Echq2zAafo
tags: [".NET", "AI", "NDC", "Conferences", "2025", "Live", "Fun", "Copenhagen", "Denmark", "Giorgi Dalakishvili"]
transcript: true
---

# Supercharged Search with Semantic Search and Vector Embeddings - Giorgi Dalakishvili

**Giorgi Dalakishvili**

`NDC Conferences` · `NDC` · `2026` · `55 min`

`#.NET` `#AI` `#NDC` `#Conferences` `#2025` `#Live` `#Fun` `#Copenhagen` `#Denmark` `#Giorgi Dalakishvili`

[Watch the recording](https://www.youtube.com/watch?v=-Echq2zAafo) · [Conference site](https://ndcconferences.com/)

## Description

This talk was recorded at NDC Copenhagen in Copenhagen, Denmark. #ndccopenhagen #ndcconferences #developer #softwaredeveloper

Attend the next NDC conference near you:

/        @NDC

Follow our Social Media!

Do you need to search your data based on meaning instead of matching by keywords or phrases? Do you need to match the data in a language different from the query term?

Semantic search, or search based on the meaning, analyzes the context and intent behind a query to deliver more relevant results. Using Vector embeddings, the data structure behind semantic search, you can enhance your search to include text, images, and other types of data. With vector databases, you can store and index vector embeddings and provide similarity search over these embeddings.

In this session, we will explore the fundamental concepts of semantic search, including vector embeddings and similarity metrics. You will learn how to generate embeddings using large language models with OpenAI. I will then demonstrate how to store, index, and query vector embeddings using the vector data type in Azure SQL Database or with the Microsoft SQL Server 2025. Additionally, you will see how to optimize the queries with the DiskANN vector index. Finally, you will see how to save and query these embeddings from .NET with Entity Framework Core and the SQL Server VectorSearch library.

Join me for a demo-rich session and learn how to implement semantic search in .NET with Azure SQL Database and Entity Framework Core.

## Transcript

*6,602 words · source: supa (en, exact timings)*

**[0:00](https://www.youtube.com/watch?v=-Echq2zAafo&t=0s)** Hello and thanks for attending my session. Today I will talk about uh superchar supercharge search with semantic search and vector vector embeddings. Uh a little bit info about me. My name is Gyorgeshuri. I'm a C# and net developer for about 17 years. I work with uh I have worked with various relational databases. I also maintain couple of open source projects on GitHub. So if you use entity framework core, I suggest you check this project. I think they will be interesting for you. So let's start with the uh with our today's topic. I will talk about semantic search, vector embeddings and

**[0:48](https://www.youtube.com/watch?v=-Echq2zAafo&t=48s)** vector databases. So first of all uh what is the difference between the uh traditional search that is uh called lexical lexical search and semantic search. The uh lexical search returns results based on exact matches of the words or derived words that the user enters. While in case of semantic search the search results may not contain the exact terms and phrases and words that the user entered. But the search results will match on the semantic meaning of the of the search query. It may or may not contain the exact words that were entered in the search system, but the results will match on meaning. For

**[1:36](https://www.youtube.com/watch?v=-Echq2zAafo&t=96s)** example, if the user searches learning how to use a database, one one of the results can be SQL crash course. It does not uh contain the exact words that the user entered but it matches based on the meaning. So what are the uh benefits of semantic search? One of the benefits is that it can distinguish between phrase phrases that contain same words in different orders. For example, chocolate milk and milk chocolate. These are two very different things. One of them is a flavor of chocolate and another the drink with flavor of chocolate. So the semantic search can distinguish between the uh meaning and what what the user wanted to search for.

**[2:24](https://www.youtube.com/watch?v=-Echq2zAafo&t=144s)** It's also easier for the users because they don't need to remember exactly the terms that you use in your database on on your website. They they may not remember the names. They they may not know what words and phrases you have used to describe the items in your database, but they know what they are searching for. They will type what they want and semantic search will be able to return the uh results based on the meaning rather than based on the exact words. So it's it's it it is more user friendly. It's more natural and it's similar to the real life experience when you go to the shop and you ask the consultant or what what you want and the uh seller will offer you the items that match your the items

**[3:13](https://www.youtube.com/watch?v=-Echq2zAafo&t=193s)** that you described. So how does the semantic search work? Uh it's it's based on three main components. it's large language models or LLMs uh vector embeddings and similarity metrics. So LLMs are trained on a vast amount of data and they are the uh they understand the context and relationship between the meaning of the sentences and they are the foundation between behind for capturing the semantics of language. With LLMs we can generate vector representations of text or any other data. Uh it is called vector embeddings and vector embeddings captures the

**[4:01](https://www.youtube.com/watch?v=-Echq2zAafo&t=241s)** uh the semantic meaning behind the sentences or words or phrases. Embeddings basically map uh the textual data to a highdimensional uh numbers which uh and in the to high dimensional vectors where the uh text with similar meaning produces vectors which are closer to each other in this highdimensional space. And the third component is similarity metrics which measures the similarity between vectors. With similarity metrics, we can determine whether the uh search results are closer to the query terms that the user entered or whether they are far apart relative to the search query. So we can

**[4:51](https://www.youtube.com/watch?v=-Echq2zAafo&t=291s)** uh generate the uh similarity uh we can calculate the similarity between the documents that we have in the database and compare it to the uh search term uh vector embeddings generated from the search search term and determine uh which results are relevant to the user. So uh vector embeddings are uh as I said it's a numeric rep representation of data and they represent the meaning and context processed by the large language model. It can be generated from any kind of data like text, images, audios or videos. We can there are different modules that support different input formats or there are also some modules that support several input formats and can generate embeddings from different

**[5:40](https://www.youtube.com/watch?v=-Echq2zAafo&t=340s)** types of inputs. The most important most important thing to remember is that two objects that have similar semantics will uh get vector embeddings that are closer to each other in the uh in the vector space and vector embeddings have many different use cases. One of them is semantic search but it can be also used for text classification recommendation recommendation system or anomaly detection or some other use cases. For example in in this uh diagram that's that you see if we imagine for a for a second that the vector embeddings are three-dimensional vectors in that case we can visualize

**[6:28](https://www.youtube.com/watch?v=-Echq2zAafo&t=388s)** the vectors. So we can see that all animals are closer to each other because they are animals. So they are separately. Uh we are inside. So inside inside this green sphere we have different animals. They are close to each other because they are all different types of animals. And then we have fruits which are far apart from animals because fruits and animals don't have anything common. But all the fruits are also closer to each other because every item like bananas, apples and any other fruit is fruit. So they are closer to each other. So basically vector embeddings are a way to convert words and sentences to numbers that capture the meaning and relationship between the uh

**[7:17](https://www.youtube.com/watch?v=-Echq2zAafo&t=437s)** between these words and sentences. Another uh analogy that we can use is the uh supermarket. When you go to the supermarket, there is a section for fruits. In this section, you know that different fruits will be closer uh to each other because uh all fruits are sold in one space in one area of the supermarket. And you if you find apple, you know that other fruit will be also some somewhere near. But some other items like magazines, they will be in a different part of the supermarket because they are not related to the fruits. So how does uh semantic search work? Uh first of all uh we have some data in our database uh represented on the uh left

**[8:07](https://www.youtube.com/watch?v=-Echq2zAafo&t=487s)** side. Uh it can be uh like I said can be text, images or any other data. uh we generate vector embeddings from this uh from this data. Uh we use an embedding generator with some uh model uh that we that we choose to generate the embeddings with this uh the embedding generator will give us vectors. We store this uh vectors in some vector store in some kind of vector database. So we we have this uh vector embeddings persisted in the database and when the uh when we have a request for search for for a search we take the search query uh we again generate vector

**[8:57](https://www.youtube.com/watch?v=-Echq2zAafo&t=537s)** embeddings using the uh using the embedding uh generator. It's uh it's important to use the very same model that we used to generate the uh embeddings from our database because uh if we uh use another model then we won't be able to compare the vector embeddings that we have in the database to the vector embedding generated from the search query. So once we have the vector embedding generated from the query term, we we use a similarity metric to see which vector embeddings in the database are close to this search uh queries vector embeddings and uh we take the nearest items and get get the uh get the items that are closer to this uh search

**[9:49](https://www.youtube.com/watch?v=-Echq2zAafo&t=589s)** query. we can use either uh exact nearest neighbors to find the exact matches or approximate neighbors uh for approximate results. So basically the process is that we generate the embeddings from the data that we have in the database. We store this embeddings somewhere and then whenever the user wants to search something we again generate embedding for this query term and find embeddings that are closer uh to to this to this embedding. Uh so let's uh let's talk a little bit more about similarity matrix and uh what

**[10:38](https://www.youtube.com/watch?v=-Echq2zAafo&t=638s)** does it mean for vectors to be close to each other or far from each other. For example, in two-dimensional space uh if we have points because in two dimensional space we have just x and y which is a point. These blue dots represent the uh vector embings from from our database and the orange dot is the uh vector embedding of the queries that the user entered. So if we uh if we look at this it's easy for us to identify which one is the nearest. You can see that the uh top blue item is the nearest one to the uh to the query embedding. So in two dimensional space we can use

**[11:28](https://www.youtube.com/watch?v=-Echq2zAafo&t=688s)** this formula to calculate the ukidian distance between points. So if we calculate the distance between all points and uh between every point and the orange point we find the smallest distance and uh that that will be the uh best result for the query and in the multi-dimensional uh space where we have where the coordinate is represented by uh a long list a long list of coordinates not just x and y. We can basically use this same formula but instead of two uh two members we we will have many members depending on the dimension of the vector. You you don't need to implement this formula yourself. All the libraries do this for us. It's just uh very very easy

**[12:17](https://www.youtube.com/watch?v=-Echq2zAafo&t=737s)** to understand what's going behind the scenes. So I I wanted to show it. So uh so yeah similarity matrix basically it's a function that takes two vectors as an input and calculates the distance between them. We use this calculated distance to judge how close or how far the two vectors are are relative to to each other. So we use this to evaluate the vector embeddings that we have in the database. uh in relevant to the web embedding of the quality. There are different similarity metrics uh they vased on speed and accuracy and there is no one size fits all similarity

**[13:08](https://www.youtube.com/watch?v=-Echq2zAafo&t=788s)** metrics and the best strategy is to use the similarity metric that the model is that that matches the model that we use to generate the vector embeddings. So these are the most common similarity metrics. Equidian distance is the one that we saw on one of the previous slides. There is also cosine distance and dot product. Uh so depending on the situation you may use one similarity matrix or or the other. So how how do we generate the uh web embeddings for the data uh in our database?

**[13:56](https://www.youtube.com/watch?v=-Echq2zAafo&t=836s)** One option is to use a third party API like open AI. Open AI has a uh API and an endpoint for generating vector vector embeddings. You give it some text. it will return back uh a an array of numbers uh representing the vector impending of the inputs. You can uh basically run the same on Azure open AI. There is also hugging face inference API. All these uh all these options are paid and uh they are very cheap but still they are paid and also if you use this this means that your data has to leave your system. It goes on third party server. So you may you may not be comfortable with that. So another option is to run the model locally. You

**[14:45](https://www.youtube.com/watch?v=-Echq2zAafo&t=885s)** download the model. You can run it on your server on your computer. It's uh depending which which model. Some require more resources. Some of them are smaller and can run on a laptop. So uh for for development for example you can uh use Lama. You can download which I will show in a demo. download the model for Ola and uh have the endpoints running on your computer and then use Olar library to to access the Olama API that is exposed by by Ola. Another option is to run the model models locally with semantic kernel. It can run ONX models. So ONX is an open open format for models. So if you

**[15:36](https://www.youtube.com/watch?v=-Echq2zAafo&t=936s)** download the model in Ox format, you can run it with semantic kernel locally on your machine. And finally, if you if you are brave, you can also uh use a library from another language. The uh the good thing is that uh it doesn't matter which programming language you use to generate the vector embeddings because as long as you use the same model and same input the uh model will return the same vector embedding whether you use it from C#, Python, Java or any other language. Uh so all these options that I mentioned either the third party APIs or running them locally they all have specific API uh depending which one you use. So the

**[16:26](https://www.youtube.com/watch?v=-Echq2zAafo&t=986s)** the the API basically differs from one option to another option. So Microsoft released an uh a library called Microsoft extensions AI which uh kind of unifies the uh the uh all all the endpoints for generating the embeddings and and also many other things but in in this session I I will talk about embeddings. It it gives us a unified interface and there are different implementations uh for uh for all the options that I mentioned. For example, there's an implementation for OpenAI, there's an open implementation for Olama and so on. So as long as you uh write your code against this I embedded generator interface, you can then uh switch the

**[17:16](https://www.youtube.com/watch?v=-Echq2zAafo&t=1036s)** actual implementations from one to another and your code that consumes this uh interface will not have to change because uh you are using the you are programming against this interface and not against a concrete implementation. So in the demo we will also see uh the usage of this library. So once uh once you generate the embeddings you need to uh store the them somewhere. So here here is where the uh vector databases come in. Basically uh we have three three major uh options. One of them is to use a dedicated vector database like quadrant chroma or so on. So these are the most well-known vector

**[18:08](https://www.youtube.com/watch?v=-Echq2zAafo&t=1088s)** databases. There are many of them but these are these are the most wellknown. So vector databases are designed only for uh working with vector embeddings for indexing them uh and retriving the relevant vector embeddings when you issue a search against against the embeddings that you have in the vector database. Another option is to use just relational database. Uh so vector support will be coming in SQL server uh 2025. You can uh try the uh release candidate if you want. Uh that's what I will be using in the demos today. It's also supported in Postgress with a with an extension with the PG vector extension and also it's available in orox if you

**[18:57](https://www.youtube.com/watch?v=-Echq2zAafo&t=1137s)** want to use it in version 23. And the third option is to use the uh traditional NoSQL databases like MongoDB radius elastic search and so on. They also support uh you storing the vector type and uh running the search in the database uh so let's uh so uh so in this session I will I will be using SQL server 2025 uh it introduces a dedicated uh type for uh for swinging vector embedding things. It's it can do exact nearest neighbor and approximate nearest neighbor search.

**[19:46](https://www.youtube.com/watch?v=-Echq2zAafo&t=1186s)** It supports all the uh similarity metrics that I mentioned. It also supports index indexing the vector embeddings and obviously we get all the other features of SQL server. So basically you you use vector data tab like like any other type and you get all the features of SQL server. So why why use SQL server for storing vectors instead of using a dedicated uh vector database? Well, first of all, uh the the less uh different uh uh software or the the less different databases you use, the less operational maintenance you'd need. You don't need to synchronize the data from your main

**[20:32](https://www.youtube.com/watch?v=-Echq2zAafo&t=1232s)** database to the vector database. uh you don't uh you don't need to care for the backups of the vector database and so on. So basically everything that you have already working for SQL server will also work for vector data for the vector data type. All uh drivers for SQL server from all the different languages are already compatible with the vector type. So if you use C# or any other language, you can use the existing SQL server library to uh to work with the wafer type. So uh so if you already use SQL server, you basically store your web with the rest

**[21:20](https://www.youtube.com/watch?v=-Echq2zAafo&t=1280s)** of your data. Uh so this is this is the uh syntax for using the uh vectors in SQL server. As you can see uh there is a dedicated type called vector. You you need to specify the dimension of the vector. In this example it's it's three. And uh once you create the table with the uh vector uh vector column you can pass the items with the uh with the JSON syntax. Uh you can see the insert statement that is inserting the vectors in the database. And then uh you can also use the uh vector distance function uh to find the

**[22:12](https://www.youtube.com/watch?v=-Echq2zAafo&t=1332s)** vectors in the database that are close uh to to the uh target uh that you are searching for. So the uh vector type stores the vector embeddings in an optimized binary format. uh with older uh clients if they don't uh if they have not been updated they work with vectors uh like a JSON array but the newer versions of SQL servant of the binary format and uh reduce the payload size and uh be be more performance. So let's uh let's see uh couple of demos how how you can uh do uh semantic search with uh

**[23:06](https://www.youtube.com/watch?v=-Echq2zAafo&t=1386s)** we see uh vector vector type in SQL server. So I I have some uh uh data some 50,000 rows. Uh it's uh news items from the last uh one or two decades. It's in JSON format that I have already imported in the database. Here is the code that uh uh handles uh generating the embeddings. For example, uh if we use Olama to generate the embeddings, you can see that I just create an uh create a client. Ola is running locally on my machine. So I can uh connect to it.

**[23:55](https://www.youtube.com/watch?v=-Echq2zAafo&t=1435s)** If I want to use Open AI for generating the embeddings, I just create a client for uh from the library that Open AAI has for C#. And once I have the generator I either the Olama uh client or open eye client I I pass the generator to to this import embeddings function. And as you can see it's it's using this interface from the uh Microsoft extensions AI library that I used. So this this function does not care uh which API you are using uh to generate the impedings. it just needs an uh implementation of this interface. So uh it this this uh this method will read the uh items from the JSON file and

**[24:44](https://www.youtube.com/watch?v=-Echq2zAafo&t=1484s)** it just calls the uh generate async method to generate the embeddings. It will the it will pass an uh innumerable of strings in in this case headlines and it will get back the uh vectors and store them in the uh in in SQL server database. So we can also uh we can also see the uh database here. So the embedding column the the last column it's vector 384

**[25:33](https://www.youtube.com/watch?v=-Echq2zAafo&t=1533s)** because this is the dimension of the model that I used to generate the embeddings. So uh the the model that I use to generate the embeddings generates the embeddings with the dimension of 384 and I can also show you some some data. So here here are the uh headlines and here are the embeddings. So a large array of numbers with dimension 384. So once once we have this uh

**[26:28](https://www.youtube.com/watch?v=-Echq2zAafo&t=1588s)** data in the in the database we can do actual search and I I have uh different uh implementations of the search. The first implementation uh pulls all the all the data all the 50,000 rows in memory and finds the nearest vectors by calculating the cosine similarity here and taking the top 10 matches. So it will uh so once when you when the user enters a uh query term what they are searching for we will again generate the vector embeddings from from from uh from the text that the user entered and pass it to the uh search in memory method. And here we uh we calculate the distance

**[27:21](https://www.youtube.com/watch?v=-Echq2zAafo&t=1641s)** from from the uh from the query term to the 50,000 items that that I have already loaded in memory. I will order by similarity in a descending way and take the top 10 best matches. So let's let's uh let's run this and see it in action. It it takes about a minute to load all the uh all the records from the database. So uh so as I said it's a the data that

**[28:13](https://www.youtube.com/watch?v=-Echq2zAafo&t=1693s)** I imported is the uh is news uh news items from the last 10 15 years. So I will enter uh I will search for plane that disappeared. So it will now generate the embedding from this term that I entered. And we can see that the uh top 10 best matches are uh the Malaysia Airlines plane plane that vanished. You can see it here. So even though it does not say disappeared, it's still it's a match for what I was searching for because it matches on the meaning. You can also So the second one is uh jet that disappears. So it doesn't have an airplane. It doesn't have plane, but it has jet. So all all the I think mo most of the matches are matching uh

**[29:04](https://www.youtube.com/watch?v=-Echq2zAafo&t=1744s)** the search terms that I that I entered. Now the disadvantage of this method is that I had to load all the records in the database and maybe for 50,000 items it's more or less okay. It it fits in RAM but if you have millions of records in the database then you you don't want to load all those all all those records in the RAM because you will need lots of memory and you can't can't fit. so much memory in the in the so much memory. Yeah, it's better to give the vector embedding of the query term to the database and have the database handle you back the uh top 10 messages or top 20 or whatever number you are uh searching for. So uh for that uh for that we the the demo will use SQL

**[29:58](https://www.youtube.com/watch?v=-Echq2zAafo&t=1798s)** server. But first uh let's let's uh let's uh yeah let's see how how we can use this uh vector data type in C. So as I said, SQL server stores vectors in optimized binary format but exposes it as a JSON array for older clients. If you are using a Microsoft data SQL client version 6 I think it should be 11 instead of 1.1. Uh it adds support for the dedicated support for the wer type. So it will use the binary format when decoding and encoding the uh vector

**[30:46](https://www.youtube.com/watch?v=-Echq2zAafo&t=1846s)** impeding. So it will have a lot less uh overhead compared to the uh JSON text. So if you are using plain ADO.NET head you there is a new uh type for the SQL parameter and uh basically you can use uh this uh SQL web type to insert these uh interesting numbers to the database if you are using entity framework core in in that case uh EF Core 9 uses the JSON compatibility feature that I mentioned and it requires this uh library from Nougat. This is this is an official library from Microsoft. It it just adds support for vector S to EF

**[31:37](https://www.youtube.com/watch?v=-Echq2zAafo&t=1897s)** core. Uh starting with EF Core 10, support for vectors will be uh will be added to to EF core 10. I think it's already available in the RC1 that was released a couple of days ago. So there is no need for extra dependencies if you are using EF Core 10. So uh so in in the demo uh you will see that basically all you need is uh that in the unconfiguring method in uh in your database context you need to specify that you are using a vector search and then you can use uh the uh function vector distance to calculate the distance between the query term and the data that you have in the database.

**[32:26](https://www.youtube.com/watch?v=-Echq2zAafo&t=1946s)** and take the top 10 results and returns that back to the uh to the user. So let's uh let's see this. So here is the uh here is the uh second way for uh for finding the uh results but in database. Again this is uh I I pass the vector embedding of the uh search term uh and and the implementation will uh we'll just uh it will order by the 50,000 items

**[33:19](https://www.youtube.com/watch?v=-Echq2zAafo&t=1999s)** that I have in the database based on the vector distance in this case cosine distance. So it will calculate the co cosine distance from the equator term to the uh to the data in the database ordered by the distance and takes the uh take the top 10 items. So the the the less the distance the closer the item the database is to the vector embedding that I uh that I generated from the search terms. So let's uh let's run it again. So now it will run uh it two times. One in memory, the second time in the

**[34:09](https://www.youtube.com/watch?v=-Echq2zAafo&t=2049s)** database. I will use the uh same term uh for searching. So we can we can see uh we can see that in the database this these are the same results but the difference is the search happened in the database. So the database returned back the 10 matches B that that match that are near near to the uh to the search term and uh we can see we can see the actual uh SQL statement that was executed by the database. If we do this uh once again I will uh I will type this same

**[35:00](https://www.youtube.com/watch?v=-Echq2zAafo&t=2100s)** term and here uh we have this uh I I queriable Oh. So this is the uh SQL statement that was generated by edit framework 9 and was that was executed that was executed on the database. So we can we can see that it's uh selecting the uh vector distance and also the ordering is done based on the vector distance between the embedding column and between the uh parameters that we passed uh to this query.

**[35:50](https://www.youtube.com/watch?v=-Echq2zAafo&t=2150s)** So we can see uh the exact same uh query here. So this this variable here this is the uh vector repres representation of plane that disappear that I type. So these these are the uh numerical values generated by the uh llm model for for that phrase. And here I'm just uh ordering this uh the order by is done uh based on the vector distance between the embedding column and this uh this vector variable that is declared here. So if I run this uh if I run this select statement you will see the exact same uh 10 results that you that we saw

**[36:42](https://www.youtube.com/watch?v=-Echq2zAafo&t=2202s)** in the demo right now. So this is the distance. The less the distance the more relevant it is to the uh query query. So this is the same uh SQL generated by EF core. Okay. So uh so we saw how to generate embeddings, how to store them in SQL

**[37:33](https://www.youtube.com/watch?v=-Echq2zAafo&t=2253s)** server, how to find the nearest embeddings compared to the uh relative to the search term and the uh next thing is uh vector indexing. So indexing indexes in the databases are important as you probably know because uh indexes help us to speed up the operations in the database. Without indexing without the indexes the uh queries can become slow as the number of rows grows. The difference between vector indexes and the uh so-called traditional indexes is that when you are doing a where statement or join or some other SQL statement when you add an index it does

**[38:23](https://www.youtube.com/watch?v=-Echq2zAafo&t=2303s)** not affect the search results. You get the same results but faster. So it it it does not change the results that you get back from the database. It only speeds up hopefully the select selects. But in case of vectors the index works in a different way. Basically all the all the operations that we saw before in in the demos that all all the searches were exact nearest neighbors. It means that when when we asked the SQL server to return the nearest neighbors, it was it needed to calculate the distance between the query term and every 50,000 records in the database. This means that as the number of rows in the database increases, the

**[39:12](https://www.youtube.com/watch?v=-Echq2zAafo&t=2352s)** number of calculations is also increasing linearly because if you have thousand items, it needs to calculate it needs to calculate thousand distances. If you have 1 million, it needs to calculate the 1 million distances. And this becomes slower and slower as the number of rows is increasing and it's very computationally heavy. Basically, it's it's there's a linear represent linear relationship between the number of rows in the database and number number of computations. So another approach is to use approximate nearest neighbor which trades off a bit of accur accuracy for a huge increase in performance. So it will uh so the ANN which stands for approximate nearest neighbor it will uh

**[40:03](https://www.youtube.com/watch?v=-Echq2zAafo&t=2403s)** do the calculations uh based on the index only on the subset of data and reduce the uh computational burden but the return the results will be approximate. they may not be the exact nearest neighbors but will be uh will be approximated uh results. So when you create an index uh on a vector column in the database does not matter whether it's SQL server or any other vector store the results that you get back will be approximated. That's that's how that's how vector indexes work. So this is uh to illustrate this is to illustrate the number of operations in uh exert nearest neighbors. It's

**[40:54](https://www.youtube.com/watch?v=-Echq2zAafo&t=2454s)** linear and it's logarithmic when it's approximate nearest neighbor. So approximate nearest neighbor will find similar vectors without searching all of them. It will offer significant uh computational improvement. It will be a lot faster. It will be it will use a lot less resources but the results will be approximate like not 100% exact but still very good. So how how do we evaluate the uh approximate nearest neighbors? How how do we evaluate whether the results returned by the ANN search is good or whether it's not so good?

**[41:45](https://www.youtube.com/watch?v=-Echq2zAafo&t=2505s)** Uh the the uh see we can evaluate it based on so-called recall. So recall is a uh is a measure that tells us how many uh nearest neighbors returned by the A&N search are the true nearest neighbors. So let's uh let's say that all these uh uh black crosses are the uh d are the embeddings in our database. The uh red cross is the query term that we that we are uh searching. the ones in the uh light gray uh I think uh there is no you can't see the difference there because of the screen but uh the five in this

**[42:35](https://www.youtube.com/watch?v=-Echq2zAafo&t=2555s)** circle around the red cross they are the uh true nearest neighbors. So if you use exact nearest neighbors, you would get these five uh uh these five results that are uh around the around the red one. And let's say that if you use approximate nearest neighbor, you get back the five from the uh orange or yellow or the yellow section. In this case, we can see that the uh intersection between these two results is the four black items which are in the gray gray area. So this means that from the five nearest neighbors which are the true nearest neighbors, the approximate nearest neighbor search gave us the four four ones because the

**[43:25](https://www.youtube.com/watch?v=-Echq2zAafo&t=2605s)** four is the intersection. Yeah. So in this case we can say that the recall is four out of five. So it means their call is 80% because four is 80% of five. Yeah. So uh ANN will uh cost a little bit in uh results. So the uh the accuracy will be a little bit lower but it will be much much faster and uh still usable in real life scenarios. For uh vector indexes there are different algorithms or different strategies. Uh there is inverted style with flat compression. There is HNSW and there is a disk AN which is

**[44:12](https://www.youtube.com/watch?v=-Echq2zAafo&t=2652s)** available in SQL server 2025 and in other products of Microsoft. this uh this was developed by micro by Microsoft research. It's high recall. So the the recall that I explained in the previous slide. So this KN&N has high recall like 95 98. So basically almost the true nearest neighbors. Uh it uh it performs better than than the other indexing algorithms that I mentioned. It's open source. So you can uh you can see the implementation on GitHub if you want to and the vector search uh function that is uh available also in uh 2025 in SQL server. If you use this uh

**[45:05](https://www.youtube.com/watch?v=-Echq2zAafo&t=2705s)** table valued function uh vector search it will use the index as long as the index in the database obviously. So to create the vector index you just need to run this statement. Uh instead of create index we use create vector index and we uh specify the column which contains the vector impedings and also what type of metric we want to use in this case cosine similarity and the index type the only supported value is this knn. So I have already created the index in the database. It it takes some time. So uh I have created it already in the database. So now we can uh we can see the uh we

**[46:00](https://www.youtube.com/watch?v=-Echq2zAafo&t=2760s)** can see the third version of the search. And because it's a uh it's a table valued function, I had to use uh the uh SQL query instead of writing a link query uh I I I had to use the uh SQL query method and pass the uh SQL statement directly. So the SQL statement we can also see it in management studio. So in in the previous example we were doing order by vector distance but now we are calling the vector search function. We pass the table we pass the column and the uh query vector and the matrix that we want to use. So if I

**[46:49](https://www.youtube.com/watch?v=-Echq2zAafo&t=2809s)** if I if I run this now, it will run approximate nearest neighbor search in the database and return the top 10 results. Now because the result the data size in the database is very small, it gives exactly the same 10 results but but now it it will it uses the uh index. So we can we can see that the actual index is used. So here we can see that it used the vector index this k&n uh uh that we have in the database. So with this uh with this approach we can run the same query with uh entity framework core. And if we uh we can also

**[47:41](https://www.youtube.com/watch?v=-Echq2zAafo&t=2861s)** compare the times. So we can see that uh the in-memory search took about 100 milliseconds. The database took about 300 without an index and with the index it takes a bit more than 130 a bit more than 100. If if I run it again it will be uh it

**[48:32](https://www.youtube.com/watch?v=-Echq2zAafo&t=2912s)** will be faster. So now uh yeah now the uh the one using the index in the database is the fastest and it's uh like less than 50 milliseconds. So it's it's a lot faster than uh than the one without index. And there is uh one other demo that I want to show. So currently all the all the demos that you uh that we saw all the demos we are using uh this model. I will now switch to another model.

**[49:38](https://www.youtube.com/watch?v=-Echq2zAafo&t=2978s)** So I have I have a second database here. It's storing the embeddings generated from this other model. Uh this model generates embeddings uh with dimension 1,24. You can see it here. So the difference between these two models is that uh this model this snowflake model uh is multilingual model. So this means that uh the previous model supported only English. This model is supporting several languages apart from English. So uh what's uh so why why would we uh why would we uh

**[50:29](https://www.youtube.com/watch?v=-Echq2zAafo&t=3029s)** use a model uh that supports uh different languages? Uh that's so so uh so yeah because this language this model supports different languages it means that uh the embeddings that are generated for example from German text uh is closer uh to the uh embeddings generated from the uh from other terms that are closer in semantic model for example in English. This means that we can enter the uh search term in German and it will find results in English records that I have in the database that more or less match

**[51:18](https://www.youtube.com/watch?v=-Echq2zAafo&t=3078s)** the meaning of the German text. So it will not it's not as good as English uh searching for an English phrase in the uh English database but it's still uh uh I had the password somewhere. I forgot that I needed this. [Music]

**[52:16](https://www.youtube.com/watch?v=-Echq2zAafo&t=3136s)** I I it was somewhere in I have it in my phone but not in here. I will create a hot spot. Oh, it's Yeah, it's connected. So, if I if I now uh translate to German and I enter it here.

**[53:17](https://www.youtube.com/watch?v=-Echq2zAafo&t=3197s)** It will now generate vector embeddings from this uh phrase, this German phrase. The first time because I switched the model, it now leads to all I manag to load the memory, the model in memory. So we still have this missing Malaysian Airlines flight as number two uh and here also. So this is these are not the uh same results that we had when searching English text in English database but uh still more or less uh relevant uh to the German phrase that I entered uh from from the uh data in English in the database. So yes, that's that's all that I wanted

**[54:12](https://www.youtube.com/watch?v=-Echq2zAafo&t=3252s)** to talk about. The uh uh demo is available on GitHub. This is the QR code uh for the uh GitHub repository. And I I hope you found this interesting and you can check the demo in my other projects on GitHub. If you have any questions, I can try to answer them. If not.

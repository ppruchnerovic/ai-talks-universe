---
id: -yTEFW2ympk
title: "Trust No Input: Taint Analysis at Compile Time - Matteo Di Pirro - NDC Manchester 2025"
slug: trust-no-input-taint-analysis-at-compile-time-matteo-di
conference: ndc
conference_name: "NDC Conferences"
category: "General software conferences"
edition: "NDC"
year: 2026
speakers: ["Matteo Di Pirro"]
channel: "NDC Conferences"
duration_min: 52
published_at: 2026-02-04T10:09:20Z
video_id: -yTEFW2ympk
url: https://www.youtube.com/watch?v=-yTEFW2ympk
youtube_url: https://www.youtube.com/watch?v=-yTEFW2ympk
tags: ["Application Security", "GenAI", "Security", "Security Tooling", "Programming", "NDC", "Conferences", "2025", "Live", "Fun", "Manchester", "England", "UK", "United Kingdom", "Matteo Di Pirro"]
topics: ["Security, safety & red teaming"]
transcript: true
---

# Trust No Input: Taint Analysis at Compile Time - Matteo Di Pirro - NDC Manchester 2025

**Matteo Di Pirro**

`NDC Conferences` · `NDC` · `2026` · `52 min`

`#Application Security` `#GenAI` `#Security` `#Security Tooling` `#Programming` `#NDC` `#Conferences` `#2025` `#Live` `#Fun` `#Manchester` `#England` `#UK` `#United Kingdom` `#Matteo Di Pirro`

[Watch the recording](https://www.youtube.com/watch?v=-yTEFW2ympk) · [Conference site](https://ndcconferences.com/)

## Description

This talk was recorded at NDC Manchester in Manchester, England.

Attend the next NDC conference near you:

/            @NDC

Follow our Social Media!

As software systems become increasingly interconnected, protecting data confidentiality and integrity is more critical than ever. When traditional access control mechanisms fall short, we must turn to deeper, language-level solutions.

This talk introduces language-based security: leveraging programming languages and their type systems to enforce security policies at compile time. We’ll focus on taint analysis, a technique for tracking the flow of potentially harmful (or "tainted") data. In particular, we'll see how we can apply it statically to detect and prevent security vulnerabilities before they reach Production. Through practical examples in Java and Scala, we'll see how to model data sensitivity, propagate taint status and catch violations at compile time.

As more code is written or suggested by GenAI, the risk of subtle security flaws increases, making compiler-enforced security guarantees more valuable than ever.

By the end of the talk, you will see how language-based techniques can reduce reliance on dynamic checks and support building secure systems by construction.

## Transcript

*7,625 words · source: supa (en, exact timings)*

**[0:03](https://www.youtube.com/watch?v=-yTEFW2ympk&t=3s)** Okay, so it looks like we're go. Uh, thank you. Thank you everybody for being here today. Uh, hope you're enjoying the conference as much as I am. And also thanks for Tendc for having me today. So my name is Mateo. Today we're going to talk about code analysis, secure coding and in particular taint analysis. We are going to see how type systems and programming languages can be used to encode security properties and enforce them at compile time to make our code more secure by design. So I am a software engineer mainly uh I've worked on many different applications in my career from uh embedded applications to distributed ones. I'm mainly a Scala developer so we'll see some Scala code

**[0:52](https://www.youtube.com/watch?v=-yTEFW2ympk&t=52s)** today. Now I'm afraid to ask this. How many of you know Scala? Okay. One, two, three. Okay. So, we'll see some Scala code. Um, we'll try not to get lost in type systems. I'll try to give you the general idea of what we are what we want to do without, you know, diving deep into into types. Um, what's important is that my academic path focused on type systems and on programming languages. So today I'm here to share with you the beauty and the power of type systems, showing how they can be used to encode and enforce security properties to have the compiler check for security violations, producing code that is secure by design and construction rather than by correction. And this is because data vulnerabilities

**[1:41](https://www.youtube.com/watch?v=-yTEFW2ympk&t=101s)** are expensive. According to IBM's um cost of a data bridge report from this year, the average cost of a data bridge was more than $4 million. Now this includes different data breaches, more one big ones. Uh it includes different types of costs. So it could be cost for looking for the vulnerability, for fixing the vulnerability, reputational damage, also cost for fees that you have to pay if you if you lose some data. But the point here is that data breaches are expensive. And they are expensive because the underlying data vulnerabilities are very difficult to find and to fix because our applications nowadays are so full of input. We we are all about data. We fetch data from a

**[2:31](https://www.youtube.com/watch?v=-yTEFW2ympk&t=151s)** huge variety of different sources. Could be users, could be your database, it could be your cloud provider, it could be LLMs nowadays. So you don't have the control anymore on the data you are you're entering in your application and the worst thing is that depending on the technology and on the language you use finding and fixing vulnerabilities might be even more difficult. So imagine if you are a C developer with a C-based application and you have a single input in your application but then you have 10 different pointers reading the memory location where the input is stored manipulating the input reading it writing it and so on. So if you are to analyze all the possible data flows you have to go through all the flows generated by 10 different pointers and

**[3:18](https://www.youtube.com/watch?v=-yTEFW2ympk&t=198s)** the number explodes if you increment the pointers or if you increment the inputs. so that they are difficult to to find. Also nowadays our applications are structured into components and into layers and each different component has a very well-defined responsibility. So if you are developing for example a web- based application, you have maybe a controller reading the input from the request coming from the user and then you have a service layer somewhere deep in your application interacting with a DB. And these two components might be developed by different teams, different companies if you employ constructors um contractors. And you might not know what happens between the controller layer and the service layer. There could be input

**[4:06](https://www.youtube.com/watch?v=-yTEFW2ympk&t=246s)** validation, there could be input sanitization, there could be input manipulation of some kind. You don't know what happens. And so analyzing the data flows even in in simple applications is very complex. Now over the years security researchers and practitioners have developed many different uh tools and analysis to track data flows. One of them is taint analysis. Now I'm going to give you an extremely general and high level definition of taint analysis and that is on purpose. So taint analysis in in this in this presentation is just a technique to track data flows of interest within your application to make sure that they are properly handled. Now if you don't know what t analysis is and if you read

**[4:55](https://www.youtube.com/watch?v=-yTEFW2ympk&t=295s)** this definition you probably still don't know what analysis is. It conveys just too little information. But that again is on purpose because we have so many different applications nowadays that is difficult to determine in many cases what is a data flow of interest and therefore it is difficult to determine how to properly handle those data flows. I'm going to give you two examples. So the first one is information disclosure or data leaks. Now in this case what you have is you read the API key for for example from a secure location. It could be your DB, it could be your cloud provider, your file system and then you use this um API key to build an API client. Now this is a

**[5:45](https://www.youtube.com/watch?v=-yTEFW2ympk&t=345s)** data flow already. We are reading something from an external source that we trust and we're using it to build a client to a third party service. Now this might be not a data flow of interest because we might trust the client. We might trust the service. We might not be interested in this print for example to analyze that data flow. So let's just not look at it for a moment. But then you have a very simple log message saying, "Hey, congratulations. You just created an API client with this API key." Now, this is a security violation because even if your logs are not public, the people allowed to read your logs might be different than the people allowed to

**[6:32](https://www.youtube.com/watch?v=-yTEFW2ympk&t=392s)** read your secrets. It could be other teams in your company or it could be other companies as well. So, you are disclosing information here. This is certainly a data flow of interest from a security standpoint. So how can we make sure that it is indeed properly handled? Well, we have different possibilities. One is to just forbid the data flow entirely. So you say, well, this is a vulnerability. Please remove the log message and that's it. Or you could mask the API key somehow just printing a hash function for example of it or printing just the first characters to make sure

**[7:20](https://www.youtube.com/watch?v=-yTEFW2ympk&t=440s)** you're not disclosing the full secret. Okay. So you can see three lines of code. You have two data flows. One of them we're not interested in. The other has two possible ways to handle it. So it is complex to define how to treat these data flows. Now the second example is the complete opposite. Here you have an application that reads input from users. Now imagine this is a file management application. So what you're trying to do is you read a file path from the user and you delete the file pointed to by that file path. But now again imagine you are developing a multi-user um system application and so a malicious user could enter a relative path pointing to the file of another

**[8:10](https://www.youtube.com/watch?v=-yTEFW2ympk&t=490s)** user or an absolute path pointing again to the file of another user. So what do you do here? You cannot just forbid the data flow because this is a valid use case of your application. The application is supposed to delete the file. What you can do though is validate or sanitize this input to make sure it doesn't contain any relative path or absolute path pointing to other users files. Now as you can see again here we have a different data flow a different way to handle it. So it's still again difficult to define properly what t analysis is in gener generic applications. We can identify three main components though. We have input sources that are where you fetch the input from. You have syncs which is where you use

**[8:59](https://www.youtube.com/watch?v=-yTEFW2ympk&t=539s)** those inputs. And then you have sanitizers or validators. And these components are responsible to enforce security properties on your inputs to make sure that they comply with again security properties and they're safe to use in your syncs. So in more general terms, taint analysis aims to track all the data flows from sources to syncs without proper sanitizers or validators in between. Okay, this is it. This is how it works generally. Now if you are into security or code analysis, you will know that for everything you have two possibilities. You either statically analyze your code which means you run something on your code base and you don't run the code itself or you use

**[9:48](https://www.youtube.com/watch?v=-yTEFW2ympk&t=588s)** dynamic analysis which means that you might not even have the code available. You just run some analysis on the running application. Again with taint analysis this is we have these two possibilities and we'll see in a minute that we also have another possibility which is more based on programming languages. Now the main benefit of a static taint analysis is that it covers all your codebase. You can run the analysis on the whole codebase you have. It will look for vulnerable data flows. It will look for vulnerabilities and it will report those vulnerabilities uh without running your your code. However, depending on many factors, depending on your programming language, on the size of your codebase, um the analysis might take a long time to

**[10:37](https://www.youtube.com/watch?v=-yTEFW2ympk&t=637s)** complete. There are some research uh results showing that static analysis can also take hours and extremely um resource intensive. The main problem though with static analysis in general, it is that it's undecidable. And this means you cannot rule out at the same time false positives and false negatives. Now false positives here are data flows marked as vulnerable which are not a vulnerability by by by themselves. Now this requires programmers developers security experts to go into the code looking for the vulnerability and this might be a lot of work again because the sec the data flow might be complex. False negatives are worse are data flows not marked as vulnerable but that

**[11:26](https://www.youtube.com/watch?v=-yTEFW2ympk&t=686s)** include that hide a security vulnerability and what can happen is that your users find the vulnerability and if they are malicious they can exploit it without you uh knowing that what is going on in the application. So this is static analysis and generally what you do to mitigate the number of false positives or false negatives is you make assumptions on your code and depending on the assumptions again the analysis might take even longer to complete if it's a very deep analysis. So generally speaking what we do normally is we turn to dynamic analysis. In dynamic analysis, we might not even have the code because we can uh just analyze an application by observing its behavior while it's running. It could be in your

**[12:14](https://www.youtube.com/watch?v=-yTEFW2ympk&t=734s)** testing environment. It could be on production. Um it could be on real with with real users and dynamic analysis can flag vulnerabilities in real time. This is what happens. The problem is then in this case for example to look for vulnerabilities we have to trigger specific data paths in your application. Now depending on the input that you enter in your application, you might or might not explore all the data paths. So what you do generally is you combine dynamic analysis with fuzzing for example. So you randomly generate inputs to explore you know as much as possible of your of your application. But then again the more inputs you generate the more input you feed your application with the longer it will take in general to analyze the application. So we'd like

**[13:03](https://www.youtube.com/watch?v=-yTEFW2ympk&t=783s)** to look for a tradeoff of these two different analysis and the trade-off in some cases is languagebased security. So with languagebased security the idea is that we don't need a third party tool analyzing either our code or our application. What we do is we use the features of our programming languages to encode security properties and enforce them either at runtime which is closer to dynamic analysis or at compile time which is closer to static analysis. We'll see in this presentation uh a form of language based security closer to static analysis because we'll try to use type systems to encode vulnerable data flows and have the compiler block the

**[13:54](https://www.youtube.com/watch?v=-yTEFW2ympk&t=834s)** compilation whenever a vulnerable data flow is uh is found in the code itself. In the end of the presentation, we'll see also some um implementations of paint analysis in programming languages that are closer to dynamic analysis. So the property that we'll try to enforce with our little framework is that if the code compiles now we are we'll be using Scala. So it means that if the code is well typed then we have no data flows using unsanitized values. So values we do not trust in security sensitive context. Now this already identifies the data flows we are interested in. We are interested in those data flows flowing

**[14:44](https://www.youtube.com/watch?v=-yTEFW2ympk&t=884s)** to things that present could present security vulnerabilities. And it also depends of course on your on your application how we make sure they are properly handled. We try to sanitize the values. So we try to forbid any flows without a proper sanitizer or validator depending on the logic of your of your of your application. Now we are software developers and engineers. So we like to start with some requirements. We'll model freemaintained levels. We'll model tainted values. These are values fetched from external sources we might

**[15:33](https://www.youtube.com/watch?v=-yTEFW2ympk&t=933s)** not trust. So we'll forbid tainted values in security sensitive context. We'll have pure values. Pure values are inputs coming from sources we do trust. Could be UDB for example. You could decide well you I I do trust my DB and I do trust all the values that come from that source. And then we have sanitized values. Those are values you did not trust before but then you run a validator or a a sanitizer and then you trust the the input. Now the difference between validator and sanitizer is that normally validation doesn't uh modify the input and then sanitization does modify the input but you know in general there you can like talk of validation and sanitization they're almost

**[16:22](https://www.youtube.com/watch?v=-yTEFW2ympk&t=982s)** interchangeable terms uh at least they are for this for this presentation. Now we want to make sure that tainted values are safe to use only after being sanitized. And we also want to make sure that whenever we combine these labeled inputs, we preserve the taint level. So this means for example that if we combine two labeled values, one is tainted and one is pure, then the result will be tainted as well because we cannot trust the result of a computation involving a value that we do not trust. Okay. So we'll see some Scala code now. Then we'll see a short demo um with a web- based application that presents an HTML injection vulnerability. So we'll exploit the vulnerability first and then

**[17:09](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1029s)** we'll see how we can secure the data flow using the types that uh we'll see we'll see now. Now the first thing we do is we model the taint levels. Now taint levels you can imagine them as labels that we attach somehow to our to our values. Now in Scala we um model them with enomes. Uh these are more powerful than enomes in other programming languages. You can imagine this as an interface taint level extended implemented by three different classes. One is is tainted, one is pure and one is sanitized. So it's just a hierarchy of possible labels that we will in the future somehow attach to our to our inputs. Now the second uh thing is the propagation. Now without looking at the

**[18:00](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1080s)** type signatures here imagine this as this is a type definition in Scala. Imagine it as a directive for the compiler. We are telling the compiler whenever we use this type in the code you have to go through these rules matching two different taint levels. So for example if we use again tainted and pure together the result will be tainted. Imagine it as a lookup table where the compiler goes whenever it has to type an expression. It goes in the at compile time P 0 and P1 will be actual types. So the compiler knows the types it's working on. It goes through all the rules from top to bottom and whenever a rule matches for example in the first

**[18:49](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1129s)** case the first rule match because tainted is the first type it will return a type. So it's a lookup table the compiler reads at compile time to infer the type of an expression. Okay, this is helpful when we combine values because we want to propagate the type of the taint without having developers explicitly write the types. Right? These are compile time directives to help developers write less code and avoid one of the common pitfalls with type systems which is you know being too much ver too much verbosity in the code and so developers try to work around the type systems um themselves. Another example is with pure and sanitized. And in this case the compiler again goes through the first rule doesn't match the second the

**[19:38](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1178s)** third they don't match then it goes here it looks for sanitized and then it returns sanitized. Okay so this is again the propagation. Now this is u just a way to model some labels that we want to attach to our types. The way we do that, one way we can do that in Scala is by creating of course a new a new class, a new type. Now imagine tact as a box. Okay, so you have your input which has type A. You put the input into the box and then you wrap the box with a wrapper and that wrapper might be tinted, might be pure or it might be sanitized. Okay, so the box that you have conveys two information about the value you have

**[20:26](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1226s)** inside the box that is your input and about the wrapper that you have outside the box that is your trust level how much you trust the input that is inside of the box. Okay, so for the rest of the presentation think of like these things in term of terms of boxes. You have values inside and wrappers around. The first function we're going to look at is a flatm function. This is how we combine values using boxes. So when you have a box and you want to make an run an operation on the value inside, one way you can do this is you unwrap the box, you take the value out, you run a function on the value and the function returns a new box with a new wrapper.

**[21:15](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1275s)** Okay, so this is how you chain normally operations in Scala. these types of uh definitions. Uh and so this is what flatmap does. It basically takes the value out of the box. It runs the function. The function returns a new box, but that box might have a different wrapper around. So what you do in general is in the return type we use the tain propagation. So this means without the compiler it doesn't matter what f the function returns the type of the wrapper around the box will have to follow the rules that we specified before. This ensures that developers won't have to write uh much code and that all the the flow in your in your code will be will be safe because it will follow the team propagation rules

**[22:04](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1324s)** that we defined before. And then we have a couple of constructors to build these boxes. We have the default constructor which is apply which is opinionated and returns a tinted box. So it's a value. It's it's a box where we don't trust the the value. And then we have an unsafe constructor and it returns a pure box. Now you might say that this unsafe constructor is a way to work around the library and to bypass the security guarantees and that is actually true but looking for calls to unsafe is much simpler much easier than looking for you know vulnerable data flows. It is something that you can spot during a code review. You can spot using a tool that reads your code. It is much easier

**[22:52](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1372s)** than spotting a vulnerable data flow by itself. Okay, so we now have these boxes, values inside and the wrapper tells us how much we trust the value inside of the box. Then we can sanitize the boxes. Now what we do in Scala normally is we try to model errors as types. So the sanitization here introduces a new type which is either and you can imagine it as a fork. When you have an error in your code, from that moment on, you can have a left branch with an error. This is a type E and you can have the right branch with a valid value. So what we're saying here, we are saying that whenever we try to sanitize a value, the sanitization function might return two

**[23:40](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1420s)** alternatives. We either have an error in a sanitization in which case we completely lose track of the tainted box. If there is an error during validation or sanitization, what we want to do is we want to drop the box entirely because we cannot trust and we do not trust the value that's inside. Using it in your application will be would likely introduce a vulnerability and the other possibility is of course we have a valid value and the valid value will have now the sanitiz wrapper around. So this is the meaning of um the sanitize function here. will see an example afterwards. Uh it simply basically unwraps the value, runs the validation function and either returns an error or a new box. But the new box

**[24:28](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1468s)** will have now a new wrapper around telling us, okay, we can now trust the value that's inside of the box because it complies with some security properties. Now the last part of of of this library is probably the more complex if you're not into Scala types and is telling the compiler when it is safe to open boxes. Now all of this will happen at compile time. So we need a way to tell the compiler well you can open these boxes only if they have a certain wrapper around. And this is what we do using this canopen interface. Actually we are just giving the compiler two empty instances of this canopen interface

**[25:18](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1518s)** saying well whenever you want to open a box you can open it only if the wrapper is either pure or sanitized. Then we use it in our in our code. And this will make sure that any time a developer calls the open function, the compiler will look for an instance of the canopen interface in its context during compilation. It will find these instances only if the wrapper is sanitized or if the wrapper is pure. In those cases, the call to open will compile. If the wrapper is tainted, the call to open will not compile. So the compiler will stop a potentially dangerous data flow in the application, potentially spotting a vulnerability

**[26:06](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1566s)** because sanitization is missing or because we did not trust the value in the first place. Now after this this long uh explanation of of tab systems, we can look at some um at some code. Now I took one of the examples of the play framework. Play is similar to spring in Java or to symfony in PHP. It's just a way to uh define web- based applications. It will have a form uh asking the user to enter the name of a widget and its price. We'll see two versions of the application. One has an HTML injection. So we'll be able to inject some HTML code in the name of the widget and then we'll try to secure the

**[26:56](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1616s)** entire flow using the types that we just saw. So using these typed boxes to prevent the vulnerable data flow and to stop the compilation if we do not sanitize the input making sure it does not contain any HTML code. So I will just show the vulnerable um version before. So the UI is not is not great but this is what example gave us. So this is the name of the of the widget. We'll start first by entering a perfectly safe names widget 4 and a price. So there is some validation being done on the inputs by the application itself. So for example, if I enter a negative price, then I will have there you go um a validation error here

**[27:49](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1669s)** saying that the price must be greater or equal to zero. So I can just enter a valid price and it will create a widget and add it to the list. Simple enough. But what if I am a malicious user and I enter a widget name with some HTML code inside. So in this case what we are trying to do is we are trying to color the number four using the color red. Okay, simple HTML code but still code nonetheless. So I will just enter a valid price and then if I click create widget I manage to inject some HTML code into the DB. Actually this is an inmemory DB as you'll see but again there is no validation alternatization

**[28:39](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1719s)** stopping me from entering potentially dangerous HTML code inside. And this happens because in the code is it readable? Can you read it? Okay. In the code we have this is our DB now very simple with some widgets are coded and then we have our create widget function. This is taken as is from the example from the play framework and the vulnerable the vulnerability relies here. So here we are just taking the widget out of the request. We are building a widget object and we're adding it to our list without any validation. Now how can we secure

**[29:30](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1770s)** this code? Well, the first thing we can do is we can add some architecture and structure to the code. So we do add a widget repository. This is a common pattern in you know web- based applications. You still have your DB here and notice that the type of the widgets is still the same. So we are not trying to add type overhead into your business uh model. We are just trying to add this additional types in the interactions that you have with your vulnerable services. Now you have this list widget method. It returns again it calls unsafe and it returns a pure wrapper because in this case we do trust our DB. So we we

**[30:18](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1818s)** are just returning a box with the widgets inside and with a pure wrapper around. So this is safe to open. But then we have the add widget method and the wi the add widget method uh operates on boxes containing widgets. of course which is our entities here. Um it operate on boxes with an arbitrary taint label. Okay. So it they could they can have any wrappers around but we add an additional constraint and then we say it is safe to call add widget if and only if you can open you compiler can open the box. And so again we add this type constraint over here as a parameter of the function to tell the compiler when

**[31:07](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1867s)** it is safe to invoke this function. And then as a matter of fact we do invoke the open function here when we want to unwrap the value inside of the box and add it to our DB. Now the controller gets now more complicated. So you still have um you still fetch the value from the request. This is where you create a widget. We can't just send this widget to the repository because it won't type it won't type check because we have different types. So what we do is we create a tinted widget here. Now of course if we were to call unsafe here then we would bypass all the security checks. But then again, this would be kind of easier to spot during a code

**[31:56](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1916s)** review or using um a simple, you know, um linear or static analysis tool. Then we have this tainted widget. The type of the expression, I don't know if it's readable, um is our is our box with a with a given taint level. So if we now try to add this widget as is without any validation now you will see that the code will not compile because the compiler now scala type errors are a bit tricky to understand sometimes but in this case it tells us that there is no given instance of can open for our paint level tainted. So the compiler is telling us well you have this box but this box is not safe to unwrap because you did not run any sanitization or

**[32:44](https://www.youtube.com/watch?v=-yTEFW2ympk&t=1964s)** validation on the input. So um notice that again if we do call unsafe uh then the code perfectly compiles because the compiler is happy because it has a tool to unwrap the the the pure the pure box. >> [snorts] >> So what we do is we run some sanitization to our value in this case is more is more like validation. We are not modifying the input in any way. We are just saying okay well look for any characters in the name of the widget that are not letters, digits or blank spaces. So if the the name contains any special characters then return an error. This is this left here.

**[33:34](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2014s)** remember the branch you're branching off return an error and completely stop what you're doing because it's not safe to walk on the tainted value anymore. Whereas if you um don't find any special characters in the in the name then it's just fine to go on and have uh you know your your widget as is. Notice that the type now is sanitized. Okay. And sanitized involves if you remember a sanitized wrapper around the box. Now also notice that we are not specifying types anywhere here. Everything is inferred by the compiler without the need for developers to write explicit types. So the verbosity of the

**[34:21](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2061s)** code is know kind of reduced. It it feels more natural to write the code like this without having the boxes all around all around the code base. And then of course you have to match on the sanitize widget. So if it's an error that you got you will return an error to the user and if it's uh if the the value was was safe then you can just add it to your DB and this call will now compile perfectly because you know the widget is now sanitized. So if we go back to our different this is a different page with the same UI. If we now try to input again the code with um the name with the HTML code inside then we'll have

**[35:14](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2114s)** there we go an error because the sanitization failed because our function recognized that the name contained potentially dangerous characters. Now you can also adopt a different approach. You could modify the input in this case by escaping for example the HTML tags. This could be a possibility. Um you could just drop the special characters if you wanted. No, that depends on your requirements ultimately what you want to do with your input when they don't uh comply with some security properties. And then again if we now enter a perfectly valid name like this you will have there you go the widget added to your your DB. So this shows how you know this

**[36:04](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2164s)** library can somehow help you write code that is safer and more secure. Um and it doesn't have to be employed everywhere in your application. Here we just try to create a more secure layer around the repository to protect the data flows going to a potentially dangerous sync which is interacting with your DB. Now the main problem in general with language based security is that it strongly depends on your programming language. So different programming languages have different you know runtimes different type um compilers different type systems. So it's not possible to write a unique solution that works for every possible programming language. As a matter of fact if you try

**[36:54](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2214s)** to implement the same library in Java then there is actually no way to tell the compiler fail if there is no um if it's not safe to open the box. JavaScript system is not powerful enough to express this compile time constraints. So what we can do here the only thing we can do is we can rely on runtime type identification. We can look if the taint level implements the canop interface. Notice that here the taint level is um these are empty classes and only pure and sanitized implement can open. But the only thing we can do is we can throw an exception at runtime to block the execution of the application.

**[37:44](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2264s)** Here we could report a vulnerability, we could add logging, we could do stuff, but the point is this is done at runtime, not at compile time. So depending on the language, you have different possibilities to enforce and encode your security properties. Now this is not the only way to do that in Java. Actually a couple of months ago there was a paper published in the coupe conference which is one of the largest Europe's uh conference on type systems and programming languages doing taint uh analysis and taint inference using type systems in Java using annotations. So also the way you implement your languagebased security checks depends on your languages and also depends on your expertise. Now if you are in Java annotations makes more sense than using

**[38:34](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2314s)** boxes type boxes because Java developers are more used to annotations right whereas if you are a Scala developer like some Scar developers don't really like annotations um we do like types more so we try to add these typections everywhere to encode properties on the code itself. So it depends on your expertise and on your team and ultimately this is just you know the the just the only one important takeaway that you should have from this presentation is that security is a team effort. So whenever you have to pick a new tool, a new language or a new analysis for your codebase, remember that you have to work with people, developers and that these teams, these

**[39:21](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2361s)** developers, these colleagues have different expertise. They are familiar with different languages with different tools. So you should try to pick tools and languages that everybody is familiar with. If you're trying to force developers to use a technique they're not familiar with, then you might find resistance. You might find people trying to work around your library. You might file people trying to bypass your security checks. Wherever if you look for techniques that everybody agrees on, everybody's familiar with, then you probably have more people embracing your security checks and writing code that is more secure and safer by design and construction rather than by correction. So, thank you. Thank you very much. I think we do have some more time for questions. You will find the code of the

**[40:12](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2412s)** short demo here uh with also a link to the implementation the full implementation of the of the box uh and also find my email here if you want to email with some more questions after the conference. Thank you. [applause] >> Yes. Do do we have a mic for the questions? We don't have a mic for the questions. Okay. >> [snorts] >> Mhm. >> Well, I start from the second question.

**[41:01](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2461s)** So the the the question was uh what's the runtime cost for these checks if you cannot do them at compile time and how you add this checks to existing legacy code. So the runtime cost depends on what you do. So if you just throw an exception that is almost negligible. It just you know part of the workflow. If you start adding logging reporting that increases the cost but that is also true for every instrumentation that you might do on your on your code. So this is why it's preferable to do that at compile time right because it doesn't add any overhead at um well it does add some overhead in opening these boxes but most of that part can be optimized by the compiler so it doesn't really you know incure into performance degradation most of the times as for how you do that on

**[41:52](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2512s)** legacy code that is that is indeed a problem. Um the good thing about these approaches is that you can add them in small portions of your applications. So you can start small by in adding type systems like more sophisticated types is small portions of the application. The ones that you feel are more vulnerable or the ones that you feel you you want to focus on and then you can expand from there uh to the rest of of the application. Of course, the good thing is that once you start to add types, you will have the compiler guiding you on what you need to to change to have you know the code the code running again. Okay. So it is easier to if you add a new type as a return type of a function then the

**[42:40](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2560s)** compiler will tell you that you look to you have to change this this and that to have the call compile again. So you can be guided also by the by the compiler in in your changes. Does that answer your question? >> Y >> Yeah. Yeah. >> Yeah. It's it's also for documenting the

**[43:28](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2608s)** code like the security types that you have you know like if you if you use types one of the other uses of types is documentation. So you you have something in the code that tells everybody yeah I have to comply with these constraints for your code to run. Okay. So this is also a a good thing. >> Yeah. [snorts] >> Yeah. Yeah, I actually did try to ask uh so I also have uh another slide here. So I did try to ask copilot to take the first

**[44:18](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2658s)** vulnerable code to take the box implementation and I asked it to secure the code using that implementation. This is what I got. So um I will take the pointer here. Notice that what copilot did here it is it created a new tainted widget using you know without using the unsafe method. So this is it recognized that it was uh you know potentially unsafe to use. It added an example sanitization. Now in this case it would replace the vulnerable characters. So it would just drop completely all the HTML tags. Um it would add some other validation in the price. Um and then basically it would do

**[45:07](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2707s)** the same thing opening um the box adding it to the DB uh almost automatically. It did not introduce the repository because it did not try to you know start the code more. Uh but in the end it did a pretty good job I think uh in this case because it was able to infer all the you know the constraints here to infer a pretty pretty good sanitization rules uh and then in the end it just used the right you know matches. Uh so this work I mean this works very well um in in general you can be guided by AI as well to to add you know your rules. Yes, >> thank you. [sighs] >> Yes.

**[46:11](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2771s)** Yes. So um yes when you match I can go back to the code. When you match to left and right here first Scala will tell you if the match is not exhaustive. So in this case it recognized that either can only be either left or right. So if you just have right uh it will give you a compile time warning. compile time warnings can be turned into compile time errors um with a flag. So that is something you can do. Um and here when you match for left for example error is just a string. It is not the box anymore. So in this case you completely lose the tinted widget. Of course you still have you know this variable. You can use it anywhere in your code again but it type doesn't change. It type is still tinted.

**[47:01](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2821s)** So even if you can use it, you still have to sanitize it again possibly with different rules, right? Doesn't mean to be doesn't have to be always the same rules. Uh but in this case with this sanitization in this branch, you will completely lose uh track of your of your tainted uh value. Does answer your question? >> Okay. Mhm. No, actually no. I looked if there is there were something for C similar using types. Didn't find anything. I just found some um static analysis tools similar with C++. Um so no, I didn't

**[47:51](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2871s)** look into other programming languages specifically. Um I know this is possible with Huscoll. You not not surprisingly. Um so actually this work was based on a previous work I did with Huscoll back at university as as I was saying uh it was a bit less sophisticated but still the type system that Huskell has is even more powerful for some things than Scalas. So not surprisingly you can do that on oncll as well. Uh as for other languages no I would I wouldn't know. Come again. What do you see? Oh yeah. It could it could be possible. I will I will look into that. That's a good suggestion. Yes. Also, because like um

**[48:40](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2920s)** Scala these days is is used in many contextes, but it's not as nearly as popular as for example TypeScript, right? So, uh it would be good to to see if that can be implemented also in other more dynamic languages like if if you pass the term. Thank you. Okay. >> That makes sense. So generally you have different tools for for any language has its tool for static t analysis or dynamic t analysis. There are for C++ and cute there's also a dedicated tool.

**[49:30](https://www.youtube.com/watch?v=-yTEFW2ympk&t=2970s)** Uh what they do is usually they they look to the code and try to see if there is some data flow that is not sanitized. They also try to see if there is a function that can look like sanitization. There are some heristics to say well this looks like sanitization this doesn't look like sanitization. Uh but then again those are you know big tools and when you start using static analysis you have to do it on the whole codebase. There are some research work showing that incremental static analysis doesn't really work very well. There's a lot for false negatives uh sometimes. So what you try to do usually is you have to apply to the whole codebase which again we go back to the first problem where data static analysis can take hour to complete. Right? So if you do that on a massive codebase it could take hours. It's not something that you can do for

**[50:19](https://www.youtube.com/watch?v=-yTEFW2ympk&t=3019s)** example a part of of a deploy pipeline as you go because if it takes hours you have to wait hours for the run to complete. So the the good thing about using types is that everything is offline when you run the analysis you are still writing the code uh during your development cycle. So that is also um a nice thing. You're not slowed down any more than you're normally slowed down when you have to type stuff and they don't type. Any more questions? Yeah. >> It could be just one two states safe and unsafe. Actually the um annotation

**[51:07](https://www.youtube.com/watch?v=-yTEFW2ympk&t=3067s)** example that I show from the paper they use just two states. uh tainted and untainted. I just wanted to model the fact that some values could be trusted by default without any sanitization. Ultimately, it could be dropped. It's it's something like null in Java. You know, it didn't hurt to have it like now, but then in retrospective, maybe it could is not it's surely not not necessary. Uh it just adds more, you know, flexibility to the whole thing. Also, if you if you dropped pure, you wouldn't need unsafe the unsafe constructor anymore. So, that is also a good point against having pure in the in the implementation. Any more questions?

**[51:59](https://www.youtube.com/watch?v=-yTEFW2ympk&t=3119s)** Okay. So, I think we can go all go to lunch and I'll be around for the rest of the day. So, if you have any more questions or thoughts, please do come talk to me. Thank you.

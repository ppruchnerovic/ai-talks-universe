---
id: jXkEdf2Pinc
title: "Block-Based Parallel Programming - Bryce Adelstein Lelbach - NDC TechTown 2025"
slug: block-based-parallel-programming-bryce-adelstein-lelbach
conference: ndc
conference_name: "NDC Conferences"
category: "General software conferences"
edition: "NDC"
year: 2026
speakers: ["Bryce Adelstein Lelbach"]
channel: "NDC Conferences"
duration_min: 56
published_at: 2026-01-13T09:50:07Z
video_id: jXkEdf2Pinc
url: https://www.youtube.com/watch?v=jXkEdf2Pinc
youtube_url: https://www.youtube.com/watch?v=jXkEdf2Pinc
tags: ["C++", "Python", "Language", "Tools", "NDC", "Conferences", "2025", "Live", "Fun", "Kongsberg", "Norway", "Bryce Adelstein Lelbach"]
topics: ["Inference, serving & GPU infra"]
transcript: true
---

# Block-Based Parallel Programming - Bryce Adelstein Lelbach - NDC TechTown 2025

**Bryce Adelstein Lelbach**

`NDC Conferences` · `NDC` · `2026` · `56 min`

`#C++` `#Python` `#Language` `#Tools` `#NDC` `#Conferences` `#2025` `#Live` `#Fun` `#Kongsberg` `#Norway` `#Bryce Adelstein Lelbach`

[Watch the recording](https://www.youtube.com/watch?v=jXkEdf2Pinc) · [Conference site](https://ndcconferences.com/)

## Description

This talk was recorded at NDC TechTown in Kongsberg, Norway. #ndctechtown #ndcconferences #developer #softwaredeveloper

Attend the next NDC conference near you:

/      @NDC

Follow our Social Media!

Parallel programming can be intimidating, but doesn’t need to be! There's a new paradigm for parallel programming that's newcomer-friendly, highly productive, and performant: block-based programming.

Block-based programming models divides inputs into local arrays (tiles) that are processed concurrently by groups of threads (blocks). Users write sequential array-centric code, and the framework handles parallelization, synchronization, and data movement behind the scenes. Block-based models have been around for a long time, but in recent years, they've grown in popularity for GPU programming in languages such as [Triton](https://openai.com/index/triton/), [JAX/Pallas](https://docs.jax.dev/en/latest/pallas/index.html), and [Warp](https://nvidia.github.io/warp/modules/tiles.html), aiming to make parallelism more accessible and increase portability.

In this example-driven talk, we'll cover the basics of block-based programming in both Python and C++. We'll present cuTile, NVIDIA's new block-based programming model for Python, C++, and other languages, and Tile IR, the new compiler stack that it is built with. We'll reveal new details about this new technology for the first time in this talk. We'll compare and contrast block-based models with traditional parallel programming models.

We'll look at a variety of examples, including a new tile-based [LLAMA3](https://github.com/meta-llama/llama3)-based large language model demo, a stencil code, and an FFT solver.

In this session, you'll:
- Learn the best practices for writing block-based parallel applications for CPUs and GPUs.
- Gain insight into the performance of block-based code and how it actually gets executed.
- Discover how to reason about and debug block-based applications.
- Understand the differences between block-based and traditional parallel programming and when each paradigm should be used.

By the end of the session, you'll understand how block-based programming enables more intuitive, portable, and efficient development of high-performance, data-parallel applications.

## Transcript

*7,861 words · source: supa (en, exact timings)*

**[0:06](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=6s)** I have for at the end of the talk I have these very cool Nvidia rulers on PCP board and the people who ask the best questions will perhaps get a ruler. They used to put they've got all sorts of you know stuff like uh LEDs etc on here. They used to put the die traces for the Nvidia GPUs, but now they're too big to fit onto the ruler. So, anyways, all right, we good to good to go. We're good for the recording. Okay, cool. So, hello and welcome everyone. My name is Bryce Adlesen back. I am a principal engineer at Nvidia where I founded the CUDA core light compute libraries team and I now lead the Vanguard programming group which is responsible for programming language design and evolution. So my job is to

**[0:55](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=55s)** make every major language a GPU language from Forran to C++ to Python to Rust to JavaScript. I'm involved in the leadership of many programming languages. I chaired the standard C++ library evolution group and the American programming languages standards committee. So today I'm excited to share with you a preview of a new programming model and compiler stack for CUDA which is Nvidia's compute platform uh that we have recently announced and will be released soon. So first what are GPUs and what are they good for? So modern CPUs and GPUs are both generalpurpose processors that can run any kind of code. They are they differ in what workloads they are

**[1:46](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=106s)** optimized for. CPUs are optimized for latency. They have more powerful cores and threads but less of them. They have less latency to access memory and a lot more memory to access but at the cost of lower throughput. GPUs on the other hand are optimized for bandwidth and parallelism. They can run hundreds of thousands of threads but each one of those threads is substantially weaker than CPUs. And unlike CPUs, GPU cores are a lot simpler. They don't have out of order execution. They don't do speculation in the same way that CPUs do. And that

**[2:35](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=155s)** means that their memory latency is substantially higher, but memory throughput is much greater. Now, there are two ways to program GPUs today. The first is grid level models where you invoke GPU algorithms on the entire input. The algorithms are launched from the CPU but run on the GPU. You don't explicitly parallelize or partition the work or data. The system does this for you automatically deciding how to divide up the problem and map it to execution resources on one or more GPUs or other devices.

**[3:26](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=206s)** In fact, as far as you're concerned as a programmer from your frame of reference, many of these models are have the appearance of being sequential. So this is the easiest and most common approach to GPU programming. There are many excellent GPU accelerated libraries that embody this grid level programming model such as KPI, PyTorch, KUDF, Kublos, KU FFT, Cub Jack, etc. Libraries should be the first thing that you reach for when writing GPU code. They'll make your life easier and they'll often give you better performance because they're written and tuned by a team of experts.

**[4:16](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=256s)** You should try libraries before writing your own GPU algorithms. For almost every use case that you can imagine, there's an existing library. And if there is, you should try it out and benchmark it. And if it delivers the performance that you need, then just use it. Don't try to reinvent the wheel because even if you do manage to do better than one of these libraries, whatever work you've put into that will probably become useless in one to two years because computer architecture is changing very fast these days. But sometimes there's no library that does what you need or you've tested and you found that you just can't get the performance that you need with the libraries that do exist or there's two different libraries

**[5:06](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=306s)** that do two different separate components of things that you need to do. But what you really need is you need those two algorithms to be fused into one. And so that's when you should consider writing your own GPU kernels. So a GPU kernel is a function that is executed in parallel by a group of threads and all of these accelerated libraries are implemented with GPU kernels. Historically GPU kernel authoring has been a pretty low-level task that you've had to do in languages like C++ or C. Today, when you write a GPU kernel, you're programming at the thread level. You explicitly code every thread that

**[5:57](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=357s)** you run on the GPU. You decide how to split the compute work into blocks, how to divide the data into tiles, and how those blocks and tiles get mapped onto threads. Writing CUDA kernels in this way gives you complete control, allowing you to program and tune every aspect of the GPU. And in each new generation of NVIDIA GPUs, we expose more tools, giving you greater control and freedom. But all of these choices can be overwhelming. It would be very nice to have a higher level abstraction that succinctly expresses kernels and allows

**[6:46](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=406s)** the system to select which tools to implement them with instead of you having to make that choice yourself. NVIDIA GPUs have had tensor cores to accelerate operations like matrix multiplication and convolutions. In every GPU generation since they were released, the structure of the tensor cores has changed. The formats have changed, the precisions have changed, the width and size has changed, the mapping to threads, etc. All of that changes every architecture. And this has forced users to rewrite tensor core code for every new GPU that we release. Then the new GPU comes out and whatever code you wrote that uses the tensor cores is essentially no longer valid and you have

**[7:35](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=455s)** to start from scratch. And what we need is a middle ground, a model where users explicitly partition work and data exposing parallelism. But that partitioning is logical, not physical. The system decides which tools to use. It will map the partitioned work and data to threads and execution resources. Because the mapping is implicit, it's portable. The system can pick different mappings for different NVIDIA GPU architectures. So in this approach, we get much of the control and flexibility of thread level programming, but we also get the high productivity and portability of grid

**[8:23](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=503s)** level programming. And we call this new model tile level programming and it is coming to CUDA soon. We have recently announced coupile which is a tilebased kernel programming model for NVIDIA GPUs. It is higher level and arraybased and automates block level parallelism and memory movement. We've built it to simplify GPU programming and to improve portability for new hardware features like tensor cores. It is built on top of tile which is a novel new low-level compiler stack and intermediate representation for tile programs that we have developed. So, coupile Python kernels are JIT compiled

**[9:13](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=553s)** to tile IR using an execution model that allows the code to be executed without the need for a Python interpreter as native code similar to Numbas no Python compilation mode or other DSLs like Triton or Jack's Palace. So, this model has some semantic compromises. All of the objects get mapped down to value or pointer semantics. So, there's no reference counting. You are responsible for managing the lifetime of things and you can't do some py pythonic things like dynamically modify types and all of this both coupile and tile will be released soon. So coupile is meant to be simpler and higher productivity than traditional CUDA programming. In the traditional

**[10:01](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=601s)** CUDA programming model, we call it simpy. And that's what I'll be using throughout the rest of the talk to differentiate it from this new model. The idea is that you should be able to get good performance faster with tile programming. So with coupile, you don't have to decide how data is loaded, mapped to threads, and stored locally. The system does that for you. You just express your computation as array operations. So to reach the speed of light for a matrix multiplication before coupile you'd have to write something like the CUDA C++ code on the left which is no easy feat and it's also not portable. That code that you'd write would change from GPU architecture to GPU

**[10:48](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=648s)** architecture. With coupile, we can write this simple code on the right, which is just what you'd see in a in a textbook on how to write a tiled matrix multiplication, and you can get good and portable performance. This is what the actual speed of light matrix multiplication in coupile looks like. Coutile will be exposed first in Python, but a C++ dialect is also coming, which will be intentionally similar to what you're going to see today, but uses modern C++ idioms and best practices. But we're going to mostly focus on what the Python exposure looks like today. So now let's explore how the tile programming model differs from that

**[11:36](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=696s)** traditional CUDA programming model. In CUDA symph the unit of execution is a thread. You write functions that are executed by many threads at once. And we call these functions kernels. The set of threads that a simp kernel runs on is called a thread grid. It's hierarchical organized into different subgroups of threads. warps of threads, blocks of warps, clusters of blocks, etc. Users have to define the shape of this hierarchy and explicitly program it and program to each level of the hierarchy. In coupile, the unit of execution is a block. A block is a group of threads or

**[12:27](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=747s)** execution resources that has a single thread of control. You write tile kernels that are executed by many blocks at once, but each block only executes the kernel once on the control thread. In simple elements, you write scalar operations on elements that each thread performs. If you want each thread to apply an operation to multiple elements, then you would write a for loop. There are no native array operations in CUDA symph. It's fundamentally a scalar programming model. On the other hand, in coupile, everything is an array. You express blockwide operations on entire arrays.

**[13:22](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=802s)** In both simple programming, the user is responsible for splitting the computation computational work into blocks and dividing the data into tiles. The key difference between simpy and tile is who is responsible for mapping blocks and tiles onto execution resources in memory. So once you've partitioned your work in data exposing parallelism, there's still a lot of decisions to be made. What elements of data will each thread work with? Where does each thread keep copies of those elements? GPUs have many different kinds of memory available to them. How should those local copies be laid out in memory? The memory layout can

**[14:11](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=851s)** have a huge impact on performance. How does each thread load and store those elements from global memory? There are now many different ways of doing this and they are growing in complexity. And then how does each thread perform computations? And again, there are now many different ways of doing this. And some of these mechanisms are specific to particular GPU architectures. They change when the new chip comes out. In simp responsible for answering all of these questions by picking from the many tools that are available. But in tile the system makes these decisions

**[15:00](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=900s)** analyzing the program and selecting the best tools for the target Nvidia architecture. All that we have to do is explicit. All we have to do explicitly is parallelize by dividing the problem up into blocks and tiles. Within each block, everything is singlethreaded and you just perform array operations on tiles. So let's look at an example. When running a simp kernel, each thread within a block will execute the kernel. But a tile kernel is run by just a single thread per block. Within a tile kernel, we create tiles which are a

**[15:49](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=949s)** special type of local array that you can perform array operations on. And those array operations are sequential with respect to the control thread, but they may be paralyzed under the hood by the system using the blocks, threads, and other execution resources. So if you know how to use numpy, you know how to use coupile python. So here is an example of softmax written in the left in straight numpy and in the right in coupile. Coutile is fundamentally arraybased similar to languages like APL.

**[16:41](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1001s)** There are two different kinds of arrays in coupile. Global arrays are typical numpil like mutable arrays that are accessible by all tile blocks. So they're mutable which means that if you modify their contents any other references to the underlying data will see the changes and they are passed in as kernel parameters or globals. You can only perform a limited set of operations on global arrays in tile code. Most importantly, loading from them to create tile arrays or storing tile arrays to them. Now, tile arrays are immutable numpy

**[17:33](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1053s)** like arrays that are local to a block. The operations on tiles will always return a copy not a view and they are paralyzed across the blocks execution resources. Most of the operations that you can perform on numpy arrays will be supported on tiles. But from the perspective of the block's single thread of control, all while all those operations are paralyzed, from the perspective of the block's single thread of control, those operations are performed sequentially just like they would be in NumPy. All of CUDA's underlying launch APIs support both simple kernels. And this means that users don't

**[18:23](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1103s)** have to care how a kernel was written, be it simpy or tile to use it. And this means that you can use and integrate tile kernels anywhere that you can that you've been writing CUDA kernels today. So you can use them in KPI, in KUDF, in PyTorch, in PyTorch, in JAX, etc. The array parameters to kernel launches can be anything that satisfies the DLPAC protocol in uh Python. In C++ it' be anything that can fit into an MD span. So this could be numpy arrays, coup arrays, pytorch arrays, etc. Now, tile arrays have value semantics which simplifies compiler analysis by eliminating problems such as aliasing.

**[19:13](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1153s)** And in fact, they have since they're immutable, they have single static assignment semantics. All tile operations return copies, not views. Now, some numpy array operations such as indexing, reshaping, transposing would typically return you back views not copy when possible to avoid creating unnecessary copies and allocations. But the tile equivalence will always return copies, not views. Assignment to the elements of a tile is ill-formed because they are immutable. If you need to change the value of some of the elements, then you would need to make a new tile.

**[20:04](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1204s)** You can do this with the wear operation, which would take a boolean tile into source tiles and return a blended tile that has elements from the first source when the corresponding boolean tile element is true and elements from the second source otherwise. It's a little bit unnerving to think of tile arrays as values. Typically, our intuition is that it's fine to make copies and temporaries of scalers like ins because they are cheap to copy. They can fit into registers and they won't always be materialized. Every time you copy an int in C++, you don't worry about it because the compiler can collapse the unnecessary copies. But we

**[20:52](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1252s)** typically think that array copies should be avoided because they're expensive and that they'll always lead to an allocation and materialization. But tile arrays are different. You have to treat tiles as you would ins. They are cheap to copy and won't be materialized unnecessarily because they are a fundamental primitive in our compiler stack. And so the compiler can see and eliminate unnecessary copies. Imagine a computer architecture where all of the instructions are array operations and all of the operons and registers are arrays. In that sort of architecture, you wouldn't worry about the cost of making these array copies.

**[21:42](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1302s)** And this is exactly the tile programming model. So let's look at another example. a tiled matrix multiply in coupile. So we can create tile with num create tiles with numpy like creation operations like zeros. The shape of a tile must be a constant expression. So something that is either a constant or that the compiler can see comes from a constant. And there's a few reasons for this constraint. First, it creates optimization opportunities. Additionally, tiles may end up being placed in shared memory or some other resource that needs to be statically allocated. Now, this is Python and we're

**[22:33](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1353s)** talking about doing JIT compilation here. So at the time that we're doing JIT compilation, we do of course know what the value of all of these inputs are. But we have to decide which of those inputs to embed into the kernel. What this means is which of these inputs if they change would cause us to have to redo JIT compilation. And if we changed if we read the JIT compilation when any parameter changed there wouldn't be a lot of point of a lot a lot of point in doing JIT compilation and caching it this ct.constant type annotation instructs the JIT compiler that this parameter is one that should be embedded into the kernel. So if that parameter ts

**[23:21](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1401s)** changes then we would have to compile a new kernel. You can kind of think of it like const expert in C++ or like adding a template parameter in C++. You change a template parameter in C++ you get a different instantiation of the function. Now remember the system decides where the tile will be stored in register memory in shared memory in tensor memory global memory or even in a combination of different memory kinds. The system will also decide what layout the tile will have. And this is often tied to what elements each thread will be responsible for when array operations are performed. And again, that is a decision that the system will make. For example, during loading, we want a

**[24:11](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1451s)** specific memory layout such that each thread is loading elements nearby to elements that their neighbors are loading to ensure that the loads can be coalesed together and issued at once for an entire group of threads, which is essential to get good performance on GPUs. Today, each dimension of a tile must also be a power of two. And this is a constraint that we expect to have at it launch but plan to relax as soon as possible. And because we may have to put a tile into all of these different kinds of memory spaces, this is why we have this restriction that the size of a tile has to be known at compilation time because some of these kinds of memory are statically allocated. We cannot dynamically decide the size of an object that we're going to put into these the

**[24:58](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1498s)** memory kinds. Remember that a block invokes a kernel just once on its control thread. Scalar operations in control flow are going to be executed just a single time. Tile load operations create a tile by copying a part of a global array. Load and store operations are indexed in the tile space. The tile space of a global array is the index is the is the multi-dimensional space of tiles into that global array of a certain tile shape. So a tile index i j etc with shape s refers to the

**[25:47](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1547s)** elements of the global array that belong to the i j j tile of that shape. If we provide a different tile shape, then the tile indexing will be different and we will load a tile with a different set of data even if we're using the same index because we're in a different indexing regime. Remember the distinction between blocks, subgroups of threads, which are a unit of execution, and tiles, subsets of global arrays, a unit of data. A block may work with multiple different tiles with differing shapes originating from differing arrays. The load and store operations are a

**[26:38](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1598s)** portable abstraction over the many different strategies for memory movement that are available in CUDA today. For example, load could lower to a traditional synchronous instructions that are executed by every thread, potentially vectorized, or it could use the asynchronous thread copy instructions available in newer GPUs, allowing memory movement to be overlapped in pipelined with compute. We could also use the temper tensor memory accelerator to perform asynchronous blockwide loads and stores allowing for even more overlap of compute and I compute and IO and exposing much greater bandwidth. The system will pick the best option available given the constraints

**[27:29](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1649s)** of the memory access patterns that you've specified and the hardware that you are running on. Next we have a matrix multiplication and here we use a matrix multiply accumulate operation unique to coupile Python but we would also have the numpy equivalents map mole and dot but there are some cases where we have to introduce our own versions of operations for example in the case of MMA to be able to expose precision flags and controls that are not present in numpy MMA, Matt, Mole, and DOT are all abstractions over hardware. This time over compute hardware. Remember earlier we talked about the differences in formats, precision, width, and mapping

**[28:19](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1699s)** to threads for tensor cores across NVIDIA GPU architectures. With coupile, you don't have to worry about all of those changes. You can write tile code once and the system will take care of mapping it to both current and future Tensor Core hardware. So this is the story for how you can pro portably program tensor cores going forward. Then finally we have a store which writes a tile into a subset of a global array. Another way to perform loads and stores is with views. View objects produce new tiles from an input array or a tile. A partition view represents the tile space

**[29:08](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1748s)** of an array under a particular tile shape. You index a partition view with a tile index which loads the tile at that index from the array. You can also assign to a partition view index to perform a store in a kernel like mapm where you are loading multiple tiles of the same shape from an array. This can be more concise and cleaner than calling load. Some examples of other types of views would be an overlapping view that would allow you to get overlapping pieces of the input to do something like a stencil or convolutions or a view on another tile that allows you to get a subset or a slice of that tile.

**[29:57](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1797s)** So here's another demo. A non-causal flash attention v2 forward kernel for a load. We can specify the memory layout of the input global array with the order parameter which takes the numpy style argument f for column major or c for row major which is the default. Now note that this only specifies how the global array is accessed not the memory layout of the loaded tile because that's up to the system and cannot be controlled by the user. We have operations for combining tiles cat subdividing subdividing tiles extract and reshaping or casting them. Tiles support numpy style broadcasting.

**[30:50](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1850s)** When operating on two arrays or an array and a scaler, the shapes of both are compared elementwise, starting with the rightmost dimension and moving leftwards. Two dimensions are compatible if they are equal or if one of them is one, in which case the dimension will be stretched to match the other. One of the objects may have fewer dimensions than the other. If so, then the missing dimensions are assumed to be one, meaning that they will broadcast. So at launch, coupile will support a wide range of numeric dtypes. Arithmetic floatingoint types and all nonflating point numeric types can be

**[31:39](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1899s)** used with all arithmetic and mathematical operations such as addition subtraction trigonometric functions, and transcendental functions. Restricted floatingoint types can only be used with certain operations such as matrix multiplication. In terms of promotions, our system of promotion is more similar to PyTorch than NumPy. Numpy prefers to promote in ways that are always safe. For example, adding a numpy array of int32 or multiplying a numpy array of int32 with another numpy array of int32 can promote to int 64 which can have a big performance impact. So we never

**[32:28](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1948s)** promote within a category. Our promotions tend to prioritize performance and are a little bit closer to the promotions that you would get in C++. We also disallow many implicit conversions that frameworks like NumPy would allow. You can write and use simple kernels in the same source file. We also plan to support hybrid kernels where you can write both simpile code in the future. Llama 3.1 is an open- source language model developed by Meta and we ported Llama 3.1 inference to coupile and we're able to get about 93% of the speed of light which is CUDN's performance and

**[33:17](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=1997s)** this was with our very infant tile compiler performance will only get better from here but coupile isn't just for large language models or machine learning this extends ends the CUDA model and applies to GPU programming in all domains from HPC to data science to computer vision. In just a couple of weeks, we were able to portweather and HPC mini app for 2D viscous fluid flow to coupile. It's a traditional HPC stencil code on a structured grid with a bunch of different physics models which are th shown here. And it doesn't actually use the tensor cores at all. However, it can still benefit from coupile because coupile will automate things like using more advanced memory movement operations

**[34:09](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2049s)** like those blockwide loads and stores which would be very challenging to program manually. We envision expressing stencil kernels like those used in Minweather like this. Each block loads one overlapping tile of elements from the global array and then creates a stencil view to that tile. The stencil view can be indexed with relative coordinates producing tiles of the same size but with the contents shifted. And this allows us to perform operations on neighboring elements. When storing the result, we only want to write the interior points of each tile. So we mask away the other points. those points will be computed and written by their by the neighbors.

**[34:59](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2099s)** So, Coutile is a major new addition to CUDA. It is the first time that we've extended a new to a new programming paradigm. But simply cannot express in coupile. It is fundamentally a data parallel array oriented model. So things like synchronization algorithms where threads within a block must explicitly communicate or sorting algorithms etc. you would probably not be able to implement efficiently in coupile. Coutile is meant as an addition not a replacement. We're very excited about the possibilities that coupile unlocks both for Python users and also for compiler authors and other languages that might want to target tile IR. This

**[35:48](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2148s)** will make it a lot easier to write speed of light GPU kernels that are more efficient and safer. If you want to learn more about GPU programming, there are many talks available to watch on demand from GTC 2025, Nvidia's developer conference. We've also released a new open-source CUDA curriculum with tutorials, notebooks, and other learning materials, which you can find on our accelerated computing hub on GitHub. We've also got lots of great courses in NVIDIA's deep learning institute on CUDA and parallel programming. And as a thank you for attending today, you can access our course catalog for free for the next six months using this QR code. Don't worry, all the links will be on the last slide.

**[36:36](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2196s)** And if you're interested in parallel programming, algorithm design, and programming language development, my buddy Connor and I have a podcast, ADSP, Algorithms Plus Data Structures Equals Programs, available on all streaming platforms. We also do have stickers. We promised we would have stickers. The stickers are here. You can go subscribe. Now, one of my favorite episodes, we interview Shawn Parent, pictured here, and he tells us about the time that he was working at Apple, for some reason was given an Apple Apple corporate Porsche. He crashed it, thought he was going to get fired, and then Steve Jobs came in and saved him. So, here are all the links as I promised. And thank you all for your attention. We've got a good bit of time if we want to do some questions. And remember, I have rulers that if if

**[37:33](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2253s)** uh people ask good enough questions, uh we'll give them out. Uh can either Connor or Ashwin write down the questions because I like to have the questions recorded for later. So, I'll give him a second to get his phone out. We'll start with Victoria. Sir, so one of the things that I found annoying with C++ algorithm is that when you chain them together, it kind of like has to finish the first one, then the second one, the third one. So I'm guessing with this approach because you're compiling the journal, right? >> Yeah. Yeah. So you you do not have the fork join fork join that you have with um the grid level models. That's the real downside of those grid level models. If we go back to here. Um, so, uh, we're getting there. It is like the

**[38:23](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2303s)** first couple slides. So, you know, it's like when you had to rewind the VCR. Whoa, that was a little bit fast. Yeah. So, so those grid level models um, you know, each the the sort gets launched, then the cumulative sum gets launched, then the divide gets launched, then the sum gets launched, etc. And because it has these seemingly sequential semantics, it's really challenging to take this and to turn it into something that's fused. In particular, this these sorts of models, things like KPI or thrust um or pietorch, these are eager models. Another example would be uh pandas if anybody's from the data science framework. So these are eager models. The semantics are that when you invoke one of these things gets launched immediately. If you have a model like this but it's lazy where you should

**[39:12](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2352s)** create these operations but then you have to explicitly launch it or something then it becomes easier to deal with this and pretty much all of these frameworks that are widely used they have some form of compiler that tries to take this code and com and compile it in a way that fuses it. So Koopi has Koopy Fuse PyTorch has like five of these different of these things. Everybody's got some form of of graph compiler like this, but it's a very challenging problem to to get this right. The advantage with the uh the the tile level model. No, where's the Yeah, this right here. The advantage with tile level is that you're still writing a kernel. You are still programming. You're still writing something that's going to be cooperatively executed. So we it's much easier for us to do the fusion because

**[40:00](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2400s)** your the semantics are that this is something that's executed by every block. Um and the compiler under the hood will do all sorts of fusion and optimizations. Um uh and that's uh that's sort of the the really the selling point of this is that we've crafted a uh a DSL that has uh very nice semantics. It's all value semantics. It has a very simple type system and because of that it allows us to do optimizations that would be very challenging to do uh in just like you know regular C++ and in the C++ form of this um we're still sorting out like what the aliasing rules will be um but the C++ form of this when you write one of these C++ kernels you'll be writing

**[40:48](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2448s)** value semantic code um and because of that we'll be able to do those optimizations in C++ as Yes Victor. >> I have two questions that are related to each other. Uh since you mentioned that the system orchestrates uh the tile layout uh and how this computation is scheduled. My question is how do we handle determinism in this context and how do you handle debugging tile based programming in the context of >> uh so this is a good this is a good question uh tile IR supports both ahead of time and JIT compilation um the for given if if you're running again on the same architecture with the same

**[41:36](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2496s)** inputs etc it is almost certainly going to give you the same layouts. Um uh the configurations and the tuning are not based on any dynamic metrics. It's based on what is your code doing in static analysis. So all the decisions are made on static analysis of the code and information about the environment. Um and you can do ahead of time compilation so that you can uh get a binary where you can profile it and where you can look at what the actual layout and structure is. Almost all of these decisions are decisions uh about uh uh like the major decisions are more so about like hardware capabilities like what architectural generation you're on. Um that said um one thing we didn't touch on. Um in a lot of these examples

**[42:29](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2549s)** here here it's we're calling the parameter R. you have to pick some size for how how big is the data in each block going to be and the question becomes how should you make that decision. Um because it's sort of a logical decision too. Um you could pick a really big size and then the compiler could decide that it's going to pick a smaller physical size under the hood. The the guidance that we're giving people for this is that the best practice for these knobs will be to do autotuning. There are also some explicit um uh performance tuning knobs that we expose programmatically um like uh that one of them is like an unrolling pipeline uh depth and then there's another one called latency which is a number between 1 and 10 which is how memory bandwidth heavy you think

**[43:17](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2597s)** your application's going to be. All of these parameters are sort of designed for autotuning. We don't have an autotuning harness in place yet, but other frameworks in this space like Triton have come with a built-in uh sort of auto autotuning harness. So, we'll probably provide something like that. Um, and that's what we recommend. It doesn't have to be online autotuning. It doesn't have to be something that you do every time you run your program, but our guy our recommendation is at least that when you know what the environment you're going to be deploying on, you know, run the autotuning framework, it'll spit out some, you know, configuration and then ship with that configuration. And we're seeing a big shift towards two things. One, JIT compilation and two, autotuning. Uh, historically, the way we've shipped software has been that we pre pre-ompile and pre-bake a bunch of configurations.

**[44:08](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2648s)** So, we something like Kublas, our linear algebra library, you know, we compile, you know, I don't know, a thousand matrix multiplication kernels and then we ship them to you. And that's why the binaries are very big. And then the Kubla decides when you run some input which of those thousand kernels it's going to use. The problem is that if you look at the direction that hardware is going like the next architecture you'd need 10,000 kernels and the one after that you'd need a 100,000 kernels and at some point you'd get to like okay now you need to have a 100 gigabyte library. So it's not practical and to it's not practical to cover the entire uh op like uh parameter space with pre-ompiled code now. And so we're starting to shift towards more JIT compilation not just to

**[44:57](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2697s)** support dynamic languages but also for statically compiled languages to offer better performance uh with the with the hope that users will be able to tolerate a little bit of startup JIT latency to get that better performance. Um, and because of this, this means that almost all of our libraries are moving towards autotuning because if you're going to do the JIT compilation, you might as well do the the online autotuning. >> Other questions? I saw some other hands here. >> Stupid question, but uh, in what situation would a classic CPU be better than a GPU? Let's say you have a data set of 10,000 entries. Uh are there any situation where you would use uh classical CPU rather than GPU?

**[45:46](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2746s)** >> Uh let's look at a slide. Uh where is there it is? Take a moment to open it up. That slide actually used to be in this slide deck. I'm not sure why I took it out. There we go. Okay, so you need a sufficient workload to see a benefit from using GPUs. If you try to like sum up a thousand ins or sort 100 floats, you're not going to see a huge speed up from paralyzing that with a GPU. Um, you have you pay high fixed costs for moving data to the GPU. Uh, and and so that's really what what hurts you is that you have this high fixed cost to transfer the instructions and the data from the host to the to the

**[46:34](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2794s)** uh GPU. Your your workload is defined by how much compute you're doing per element which we call the operational intensity and how many elements you have. A good rule of thumb is that if you're doing compute if you're doing you know like four ads per element they or like applying a function to each element or summing them up then you typically need gigabytes of data or at least gigabytes of or like millions of elements to really see a benefit. if you're doing more than O compute or if your uh O has a really big constant factor. So if you're doing something like sorting or matrix multiplication which is n squared then you'll see speed ups with less data. So that's that's the typical guidance we

**[47:21](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2841s)** give for the for the sort of cut off point. Um the other thing is that if when you're starting to GPU accelerate your application, you have to keep in mind that the fixed costs that you're paying you're paying them uh just once for every time you go to the GPU. So a great example, there was a talk at um at Pyon this year where somebody was talking about using our accelerated data science library and examples of where sometimes using GPUs was a gotcha and you didn't get better performance. And they were showing something where they the workflow was like uh do some CSV parsing, do some data science and then like do some CSV output and they moved the data processing part to the GPU and they didn't see a speed up and one of our engineers was in the audience and was like hey wait a moment and he went

**[48:08](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2888s)** and took wrote what they were doing himself in a notebook and then they just moved the parsing of the CSV and the writing of the CSV from the CPU to the GPU and that led to seeing a speed up over the CPU instead of you know not seeing a speed up and the reason is because there was no additional cost for moving that operation from the CPU to the GPU since you were already doing that data processing there the next stage of the pipeline was already there you're not paying for another transfer and you'll see the you'll see a speed up from doing that operation you see a speed up from doing that CSV parsing on the GPU it might not be worth it if you were only doing the CSV parsing to do it on the GPU. But if you're going to do that and then do something else, moving

**[48:55](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2935s)** it once you've decided you're going to do something on the GPU, moving as much of the pipeline as possible there to minimize transfers to keep the compute local to where the data is, that will have a big impact. All right, other questions? Yes. Why? >> Why the strict immutability on tiles? Are there optimizations we can't perform? Uh yes. So uh in uh most compiler frameworks uh the most like compiler frameworks like LLVM and ML which we use uh use a single static assignment model where like you uh when you assign to a var you assign to a variable once and its value does not change and that unlocks many many types

**[49:44](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=2984s)** of optimizations. Um when we were designing coupile we had to very carefully decide which things were going to have a onetoone mapping down to the IR and we decided that um that tile objects were going to have a onetoone mapping. So the tile objects directly map down to tile IR SSA values. Um if you know that some like conceptually if you know that something is never going to be changed by anybody then it's very easy to say eliminate unnecessary copy copies or do dead code elimination um or fuse operations together because you know that there's no other observers like you know that I uniquely own this value. Nobody else is looking at this value um and anybody else who who like has touched this value they've only ever

**[50:33](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=3033s)** made a copy. No, there are no references to this thing. So I can do whatever I want with it. So it does unlock a ton of optimizations. Additionally, because you can only get it by value, you never have an aliasing problem with tiles. Um, and yeah, so it just there's a there's a ton of things that it unlocks and it's fairly fundamental to the underlying model. Now question in the back. >> What happens equally sized tiles. >> Yeah. So if you have if you need to divide into tiles that are not equally sized um that is a good question. I

**[51:23](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=3083s)** think that you could either uh you you could either like do sparse tiles um where like if you know if you know that you're not going to like you could just like load create a tile that's larger than you need and just like sparsely access it um and the compiler is able to do some amount of optimizations to uh like if it knows that a large part of the tile is unused. there's some tricks that it can do to eliminate that. If there's a really a lot of load imbalance across different blocks, uh I think it's very tricky to do that with this model because again a lot of the places where tiles live are going to be statically allocated. Um you certainly can do uh like random access uh into tiles and uh the tile uh will be will have strong support for doing like

**[52:11](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=3131s)** structured sparsity and things like that. Um but I think that uh having tiles of differing sizes uh within the same block uh like with within the same kernel like having the one a tile of one size and one block and one size and another block that's just fundamentally going to break the programming model. Other questions? Yeah. >> How does the library deal with that? >> Um first I'll say it's important to not think about this as a library. It's a it's a compiler and if you think about it as a library, you may be surprised when it does compiler things. Um how does it deal with data sets that are larger than can fit on your GPU? I would say that's a separable issue. Uh this is a library just for writing the kernel. So the this you know just takes in I and

**[53:02](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=3182s)** O which are some arrays some global arrays somewhere and the question of how those global arrays got to the GPU that is outside of the scope of like what this does. The typical way that that you do this there's a couple different approaches. Um firstly you could use CUDA unified memory which allows you to allocate memory that can be accessed by both CPU and GPU and will be paged into the GPU. Um, and if you have sparse access patterns or if you've got a data set that's larger than what can fit, this can be a very good approach because then you can let the operating system and actually the hardware features that uh that support unified memory do the paging and the movement of your data between CPU and GPU. You can also um design a kernel in a I know I just said that this is outside the scope of what

**[53:50](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=3230s)** you'd handle here, but you can design a kernel where the kernel will uh pull in data by batches. Um and in particular that works nicely with coupile because uh coupile automates making your data movement asynchronous. Uh so it uses all of these memory movement mechanisms that are asynchronous. And so if you write sequential code in coupile that pulls in data chunk by chunk under the hood the compiler will uh pipeline that data movement so that while you're doing your compute the next chunk of data will be loaded in but you don't have to explicitly program all of that and that's actually one of the huge uh things that makes this so fast. So this uh in this matmo kernel right here on the right on the left the matmo kernel

**[54:38](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=3278s)** on the left is explicitly doing this data pipelining and that's why it's so long on the right we have these passes that do this automatic data pipelining um so that it doesn't look asynchronous on the right here but that will actually end up being pipelined and overlapping the movement of data from global memory to local memory um with the compute Other questions? All right. Well, I think we're about at time. If you want stickers, I also have Nvidia stickers and ADSP stickers. You can come up here and uh I'll be around for the just today. I'm leaving uh tonight at like 9:30. So, uh if you have questions for me, try to find me sometime after this.

**[55:27](https://www.youtube.com/watch?v=jXkEdf2Pinc&t=3327s)** All right. Thank you all.

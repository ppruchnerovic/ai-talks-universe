---
id: BGTtk3geHoY
title: "The d9d Project: Building an extensible and scalable distributed training framework for LLMs"
slug: the-d9d-project-building-an-extensible-and-scalable
conference: pydata
conference_name: "PyData"
category: "AI engineering & agents"
edition: "PyData"
year: 2026
speakers: []
channel: "PyData"
duration_min: 47
published_at: 2026-08-23T07:00:03Z
video_id: BGTtk3geHoY
youtube_url: https://www.youtube.com/watch?v=BGTtk3geHoY
tags: ["Python", "Tutorial", "Education", "NumFOCUS", "PyData", "Opensource", "learn", "software", "python 3", "Julia", "coding", "learn to code", "how to program", "scientific programming"]
transcript: true
---

# The d9d Project: Building an extensible and scalable distributed training framework for LLMs

**Speaker not identified**

`PyData` · `PyData` · `2026` · `47 min`

`#Python` `#Tutorial` `#Education` `#NumFOCUS` `#PyData` `#Opensource` `#learn` `#software` `#python 3` `#Julia` `#coding` `#learn to code` `#how to program` `#scientific programming`

[Watch the recording](https://www.youtube.com/watch?v=BGTtk3geHoY) · [Conference site](https://pydata.org/)

## Description

Welcome to the PyData & PyCon Yerevan 2026 video collection - our biggest edition yet, held on 24-25 July in Yerevan, Armenia.

From data science and machine learning to Python tooling, production systems, research, and open-source technologies, these recordings capture the ideas, experiences, and practical knowledge shared on stage.

🌐 Website: https://pydata.am

📅 24-25 July 2026 · Yerevan, Armenia

www.pydata.org

PyData is an educational program of NumFOCUS, a 501(c)3 non-profit organization in the United States. PyData provides a forum for the international community of users and developers of data analysis tools to share ideas and learn from each other. The global PyData network promotes discussion of best practices, new approaches, and emerging technologies for data management, processing, analytics, and visualization. PyData communities approach data science using many languages, including (but not limited to) Python, Julia, and R.

PyData conferences aim to be accessible and community-driven, with novice to advanced level presentations. PyData tutorials and talks bring attendees the latest project features along with cutting-edge use cases.

00:00 Welcome!
00:10 Help us add time stamps or captions to this video! See the description for details.

Want to help add timestamps to our YouTube videos to help with discoverability? Find out more here: https://github.com/numfocus/YouTubeVideoTimestamps

## Transcript

*7,135 words · source: supa (en, exact timings)*

**[0:05](https://www.youtube.com/watch?v=BGTtk3geHoY&t=5s)** Uh hi everyone. I'm going to tell you about the DND project. The voice for distributed but with nine letters squeezed and it's a train framework for large language models. Uh a few words about me. My name is Maxima Fanasf. I'm the D&D project maintainer and also I'm a research engineer at touchka.com. I lead our internal foundation models team as well as do all this distributed tank stuff there. You can find me on linkadin on github if you want. Yeah. So agenda of my talk uh firstly I will introduce uh those to those of you who aren't related to distribute training to the distributing how it works in general. I won't give you a big lecture about this. I'll try to be short but informative. Then I'll talk about uh problems uh

**[0:55](https://www.youtube.com/watch?v=BGTtk3geHoY&t=55s)** about current tooling for distribute training as I view this stuff and then I'll talk about the framework itself how we made them the highlight APIs I find the most interesting uh the project road map and the Q&A where you can ask me anything so why uh do we need this tank that's the first question I'll answer in my 101 section then I'll tell you what dispute tank regimes are there There are a lot of them. I want to show you the diversity of them and then I'll show you how these methods work together actually. So, uh why do we ever need this train? Uh let's take a quen free 235 billion parameters model and let's take about let's talk about train it in 16 bits precision in a full train

**[1:45](https://www.youtube.com/watch?v=BGTtk3geHoY&t=105s)** scenario. So we either pre-train it or just provise tuning without lower adapters. So the model weights alone are just under 500s of gigabytes of video memory we need and if we try to train it we need also to take into account the gradients the ADOM states there are two of them and uh this way we need just under two terabytes of video memory just to fit the model so we need at least 22 H100 Nvidia GPUs and uh yeah it uh obviously won't even fit into a single GPU That's reason number one why would we need a distribute tank and also let's take the deepsek with free pretank scenario and yeah let's assume an equal model flop stizilization it's a tough assumption but it's just to show you the

**[2:34](https://www.youtube.com/watch?v=BGTtk3geHoY&t=154s)** idea so uh the guys reported that they were purch Nvidia GPU for Chinese market yeah on a small cluster of such two GPUs. It would be 10 years to train a model of such scale and yeah, we obviously can't wait for 10 years. AI is falling so quickly. So, we need to do it faster somehow. Yeah. So, we should scale hardware on 1,000 of GPUs. It would be just uh a bit more than 100s of days to pre-train deepseek. And on a large cluster of 16,000 of GPUs, it would be just under 7 days the perch tank run. So yeah, that's another reason why would we scale GPUs

**[3:24](https://www.youtube.com/watch?v=BGTtk3geHoY&t=204s)** and why would we need distribute tank. So in other words, we do distribute tank in order to scale both computing memory to train large enough models in a reasonable amount of time. That's important. Yeah. And now uh I'll show you the strategies to facilitate the distribute training. So I prefer to group them into three categories. First one is about data. It's data parallelism and fully shared data parallelism. Uh second category is horizontal parallelism. That's how you can split each individual layer uh onto your GPUs. And the third one is vertical parallelism. That's how you can split the model vertically layer by layer. So I'll show you some of these methods and describe in a few words how they work. So the simplest distributing strategy is distribute data parallelism. So

**[4:12](https://www.youtube.com/watch?v=BGTtk3geHoY&t=252s)** basically it works the way you have the same train loop and it's a bit different from an offline one. So the only differences there is that on multiple GPUs on different GPUs you just use different shards of the data set. So for instance GPU number one uses samples from uh one to 1,000. GPU number two uses samples from 1,000 to 2,000 and so on. So you shar the data. And the second difference is that you synchronize your gradients after a backward pass by using the already collective communication that works just the way you have uh two local gradient tensors. You aggregate them, collect them on each GPU and reduce using the average operation. So you get your global gradient this way. And yeah, it's a pretty uh a pretty

**[5:03](https://www.youtube.com/watch?v=BGTtk3geHoY&t=303s)** popular method. It works really well for small models, but that's a caveat. For DDP, the model must fit on one GPU, so all the states should fit. So, it's perfect for small models, but uh DDP alone can't make us train DPS with free. Yeah. Uh another one uh about data is FSDP fully sharded data parallelism. It works just like DDP but uh before doing uh some computations during forward and backward passes it has each layer sharded across your cluster across your GPUs. So if you have some linear layers all of these will be sharded by parse on each GPU. GPU will hold only a shard and before doing any computation a forward pass or backward pass of some layer it

**[5:52](https://www.youtube.com/watch?v=BGTtk3geHoY&t=352s)** will gather parts of the model weight matrix onto all the GPUs do the computation and reshart it all again. So you can imagine a dog sitting on a train and playing tracks just in front of it and the tracks before the train are disappearing. So that's FSDP. Yeah, it can scale beyond DDP. So you can fit models that do not fit into one GPU. But it's actually pretty communication heavy. So if you have such a large layer that's uh takes longer to collect than to compute, you will be bottlenecked by FSDP. That's why horizontal parallelism exists and it could be combined pretty well with GDP or FSDP. An example of result method is tensor parallelism. So

**[6:42](https://www.youtube.com/watch?v=BGTtk3geHoY&t=402s)** it's a way for charging uh your linear layers but not in the way the model parameters are collected but in the way the activations are collected. So if your model weights are large but activations are not you can use TP. So it can scale beyond FSDP even. And uh the caveat here is that DDP and FP both were model agnostic. So you can use them with anything literally but TP is model specific. You can use it only with linear layers. You have to put the communications in your model code and yeah it's synchronous. So you have to wait for communications to be communicated. You can't do it asynchronously unlike FSDP. Yeah that's another one method. So I've told you

**[7:31](https://www.youtube.com/watch?v=BGTtk3geHoY&t=451s)** about splitting the data about splitting layer but what about uh a case when we want to train a model on long context so if we get 1 million tokens sequences they will all land on each GPU and obviously won't fit. That's an example like uh somebody trying to fit a large sofa onto a small car. Yeah. So we somehow have to split the sequence two and yeah before telling you about contextualism I want to tell you about the al collective communication. So previous methods heavily relied on uh uh all gather that's just you collect part of the tensor and concatenate them and all reduce uh that you collect different tensors and and sum them average them.

**[8:20](https://www.youtube.com/watch?v=BGTtk3geHoY&t=500s)** Yeah, or reduce is a bit uh harder, a bit more complex, but it's really useful for context parallelism and some other paral methods. So, it works the way that every GPU sends a different piece of data to every other GPU. You can imagine some postmans doing some deliveries here. It's just like a transpose or a shuffle operation across GPUs. And yeah, it's used for one of two context parallelism methods. one of connected methods that's called ulysis. It's from deep speed if I remember it correctly. Yeah. And it works the way that yeah the sequence is split just like in data paralism we split the batch we split data we share it now we sh the sequence by the cland dimension and each GPU uh

**[9:10](https://www.youtube.com/watch?v=BGTtk3geHoY&t=550s)** will know only about a part of the sequence and uh this way we can compute all the layers like MLPS layer norms embeddings and so on but for attention layer there's some trick uh for attention we do a swap using an alter operation. So in four uh four activations entering the intention layer will have like uh full hidden state for some part of the tokens and we'll exchange it to a partial hidden state for all of the tokens like we can convert sequence dimension to uh hidden state dimension in order to compute uh attention heads in a distributed way. So if you remember how multi head tension workshes

**[9:58](https://www.youtube.com/watch?v=BGTtk3geHoY&t=598s)** your hidden states into parts and now you can compute each part of that using different GPUs. Yeah. And then you'll reverse that using another al operation and yeah that's context parallelism. Yeah. Uh so uh if you can scale context parallelism for instance if you have uh less tension heads than your cardality for context dimension you can use another method that's called shrink attention it's more complex it works like distributed flesh attention actually yeah uh you also split the sequence so it GPU contains only a short of the full sequence but you don't do these tricks with hidden states you just uh keep uh the sequence split and uh change how the scale report attention

**[10:47](https://www.youtube.com/watch?v=BGTtk3geHoY&t=647s)** operation is computed. Yeah. Now it is described more preciously in uh an ultrasale playbook from hugging face. It's a great material. So yeah, if you want to know more about ring attention, you can just read ultrasale playbook. It's a great thing from hugging face team. Yeah. And uh the prefal one horizontal prism method is also special. So context was special for attention and now uh all the new models are mixture of expert models and yeah we have a special par fore models too. It's called expert parallelism. Yeah uh it works uh somehow similar to context parism that it uses ultral communication but for a different purpose. So uh fore we have a router that decides for which experts a token

**[11:39](https://www.youtube.com/watch?v=BGTtk3geHoY&t=699s)** should be purchased by and then we have different experts. You can think of them like of a set of different MLP networks. Yeah. So for instance quen free uh has uh 128 experts. Each layer has this amount. Yeah. And each token is processed by some subset of this experts. So it's sparse and uh yeah experism utilizes this sparity. So after router has made some decisions we can run an ultral communication to send all the uh local tokens to uh all the GPUs that host experts that should uh compute these tokens. So yeah it's also useful. It's used heavily nowadays in novel models. Yeah. And that's all about

**[12:28](https://www.youtube.com/watch?v=BGTtk3geHoY&t=748s)** horizontal parallelism. And yeah, let me talk a bit about pipeline parallelism. It's a vertical parallelism method that allows you to cut the model by depths. So you have some model that has some layers and you just split it by layer from 1 to 8, from 9 to 10 and so on. Yeah. Uh and then you run your execution in microbatches. So for instance, GPU1 hosts stage pipeline stage one. It processes microbatch one. Microbatch 2 sends the activations to GPU number two that hosts next layers. They pro two next next and uh vice versa for the backward pass. The most important thing here is that we have a lot of pipeline bubbles. Yeah. So these are the times where our GPUs are underutilized. So if

**[13:17](https://www.youtube.com/watch?v=BGTtk3geHoY&t=797s)** we can compute anything, we'll have a bubble. Yeah. And there are some novel pipeline parallelism schedules that can tackle with these bubbles. I've shown you the simplest one. It's called Gpipe by the way. Yeah. And it has bubbles, but some schedules don't have bubbles. Yeah. And the most important thing about panism is that it's best for mult setups because sending communications is really cheap. It's uh not that expensive. For instance, if we compare to sending model weights in FSDP or sending big activations in tensor poly yeah pipeline communications are really cheap and that's why it's it was heavily used for training GLM5 for instance they used pipeline print for training deepseek all the really large models plus paralism yeah and why I've told you all this

**[14:05](https://www.youtube.com/watch?v=BGTtk3geHoY&t=845s)** information that's because I wanted you to show I wanted to show you the diversity of distribute training strategies and Uh yeah, modern models may combine all of them actually and their developers trying such large models have to think about configuring cardalities for each of this training dimension. Yeah. And it can be pretty tough. Yeah. And also developers of training frameworks have to think that there are actually no like some magical 6D parallel mesh. We can map onto real GPUs. Actually some paris methods are special for some layers. For instance, we can apply exportalism only on layer. We can apply context paralism only on attention layer. So yeah, we have

**[14:55](https://www.youtube.com/watch?v=BGTtk3geHoY&t=895s)** actually got different logical grids that map parallelism dimensions onto real GPU hardware. Yeah, that's also important things that the framework developer should care about. Yeah, and actually the developers utilizing the framework should care about where to sit in that 6D dimensional space. So what train configuration should they they use for exactly their own cluster their own model to get the best model utilization. So there are a lot of problems we have to solve. Yeah and actually yeah we can solve this problems with boptors but actually nobody does it. We need some tooling for solving this and now I see the landscape for this tooling that in a way that we have two species actually.

**[15:44](https://www.youtube.com/watch?v=BGTtk3geHoY&t=944s)** First one is monolyic frameworks and another one is falcon go repositories about monoliths there is a great example that's called megatronm it's battle tested and it's really fast and nvidia team did a really good job here so if you want to train some uh model using some regime that's supported by megatron out of the box it will work it will work fine it will work fast it will train a model for you but uh if you want to change anything for instance if you are a research laboratory creating a new parallelism method or a new model that's not supported obviously by megatronm you have to fight the framework you have to patch it and I usually joke that you have to be a PhD in Nvidia projects to do it properly yeah so it's performant

**[16:33](https://www.youtube.com/watch?v=BGTtk3geHoY&t=993s)** but it's huge it's a really it's a really major project that could slow your research duration if you want it to be fast yeah Another one is an example of a forking door. It's torchon. It was made by the pytor team and I really like to titan actually cuz it gives you full control over every detail. But now every detail is yours to maintain. So if you want to patch torch titan you have to fork it. You have to maintain a distance between your patch for your own local model and the mainstream. So sometimes they may merge it or they may not. So you can just install torch titan and compose it a way you want. You have to fork it modify it but it it works actually for some body pytor team

**[17:23](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1043s)** actually trained llama using torch titan and they were fine with that. Yeah and actually I see a gap here. So we have monoliths that are powerful but rigid. We have homebrew setups for go repositories that are flexible but endless. And I actually wanted to use something that's hackable, modifiable and still reu and still fast and still working fine. Yeah. And that's the reason why I thought about building a custom train framework called D ID. And I imagine that developer experience with D should work in this way. So for monoliths is if you want to change something you'll spend a lot of time for home setups you have to fork and patch them and rebase and repeat it a lot and

**[18:11](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1091s)** for another one framework you can just compose it the way you want and reuse so that's what I want from a developer experience here and actually to make this work we have pinned some principles before even writing the code so this principle should be uh across the whole framework and this princip principles actually didn't came from nothing. It came from a real pain points from tools we used for instance Megatron and Torch Titan. Yeah. So first principle we've paint is that Dad should be composible but not monolithic. So the framework still shall should own the plumbing but the developer should compose it the way he actually wants. So actually D9D should be like a box of Lego bricks. So you still don't uh write your train loop

**[19:00](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1140s)** from scratch but you decide how your loss is computed, how your model is constructed, how it's parallelized across the cluster and yeah you have you you should have a set of interfaces you can extend and set of bricks you can compose and example of this is how our train configuration works. Uh train configurator is an object that does all the dependency injection of our for our trainer object and what it accepts at construction. It accepts device mesh your cluster topology your 6D parallelism setup. It accepts some parameters for instance how often we should lock our loss in uh 1 DB for instance and yeah it uh provides your space for your own interfaces for model

**[19:47](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1187s)** provider and task provider. You fully decide how your model work, how your task provider works, how your loss is computed and also we provide interfaces for optimizing long schedule for instance but we also have an automatic implementations. If you don't care you can just use ours. So you can use default optimizers from PyTorch uh instantiated from pedentic configs. Yeah. And also about the data you can create your fully custom data stack for this. So you can stream different sources onto different date parallelism cardalities but uh yeah if you don't care you can use just our date provider that will take the whole data set that will shard it by indexes indices across the date parallelism axis and yeah it

**[20:36](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1236s)** will just work. Uh so uh you can either build your own thing or use our bricks and that's an example how a compute loss function is defined for supervised fine tuning. So our models when provided labels return lobas uh for target tokens. So you can compute cross entropy you can compute dpo loss gpo loss and all of them. So yeah here we just uh compute cross entropy by summing these lock props and dividing by amount of tokens and also an important thing to note is that we support waiting losses. So uh it's really important for train language models because uh in this case you may have different batches holding different amount of tokens and if you

**[21:25](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1285s)** try to average your gradients across uh that were produced by losses computed by different amount of tokens you will actually have this gradients averaged by the same magnitude. But if we weigh this uh gradient by amount of tokens uh we'll take this into an account and average weights using different magnitudes. So it there was a lot of problems with this in other projects. Even Unslaught guys posted a blog post on this recently and it was fixed in torch titan in previous November. So yeah it's an important problem we thought about. So the developer sees weight loss waiting and he can take it into account and think hm how should I wait my losses in my distribute setup. Another example of composibility is that we provide some

**[22:16](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1336s)** functions for paralyzing horizontally our models from catalog. But these functions are pretty simple actually. You if you want to create your own model with your own distributed train strategy you can just change these functions. You can uh utilize some other paralyze functions written by yourself. By the way, all the internal paral paralization functions from D9D are written using the modern PyTorch dtener API. So it's pretty simple. There are some of them for tensor print for instance in PyTorch. Yeah. And the second principle is that everything should be white box. So the developer should be able to inspect every object in DND. So model should be a real neural pytorn module

**[23:05](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1385s)** and know some layer specs that are constructed somewhere else and there should be no no hidden reflection magic for instance. So the developer uh will should be able to know what is happening under the box and example of this is actually our train loop. Yeah, we have written the framework the way our train loop train loop actually reads like a train loop. So if you have seen some train loops from Py tutorials, you definitely have seen one. Uh our train loop reads exactly like this. So we have some another things like gradient clipping firing even bus events. But actually it just a train loops that just iterates over the data that computes forward backward doing optimization syncing gradients and so on. Yeah. So you can read it and understand what's happening under the box. Another example

**[23:55](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1435s)** is uh how we do splitting models uh for pipelining. Actually we have an example from titan on the left hand side. Yeah it works but uh in my opinion it's not that transparent as I wanted. So in titan it works the way you first construct the whole model and then in another model you have it to be split. So it deletes some models. It uh sets some models to none. It copies your whole model. And uh if you don't know how exactly to titan works, it could be pretty hard for you to understand what's happening. Uh in in D9D, we thought about this and decided that actually each model should be pipeline aware out of the box. So uh actually you can open

**[24:44](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1484s)** your model uh declaration declaration of your model and to read its init forward functions and check what layers are related to first pipeline stage to last one to some intermediate stages. So yeah it happens in one single file and you don't have to uh look over the whole codebase for it. Yeah, that's how we made it. And the third principle is pretty controversial, but I name it that we wanted DND to build like real software. Like we usually see a problem that some ML tooling is underengineered by sping standards. For instance, no strict type checking leading to type checking errors, uh no strict linting and so on. We actually refused to accept that because now we can use the modern uh stack for it and yeah we use it uh

**[25:32](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1532s)** used it and now we have more than 800s of unit tests there will be more we have some strict linting rules some strict type checking from end to end so the codebase is type check from end to end expect for the GPU kernels actually and yeah we have introduced the DP process actually it's some kind of RFC you and think it's uh something like P for Python but DP for D9D. We made it because we wanted the developers to uh design each new large feature clearly. So we don't want it to so we wanted ZND to become a foundational project and any design mistake here is expensive. So uh we introduced RFCS and

**[26:22](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1582s)** we force developers to write some RFCS before coding and it also helps I agents to do implementations better. So you firstly design your DP with clo and then it can implement it in many cases in few shots. So yeah also about recording we have pinned some opinionated design principles and they helped us to um to write a lot of code using code and uh it actually now generates a lot less slope than before. So we are fun so we are fine with it. Yeah. uh and u I've shown you their principles and now I want to show you their coolest in my opinion actually things APIs they produced and uh first one is dispute context. So in

**[27:12](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1632s)** some other dispute tank frameworks there are some problems like uh some frameworks provide you a top level model that owns the source of truth of your distribute topology. Some of them provide some got object that has distribute topology and a lot of other stuff. In DND, we wanted to create a small object like uh that's called distribute context that created from device mesh parameters and it's built once and passed explicitly everywhere. You can use it for creating this logical views I've shown you sometime before. So you can use these views for different layers and yeah you can trace what's happening from enter from creating the distributed context to where it's actually used and it also contains some

**[28:01](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1681s)** u uh utilities for making some distributed aware checks for setting timeouts for your NCCL pro groups. So yeah you can control your cluster topology from one object and that's cool in my opinion. Yeah, another one is that how we handle model states. Actually, in training large models, handling states is pretty complex cuz your model states could easily wait hundreds and thousands of gigabytes. And uh for example, we can take a case where we want to convert some model checkpoint that published on hugging phase to some format of our internal framework, not hugging face transformers. So yeah, we have a large checkpoint. We have a small small machine that can fit all the weight. So

**[28:49](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1729s)** what can we do? Uh actually to solve this problem, we made all the checkpoint proing system uh a graph proing system actually. So check points in ND are graphs. So you can uh create a graph mapper for it stating what nodes it should accept and process, what nodes it should output. You can use some uh mappers that rename some model state keys that merge some uh states. There's an example of one that converts uh separate query and value projection intention layer into a single one. Yeah. So we made a build system for tensors. You declare targets and dependencies and it works. And also yeah you can do flange point conversion and distribute to lunch point conversion actually uh

**[29:38](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1778s)** because you can create your checkpoint mapper declaratively you can uh wrap it into another mapper that will sh all the work across uh distributed system across your uh workers or GPUs or CPUs either depending on which device you'll convert each point and you just run a single method provided by D9D and that's you the way you can convert each points between different formats. Also, it was a pretty cool use case for parameter efficient finetuning. So, if we apply Laura adapters to a model, we have its weights mapped. So, we had in past for instance a model named attention and after applying Laura, it will be transferred to attention.base. So, we wrap it into an Lora into a Laura layer of some kind. Yeah. And it's also a case

**[30:29](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1829s)** so we can create a model state mapper. We can use it to load the the stock model checkpoint on the fly to a new converted model changed model and also we can use uh model state mapping for merging uh Laura adapters into base weights and renaming it from attention.base to just attention for instance. Yeah. Also I want to talk a bit about kernels and how we use them. Actually yeah a great example of uh where we really need custom GPU kernels is a mixture of experts layer cuz if we compute all the experts using a for loop it will be slow. If we use some uh out of the box all operations it will be also slow. So we for instance utilized

**[31:17](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1877s)** DPP project from the deepseek guys to do all 12 communication and we utilized some group gam project from a guy from Nvidia that computes all the experts in one pass in one kernel launch actually. Yeah. And for instance that how we handle the kernels. They are not hardcoded. They are not like spread across the whole codebase. They are strategy. Uh you can clearly see what kernel is used. And yeah we took some other kernels from the community. Uh apart from GP and group game we use flash tension two and four from MR tree. We use cut cross entropy from Apple ML team for fused linear layer and cross entropy and yeah we don't write we don't want to write all the kernels but

**[32:05](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1925s)** sometimes when there's no a good project that provides some kernel we need we can write it for performance needs so we have written our own arms norm kernel by the way now there's another project by tree we can use instead of our own implementation but uh it wasn't in the past so we have written one Yeah, we've written our own swigloo kernels, our own stoastic rounding kernel. Yeah, stoastic rounding is actually a fun thing that allows you to train models in BF16 precision uh in a more precise way. So yeah, how it works actually. So I personally train a lot of models in BF16 using stocastic optimizers. The difference here is that you can compute your updates for Adam. you in FP32 precision and then when you

**[32:57](https://www.youtube.com/watch?v=BGTtk3geHoY&t=1977s)** have to cast your FP32 uh tensors into BF16 precision, you can just you can truncate them but the result will be biased by rounding and yeah to make it unbiased we can just add a random noise to a number in a FP2 precision and then cast it and then it will be unbiased. So expectation of rounded fp2 will be uh the same as uh regular fp2 tensor. That's how that's how stocastic rounding works. And uh yeah actually I see that not many people are aware about it but I'm a big fan of stoastic rounding by the way. Yeah. And for changing the kernels uh for choosing the kernels in cases we have a lot of them there are

**[33:47](https://www.youtube.com/watch?v=BGTtk3geHoY&t=2027s)** some functions that will automatically detect uh kernel the best kernel you can use based on your hardware and on your needs. So because some attention kernels for instance doesn't support some older hardware some kernels doesn't support attention syncs that are needed for GPT OSS model for instance. So yeah we have small functions for auto detection. So no hidden invariance. So no detection spread across the whole codebase. You can you can read one function and uh understand what happens here. Yeah. And also I want to show you how our forward backward execution engine works. So each train loop should actually answer two questions. How long do we train and what does one step fit the model? And actually in some train loops I saw uh

**[34:36](https://www.youtube.com/watch?v=BGTtk3geHoY&t=2076s)** every train loop component answers these questions a bit each of them. So there are some invariance related to length of a data loader to some configuration to some uh uh other things and in DND we actually wanted to create single abstraction that will own all the data stuff. We created a microbatchp stream. It's uh this protocol is uh is uh is implemented already by a stateful data loader from torch data package for instance. So if you don't write your own one, you can just pip install to data and use stateful data loader as a microbatch pack stream. But if you don't want to rely on to data, if you do if you do want to create something custom, you definitely can just use our our

**[35:24](https://www.youtube.com/watch?v=BGTtk3geHoY&t=2124s)** protocol. Yeah. And it will answer the duration question and the deadstream question. Yeah. Uh and also uh that's uh a thing I want to say about how we do granulation pipelining. So in some out of train loops for instance in torch titan I saw the guys made different code path for pipelining a different code path for gradient accumulation different code path for regular forward and backward. And uh in my opinion it was uh pretty uh clutches in a way that you as a developer uh should read all the code path and understand them. Actually in DND we managed to merge all these path into a single one uh by creating a little virtual machine that will do all

**[36:12](https://www.youtube.com/watch?v=BGTtk3geHoY&t=2172s)** the forwards backwards communication for pipelining. Uh and yeah actually we took this idea from a tosh.pipeline package. So pytor guys did a really good work here. So actually we uh used their idea we refactored it a lot because for the implementation didn't support passing arbitrary py trees in between pipeline stages. Uh we wanted to pass arbitrary py trees for instance. So yeah actually we created a virtual machine for forwards and backwards. So it's uh the one uh VM that runs grain accumulation uh trivial case and pipelining and also we do support so-called zero bubble pipeline parallelism. Uh that's a thing that allows you to tackle with those nasty bubbles in some slides

**[37:03](https://www.youtube.com/watch?v=BGTtk3geHoY&t=2223s)** before I shown you those and yeah the problem there is zero bubble pipeline parism requires you to do a backward pass in a special way. So you have to uh separate it in two stages. First stage is backward from outputs to inputs. So they can be sent to previous pipeline stages and second one is backward from intermediate activations made from the first stage uh to uh model weights. So you can optimize. Yeah. And PyTorch actually supports this but it is it contains some problems some bugs and we have reported them to the team and they are working on it now. But for now uh since these bugs are not fixed we had to create our own hacks for making ZBubble

**[37:52](https://www.youtube.com/watch?v=BGTtk3geHoY&t=2272s)** pler block and I believe someday these problems in PyTorch will be fixed and yeah we can remove our own implementation just use the one from the guys. Yeah, also I want to show you that uh we support collocated reinforced learning by uh uh allowing the developers to put our trainer onto sleep and uh wake it. So you you can offload all the states from GPU memory to CPU memory and it will work. So it's also a great demonstration of our event bus system. So actually you can subscribe to some train events. You can do some work there. For instance, put a trainer onto the sleep, run your rollouts, wake up the trainer and integrate it with some

**[38:40](https://www.youtube.com/watch?v=BGTtk3geHoY&t=2320s)** uh uh a setup, reinforce long setup. Yeah. And the whole thing actually if we take a pretank scenario of Gwenfree MOE model is not that large. It actually provides all the interfaces implementations and it provides all the idea. It even provides a custom data stack and uh yeah it's actually just under 300s of lines. So yeah it's not a oneliner definitely you have to write some code with DND but the the win here is that you can customize anything actually. Yeah. And where we run D9D actually we run it at toka and we use it to continually pre-train not pre-train from scratch but continually pretrain

**[39:29](https://www.youtube.com/watch?v=BGTtk3geHoY&t=2369s)** from uh original checkpoint uh quen free model we use for mid training of 235 billion parameters quen free model also I personally use ng at home for some uh uh small scale classification and post training uh on my single GPU even without a distributed cluster and and that's as the same framework for these two different cases and I really like how it goes that I can actually write the same code and it will be working on a single GPU on multiple GPUs on one billion parameters model on 200 billion parameters model. Yeah, I like it. And actually, yeah, D9D actually started uh with that. I had a really a really good uh end of the previous year. I I've

**[40:20](https://www.youtube.com/watch?v=BGTtk3geHoY&t=2420s)** created that idea and create the framework and uh yeah, it actually now works in 6 months. We got it working in TOKA in my own projects, but there are some things to finish before the 1.0 release. Actually, we have to add more models because now only the quen free is supported. Quen 3.5 is on the way and we also have to add all the GileM ki models. Uh yeah, we have prepared with for it. We already support gated data networks for instance. But yeah, we have tried the models. We'll do it somewhere in this year. We also want to refactor context parallelism because initially we were using pytorch one but pytorch do not support uh flesh attention for it. Uh it's context parallelism

**[41:09](https://www.youtube.com/watch?v=BGTtk3geHoY&t=2469s)** implementation relied on torch sdpa uh prop package. So we actually want to write our own engine utilizing some other text implementation. We have seen some pro for instance from guys that developed chemia also tensorism now is unfinished in D9D not because it's somehow somehow complex to add it but just because nobody from our team actually utilize it so we'll still add it but not now yeah and we want to add some examples beyond supervised puning because we want examples for classification for embedding embedding models for reinforced learning and so on yeah that's a road map we what of things we want to do before link 1.0 So yeah actually we also are experimenting with

**[41:57](https://www.youtube.com/watch?v=BGTtk3geHoY&t=2517s)** our own reinforced learning framework. There are only experiments for now. So don't expect it to be released anywhere soon. It may be not even released uh even because uh we still did not confirm that our rail framework is fine. We didn't finish writing it. So yeah we are working on it. we can uh ship something or we we cannot just saying that we are working this kind of stuff too and yeah you can find the project on GitHub it's fully open source it's licensed under app 2.0 license so yeah you uh can stir what's happening there I'll personally advise against using it before 1.0 comes out but you can check the code you

**[42:46](https://www.youtube.com/watch?v=BGTtk3geHoY&t=2566s)** can contribute you can run some workloads that are supported for now and yeah the team the core contributors are there there's Daniel Vlad me uh all these guys made D&D happen and yeah thank you all for your attention you can ask the questions now I can answer them >> okay thank you very much if you have questions microphone will be given to you first person to raise hand. Yes. >> One question per person. >> Okay. Yeah. And I don't know if I asking asking too much because yeah I really want to involve and try the framework but I do not have one single H100 but so I maybe I'm asking too much. Uh is is

**[43:35](https://www.youtube.com/watch?v=BGTtk3geHoY&t=2615s)** there any possibility we can use this framework to uh do some post training or reinforcement training upon some very small model? >> Yeah, sure. I use it on my 30 90 GPU and it works fine for this case. Yeah, I do training embedding models, classification models. Yeah, it it will work. Thank you. Uh do you have any uh traceability inside your uh framework to monitoring uh efficiency of training for example uh when your trainer is sleep too long uh and uh your roll out of and uh wise exchanging uh allocate too much time.

**[44:26](https://www.youtube.com/watch?v=BGTtk3geHoY&t=2666s)** >> Yeah. Uh for the trainer we do support PyTorch profiler. So we have integrated with it. So you can just enable it in the config and watch the traces using perto. It's a great tool for view profile traces. Yeah. And it will work. And for the reinforced learning, we still haven't finished making out real framework. So there not yet. But for the trainer, yeah, you can start file. >> Um, thanks. Um I I was wondering if D9D gives some kind of starting points when you have a specific hardware you don't know where to start from like will it kind of help you guide what type of parallelism strategy to choose from what like hyperparameters or learning rate is is there something like like user guides

**[45:15](https://www.youtube.com/watch?v=BGTtk3geHoY&t=2715s)** or maybe like some defaults that you can use? >> Yeah. Uh I transfer that for the introduction into how to configure distributed setups. You can really read the ultras scale playbook from the hugenface team. It's great. I really loved how the guys made it. Uh and for the DND, I plan to make a quick introduction guide for small scale, for midscale, for ultra scale, but a bit later when 1.0 comes out. Yeah, thanks for questions. >> Thank you for the talk. Just wanted to ask if it is possible to fine-tune an already made model. I didn't hear >> fine tune. >> Fine tune. Yeah, we can use it for finetuning. I use it to fine-tune queree embedding models and quen free classifiers and free large language

**[46:04](https://www.youtube.com/watch?v=BGTtk3geHoY&t=2764s)** model. Yeah. So yeah, it's it's suitable for funing too. >> Thank you for the talk. Have you tested your library to see what kind of MFU you get? >> So sorry I can't >> Have you tested the the library to see what kind of MFU you get? Uh yeah about the MFUS we didn't do the benchmarks yet but we did a lot of uh a lot of exercises with torch profiler so we eliminated a lot of CPU GPU things for instance we even have found one in lier kernel library that's why we are not using it but about the MPU we will benchmark it and report on different cluster size and setups when 1.0 talk comes out. So now we're building the infra and then we'll measure and yeah now we just use storage profiler to find

**[46:54](https://www.youtube.com/watch?v=BGTtk3geHoY&t=2814s)** the big nasty stuff.

---
id: a2As90UbmcQ
title: "Michel Semaan - Querying the queries: SQL Metaprogramming in Python | Pydata London 26"
slug: michel-semaan-querying-the-queries-sql-metaprogramming-in
conference: pydata
conference_name: "PyData"
category: "Practitioner AI conferences"
edition: "PyData"
year: 2026
speakers: ["Michel Semaan"]
channel: "PyData"
duration_min: 35
published_at: 2026-06-15T15:54:11Z
video_id: a2As90UbmcQ
url: https://www.youtube.com/watch?v=a2As90UbmcQ
youtube_url: https://www.youtube.com/watch?v=a2As90UbmcQ
tags: ["Python", "Tutorial", "Education", "NumFOCUS", "PyData", "Opensource", "learn", "software", "python 3", "Julia", "coding", "learn to code", "how to program", "scientific programming"]
topics: ["Data engineering & MLOps", "Evals, observability & reliability"]
transcript: true
---

# Michel Semaan - Querying the queries: SQL Metaprogramming in Python | Pydata London 26

**Michel Semaan**

`PyData` · `PyData` · `2026` · `35 min`

`#Python` `#Tutorial` `#Education` `#NumFOCUS` `#PyData` `#Opensource` `#learn` `#software` `#python 3` `#Julia` `#coding` `#learn to code` `#how to program` `#scientific programming`

[Watch the recording](https://www.youtube.com/watch?v=a2As90UbmcQ) · [Conference site](https://pydata.org/)

## Description

Large SQL codebases inevitably accumulate duplication, inconsistency, deep nesting, and subtle logic errors, making refactoring slow, risky, and often unrealistic to do by hand. This talk shows how Python metaprogramming can turn SQL itself into data that can be analyzed and transformed safely and automatically.

Instead of relying on fragile regex patterns or manual inspection, we use Python to parse queries into Abstract Syntax Trees (represented as nested dictionaries) using libraries such as sqloxide. Once SQL itself is encoded as data, entirely new workflows become possible.

The session walks through practical examples of treating SQL programmatically via tree operations in Python: computing subquery depth for linting, wrapping all denominators in NULLIF() with a simple AST rewrite, auto‑aliasing aggregate expressions, and generating dependency graphs of temporary tables used across pipelines, among others. Each example highlights how metaprogramming enables precise, automatable refactors that would be error‑prone or impossible through text manipulation alone. This talk is designed for analytics and data engineers who work with large SQL codebases.

SQL sits at the heart of most analytics and data engineering work, yet the way we maintain SQL rarely scales with the complexity of our pipelines. As codebases grow, SQL tends to accumulate structural debt: duplicated logic, subtle inconsistencies, deeply nested subqueries, and transformations that are difficult to apply reliably. Teams often end up relying on manual pattern‑matching, ad‑hoc scripts, or one‑off rewrites, approaches that are fragile and nearly impossible to generalise.

This talk presents a more systematic solution: treat queries as manipulable data through metaprogramming in Python. Instead of working with SQL as raw text, we use Python to parse queries into Abstract Syntax Trees (ASTs), unlocking the ability to inspect, analyze, and modify SQL with precision at scale.

After introducing the intuition behind SQL ASTs, we walk through what they look like in practice using Python libraries such as sqloxide. With queries represented as nested dictionaries, we can traverse them, detect patterns, and apply targeted modifications without breaking syntactic structure. The session demonstrates several real examples that highlight the power of this approach: evaluating subquery depth for complexity diagnostics, adding defensive transformations such as wrapping denominators in NULLIF(), generating consistent aliases for aggregation expressions, and extracting table references to infer dependency graphs across staging or temporary‑table‑heavy pipelines.

Rather than offering a single tool or framework, this talk focuses on the underlying metaprogramming techniques that empower engineers to build their own SQL analysis and refactoring utilities. Attendees will leave with a clear mental model of how SQL parsing works, how ASTs can be manipulated in Python, and how these patterns can be applied to enforce standards, build linters, or automate large‑scale refactors.

Background required:
- Intermediate familiarity with Python (nested dictionaries, basic tree algorithms).
- Intermediate familiarity with SQL (CTEs, subqueries, aggregates)
- No prior knowledge of compiler theory or ASTs is assumed

www.pydata.org

PyData is an educational program of NumFOCUS, a 501(c)3 non-profit organization in the United States. PyData provides a forum for the international community of users and developers of data analysis tools to share ideas and learn from each other. The global PyData network promotes discussion of best practices, new approaches, and emerging technologies for data management, processing, analytics, and visualization. PyData communities approach data science using many languages, including (but not limited to) Python, Julia, and R.

PyData conferences aim to be accessible and community-driven, with novice to advanced level presentations. PyData tutorials and talks bring attendees the latest project features along with cutting-edge use cases.

00:00 Welcome!
00:10 Help us add time stamps or captions to this video! See the description for details.

Want to help add timestamps to our YouTube videos to help with discoverability? Find out more here: https://github.com/numfocus/YouTubeVideoTimestamps

## Transcript

*5,728 words · source: supa (en, exact timings)*

**[0:04](https://www.youtube.com/watch?v=a2As90UbmcQ&t=4s)** So, uh, thank you everyone for attending. My name is Michelle and today we're going to go over SQL meta programming. And the initial thing we want to start out by seeing is SQL code bases tend to accumulate structural debt. And that is because unlike Python, Python is a language where there's one and only one way to do things. That's part of the zen of Python. However, in SQL, there are multiple ways to do exactly the same thing. So, for example, in the first row, you can see that you can either write a table as a CTE or as a subquery. And this is sort of an organizational difference. There's no one right way to do things. And even when there is one way, one right way to do things, like in the second example

**[0:52](https://www.youtube.com/watch?v=a2As90UbmcQ&t=52s)** where you can see someone cast a value to zero if it's a null, there's actually a much easier function to do so that not a lot of people might know about. And even in the third example, the uh example you see on the left is a very traditional sort of applying a window function and then filtering out the results uh you know top uh song by artist. This is a very common use case for that. However, there is actually a function that simplifies your life and does it all in one go that not a lot of SQL developers know about. And one of the uh problems with uh maintaining SQL is that essentially if the output is right, you're not really going to care how you got to the output specifically

**[1:39](https://www.youtube.com/watch?v=a2As90UbmcQ&t=99s)** with SQL with SQL because the language itself is simple and you tend not to really think about how you write it. You just care about the report output at the end. So that really tends to accumulate uh um code smells uh sort of anti-atterns and especially once you start to have SQL code bases that are you know 10 20 30 40,000 lines of code long this becomes a little bit of a nightmare to manage. So the central question we want to answer today is how can SQL code quality and correctness be maintained at scale? And we're going to go over an initial example and sort of to motivate why we would want to do meta programming. And the definition of meta programming is programs that process other programs. So

**[2:29](https://www.youtube.com/watch?v=a2As90UbmcQ&t=149s)** usually you write a program that gets a value or takes in user input. In this scenario, we're writing programs that will read other programs. And the initial motivating example is this very very common data pipeline antiattern. It's not actually ideal to write pipelines in this manner, but many many people tend to do this. I've been at multiple companies where this pattern was applied. And in this specific example, we see an initial source table A. And then we have two intermediate intermediary tables B and C that derive from A. And these could be different granularity levels, different group byes. And finally in the output, we join them in table D. And there is in this pipeline due to I don't know uh

**[3:18](https://www.youtube.com/watch?v=a2As90UbmcQ&t=198s)** developer uh laziness or due to sort of we made a change and did not take it out. There is a redundant orphan table at the end uh called E. So for five tables this is pretty easy to put in as a mental model in your head because it's only five tables. But I have personally seen this pattern uh applied to pipelines that are several thousand lines of code long. And that essentially becomes a nightmare to map out uh by hand. So you need something to draw the dependency graph and see that table A is the source for B and C which is the source for D and that E is an orphan and you can even automate the removal of E. But to even get to that level, you first need to create this graph. So a question here. What is your first instinct as an

**[4:08](https://www.youtube.com/watch?v=a2As90UbmcQ&t=248s)** engineer when you look at this script and you want to create uh a graph where the nodes are the tables and the edges are uh the dependency of one table on another. If you look at the script, what's the first tool that comes to mind to do this? A hint, it will involve some sort of text manipulation. How do we extract table A, table B? How do we extract the joints out of this uh script? What's your first instinct? Exactly regular expressions. So the first instinct that you would have to analyze text is usually to use regular expressions. And for those of you who are unfamiliar with regular expressions, it's basically a mini language, what we call a DSL, a domainspecific language,

**[4:58](https://www.youtube.com/watch?v=a2As90UbmcQ&t=298s)** because it's not general purpose. It's built only for the domain of uh processing uh text. In fact, just describing textual patterns. That is literally the only purpose for this language. So, we're not going to go over the language itself. Uh this is a sort of very standard tool in the toolkit. Uh I will just briefly explain how it works. Um you basically have symbols that represent certain uh sort of you know a digit or a word or a character and you can specify if you look at the second line here if you want one or two or more and you can even uh specify how much of each character that you want and this language essentially describes patterns of text. If you want a concrete example, you can

**[5:46](https://www.youtube.com/watch?v=a2As90UbmcQ&t=346s)** look here. And if you look at the text, you see order 42 cost $1299. And let's say we want to find all of the numbers in this piece of text, we would write this regax. We want to capture any series of digits. So, and if you want to apply this to our specific uh SQL query, this specific uh uh rag script, and I can zoom in here if you want to see it. This will capture, if you look at the output, uh this will capture any word that comes after either a from clause or a join clause. So, hypothetically, this should give me all of the tables that uh a certain query depends on. And you can see the output in this case is a and b.

**[6:36](https://www.youtube.com/watch?v=a2As90UbmcQ&t=396s)** So this is regax and regax is uh very well studied uh you know it's it's a well-known tool. And if we want to apply regax to the specific example uh we listed at the start it would look something along these lines. So we first want to have two uh regax patterns. The first one is the temporary table uh capturing uh uh uh regax and this will capture the actual table itself the node in the dependency graph. The second one is uh one that captures the dependency the edges. So this will capture uh any table that comes after from or join. And if we look at the output or if we look at the query here,

**[7:25](https://www.youtube.com/watch?v=a2As90UbmcQ&t=445s)** we can see the example we set out at the start. And we will simply all that we will do is simply process it line by line and then try to see if that line captures either the creation regax or the from join regx and that will essentially allow us to create the dependency graph and uh you know represent it as a UML represented in Neo4j. The point is we actually now have this graph and you can see here that E is an orphan and E can be essentially eliminated. So the practicality of building these types of graphs or even the practicality and the usefulness of inspecting your SQL code and optimizing it via code meta programming SQL is apparent. However,

**[8:16](https://www.youtube.com/watch?v=a2As90UbmcQ&t=496s)** I just want to ask you, is this particularly readable? If you're looking at the script and you and you encounter this, I would start to swear. I think this is not readable. This does not tell me what I'm capturing. Uh, Regax is infamously very very not readable. So actually if you really want to build the correct robust uh uh uh capture statement because you know a from statement in SQL can have aliases it can have spaces it can have new lines there can be a subquery in it the cases are almost infinite and to actually build a robust pattern capture for from it looks something along these lines and you know this is monstrous this is a nightmare that I do not want to have to read this or maintain it I

**[9:03](https://www.youtube.com/watch?v=a2As90UbmcQ&t=543s)** cannot debug this if I want to debug this Right. So, Regax does work, but it has many different uh pitfalls. Unreadability is probably the biggest one. One of them also is that it is essentially read only. So, it's a domain specific language built for uh matching patterns of text. That is the only purpose it is built for. It does not handle transformations. So, it can only capture. Uh another one is that it is fragile to formatting. And you know as we just discussed new lines uh aliases if you use back codes to you know uh put in a column name that you will need to account for all of these different cases in the regax uh uh capture uh string itself. And finally and probably the

**[9:51](https://www.youtube.com/watch?v=a2As90UbmcQ&t=591s)** most important pitfall aside from unreadability is that regax does not know that it's editing or capting well capturing pieces of an SQL query. It doesn't know. It's a blind tool for text pattern matching. It is not a tool for SQL code inspection. It can be used for that, but it is not the primary purpose of it. So, if we have a generic tool, probably we can find something that's specific that might be uh more applicable to us uh for this particular uh uh use case. And I just want to reference one last uh thing about the pitfalls of Regux. Uh I'm not sure if you know this infamous Stack Overflow answer. This is like 15 16 years ago where uh someone wanted to

**[10:40](https://www.youtube.com/watch?v=a2As90UbmcQ&t=640s)** create a pattern matcher for HTML and they basically built uh a regax string. You can look at it here where it essentially sort of uh captures a tags and they did not want it to capture other tags and they asked you know is regax the right approach. Is this the right string? And this is one of the most infamous answers on Stack Overflow. You know the guy starts out by stating the reasons why and then you sort of see a steady descent into insanity as he sort of tries to explain more and more that you know this is not the tool for the job. Don't use regax when you have parsers that are built for that. So again, regax will work, but you will hate your life while you're attempting to do the solution in reg. And here there was a little bit of a

**[11:30](https://www.youtube.com/watch?v=a2As90UbmcQ&t=690s)** spoiler for the actual solution that we're going to go with, which is parsers and asd. So an AD is a fancy word uh that simply means how the compiler and uh you know how the machine will actually run the code at the end because as you know in the end all computation you can reduce it down to two inputs and a logic gate and an output and some combination of the above. I'm oversimplifying but that's basically what you can do in the end it's all logic gates. Two inputs and an output looks like a binary tree. So you know theoretically at some point your code is going to be translated to some sort of tree and the a is actually how that uh structure is represented. So if you have this Python code x is equal

**[12:18](https://www.youtube.com/watch?v=a2As90UbmcQ&t=738s)** to a plus b / 2 the actual way in which it will be executed is uh you know it will start at the bottom level it will add a plus b it will get the result of that give it to the division operator which will divide it by two and then it will go to the root level and then assign this value to x and this is basically ass are the eventual fate of all programs all programs get converted to ass In fact, the job of the parser is to take the code as input and to output the ASD. And every language will have its own parser. Many languages have several parsers. The idea, we don't really care about the specifics of each parser. All that we want is to take a query in and output a tree. Once we output a tree, we

**[13:07](https://www.youtube.com/watch?v=a2As90UbmcQ&t=787s)** just have a tree. And the tree is a very well-known data structure. And we can apply all of the classic three algorithms that you learned in university from the FS to BFS to islands to net all of these you can apply them to SQL and actually get meaningful results. So let's look at an example of a ASD. So this is sort of a generic query. We don't specifically care about what it says, but uh I just want you to see how it's actually represented in the output. And uh of course uh in Python we will store this as a JSON as a dictionary because we can nest as many things as we want. Uh we will simply import SQL oxide. There are other options as well. I just used SQL oxide because it outputs a sort of very simple JSON dictionary

**[13:56](https://www.youtube.com/watch?v=a2As90UbmcQ&t=836s)** that we can sort of uh inspect. But if you want to look at the output on the right hand side, you can see that initially we have a width. So this is a common table expression and that is basically represented in the query in the uh dictionary and actually in the dictionary what we find is that the key represents some semantic concept in SQL whether it's a clause a filter whatever an alias and the value is either some nested structure or the thing itself and I'm just going to give you an example the alias for this common table expression is t. So with t as select x from y, we can look at the alias and find it in this particular part of the uh dictionary.

**[14:45](https://www.youtube.com/watch?v=a2As90UbmcQ&t=885s)** What do you think would happen if I mutate this t and I call it f? What would happen to the original query? If I changed the the value of t here to x k y, what would happen to the original carry if I uh translated it back? It would change basically it would the with t here would become with x with k with y. So what is the idea here? We change the query into a JSON to a dictionary. We modify the dictionary and then we return it back as a query. And that will allow us to basically ad hoc change SQL queries. We are writing a Python script that is taking a JSON representing an SQL query and either

**[15:35](https://www.youtube.com/watch?v=a2As90UbmcQ&t=935s)** just inspecting it or modifying it and returning it back as a new query. And the a is always onetoone with the code. So uh it it's you can rely basically that you know one code always has one as representation and vice versa. So this is the output. uh and you know if you even want to look later on below you can see that you know here we have u select x from y uh this is an unnamed expression because we did not put in a uh alias and if you even scroll down below I'm just going to show you some other things as well here for example if we have a group by function we have an identifier this will basically give us the function that we're using um and you know even here you can already imagine the

**[16:23](https://www.youtube.com/watch?v=a2As90UbmcQ&t=983s)** applications of this you can change functions, change variables, change columns completely ad hoc. Completely ad hoc. It's simply uh a modification of a uh dictionary. >> Yes. Go ahead. >> So yes, so basically uh if we scroll down below there should be a from here it is. So basically the key is always either like a semantic concept in SQL or the clause itself. So a from or a select or whatever and the values within it will give you the tables, the joins etc etc. So uh what I usually do is like I I obviously don't memorize the structure. So I just output whatever I want. I just

**[17:11](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1031s)** look at how I want to modify it and then target that small part of the JSON. I'm actually going to show an example at the end. Uh obviously this is not meant for like human consumption. >> So it's not uh you're not meant to read the entire thing. You're meant to read the parts you want to change or the parts you want to inspect and work only on those. So like you can basically ignore the majority of the uh tree and just target the the thing you actually want to inspect on it. But this is where it would be stored. There's a specific from key that's in the query itself. So if you modify the the T the W with T >> then then it gets update. >> Yes, it won't get updated here automatically here because it's you will need to do it manually. Uh not manually like with the uh script. Uh it doesn't it's not reactive if that's what you're

**[18:01](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1081s)** asking. But it's quite easy to just modify it with the script itself twice. But it's not reactive. No. Uh any other questions? So actually let's go back to the initial example and redo it with as the primary difference is that you no longer have these weird reax uh strings that are essentially unreadable. You actually have something that is aware that we're working with tables and is aware that we are working with uh dependencies of tables. So we actually now look at the key that's the table key whether it's in the actual dictionary itself and then we start to build these uh we start to build these uh edges in a

**[18:51](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1131s)** way that again is SQL aware so here we're not building some generic regax we are actually getting a key representing the create statement for example in SQL or the from or you know the where the having the wage. So the most important the most important part of this is we are now working at one level of abstraction higher than we previously were. We are no longer working with a generic matching tool but we have access to each and every clause in the query and we can actually uh uh manipulate them use them and so on. So I want to go through several small examples of how you can actually use this practically speaking. um even starting tomorrow, right? One very

**[19:40](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1180s)** common anti-attern in SQL is you know you have a nested subquery and a nested subquery and a rested subquery and a nested subquery. Usually you don't need to do this 99.9% of the time you maximum need like a subquery depth of like maybe three. 99.9% of the time and usually especially when you're you know a little bit more junior you sort of want to show off that you know how to use recursive subqueries and you usually want to put something in the CI/CD pipeline to you know say this subquery depth is greater than x don't uh or you know at least review it lint it whatever and to note that I have not at least found any SQL lers that do this so that's why I sort of initially wanted to explore that as well. So if the SQL query is a tree, how do we get

**[20:31](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1231s)** the depth of a tree? DFS depth first search. This is an this is three algorithms 101. The first algorithm you learn on a binary tree is probably DFS. All that we're doing here is using DFS to calculate the depth of the subqueries that are nested within. And you can see this is literally just like a standard algorithm. And we can see here that if we try to run it on a query that is uh one layer nested, it returns two. But if it's two layer nested, it returns three. And literally all that we're doing here is whenever we see a select, we see if there's a select recursively that's nested within it. And we're just adding plus ones. That is literally the entire algorithm. And you can just write this as a script in your CI/CD pipeline

**[21:21](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1281s)** throwing up a warning if you know it's greater than some x. So this is one example with what you can actually do now that you have a mental model of SQL queries as trees. The other one is autoal aliasing aggregates meaning you know very I I do this as well when I'm lazy and I just have an aggregate function. I will just do something like select some from a I will not name it. Um many other people use this as well. It's sort of you know developer laziness uh uh on our behalf. Uh and what we want to try to do is to see is there a way to reasonably automatically autoalias all of this. For example, uh sum of a gets transformed to total underscore a some sort of

**[22:11](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1331s)** automatic transformation that happens uh on these um on these aggregates. So before we look at the actual algorithm, we first want to inspect and this I think goes back to your question like you know it's a big tree right? How do I how do I find the from you know how do I modify the T's and so on. What I tend to do at least you know for this example is I I want to look at the sum of a and I want to see how is that represented in the a. So what I did here was I took some of I wrote the query I parsed it I took the select. This part just takes whatever is in the select and this is the uh uh JSON output. This is the dictionary output and you can already immediately spot unnamed expression.

**[22:58](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1378s)** So this is an un unaliased expression sum of a there's something there that we haven't given a name to and you can see uh within the function itself you see a list with the identifier you even see the arguments uh that pass into the sum function and so on. So we know what we want to target. We know what we want to match and what we want to match is an unaliased aggregate uh expression. So we also want to see what is the end result that we want and we essentially work backwards from it to transform the thing on the left to the thing on the right. And what we find is the exact same query when we add as total underscore a to it. When we add an alias to it, it looks different. It looks like this. The key is

**[23:49](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1429s)** expression with alias. That's an immediate change we see. And then we see that for the most part it's relatively speaking the same. All that we're doing at the end is just adding an alias key with the actual value which is total A. So actually what we know we want to do is transform this uh structure to that structure and if we want to add the alias we will add it as the value of this uh key alias.value. So now we have this transformation planned out in our minds. How do we actually execute it? How do we actually write code that does this? So it would look something along these lines. What I first started out by doing was an alias map. So if we have a sum,

**[24:38](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1478s)** we start with we prefix it with total. If we have average, we prefix it with mean. If we have count, we prefix it with n, etc., etc. This is just to make it a little bit more interesting. as an example. We can even map it. We can have custom logic built on top of it. And then with this function, I isolate, if you remember, the key I was just talking about the alias that has the actual name of the uh aggregate function. So this part is isolated there. And then basically what I do is if you find an unnamed expression get whatever is happening in that unnamed expression map it to the alias map see what function is actually there and then return exactly the same uh uh expression except you're adding an

**[25:27](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1527s)** alias. So all that we did was if we go back we transformed that structure preserving the exact computation we transformed it into that structure. That's that's all that this function does and we can see the result immediately. Select sum of revenue average of price count of orders. This will basically if you notice there are no aliases here but the output is you know select sum of revenue as total underscore revenue average price as mean underscore price and count of orders as n underscore orders. So basically this removes all of the hard work for us right if your if the tree algorithm itself is uh uh airtight if the logic is sound and if your transformation is sound this work

**[26:15](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1575s)** can entirely be automated. So this is another example uh in which you can actually use the tree structure uh to do uh uh transformations on mass right like you can basically safely run this on your codebase and relatively speaking be confident that 99% of the time it's going to work and one other example I want to give which is also quite similar to this is wrapping denominators in null if and this is sort of also another classic sort of initial SQL error If you have select sum of A divided by sum of B, sum of B if it turns out to be zero, the SQL query is going to bug out. So how do we automatically identify every single denominator in our entire codebase and just drop it in an olive? No matter what

**[27:04](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1624s)** the expression there in the denominator is, this could be a uh single column, this could be a function, this could be a subquery, this could be a whatever. as long as it's in a denominator, I need to wrap it in a null if. So, how do I actually do this? The process is quite similar to uh to uh what we did before, which is basically I start out by isolating the specific uh structure in the a represents the null if function. So when I did this I found that this is the structured function identifier the value is null if because that's that's what signifies what the function is and then basically in the arguments here the arguments uh uh are it's this is basically a list the first

**[27:58](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1678s)** uh value in the list is the first argument the second value in the list is the second argument and I basically know that I don't really need to care about the second argument I want it to be zero. All that I want to edit is the first argument of this function. So I need to edit this exact position in the schema. That's all I need to touch. I need to touch nothing else. All literally all that I need to do is take whatever was in the denominator and paste it here. That's it. And the other thing we actually also need to know is well how are division operators actually represented uh also in the ASD itself and this is actually expressed as something called binary op uh uh because it's a uh left and right operation it will take the

**[28:46](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1726s)** uh left value which is the numerator as x uh the operation is divide and by the way even looking at this you can already conclude you can arbitrarily change divide to like multiply whatever sum you this is all your SQL query is data that you can edit is basically the takeaway of all of this. Um but actually what we want to modify is the right uh uh key here which is the denominator because we know here that when we wrote select x / y and we parsed it we got that value as the uh denominator. So the actual application of this would be something again very very similar to what we did with the auto aliasing. It's just a different sort of shift. Uh if we

**[29:36](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1776s)** have a binary operation and that binary operation is a divide uh we will simply uh uh copy whatever is there and just wrap it in the uh null if expression that we saw. Just put that expression in the list that we saw here before as the first um uh as the first uh element in this list in the args list. That's all we did. We copied that. We pasted it there. We mutated or transformed the original a and if you look at the example select a divided by b and x divided by y minus z we can see that it will basically capture both cases. So it is robust to whatever is in the denominator. If you have you know a correlated subquery if you have a whatever in the denominator

**[30:24](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1824s)** it will capture it. So this will eliminate one bug forever from your codebase forever. You should never find a division by zero error in your codebase again. just by running this ever. So this is also very useful for defensive programming essentially. And one last sort of quick example just to also show that uh you know there are so many applications of this. In fact there are infinite applications of this. The mental model is your SQL is is data that you can edit and data that you can manipulate. The is the takeaway from this talk. And here for example, let's say we have a very long query. We want to find unused common table expressions because you know sometimes I might try write a long query. I might forget that I you know set a comment table

**[31:12](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1872s)** expression somewhere and uh you know I want to automatically identify candidates for elimination. So if you look at the unused uh CTE uh uh sort of example query we have with select uh with A as select A with B as select B with C as select blah blah blah in the end it tells me that C is not used because it's defined once and referenced nowhere and how did we actually do this also a very similar process this is basically a graph algorithm I will build the edges out if I find an orphan that is not being referenced. If I find a leaf node that's not being referenced anywhere, that is what I will return. Um and also just to end with a couple of other examples. Usually if you have two

**[32:01](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1921s)** queries with the same granularity level. So group by year city group by also year city but like with a different expression on top. Usually you can combine the queries because same granularity queries you can refactor whatever is in the wear clause. You can refactor whatever is in the select to accommodate both of the queries. you can just write uh write a uh a uh a function that uses the SQLs to find all queries and group all queries that have the same granularity level and you can even write code that auto transforms them automerges them. So uh the applications are essentially only limited by your own imagination. uh start to think of SQL queries not just as queries not just as code but as

**[32:50](https://www.youtube.com/watch?v=a2As90UbmcQ&t=1970s)** data that you can manipulate and I will end on two uh small uh FAQs. The first question is this seems like a headache. Why would I not just use an agent to do all of this? And there are actually many reasons why you would not want to use an agent. First of all, a deterministic process is always more reliable by definition than a probabilistic one. uh LLMs are not deterministic. The second one is token costs. I I think enough said. Uh the third one is especially with large language models, LLMs tend to hallucinate quite often. And actually something I found is uh LLMs tend to use a lot of cross joints even though you know 99% of the time um no one uses it's very very rare to use cross joints uh but they tend to

**[33:39](https://www.youtube.com/watch?v=a2As90UbmcQ&t=2019s)** be over represented in the uh code that SQL uh that LLM's output and that is essentially because they're over represented in the training data which are essentially SQL tutorials but the middle ground here is to use the LLM to write the tree algorithms so you don't need to do the difficult work of the parsing. The last one is is Python the best suited language for this? Probably not. You can get by with Python. The thing is that at some point you have to start thinking of you know code as data and data as code. This is actually what homoiconic languages do. Usually lisps you also have Julia also they do all of this. Python will work but you will probably start hating yourself if you sort of take this all the way to production with it. But the main takeaway is your SQL queries are also data. And thank you.

---
id: dYt_fabaK60
title: "Tom Augspurger - GPU Accelerated Zarr - PyData Global 2025"
slug: tom-augspurger-gpu-accelerated-zarr-pydata-global-2025
conference: pydata
conference_name: "PyData"
category: "Practitioner AI conferences"
edition: "PyData"
year: 2026
speakers: ["Tom Augspurger"]
channel: "PyData"
duration_min: 26
published_at: 2026-01-09T17:53:05Z
video_id: dYt_fabaK60
url: https://www.youtube.com/watch?v=dYt_fabaK60
youtube_url: https://www.youtube.com/watch?v=dYt_fabaK60
tags: ["Python", "Tutorial", "Education", "NumFOCUS", "PyData", "Opensource", "learn", "software", "python 3", "Julia", "coding", "learn to code", "how to program", "scientific programming"]
topics: ["Inference, serving & GPU infra"]
transcript: true
---

# Tom Augspurger - GPU Accelerated Zarr - PyData Global 2025

**Tom Augspurger**

`PyData` · `PyData` · `2026` · `26 min`

`#Python` `#Tutorial` `#Education` `#NumFOCUS` `#PyData` `#Opensource` `#learn` `#software` `#python 3` `#Julia` `#coding` `#learn to code` `#how to program` `#scientific programming`

[Watch the recording](https://www.youtube.com/watch?v=dYt_fabaK60) · [Conference site](https://pydata.org/)

## Description

The zarr-python 3.0 release includes native support for device buffers, enabling Zarr workloads to run on compute accelerators like NVIDIA GPUs. This enables you to get more work done faster.

This talk is primarily intended for people who are at least somewhat familiar with Zarr and are curious about accelerating their n-dimensional array workload with GPUs. That said, we will start with a brief introduction to Zarr and why you might want to consider it as a storage format for the n-dimensional arrays (commonly seen in geospatial, microscopy, or genomics domains, among others). We'll see what factors affect performance and how to maximize throughput for your data analysis or deep learning pipeline. Finally, we'll preview the future improvements to GPU-accelerated Zarr and the packages building on top of it, like xarray and cubed.

After attending this talk, you'll have the knowledge needed to determine if using zarr-python's support for device buffers can help accelerate your workload.

## Transcript

*5,230 words · source: supa (en, exact timings)*

**[0:06](https://www.youtube.com/watch?v=dYt_fabaK60&t=6s)** Hello everyone, welcome to this session GPU accelerated ZAR. Uh our speaker today will be Tom. Um and give me a second while we welcome Tom to the stage virtual. >> Thanks MK. Uh cool. Yeah. So my name is Tom Auxburgger. I'll be talking to you today about uh GPU accelerated ZAR. So um first of all just a little bit of background about ZAR. Um it's a it's a storage format for in dimensional arrays. Uh kind of like HF5 if you use that. So uh in dimensional arrays think like you have a big temperature array or something. Um might have val dimensions like XY Z like a height above ground time ensemble model number. You have these you know high dimensionality

**[0:54](https://www.youtube.com/watch?v=dYt_fabaK60&t=54s)** arrays. Um, and these arrays, uh, since they can get so big, um, they'll be kind of chunked up into smaller, uh, pieces along one or more dimensions, that makes it easier for storage and transmission and analysis to to chunk it up into smaller pieces. Um, uh, the individual chunks, they get compressed uh to save on storage costs and can speed up your analysis even. Um, and ZARS, yeah, it's it's used on cloud, HPC, local file systems, lots of domains. I think it started in genomics. Um used a bunch in microscopy. I came across it in geospatial. So domain agnostic format. You can kind of arbitrarily nest like groups and arrays inside of those things. Whole zar hierarchy. Um today we're just going to be talking about arrays and uh the chunks that are inside those arrays. And one additional thing

**[1:42](https://www.youtube.com/watch?v=dYt_fabaK60&t=102s)** something that's new in Zar version three is the concept of shards sharding. So uh the idea behind a shard uh you still at the top level you still have the arrays this large uh logical individual array and at the bottom you still have the chunks these individual pieces of the array. Um but if you take a bunch of chunks compress each of those individually and then concatenate those bytes together um that gives you a shard and that shard is what's actually written out to the file system or the object storage. So again, the hierarchy is arrays and then shards are collections of chunks that have been written together to a file. Um, shards are great for both like object storage and HPC file systems just like having fewer files um on the actual file system or object storage is generally uh good for these things. Um, it's also great

**[2:32](https://www.youtube.com/watch?v=dYt_fabaK60&t=152s)** for GPUs. Um, as we'll see in a bit, gives us this additional layer of concurrency that we can use to parallelize stuff which is which is good. um all Zara workloads at a high enough level look pretty similar you know reading some metadata and data from storage uh decoding those bytes so you run it through this codec pipeline to decompress and transform the bytes into an array and then perform the computation so here's a quick little example using xray x-ray if you haven't used it it's like this uh library for working with these types of labeled in dimensional arrays um we can read some data from Google cloud storage um x-ray has this highle syntax for slic slicing, you know, give me the uh values at this particular coordinate. This day, it knows how to translate that down to that's, you know, these uh coordinates map to these specific chunks in the ZAR

**[3:19](https://www.youtube.com/watch?v=dYt_fabaK60&t=199s)** array. Go ahead and read those out. Do your computation. In this case, doing a difference between two observations or uh model outputs in this case and then making a nice pretty plot. So, that's the type of data that we're we're working with in ZAR. Um, Zar Python 3 uh has had this nice uh little feature to kind of natively use uh Nvidia GPUs for um for its like uh arrays once you once you get data out of it. So you can uh with this oneline change you can open up an array and then when you index into that array whereas you typically get a numpy in the array living in you know host memory using the CPU for operations instead with this configuration you'll get back a kupi in the arrays that lives in GPU memory and it uses CUDA kernels uh to do the computations. So that's

**[4:06](https://www.youtube.com/watch?v=dYt_fabaK60&t=246s)** relying on coup this library that gives like a numpy like API but on on the GPU. Okay, a little bit more background about GPUs. Um, you know, for for those who are maybe more on the Zar side and are interested in GPUs. So, they're massively parallel processors, right? They're extremely fast. Like, I'm consistently surprised at how fast they can be for the right problems. Um, you know, traditionally that's like uh rendering pixels for video games, right? AI and ML workloads. Um, I'm on the rapids team at NVIDIA, so we're working on like data science, data analysis type problems. Um it's like a lot of different types of problems can be accelerated with GPUs if you set things up properly. We'll be going into that in detail. Um GPUs typically often um not always but often have like separate pool of me memory like physically separate uh

**[4:55](https://www.youtube.com/watch?v=dYt_fabaK60&t=295s)** memory on the chip that is just for the GPU. Um this uh memory is relatively fast. Okay. So on an H100 GPU you have like three terabytes per second from the GPU to the GPU cores. Um but it's relatively small, tends to be relatively small. Um something like 80 gigabytes on this particular chip, which a big improvement over previous generations and newer ones have even more. But uh broadly speaking, they GPUs tend to have less uh RAM, less memory available. And then the interconnect between the host operating system and the device, the GPU, if you want to move data from the host to the device, tends to be relatively slow. So like theoretical uh max over PCI is like 128 gigabytes per second. So relatively slow interconnect here. Um all this kind of means you want

**[5:44](https://www.youtube.com/watch?v=dYt_fabaK60&t=344s)** to be kind of careful with uh GPU memory since you tend to not have as much of it. Um and you want to be careful about data movement. Ideally you pick a spot in your pipeline where you move data from the host to device and you kind of leave it there over on the GPU, do all of your work and then bring it back at the very end. You don't want to be moving a bunch of data back and forth between the host and the device. Um, we're going to talk a bit about like um about how to program GPUs. Well, um, the main trick is, you know, just keeping them busy. Presumably, you're using GPU because you care about performance and you want to get the most out of it. Um, today we're not going to be talking about like how do you write like an actual GPU kernel. The kernel is just like a basically a function that runs on the GPU. Um, there's many many talks about like how do you do that with Nvidia GPUs and CUDA. We're not going to

**[6:32](https://www.youtube.com/watch?v=dYt_fabaK60&t=392s)** be talking about that. We're going to be focused on kind of higher level things uh before we get to actually calling kernels. So, um want to take like a moment just to think through like okay, we have some code and how does that actually run on the hardware at a very very high level. Um so on the CPU things are fairly straightforward. um you know we don't know know exactly what read chunk is doing but assuming it's not doing anything weird with like threads or async or whatever it's going to run pretty normally like it's going to do its thing going to get its result return it and then our program will move on to the next line right it's going to do its thing uh compute the value return it control flow flows on to the next line of the program it's kind of this very linear thing uh all makes sense nothing too surprising here um on the GPU though things do kind of get a bit more

**[7:21](https://www.youtube.com/watch?v=dYt_fabaK60&t=441s)** complicated um because we've got these two things. We have the host, so our Python program in this case, it's running on our host, our CPU. Um but we're offloading work to our device, to the GPU. And so we kind of have these two things going on. Um when you're programming GPUs, um you know, in order to keep them busy, you're kind of have these uh two things. You want your host to kind of stay ahead of your GPU essentially. So, it's going to be doing things. Uh, maybe it makes some sort of CUDA API call here to tell the device, hey, I'm giving you some work to do. Go do it. I'm not waiting around. Okay, I'm moving on to the to the next thing. So, our host program has already moved on to this uh second read when this uh first read on the device maybe hasn't even completed yet. So, I kind of have this uh halfway shaded here because

**[8:09](https://www.youtube.com/watch?v=dYt_fabaK60&t=489s)** like this is currently in flight. The GPU is working on it, but it hasn't finished. But our Python program, our host has kind of moved on and it's, you know, maybe even moving on to like transform. So now we're calling a function on the result of this function and maybe it's not even ready yet. And so like what does that mean? So it's a little takes a little getting used to. There are reasons for it, but we do have these two layers here. There's what the host is doing, which does kind of map back to that linear control flow in your program, right? Just stepping through these. That's just on the host side, and that's could be potentially could be ahead of what's happening on the GPU. Okay, if you do need to bring these into uh back into line for whatever reason, uh there's this uh at least in Cudalan, there's this uh default stream that everything by default runs on. And if you need to uh bring them back together, you can call stream.synchronize.

**[8:57](https://www.youtube.com/watch?v=dYt_fabaK60&t=537s)** Um, one thing I'll note about like this program, and this will be true of the ZAR workload we're taking a look at in a minute, is um there's this like additional layer of concurrency here that we could be uh exploiting that we're not. So we have this read operation on A and then we transform A and then we have this read operation on B and we transform B but A and B don't interact and so those could run at the same time or in any order. So um in CUDA we have this default stream but you can also provide your own streams and this is a way of expressing some additional concurrency that's available in your program and telling the GPU hey these things can run in any order um as long as the transform for A happens after the read for A. Likewise for B. So there's a way to tell the GPU, say, hey, there's this additional currency. You figure out, you know, what order to run that on

**[9:45](https://www.youtube.com/watch?v=dYt_fabaK60&t=585s)** based on what resources you have available, what else is going on, things like that. So we're going to be using this uh in our ZAR workloads. So quick little summary there is like in order to get the most out of your GPU, a lot of it comes down to just like making sure your host program is giving it sufficient work to do. Okay. So we're going to be uh using some tricks to uh to make that happen. Okay. Um, I want to take a look now at kind of like a sample ZAR workload. Um, very very artificial. I'll be the first to admit that. Um, a couple things to point out. So, we have a a an array here. So, the the lowest level is a large logical indie array. Um, it's only three gigabytes or so. Um, the workload we're going to be doing is operating just at the shard level. So, we're parallelizing things at the shard level.

**[10:34](https://www.youtube.com/watch?v=dYt_fabaK60&t=634s)** That's kind of like the most data that we need in memory at once. So the, you know, absolute size here doesn't matter too much. Um, what does matter though is that the shard here fits in memory. So we're going to be processing these one shard at a time. Basic workload is to read a shard. Um, you know, do all the decoding and stuff to interpret those bytes as an India array. Um, and then do a bunch of matrix multiplications. So insert your own custom workload here, but the reading and decoding part will look the same. Um the last bit want to mention is that we are using shard. So we each uh shard each file on disk is going to have a whole bunch of chunks in it. So in this case 400 I didn't tune this at all. Um but yeah the idea is that we have at least some chunks and a sufficient number that we can get some parallelization out of the decompression step. Um all the data is dstandard decoded. Um uh yeah and then yeah we're

**[11:24](https://www.youtube.com/watch?v=dYt_fabaK60&t=684s)** going to do our workload here of loading data and a bunch of m mult matrix multiplications on each shard. Um quick little performance summary. We're going to be comparing a few uh implementations. Um these numbers here, I I hesitated to even put them in here. Ignore these numbers. Um also ignore like the you know relative like the ratios here. I don't think they matter too much like at most just care about the you know direction or rough direction of like which is faster than which. So uh comparing a few implementations here. First one's very basic you know zar and numpy implementation. So, we're using ZAR um just from a local file system to to read the data. Um uh local versus remote really doesn't matter here. Um Earth Mover had a really good blog post um on

**[12:12](https://www.youtube.com/watch?v=dYt_fabaK60&t=732s)** like uh getting the most out of your uh uh IO on the cloud. So, from S3, I think their experiment was the upshot of it is like yeah, it doesn't really matter. You can max out the nick and yeah, things will be good whether you're on local remote file systems. Um in this case we're using a local file system read the data into host memory. Um we're using numcodex to do the zstandard decompression and then we're using numpy and whatever uses bloss multiplication. Um and then storing that scaler result somewhere. Okay. Uh second implementation is the same except we've enabled done the zar.config.enable GPU. Um so today that means the IO so the reading of files from the store and uh the decompression that's still going to happen on the host uh and the CPU using num codecs for the decompression

**[13:00](https://www.youtube.com/watch?v=dYt_fabaK60&t=780s)** um and then it's going to copy the decompressed the decoded bytes um over to the device and use coupon for the matrix multiplication. So the difference between here is just the matrix multiplication. Turns out yeah coup and GPUs are fast at matrix multiplication. So we get the speed up here even despite having to transfer the data. Um final thing is a kind of custom codec uh custom IO pipeline. Um I was just kind of curious like so yeah this is faster um but like is it as fast as it could possibly be? And that's what we're actually going to spend most of the time on uh is like no this this like basic Zar Python enable GPU is like very convenient but it's not yet at like the speed of light. We can get faster here. And so we'll be talking about what makes that faster for the remainder of the talk. Um so again just this IO pipeline

**[13:50](https://www.youtube.com/watch?v=dYt_fabaK60&t=830s)** right what does it involve? You have to read bytes into uh in this case host memory and then transfer those bytes to device memory. Um I will mention there is a GPU direct storage is this project from Nvidia where you can kind of bypass the OS entirely for IO. Um, I haven't had a chance to try that out, but would be fun to see what that does to performance. But anyway, in this workload, we are reading bytes into host memory and then transferring the bytes to the to the device. Um, once you have these bytes and the device, you can uh decode them into an indie array. And remember, we have a shards worth of bytes. So, we have a bunch of um bunch of arrays here concatenated together. We want to decompress each of those into an India array. We're going to be using NV comp for that. talk a bit more about that in a minute. And then matrix

**[14:37](https://www.youtube.com/watch?v=dYt_fabaK60&t=877s)** multiplications with two pi. Okay. Um, end of the day, you know, what makes the GPU fast is always going to come down to the hardware, right? You have tons and tons of cores, you have fast memory, all that stuff. That's what that's what makes it fast. And our main goal uh at least like in this uh prototype and I think in general when you're using GPUs is like, you know, make sure you're not slowing down the GPU. Give it enough work to do. Avoid like these pipeline bubbles and slowing it down on the host side. make sure to give it enough work to do. Um, so when I would look at like the initial profile of like the zar.config.nable GPU, notice like a few things that were slowing us down. Um, first thing was uh memory allocation. So asking your OS for memory either on the host side or the GPU, both sides. Um, you know, if you're doing that on a

**[15:24](https://www.youtube.com/watch?v=dYt_fabaK60&t=924s)** critical path for a fast enough workload, um, that can slow you down. Um, and this absolutely applies to the CPU versions too. And so like things with like intermediate um intermediate arrays and like reusing buffers and things like that I think is something that um Zar Python can pick up. So check out this issue if you're interested in that. Um second thing we're going to use is um pinned host memory. So this is a special type of memory. When you ask the operating system for memory, you can say, "Hey, I want you to put this memory here and uh don't move it. U don't like page it out or anything. If system's low on memory, um it needs to not move." and that's what lets the GPU kind of directly access the memory and that's going to speed up our transfers from the host to device. Um, and then the final thing we're going to do is be really careful about using like CUDA streams and this additional layer of concurrency

**[16:12](https://www.youtube.com/watch?v=dYt_fabaK60&t=972s)** to overlap all of these different pieces of like uh reading data from disk uh these host device transfers decompression the actual matrix multiplications kind of want like all of those or as many of those running at the same time. Um, and what that comes down to basically is making sure that your um, host program uh, doesn't slow stuff down. Give the GPU sufficient work. Use CUDA streams to express this like additional layer of concurrency and do more than one thing at once. Okay, let's go through the individual stages then. So, first of all, we're going to read some bytes from disk. Um, so, uh, like I mentioned, we're gonna be very careful with memory allocations and in particular, we don't want to allocate any large buffers inside of this critical loop. So, by the time we've decided, okay, I'm going to be

**[17:00](https://www.youtube.com/watch?v=dYt_fabaK60&t=1020s)** processing this specific shard. Um, we kind of want like all of our memory available ahead of time. Um, this isn't always going to be um, an option for you. Um, like if you're in a Jupyter notebook or something just doing some interactive analysis, probably not an option. But if you do have like a you know an ETL pipeline or something where you're processing thousands of shards um you know maybe you do have this option for allocating a big pool of memory ahead of time and then reusing bits of pieces of that pool for um for processing individual shards. Anyway uh for this workload we do have it available um and we've allocated this host buffer ahead of time. Um you could use uh you know regular Python uh you know bytes or whatever numpy empty. In this case, uh we are using coup x to allocate this uh uh slab of pinned

**[17:47](https://www.youtube.com/watch?v=dYt_fabaK60&t=1067s)** memory. Okay, that'll come uh be relevant in the next section when we're talking about transferring. Um then we're using basic Python, you know, open file object reading into an existing buffer here. Uh nothing special about that. I will note though that this is running on the host, right? So this is a blocking API. Um if it were too slow, then we could potentially offload this to a thread. um when you're reading from remote storage, you're probably using async.io to do a bunch of these uh concurrently. So anyway, I'll just mention that this is on the host. Um and I'll show a bunch of insight systems profiles. Um uh the way to read these is kind of on the bottom here. This is for the first shard. Uh we have what's going on our on our host. So this is running on the CPU reading from disk. It takes about 16 milliseconds to read this um particular chunk or sorry shard. Um, and

**[18:37](https://www.youtube.com/watch?v=dYt_fabaK60&t=1117s)** while we're reading this first one, uh, you can see up here, this is what our GPU is busy doing, and it's it's doing nothing right now because it's the first shard. Um, but if we look at like the second or third, whichever one this was. Um, you know, it took 21 22 milliseconds to read this uh, specific shard. Um, and while we were doing that read on the host, we're also our GPU is busy doing other stuff up here on on a previous shard. In this case, decompressing some bytes. So that just tells us that we are getting some overlapping of IO on the host and compute on the device. So that's great. Um once we've read data into host memory, we can go ahead and transfer it to our device. Um so again, we've used this kind of special type of host uh pinned memory, right? So the OS isn't going to move it. Our GPU can kind

**[19:24](https://www.youtube.com/watch?v=dYt_fabaK60&t=1164s)** of use a fast path to copy it from the host to the device. Um and then just like on the host side, we don't want to be making large device allocations um on the GPU. So again, we've pre-allocated some slab of GPU memory uh which we have in this coupi indie array here and we're using its set uh method to set the contents of that array to be the host buffer. We didn't really talk about shards like how they work internally, but there's this index. Um, not going to get into that in detail, but yeah, we're going to copy the contents of that shard over to the to the device. Um, this is happening in CUDA land now. So, previously we're just on the host. Now, we're moving to the GPU. So, this is going to happen on a CUDA stream. And this is way too small to see, I know, but the only thing that really matters is right here under my mouse, we have

**[20:11](https://www.youtube.com/watch?v=dYt_fabaK60&t=1211s)** this little red section. So, that corresponds to what happens on the host. And that's this bit of code here. Just calling the set. And that's doing some CUDA meopy async thing just calling a CUDA API on the host and instructing the device hey copy the contents you know from this memory address in host land over to the device um and that so this small thing which takes like tens of microsconds expands to this longer thing like tens or hundreds of milliseconds but it's running at nice like 50 gigabytes per second or so um if I remember correctly to transfer the data so this ends up not being our bottleneck. Yep. Uh great. Oh, and one more thing to mention. Uh it is happening on a CUDA stream. So this thing is uh in the teal is the memory copy and then insight shows the kernels like the actual

**[21:01](https://www.youtube.com/watch?v=dYt_fabaK60&t=1261s)** computation functions in the lighter blue here. And we can see as we're copying the specific shards worth of bytes over to the device, the GPU is also busy computing um a matrix multiplication. So we're getting overlapping between the hosted device transfers and um and computations on previous shards. Um once we've got data to the device, we need to decode it. Right? Right now we just have a shards worth of bytes, bunch of arrays that have been concatenated together. Um we need to know where each uh like each array begins and ends so that we can decode it properly. Um I have no idea how to write a Zstandard decoder. Um fortunately um Nvidia publishes a MV comp library which has a bunch of these in uh codecs including Zstandard. Um so the nice thing about that API and again

**[21:50](https://www.youtube.com/watch?v=dYt_fabaK60&t=1310s)** this is why it marries so nicely to to ZAR sharding is you can provide it a uh basically a list of arrays to to decode. So, we've got this uh contiguous chunk of memory, and we're going to use the shard index, which we again didn't have time to go into, but that just tells us where in this big uh stream of bytes where each array begins and ends. And we can slice into the big array and get a bunch of smaller arrays that are just views on that original array. And then we can tell it, I want you to decode into these this output buffer or these output buffers. Okay, again, we're not making large allocations here. where we can have this again another pre-allocated block of GPU memory to decode into. Um and the other thing I'll mention is um so inv has Python C and I think C++ uh APIs. I wrote this like

**[22:38](https://www.youtube.com/watch?v=dYt_fabaK60&t=1358s)** very simple binding around the C API that gave like complete control over the memory uh allocations that wasn't quite available through the Python API. Um so we get end up getting this like really nice non-blocking behavior where we can control exactly where we decode the bites into. Um, if you run into similar issues, definitely do get in touch and we can figure out a way to make it happen. Uh, but yeah, this is the type of stuff that like ends up working out really nicely because we have a read or decode uh happening here. I forget how long the host side takes. Not super long. It does a bit of setup and then does uh the decompression on on each of those individual um chunks uh decompressing them into into uh arrays on the device and it does it super fast. Um, this is a diagram from my colleague Ace showing a bit about like how this

**[23:25](https://www.youtube.com/watch?v=dYt_fabaK60&t=1405s)** works. Um, if you think about like how do you decode like a single array in parallel with the GPU? I don't know how you do that. It sounds really challenging because it's just like stream of bytes. How do you chop it up? I have no idea. But when you have these bunch of smaller uh LGBO or in this case the standard streams, you're able to parallelize that at the choke level which is great. And then even inside of that some some stuff I don't understand about it is able to do stuff at the thread block level. So you get lots and lots of parallelism. It runs really quickly. Um last thing we're not really going to talk about just the matrix multiplication. Again we're using coup to do that here. We do a bunch of them so it doesn't finish like instantly. Um but yeah we we get the matrix multiplication. I've shown like the warp occupancy at this point. Like if this is your bottleneck, you can start to dig into okay, what is the kernel doing and

**[24:13](https://www.youtube.com/watch?v=dYt_fabaK60&t=1453s)** like how much my GPU am I actually using? Give some more detail than just like a simple percentage, but we won't get into that. That's uh relatively less important here. So um yeah, just to summarize like we had those three implementations. We had the numpy implementation, the ZAR and KPI implementation and then kind of this custom uh custom implementation to understand where the slowdown's at. Um and those came down to a few things. So memory allocations uh or one kind of doing memory allocations inside of like a hot loop where you're processing a shard or a chunk um can slow you down. Um you want to make sure you're using the right type of memory um so that you get these kind of non-blocking transfers from your host to device and once you've gotten the data to the device kind of leave it over there. So we were able to uh we were able to use envy comp for the

**[25:01](https://www.youtube.com/watch?v=dYt_fabaK60&t=1501s)** decompression um get the data over compressed data over to the GPU as fast as possible and then do all of the work over there including decompression. Um at least for this workload and for many workloads but for this our workload we we did have this additional layer of concurrency. Um so we're able to process more than one stream at a time and different streams are able to be at different spots using different um subcomponents of the the GPU and the host system. So we get this really nice concurren concurrency across SAR. So uh sorry across shards. So um these like are generally true things that will help you whenever you're doing GPU like data analysis type tasks. Um but I'm also hoping to bring these like specific improvements to ZAR so that you know we can have the you know ZAR and KPI uh pipeline be as fast as it can possibly

**[25:48](https://www.youtube.com/watch?v=dYt_fabaK60&t=1548s)** be. Um if you want to see any of the code for this it's all on my GitHub here at the CUDA streams sample repository. go and check it out. Um, if you're interested in this type of stuff, definitely do reach out um, to me. My email's here. Um, or you can find me on GitHub or anywhere on the internet. Um, yeah, and thanks for listening. Appreciate it.

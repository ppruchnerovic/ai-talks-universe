---
id: PYXL1tbzNWU
title: "José Quenum + Marthin Thomas - HPC Implementation of a Hybrid Recommender System in Julia-PyData"
slug: jose-quenum-marthin-thomas-hpc-implementation-of-a-hybrid
conference: pydata
conference_name: "PyData"
category: "AI engineering & agents"
edition: "PyData"
year: 2026
speakers: []
channel: "PyData"
duration_min: 23
published_at: 2026-01-09T17:56:54Z
video_id: PYXL1tbzNWU
youtube_url: https://www.youtube.com/watch?v=PYXL1tbzNWU
tags: ["Python", "Tutorial", "Education", "NumFOCUS", "PyData", "Opensource", "learn", "software", "python 3", "Julia", "coding", "learn to code", "how to program", "scientific programming"]
transcript: true
---

# José Quenum + Marthin Thomas - HPC Implementation of a Hybrid Recommender System in Julia-PyData

**Speaker not identified**

`PyData` · `PyData` · `2026` · `23 min`

`#Python` `#Tutorial` `#Education` `#NumFOCUS` `#PyData` `#Opensource` `#learn` `#software` `#python 3` `#Julia` `#coding` `#learn to code` `#how to program` `#scientific programming`

[Watch the recording](https://www.youtube.com/watch?v=PYXL1tbzNWU) · [Conference site](https://pydata.org/)

## Description

This talk discusses a hybrid recommender system implemented in Julia for preselecting job applicants. The recommender system is built using a neural network adopting a hybrid architecture that combines convolutional layers of a graph neural network and a transformer (both encoder and decoder). We discuss the preprocessing of applicant metadata and job adverts to generate a heterogeneous graph. Next, we present the recommender as a model and its training using an HPC.

## Transcript

*3,150 words · source: supa (en, exact timings)*

**[0:06](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=6s)** Hello everyone, welcome. We are really excited for our next session uh with Jose Quinnim and Martin Thomas. Um really excited to learn more about the hy hybrid recommener system in Julia. Um just a reminder to everyone, we'd love to have audience interaction, so please feel free to put your questions and comments in the chat. and we've just got some great information coming our way. So, I'm going to go ahead and turn it over to Jose. Jose, thank you so much for being here today and we'll turn it over to you. Thank you very much. Thank you, Erin, and greetings everyone. Good afternoon, morning wherever you are. So, I'd like to discuss um our implementation of highway recommen particularly the implementation we did

**[0:53](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=53s)** in Julia for an HPC environment. Uh first of all let me give you some background. Typically when you're developing a recommener system you actually need three essential components right the first one is the candidate generation and for the candidate generation you can use two possible techniques uh either content based filtering where you actually look for the similarities between the items and based on the previous preference of the user of the specific user you're targeting for um items, right? You can just do a product and find the similarity and based on that uh recommend new items for the user. The other approach uh common approach is

**[1:44](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=104s)** actually collaborative filtering where you look at the similarity between other users and items and then recommend an item based on what other similar users have actually recommended. uh and and the common and one way to do it is actually to use a weighted matrix factorization which I summarized here. So the idea is to learn actually two latent um matrices, the user representation and the item representation and try and get uh a fl a [clears throat] multiplication of those two matrices that is as close as possible to the uh to the interaction uh um data that you've collected. And you can do it by

**[2:36](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=156s)** taking into consideration uh data uh if if you have a sparse matrix then you can take into consideration data that has been observed and the data that hasn't been observed. Then the next step is for you to score the items that uh the candidates that you generated. You can then do a scoring and then rank them first. And again use additional criteria like freshness, diversity and fairness to rank them to give a chance to you know new new items to actually be recommended or make diversity or or fairness prevail in your recommendation. In our case, our focus was to actually develop recommener system for candidate

**[3:26](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=206s)** pre-selection in a recruitment process. So we have two essential uh components the job adverts uh that we've collected and the different applicant profiles. And so what we do is actually um use some NLP techniques to extract maximum information that we can from uh those two pieces of data and then construct a hogenous graph. Uh in our hogenous graph what we essentially represent uh includes the different job applicant and the job advert. And so we will integrate the representation of the the job adverts and the applicant. And we also the link in the graph actually the edge

**[4:14](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=254s)** will um indicate the score of the applicant show how strong is the the connection between a particular applicant in the job. Overall this is the architecture that we adopted. So we take our initial data um a bunch of um job adverts and applicants and then we construct. So we extract the necessary information and construct a hogenous graph and then we pass that hogenous graph to a graph neural network where we um in fact run several um convolutional layer for representation. Uh once we uh understand the link between the

**[5:05](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=305s)** between the applicant and the job advert then we pass it uh toward a dense layer before we push the information to a transformer to further determine the score uh that um we should allocate to um for an applicant to a job advert. And that's how we eventually get the the recommendation. I'll just keep so the convolutional layers that we considered here actually there are four convolutional layers that we considered in in this implementation. We have GCN, we have the graph attention network, we have graph Sage and RGC. And what we do is actually train different models where we combine these uh

**[5:53](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=353s)** different so we'll run models where we have those um layers individually but we'll also run other models where we have a combination of these layers to further learn and and then compare the the quality of the recommendation. So this is uh the typical operation that happens during the convolution. Uh if the the graph neural network when it's doing the convolution it does two essential operation the updates of the information after aggregating information from the neighbor. So it will take a node and then from that node look for aggregate information from the neighboring nodes and then update it on the current node. So let's uh dive into the implementation

**[6:44](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=404s)** based on the background that I just gave you. So our implementation uses a lot of predefined packages, flux, transformers, distributed and so forth. And so uh we also because of the you can see that our our architecture is quite heavy. So it requires a lot of uh compute resources. So we turn to an HPC environment and the typical HPC environment that we use is a 48 node cluster right where each node has 24 cores CPU. Also the network the interconnect between the compute nodes is actually infin latency infin. So basically to submit a job in our

**[7:33](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=453s)** environment um is just a script right and and we actually using environment environment we're using slum so all we do is just write a batch script that is actually um compatible with slum and what we essentially um provide in the in the script is just some um a description of the resources that will require like how many how many nodes do we want for um our computation uh how many task are we are we executing and how many tasks per CPU essentially we load uh the Julia package and then run the Julia file here I'm

**[8:24](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=504s)** just I use one example where we have a file called model Julia And that's what would eventually run. So we execute that uh script by simply calling the sbatch um command and pass the path pass the name of the script that just wrote on the left hand side. Before we start training the recommener system model, first we do our data uh prep-processing and we do our data prep-processing in two stages, right? The first stage is actually to collect the raw data and then transform them into data frames. So for that first we have our applicants that are actually represented as a CSV file. And so we load them and then just um uh sort the different applicant and create a data

**[9:16](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=556s)** frame out of it. Very straightforward. And and once we do that we save it in a in another in a binary format. The next thing we do is actually load the job adverts and that one requires uh so so so so here the setup is that we get the job advert as a bunch of text files in a folder. So we just provide the the name to the folder and then extract each file and once we extract the files then we build a dictionary where we map the name of the the file right which in the naming we have the the job ID the advert ID actually and then the content that is provided in the text and once we do that then we uh create a new data frame and the

**[10:06](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=606s)** interesting thing is now we have two data frames names, right? One that corresponds to the the applicant, the other one that corresponds to the advert. And eventually we bring both of them together by doing a join where uh the the common uh column is the job ID. So we make sure that both side we have the job ID column and then we simply do a join and merge to the uh the two pieces of data and then uh we proceed uh with generating the embeddings uh because there's a lot of textual information that we've extracted and so we simply use what to uh here to extract the embedding embedding vectors

**[10:53](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=653s)** corresponding to the adver themselves and the applicant profiles. The second stage of our data prep-processing is where we now uh embrace uh mostly distributed environment where first of all we load all the packages that we need. Uh so first of all we require a number of processes um from the slum manager and then we load all uh the packages that we need on all the nodes that have been allocated for our job and then one by one first we we load the applicants remember once we did the first pre-processing of the applicant uh we saved it in a file so now we can just uh reload that file in all the nodes also

**[11:44](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=704s)** embeddings uh which is the kind of a um using the what tove um package we were able to generate specific description for the um for the applicant. So we also load the embeddings of the applicant and finally for the job advert we also load the embedding vector. So we have three embeddings right the applicant the we have two embeddings and the the binary uh representation of the applicant and then we compute uh the occurrences for each job advert how many applicants how many uh applications did we receive then the next the next step for us is to

**[12:33](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=753s)** actually construct uh the graph And so in order to construct the graph is a hogenous graph. There are two types of nodes, right? First type of node is the job advert and the second type of node is the applicant. And so here to construct the graph we just go through the the dictionary that we built for the the job advert and the occurrences like how many applications did we receive and for that and we use that to actually extract the information. So when we're building our graph, we make sure that for each um each job so our homogeneous graph will have small clusters kind of and each cluster will have will take one job adverts and then all the connected nodes will be the applicants, right? And

**[13:24](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=804s)** that's what we're doing in the for loop. and and um in in some cases because we haven't received uh because you don't expect uh all the job adverts to receive the same type of um application the same number of applications. Yet when you're constructing the graph at the end of the day in order for you to pass that graph to the graph neural network you need to have uniformity in terms of dimension. And so when uh you don't reach the maximum number of applications and that's why we compute the maximum number of application and if you don't reach you just kill it and I think that's what's happening in this um this line here what we simply do is just augment with empty uh uh edges right so that we

**[14:17](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=857s)** make sure that all the graphs have the same And then uh here was simply um so this was just a description of the build graph function. This is actually where we are constructing the graph. So we simply pass the sorted occurrences and then all the data that we've loaded the applicants embeddings both the applicant embeddings and the advert embeddings. And once we construct our graph then uh we split the graph that like you would do typically when you're training a model right uh your training and test data right so we our training graphs and test graph we split them 80% 40% and then we use data loader to then uh map um

**[15:07](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=907s)** uh to the data loader to actually create the data set for training the training load and then the testing load. Now the model itself. So for the model itself um I take you back a few steps back. This is our model right. So now we have to our model actually includes the graph neural network and the intermediate layer where we harmonize everything before we pass it to the uh transformer. So this is actually the model that we're implementing here. So we are adopting the typical transformer implementation where we have

**[15:56](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=956s)** six layers for our encoder and six layers for our decoder and then we build a sequence to sequence by combining the encoder and the decoder. So the the the the next step is actually the whole model as I presented graphically is represented in the structure where we have uh a strct uh data data structure that actually uh captures the graph neural network the intermediate interface and the transformer and then we just write a function to um instantiate such a model. Um I I should say that um because we use sometimes so so when you're generating the the graph neural network um you have to decide whether you using a single convolutional layer

**[16:46](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=1006s)** or a combination of convolutional layers. And so if you're using a combination of convolutional layers uh at this stage, you should instead of just passing that convolutional layer, you should use a chain of them and indicate how many convolutional layers are actually being combined in this graph neural network. So in this instance, we're combining the gut convolutional layer and IGC convolutional layer together in our graph neural network. And then here we simply instantiate the model with all the parameters and go ahead and and then we train the model. Here I should also point out that uh typically when you when you build it a

**[17:39](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=1059s)** recommended system one expects you to create a classifier right but here we're doing a a regression because what we want is actually for the transformer to learn how to score, how to actually generate a score for the uh for the um I should say the adequacy or or how much a a candidate or an applicant is suitable for a job application. Right? So here we focus more um our model actually produces um regression and then we train the model based on that. Yeah. So we we simply train the model right and

**[18:28](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=1108s)** then once we uh so we use the once with the function the train model function that is defined uh the next step for us is to then uh train the model on all the workers that have been allocated for um our job and we simply make sure that there's a synchronization and the HPC takes care of um the call to distribute it and and sync The HPC makes sure that um the the different workers are synchronized across the different comput so we don't have to worry about that. One important point is that um we haven't I I haven't used um a package like MPI at all but it is possible that um you consider MPI or

**[19:20](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=1160s)** even Dagger that you use MPI through Dagger or direct MPI in your implementation particularly for the data prep-processing right so if your data prep-processing is so large that you can handle it on a single node what you can do or or you don't want to use distributed for the seven part where I showed some distributed calls. If you don't want to use distributed to do it, then you could actually call NP use NPI to do so. And here I'm just um highlighting the two essential um collective communication um uh calls that you could consider which is all gathered and all reduced. And this is just one example where we take all the job adverts. Imagine that you have all your job adverts in a folder

**[20:09](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=1209s)** and then you want all the different uh nodes that are participating the pre-processing to just uh focus on pre-processing a portion of the file. So what you could do is on the on rank zero make sure that uh you get all the files the name of the folder and then you construct the files. And what I'm doing in assign to rank is actually assigning specific files to a specific right. So I will take for each rank I will jump um I'll jump because I have the size determines how many processes I have right in the communication world. And so when I when I I jump by size, I can then make sure that if I for example I have 25 files and I have four components,

**[20:57](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=1257s)** every uh four steps, I've just keep skipping right? And then I can go back to the the process assign adverts is very similar to what I did in the previous case only that this this is a subset of the the files and this has been assigned to a particular rank. So once it's done right the rank will now have to uh uh communicate once they finish the processing they got the small extracted adverts and now you have to aggregate. So what I want to do is not to gather everything on the rank zero but make sure that all the component receive the copy from all others. Right? So I rather do and all gather uh and and and in this case I'm sharing the keys and the values

**[21:47](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=1307s)** of the the the of the rank advert that has been uh constructed. So it is the adverts that have been extracted from the different runs the different processes and they're all aggregated and and combined on every um every nodes that are participating in in the MPI job. So I think it's time for me to wrap up. And so in summary, what I discuss here is a the implementation I should say of the recommener system that focuses on candidate pre-selection in a recruitment process. It is an a hybrid architecture where we combine graph neural networks and transformers and in between we have a neural network

**[22:36](https://www.youtube.com/watch?v=PYXL1tbzNWU&t=1356s)** that serves as an interface to flatten everything. Um our implementation focuses on a parallel or HPC environment where we take care of the data prep-processing uh using the distributed package and also I just showed you a way of doing it with MPI and we also train the model in the the distributive environment. Thank you very much.

---
id: ZSFfv2cckx0
title: "Chris Rackauckas-Why Julia_s GPU-Accelerated ODE Solvers are 20x-100x Faster than Jax and PyTorch"
slug: chris-rackauckas-why-julia-s-gpu-accelerated-ode-solvers
conference: pydata
conference_name: "PyData"
category: "Practitioner AI conferences"
edition: "PyData"
year: 2026
speakers: []
channel: null
duration_min: 34
published_at: 2026-01-09T17:56:36Z
video_id: ZSFfv2cckx0
url: https://www.youtube.com/watch?v=ZSFfv2cckx0
youtube_url: https://www.youtube.com/watch?v=ZSFfv2cckx0
tags: ["Python", "Tutorial", "Education", "NumFOCUS", "PyData", "Opensource", "learn", "software", "python 3", "Julia", "coding", "learn to code", "how to program", "scientific programming"]
topics: ["Inference, serving & GPU infra", "Training, fine-tuning & model building"]
transcript: true
---

# Chris Rackauckas-Why Julia_s GPU-Accelerated ODE Solvers are 20x-100x Faster than Jax and PyTorch

**Speaker not identified**

`PyData` · `PyData` · `2026` · `34 min`

`#Python` `#Tutorial` `#Education` `#NumFOCUS` `#PyData` `#Opensource` `#learn` `#software` `#python 3` `#Julia` `#coding` `#learn to code` `#how to program` `#scientific programming`

[Watch the recording](https://www.youtube.com/watch?v=ZSFfv2cckx0) · [Conference site](https://pydata.org/)

## Description

You may have seen the benchmark results and thought, "how the heck are the Julia ODE solvers on GPUs orders of magnitude faster than the GPU-accelerated Python libraries, that can't be true?" In this talk I will go into detail about the architectural differences between the Julia approaches to generating GPU-accelerated solvers vs the standard ML library approach to GPU usage. By the end of the talk you'll have a good enough understanding of models of GPU acceleration to understand why this performance difference exists, and the many applications that can take advantage of this performance improvement.

## Transcript

*7,405 words · source: supa (en, exact timings)*

**[0:06](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=6s)** Welcome everyone. I am so delighted that you're here today. I'm Aaron Gray, the grants manager with Num Focus and I'm thrilled to be part of this community. I'm so excited to introduce our next speaker who some of you know may know well, but if not, you're in for a great session. Um, Dr. Dr. Chris Recus is a VP of modeling and simulation at Julia Hub, the director of scientific research at Puma's AI. You may know Dr. Chris as also as the co-PI of the Julia lab at MIT and also the lead developer of Siml open-source software organization. So, I'm really excited about this presentation today. Um, I really encourage everyone to please interact in the chat. let us know if you have any

**[0:54](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=54s)** questions, introduce yourself. Um, we're delighted you're here today. So, I'm gonna go ahead and turn it over to Dr. Chris. >> Yeah, thank you very much for the kind introduction. So, yeah, so what I'm going to talk about today is, as the title says, why Julius GPU accelerated OD solvers are 20 to 20 to 100 times faster than Jackson PyTorch. Now, I think that the why is what really matters, right? You know, a lot of people look at the numbers and just go, "Oh, you know, speed up. just they want just want to talk about like oh I don't believe that kit exists or anything but what I'm going to do is I'm gonna really go into the why and we're gonna explain all the milliseconds about where the performance difference comes from and at the end you should have a much better diff uh understanding of where performance comes from in programs and how we got this performance and what that means about the the use cases and everything associated with it. So I hope

**[1:43](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=103s)** that you know by going into copious amounts of detail we can really kind of make the numbers make sense. So, first let me kind of give a quick start as to you know what we're talking about. So, I'm I'm Chris Recus. I built out this Simo open source solvers in the Julia ecosystem. If you come from something like Python, you might understand it as like, you know, pi sci in Julia. Um it does things like linear solving, nonlinear solving, differential equation solving optimization integrals partial differential equations, right? That that space of sci. It's a little bit different than scypi because it also includes, you know, integration with machine learning. So kind of what you would think about as PyTorch and Jax. Also things like GPUs and also the space of things like Medelica and Simulink. So things around uh symbolic modeling languages uh symbolic computing. So things like simire also kind of folded

**[2:30](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=150s)** in here for that reason. So it's it's kind of an expansive organization but it's it's around these kind of solvers and the utilities around solvers um more generally. Um so this is what we work on and of course we have a lot of these papers. In fact, this one just got into uh um ACM transactions and mathematical software, which explains how our new nonlinear solver, so things, you know, like um uh things like Newton's method and such, how they're able to be, you know, more faster and more robust on on uh things like battery models and and models of chemical reaction systems and spatial and everything, right? So, you can see, for example, Minpac being the thing that the Forran code that is wrapped by Cypi and and also Sundials in here. you can see the brand new methods that are you know what they're doing and and you know the paper goes into the

**[3:17](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=197s)** whole architecture as to how we're able to achieve that performance. A lot of this comes from mixing symbolic tooling into the solver something that we call symbolic numeric methods. So that way it can do things like automatic differentiation but not just automatic differentiation but also discover sparity patterns so that it can use sparity patterns without the user ever having to intervene and give a sparity pattern. So there's a lot of this kind of tooling that that we develop just kind of to give a flavor for what we do. Um but one of the things that we're really well known for is the differential equation solvers right so you know this is just a high level overview of some of the results of differential equations.jl scale. So this is you know the non-stiff OD solvers you have uh for example you know ours library for uh de solve versus you know something like mat lab and python and you know you you could see that for orders of magnitude the same solver or

**[4:05](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=245s)** the same problem solved to different orders of accuracy or uh you know you're able to see in vertical lines you know this performance difference of a few few orders of magnitude right so we generally say around you know 50x to sci 50x to mat lab 100x to to RSD solve but it can be dependent a lot on the problems and all sorts of things and we'll go into a little bit of detail there right so we have this for non-stiff ODEs we also have this for stiff ODEs and one of the things that our organization has gotten really well known for is also not just that it's able to outperform some things from Python or or R but we actually build all the solvers ourselves right so we have you know these direct comparisons against things like uh CVD and the the forren codes right and we're able to show that you know some of these new methods like rose and brock methods can outperform you know even when you're going directly against the forrren

**[4:53](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=293s)** methods because you know so there's a lot of things that we do in terms of new methods uh so it's not just implementation also things around new methods and especially methods that are designed to exploit new details of hardware that were not exploited in the methods that were created back in the 80s and 90s right so so that's who we are but I I think you know uh even even even just to start just to get in the right framework I want to kind of go into the reasons for performance on these ODS, you know, because people kind of look at this and just go, okay, 50x, 100x, this is JIT compilation or, you know, people just kind of throw one term out there, but I I think that, you know, if you actually sit down and try to think about performance, you can actually understand what's going on. So, let's let's do that just for simple OD, right? So, so here I'm looking at the lock voltera model. So, this is actually all pulled from a paper in nature

**[5:41](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=341s)** methods called Julia for biologists. If you're curious about all the details, you can dive into that. But here's one graph from there, right? So this is a lock voltarum equation. It's a non-stiff OD of just two equations. And we can see for example the performance of Python, Mat Lab and R as kind of one grouping right here. These are like the highle languages. And then you have the C and four transolvers directly. Right? This is kind of this middle language uh performance. And then we have the new methods within Juliet. Right? Again the main difference here is implement is things around modernizing implementation and also just new methods. Right? Fern 7 is more efficient than methods like DP5 and things like that, right? And so there's new methods, but there's also gaps due to implementation. In fact, there's a large implementation gap here, right? And so the question people always have is like, oh, what if I use Numba, etc., etc. Well, let's actually just go

**[6:30](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=390s)** directly into that, right? So this is the logical volter equations, right? There's eight scalar operations as part of the code. And now in Julia, right, what and what ends up happening is you effectively only have, you know, you you effectively will fuse a lot of these these function calls into a single thing because at the compiler level do something called inlining where it wouldn't call functions. It would actually just directly put in the the lower level primitive that it needs to do within uh within the code. Right? So instead of having function calls and function calls and function calls you can decrease that stack of function calls uh by using this kind of inlining that is that is done within the LVM compiler right um and if you do if you use like something like Numba then you will also get that as well. So what you do with Numba is you can take your you can you can take this f function that

**[7:18](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=438s)** you would send to the OD solver. you JIT compile it and then you send that to Scypi and you say okay now run run my OD on this F right and if you don't do that then you have eight function calls that all have to go back to the Python interpreter right um and so if you just kind of count it up you just see okay a function call in Julia costs about five nonds a function call in the Python interpreter uh takes 150 nconds now for if it's Numba you call a faster function but you still have to call that function from from Python right um so if if you actually been counted up, right? So it the there's a difference in the way that the Julia functions are written. So the Julia OD solver functions are mutating an array. The ones in Scypi, they're actually computing a new array. The time to allocate that array in Python is 300 nconds. Each floatingoint operation is just two nanconds and there's eight of

**[8:07](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=487s)** them, right? And then count up the number of function calls. If you just do this back of the envelope calculation, you can see that, you know, this is what we would expect the timings to be about 20 seconds, uh, 1,500 seconds, about 4,000 or 450 seconds. If you actually then time it on your computer, it's almost exactly what you expect, right? So, so what what I'm meaning to show here is that, you know, like the fact that we get this order of magnitude difference like you can actually understand directly where that comes from, right? So using Numba inside of Scypi is slower than Julia which is hitting 20 nconds instead of 425 nconds because 300 ns of that is from the allocation the array that comes out and about 150 nconds of that is from the is from the the Python interpreter right so you know again if you if you're if sci is actually running in Python but then

**[8:57](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=537s)** the the OD the OD solver is actually written in forran what it's actually doing is it's sending that that function into for the forrren code, but every single time it calls your f function, it hits the python interpreter and then it has to go into the numpy thing, right? Or numba thing. And so that's a that's where the 150 nconds come from. And so you can see exactly there that you know if you there there's two principles then you need to design libraries about around being mutating if you want to get the maximum performance on CPU and you need to design your your your system so that way you don't have this interpreter overhead at all which can be difficult for high level uh for things that take in high level functions. So things like optimization libraries, things like differential equation libraries, right? If the user is actually passing you a function, it could get be pretty hard in a lot of high level languages to get rid

**[9:44](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=584s)** of this this top level lang uh function call. And this is one of the big reasons for an implementation difference in Julia specifically in the space of um differential equations and optimization. Right now, if you're doing something like neural networks where you're just calling kernel calls, then you're never then the user is never actually writing a function that's given to the system. You know, you're using a lot of built-in functions. this effectively doesn't exist and the allocation time is kind of decreased because and I'll go into that in a little bit and so you don't have as much of a difference but in this domain of differential equations you see this pretty huge difference and it's pretty easy to explain just by reading off exactly where the where the timings come from right so I want to do this now in the context of going to GPUs um but the the real idea then is that choosing the right parallelism for ODS depends on the problem so let me first kind of go get

**[10:32](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=632s)** into this in a problem dependent way before I get into the main result. So there's three different ways that you might want to parallelize an OD solve. If you have a big set of ODS, so think of partial differential equation, you want to parallelize within your solve, right? So within that OD that you're solving, you want to paralyze the operations of it because they're just large enough to be parallelized. Um so PD discretizations, large reaction networks parallelized within the solver. If you have a small OD system, then you actually don't have enough parallelism to do think about like paralyzing the Laurens equations, right? There's just you know arrays of three things paralyzing three things at a time doesn't give you anything. What you can do is you can develop methods that give you more parallelism things that are solving parallel in time. Right? They're less efficient methods but they are methods that naturally just have more computation that can happen at the same

**[11:20](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=680s)** time. So it is less computationally efficient but could be faster if you're exploiting the right hardware. Right? Then there's a third set of problems which is the one that we'll be talking about in the new the the you know the the results against the machine learning libraries which is the ensemble case where you have the same OD that you're solving with many different parameters. So say you're doing a parameter sweep over over a space, right? For that kind of case, what you would do then is you say, okay, you know, I have this one set of ODEs and boom, I I solve a parameter 1, two, three, four, parameter 10,000 all at the same time, right? Um, and this is like a this is a very um, you know, embarrassingly parallel kind of problem over the space of ODEs, which are, you know, kind of nonp paralyzable because they're kind of stepping through time, right? So you can try to paralyze them by doing parallel in time kinds of things or if you have a large enough

**[12:08](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=728s)** system you can parallelize in space but all three of these cases require different ways of doing the parallelization. So I want to kind of cover the two of them really quickly before I get into the the the ensemble case. Um if you have large OD systems then what you want to do is you want to say you know oh GPU parallelism what you want to do is you want to make sure that your state or your initial condition lives on the GPU. uh you want to make sure that the all the solver internals are on the GPU as well which means you know all the broadcasts that are going on inside the solver and all the linear algebra has to live on the GPU the user's function right remember that f that that I told you about you know the user fun definition the OD also has to be written to work on GPUs and then um and this and then uh the solution just needs to stay on the GPU if you satisfy all those then you do pretty well for the large OD case right so this is what

**[12:56](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=776s)** you know uh PyTorch and Jax will also do this effectively. The Julia solvers have this and then but also you know even a lot of other codes like sund dials and C++ you know they have a way of using an array a GPU based array that you can write operations on for anything that's like these large OD cases this is what the you know when people say I have a GPU compatible OD solver they generally mean that they support this specific case right um now there are some cases for which this works well and some cases where for which this doesn't really do all that much right the key thing to really keep in mind is you know to think about what happens for why the machine learning libraries end up being efficient for machine learning but not necessarily for a lot of scientific computing comes down to the fact that you have uh that O of N cubed operations are so much in the the computation that

**[13:46](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=826s)** you can ignore other things so what I'm showing here is the difference between um allocating operations versus non-allocating operations on GPU and CPU you can see that for O of N operations like this is like a broadcast or a map expression This is the optimized way of this is the unoptimized way of doing it versus the optimized way which is uh broadcasting into an already allocated array. You can see that the the memory cost of of an allocation is actually scales like O of N. And so it's always faster to do the array allocations correctly. And here you can see the difference between between the the GPU one or the CPU ones and the GPU ones. GPU is not even faster until you get a large enough matrix. Oven squared kind of is ends up in a in a middle case. But Oven what you see with ON cubed operations is that the cost of doing the allocation versus not

**[14:34](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=874s)** doing the allocation effectively zero once you get to large enough matrices right and so when you think about like the these these codes that are optimized for you know GPU computing something like PyTorch right what they're really relying on is the fact that when I do machine learning I'm doing a lot of big matrix mult multiplications these are O of N cubed and so a lot of the computational details around the user doing memory management the user doing manual SIMD D doesn't actually matter and that's why these are fast. But if you're in the space of actually doing a lot of O of N operations or O of N squ operations then that that that assumption is actually not correct, right? Um and so it means that applying some of these machine learning libraries to cases to some of these cases will give you some suboptimal results. And one way to actually understand this then is that if you're using implicit meth solvers for for ODEs, right? These

**[15:21](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=921s)** naturally have dense linear algebra in the LU factorization. And so doing this array, you know, oh, I have, you know, big arrays, so let me put it on the GPU, that will end up doing pretty well. If you're if your linear algebra is using things like QDSS and whatnot, then implicit solves will do pretty well on GPU once your ODS are sufficiently large. That's not necessarily true for explicit ODEs because they're all O of operations. you might get like a 2x 3x speed up, but you don't get that magical, you know, oh, the GPU is 10,000 times faster because when you're only doing OAN operations, a lot of the memory operations, a lot of the kernel call overhead actually starts to matter a lot. And this is something that we'll see in the ensemble case as well. So, this is just to go over, you know, like when a lot of people talk about GPU accelerated OD solvers, a lot of times we're talking about this case. The Julia solvers handle this. They do just about

**[16:09](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=969s)** the same as as the a lot of the other codes because in this case everyone has the same architecture for how they're doing the parallelization for small ODEs. This is kind of a topic that we talk about separately. For the sake of time I'm going to kind of jump over it. We have different methods like extrapolation methods that can solve for different DTS at the same time. Then there's special parallelizations things you can do yada yada yada. It ends up being faster for only small OD. So for example, systems of five to 10 200 ODEs, it ends up being the fastest stiff OD solver by about 4x because it's it's it's a specialized stiff OD solver that is forcing you to allow have more parallelization. Um but once you get to large enough uh problems, it turns out that the parallelization within your BLOS library starts to kick in for the LU factorization and the multi-threading

**[16:57](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1017s)** there is just fine and so that ends up not being any faster, right? So for small enough ODS, there are even tricks you can do on CPU. But now then the last thing is for ensembles of small ODS what are the kinds of tricks that you can do specifically on GPUs right so so the key thing here is that um for for you if if you if you're to say okay I'm going to take a machine learning library and I want to make this work in a way that is doing you know GPU accelerated kernels for an OD solve right the first thing that you can really come up with is the idea well I do have this thing over here right I have this idea that if I if I have a large enough set of ODEs I can put them into a GPU array and I can just do GPU operations on every step of the call. Right? And this is effectively what what the machine learning libraries will do when they approach an OD solve, right? They they say, "Hey, look, I have

**[17:46](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1066s)** a you know a py a pietorch based OD solver. I can GPU accelerate it. Um, and if I want to solve a bunch of the same OD for different parameters, I can just, you know, instead of having my initial condition of three things, I can have initial conditions of three by n or I'm just solving n versions of that same OD at the same time. Now I have big enough kernels to put it onto the GPU, right? And so it's basically using a current uh some some some approach where you just say I have this OD. Now let me turn every operation into an array of you know the value of x for every different parameter the value of y for every different parameter and run all those simultaneously by using GPU arrays over that whole space. So I convert the problem of solving many uh the same OD with many parameters into a problem that is just one giant OD and then then I use

**[18:34](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1114s)** this technique right here. Right? you you should start to probably already kind of trigger something in your head to say, well, is it is it actually efficient to shove my way of solving this problem into this form, right? But, you know, it's if if if you just put VMAP on your on an OD solver that's written to VJAX compatible, that is effectively what you'll get, right? So, so you know, I think a lot of people think about it as like VMAP is a thing that gives you parallelism. It's not that it gives you parallelism. It gives you a very specific form of parallelism that is generating larger kernels for using GPU operations. Right? Then the question is is that the right way to do parallelism for for the ensemble problem, right? Um now let's let's take a look at the same case here. Right? So you had this loa equations, right? And again you have eight scalar operations. Now if you turn this into something

**[19:23](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1163s)** where every single operation is now like instead of just looking at oh I have X you now have Y you now have X for every single OD that you're solving at the same time. So X is an array GPU array of all those ODS solved and Y is a GPU array of all those OD solved right and what ends up happening here is you have say eight scalar operations right so you have to do you know operation of X* Y and then a scaler times it d and each one of these calls ends up being a a kernel call to the GPU now you know if you're using something like PyTorch it might all be a different kernel call if you're using something like Jax it might fuse some of these kernel calls but still you're doing a lot of kernel calls in every single call of f right now. The other thing that you can imagine is what if I took my entire OD solve and generated a CUDA code for that entire OD solve that is specialized on it. Right?

**[20:12](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1212s)** If you did that, then you'd only do one kernel call for the entire OD solve. And then the question is how much of a performance difference would you get from that? Well, it takes about 5 to 10 micros in order to do a kernel call on the GPU. So, let we just calculate it out, right? So if this f call takes uh takes eight kernel calls right and it takes five micros secondsonds to launch uh then and and every single step of a if of an explicit OD solver method takes uh it takes actually five f calls then what you end up having is every single step has an overhead of 28 milliseconds. You do that for solving you know you do that over a thousand steps of the solve you're at 28 seconds versus five milliseconds. Right there you can already kind of instantly see what the difference is. So if you were into in if you're to take this VMAP approach and

**[21:00](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1260s)** say I'm going to generate all my calls to be to be you know every step of my thing to always be doing GPU calls. The problem is then you have a lot of GPU calls and a lot of these GPU calls are all O of N and so then you're spending a lot of time doing kernel call uh call launches and so you end up having a lot more overhead versus something where you build a specific kernel. You compile you basically write a CUDA kernel specifically only for that OD. You compile it and then you only do one kernel call launch to solve the entire OD. If you get rid of that overhead, then boom, there's there's there's where your thousand times performance difference come from. Now, Jax ends up doing a little bit better than this because it can kind of do some kernel fusions in here. So, it's not doing eight operations. But this is then the idea that it's still doing, you know, one of these operations seven times per

**[21:48](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1308s)** step, a thousand steps versus just literally one time through the whole solve, right? And this is how we actually end up at this result here where, you know, where you have that the Julia solvers ends up being much faster than the PyTorch and Jax methods, right? because it's not that you know we're just all using GPUs. We're using GPUs in fundamentally different ways where what we're doing here is we're generating a kernel specific to this GPU. we're compiling it and then we're running we we basically s call that kernel with all the different parameters and what we get back is the entire OD solve from one GPU call versus the machine learning uh based approach which mean the machine learning libraries are effectively doing every single step of the code and all every O of operation is doing one kernel call getting the GPU array back one kernel call get the GPU array back and that overhead from the kernel calls adds

**[22:36](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1356s)** up in fact one of the ways that we're able to kind of prove that this is that this is the case is that we actually have implementations of the Julia solvers in diffic right uh so we're able to just show that it it is inherent to the approach of how the parallelism is being designed that gives you this performance difference the other thing that you can see from this is that there's a library that was written directly in in CUDA where the user has to give their OD as a CUDA kernel and and then it'll compile it in the similar way that we do and you can see that we get about the same performance as that so it's it's a design decision the way that the parallelism is designed that gives you this 20x to 100x performance difference because of the kernel call overhead that's going on right and now you go again why is why is this kernel call overhead something that you don't care

**[23:23](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1403s)** about as much in machine learning why is b to table be fine for machine learning because it's really focused on o of n cubed operations like matrix multiplication whereas what happens with the vmap here is it's doing a lot of o of n operations and those are just not as efficient on GPU they end up having a lot of kernel all launch overhead. Um and so what we then able to do is we able to say well as long as we can generate kernels for different devices we can do this on CUDA 1 API AMD GPU and also Apple metal devices and get really slick really optimized codes um using the for this specific case right now it does it does get mean that we have limitations right we need to be able to make sure that the entire OD solve lives in within a kernel which means you need a sufficient register space so this only works for OD's up to size 200 or so so there's limitations that come from the

**[24:10](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1450s)** design. But if you're doing something like looking at a chaotic system of less than 200 um of less than 200 ODS like for example the Lawrence equation doing a parameter sweep is literally a 100 times faster if you take this approach. Um and then this also applies to to stiff OD solvers. You can you know you can see that here's the the stiff OD solvers that have a kernel generating approach versus the the Jax ones as well. And you can see that there's this similar 76 to 130 difference due to the way that the um you know even even in this case you might say it's a stiff OD solver. So it does have a bit of the the O of N cubed um operations but it ends up being a batched LU factorization instead of being just a full LU factorization and so it's just not able to be as efficient as a full O of N cubed kind of case. Now, one of the things that you might find interesting

**[24:56](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1496s)** in here though is gen soda is actually a CUDA kernel that is written directly for um uh for for solving stiff ODEs and we're able to outperform that quite a bit as well. And that comes down to the fact that we're using a different OD solver than what you'd expect to use for this case and and for this equation actually some of the BDF methods end up being some of the most efficient. But it turns out that the the methods that are efficient for CPU are not necessarily those that are efficient for GPU because if you always force a fixed amount of work then if you don't have a fixed amount of work like a Newton method then on GPU you might need to run all iterations that Newton method whether it's required or not just because different warps might require that and so moving to a less efficient method might actually be faster on GPU just because it's it's uh it's ideally built

**[25:44](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1544s)** for the hardware. uh we go into that in a lot more detail inside the paper but this is actually why why even though that this is a handwritten GPU uh CUDA kernel are the ones that were generating out Julia are much are much faster by you know a few orders of magnitude because there's also an a method difference and that method actually matters for the hardware so you have to think about both at the same time but um does this apply to other problems it turns out that we built for example global optimizers using the same idea particle swarm method where we put the entire particle swarm on GPU where doing different parts of the particles at the same time. It has all this kind of stuff. You can you can read about this and here as well. Again, it's not it's not it it's it's compiling the entire optimization to the GPU and running the whole particle swarm there to be able to cut down in kernel call overheads and get that same major performance

**[26:30](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1590s)** improvement. Um so the conclusion is that you know performance is not just a number right you know and I think everyone just likes to say like oh it's 100 times faster. No but like these are not just numbers you can know exactly where the performance difference comes from. And when we talk about GPUs, you really need to use the right parallelism model for the right problem. Just because you're using a GPU doesn't mean that you're using the GPU in the correct way for your problem. And a lot of these machine learning libraries in particular, they only generate GPU parallelism in one way. And that one way is not necessarily efficient for all ways that you might want to parallelize. And I think that this this this uh this project really kind of highlights that, you know, there are other ways that we might want to be using GPUs. Um and and we really need to consider the architecture for how you use GPUs um if you really want to get that maximal

**[27:19](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1639s)** performance. So yeah, thank you. Hopefully by the end of this now everyone understands why we are faster, right? Not that not just that the Julia tools are but this is why. So thank you very much. And I see a few questions here all um yeah so uh Julie uh so Julia aims for both high performance scientific ML and computing. How does Julie compare to Mojo? So the main thing with Mojo, right? So the main thing with Mojo is that Mojo doesn't do a lot of the optimizations directly, right? Mojo gives you a syntax for being able to write out CID arrays and things like that. Julie is a and so when you actually want to optimize your code, your optimized code looks a lot like Rust actually. Um you have to write down all the types. You have to write everything into SIMD vectors. You have to you know manually SIMD everything. The Julia code actually is exactly I mean it's high level code just kind of

**[28:08](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1688s)** retargeted. So, you know, Julia in the end ends up looking a lot more like Python um in the optimized form than Mojo, which I find it looks actually a lot like Rust once you've done all the optimizations. I would say check out the the the the um check out the blog post about optimizing Mojo for BLOS operations and that will kind of describe in detail once you see the code at the end, you'll see that it it looks a lot more like you're writing like lower level LVM type of code. Um and and there's a reason why. Uh and we could go into details there. Um how does Julia compare to to building code in say in forran skipping over uh Python? Yeah. So so I think um the Julia is still a high level language right something kind of looks like Python. Um now in forran there are a lot of array constructs and things these days. In general forran

**[28:56](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1736s)** does get a little bit more optimizations in Julia because forran does not allow certain forms of aliasing and that can help the compiler get a few more optimizations. But the Julio programming language has a much higher level in which case sometimes allowing the compiler to do more for you can help. In fact, so this project for example, right? Um the user actually gives us an f function written in Julia and we are able to retarget this to to to uh to you know.pt CUDA kernels automatically in a way that can be kind of difficult in some lower level languages, right? Because we want to say give me the math and I'll reinterpret it into different implementations. Whereas when you go down to something C or forran, you're more naturally writing down an implementation rather than the high level math. Right? Julia kind of sits in between the two spaces nicely. But I mean there is something lost with forran you actually get more um aliasing

**[29:46](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1786s)** optimizations automatically. So in Julia if you want to get the same SIMD sometimes you have to do the IVE operation. You have to manually tell the compiler to ivy because it's not able to prove the anti-aliasing. uh so when you're writing your own bloss libraries when you're writing your own linear algebra that is the one thing you have to keep in mind in Julia that you might not have to do in forran um I mean so so if if I understood depending on model size you said you change your parallelism uh yeah exactly so I'll go back to that point so depending on your model size in the applications you actually have to change the model of parallelism uh Julia does not offer an advantage over here in fact I think the fastest one still in the space is sund dials we're behind we're about like you know 75% 80% of the way there um you know so for the large OD systems on GPU sund dials and Julia are kind of close but sundials is a little bit faster for small OD systems we have a lot of novel

**[30:34](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1834s)** methods but they mainly are just for for ODEs it's really difficult to make small ODS parallelize well on GPUs for ensembles of ODS then we have this special trick that gets us an extra 100x in this case but yeah it depends on your use case how you're going to parallelize um can you explain a little bit more detail why zero has allocation zero Julia has zero allocations in the example I Yes. Um, let me actually go to the documentation, I can just show you exactly what this looks like. So, if I go to docsiml.ai. Um, and then I look at and then we look at the um, we look at the the the uh differential equation solver here, right? So, I'm going to pull over to this tab in a second here. Um, so if you look at the differential equation solver, you go into the first tutorial, right? um you then go down here.

**[31:25](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1885s)** So here's a Loren equation, right? Um now you write down this Loren equation. Notice the funny thing about the way that the differential equations are written in Julia is that there is no array that is returned from this function, right? Instead, what's getting what happen what's happening is you have a you have the array u that comes in and the derivatives are written as a mutation to this input array. There's no array that's created. Right? If you look at something like the scypi od solver, they tell you to create an array as the output that is the derivative, right? That right there is the array that is that is being allocated. So what what Julia the Julia solvers do is they actually have a pool of memory and they require that the user directly writes into that pool of memory rather than creating new memory and that that is exactly where that allocation comes from. It's a high level design decision for the library that then has this performance effect. Um now I will say

**[32:13](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1933s)** that you know Julia tools are not the only ones that do this. the underlying forrren tools most of them actually have an inter have an interface like this. So when scypi actually writes the interface over the forrren tool, they then expect the user to create an allocating uh uh create an allocating function that returns an array and then they do the in place right into the forrren code. And so that overhead of the 300 milliseconds or 300 microsconds, right, is just part of the scypi design. um you know and and so so when you end up having some other libraries that are trying to do optimizations in Python, right, they tend to to to move the J compilation and such to a different level and they they go around the sci solver because of this design in there and you know you there there's there's trade-offs in either way, right? Um you know by by requiring the user to write this in a different

**[33:01](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=1981s)** way, they have to be pretty careful about what they're doing, right? if you allow mutation on arrays to be to that you know that can be an unsafe operation in some cases but in some cases actually required for performance. So you know it's kind of a it shows you that you know some of these things around like you know like oh we mutate we you know we have a four argument syntax where we where we mutate and then sci has one where we return the array. It's not an arbitrary choice. It's it's a design choice about you know flexibility speed and and what what users expect highleveness and whatnot. and and and it reflects in the actual performance you get out. Um yeah, I think that uh I think that covers all the questions in the chat. Yeah, and I could definitely jump into the discord and also say that if anyone

**[33:50](https://www.youtube.com/watch?v=ZSFfv2cckx0&t=2030s)** wants to chat about any of these details, uh we're talking about topics like this all the time. Um especially in the Julia Slack. So this Julia Slack has, you know, 15,000 people or whatnot who like to talk about performance of code. So, if you want to just scan this QR code and and jump into the Slack, I'm happy to go into copious amounts of detail into, you know, I cut out all the flame graphs and everything. I could have gotten into a lot of more detail, but I I think that this is a high level enough detail where someone can start to understand what what things are thought about when trying to to build performant methods for these cases. Um, but again, happy to go in in more detail anytime. Just show up at the channels. So, thank you very much.

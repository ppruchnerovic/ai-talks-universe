---
id: qUlmamw4nf0
title: "Boris Lau-Let Me Structure Freely_ How to Improve LLM Structured Output Quality - PyData Global 2025"
slug: boris-lau-let-me-structure-freely-how-to-improve-llm
conference: pydata
conference_name: "PyData"
category: "Practitioner AI conferences"
edition: "PyData"
year: 2026
speakers: []
channel: "PyData"
duration_min: 23
published_at: 2026-01-09T17:54:54Z
video_id: qUlmamw4nf0
url: https://www.youtube.com/watch?v=qUlmamw4nf0
youtube_url: https://www.youtube.com/watch?v=qUlmamw4nf0
tags: ["Python", "Tutorial", "Education", "NumFOCUS", "PyData", "Opensource", "learn", "software", "python 3", "Julia", "coding", "learn to code", "how to program", "scientific programming"]
topics: []
transcript: true
---

# Boris Lau-Let Me Structure Freely_ How to Improve LLM Structured Output Quality - PyData Global 2025

**Speaker not identified**

`PyData` · `PyData` · `2026` · `23 min`

`#Python` `#Tutorial` `#Education` `#NumFOCUS` `#PyData` `#Opensource` `#learn` `#software` `#python 3` `#Julia` `#coding` `#learn to code` `#how to program` `#scientific programming`

[Watch the recording](https://www.youtube.com/watch?v=qUlmamw4nf0) · [Conference site](https://pydata.org/)

## Description

Ever wonder why structured LLM output doesn’t feel as reliable as its natural language responses? At Khan Academy, we asked ourselves the same thing—especially as we leaned heavily on JSON-based structured outputs to power our AI tutor, Khanmigo.

Surprisingly, the root of the problem often lies in one of the most familiar tools in a Python developer’s toolbox: the humble dict. In this talk, we follow the story of how dictionary ordering can shape (and sometimes distort) structured LLM output. We’ll walk through how different frameworks—OpenAI, Claude, LangChain, OpenRouter, vLLM—handle structured responses, and why those differences matter more than you’d expect.

Along the way, we’ll share practical best practices we’ve developed to improve structured output reliability, observe subtle failure cases, and debug weird edge behaviors. If you’re building LLM apps with structured output, you’ll leave with concrete tips—and a deeper appreciation for the details that make or break your system.

## Transcript

*4,191 words · source: supa (en, exact timings)*

**[0:06](https://www.youtube.com/watch?v=qUlmamw4nf0&t=6s)** Hi everyone. Welcome back to Pi Day the Global. Um we're excited to have you join us here. Um little bit of um logistics. Feel free to use the chat to connect to attendees and share your thoughts during the session. Uh if you have any questions, please post them in the Q&A tab and we'll try to address them at the end of the last five minutes of the session. Uh now it is my absolute pleasure to uh introduce you to Boris Lao. He is going to do a um uh a topic of let me structure freely or yeah let me structure freely. How to improve LLN structured output quality. So with that let's give him a warm welcome. Boris you're on the stage. >> Hello everybody. Now you can hear me. Okay. Uh my name is Boris. I'm from

**[0:54](https://www.youtube.com/watch?v=qUlmamw4nf0&t=54s)** Vancouver, Canada. Um hi Daniel also in the chat. And yeah um today we're going to talk a bit more about structured output. Um so yeah so why am I interested in structured output? So I work in Khan Academy which is a nonprofit um that helps to provide education worldwide and in we are actually experimenting with building a product called Kamigo which is education chatbot. So, Tamigo helps you to break down a complex math problem into smaller steps and not just give you the answer but guide you along the way in those smaller steps in order to learn about the problem uh learn about the um learn about problem step by step. So, um this is what we do. I also help out with organized the Vancouver local Vancouver

**[1:43](https://www.youtube.com/watch?v=qUlmamw4nf0&t=103s)** page group and we use quite in Kamigo we use quite a lot of structured output. So we learned a few lessons that we'd love to share with you all. So the agenda today is like introduce what structure out is, what lesson we learned as well as like some of the more advanced um structure output usage technique that we have learned in Khan Academy. All right. So yeah to set the stage um what is structured output? Um large language model takes an input in language natural language format um and then is output a certain um answer there. So for example here is um input here where we're talking about let's shuffle five objects um and then kind of come back with an answer multiple choice on the answer what would that end of the object answer be so you can do this in a

**[2:32](https://www.youtube.com/watch?v=qUlmamw4nf0&t=152s)** natural language format so for example here I call the natural language um LM and then come back with the answer here it thinks through it and then include the answer towards the end here um so yeah this is the natural language way to talking to LM Um and so the other um way to talk to the LM is using something called structured output where you provide a structure schema. So in this case the structure schema is actually here in a um pyic based model that define a field that would have answers and basically come back come back when you call the lm and come back with a JSON object with the answer field as expected. So this is structured output. So there is this way of controlling what the output is. And often when we use

**[3:22](https://www.youtube.com/watch?v=qUlmamw4nf0&t=202s)** structured output, we kind of think of it as just mean to the end that you're either outputting a lecture language or outputting a JSON object, right? It's just similar. But that's actually quite a few nuance about structured output that we learn um in Kami. So one of the question we often I'm in the um data infrastructure team in Khan Academy. I often get asked by product team and to ask wait why are we getting this schema validation error in um structure output is structure output actually lying that it's not actually returning a valid JSON. So that's a question that we have and let's do some experiments. So set up a prom and I'm actually now in this case the prom instead of just asking it to shuffle I'm asking to hey don't give me JSON but in the other hand I'm actually

**[4:11](https://www.youtube.com/watch?v=qUlmamw4nf0&t=251s)** calling a JSON um calling the structured output um API and with the answers here and as you can see it still come back with JSON so I can this is deterministic that it often um that the LM when calling structure output will always return a better JSON and we can actually reason about this on understanding how LM and structured out work. So LM is basically a neural network that outputs certain probability on the next token that's generated. And in the case of structured output, what happened is that in this case when you're outputting the potential next token here, we apply something called constraint sampling where we actually constrain it based on what the JSON schema will expect from the grammar. So looking back at this example here right for example if the

**[4:59](https://www.youtube.com/watch?v=qUlmamw4nf0&t=299s)** answer the first character is going to be open open curly bracket back open curly bracket if you're expecting a JSON so you can constrain that way and the next token is going to quote um so you can constrain this grammar based on this sample selection so this is a deterministic algorithm and therefore it's always going to return a valid JSON object having said um whe whether return a valid JSON schema that's a different question now let's skip an other tricky um schema in this case right in this case uh we know the multiple choices A to E but we are actually specifying a JSON schema through identic of giving a string lit of F and others so what will happen here actually the LM will come back with

**[5:49](https://www.youtube.com/watch?v=qUlmamw4nf0&t=349s)** answer C um in some cases um so it's like desperate to get answer right um ignoring this ignoring the string le schema that we provide. So this and as we can see when we do the validation through paidantic it come back with an error here and that's um quite interesting here. So what happened is that going back to the understanding on how structured output work the grammar that is passed to the constraint sampling often will only specify that either what fields it have in the objects or what is a valid JSON but it does often does not constrain what the string literal is. It really depends on what back end you use. So it's often case that depends on for example even if you use VLM as a background back back end or for the back end that you often

**[6:38](https://www.youtube.com/watch?v=qUlmamw4nf0&t=398s)** get different behavior. So you got to check what how specific the grammar is depends on the back end that you use for LM. So it always return a valid JSON object but it doesn't necessarily in some back end does not validate against the schema. So that's something that you got to be careful about. Um and of course there's some ways around this. For example we can use prompting around this. So, paid provide a field description that allows you to specify the description in the field and this is a very useful way to specify exactly what you want in that field or what information you want in that field. So for example here if we start with this problem hey if it's a to give me others it will give others so you get this safety net on like up valid JSON but having said that this is not deterministic

**[7:27](https://www.youtube.com/watch?v=qUlmamw4nf0&t=447s)** so yeah um so whether is now we've got a potentially valid JSON and validated JSON schema output on a structured output the next step we often ask is whether structured output will impact the quality of the out completion And that's actually quite a few studies on it and it actually get a bit spicy here. Um so it actually started originally with let me speak really um basically a paper that kind of study what the impact of structure output is and basically the paper promises that um natural language um is better quality when they go into structure output of quality degrees and then text which is an organization that start to um provide a product in fact it's a pipeline library called outlines that they do that provides structure output from large language model and they are ahead of the time they do this even before structure output is a thing in a lot of

**[8:17](https://www.youtube.com/watch?v=qUlmamw4nf0&t=497s)** the um API that we have and um so yeah um they kind of like that's they kind of provide a rebuttal on that paper and study study the study and understand that hey there's some um potential study difference there um and then the third paper that kind of like um show the middle ground of that. So the summary of that meta study is that generally uh we saw different um quality of this um where red is the structured output um blue is natural language output and we saw see based on this specific benchmark specific model that we are seeing um either comparable or like degrading of the structure output outcome. So it's interesting and but in Khan Academy can be we actually have seen a case uh where well seen quite a few cases um that structured output have

**[9:07](https://www.youtube.com/watch?v=qUlmamw4nf0&t=547s)** failed um we started using LM um started with um collaborating open AI GT4 um a few months before GT4 is public um and we actually end up kind of having to migrate a lot of our work from natural language output to structured output um this is one of the case that we've seen that actually um see breaking down a problem. Um so basically given this math problem we want to understand the stepbystep instruction and how you you approach solving this problem and put it in the prompt to guide the student during the chat. So in this case let's break down this problem. We know the correct answer is 10.8 a ch 69.1 and um let's call this in the natural language way and we kind of call this in the natural language um and they come back with the steps um and then come back with the correct solution 69.1 10.8

**[9:56](https://www.youtube.com/watch?v=qUlmamw4nf0&t=596s)** eight in a craft solution. But if we call it with a structured output, here's the structure you provide, right? Um kind of like the math reasoning and we want the steps and what steps we want each of the explanation. You look at the end of steps and then come back with X equals 10 or 25. So the answer is wrong and we often see this. So we're wondering, wait, what's going on here? The answer is like structure output is um structure output is doing worse. But what is more interesting is in fact the correct answer is embedded in the steps here. So you probably can't read unit code but this is actually the correct answer in the middle algebraic in algebraic form that 40 plus or minus 5 um square root of 34. So this is actually a correct answer and the lm

**[10:44](https://www.youtube.com/watch?v=qUlmamw4nf0&t=644s)** kind of talk itself back into a wrong answer. In fact you look at the final completion here. So what happened is that the final answer is not quite final. actually give the answer first in JSON JSON output. It sets itself without thinking that's x= 10 or x= 25 and then it comes back and go oh wait actually um we got to the right answer but then I got to agree with the final answer that I let's talk about and kind like give this kind of approximation something something and then talk itself into the wrong answer which is self-consistent which is great but it's not the right answer. So the fact is that's our thought in how we prompted right um JSON what happened as I mentioned a structure output rely on a JSON schema and a JSON schema um JSON schema is the fact that it's rely

**[11:31](https://www.youtube.com/watch?v=qUlmamw4nf0&t=691s)** itself is a JSON that specify what you want to output in this case the JSON schema if you look at the JSON schema the final answers is before the steps f come before S lexical ordering um and that's true for an older version of Pyantic uh pre- November 24 um so yeah pre- November 2024 but it's not in the latest version of pyantic so you got to be careful about that the latest version of pyantic is insertion order um class definition order um ordered so um in fact this is really weird because JSON itself is and treat object as an unordered key value pair but the fact is that we're using JSON schema to instruct the LM to output a JSON on but the order actually impacts the impacts the quality

**[12:21](https://www.youtube.com/watch?v=qUlmamw4nf0&t=741s)** um it depends on implementation a lot of the implementation actually of LM actually care and back end cares about this ordering but on the language side and framework side u when you're a developer you got to be really careful um especially if you're doing cross language um cross language um serialization of this JSON if you've got a multi-urface environment for example um go and rust random order um JavaScript if the key is numerical tree is a array but if a string key it treat is a object u so there's different order associated with it so you just got to be careful on what you are sending to the lm when you're sending a JSON schema so yeah um this is an interesting case um of understanding what is happening with JSON um how you want to get better output from JS structured output

**[13:12](https://www.youtube.com/watch?v=qUlmamw4nf0&t=792s)** um the next we also talk about right ordering we talk using a field description to improve output. Another trick that we use is actually kind of like using this to break down the steps of reasoning. Using the structure to break down the steps of reasoning here. So in this case going back to our shuffling example the problem is shuffling shuffling the five objects. Right? And with this actually we can sort of break it down so that we provide a structure that asks for the reasoning and even ask a reason in small steps and even ask a reasoning with the specific state after each steps right so that the the each steps is like iteratively improving and less work on each of the steps and then we basically come back with a structure output on kind of showing that

**[14:00](https://www.youtube.com/watch?v=qUlmamw4nf0&t=840s)** hey this is the different steps this is a state and then come back with the correct answer finally and we kind of have this comparison here and I think um we noticed that with structured language without thinking um that is roughly the same uh but with thinking I think we've seen some potential improvement there um partly because like more specific about what you are actually improving what you want to think about um but this going to point on like whether this is just another way of prompting right in fact we're creating this custom structure structure here that kind of like create the output. And isn't this just another way of doing prompting itself and this is what we're interested in is that whether we can think of this way of using natural language prompting or using the structured data as prompting

**[14:49](https://www.youtube.com/watch?v=qUlmamw4nf0&t=889s)** as different different solution to prompting. Um so that's the chain of thought which is popular way of you like ask an alum to think about things and the structure of thought where you're actually defining the data structure to elicit the LM to think about things in a certain way and think about certain things. Um we argue that actually like it's quite easy to use in this case because a you can precisely kind of define what points that you are thinking about. You can do it in natural language but it's a bit harder to be specific about it. um there's no constraint um no deterministic constraint sampling that kind of like influence that way um you can break down the small step iterative as well as the most important thing I think is for developer it's quite easy to experiment because if you are changing different thinking all you need to do is ignore the fields that you use to elicitate the thinking without

**[15:37](https://www.youtube.com/watch?v=qUlmamw4nf0&t=937s)** actually like needing to be careful about how you pass the output it's just the same answers that you're going to give um so yeah I think this is like quite quite enticing from a code management perspective but whether it is actually more effective I kind this this like kind of a side quest I actually ended to kind of want to measure whether chain of thought and structure of thought how do they compare um so it's a bit of side quest um I actually like kind think about okay how do you measure this um you have got the human you got human that kind of like iterate on the prompt here we also got something called automatic prompt engineer that allows us to iterate the problem. So in this case we're exploring a um framework called DSp. DS PI is basically once you declare certain iterative decorative program um then it

**[16:26](https://www.youtube.com/watch?v=qUlmamw4nf0&t=986s)** basically you just give it a bunch of data it will optimize the problem. Um so the workflow is that you define a you have a set of data you define a signature um and you define a module that kind wrap around the signature that define the variables that you want to optimize for and it will go and do the magic and optimize it for you. So here's an example of how we define it in chain of thought, right? You define a signature on like the input and output signature. You wrap it around a chain of pot module which specify the prompt the prompt that is it thinking is the variable that you want to optimize for and then we add a optimizer around this where um it will kind of optimize for this prom variable that you have which is a prompt in the training port and um optimize for that.

**[17:16](https://www.youtube.com/watch?v=qUlmamw4nf0&t=1036s)** So for example here uh we start with the original problem right it's just a really simple the task um this optimizing for the shuffle five um benchmark data set um and the optimize problem come with this right it actually like comes back with the different um different things that you want to do um in kind of like thinking about how to think about this problem uh which is pretty interesting so yeah um kind of like basically go through um different iteration in this case I was just working 100 samples it go through seven iteration and you can see the hue climb is not that that much that it kind of like go through a different iterations here and kind of like have different prompt they iterate on um so yeah I think this interesting way to like kind of like have a automated way to optimize a prompt so

**[18:08](https://www.youtube.com/watch?v=qUlmamw4nf0&t=1088s)** this is good because then now we can actually like introduce a different module in this case I introduce something called a despise structure of um structure structural thought model. Instead of a chain of thought, you actually optimize for a prompt. You in natural language you have a structural thought where you optimize for the data structure. Um so in this case you apply the optimizer this way. Um similarly you kind of like basically you define a signature that signature in this case is a structure output and you wrap it around module which is structure of dot. Um and then you throw it in the same optimizer. You use the same optimizer structure optimize against the natural language format and the structure output format and you'll come out with the result and here's the optimization right so the initial um structure here only one field in the answers the output

**[18:56](https://www.youtube.com/watch?v=qUlmamw4nf0&t=1136s)** structure here optimized structure actually contains the actually contain the reasoning the contain the reasoning overview and then contain step by step tracking the um tracking the like operation of the swap um it's interesting to see Now how the LM thinks about this. Oh, one thing to notice is that the optimization model, you can use something that's quite expensive. For example, you can use the most advanced um thinking model while the actual um model that you're optimizing for, you can actually use a much cheaper model. In this case, I think we're using haiku free um and um called ha free as a cheaper older model and it's actually like um can works out that way to use allow you to use cheaper model and yeah um as you can see it actually started really badly just because the fact that when you structure output you're actually taking a lot away from the

**[19:45](https://www.youtube.com/watch?v=qUlmamw4nf0&t=1185s)** reasoning um and then once you have once you have optimized once it introduced reasoning it's actually doing pretty well. There is a case that you often see for example in this case you see the fields actually start appearing with overfitting to the problem that you can see Alice Bob CLA that's like define the data structure that's why it's over optimizing um this is based on the training set um accuracy um but once you validate uh we actually end up didn't end up choosing this um data structure here so yeah and the comparison study is um just the initial comparison study I used the shuffle 5 shuffle 7 because GT4 is so good at shuffle 5 already and we also use GSM8K as a benchmark. Um in the shuffle case we often see an improvement in the our structure of thought um accuracy optimized accuracy but in the chain of thought we actually

**[20:33](https://www.youtube.com/watch?v=qUlmamw4nf0&t=1233s)** see that it's not in some cases degraded um the hypothesis that is probably like for very algorithmic task that you have for got really clear definition of structure uh it might works better with structure of thought but if you want to like have some more of a free form task uh which a lot of cases do um actually let let the LM speak freely. That actually works a lot better. Um yeah and also the other thing is that like careful about latency with structured output you're often outputting a lot more tokens that sometimes it might improve um impact latency and I saw it's an interesting experiments because you kind of like to be honest when I'm programming I kind of think about it this way right sometime I think about algorithm think sometime think about the data structure and how I would structure the program to think about how I would program and I think

**[21:22](https://www.youtube.com/watch?v=qUlmamw4nf0&t=1282s)** this actually speaks to um Rob Pike's five rule of language programming The five rules kind of talk a lot about like don't pre-optimize or like lean on simplicity. Uh but the rule five actually speaks to me quite a lot and it's probably speaks to pi data a lot. Um it says data dominates. If you choose the right data structure and organize things well the algorithm will almost be self-evident. Data structure not algorithm are central to program. So in this case it actually speaks quite well to this right because you're using a data structure to control thought and you're organizing a data structure to control thinking. So maybe it's like central to programming prompting. I think there's some interesting case that you helps you to reason about how to reason um using this technique here which is interesting. So yeah um that's it. Um thank you very much for um joining this presentation.

**[22:12](https://www.youtube.com/watch?v=qUlmamw4nf0&t=1332s)** Um I think a call out is that a lot of these are based on older benchmark and I think this to be honest comparing to two different technical from engineering is quite difficult. It really depends on your data set and um if you've got interesting problem that you're trying to solve um yeah please connect I would love to hear um all the code is on also this presentation um is in uh GitHub repo here and I've also got a branch on the DSpay that allows us to do the structure do um um module that wraps around if you got interesting use case there um give me a shout it's not quite in the PR ready stage yet but hopefully you can help other people and Yeah. Uh please do connect either ping me on the chat here or like um yeah on discord or connect with me on LinkedIn. Um yeah, thank you very much for joining.

---
id: b7NCyZZeCD0
title: "Jim Dowling-From Feature Engineering to Context Engineering for Agents-PyData Global 2025"
slug: jim-dowling-from-feature-engineering-to-context-engineering
conference: pydata
conference_name: "PyData"
category: "Practitioner AI conferences"
edition: "PyData"
year: 2026
speakers: []
channel: "PyData"
duration_min: 30
published_at: 2026-01-09T17:54:26Z
video_id: b7NCyZZeCD0
url: https://www.youtube.com/watch?v=b7NCyZZeCD0
youtube_url: https://www.youtube.com/watch?v=b7NCyZZeCD0
tags: ["Python", "Tutorial", "Education", "NumFOCUS", "PyData", "Opensource", "learn", "software", "python 3", "Julia", "coding", "learn to code", "how to program", "scientific programming"]
topics: ["Agents & orchestration", "Classic ML & data science", "Data engineering & MLOps", "Prompting & context engineering"]
transcript: true
---

# Jim Dowling-From Feature Engineering to Context Engineering for Agents-PyData Global 2025

**Speaker not identified**

`PyData` · `PyData` · `2026` · `30 min`

`#Python` `#Tutorial` `#Education` `#NumFOCUS` `#PyData` `#Opensource` `#learn` `#software` `#python 3` `#Julia` `#coding` `#learn to code` `#how to program` `#scientific programming`

[Watch the recording](https://www.youtube.com/watch?v=b7NCyZZeCD0) · [Conference site](https://pydata.org/)

## Description

Context Engineering for Agents involves getting relevant data into the LLM’s prompt and builds on in-context learning capabilities of LLMs. But LLMs have finite sized context windows, so you can't just dump unprocessed context data into your Agent's LLM prompt. You need to select the right data, process it into the correct format, and compress or summarize the data before its use as context data.

In this talk, we will introduce techniques for selection, preprocessing, and compression of context data, taking inspiration from the tried and tested techniques used for feature engineering for ML. What goes around, comes around.

## Transcript

*5,645 words · source: supa (en, exact timings)*

**[0:06](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=6s)** Hi everyone and welcome to PI data global 2025. Thanks for joining us today. Um I'm excited to introduce Jim Dalling, the CEO of Hops Works, a uh longtime presenter at Pi Data Conferences. If you know him, you love him. We're glad you're here, Jim. We're glad the audience is here. Um, we encourage you to interact in the chat feature which is on the right side of your screen. Um, also have a Q&A option. So, we'll do that at the end of the session. If you have any questions, feel free to um, drop those there and we'll address them towards the end. But without further ado, Jim, the floor is all yours. >> Thanks, Tamara. Great to be here. Hi everyone. Welcome to you wherever you

**[0:53](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=53s)** are around the world. I'm going to talk about the journey from feature engineering to context engineering. Many people have a background in machine learning at PI data. They're interested in building aantic systems. I'm going to talk about a lot of the techniques from feature engineering that are applicable to context engineering. Um, and I'm going to talk about it in in in the context of building agentic applications. So I'll start with the fundamentals which is there is a new class of machine learning in uh exhibited in large language models or LLMs which is called in context learning. So on the right hand side we have uh two different stones. One is called lumemerite and those other is called zeronite. And in machine learning what we would try and do is given some examples of these stones. So given some features such as the color of the stone, its hardness,

**[1:42](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=102s)** whether it glows under ultraviolet light or not, we'd classify the stone as lumemerite or zeronite. And here we have three examples of different stones with different feature values. And in traditional machine learning, we would train a model binary classifier and given some new features. Here we have a new stone. We'd like to classify it as light gray, hardness 5.5. It doesn't glow under UV. What is it? Lumite or Xite? Now lumitis do not exist but if you paste this into chat GBT or any other LLM it will actually work. It will it will be able to correctly classify the stone based on the features values that we provided and the examples or samples that we gave with the uh label here of loomers arenite. But this is called in context learning. You have to

**[2:29](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=149s)** pass all of the examples in with your question asking what's this type of stone but I passed the examples into the prompt. So we don't have a separate training phase. We just do it in a single pass with the LLM. It's called in context learning. [clears throat] Now in context learning is important because if I have a chat GPT our chatbot. I've got a chatbot here where where we have a user who asks a query. The way chat bots work is that we'd have a static system prompt that we would add to our query. So for example, we might say that um please be nice in your responses. Don't be vulgar. um you know don't do anything which is against my policy and then you ask the query the LLM will give you a response but if if we have some data that's not included in the LLM's training data so the LM doesn't know about that data if we want

**[3:18](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=198s)** to include a relevant subset of that data into the into our prompt that we send to the LLM we can call that data the context data and the purpose of context data is to help the LLM provide a better response to a query and it will do that using in context learning. So context data is the data we inject into our prompts and in context learning is the me one of the mechanisms we can use to help uh fulfill tasks and and have agents do the work that we want them to do which is to provide better responses to queries. Now getting private data data that's not in the LLM's training data set into an LLM context engineering is what I'm going to talk about primarily today. You can also have static data included in your prompt. We call that prompt

**[4:04](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=244s)** engineering. Um or you can fine-tune a large language model with supervised fine-tuning or some variant of of uh reinforcement learning with with humans involved. Um and I'm not going to talk about supervised fine-tuning of large models today. It'll be primarily about context data. And the reason why is because we see a lot of um applications now using context engineering to solve problems. Clara are a well-known pay buy now pay later provider and they claim that 65% of their customer service calls are handled by agents and those agents use private data. So LLMs are not trained on Clar and Clara's financial transactions. They don't know anything about them. You could say well I could fine-tune a large language model and

**[4:51](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=291s)** their private data but we'll see that context data is an easier way to get started and it's also more interpretable. So it's easier for someone to debug and say well I know what context was inserted into the prompt so I can understand why the agent answered the way it did. Context data is also important where we can't use supervised fine-tuning which is real time context data. So obviously it takes a long time to fine-tune a large language model, a foundation model. But if we have an application like a video application like Tik Tok, you're clicking on buttons, you're liking videos, you're sharing, you're swiping, you're viewing, that metadata, the clicks and and actions that you take can be used as context to help agents do something intelligent with your application. And that data needs to be very fresh, otherwise it won't be

**[5:41](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=341s)** relevant to the application. So your click is only maybe relevant for 10, 20, 30 seconds. We need to include that data in the context in order for the agent to do something intelligent with that data. So real-time data really can only come in as context, not with supervised fine-tuning. So the talk is primarily about saying that context engineering is better than fine-tuning to get started and it's the dominant paradigm now for incorporating new data into LMS data not included in the training data set of the LLMs because you can ship your improvements in hours instead of days. You can support real-time data. You don't need to retrain a model when a new frontier model appears. You can just keep your context engineering pipelines and your your retrieval engines in place in your agent and you just then replace your your large language model version

**[6:29](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=389s)** in the back end. And then the context state is very interpretable and explainable for both the the users of these agentic systems and then the developers of it. That's some of the motivation for favoring context engineering. So so what is context in and how does how does it get there? we said that it's a subset of data that's going to be relevant for the query and it will help the uh provide a better response from the LLM. So in Agentic infrastructures we have an agent here. It has an API and a client can issue a query via the API and get a response. The agent is now responsible for selecting the correct context data based on the query. So the agent can use a protocol like MCP model context protocol to go to some rag data source that stores context data. The data from there

**[7:16](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=436s)** must come from somewhere. So often we'll have to build pipelines to get the context data from the sources that we want to use as context for our agentic applications and make that data available in a way that's easy to retrieve. Typically we do that using functions at the moment. So um LLMs can respond with you can is a query and they'll respond with a a function that you want to call via MCP which can retrieve the context data for you and that could be inserted into your prompt and then included in the uh prompt that's get sent the context window sent to the LM directly. So this looks very similar to people who are familiar with feature engineering. If you're building a machine learning model, we might have for example a feature store here which is which has pipelines of feature data fed to it and we use that premputed features to inject

**[8:05](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=485s)** features into real time AI models. Um so that's the kind of starting point for this. Um but the context data I said as I said before it's primarily going to be either private data that the LM wasn't trained on or recent data that was created after the LM training cutoff date. So I'll go into a little bit more detail on the similarities between feature engineering and and uh context for agents to get started. So if we think about feature engineering, we often say that well I need to use features to help um train a model and those features should have predictive power for the target or label that I'm trying to if it's supervised machine learning that I'm trying to train to predict. H so use the same is true for LLMs. If I have an agent, I only want useful context data.

**[8:53](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=533s)** I don't want to just dump all the data in because lens can get confused. H we don't want to have redundant data. So if I have a redundant feature, it'll make my model go a bit slower in machine learning. If I have redundant context, it can confuse an LLM. If I have an irrelevant feature, it won't have any predictive power, but it will make my machine learning system go slower and it might reduce the quality of predictions. Uh the same is true for context. But in context we can also have the addition that the LLMs can interpret the the feature values the input context data. So you can have contradictory context where you have one claim of one piece of one fact maybe in one part and then an alternative fact in a different part. So if those are contradictory pieces of information, it won't help um the a the LLM answer the query

**[9:41](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=581s)** correctly. um both cases if I'm working in a kind of an enterprise environment there's certain features I'm allowed to use because of the the rights of the users of these models the the legal jurisdiction I'm located in and the same is true for for context data you have of course data that you are not allowed to use uh in particular agentic applications and then there's of course infeasible features so maybe you can't compute the feature it takes too long to compute it you won't hit your SLA maybe the feature is leaky or it's it's it's not it's too expensive because it requires you know GPUs that your production system just don't have available. Um the same is true of context. It maybe that there's some context information that would help your agenda application but for some reason you can't retrieve it or you can't make

**[10:28](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=628s)** it available in a context pipeline. So let's move on to a couple more similarities. Uh we can look at what we call um freshness of context. So we gave the example of Tik Tok and maybe I click on a video or I share or like a video. So the time between you taking that action and that context being available for the agent to include in the prompt is what we call context freshness. And we have an analogy in in real time AI systems called feature freshness. So both of these are important concepts when it comes to building real-time agentic applications or real-time machine learning applications. How long will it take between an action being taken in an application to when that data can become available for use uh to make predictions with? Um if we go and look at machine learning systems now and we talk about what does

**[11:17](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=677s)** it mean to deploy a model for use uh by real-time clients, we can see in the bottom left we have a model here and it calls model.predict and the input to the model is what we call the model signature. So if you're working in in machine learning, you'll be familiar with the fact that features that come in are typically represented in a numerical format. Um maybe we have to normalize some features. If we have a gradient descentbased model that we've trained, we might have to one hot encode certain categorical features. For example, for XG boost if you're using a a decision tree. So the data coming into the model will will be in this numerical format. But the clients who want to make an an invocation to make a prediction request and in this example, this is from my book that the O'Reilly book that that

**[12:05](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=725s)** I've just came out a week or two ago. Um we we have a client that wants to predict credit card fraud. So they're going to send in the amount of the credit card payment, the tra the time stamp, whether the card was present, the IP address of the terminal or the internet service, the credit card number, and the merchant ID. That's all the information that comes out. and we want to make a prediction if there's fraud or not. Now, um that is not the information that goes into the model. So, this API to our model deployment is not the same as the input or the the schema or signature for the model. The input data of the model is going to be different. And the reason it's going to be different is because with this prediction request, this this deployment API, we're going to look up premputed features using the credit card number and merchant ID. We're going to compute

**[12:52](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=772s)** features using some of these request parameters that came in and we're going to put them all together and we're going to uh transform some of the feature data and then we'll send that transformed feature vector into the model and [clears throat] we're going to log all of the predictions and the the feature values. So, so all of this means that basically models that you deploy their API for calling them is not the same uh as the as the actual input signature to the model. Now, the same is true when you develop an agent. So often there's an assumption that because my LLM takes a query string in and then we get a a response which is also a string. Now we know internally that the the LLM will take a tokens in and but but you know often you'll you'll you'll uh you'll pass in the string and a tokenizer will then tokenize that uh string for you.

**[13:41](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=821s)** But the API is effectively you can think of as being a string coming in and a string coming out. But when you build an agent you are not restricted to having that as your API. And this is important. Uh what you can also do and this is what I'll talk a little bit about later is I can write an an agent API that also includes um for example ids of entities and I can pass those as context. I can say for example uh scan the last seven days of and here's the customer ID do a primary key lookup and here's the order ID. So if I pass that as additional context along with the query which is what it should be when will I receive my order. Um that query can come down to our agent and our agent can use these entity ids and and and instructions to retrieve context. And in this case we

**[14:29](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=869s)** don't necessarily always have to receive retrieve from a vector index. We can also retrieve because we have primary key lookups and scans. We can retrieve from a relational database like a feature store that that I'll talk about later. So we're able to retrieve all sorts of context information not just uh chunks of data encoded uh in a vector index but [clears throat] also uh enterprise data in tabular format from a platform like a feature store. So once all of this data has been uh included as context in our prompt then we'll have the text API to the LLM which will give us our response. The Asian API is not the same as the LM API as is the case for machine learning. Excuse me. So another uh comparison we can think of and I'll give an example later

**[15:19](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=919s)** is that if an agent uses in context learning it's doing learning do we not need training and test splits of our data because that's what we do in machine learning. In machine learning when we train a model we train on the train set and then we evaluate the model performance on the test or validation set. Now in in agentic applications how do we train? Well, training means in context learning. And typically what that means is in my prompt, I'll include many different examples of the task or action I would like the the agent the agent to perform. So I give these as hints. We call those multi-shot examples. Multiple shots means many examples of the same thing. Um and then the the agent I say sorry say the LLM can perform in context learning with those as effectively the training set for you're training or or um preparing

**[16:08](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=968s)** the LLM to answer that question um in the way you would like it to answer it. Now when we have an agent and we're h and we're happy that we think it's working as as as we expect we'd like to make changes to the agent and and check test if the performance of the agent is improving or disimproving. Then we'll look at eval.s. Evals you can think of as a a test set that we run through our agent and we our agent gives us responses for all of these rows in our eval set and then we'll we'll score all of the responses to see if the agent is now performing better than before or or worse. So we'll have a look about that in a in a few slides time. So let's uh let's introduce evals first. And I mentioned eval are basically test sets for agents. So if I have a an agent here

**[16:58](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=1018s)** and we have production requests, so request traffic coming into the agent, the agent will uh log all of these requests including the response. We've got an input and output. Um the the agent may take many steps to perform the before it sends a response. So we can log all of the steps, but what we're able to do is is score all of these steps. And you can the simplest one is to score just the request and response. But you can also score internal steps like looking up data from your rag data source. Um so once we have our scores what we can do is we can analyze uh each of these log values and say well I can use some of these examples as eval because maybe my agent didn't answer as well as I expected. So, I'd like to keep that one so in future I want my agent to answer better and I can also get insights on how to improve the agent

**[17:46](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=1066s)** maybe to add some instructions into the system prompt or maybe how to get additional context data from a from a different store or the same store. So, the eval are going to be that that we can generate from the yeah the the production traffic. You can also generate eval using synthetic approaches but but often it's good to generate them from production or expected production traffic. And then often you'll start by manually scoring a lot of these traces uh and collecting your evals. So [clears throat] the evol are are really useful because they can evaluate. They're the test set for our agent. They'll check its performance. I want to make changes to improve an agent. I want to check its performance. I'm going to run my evals. um and it's going to store logs uh for all those evals and then we're going to

**[18:34](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=1114s)** evaluate the the performance of of of the agent on all these evals. I mentioned earlier that you can evaluate that performance uh manually and that's typically how you you start off by doing it manually but over time you you have typically two different types of of of agent tasks. One are objective ones where you might have an evaluator to say well did the task get executed? Maybe you check that the agent performed an action as expected. But we also have subjective tasks where the where an LLM can act as a judge. So you can automate this processing of running your evals and scoring them by having an LLM as a judge take all of these outputs and then writing these scores. Now [clears throat] how do we get an LM as a judge to perform well? We'd like it to know what is a good score for some rows. Well, what we can do is we can give a

**[19:22](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=1162s)** training set to the LM as a judge. So once we've got our results of our scores, we can basically say, hey, the agent improvements were were good or not, and we can accept the change or not. But let's look at at how we we do the LLM as a judge. So the LM as a judge, we we want to basically say to it, look, here are some good examples of requests and responses and scores. So we can pass them into the prompt um of this LM as a judge, and they'll end up being h examples, these multi-shot examples that we'll use for in context learning. So when we have another row that we would like our LMS judge to evaluate, it will use these rows as effectively a training set for in context learning and they'll give you hopefully a better score. So this is a good way of evaluating the performance of your LMS as a judge. Now to just to move on on um what else

**[20:13](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=1213s)** can we we talk about in terms of going from feature engineering to context engineering um and if we look at real time context serving and in particular of tabular data. So a lot of the most valuable enterprise data stored in tables and what we see many of the the large cloud vendors their application architectures look something like the following. You have applications which generate um data and they typically go directly to a lakehouse table. So either through streaming architectures or batch and from there they synchronize the data with some sort of low latency context serving database. So for example um data bricks have something called lakebase um and that gives you relatively okay query latency. you're going to have a query latency of you know 10 20 milliseconds on postgress but uh the time it takes from your agentic application generating

**[21:02](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=1262s)** the event until it's available for serving will be minutes so what we can say is the context freshness is not good it's it's low so what what we need to do is we need to learn from uh real-time AI systems so I I work at a company called hopsirs and in hopsirs the applications can stream the data directly to the low latency context serving layer so in our case it takes subsecond to get the data from the application until it's ready and available for query. So you don't just have a low latency query, you'll also have very fresh context um data. So you know 2 millisecond latency and then less than a second context freshness. That means you're really able to build real-time agentic applications. So I'm going to put this all together in the context of an agentic architecture and this is covered in my book um that

**[21:50](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=1310s)** I'll give a reference to later on at the end. But um [clears throat] so context data will come from I've I've talked about you know data coming from vector indexes and data coming from tabular data sources and they're probably the two most important data sources. So the the vector index data source is a very very well-known one. You're going to have some documents or files up here. We'll have a vector embedding pipeline. So it's going to compute them vector embeddings over all the chunks and we're going to have to chunk our data here. we'll store it in the vector index and when a query comes in from our application. Um we'll use that query to retrieve similar um chunks of data and we'll include them as context in our prompt template and send them the lambda. And this is a pretty well-known uh architecture. It's classically what people call retrieval

**[22:37](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=1357s)** augmented generation or rag. Um the user query string is passed directly to the vector index for for similarity search. Now that's not the only form of rag because rag just means retrieval. Um we can retrieve data also from um tabular data sources. So one way you can think about doing it is oh I have an application here on the left. It has its own application database. Can my application just retrieve the context data it needs locally? I can my application can basically say oh I've got data related to an order an article that I want to purchase and maybe my user ID. retrieve that as context data, pass that with the query and then um the a then hopefully you know you'll get a a better response to the to the query from the LLM and yes you can do this but it

**[23:27](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=1407s)** is the application developers responsibility to retrieve and send the context data agent and it does increase app complexity quite significantly. Um so uh it's challenging for app developers to write that code and and it you know nowadays people are vibe coding a lot of these applications and it's not that trivial to vibe code an application that's going to retrieve relevant context data. So what we've been working with at Hopsir is to try and um have an architecture that makes it easier to retrieve this tabular data from uh from application databases. So what we do is in in hops we can take uh application tables and databases either you can mount them in the feature store so there's no copying of data but if you for example don't want to issue queries directly on the application database you

**[24:14](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=1454s)** can have of course pipelines so feature pipelines that will compute features or context data and copy it into the feature store. So then the question is well how do I get access to that context data? How do I has my application reach agreement with the agent on what context data to include and the prompt? And our answer is to use entity ids. So every application will naturally have some user ids, session ids, product ids, order ids, article IDs and so on. And when the application issues a query, it wants to perform a task. it passes the the relevant ids and the agent can then use those ids via an MCP API to the feature store to retrieve relevant context and put it in the prompt. Um so what you can do here is how do you know what the relevant IDs are but your

**[25:03](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=1503s)** application will have a number of tasks that it performs that it needs help from agents from. So you can basically say okay for task one we need these ids for task two we need these ids and so on and then we have the application entity data down here um which becomes available in our feature store to for for querying. So for example if I have task one and I should query for it the application will provide the user ID and the order ID but the other ids are not needed. So the agent will know to retrieve only the context data for these two particular ids user ID and order ID and if you have a different task like task N um it maybe retrieves user ID and article ID and so on. So you have to decompose your app into tasks and then provide the uh appropriate set of entity ids for each

**[25:52](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=1552s)** task and the agent will then retrieve them uh from the feature store. Um we can also stream data from the applications directly into the feature store and you can you can um instrument your app to collect all of the actions and clicks and so on and then within a second or or less the data will become available for um retrieval. So couple of slides left um and we'll be done. the if we put it all together and we put these three parts together, this is the kind of what what is covered in the book as a kind of a a a a semicomplete architecture for retrieving context information and and ingesting context information for aic applications. We have our our vector index here. you have the feature store

**[26:40](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=1600s)** and then you'll have other data sources for context like web search external APIs uh graph databases and so on that we haven't included here uh and of course traces your agent should store its traces and we we provide support to storing them in the feature store so what we're working on at Hopsworks now is we're working on this this architecture we have a um we have this coding agent called brewer which helps you build these pipelines to feed the data into the system and help you um decompose your applications into these uh so your your backend pipelines into these different components and um if you want to learn more h you can um download a copy of this book. So I can offer a special kind of um offer to people who are on the call. If you scan this QR code

**[27:30](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=1650s)** um you you enter this pro uh promo code PI data 2025 you'll get a PDF copy of the book. Uh, it's also available for purchase on Amazon if you prefer a paperback copy of the book as well. And I think with that I'm I'm on time and uh I'll see if there are any questions. I'll leave this on the screen for the moment. Um, if people have a look at it and that's it. >> Awesome. Thank you so much, Jim. We do have a couple questions. Um, let me see. >> I think I'm Have I stop I'm still sharing. How do I stop sharing? Oh, there we go. >> Yeah, there you go. Here we go. Here's the first question. >> Um, okay. So, we have, would an LMA judge overevaluate another LLM? It feels

**[28:20](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=1700s)** like it would tend to be more positive seeing that most LM are trained to be agreeable and supportive. Um, you know, I think I mean it's a good question. Uh, so the general guidance people give is use the frontier. So the most powerful model is an LM as a judge. If you're running a valve, that can be expensive, right? It can it can cost a lot if you're going to just rerun your evals all the time. So um uh you know at the moment that's the best we have now. You know, do they work? Are they completely um you know reliable? No. Um do they have biases? Yes. I can't say all the biases they have. So will they over because they tend to be nice. They tend to give good answers. Um I think you know the the challenge there if you saw on the slide when I talk about eleism of judges yes I

**[29:08](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=1748s)** can have this training set as as kind of examples in the prompt for in context learning. Um but if I want to evaluate them I can have another test set that I run against the LM as a judge and see how it performs and if I change my foundation version of my LLM and see if it performs better. Um that's one way you can do that to evaluate your LM as a judge but you often do it by hand. Sorry, that was my timer. Says my 30 minutes. Um, okay. Hopefully that that sort of answers the question. >> Yeah, and that's perfect timing. Uh, your timer worked out pretty well. Uh, thank you so much, Jim. And if anybody has any other questions, please feel free um to hop over to Discord and ask those um any of the speakers or >> there is somebody asked about the the

**[29:55](https://www.youtube.com/watch?v=b7NCyZZeCD0&t=1795s)** presentation is in the resources button on the left. So, you can get a copy of the slides there. They're already there. >> Okay, thanks everyone. >> Awesome. Thank you everybody. Thank you Jim.

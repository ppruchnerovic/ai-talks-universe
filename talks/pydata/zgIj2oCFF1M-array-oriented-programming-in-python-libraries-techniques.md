---
id: zgIj2oCFF1M
title: "Array-Oriented Programming in Python: Libraries, Techniques, and Trade-offs [PyCon DE & PyData 2026]"
slug: array-oriented-programming-in-python-libraries-techniques
conference: pydata
conference_name: "PyData"
category: "AI engineering & agents"
edition: "PyData"
year: 2026
speakers: ["Iason Krommydas"]
channel: "PyData"
duration_min: 67
published_at: 2026-08-04T22:20:21Z
video_id: zgIj2oCFF1M
youtube_url: https://www.youtube.com/watch?v=zgIj2oCFF1M
tags: ["PyCon DE", "PyCon DE 2026", "PyData", "Python", "conference talk", "data science", "machine learning"]
transcript: true
---

# Array-Oriented Programming in Python: Libraries, Techniques, and Trade-offs [PyCon DE & PyData 2026]

**Iason Krommydas**

`PyData` · `PyData` · `2026` · `67 min`

`#PyCon DE` `#PyCon DE 2026` `#PyData` `#Python` `#conference talk` `#data science` `#machine learning`

[Watch the recording](https://www.youtube.com/watch?v=zgIj2oCFF1M) · [Conference site](https://pydata.org/)

## Description

🔊 Recorded at PyCon DE & PyData 2026, 15.04.2026

🎓 Watch Iason Krommydas break down the modern Python array ecosystem to discover how to choose between NumPy, JAX, Numba, and Awkward Array for maximum performance in your scientific computing workflows.

Speakers:
Iason Krommydas

Description:
Array-oriented programming in Python shifts the focus from individual element manipulation to operations on entire data structures. This paradigm addresses the performance bottlenecks of the Python interpreter by offloading heavy computations to pre-compiled C or C++ libraries. While imperative programming relies on explicit loops and functional programming uses mapping functions, array-oriented programming utilizes implicit loops through vectorization, which aligns with hardware-level SIMD (Single Instruction, Multiple Data) operations common in CPUs and GPUs.

Key tools for this approach include NumPy, JAX, and Numba. NumPy provides the foundational array object and vectorized operations, though it often creates costly intermediate arrays in memory for complex expressions. To mitigate this, NumExpr fuses operations to reduce memory overhead, while Numba uses LLVM to compile Python bytecode into machine code, allowing imperative loops to run at C-like speeds. JAX further optimizes this process by tracing functions to create an intermediate representation (HLO), which is then compiled via XLA (Accelerated Linear Algebra) for efficient execution on CPUs, GPUs, or TPUs.

A primary trade-off in array-oriented programming is the handling of conditional logic. Standard Python if-statements cannot be applied to arrays because the truth value of multiple elements is ambiguous. This is resolved using boolean masking—applying a filter of true/false values to update only specific elements. In many cases, it is computationally faster to perform redundant calculations on all elements than to manage the memory overhead of masking. For datasets exceeding available RAM, libraries like Dask extend these capabilities by implementing chunking and parallelized computation graphs.

⭐️ About PyCon DE:
PyCon DE is the leading conference on open-source Python applications in AI and data science. It brings together industry professionals, researchers, AI and data science practitioners, and software engineering communities, providing a unique platform for collaboration, knowledge sharing, and innovation.

The PyCon DE & PyData 2026 conference delivered an exceptional experience, fostering stronger connections within the Python community while showcasing the latest advancements in artificial intelligence and data science. Attendees enjoyed a diverse and engaging program of talks, workshops, and networking opportunities, further establishing the conference as a premier event for Python, AI, and data science enthusiasts across Germany.

PyCon DE 2027 will take place in Heidelberg from 19 to 23 April 2027.

•  Newsletter: https://2027.pycon.de/newsletter/
•  LinkedIn: https://www.linkedin.com/company/pyconde
•  X: https://www.x.com/pyconde

Links:
• Conference website: http://pycon.de
• Other sessions: https://2026.pycon.de/talks/

The conference was organized by
• Python Softwareverband e.V.: http://pysv.org
• Pioneers Hub gemeinnützige GmbH: http://pioneershub.org
in collaboration with NumFOCUS Inc.: http://numfocus.org

If you enjoyed this session, please like, and subscribe to our channel for more insightful talks and discussions.
Share this video with your network to spread the knowledge!

Hashtags:

Acknowledgements:
Special thanks to all the volunteers and sponsors who made this event possible.

About:
Python Softwareverband e.V.:
PySV is a non-profit that promotes the use and development of Python in Germany through events, education, and advocacy, fostering an open Python community.

Pioneers Hub gemeinnützige GmbH:
is a non-profit fostering innovation in AI and tech by connecting experts and promoting knowledge exchange through events and collaborative initiatives.

NumFOCUS Inc.
supports open-source scientific computing by providing financial and logistical support to key projects like NumPy and Jupyter, promoting sustainable development and collaboration.

www.pydata.org

PyData is an educational program of NumFOCUS, a 501(c)3 non-profit organization in the United States. PyData provides a forum for the international community of users and developers of data analysis tools to share ideas and learn from each other. The global PyData network promotes discussion of best practices, new approaches, and emerging technologies for data management, processing, analytics, and visualization. PyData communities approach data science using many languages, including (but not limited to) Python, Julia, and R.

## Transcript

*11,105 words · source: supa (en, exact timings)*

**[0:06](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=6s)** All right. Uh welcome everybody. Uh thank you for uh being here. Um I'm Yashan as uh I was said and I would like to talk to you about array oriented programming in Python. Um before I get into why we want to talk about that. Uh let's talk about the setup a little bit just to get a head start. Um so this is the schedule on uh the PyonD website. And here I have a material a link to the GitHub repo and it's also at the bottom uh as part of the material. And if you click on it, you'll go on this repository. Uh this repository has a lot more material. We will not cover all of it. Uh there is an outline in the readme. Uh I hope to get uh down to part and be done with part three at least. We'll see about four and five depending

**[0:54](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=54s)** on the time because it's a pretty crowded room. Uh but regarding the setup uh it's pretty simple. Uh if you have Pixie installed uh if you're a Pixie fan or you can install it via just a curl command you can just clone the repo and you can run pixie run start and this will just create an environment for you and run a Jupyter lab session. Uh at the same time uh I have like uh virtual end and con instructions uh but they contain more steps. There's also this launch binder link uh where you can click on it and you will head over to uh binder and this basically builds and runs the repo as a docker container and you get the same thing on the browser. You'll see like that I instantly get a jupitter lab session. Uh for me for the sake of uh performance I will just run it locally. So I will just go back and uh I have the

**[1:45](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=105s)** repository cloned even on my machine. So I will zoom this thing in and I will just run pixie run start. I already have the environment so it doesn't need to build and I actually want to open this in another browser to be more clean. So I want to open it up here. Okay. Uh all right. So try to go through the setup as I speak because there will be some puzzles and some exercises for you to do. H but we'll get started with part one. So we go on the part one lecture. Let me see if I can do presentation mode. And I guess I can zoom in a little bit. And I hope that's visible. You also have the extra screens at the back. Um but yeah, uh let's get started. So as you may know, uh there's different uh programming uh paradigms and they are

**[2:33](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=153s)** rough classifications of styles of programming and I've named here a few. So there's structured programming. uh you use uh if uh statements uh for while statements basically is for control flow and nearly all languages have that uh procedural programming it you define subprograms called functions and again nearly all languages have that uh and then you get into object-oriented programming not all languages have that and you treat all objects treat all things as objects and they can be modified by by methods and so on Python, Java and C++ have this uh and there's more. There's actor-based programming. Uh imperative programming is something uh pretty common. Uh functional programming is a very different paradigm in languages like lisp and strict

**[3:21](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=201s)** functional programming in languages like Haskell. There's also reactive programming where relationship between variables are kept. So you modify a variable and all the related variables are modified as well. Declarative programming for languages like HTML and CSS mostly like for markup things and SQL. Um and yeah and eventually most programming today is structured, procedural, object-oriented and uh imperative and uh we can look at the difference very easily even in Python uh between imperative and functional. I will just import numpy. I I just want to use it in this case as like a container in the form of a vector let's say and um you can have uh you can do uh let's say you

**[4:10](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=250s)** want to take the square of every element in the array and you can kind of do that uh imperatively you can have your input data and you can create a vector or an array in this case a numpy array for the output that just starts as zeros and then you can iterate over all the input uh over all the elements in the input square them and set them to the corresp responding position in the output and that's a imperative programming style. You can do the same uh functionally in Python uh where you will have a square function that describes what what it's meant to do on one element and then you map that function across all elements and then you create the output array using from iterator. So you can achieve the same result and uh Python has a built-in syntax for some functional operations without

**[4:59](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=299s)** making the functions explicit. So for example uh list comprehensions are are a type of that. So uh you do not make the function explicit but it is a form of functional programming when you use list comprehensions and dictionary comprehensions. Okay. Array oriented programming. Um why do we need to talk for about array oriented programming first of all? So array oriented programming uh basically the reason we talk about it is because uh it's uh how big it is in uh data science and uh dealing with data in Python and it's a very distinct paradigm and the way it basically works is that if you want to have a something very performant in Python you basically are not very allowed you're not allowed to do it in Python it will be the performance of Python kills you. So most

**[5:47](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=347s)** code today whether it is machine learning, data analysis or things like that they have kind of have two layers. You do the lightweight metadata bookkeeping in Python. So you have your arrays as Python objects and you express what you want to do with them in Python but all the heavy computation is offloaded into some pre-ompiled library that hopefully someone else has written for you. Um the way it is in Python is that uh it works like this basically as a paradigm in the beginning is you have this array and you say I want to square my array and there's an implicit for loop there. So what numpy will do is will say aha I want to square that array. I want to I need to loop out of on every element of the array over every element of the array square it and and

**[6:35](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=395s)** output it somewhere. And in this case, we save the output to a variable called output data. So there's some distinct differences that I've listed here uh with respect to functional programming. Uh in functional programming, the functions describe uh what the function is meant to do on individual elements. In array oriented programming, that's not the case. You describe what you want to do on the whole array. Uh and array oriented programming sometimes encount encourages in place operations. uh numpy can do that but there are exceptions like Jax doesn't have in place mutation um and um in functional programming that's either not not encouraged or forbidden uh and array oriented programming uh emphasizes in changes in the distribution of data while functional programming emphasizing

**[7:22](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=442s)** changes in components of a data structure. Let's look at a larger example. Uh I'm a physicist so we'll do a we'll use a physics example. It doesn't matter what it does. But what I want to have here is I will have a system of the sun and two planets and I want to calculate the gravitational forces between them. It doesn't matter how that's done or um uh what you want to look for is the style. So if you look at the imperative programming style, uh it works like this. I will loop over all the elements in in the input and I want to take all the possible pairs of planets. Of course, I don't want to calculate the force of a planet with itself because that force is infinite. Uh so I will have this if statement if I is not equal to J. And then I calculate the forces. I calculate the displacement. Uh I calculate the forces

**[8:11](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=491s)** and then I in the total force I just like some plus equal the force of another object and then I return the total forces. So this would be an example of how to do this imperatively. In functional actually this is very complicated to express in Python. uh in functional I can import things from iter tools and fun tools and I can have I can express everything as a function. I can have a negate function that takes the negative. I can have a subtract function. I can have an add to add two vectors. I can have a function to take the magnitude of a vector and so on. And I have sub functions and then I call them eventually and I will get uh create this function. Let's look at the array oriented programming. the array oriented programming on first look it's much much smaller right uh it's a little bit more complicated to express so for example to

**[9:00](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=540s)** do all the possible pairs I have to use this uh weird function called try up indices in Python uh this gets me the indices of all the possible of all the possible pairs uh but once you get your logic around it and the dimensionality this function is smaller number one it's smaller Number two, all three of these functions produce the same results when I run them. Obviously, otherwise I would have made a mistake. Um, and what one can do is you can see uh we can just run them. Let's hope this works. Uh, so I can create an animation uh the sun earth moon system and I can let it play.

**[9:48](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=588s)** So this is what these this solution gives me. Uh and this is how it basically works. Uh I can have a three-body problem and I can get this animation in map lib. It's a nice solution to the threebody problem. Um and I can uh just like throw 25 planets out there. Um and I'll probably get some crazy things happening. >> [laughter] >> Um, so I can do all this stuff and all the functions give the same results. But let's time them. If we time them on a relatively large input, relatively large, only 500. Um, but let's try to time it. I'll use the magic um time it util of uh IPython. Um, okay. Imperative

**[10:39](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=639s)** forces about 250 millconds per iteration. functional forces is much much slower. Um like this is a horrible idea to do in Python for this case. Let's look at the array forces. The array forces is much much faster. So it was shorter to write and much much faster to execute. Okay so why is that? Uh this is a comparison. So for all these pairs, this is the number of lines of Python and the number of Python byte code instructions that it has to execute. Um but the other thing is what do these bite code instructions do? Um so uh the functional and array forces have the same number of bite code

**[11:26](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=686s)** instructions but the bite code instructions may be in the functional forces are doing Python things while the array forces may be call into that uh C library numpy and do the computation there. Um so this is how it typically works. uh I can have this uh and if we look at this I can make a function that just returns x plus x and I can run it on an big numpy array 1 billion and I can look at the byte code of that. The bite code is very very simple. Byte code basically if you look at the Python byte code well this just says like load X and X again and just uh binary add them together. And this binary operation will call into uh NumPy and NumPy knows how to do this addition fast. Uh and you could see array oriented

**[12:15](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=735s)** programming as a workaround for Python being a slow language. Um and it's more general than that though. uh it's equivalent to vectorization at the hardware level and if you're programming in uh GPUs in any language you need to be thinking in arrays because arrays can do well the arrays can um sorry GPUs can do compare and branch operations really well but they can do vectorized adds and multiplies very very well for example so you need to be thinking about that uh here's a little bit of history of um aroriented languages uh and libraries basically not only language so history starts within the 60s with APL and today we have uh the modern ecosystem where a numpy came up here and modern libraries and now Jax uses XLA. Uh Matlab is also

**[13:04](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=784s)** reoriented um yeah um and uh the consideration to take is about data frames. So if we try to think about which is the most um idiomatic pandas if you ever use pandas um so the sec the first one would be the array oriented one in pandas the second one would be the uh functional one and uh well the third one is basically forbidden you don't want to do that uh but you would iterate uh over the data frame um but if you are a person who thinks like in these query languages maybe the second one is more idomatic to you to To me, this one is more idiomatic. Uh, but that's like to me that's a personal preference. Um, all right. So, how to think in

**[13:54](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=834s)** arrays? Uh, the best way to think in arrays is by actually solving problems. So, we'll start doing just that. We'll have a few puzzles here. Um, okay. And, uh, let's get started with those. So, I will get started and I will have this array. And this array basically looks if I draw it, it looks like this. It's like two layers on top of each other. And uh each one of those layers is u three rows and five columns. So the array looks like this. And I can write a slice in numpy to select the elements that I have uh colored here. So I can do it like this. I can say I want both layers and I want all the rows then skipping

**[14:43](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=883s)** the first one and all the columns skipping the first one. So that will get me this or I can select these elements uh and that's the puzzle uh is to write a slice. It will not be long. It's like two like maybe a minute or something maybe a little bit more. Uh to write a slice to select these elements and I have the solution down below. I don't want to scroll to that but uh yeah let's do let's do like one to two minutes about this. You the job is to write a slice that select the colored elements in this picture. The blue ones at the top. Oh, by the way, any questions? You can just raise your hand and uh work without >> Oh, you need the mic. [laughter]

**[15:32](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=932s)** >> If any any of these would be that fast without numpy or is the numpy like really integral like extremely important for this array based programming? >> Uh there are many other array libraries. So you could do the same with jacks. Eager jacks would be like it would be a little bit slower than numpy but excel compiled jacks will be faster than numpy uh pietorch uh there's many array libraries out there like even the tensorflow tensor is uh can kind of do that uh yeah so there's many many array oriented libraries out there basically anything that has a tensor object is an array library >> the pure python ar >> well the pure python If you're if you're talking about the array module, that doesn't offer you all that. You can't

**[16:20](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=980s)** say array + one in the array module. So it's it's it's a storage container which is efficient because it's a C, but it it's not a computation uh engine. You can't Yeah. Okay. Uh I will move ahead with the solution because this is a relatively simple problem. Um so the solution is this. Uh okay. So you can it's actually like two sol like there's two ways to do that. You can keep the first dimension or you can drop it. Um so you can do that. So you can say I want from zero to

**[17:12](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1032s)** one while leaving one out in the first in the first axis. So that drops the second layer. And then you want to say I want all the rows because I want all the blue all the blue uh from top to bottom. And I want from two to the end. So skipping the first two in the last dimension. That will get you this. And if you look at the shape of that, it kept the first dimension. So it's 1x 3x3. Um what you can also do is you can say uh I actually only want the zero here the f the first one. Um and this uh produces like the same uh let me show that produce the same thing but it shape is 3x3. So it dropped the first dimension. So it kept only the inner two. Um these are different answers. Uh both

**[17:59](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1079s)** are fine. So we'll move on to numpy puzzle 2 and I will show the hint even. So the task here is to compute the size of the spa of the spaces between consecutive elements in the following array. So uh you have this array uh 1.1 2.2 3.3. So what's the space between the consecutive elements? We can easily see that it's 1.1. The job is to do it in an array oriented way. And I will give you the hint. You can think about it this way. If I take my array and I subtract it from it and I subtract itself, I get all zeros. But what if I can just uh not get the whole array? If I can get the array skipping the first element all the way

**[18:48](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1128s)** to the end and subtract from that the array from the beginning to all the way by the last element, I get what I want. So the job is to write that using slicing. Uh two two two minutes maybe. Yeah. Okay. Uh need more time or no. Okay. Good. All right. So, uh let's move on to the solution. The solution is this. It's very simple. Uh the this slice skips the first element. So it's from index one all the way to the end. And then this one skips the skips the last element because slicing in Python is to the one before. So you go to the one before

**[19:37](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1177s)** minus one. So it skips the it skips uh the last element here. If you do that you get that. And there's a utility numpy because you don't need to think about that. Uh numpy has a np diff. So you can call that and you can do you can get exactly the same thing. All right, following this in a very very similar manner, I have this parametric curve here. Parameterizes from 0 to 2 pi with respect to t. I can plot this curve. It looks like this. It's a listenu curve. Um, and the task is to calculate the length of this curve using array oriented programming. So the way you want to do that of course is that this curve is from points. So you want to

**[20:24](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1224s)** take all of the small segments. The rule to comput its length is the Pythagoreans theorem. So you kind you want to do that for each one of those segments uh of x and y points. Um let's do three to four minutes maybe. Yeah 3 to four minutes for this one. So you want to calculate all these points, all the all the delta Rs and uh sum them together basically. All right. Um need more time. No good. Okay. Uh let's move on to the solution about this problem. It's a pretty simple one. Uh you need to use exactly what we did right above.

**[21:13](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1273s)** uh you need to do exactly this trick to get all the x differences. You need to do exactly the same trick to get all the y differences. And then you need to square them, square each, add the square root on the outside to calculate the delta rs. So this would be an array of all the delta rs. And then you need to sum them together. And this is the solution. And you just get a single scaler out. The length is 21. And by the way, this and this can be replaced with a numpy diff each. So you can do that too. All right, a little bit harder problem. Uh so we'll spend a little bit more time on that. So this is a picture. It's a picture of my experiment. Uh well, when it was being built. Um but

**[22:01](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1321s)** it's in the data folder. And the shape of this picture, it's 1920 x 2560 and times 3 for the color because it's RGB. Um, they're all uh unsigned integer of 8 bits. 8 bits each. U int 8 is that in numpy. So the task is to scale this image down sample the image by a factor of 64 on both sides. So the strategy for that would be to do something like this. So your strategy should be uh to reshape the array. There's numpy reshape for that or dot reshape on the array. And then dimension of 1920 becomes 1920

**[22:53](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1373s)** integer division by 64. And dimension of 2560 becomes that. And then you want to take the mean across all that. You want to get the mean color. You you want to get the mean pixel value. Let's say um so you want to kind of do that. So your array starts with a shape like this. You want to transform the array to a shape like this. And then you want to reduce those dimensions by taking the mean across 64 here and across 64 here. So your final array should have a shape a shape like this. And you can use reshapes and you can use also because numpy will turn the to the floating point dype. You can then uh use as type uh as type and you intate to view it back as an unsigned integer. Uh so this is the job. Uh five minutes at least. Yeah,

**[23:43](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1423s)** five minutes. And I think the solution should be right below but try not to look. Okay, I will move on to the solution uh a little bit for the sake of time. Uh the solution, well there's three uh almost or very very equal solutions that get you the same result and they're very very close. So the simple one to do is to do this. So you take inside your image and you reshape it according to exactly the shape that we said and then you take the mean along axis three which will be the new axis that is created and then you take the mean along axis one which will be the other new axis uh that was created. So you can do that or you can do the same and you use

**[24:32](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1472s)** negative axis numbers. It's extremely identical axis go backwards start from minus one and the last and they go inwards. So you can do that or as the third option is to use uh keep dims equals true. That's a that's a argument of numpai's reductions like mean uh to keep the access the axis number from changing and only remove the length one dimensions afterward. So this would be like what we did at the beginning where we had two layers. We could either drop that layer or keep the other layer. So this would keep the dimension but it will be of size one dimension while normally numpy minim will drop the dimension completely. So you can do that and then you eventually you can do keeps the axis will not change and then you can re finally reshape it eventually to drop it. Finally all these are

**[25:21](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1521s)** equivalent by the way and I want to convert the type to unassigned integer uh one bite and I can look at that and it's a down sampled uh picture. It's the same picture just downsampled. Okay, the final puzzle, this is some uh data from uh the file format is from my experiment, but it doesn't matter. Uh is we can look at this file and we can get some raw bytes. We get a bite stream from somewhere. We get some bytes and we have this and we can view those bytes. Uh if we look at even that, it's just some bites. It's pretty big. Uh yeah. Uh oops. Yeah. uh and I can interpret those bytes using np from buffer function as an unsigned integer 8 and I can tell it its size uh

**[26:11](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1571s)** no m doesn't matter how I came up with it. So this should be a uh 300 uh 3,131 floating point values. But if I view it as a float, you'll see that the numbers are weird. And that's the re the reason for that is that this file format is big Indian. So the most significant bit is uh is the other way around to normal computers nowadays. There are there are old machines like IBM 390X that are big Indian uh but they're pretty old machines. Um one way to do that is you can view it with numpy with the wrong Indianness. You can view it as big as big Indian and the numbers are now fine. So this means uh this is the bite order. So this been big Indian and this is little Indian. So

**[26:58](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1618s)** this doesn't change anything. This does and uh f4 means uh float 32 uh in numpy. Um so you to fix the emptiness what you need to do is you need to reverse the order of bytes in groups of four. So you want to take every four bytes and you want to reverse them. And that's possible with a reshape and a slice only. And I'll show you where that came up recently. It's uh actually good timing. Um but you can do that. And numpy also has a bite swap function dot bite swap on an array but you don't want to use that in this case. Uh so yeah uh let's do another five minutes for this problem or four or four minutes or 55 but yeah uh that's the task.

**[27:50](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1670s)** Okay. Uh I will go to the solution for this. So this is the solution. It's pretty simple. Well what you want to do is you want to reshape the array. And let's actually view it step by step to see the shapes. You want to do this. So the original array had this much has this shape. You're going to use dot reshape. You going to say minus one on this axis and four. And the shape of that will be it will extract every four elements into an extra dimension. So this has 31 31x4 and you want to reverse that dimension. So I want to keep everything in the first dimension. I want to keep everything here. And with this slice trick, I want to reverse the order in

**[28:40](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1720s)** that dimension. And then I do again. So the shape of this is the same. If I copy it over here and I do shape, it's the same, but the order of the order of those four elements has changed. It's now reversed. And I can do another reshape to flatten it out. reshape minus one means flatten it out. Um, and if I do this and I view it now, so the actual array is in date. So if I view it as float 32, you'll see that now the numbers are correct and it's it's way more efficient. I did this bite swap now it's efficient. Numpai doesn't have to work with the big ND and D types. Where does this come up with? Like why do I care about this? I actually had to care about this very recently and I want to show you. Uh I actually I wanted bite swap to work on uh on uh multiple libraries. I

**[29:31](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1771s)** wanted by swap to work on numpy jacks and kai and um one sec. Uh I want to search for it. Uh come on not is open. Uh let me see if I can find it. Yes. So I asked this question on Jax. I asked why don't Jax implement byte swap and I said that I can have byte swap in a very very similar way. So you'll see that I I I do this view. I flatten it and then I reshape it with depending on how many bytes I have. So this would be four here. Then I do the reversal and then I do exactly the same thing and I

**[30:19](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1819s)** also because it's not flat I have to form it back to the original shape. The answer was that there's no particular reason and uh Jake who's Jack's uh maintainer told me that it would be the actual implementation would be like this version um but it can use lower uh lower level things. And if you look at the actual implementation that Jake did like very very quickly, uh if you look at the changes here, it's almost the same, but it uses a lower level primitive. So it uses this bitcast convert type instead of a view and it uses this lax rev to reverse the bytes. So this is like lower level primitives that Jax has. But like this is one case where I had to care about this very very recently. Uh okay. Uh any questions here before we move on to uh part two which will be about the

**[31:08](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1868s)** limitations of reoriented programming. No. Okay. Oh, and by the way, uh there is for a much larger tutorial that would be like four hours. There's a pretty fun project in part one which is to implement Conway's game of life using an array oriented way. So you'll have to calculate all the live neighbors and do an update to the world using array oriented. So you not you will not for loop over all the elements of the world. Okay. Now we move on to part two and we look at the limitations of array oriented programming. We've seen the advantages how it's easy and expressive and how to can express things and but let's consider the disadvantages. So we'll have a an a formula here a function which is just a quadratic formula.

**[31:55](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1915s)** And this function the way it's expressed you can see that it doesn't really care about what the input is. It can be numbers or it can be arrays of equal dimensionality and they will all these operations like additions, multiplications and so on will be done in an elementwise fashion. I can create some big arrays here 5 million each and I can do an imperative quadratic formula. So I will loop over all the elements and I will calculate the quadratic formula and I will time it. If I time that thing, we will see that it takes it's pretty slow. Takes about one and a half second per iteration. Now let's time the quadratic formula with numpy. Much much faster 28 millonds.

**[32:44](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=1964s)** And now I have imported a library here called num expert. And I can tell num expert to evaluate this. Uh I have to pass it as a string. But I can say I want to evaluate this expression using num expert. Do you think this is going to be faster, slower, or equal to numpy? Uh just show of hands who thinks it's going to be faster. Show of hands. Who thinks it's going to be slower? Okay. More slower. More people say slower than numpy. Okay. It's much faster than numpy. The slowest run is because it has to compile this expression. is slower but yeah uh let's try to run it again for example okay it's faster than numpy and why is that what's the problem with array oriented programming or with libraries like numpy

**[33:32](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2012s)** the problem is that each operation creates a new output so when you want to you have to array a plus b a plus b you add them together numpy says hm I need to create new memory it asks your system allocator called malo and or your python allocator uh and it requests new memory to store the output. Then it does the addition it it stores that it stores that on the new on the memory it requested. Then you multiply the array by something. It does the same again. So every operation in numpy creates new arrays. So we have a problem of intermediate arrays. Num expert is able to fuse that one out and says hm I don't need all these intermediate arrays. I don't need to do four times a store that in a temporary then multiply by C. Nump says I can do one loop. Uh is there a question? Oh no.

**[34:21](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2061s)** Okay. Uh and that's the problem. And if the arrays are larger than your CPU caches and it has to reach out to memory and that's slow. So what Numpai is doing is almost like this internally. So it negates B stores that in temporary takes a square of uh B puts that in temporary and so on. So so that's what Numpai is doing or almost that internally and if you time it with the previous one you'll see that Numpai can do some a little bit some smarter things but it's same order of magnitude. It's really close like this one where I explicitly had all the temporary Python objects in Python code. um it's almost the same. So right so numx on the other hand turns something

**[35:12](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2112s)** in uh some turns the expression into something that can be quick quickly computed. Um right so one can even one can go even further. Have you has any of hands who has used number before in the room? Okay a few people right. So you can actually compile that expression. There's a decorator using number. Namba is a compiler for the Python language. It compiles Python byte code. It looks at the bite code on the first run, tries to compile that using LLVM and it then it replaces the function with that compiled code basically. So this decorator on the first call it will look at the bite code, compile it and then it calls that it doesn't call Python anymore. Um so we can do that and if we time it we'll see that this is uh much much faster.

**[36:02](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2162s)** um or we can compile it about Jax and we'll hear more more about Jax in the in the next part. So Jax uh can do that. It's it's a basically the same expression. I just like use Jack's numpy instead of numpy and I use the atjit decorator to compile it and I can time that. I have to use block until ready here because Jack does asynchronous dispatch. So if I don't use that I will only be measuring the time to do the dispatch but don't actually compute it. Jax is like on par with numpai number really or really close even faster than number uh well it's mostly faster than number I will say if we make a scorecard of some numbers like these were run some time ago on another system it can look something like this Jax now has gotten obviously much better so this is like not true anymore here we

**[36:50](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2210s)** saw it was faster uh but it looks like that and the exact result depends on many things whether you compile with fast math number has a can do fast math. There's different compiler optimizations that can be or cannot be enabled. Um but the idea is usually that uh the general pattern is numpy is orders of magnitude faster than pi than pure python and then compile and then these compile libraries like numpa and jack is actually down here. This I should update this is a bit wrong. Uh are faster than numpy. Uh and they can be depending on what they do. They can be much much faster than numpy. uh let's talk about expressions expressiveness uh and things you can't do. So suppose the scypi library doesn't exist and I

**[37:38](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2258s)** want to the log of the gamma function. Uh scypi has this so I can just reach out and it's efficient. I will go and read off like a book like numerical recipes and I will find that this is the algorithm translated from forran to python and this is the algorithm. You'll see here it has a fixed size loop. it's over the over some coefficients and that's the algorithm to compute the log of gamma function and I can compare that I can compare that with uh again sci's implementation and I can see it's exactly the same right and if I look at the difference pretty small difference compared to sci so my implementation is correct okay but this function you'll see that it doesn't matter if it

**[38:25](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2305s)** run over single element so here in this case I run over element by element but I can also run it over the whole array. There's nothing inside the function that says that it can't run over arrays. The loop is over a fixed number of coefficients. It does a global update. There's no if condition. There's nothing like all these operations can be run over arrays element wise as the same way they can be run over elements. Um but let's look at a bad function. So if I want to the what is this the regularized incomplete gamma I think. Yeah. Uh so you'll see that the algorithm here is different. So the algorithm here says do this 100 times but if this condition is true then return.

**[39:17](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2357s)** Okay otherwise it did not converge. Uh all right so we can evaluate with with Python scalers it turns out like this expression I can evaluate with um right but that's a single element so this is like it says like if this delta is small enough of a single element return early can evaluate it and it works element by element right I have this I I do this list comprehension uh and I can look that it's it's uh cyp has this uh it's correct but let's try to evaluate with an array x's is now the whole array we can't do that numpy rightfully says the truth

**[40:07](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2407s)** value of an array with more than one element is ambiguous and that's true because delta and these things here are now arrays so what does an if statement mean on an array If uh what if the whole array if any element if all the elements so that's ambiguous that's why numpy has np all or np any to check the truthness of all the elements or any element and whatnot but you can't do this um so there's two ways to work around this issue in arorentoriented programming mainly one is to remove the if statement and needlessly compute compute more like go all the way to 100. I don't care if an element has converged. Keep doing this iteration. And you'll see that it's now

**[40:55](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2455s)** evaluatable. Number one, and number two, it's correct. Pretty close. Cypi or you can do the more complicated thing. You can keep track of the ones that have converged and not needlessly update those. You can do these like end equal updates. You can keep track using array oriented using a mask of which ones have convergent, which one have not. This is also correct. This will get you the number. So this is like the array oriented way of writing the if statement basically because you have a mask over all the elements um and tells you which ones you need to update and which ones are not with trus and falses. But which one do you think is going to be faster? Which one do you think that is going to be faster? Do you think that the one

**[41:42](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2502s)** that's doing that we're doing this masking is going to be which who thinks that this one is going to be faster? The one that we're doing the masking. Okay. And who thinks that the other one is going to be faster? The one that we do we needlessly go to 100. Okay. So let's run it. Yeah. [laughter] The one where we keep going all the way to 100 without caring is faster because all these extra operations in numpy have need to create new memory, you need to create new arrays, you need to do more operations. So it's slower. So it's very often more efficient in array oriented programming to just keep going without checking. And if you've done any machine learning, you probably have seen this kind of loop. So the machine learning model has like many

**[42:32](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2552s)** parameters like let's say maybe like a few if maybe like a simple feed forward network but no one ever checks has this weight converged no you always update you say I want to do this for an epoch you remove it you remove it from the optimization process is hyper parameter and you keep going 100 times even if a weight is at the local minimum it will be stuck here it will not move by the minimization algorithm but you keep going and this is like the algorithm. Um, okay. Any questions here? And we'll go to part three afterwards. Either I'm doing a good job explaining or a very bad job. Okay. Uh, right. Let's move on to part three. So, part

**[43:19](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2599s)** three will talk about how we overcome the problem of part two. We talked in part two that you know um there's all these intermediate arrays. Numpai has to request memory all these uh temporary things. Uh yeah what to do with it. Okay. So let's look at JIT compilation a little bit and we will consider the quadratic formula again for just because it's a continuation. It's a numpy quadratic formula. We've timed it. We saw that on my laptop it takes about this time. Okay. We can number one compile this quadratic formula with number. You can use the number jit decorator or njit is stricter means that number is not allowed to fall back into python. So

**[44:09](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2649s)** njit means that everything needs to be compilable in this expression. If you use jit with no python mo with python mode on uh namba can fall back into calling the python interpreter if there's something that it can't compile. It can be slow of course right? ng is strict. It's no python. It's equivalent to JIT no Python no Python equals true. Uh and it means that everything needs to be compilable. So all the Numba needs to be able to infer all the data types and everything to compile that into machine code. What different pattern you notice here? If you notice here when we wrote this numbered function we switched to writing from array oriented code we wrote imperative code. So we looped over the elements. We wrote C like or C++ like code let's say in Python and we let Numba do the job to compile it. So let's

**[44:59](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2699s)** look at that. Um okay the first one is slower because it has to compile the first time because it's going to run it in Python and compile it. But the second one is faster. You'll see that numpy took about 27. number here takes about six to seven milliseconds to do this when compiled. And let's look at the Jack's case. Well, the Jack's case, the formula is exactly identical. In Jax, we write array oriented code. We can time it. We do it once. It's slower. It's it's a little bit slow because Jax has to compile. Now, it's super fast. So, uh the the um but Jax is much faster though. The question is why? Um well

**[45:50](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2750s)** it's very hard. I found it it's very hard to make jacks not use multiple threads. So jacks implicitly like multi- threads operations too. Uh and you can do this in numpy in in number two. You can use parallel true during the JIT compilation. Um and if you time it again you'll see now it's on par with jacks. So if you let uh use multiple threads it's going to reach jack speed for this problem. Um and the thing is that one thing you notice is whether you write array oriented or imperative code in Python you write imperative code you write these loops over the elements in number you write imperative code that you compile it and it's fast in array libraries like numpy

**[46:40](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2800s)** awkward array or jack you write vectorzed code and J because it has the ability to compile too. It's going to be faster than those. Okay. And let's talk a bit about uh very briefly how does the JIT compilation even work in Jax. Um so the first step is Jax traces through the function with uh no real data. It only tracks shapes and D types across the function and it emits an intermediate representation that calls Jack's PR. You'll see if you read through this, it looks very much like our pendant quadratic formula. The integer power y = 2b. So you can see the the mapping between these two. Then what Jax does is it lowers it to something to another intermediate representation

**[47:28](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2848s)** called stable HLO that also looks like a bit like our pedantic quadratic formula but more complicated. Um, and then it compiles that lower.compile and then I can call the compiled program and it works. So, so that's all the steps J does inside when you have a JIT decorator on top of a function. Now, let's talk about the bit of the limitations of both. So, the limitations of number. Well, I will try to put a dictionary here in my function. Whoopsie. Uh, yeah. uh number tells me I can't compile that in no Python mode. Uh because the d the dictionary doesn't have known types. So you don't know what the values are or the keys can be. It

**[48:16](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2896s)** can be of any type. Numba internally has a number typed dict. So you can tell number that these will be the data type. So then it can compile it. Um but that's like one limitation of numpy and the number and I think there is a uh for more information here will number work for my code. Uh it's in the docs of number. uh tells you whether it's a use ca good use case or not. Limitations of jacks. So because we're writing array oriented code, we talked about uh these if statements and the the why they can't work very well in array oriented code. But what about this? I'm using any. So this is a number. So I have here if any of the elements of the array are greater than three, sum them. Sum all the elements of the array. Otherwise do the

**[49:05](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2945s)** product Jack also can't compile that attempted boolean conversion of traced array with shape bull. The problem here is that because during tracing you don't know the date of the array. You only know shapes and d types and then you don't know what array you're going to call this function with. So Jax doesn't know do I compile this branch or do I compile this branch. It doesn't know. uh it's you you're in Jax in general you're not allowed to have operations that depend on the contents of the data in machine learning that's fine because all the machine learning models there's nothing that has to do with the contents of the data inside the model it's like do these matrix multiplications I don't care about the input uh that's the limitation of JS uh data dependent operations are not traceable

**[49:52](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=2992s)** um another thing is you can't compile problem with programs with unknown shapes we've talked a bit about this with the Jack's team they may at some point have this for the only the CPU but they don't want to have this I think it's not even possible maybe for the GPU uh but this operation of course I don't know the shape I don't know how many elements are going to be larger than three so I don't know what the shape of this operation is going to be I always need to know the shapes and d types so this one is another thing Jax can do array boolean and this must be complete concrete Um impure functions are also very very dangerous with jacks. Uh because here I have this do sum equals false uh and number actually. And then I

**[50:43](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3043s)** compile this. If do sum do the summation or do the product. Whoops. I said do sum equals true here. But the function does not care. It's already compiled. So number does not look into P does not look into these variables like it it has compiled it on the first call. It has compiled it here and now it says oh here's the machine code for that function. So every time you call that function call that C code. It doesn't matter if I change something. So don't do impure functions. I always have a have pure functions. Same for Jack. Same exact same problem. Okay. And if you look at actually because I don't know how to do this in number, maybe it's possible. If I look

**[51:30](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3090s)** at it in Jax, you will see that Jax actually uh compiled the product. If you look at here, if you look at the traced program and you'll see that well do some false actually compiled the product. So every time you call it, it will do the product. Doesn't matter if you said do some equals true. Okay. Uh I don't have a GPU so I can't run anything. Um but there's JIT compilation for GPUs too using Namba CUDA and there's like more uh I think Nvidia has like has multiple like kind of domain specific languages in a way to jit compile for GPUs and jacks on GPUs. This is the funnest part is that you don't care at all in Jacks. Uh it just automatically dispatch. So the symbolic operations of

**[52:18](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3138s)** the intermediate representation will just dispatch to GPU kernels if your input is on GPU and will dispatch to CPU kernels if your input is on CPU. So with the with jacks or other libraries like that PyTorch or everything else PyTorch has torch compile for example and things like that the same code works on GPU and CPU you don't you just need to change the the device input uh the sorry the location of your input. Um, okay. Uh, questions here. Uh, any questions? Nope. Okay. Uh, let's see. I have 20 minutes, I guess. Uh, all right. So, I will say that um uh I I will not spend time here. It's a

**[53:06](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3186s)** short tutorial to look on the project. Um but there is a uh you can try to do this at home. The job is to JIT compile this function. Uh but I want to point to the solution. There's a wonderful write up here. Uh again if we look at very very briefly the solutions you will see that in the Jax case it's array oriented code the and in the number case it's python code that number has was able to compile and I can even use fast math equals true which is equivalent to -f fast math in the C++ compiler um but there's this wonderful write up in the end and I want to show this plot basically uh there was this uh discussion why is Jack so fast uh it's uh Jack's discussion. It's a pretty long discussion and it came from this uh plot

**[53:56](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3236s)** basically uh and this is how the landscape roughly looks regarding this again is that python is going to be slow numpy array oriented it's a lot of inter all representations of the same problem so it's this mandelro set on on different accelerators so this is C++ imperative binded with pi by 11 syon number imperative Numba vectorz jacks array oriented coupi arrays numpy for GPUs and then kupi with a custom kernel imperative per pix per pixel numba cuda and jacks array oriented with JIT compiled and this is roughly how it looks. So the best CPU is about here all of them are uh faster than uh all of them are faster than of

**[54:44](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3284s)** course Python. The CPU is faster than even numpy basically. So if you write your custom code and you compile it with Namba or C++ and you bind it with pi bind 11, it's probably going to be faster than numpy. Um but you don't get the beauty of writing numpy. Let's say you have to write your own uh C++ loops or whatever. And the GPU is obviously much faster in the end, right? Because uh GPUs are very very much built for these types of operations. Okay. Uh okay. I want to talk uh okay let's do part five actually instead of part four given the time I think it's more important to show part five. Um I just a curiosity how many of you would say are like uh data scientists or working with big data or Okay. So

**[55:35](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3335s)** there's a few. Okay. Right. So I want to mention a bit about uh tools you may want to reach out for in large data sets. So everything we've been we've done here is my data fits in my RAM like my arrays. Okay. I have I can have my computer like 128 gigs of RAM but that's not big data right? Uh okay. And so real actual I can have this array which is only like 800 megabytes now but real scientific and industrial data sets are gigabytes or terabytes of data. Uh and we need strategies for chunking the array splitting it up because uh no one has a machine with that much RAM. Um we need to maybe compress and often data are we want label

**[56:23](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3383s)** dimensions. I see here an XR sticker. Um uh you may want to have label dimensions. Maybe like the index number five doesn't mean anything. Maybe that index means Wednesday of that year of that month something. So you want labeling too. Okay. So the first library I want to mention this will be just mentioning like just what things exist and what things you may want to reach out for. They're all like array oriented libraries or array libraries. So there's Daskk and specifically Dask array. So dask array is a chunked and parallel arrays. So a dask array I can look at it here. It doesn't have it doesn't hold any actual data. It's this array. I know its shape and d type. I know it size. I know the chunk size. And I know the d type here.

**[57:13](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3433s)** And you can do operations as if it was numpy. It has a numpy API. Numpy like API. And the result is uh here another array. Uh okay, I took the square and I reduced the dimension. So it's going to look like this. So this has these types uh this shapes and this is the size of the chunks. Um and this is not computed. So this is actually a computational graph. I can even view it uh if I do result. Please graph v work. Okay, thank you. Uh yeah, I can visualize this. So this is what Dask does internally. it uh it does it creates a graph of operations to be done uh in a chunked manner and eventually uh I to actually get the result I need to call compute and this

**[58:01](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3481s)** can run on uh a lot of things that is dusk distributed so it can run on my local CPU I can use multi-threading it can use multipprocessing or it can be like a distributed cluster with like a thousand workers uh you can uh put this on like uh you can have like a thousand AWS Lambda functions or whatever that uh do this parallel work. Um let's I want to try to time it in this big array. Uh it actually is faster than numpy. Okay, cool. Um maybe it's not for some problem for these small problems on my on my laptop, but it actually is faster than numpy. Um this chunking and the fact that it can do parallel work using threads, it it makes it faster than numpy in this case. So there's another library I want to mention. There's Zar. Zar is more like

**[58:51](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3531s)** or at least to me an array storage format designed for large data sets and it's also a chunked uh it also provides a chunked multi-dimensional array. That array can live on disk or it can live on your memory too. So I can open here a zar storage. I can put in my temp directory and I can I can first open it then I can set all of its elements to my big array. And if I look at the info, I'll see here that it's an array. It has little ndiness float 64. That shape uh uh chunk shape. It has some specific compressors. Uh it's a compressed array on disk has this number of bytes. Uh it's not read only. So I can write into it. And this array now just like lives here. It doesn't have anything. If I look into it, oh, it's something. It

**[59:39](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3579s)** doesn't have any data. And Zar basically uses indexing to evaluate. So this one means I want these elements and this and ZAR will not grab the whole array. It will it knows the mapping of these chunks to disk. So it will only grab a decompress the chunks that's needed and fetch you your array. So you can imagine how that fits into big data uh models. Um and it also has like a and it also works together well with Jack with the Daskk. So you can open up a a ZAR storage as a uh as a dask array from ZAR and you can do the same thing. You can do you can square it and you can uh do the mean and you can compute it. Um there's bloss uh is bloss is a very very high

**[60:28](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3628s)** performance compression library designed for numerical data. Uh and it's often used as a compression codec inside zar but it also has its own array container. And what basically what the ZAR people have found is that often sometimes decompressing is cheaper than going is cheaper than going through the CPU to fetch all the memory being in your RAM. So they found that getting getting from your RAM a smaller chunk and decompressing it is faster. Um so yeah I can create some random integers. I can do blocks to as array. All of those have an array API compatible thing. So the the operations look either are identical to NumPy the API or very very similar to NumPy. So it's very very intuitive to switch from

**[61:15](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3675s)** one library to another how to use it. And this is what um uh Zar shows me. If I can look at the array, it doesn't tell me anything, but I can look at a lot of these things, right? I can look at the number of bytes. Uh it's 400 megabytes. It's 84 megabytes compressed. That's the compression ratio. And you can do you can sum it for example uh and you'll see that okay in this case on my machine it's a little it's a little faster than uh num that this is numpy I'm pretty sure right yeah the numpy uh zar also has a lazy expression engine I'm not very I'm not a big user for for of sorry of bloss I'm just mentioning it mainly uh it also has laziness and it does all these in a chunk manner too so this expression is not evaluated on

**[62:04](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3724s)** spot. It's a lazy expression. You need to call compute to actually evaluate it. And then I think the last thing that I have and uh in the end is x-ray. So x-ray uh offers uh is a adds names and coordinates to array dimensions. So instead of remembering basically what axis 0 and axis one means, you may have longit longitude and latitude if you're working with like weather data. It's very common in uh earth sciences, oceanography, uh and things like that. Uh climate science. Yeah. So you can do that again. You can have an array. You can call its dimensions time, latitude and longitude. And you can uh have the coordinates to be this. So this is at this latitude, this at this longitude. And this coordinate is time. So I can

**[62:51](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3771s)** have actually like day times here. And actually uh it has a very nice HTML representation uh XR. So you can do things here. You can click things. So yeah um and you want to say I want to select from this array called temperature. I want to select where the time was nearest to this. That has a lot of meaning for a scientist dealing with these types of data. And you can do that. uh or you want the mean over well what axis 0 one uh 10 million I I don't know uh but time time has a meaning for uh for a scientist and of course you can plot that and it just looks like that it's just random data uh this is like the the average over

**[63:39](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3819s)** time uh in latitude and longitude and the last thing I want to mention is how they all fit together uh is that bloss compresses and chunks of numerical data z organizes compressed data into an array on disk format. Dusk reads those and processes all those chunks in parallel. It works well with ZAR and so on. And XR wraps the result with label dimensions and coordinates. So like the uh D XR people are big users of Dusk. So XR can technically eat any array API compatible array library. So it just it doesn't care if it's like a numpy array or a Dask array as long as they're both uh array API compatible. So I can chunk I can use this operation to get a X-ray data array that that is backed by Dask as you can see here. Um and I can uh yeah I can

**[64:32](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3872s)** resample it. I can compute that and then when I compute it actually uh goes down to numpy and evaluates to a numpy array. Um okay and the last thing I want to mention here is that uh the main takeaway is that you can scale array oriented programming to data sets much larger than your RAM uh without changing the way you think the same slicing broadcasting all these rules you learn with numpy they work across all these tools and the aroriented programming I'm just mentioning these extends to GPU programming so there's kayi which is a numpy compatible array library for GPUs rapids is a stack of GPU accelerator libraries like KUDF instead of pandas. Uh KPI numeric is a drop-in replacement for numpy that runs on CPUs and uh on multiGPU nodes. Numbuda is to write

**[65:22](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3922s)** kernels on GPUs jacks. PyTorch even though it's a machine learning framework, it's actually also an array library. The torch tensor is an array is an array library. It doesn't have the same API as numpy uh but it's an array library. And there's more things. So you can uh CUDA Python exposes uh CUDA runtime and bindings to Python and Nvidia CUDA core compute libraries also um have Python bindings for GPU accelerated parallel AL algorithms. So um and the last sentence is that array oriented thinking is the common thread and whether you're scaling up to GPU or scaling up to dask or you're using numpy on your laptop the programming model stays the same. So very often you can have code that works across all of these and you just want to change like your library or maybe your the container the

**[66:11](https://www.youtube.com/watch?v=zgIj2oCFF1M&t=3971s)** storage of your array. Um that's all and uh thank you and uh questions. [applause] There's nine minutes for questions. Um I'm checking the uh online part and there are no questions. So, if you guys want to speak up, we'll get the mic over to you. Okay, that seems like there's not much questions, but you can always ask your questions in the Discord and uh um Yasan will be answering that. So, let's thank Yasan again for a wonderful tutorial and have the have a nice day. [applause]

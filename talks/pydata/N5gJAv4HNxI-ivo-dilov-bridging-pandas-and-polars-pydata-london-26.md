---
id: N5gJAv4HNxI
title: "Ivo Dilov - Bridging Pandas and Polars | Pydata London 26"
slug: ivo-dilov-bridging-pandas-and-polars-pydata-london-26
conference: pydata
conference_name: "PyData"
category: "Practitioner AI conferences"
edition: "PyData"
year: 2026
speakers: ["Ivo Dilov"]
channel: null
duration_min: 34
published_at: 2026-06-15T15:52:27Z
video_id: N5gJAv4HNxI
url: https://www.youtube.com/watch?v=N5gJAv4HNxI
youtube_url: https://www.youtube.com/watch?v=N5gJAv4HNxI
tags: ["Python", "Tutorial", "Education", "NumFOCUS", "PyData", "Opensource", "learn", "software", "python 3", "Julia", "coding", "learn to code", "how to program", "scientific programming"]
topics: ["Classic ML & data science"]
transcript: true
---

# Ivo Dilov - Bridging Pandas and Polars | Pydata London 26

**Ivo Dilov**

`PyData` · `PyData` · `2026` · `34 min`

`#Python` `#Tutorial` `#Education` `#NumFOCUS` `#PyData` `#Opensource` `#learn` `#software` `#python 3` `#Julia` `#coding` `#learn to code` `#how to program` `#scientific programming`

[Watch the recording](https://www.youtube.com/watch?v=N5gJAv4HNxI) · [Conference site](https://pydata.org/)

## Description

Ivo Dilov - Bridging Pandas and Polars: The Hidden Costs of Dataframe Interoperability

The Python data ecosystem is migrating from NumPy-based arrays toward Apache Arrow. Polars is built entirely on Arrow, and Pandas is heading in the same direction. Yet differences in string encoding, missing values, schemas, and index metadata make interoperability between the two formats surprisingly costly and error-prone. This talk examines these challenges through a case study of how ArcticDB, the open-source client-side dataframe database, navigated this same migration.

As organisations adopt Polars alongside Pandas, a critical question emerges: how do you move data between the two without silent data loss, performance regressions, or broken round-trips? The answer is more complex than calling polars.from_pandas.

Pandas stores data in NumPy arrays by default, though as of 3.0 it uses Arrow for strings. Polars is built entirely on Apache Arrow's columnar format. For each area where these formats diverge, this talk will explain the problem and show how ArcticDB, a dataframe database that must serialize, store, and reconstruct both formats, solves it in practice:

Memory layout: How NumPy and Arrow represent the same logical data differently, and how a dataframe database can bridge the two
Strings: NumPy object arrays vs. Arrow's offset-based binary buffers -- why Arrow is dramatically more efficient and the cost of conversion
Missing values: NaN/NaT/None sentinels vs. Arrow's validity bitmask -- why a Pandas NaN behaves differently from a Polars null and what breaks during conversion
Schema differences: Different supported data types and different allowed column names -- e.g. Pandas allows mixed-type columns that Arrow cannot represent
Pandas-specific metadata that has no Arrow equivalent: Index and RangeIndex semantics, and MultiIndex which uses an entirely different memory layout with its own performance implications
Together, these issues make conversion between Pandas and Polars far from trivial. This is especially challenging for a dataframe database like ArcticDB, where petabytes of Pandas DataFrames are stored and users increasingly want to read them back as Arrow. The talk will include benchmarks comparing native format reads against conversion-based approaches, and practical takeaways for anyone migrating a codebase, building a library that supports both formats, or choosing a dataframe database. The talk will include benchmarks comparing native format reads against conversion-based approaches, and practical takeaways for anyone migrating a codebase, building a library that supports both formats, or choosing a dataframe database.

www.pydata.org

PyData is an educational program of NumFOCUS, a 501(c)3 non-profit organization in the United States. PyData provides a forum for the international community of users and developers of data analysis tools to share ideas and learn from each other. The global PyData network promotes discussion of best practices, new approaches, and emerging technologies for data management, processing, analytics, and visualization. PyData communities approach data science using many languages, including (but not limited to) Python, Julia, and R.

PyData conferences aim to be accessible and community-driven, with novice to advanced level presentations. PyData tutorials and talks bring attendees the latest project features along with cutting-edge use cases.

00:00 Welcome!
00:10 Help us add time stamps or captions to this video! See the description for details.

Want to help add timestamps to our YouTube videos to help with discoverability? Find out more here: https://github.com/numfocus/YouTubeVideoTimestamps

## Transcript

*4,934 words · source: supa (en, exact timings)*

**[0:02](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=2s)** Yeah. So I'll talk about uh bridging pandas and polars uh the two most commonly used data frame formats uh and the hidden costs of data frame interoperability uh and I'm ev software engineer at Arctic DB which is one platform that has faced these challenges. So uh yeah the agenda for today is uh to cover what data frame interoperability actually means uh and why is it important and we'll cover many of the differences between pandas and polars memory layouts uh the sparse data uh differences between the two the schema differences and uh how should we handle pandas indices in polars

**[0:51](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=51s)** uh so yeah Let's start with data frame interoperability. So uh for a system to be interoperable between dataf frame formats we want that to be relatively seamless. So without much code changes to be able to work with both pandas and polars uh formats. We want the we want it to work reliably. So uh every data frame can be represented in both formats and we especially want uh to be performant. So to be efficient in terms of both uh time and memory uh in the context of a dataf frame database uh this means that we should be able to store data frames in bold formats. We should be able to modify existing data

**[1:39](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=99s)** frames in bold formats and we should be able to read them out in both formats. So for example a dataf frame database should allow writing a polar data frame to storage. uh appending a pandas data frame to that and reading the result as either polars or pandas. Uh I will cover this talk through the lens of Arctic DB uh which is uh high performance dataf frame database. It's uh time series focused. It's developed at Mang Group uh for its quantitive research use cases uh and it's open source on GitHub although worth noting that latest versions are under BSL. Uh it was initially designed with a pandas only interface. Uh the polar

**[2:28](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=148s)** interface appeared later and we'll go through some of the challenges to adding this interoperability in this talk. And yeah, it's worth noting that on storage, Arctic DB uses a custom format which is neither pandas nor polars. Uh and yeah, it hasn't changed during the migration. The migration is only just uh attaching a polar interface to the existing system. So uh why does dataf frame interoperability matter? Uh probably some of you have worked in large organizations and have noticed that different teams prefer to do things differently sometimes. Uh and uh it's very frequent uh for example for uh hedge fund manager to have uh different

**[3:18](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=198s)** system talk to each other but fundamentally be written in different uh using different libraries. So we could have something like uh market data gathering uh platform which is using polars. Uh this then feeds into an alpha prediction model uh which is based on pandas and scikitlearn. And then when we go to the trade execution itself, it can be actually uh based on rust but using arrow memory format which is uh what polars is actually based on. Um so uh dat frame interperability is important because it will allow mixing those different platforms together in an efficient way. Another reason why interoperability is important even if you use the same uh format throughout

**[4:07](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=247s)** your systems is that eventually you might want to upgrade your formats. Uh so many uh companies uh are currently upgrading from pandas to polers because of the performance benefits and uh if you have good solution for data frame interoperability, it will allow you to migrate different systems one by one instead of moving them all in one big bang upgrade. So uh let's start talking differences between pandas and polars. So we'll start with the memory layout differences. Uh so uh pandas data frames have many different possible uh memory formats. I'll I'll talk mostly about the most commonly used one which is just uh the

**[4:58](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=298s)** sequence of numpy buffers. So essentially each data frame can be represented as a sequence of columns. Each column is a continuous block of memory uh from the same data type. So in this example, column A is uh full of integers. They're all in continuous memory. Column B is in another uh continuous memory block uh and so on. Um as mentioned, pandas allows different memory layouts. So for example, if all your columns are actually from the same data type, uh you can consolidate them together and actually have all of the columns in a single continuous buffer represented as a two-dimensional numpy array. Uh as for polars, uh its data frames are based on the arrow memory layout which

**[5:50](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=350s)** was uh briefly discussed in previous talks as well. But uh arrow memory layout essentially allows each data frame to be represented into multiple record batches which are essentially row slices. So for example we could have one record batch which is rows uh the first 10,00 rows then have another record batch which is for the second 10,00 rows and each record batch has its own columns in continuous memory. Uh but this essentially means that uh we could have one column for the entire data frame for example column A to be split into multiple different continuous memory blocks. Uh it's worth noting that uh most commonly uh if you use uh polars it will prefer all the data to be in a single

**[6:39](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=399s)** record batch. Uh but it depends for example if you're using streaming data then uh it's more probable to be more fragmented in terms of memory. Uh another big difference uh in the memory layout is how the two systems handle uh strings. Uh in particular uh the numpy string format and the arrow string format. Uh it's worth noting that uh pandas before version 3.0 uh the default way to handle strings was to use the numpy string arrays which I'll cover here. Uh so the way we handle string columns is to just have uh what we call an object data type uh numpy buffer which is essentially just a continuous memory

**[7:26](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=446s)** buffer which holds pointers to python objects and these python objects are allocated by python on the heap and for strings they're just columns uh there's just strings in python so if we have the uh column containing hello world exclamation mark uh this to have three different Python strings allocated by Python and pointers pointing to them in the numpy buffer. Uh important to note here is that uh constructing such an numpy buffer requires actually acquiring the gill for each uh for each string allocation uh which essentially means we can't do all the allocations in parallel uh because of the global interpreter lock.

**[8:14](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=494s)** Uh the other more modern uh way to store strings is the arrow format. So pandas 3.0 and later by default uses the arrow format. Uh but it's still uh uh in terms of interoperability you might have different systems speaking different pandas versions. Uh so it's important to interoper to be interoperable between the two. Uh and yeah how are strings represented? Uh main benefit with arrow strings is they don't actually require allocating Python objects. they're just natively represented in the arrow memory layout. So if we have the same input of hello world exclamation mark uh the way this works is we concatenate all of the strings together into a single uh data

**[9:04](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=544s)** buffer uh which we can see below and uh we have a separate offsets buffer which points into the data buffer. So uh in the offsets buffer we hold the starting position of each particular string. So uh if we want to read the second string world, we look at the second element at index one uh in the offset buffer which is five. So we can start reading from five. And when do we stop reading? When we stop reading when we reach 10 which is the place where the next string starts. And yeah an important note there is that uh since we need to know the end of the last string as well the number of offsets are actually one element more than uh the number of strings in the

**[9:52](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=592s)** buffer. So uh since we have uh so many differences between the memory layouts uh between pandas and polars uh converting between the two is obvious can obviously be expensive. Uh the simplest case uh in which we have the memory layouts actually match is continuous numeric buffers. So if we have one continuous memory block of ins and one continuous memory block of both in polar and in pandas we can actually cheaply convert between the two. We can just move the pointer from one to the other and end up with shared ownership between pandas and polars. uh but by default this doesn't happen like if you for example want to convert

**[10:41](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=641s)** a pandas data frame to polers by using polars from pandas this will even if the memory layout matches it will do a memory copy. Uh this is actually a good thing in most cases because shared memory ownership can be confusing to users. Uh it's much easier to explain what happens if you are the sole owner of the memory. But it's possible to do the conversion more quickly. For example, if we go through the pyro uh package as an intermediary, uh where we cannot avoid uh CPU cost and memory copies is for example if we have multiple record batches uh in the polars uh or arrow frame. Uh in this way uh the array might be separated into multiple chunks uh what we call a chunked array.

**[11:30](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=690s)** uh and if we want to convert it to a numpy buffer, we essentially need to allocate one continuous buffer to place uh all the data in it and memory copy each chunk uh one by one into that. Uh still uh that's fairly efficient. Uh it's just a memory copy and even more uh we can do the different memory copies in parallel because they touch uh different memory. The the more expensive bit is actually how we handle string columns. Uh if you want to convert a pandas 2.0 to a pandas 3.0 or pandas 2.0 all to a polar data frame. We have to handle uh all the differences discussed before and in particular we have to uh allocate or deallocate uh the Python strings which require getting the global interpreter

**[12:21](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=741s)** lock which means we can't really do that uh in parallel. We have to do them one by one. So uh how do we solve the problem of integration? Uh any system that needs to work natively with both formats essentially has to make a choice. We either use one of the interfaces either arrow or numpy by default. Uh and if users need to work in the other format, we essentially pay the conversion costs. So if we had a system written uh solely in arrow but we had users that want to uh use pandas they will have to pay the conversion cost between the two. What we have done in arctic db is uh a bit different. We have made uh two different interfaces both to arrow and to numpy

**[13:12](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=792s)** which work natively and directly translate into the internal storage layout of arctic. So you can uh write a pandas data frame into Arctic DB and the Arctic DB engine will uh natively understand the memory layout of the pandas data frame and efficiently encode it into the storage layer. Uh and it can do the same with the polar data frame and on the read side we can decode the what we read from storage directly into pandas memory layout or into polar's memory layout. So uh in with this uh double integration we can uh actually achieve uh good performance in both cases. So uh let's talk some benchmarks. Uh if

**[14:02](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=842s)** we're discussing the numeric case which is a bit simpler um for of the benchmarks you see on the right we have used uh 10 million rows by 100 columns random floats data frame and you can see uh the conversion costs uh between converting polars and pandas via memory copy like with the native polars methods uh from pandas or to pandas uh it actually takes around uh 250 to 300 100 milliseconds to do that conversion. Uh this can be uh in some use cases this can be quite expensive uh especially if you compare just to regular read from uh storage. Uh so uh in terms of read operations I have compared Arctic DB uh which is the

**[14:51](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=891s)** progress I'm working on to the most commonly used uh storage format for data frames uh which is parquet which is arrow based. Uh so if we read from local disk uh from paret to pandas it is actually quite slow. It takes around 3 seconds. Uh that's because paret is not really optimized for pandas. It's optimized for arrow and as you can see reading into polars is actually quite fast around 300 milliseconds. Uh and Arctic DB with its bolt integration into pandas and polars can actually read uh the result relatively quickly around 250 milliseconds uh into both memory formats. uh initially with RTDB we had only the pandas integration and if we had done the naive conversion approach uh I read

**[15:42](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=942s)** into pandas then convert to polars it would have actually resulted in around 500 milliseconds of read time which as you can see is around two times slower than what we actually get uh and uh about the string data frames which are a bit more interesting here we have benchmarked a pandas 2.0 to a polar conversion. Uh this uh in this case I've chosen a dataf frame which is roughly of the same size as uh in terms of memory usage uh because strings are more are larger. So I've used one million row uh data frame with 100 columns and uh converting between the two between polars and pandas uh then becomes multiple seconds. It can take up to five

**[16:31](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=991s)** 8 seconds uh to do the conversion essentially because you uh have to allocate or deallocate the Python strings uh from u from that uh and yeah in terms of read operations if we read uh either from from local disk from parquet or arctic DB uh it is still quite expensive to read into pandas because we have to allocate all those strings for the numpy buffers. So it takes uh 5 seconds for park or 3 seconds and a half for arctic db. Uh and uh here we can see one of the big differences uh and important reasons why uh arrow is becoming more important. Strings are much better handled. Reading both from paret or arctic db is less

**[17:20](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1040s)** than a second less than half a second actually. Um and yeah if we had in this case if in Arctic DB we had implemented the polar reads as reading into pandas and then converting it would have actually taken uh around 10 seconds which is very slower than uh what we currently have uh at around 500 milliseconds. So uh that's it for the memory layout and we can talk a bit about how sparse data is handled differently between the two frequently used memory layouts. So uh in pandas uh with numpy buffers the def facto way to handle sparse data is

**[18:08](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1088s)** with what were called no value sentinels. So for example, if you have a float column, we just put put nuns inside the float column or if you have a time stamp column, you put nats uh not the time in the time stamp column. Uh and these special byte values represent uh missing values. There are other ways pandas can handle uh missing values. For example, sparse arrays uh but they have a different memory layout and have other performance repercussions. And um as uh previously mentioned, newer versions of pandas can actually use arrow memory layout uh which has a different way to handle sparse data. So uh how does polars do sparse data? Well, as mentioned, polars relies on the

**[18:57](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1137s)** arrow specification of uh of memory layout and arrow has a native representation of sparse data. So if we have missing values, we represent that with what is called the validity buffer. So we can have ones or zeros uh depending on whether the value is present or missing. Um and this essentially means that where we have a zero in the validity buffer, this means that this value in the data buffer is missing. And it doesn't really matter what bytes are written there. It can be any bytes uh as long as uh they're of the right size. So how do we integrate the different uh sparse data? Um so in terms of conversion costs uh numpy

**[19:47](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1187s)** to arrow or arrow to numpy it's uh essentially a different memory layout and we can't really avoid doing a linear scan uh to alloc to find where the no value sentinels are or to find where the validity bit map is set to zero and assign no value sentinels or assign values to the validity bitmap buffer. So there is a conversion cost similar into the memory layout. What Arctic DB does for this is uh to just as uh previously mentioned it has integrations with both formats. So it natively either directly populates the validity buffer or directly populates the no value sentinels uh to do that efficiently. The more interesting question is what we do about sparse integers. uh because as mentioned uh polars uses uh no value

**[20:38](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1238s)** sentinels to represent missing values but there is no no value sentinel for int columns. Uh each byte value for integers is a valid integer value. Uh so we are essentially left with a choice. If we want to represent a polar data frame with missing int columns how do we do that in pandas? Well there is no ideal solution. what Arctic DB does and uh what pandas does with a lot of its own operations itself is to just represent the nose as zeros uh which is fast because it's essentially the same as doing a no sentinel just labeling zero as the no sentinel but this loses information if we had a zero or a missing value we

**[21:26](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1286s)** wouldn't be able to differentiate the two another option is to convert floats to convert the ins to floats and use nuns. Uh this requires a bit more performance cost because we need to cast each integer into the float in the first place. So we have one more uh O of N uh CPU cost but also it loses some precision. Uh as you probably know if we have a 64-bit uh float this means we have uh only 56 bits for the mantisa which can actually represent the integer precisely. So if we have integers larger than than 2 to the 56 they will actually get rounded to let's say a value divisible by two or divisible by four

**[22:14](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1334s)** etc. Another option uh is the previously mentioned sparse arrays. Uh but they have many performance implications. Uh namely they don't support random access. Uh like you can't get the fifth element of the sparse array very quickly. Uh so uh yeah it's an option but it's not ideal in terms of performance. So uh let's talk about some schema differences. um uh and more particularly in column names. So uh panda schemas are actually fairly relaxed. Uh they do allow duplicate column names. You can have a pandas date frame which has only the same column name which is quite absurd

**[23:02](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1382s)** but it's allowed. Uh it also allows uh strings, integers, any other types to be the column name. Whereas our schemas are quite strict. They do not allow duplicate columns. Each column name is uh a string only. Uh and this leaves us with some questions on how to convert the invalid pandas data frames into uh polars. So uh what we do about the non-string column names? Uh well, it's pretty obvious. We can just convert them to strings. For example, the integer one we convert to the string one and use that as the column name in uh polars. Um this can result in some duplicate column

**[23:49](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1429s)** names like if we had the different column names the string one and the integer one that would become to two different string one representations. Uh so we do have to handle duplicate column names. um for duplicate column names uh standard conversion methods to just throw an exception because it's uh essentially correct. This is an exceptional situation. It's not representable by the polar memory layout. Uh but Arctic DB uh has pabytes of pandas data frame stored inside it. we can't really afford our researchers to lose access to some of their data uh if they want to convert to polars uh because they have used some invalid arrow uh schema. So ARTDB has to solve the

**[24:40](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1480s)** duplication problem and uh I obviously there's no ideal way to the duplicate columns. Uh what Arctic DB does it it just adds underscores uh until it achieves through the duplication. So for example one and one will become to one and one with underscores. Uh renaming columns is not ideal. Uh we need to somehow preserve the original columns uh what they were. Uh so there is a piece in Darl schema which allows us to store metadata and that's essentially where we store just some JSON metadata about the column uh which allows us to return it back to pandas if it's needed. So it has the original name which is just the one integer and the new field name which is the one we see

**[25:29](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1529s)** in the arrow table. Uh and just yeah a quick example in Arctic DB you can do the following uh you can create a data frame which has three columns named one three two columns named coal uh and if we read it back as polars we will actually get something that looks like the result on the right we will have underscores the duplicating and we'll have the integer one be converted to a string one um and we will have the additional metadata in the polar data frame if we need to extract that. So uh there are other uh pandas metadata we want to care about and the big elephant in the room is uh pandas

**[26:16](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1576s)** indices. So uh pandas dataf frame can have the concept of an index. What does that mean? So the index is essentially uh a special way to quickly access a particular row. So for example you can find the row which has a particular time stamp if your time stamp column is the index. Uh there are different types of pandas indices. Uh the most commonly used one by default is the range index which is just a numbering from uh zero up until the size of the date frame. Uh so you have like uh 01 to up until n minus one. uh this is not actually represented in memory as a separate column. It's just represented like a piece of metadata. It just stores

**[27:05](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1625s)** the start, the stop and the step similar to a Python range function. Uh the most common use case uh in particular for Arctic DB is time series indices which is just one column which has the special property of being an index. Uh it can be of many types. In Arctic use case, the most commonly used one is uh the the datetime index and it has additional metadata uh alongside it like whether the index is sorted in ascending, descending where or it's unsorted. There's also multi-index where we can have multiple index columns. Uh this is essentially similar to having one index one column but multiple. So you can find the column which has uh these three values for these three columns fairly

**[27:53](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1673s)** quickly. Uh it's important to note for multi-index the memory layout is actually different. It doesn't use the columnar memory layout. It uses a more optimized format for quickly finding uh that particular row. So uh how do we represent such pandas indices in polars? As mentioned before, ARDB has pabytes of pandas dataf frames written inside it. We need we must have a reasonable representation when we read back those into polars. Uh well range indices uh they can be represented as plain metadata added uh to the same arrow schema metadata. Uh so they're not that interesting and the most common use case at least for RTB is the single column index. uh we can just represent it as a regular column. We

**[28:43](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1723s)** just put it uh as the first column of the polar data frame. Uh we preserve the name of the index column and if it's missing we just use the basic index name uh because empty uh column names are not allowed in arrow schema as well. And we should preserve any uh index metadata that we had. For example, if the index was sorted, we actually want uh to tell polers, okay, this column is sorted. So polars can be more efficient when it does uh processing like it can use binary search or more efficient group by knowing that the column is sorted. Uh for multi index, we just attach the multiple columns up in the front of the polar data frame. Uh but yeah, it's worth noting that this actually requires

**[29:33](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1773s)** some memory layout changes. So it's uh not exactly the same uh data structure but it's the uh arrow equivalent of that. It just stores them as regular columns in polars. So uh what do we do in the other direction? If someone is writing to the uh to the database a polars data frame uh how do we interpret that? Does it have an index? Does it not have an index? Well, essentially for the indices, it's a userdefined property. You can't really say if a polar data frame should or should not have an index. Uh it's something that only the user can specify. If they want uh to have an index, they can specify that uh with an appropriate API or if they don't

**[30:22](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1822s)** care about indices at all, that should also be fine. Uh we can default to the default R range index used by uh pandas. Um yeah and in RTB particular it this is very important if we have uh time series uh column in the polar data frame we want to be able to index according to that to have arctic DB optimized operations on that time series index. So uh let me go through a quick example. Uh the font is probably a bit small for the actual code example but essentially we can write uh pol 100 million row polar data frame. Uh you can see on the left part of the example. uh we have a data frame which has 100 million rows

**[31:13](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1873s)** and the first column is a time stamp column and when we write it into RTDB where we store in the library we write the data frame and we just specify use the first index as the column uh sorry use the first column as the index uh then when we read that back into pandas we will essentially represent the time stamp column as the index column and more importantly uh once we know that the first column can be used as an index, we can do more efficient operations on it. For example, uh in this code step I have used here, we just want to get the date range uh from the 6th of June June to the 7th of June. Uh and we can read that out in just one millisecond. Uh which is quite fast for

**[32:03](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1923s)** 100 million rows. uh if we had to actually read the entire data frame it would have taken more like 500 milliseconds. Uh so yeah this uh shows how Arctic DB can leverage the index for uh more optimized performance with the time series column. So uh to wrap it up uh some takeaways from uh this talk are that uh conversion between polars and pandas formats can be surprisingly expensive in some cases especially if we're talking about pandas 2 and uh string columns um for good if you want your system to have good performance with both you essentially have to have native integration with both formats for a

**[32:50](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=1970s)** database like Arctic DB B it needs to have a way to decode the storage directly into either pon pandas format or polar format u and it has to do that efficiently. Sparse data is represented differently. There are especially some trade-offs in how we should represent missing int columns with pandas. Uh no ideal solutions there. All come with trade-offs. Schemas can be incompatible. uh to translate between the two we need some form of metadata and a translation layer and uh pandas index handling in polars uh requires some custom APIs for the users to specify their indices or uh meaningful representation of those indices when users want to read back

**[33:39](https://www.youtube.com/watch?v=N5gJAv4HNxI&t=2019s)** polars that is >> yeah thank

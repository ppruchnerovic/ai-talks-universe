---
id: 03h0SRCUAI4
title: "AI Dev 26 x SF | Matthew Xu: The 4-Legged Identity Challenge"
slug: ai-dev-26-x-sf-matthew-xu-the-4-legged-identity-challenge
conference: ai-dev-deeplearning
conference_name: "AI Dev (DeepLearning.AI)"
category: "AI engineering & agents"
edition: "DeepLearning.AI"
year: 2026
speakers: []
channel: "DeepLearningAI"
duration_min: 17
published_at: 2026-05-21T21:58:57Z
video_id: 03h0SRCUAI4
url: https://www.youtube.com/watch?v=03h0SRCUAI4
youtube_url: https://www.youtube.com/watch?v=03h0SRCUAI4
tags: []
transcript: true
---

# AI Dev 26 x SF | Matthew Xu: The 4-Legged Identity Challenge

**Speaker not identified**

`AI Dev (DeepLearning.AI)` · `DeepLearning.AI` · `2026` · `17 min`

[Watch the recording](https://www.youtube.com/watch?v=03h0SRCUAI4) · [Conference site](https://ai-dev.deeplearning.ai/)

## Description

As MCP systems scale from local setups to shared infrastructure, developers face new challenges around security, identity, and access control that aren't solved by standard patterns.

This talk by Agentic Fabriq's Matthew Xu breaks down common failure modes (like over-permissioned tools and weak auditing) and shows how to implement practical fixes such as least-privilege access, guardrails, and privacy-aware logging.

It also covers the "4-legged" identity problem and developer-friendly approaches like token exchange and structured auth flows for production MCP deployments.

## Transcript

*2,656 words · source: supa (en, exact timings)*

**[0:07](https://www.youtube.com/watch?v=03h0SRCUAI4&t=7s)** Hi everyone, thanks for being here. I'm Matthew, the CTO of Agent Fabric, and today I'm going to talk about what it actually takes to solve the four-legged identity challenge, and specifically why security and identity become defining problems once you move beyond local deployment. The framing I'm going to use here is what we call the four-legged identity challenge. But I want to clarify one thing in the beginning. It's that four legs is actually just the minimum viable complexity. In real world systems, you often end up using five, six or longer delegations. Complexity grows very quickly and systems we're used to, especially oath are not designed for that. So what I want to do today is walk through how we got here, why traditional models break, and then how can we build models that actually safe to deploy.

**[0:55](https://www.youtube.com/watch?v=03h0SRCUAI4&t=55s)** So let's start with how OOTH was originally designed. You can see here that we have a diagram of how traditional OOTH works. We have three actors. The user, the application, and the identity provider. The user sends a request to the identity pro uh to the app which then sends a request to the identity provider which then requests that the user goes through authentication. Then the user grants consent to the app. The application receives an access token. This is a very clean system. The token represents the user and the app has access is delegated to to do stuff on user's behalf. The delegation is simple and explicit.

**[1:42](https://www.youtube.com/watch?v=03h0SRCUAI4&t=102s)** There's only one hop here. And we know exactly who is acting. There's no reasoning layer. So there's no switching identities. And who holds a token is who executes the request. So now let's add an API call to this model so we can extend it by introducing a downstream API. So what this does is it takes the access token from the IDP then calls the API. The API will validate the token. It will check for any scopes and enforce any authorization then proxy the call. So this is still within the OOTH confines that we have today because the system is distributed across services and the identity model is still intact. The API sees the identity enforces permissions.

**[2:30](https://www.youtube.com/watch?v=03h0SRCUAI4&t=150s)** Delegation chain is still short. So this is what we call the three-legged flow. App executes user intent through an API. There's no autonomous system, no decision-m layer and it's purely deterministic. And this is the reason why OOTH scaled so well. The entity acting is the entity authorized which is a core assumption that OOTH holds. And now this is where things start to change. So up until now applications directly executed user actions with agents that assumption breaks. We can introduce two new components an agent and an MCP server. The flow becomes user to agent to MCP server then to API. And this is where OSH starts to fail. It's because the application is no longer the final executor. It holds

**[3:19](https://www.youtube.com/watch?v=03h0SRCUAI4&t=199s)** control to an agent which then decides what actions to take. The MCP server then executes those decisions against downstream APIs. What used to be a single delegation is now multihop and is dynamic because of the agent. This creates a key ambiguity. When the API is called, we have the question of whose identity is being used, the user, the agent or the MCP server. And ooth doesn't handle this well. Tokens are bound to an audience can't be reused and in this model identity gets lost across the chain across the chain. If MCP server uses zone credentials, the API sees only the server and not actually the user. Thus, the result is what we call identity lost midchain. It's the core of the four-legged identity

**[4:07](https://www.youtube.com/watch?v=03h0SRCUAI4&t=247s)** challenge and leads to real world issues. And importantly, this doesn't stop at four legs. As shown here, you can introduce even more things. In this chart right here, we see we added an identity provider. The chain becomes more complex. You you might be able to add another agent calling another agent, multiple MCP servers, or different IDPs per service as seen here. Now, we see that identity is not just being passed along a chain. It's actually forming a graph. So, the problem is no longer just how do we pass identity forward. it becomes how do we preserve identity across these very large graphical systems over multiple hops and if necessary across time that's where traditional oath falls short. So some patterns that I've seen people

**[4:57](https://www.youtube.com/watch?v=03h0SRCUAI4&t=297s)** use today are here on this slide. Um the first is non-delegating client credentials. What this means is that the MCP server calls downstream APIs as itself. It's easy to implement and works everywhere, but it completely uses user identity. This is the situation that we saw earlier where the API only sees MCP's off, not the user off. Downstream systems have no idea who initiated the action. The second tok is token exchange. This is a cleanest model where you take a user scope token and exchange it for a new token that's valid for this downstream API. We mentioned earlier that tokens are audience bounded and this solves that problem. It preserves identity end to end. But the challenge is that this token exchange or

**[5:46](https://www.youtube.com/watch?v=03h0SRCUAI4&t=346s)** it's an RFC protocol is not widely implemented and is still limited. The next is pre-injected token. The application fetches all tokens up front and passes them through the system as necessary. This works without with any IDP but it assumes you know all downstream dependencies ahead of time which in a dynamic system is going to be impossible. And the fourth and the last one is out of band login where the user explicitly authorizes each downstream service. What this means is that whenever they try to access it each service will request that the user go through an ooth flow. However, this requires extra browsers, tabs, and a lot of friction. It is not something that a lot of customers would like to see. So, let's go deeper into the solution that I mentioned, token exchange,

**[6:34](https://www.youtube.com/watch?v=03h0SRCUAI4&t=394s)** because it really is the core solution. On the slide, you can see what token exchange request looks like. You take a subject token, which is typically a user's access token, you request a new one with a different audience. The new token represents a delegated identity. It still encodes the user but is scoped specifically for the downstream resource delegating that resource to act on behalf of the user. This is critical because it allows you to preserve identity while still respecting the audience constraints of OOTH. But the reality is that support for this is uneven. Keycloak supports it well. Microsoft Entra doesn't fully support it and Octa has partial support through claims. So it's hard to kind of mix and match these. And like we mentioned before, the OOTH is not standard for agentic systems and

**[7:22](https://www.youtube.com/watch?v=03h0SRCUAI4&t=442s)** we have to fill the gap. So these four RFC protocols that are enforced for MCP work together to support agents. First we have RFC 97828. When an agent encounters a resource or an MCP server, it needs to know what it is and what authentication it expects. This protocol allows a resource to tell the agent which servers it trusts and what access patterns are supported. So moving on, once you figure out what authorization server you you want to use, you actually have to figure out what API call to make and how to make that call and how to authorize the agent. Once the agent identifies the correct authorization server, this RFC provides key details like endpoints,

**[8:10](https://www.youtube.com/watch?v=03h0SRCUAI4&t=490s)** support capabilities, signing keys. Now we move on to 7591 where clients can register themselves at runtime. In order to to act uh in order to get a token that they can use to then call this MCP server, the agent must ask the user to authorize themselves. And this dynamic client registration allows agents to register themselves, get a client ID and use that client ID to ask the user to authorize themselves. And lastly, we have RFC 8693, which what we mentioned is token exchange. So, it needs tokens for each step. And this RFC enables token exchange with the correct audience, scope, and delegation.

**[9:01](https://www.youtube.com/watch?v=03h0SRCUAI4&t=541s)** So now we can kind of look at how MCP is used in a real world example with all these RFC protocols because right now to me, they just sound like numbers. agents discover. Let's say an agent discovers an MCP server for the first time. It'll use RFC 9728, which specifies a specific endpoint to call to find metadata about the authorization server. So now it's going to figure out what authorization server it needs. But the agent actually needs to figure out how it's going to get those tokens. So RFC 8414 is used to get a resource doc from the authorization server. This will tell it the exact information it needs to call that authorization server to get a token that they can use. But the issue is is that for most of

**[9:50](https://www.youtube.com/watch?v=03h0SRCUAI4&t=590s)** these MCPs, each agent has to have a client ID and to pass in in order to get the initial user token on behalf of the user. So RFC 7591 can allow the agent to obtain one of these by dynamically registering it and signing it up basically identifying itself to the MCP server. Then after that the agent can get the initial user token through OOTH flow. No mechanism is used here. It's just proof that user is authenticated. And then if the MCP server wants to call an downstream API for something else, RC8693 can be used to exchange a token, scoped correctly, delegated, and everything is good. So key takeaways

**[10:38](https://www.youtube.com/watch?v=03h0SRCUAI4&t=638s)** from these past couple slides, I think, are that four-legged identity is the default. It's not just an edge case. Every agentic system becomes multihop. And it isn't just limited to that. there are going to be multiple hops more hops than just four and that's the reality. So this is a huge problem and oath is incomplete for agents. You can try to generalize and extend OOTH to agents but you're going to need token exchange for correct delegation and if you want to dynamically uh discover these MCP servers they're going to have a different kind of O built in as well. And because there's an OOTH gap, RFC's help complete it for agents. It helps servers and agents establish trust and identity and

**[11:26](https://www.youtube.com/watch?v=03h0SRCUAI4&t=686s)** actually use them and allow agents to delegate identity down the chain. So I think on the right here, here's a little uh graphic. We made some labs to help you understand how these work together. Um I'll talk about them in the next couple slides. So first lab is standard token exchange. This is the very basic uh token exchange flow. You have a user logging into a web application, getting a token and then exchanging that one for a new one. And what's useful to see here is you can actually see exactly how the token transforms. You can decode each claim and then see how the audience changes, see how the scoping changes and how identity is preserved along the chain. This is the simplest working example of

**[12:13](https://www.youtube.com/watch?v=03h0SRCUAI4&t=733s)** the changes of the concepts we've been discussing. To take it a step further, you can actually u use a second lab where instead of having each component handle token exchange individually, you can introduce a broker layer. So this broker centralizes everything and sits between the application MCP server and downstream APIs. Ensures that identity is handled consistently across all of these resources. You can see in the diagram that it integrates with systems like keycloak for identity, open bow to store your secrets, and a cache layer if you want to reuse tokens. It's much real, it's much closer to what a real production system might look like. Um, and because once you scale beyond a few services, you need a centralized place to manage these transformations.

**[13:03](https://www.youtube.com/watch?v=03h0SRCUAI4&t=783s)** And this is where aentic fabric comes in. The hard part is not just choosing ooth flows. It's making identity coherent while agents discover tools dynamically. Aentic fabric sits as the identity and connectivity hub. So answers the question on every hop of who is acting on whose behalf is the agent acting and what audience is a token for, what scopes are actually allowed and why the action was permitted. The important point is that this is a runtime layer you can reuse instead of rebuilding delegation logic in every agent or MCP server. What we provide is a brokered identity layer that sits between the agent runtime and the systems it needs to interact with. Our goal is simple. On the left you have the agent runtime.

**[13:51](https://www.youtube.com/watch?v=03h0SRCUAI4&t=831s)** We want the agent code to be focused on reasoning, tool use, and actually being being smart, learning from its mistakes, not oath edge cases. All of the complexity around this is abstracted into agentic fabric in the middle. Inside that layer, we handle the full life cycle. If an agent wants to dynamically use new resources, we support it. If you want your agent to follow a preconfigured flow, we also handle that. And most importantly, we manage token exchange, which allows identity to propagate correctly across multiple hops. On the right, it connects cleanly to existing infrastructure, which is your IDP, MCP servers, and downstream APIs. And the reason we it works so well is that we centralize this delegation. Instead of pushing tokens and secrets into agents, we issue these tokens for your agents at every hop. So

**[14:41](https://www.youtube.com/watch?v=03h0SRCUAI4&t=881s)** you get dynamic agent-driven systems without losing identity, security or auditability. But not only does Agentic Fabric function as a broker, it functions as a governance layer with the level of O builtin. Aentic fabric can actually tell who's acting by forcing authorization of both the user and the agent. We'll know exactly who the user is and exactly who the agent is. This way, permissions are scoped to a specific tool, resource, and action and policies are evaluated at runtime before credentials are issued or a call is proxied. And additionally, we because we track who the user and the agent is, we allow you to control what each user does with an agent. Not only do we allow you control what agents do, but also what a user can do.

**[15:32](https://www.youtube.com/watch?v=03h0SRCUAI4&t=932s)** This combination allows us to govern your organization at a much deeper level and based on the role within an organization. But not only do we enforce these role based policies, but we also log with full context downstream tool and API calls are visible in the audit trail and you can see exactly what you're using what your users are doing with the specific agent. What this points to more broadly though, and I what I want to emphasize the most is that agents don't fit cleanly into existing identity and access models. There's something completely new that operates with autonomy, context, and intent. IM systems are built for humans and static services, treating treating them as separate entities. But agents combine both. They decide what tools to call dynamically but can access tools on

**[16:21](https://www.youtube.com/watch?v=03h0SRCUAI4&t=981s)** behalf of a user often as a result of some trigger or prompt and this is how they function as more of a service. They can make their own decisions but they also do your bidding. They function as decision makers and service accounts and as a result they require a new governance layer to account for both sides of the equation. Aentic fabric provides this. So, if you would like to try any of the labs mentioned in this talk, feel free to scan the QR code or go to the link to test it out yourself.

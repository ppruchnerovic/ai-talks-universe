---
id: yLDiQBdwlHU
title: "Metaprogramming, Synthesis & Verification • Nada Amin • YOW! 2025"
slug: metaprogramming-synthesis-verification-nada-amin-yow-2025
conference: goto
conference_name: "GOTO Conferences"
category: "General software conferences"
edition: "GOTO"
year: 2026
speakers: ["Nada Amin"]
channel: "GOTO Conferences"
duration_min: 38
published_at: 2026-06-02T12:00:07Z
video_id: yLDiQBdwlHU
url: https://www.youtube.com/watch?v=yLDiQBdwlHU
youtube_url: https://www.youtube.com/watch?v=yLDiQBdwlHU
tags: ["GOTO", "GOTOcon", "GOTO Conference", "GOTO (Software Conference)", "Videos for Developers", "Computer Science", "Programming", "Software Engineering", "GOTOpia", "Tech", "Software Development", "Tech Channel", "Tech Conference", "Today in Tech", "YOW!", "YOWcon", "Nada Amin", "Metaprogramming", "Synthesis", "Verification", "VerMCTS", "Dafny", "Holey", "Dafny Sketcher", "Monte Carlo Tree Search", "Relational Programming"]
topics: []
transcript: true
---

# Metaprogramming, Synthesis & Verification • Nada Amin • YOW! 2025

**Nada Amin**

`GOTO Conferences` · `GOTO` · `2026` · `38 min`

`#GOTO` `#GOTOcon` `#GOTO Conference` `#GOTO (Software Conference)` `#Videos for Developers` `#Computer Science` `#Programming` `#Software Engineering` `#GOTOpia` `#Tech` `#Software Development` `#Tech Channel` `#Tech Conference` `#Today in Tech` `#YOW!` `#YOWcon` `#Nada Amin` `#Metaprogramming` `#Synthesis` `#Verification` `#VerMCTS` `#Dafny` `#Holey` `#Dafny Sketcher` `#Monte Carlo Tree Search` `#Relational Programming`

[Watch the recording](https://www.youtube.com/watch?v=yLDiQBdwlHU) · [Conference site](https://gotopia.tech/)

## Description

This presentation was recorded at YOW! Australia 2025. #GOTOcon #YOW

Nada Amin - Assistant Professor of Computer Science at Harvard SEAS

RESOURCES

Links

ABSTRACT
With live examples Nada will demonstrate:
• Dafny and verification-aware programming, including proof by induction to verify properties of programs (for example, that an optimizer preserves semantics).
• Multi-stage programming, a principled approach to writing programs that write programs, and its incarnation in multi-stage relational programming for faster synthesis of programs with holes—with the theoretical insight that a staged interpreter is a compiler, and a staged relational interpreter for a functional language can turn functions into relations running backwards for synthesis.
• Monte Carlo Tree Search, specifically the VerMCTS variant, and when this exploration-exploitation sweet spot is a good match for synthesis problems.

The ideas will be demonstrated with live examples and open-source artifacts spanning verified programming, relational programming, and LLM-guided synthesis, including Dafny Sketcher, multi-stage miniKanren, VerMCTS, and Holey. [...]

TIMECODES
00:00 Intro
00:14 Verified synthesis
02:04 Neurosymbolic spectrum
03:14 Verification with Dafny
05:25 Demo
17:52 Orchestrating program synthesis
18:55 VerMCTS setup: Verified programming
20:41 PUCT
22:33 Dafny Sketcher
27:06 Holey
29:29 Demo
30:20 Lessons from Holey
31:15 Relational programming
33:22 Demo
35:46 Takeaways
37:27 Outro

Download slides and read the full abstract here:

RECOMMENDED BOOKS
Forsgren, Humble & Kim • Accelerate: The Science of Lean Software and DevOps • https://amzn.to/3tCz1xO
John Arundel & Justin Domingus • Cloud Native DevOps with Kubernetes • https://amzn.to/3hKZvI5
Wynne, Hellesoy & Tooke • The Cucumber Book • https://amzn.to/3tEUINJ

CHANNEL MEMBERSHIP BONUS
Join this channel to get early access to videos & other perks:

Looking for a unique learning experience?
Attend the next GOTO conference near you! Get your ticket at https://gotopia.tech

## Transcript

*5,749 words · source: supa (en, exact timings)*

**[0:02](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=2s)** [applause] Yeah. So I'd like to touch upon meta programming and focus on synthesis and verification and the combination of the two with verified synthesis. So I think about synthesis as generating code from some high level and yes we can also think of natural generating code from natural language with LLMs as a form of synthesis or input output examples or more broadly specifications formal or informal and verification ensures that the code matches the specs which is easier to doal specs than informal specs and um and I and I think this is becoming important in terms of um of of the the kind of volume at which we're generating code with LLMs and trying to to make sense of it and in fact most of

**[0:52](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=52s)** the code is manually reviewed which is important but how do we reduce the surface checking of what we need to review. So verification can help answer that question with specifications being the the focus of what we we we review and then the quotes following the specifications is sort of formally guaranteed and verification can also guide development in the sense that attempting a proof of correctness can improve understanding and direct repairs. And so the this iterative loop between the tools and and the the the generation engines can be improved by by thinking about verification in the loop. And historically verification has been used very heavily in um in software in in software critical systems. And I

**[1:44](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=104s)** think there should there will be and there is already a push towards making this more mainstream just because it the bet is in the future the proof burden will be much much alleviated because in addition to code generation we can do proof generation with with the existing technologies that we have. And so in this talk I'll focus on different aspects of u of u um code generation verified code generation on the more neural side and here for this talk I'm limiting myself to um to really LLM completions and using that with an outer shell of verification and then on the more symbolic side I will look at how to uh generate um code sol basically generate code by solving constraints and

**[2:33](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=153s)** thinking maybe how this could in the future be extrapolated to combining these two approaches. And in the middle, Daphne Sketcher is combining symbolic uh generation of proof sketches. So like partial programs which partly prove what um uh you can think of it as a program with some gaps and it's trying to generate a program that is really a proof of what is going on in the system and also looking for counter examples to to the to the state to the specifications that the system might might that we we might want the system satis satisfy. So I'll jump into these things and because verification is so important I'll just start by thinking about it in in the context of this Daphne verification our language that I'm going to show and so the core

**[3:22](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=202s)** concepts in Daphne of how you do verification is you're really doing designb contracts which you might be doing in your programming already like having assertions to to check that the code is on track or having preconditions and postcond conditions and methods that you you have you're saying if my precondition holds then after the method executes my post condition will holds. So this is designed by contract and in Daphne it's verified and it's verified for all executions of the program. So we're not actually running the program to decide whether the contract holds. we are actually reasoning about the the program and proving that it holds for all executions and defins imperative functional and or programming. The demo that I will do will be in the functional subset and

**[4:11](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=251s)** that is that is a subset the functional subset is also traditionally the subset where people have done proof assistance and functional pro in functional programming proving and programming are very closely related. Daphne has in addition semi-automation. So part of the part of the advantage of using Daphne is that the proofs can sometimes be very lightweight or even even non um like just an empty proof will work because this the automation infrastructure of Daphne will do all the work and we'll see this in the demo and it's based on decades of research and development. This was released in 2009 by Rooster Leno at Microsoft research and it's been used in safety critical systems. So for example uh recently the AWS new authorization engine has been

**[4:59](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=299s)** rewritten in Daphne and compiles to Java and is invoked one billion times per second and it and the formal verification has provided a lot of insurance about the correctness of the code. And I say based on decades of research, you can even trace this back to the late '7s uh uh in terms of um of like the aimatic semantics the that that is at the foundation of tools like Daphne. Okay. So let's uh jump into the demo. So I'm going to do a a case where I I I I write a data type. I write some functions over the data type and then I write lemas uh about relating what the the functions do. So providing

**[5:46](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=346s)** properties of of of these functions. So let's start with a data type for arithmetic expressions. So we can say we have constants of variables and addition which is recursive. Here we have sub expressions for addition. So this is a very simple data type for arithmetic expression and u yeah and then and then now we can start writing a function about it. So let's say we write want to write a little evaluator that takes an expression and returns a number. So we can do pattern matching which also is very common in any functional

**[6:36](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=396s)** language. So we can pattern match against E and if we have a constant we can just return n. Now what do we do for variables? Well, I mean, we kind of have to give a a number to a variable and we instead of making it up, we can just say we have some sort of environment which goes from strings which map strings to int and we're also using a function to say what the environment is. So now I can pass in an environment and say a variable just calls this environment and then I can do addition. So this is recursively calling the sub expressions. Um so we do eval e1 m and then we add with eval e2 m. And so daphne has proved that this pattern match is exhaustive

**[7:24](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=444s)** and also that this function is terminating because we're basically having a decreasing measure on on e. We're calling e on smaller sub expressions here. And now we can prove something about this function. So let's say we prove that um if we have an expression and an environment and so here I'm putting an empty we can prove that basically the expression evaluated in the environment is the same if we add a constant zero here at the beginning. So yeah, so we have this and the the system goes definitely goes through and there's

**[8:12](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=492s)** no problems detected. So this is actually proved and if I change this constant zero to one, there is indeed a problem. It can't prove it anymore. So that shows that we've actually proved it for any for any expression E, we can add a constant zero at the beginning. So let's take this a little bit further and write a little optimizer function optimize And this is just a transformer. It takes an expression and returns an expression. It doesn't do any evaluation. And we can say if we have uh really the cases we want to consider and maybe we we'll think later about how to rewrite this. But for now I'm just going to do uh this and then so I'm just removing addition by zero.

**[9:05](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=545s)** And then I'm recursively doing this for the sub expressions and then for the other cases I just return the expression as it is. So again this works and now I can define a stronger version of what we showed earlier. I can pro show that the optimizer preserves the semantics. Uh so I can say um eval is the same as eval optimize EM. So I proved that this optimizer doesn't change the semantic. I can run it before

**[9:53](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=593s)** evaluation and the result will be the same. So that's good. Now maybe we can think of whether this optimizer is optimal for some definition. And here we actually define what optimal means. So maybe that's a little bit risky because I'm using the same code to define my predicate for what I mean to be optimal. But we'll see if this helps. And then I have this. And then if I have a constant here then this this means it's false. It's not optimal. If I have a constant here, it means it's false. It's not optimal. And then if I have E1 and E2, I just recursively

**[10:41](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=641s)** do optimal of E1 and optimal of E2. and then and otherwise it's actually true. So we we we define this and now like before I can say lema uh optimize optimal and this only takes an expression because we're not dealing with environments and evaluation and we're just saying that optimal optimize of E. So when I do this, I see that it actually doesn't go through and now it's time to debug what is going on and try to to write a proof or maybe re uh fix the code if necessary. So the first thing I'll do is use one of those sketcher from Daphne sketcher that is like providing these symbolic routines that an LLM or a user can use. So I'm

**[11:31](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=691s)** going to provide one of this these routines. I'm going to do an inductive search and it's returning something and we see that it kind of did like it's following the the function optimize. So considering the cases and this worked this worked but when we do the add E1 E2 case it says that it can prove the post condition in this case. So we can see a little bit what's going on in the example here which is that we're doing an ad of optimizing things. But what if one of these optimized things ends up being zero? That wouldn't work very well. And we can actually ask uh also one of the sketchers for a counter example to see if it can find something. So this is running one of Daphne's

**[12:20](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=740s)** underlying tool using the verification engine with an SMT solver to find a counter example. And it it found this one which I can now say assert optimal optimize of this with this and this and we see that it's actually it's actually not verifying but if I do the negation this verifies which means this is an actual counter example to our optimizer being optimal for the definition that we gave of optimality. But this is really saying that we have 0 + 13 plus 0 [clears throat] plus 0 and that becomes 13 + 0. But then this is

**[13:10](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=790s)** not optimal and this is what we're returning based on our definition. So already like trying to prove that the system is optimal is guiding how to repair the code to actually make it optimal. And that is something you can imagine in a in a verifier loop as well with with with LLMs which I I'll show I'll show the log in an example. But let's try to verify it by hand first to to fix the code by hand. So we have this function and really what we want to do is first pattern match and then and then optimize. So I want to sorry first optimize and then pattern match. So that's what I want to do. And I'm putting the cases from before, but now we're just considering these things. And we're also, this is actually already

**[14:03](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=843s)** optimized, so we don't need to optimize again. And here as well. And then we have the case OE12. And then let's add OE1 OE2. So if we change it to this, then we see that our counter example is now no longer a counter example. If we if we put if we remove the negation, it actually checks. But we still have an issue with the with the proof as a whole. And that's because the the uh the semi-automation is not strong enough to prove this case. And we can try to use the sketcher again or we can try to do it manually in Daphne here. This is really just your programming to be doing to to to to do

**[14:52](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=892s)** proving and I'm just using the sketcher to make this easier for me. So I I will do the inductive search again. And now when I put the sketch, it actually goes through. So what this is saying is that it's doing a structural induction on E. Basically recursion in the proving context becomes induction. So if we have an expression E1 sub expressions E1 and E2, we can call the function again to ensure the post condition and so we're ensuring assert optimal optimize E1 and we're assuring assert optimal optimize E2 and then we can definitely done does the rest of the pattern matching to prove this. >> [clears throat] >> So how does this work in um in um in a orchestrated context like orchestrating

**[15:42](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=942s)** LLMs and verifiers to do this? So here I have a log of um Daphne Sketcher working with the Monte Carlo tree search um orchestrator which I'm going to come back to Monte Carlo research in a in a in a second but just to show we I'm starting here not from scratch. I could start from scratch, but I I specifically wanted to see what happens when I start with the buggy code, so to speak, buggy because it's not optimal. And then the system um basically tries the sketchers and is not able to do it. And then it gives a prompt to so here we have a prompt uh to the LLM where you are implementing a lema in

**[16:28](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=988s)** deafne that is specified but not fully implemented and it gives the code and then it even says that actually you're supposed to prove that this optimizer is optimal but we have a counter example to that and so immediately the the LLM given this choice will decide to op to edit the optimize function instead of going down the rabbit hole of doing a proof that is impossible and if you try to to if you of course if you didn't give it that information most LLMs will just go down the rabbit hole and then so it's trying to edit optimize and it's taking a while I'm using quen 3 coder and then at the end it found some proof uh some some program that looks like this which is I mean in spirit similar to what we've done just expressed a

**[17:17](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1037s)** little differently So that's the the function optimize and basically the the the signal the verification signal is given by the sketcher that completely fills in this uh this inductive sketch which is complete. So it's it's an it's a proof and so the the the LLM could focus on like figuring out the right uh code for the optimized function and the rest of the system was able to navigate the proofs to get to a correct solution. So in uh so what I'd like to to talk about now is uh how we we how we started in this realm of doing orchestrating program synthesis and more specifically verified synthesis and one of our experiments from last

**[18:05](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1085s)** year was really when we started using Monte Carlo research for to do that and it's synthesizing multi-step programs using a verifier large language model and research. So compared to the setup that I've shown in the logs here, this was more left to write string completions by LLMs and trying to do and and and at every boundary of like say a function boundary or um or at every step basically checking checking that um that that that everything is on track using a verifier. So the verifier ensures that we're on track and the LM just makes these completions and it's all happening in a tree search where you have these partial programs from left to right that are that are considered based on like how promising they are.

**[18:53](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1133s)** And so what we we did in particular was to take think of this problem. So starting with an English language prompt generate all of the following specifications code and proofs of correctness. And our setup was language agnostic. We could use Daphne or other uh verification aware languages proof assistants like rock and lean. And um so this is an example a subset of what I showed in the demo for for a prompt in Daphne. So we say in Daphne write a data type for arithmetic expressions then write an evaluator then write an optimizer an optimizer then prove that the optimizer preserves the semantics. And most LLMs are able to write this this sort of Daphne code for the property of optimizer proof semantics.

**[19:41](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1181s)** Very similar to what I've shown in the slides. Basically equating evaluation optimized and unoptimized. Okay. So how do we do this? Well, we use a a Monte Carlo research slightly modified. So a Monte Carlo tree search has four phases. select, evaluate and expand and then propagate back propagate. And the the work we've done is really kind of in the evaluation and maybe expand phase use the verifier instead of doing roll outs and looking ahead to see whether we were on track. And the selection phase is really thinking about how to score a node so that we decide which child to follow until we reach a note to uh to evaluate and expand. So just I think I I will

**[20:31](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1231s)** show how selection scoring works because I think it's an interesting um like it's an interesting thing to think about that quite quite general in terms of thinking about this trade-off between exploration and exploitation. So when you're doing search for a solution, you can use breath first search or depth for search or something like this. And Monte Carlo tree search is specifically tuned to do this this this trade-off between exploration and exploitation. And it's really just in this core, we're summing a term that is exploitation. So in our case, averages the verifier feedback and exploration with how adventurous we want to be. And really the way we set it up here is we want to go deep instead of wide to to to do as more as as long programs as we can before giving up. And then here we favor underexplored actions

**[21:20](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1280s)** a little bit uh if we can. And so this sort of setup means that we will try to go depth first to to explore a solution. But if we end up in a spot which is always like causing the verifier to to um to reject to reject the node, then we we we kind of backtrack into another more promising part of the tree based on this score. And um so our main change is really to optimistically use the verifier. So if the verifier says I don't know or okay, we we we continue. But if the verifier says this is definitely not on track, somehow this program has an error very early on that there's no way to recover from it, then we fail and do not even add it to the tree. And so by this

**[22:08](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1328s)** optimistic use of a verifier, we just make sure everything stays on uh on track on on guard rails. And so in terms of uh results here, what we found is that Daphne performed quite well with our method, the red the red bar against other baselines, especially because of the semi-automation. And this was the this was the reason we decided to continue with semiotation in in semi-automation in Daphne sketcher thinking about how to have these symbolic routines that are providing information and providing uh partial programs that can be combined with with LLM partial programs as well. uh we did run into some um complications which I I guess I'm terming it the

**[22:56](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1376s)** scaffolding paradox which is when we wanted to scale this up in some sense it was um it was hard to get weak LLMs to use the scaffold so to use these symbolic routines that we provided because the edits the underlying edits that they that the LLMs needed to do to finish up the the proofs were too low level I mean too low quality and then strong LLMs like uh Sonet 4.5 for example wouldn't even need the scaffold in most cases like just by doing a simple verific verification driven feedback loop this would be it would be able to solve uh many problems and so this difference between weak LLM and strong LLM is really already the difference between set 3 and set 4.5 and in general that's a general issue with

**[23:44](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1424s)** neuros symbolic system how to get um how to get the LLMs and the and the symbolic system to co cooperate in a in a useful way that basically u is still useful regardless of how good the LLM is. It's still it's still helpful. And in terms of structured edits, we we were we were thinking about more frictions in the structured edits case than the left to right case because there is um no no uh no good sign of progress in terms of signal. we can't really just use the the number of errors because it can be misleading. And then the the the choice of where to edit what to the do you edit the optimize function or do you edit the the the the

**[24:34](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1474s)** proof uh is is also another complication on top of what to edit, where to edit and what to edit. And then the skill which is what I want to focus on just a little bit that the edits must be high quality and minimal. And that's something we've tried to solve with fine-tuning. So thinking about so in Daphne annotator with the discovery system Daphne synth we try to think of how to get better local edits by thinking of uh having um a loop with um with um um like basically an orchestrated loop where we generate diverse high quality uh quality data set of successes and then extract task from this and then do supervised fine-tuning LLMs and then scaffold holding and this back here would with with defin

**[25:23](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1523s)** annotator we did the first part of doing this and already got this boost in performance from 16 to 51% in terms of annotation. So just to give an idea of what Daphne annotator does here is an imperative code written in Daphne and it's it's calculating the square root of a number using a wide loop and you really need this invariant which is a proof annotation in order for the system to be be able to verify the post condition here and definitator is able to automatically provide such loop invariance for for many programs. So going back to this picture, uh this is kind of where we're at. We want to co-evolve an LLM and a discovery system to get better edits and to get like a symbiosis with uh the the symbolic

**[26:13](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1573s)** routines that we might provide. So I showed a few ways to to to think of combining unreliable L&Ms with reliable verifiers. And um and the main thing really is that generate and test is a starting point. and the verifier acts as the as the check for the test. And we can increase sample efficiency with synthetic data and fine-tuning. And we also saw in this interactive way with the in the demo and also with u with um with the with with the DNE sketcher orchestrated MCTS loop that verification can boost development in in some cases. And now I I want to talk a little bit about doing a different exploration of of the trade-offs between evaluation,

**[27:02](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1622s)** search and constraint solving focusing on constraint solving. So this is um the segue to to Holi which is a Python library for program synthesis and symbolic execution combining constraint solving and LLMs. And it uses this idea of multi-stage programming which is really a meta programming approach to write programs that write programs. And in this case, it's our way to go from uh a py python code and execute it to get SMT lib code that can be passed to a solver to solve for the constraints in the code. And um the setup is to solve Python programming puzzles and I'll give an example of this and LLMs are also able to guess some puzzles answers and we we can combine these things together. So

**[27:51](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1671s)** for example uh this is one of the satisfiability predicate that we might have. We say we're looking for a string such that set of solds where we're counting the number of of O's in the string and we get 1,00 and we're counting the number of OO and we get zero. So as a human you can think of how you would go about this problem of of finding a value or or like an expression that satisfies this uh this predicate. And when we pass it to an SMT solver, it times out for reason I will get back into. But it's kind of hard to reason backwards about about these uh these these numbers. But if we try three instead of 1,00 it gives the result OC. So that's an acceptable solution. And the and if we ask an LLM to extrapolate

**[28:42](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1722s)** from 3 to 1,00 it gives this this result for example. So it it was able to extrapolate. This was set for 3.5 I think and it was able to extrapolate but not to do it from scratch but set 4.5 is able to to do it both from scratch and extrapolate. But just to give a maybe a little bit more of a of a of an idea of how this works. Um basically you have the Python code. you're you're you're basically overloading operations so that this X is actually a symbolic X that with an overloaded operation will construct a constraint as a side effect. So here the constraint is fairly similar to what we started with but then you can pass it to an SMT solver to get a result. And just to give um another example of this,

**[29:34](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1774s)** uh this is like um a a Python satisfiability predicate which is a little more complicated because it uses a for loop and mutation and things like this. And I can try to to see what the code looks like and it looks something like this. And it's it's very long code because there's no there's not too much sharing in the system. And then if I if I run if I run on this I basically get the the solution here um it solve the puzzle with this solution and it's verified and an LLM some LLMs can like 4.5 maybe gets set one time out of three or things like that. Um so this was about synthesizing values from constraints and that's already can

**[30:24](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1824s)** be useful in some settings but I also want to go further than synthesizing values and thinking of synthesizing expressions in a language from constraints. Um in terms of future direction, one thing is that the the count example really showed that we were starting with a function and saying things about what should hold about the output of the function and that's very hard to then solve for what the uh solve for the input of the function. So this is a move where we want to go to relational programming to think about uh running things backwards and in multiple directions. So uh not not having a distinction between inputs and outputs. So that's what I I want to talk talk about next and I'll I'll I'll skip on

**[31:12](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1872s)** multi-stage programming and talk about relational programming. So really we saw that um what we we want to we want to basically uh think about uh how to how to reason about multiple directions and basically do synthesis from specification. And the way we're going to think about it is by programming with relations instead of functions. So by programming with relations that means we're programming directly with constraints. And that's very useful in the context of thinking about starting with high level constraints and generating uh code or expressions. And so this is based on a unified approach to solving seven programming problems. And the unified approach is to write a relational interpreter. and basically then generate expressions.

**[32:03](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1923s)** This relational interpreter is for a functional language and then generate functional expressions by running things backwards in the interpreter. And recently uh just this summer we presented multi-stage relational programming which is using the meta programming approach similar to how holy executes in stages to do that for for um for um for relational programming. And the the big benefit is that we're basically turning interpreters into compilers. We're running interpreters but without the overhead of interpretation. So just to give an idea of this, imagine you have this tower of things where you have racket at the bottom which is a lisp scheme like functional language and mini canin is written in racket and then you have this um relational this this

**[32:53](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=1973s)** this interpreter that takes a functional that is written in a relational style and uses a functional language and then say you have a small proof checker that just checks whether uh a proof is is correct satisfying the the the low-level rules of logic. And now you can turn this basically into a t theorem proving theorem prover by putting the the putting an unknown for the proof. So let me show uh some an example of this. So this is a a very small uh proof checker that is written in a functional style and it was written by by Mattmite without consideration of relational programming first just as a pedagogical example. So it covers assumptions modus ponent and conditionals. So it's

**[33:44](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=2024s)** explained when is a conditional rule valid and things like this. And it's it's uh it's it's calling itself recursively here for the conditional. And now we can basically ask for a proof of C given a context with A. A implies B and B implies C. And so let me show this and it provides this proof that that satisfies the proof checker. And you see that to prove this it's using modus ponent twice and then it has it's using three assumptions which sounds reasonable and you can try to do it for a bigger development and you can see that you just get a

**[34:33](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=2073s)** bigger proof here. And uh another example that is kind of quirky but shows that the constraints can can be um can be fairly specific where we have let me just show it here. We have another interpreter on top of the of the of of the of the relational interpreter. A quasi quote interpreter that understands quasi quotation in this list setting. And then we have a quasi quote relational interpreter and we ask for a coin a program that evaluates to itself and we get we we'll see what what we get here. So I get an expression like this with some side conditions. But this is basically an expression in racket or scheme and if I've evaluated it evaluates to itself

**[35:22](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=2122s)** and really what we ask for is we have this relation this evaluator relation which takes an input expression and an output value and we say they have to be the same thing. So to um to think about um so this was the the demo of multi-stage relational programming and to conclude what I want to to talk about is like just a few takeaways from from this talk. So I I showed a spectrum of combining neural and symbolic methods. in some sense I didn't show exactly how you would combine the very constraintbased methods with the with the with the more LLM based methods but I think there are ways to use this so for example already in the past I've I found that it was useful

**[36:10](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=2170s)** to use an SMT solver with an LLM so the LLM would generate SMT code which would be higher level and declarative and then the solver would compute the actual things would be the calculator for this so to speak and that's a division of labor and there might be others and then I showed this generate and test but also with fine-tuning to improve sampling efficiency so how often the test actually passes after generation and constraint solving as an alternative and then I really think the future is strong guarantees we need strong guarantees about all this generated code and there is a question of how to specify and ensure guarantees and a few things to think about is we might want to actually use infor formal specs because it's more convenient. We just want to use natural language to to

**[36:58](https://www.youtube.com/watch?v=yLDiQBdwlHU&t=2218s)** to say what we want. But then the system can internally use more precise guarantees, more formal guarantees and we can check them. We can check those formal specifications. It might be more tractable than checking all the code all the time. And so I showed these this LLM based verification and this relational programming. I've worked with a lot of people at Harvard and elsewhere on this on these topics and on these projects and all these projects are open source and here are the links. Thank you.

---
id: o3--D6ydS4o
title: "TDD & DDD from the Ground Up • Christopher Simon • GOTO 2025"
slug: tdd-ddd-from-the-ground-up-christopher-simon-goto-2025
conference: goto
conference_name: "GOTO Conferences"
category: "General software conferences"
edition: "GOTO"
year: 2026
speakers: ["Christopher Simon"]
channel: "GOTO Conferences"
duration_min: 47
published_at: 2026-04-17T12:01:37Z
video_id: o3--D6ydS4o
url: https://www.youtube.com/watch?v=o3--D6ydS4o
youtube_url: https://www.youtube.com/watch?v=o3--D6ydS4o
tags: ["GOTO", "GOTOcon", "GOTO Conference", "GOTO (Software Conference)", "Videos for Developers", "Computer Science", "Programming", "Software Engineering", "GOTOpia", "Tech", "Software Development", "Tech Channel", "Tech Conference", "Today in Tech", "GOTOcph", "GOTO Copenhagen", "Christopher Simon", "TDD", "DDD", "Domain-Driven Design", "Test-Driven Development", "Software Design", "Software Testing", "Event Storming"]
topics: []
transcript: true
---

# TDD & DDD from the Ground Up • Christopher Simon • GOTO 2025

**Christopher Simon**

`GOTO Conferences` · `GOTO` · `2026` · `47 min`

`#GOTO` `#GOTOcon` `#GOTO Conference` `#GOTO (Software Conference)` `#Videos for Developers` `#Computer Science` `#Programming` `#Software Engineering` `#GOTOpia` `#Tech` `#Software Development` `#Tech Channel` `#Tech Conference` `#Today in Tech` `#GOTOcph` `#GOTO Copenhagen` `#Christopher Simon` `#TDD` `#DDD` `#Domain-Driven Design` `#Test-Driven Development` `#Software Design` `#Software Testing` `#Event Storming`

[Watch the recording](https://www.youtube.com/watch?v=o3--D6ydS4o) · [Conference site](https://gotopia.tech/)

## Description

This presentation was recorded at GOTO Copenhagen 2025. #GOTOcon #GOTOcph

Christopher Simon - Live Coding Technology Coach @ChrisSimonAu

RESOURCES

Links

ABSTRACT
Are you interested in using Domain-Driven Design (DDD) to create maintainable and scalable software, but not sure how to get started?
Or perhaps you've heard that DDD is only suitable for complex domains - and when starting out, you're not sure if your project will need it?

Join me for a live coding demonstration that will show you how to apply Test-Driven Development (TDD) from the very beginning of a project so you can bring DDD in when you need it.

We'll start with the simplest possible implementation - a basic CRUD system to help a university handle student enrolments. We'll gradually add more complex requirements, such as the need to ensure courses don't become over-enrolled - which will prompt us to do some code-smell refactoring, strangely enough arriving at things that start to look like the DDD tactical patterns of repositories, aggregates and domain services.

In implementing these requirements, inspiration will strike! What if the model were changed - what if we allowed all enrolments and then allocated resources to the most popular courses as required so we never have to prevent a student from enrolling?
We'll now see how the TDD tests and the neatly refactored domain models make it much easier to embark on this dramatic change - in other words, how much more maintainable our DDD codebase has become.

The code in this demo is available in either C# or Java - the audience will vote on the day which language is used! [...]

TIMECODES
00:00 Intro
03:35 Demo
40:05 Event Storming
42:16 Demo
45:55 Outro

Download slides and read the full abstract here:

RECOMMENDED BOOKS
Kent Beck • Test Driven Development • https://amzn.to/4qqyXxr
Saleem Siddiqui • Learning Test-Driven Development • https://amzn.to/35OMb3n
Eric Evans • Domain-Driven Design • https://amzn.to/3tnGhwm
Vaughn Vernon • Implementing Domain-Driven Design • https://amzn.to/3o0KZjl
Vaughn Vernon • Domain-Driven Design Distilled • https://amzn.to/3IEufpT

CHANNEL MEMBERSHIP BONUS
Join this channel to get early access to videos & other perks:

Looking for a unique learning experience?
Attend the next GOTO conference near you! Get your ticket at https://gotopia.tech

## Transcript

*9,416 words · source: supa (en, exact timings)*

**[0:11](https://www.youtube.com/watch?v=o3--D6ydS4o&t=11s)** [applause] Thank you so much for that lovely introduction and thank you everybody for joining me here this morning. As you can probably hear from my accent, I'm from Australia. Uh I have sometimes gotten some feedback that when I get very excited, I start talking very quickly. So if I do that and I start to become incomprehensible, please do raise your hands and ask me to repeat myself. I will not be offended. Uh as was just said, we're here to talk about test-driven development and domain driven design. Um I'd like to share a little bit about myself first though, which is that uh I've always been a software developer my whole career, but I did spend a lot of time as a CTO of a startup in Australia. We launched two businesses. Um and these days I spend my time as a coach and a trainer and a consultant. And I find that the part of the work that really invigorates me is working with technology but allowing it

**[1:00](https://www.youtube.com/watch?v=o3--D6ydS4o&t=60s)** to connect with business outcomes and user outcomes. So I found myself listening to the keynote this morning sort of nodding vigorously about the need to connect with our users and to understand the problems that they have that we're trying to solve for them. So today we're going to be talking a bit about how to actually do that using some of these practices test-driven development and domain driven design. The inspiration for this talk came from a great question I got after another domain driven design talk where someone said this DDD idea sounds wonderful for very complicated systems but what if you start out thinking it's going to be very simple and you only find out later that it's quite complex how do you deal with that I thought that's a wonderful scenario that I could demonstrate to you because what we're going to be doing is test-driven development to keep the simple uh keep the application simple

**[1:48](https://www.youtube.com/watch?v=o3--D6ydS4o&t=108s)** and to help us grow it over time and then we're going to turn to domain driven design when we start getting stuck and we discover that there was hidden complexity all along that we were unaware of the domain that we're going to be exploring is a very simple one you might have done in a practice exercise I know that some people do interview coding uh tech tests around this it's a simple university registration system uh this is a classical kind of entity relationship model where we have a student who enrolls in a course uh and the courses are assigned to rooms very straightforward the main business rule that we're going to be building up to today uh is that the rooms have a capacity much like this room today only a certain number of seats. So if we allow too many students to enroll in the course some of them will get very sore legs. So we need to make sure that we block the enrollments of the students um once we hit the capacity of the course

**[2:37](https://www.youtube.com/watch?v=o3--D6ydS4o&t=157s)** and we say this course is now full I'm sorry you can no longer enroll. That's going to be the business rule that we build up to. Uh we're going to be using test-driven development. As I said, who in the room regularly practices test-driven development? Oh, this is wonderful. I don't think I've ever had so many hands put up to that question uh for this talk. Um I want to say that I know that test-driven development is something that there's a lot of opinions about out there in the world. Uh I'm not here to tell you that this is the one and only way to do it. I'm just showing you what I like to do. Uh and hopefully you might find something in that. But for those of you who don't practice it, um it's a very simple technique. We start by writing a failing test. We often call that the red phase because our tests go red because we haven't yet implemented the functionality that's going to fulfill it. Then we make the test pass hopefully in a very simple way. Then we

**[3:26](https://www.youtube.com/watch?v=o3--D6ydS4o&t=206s)** do some refactoring to clean it up. And it's this refactoring step that's critical because it allows us to keep the code simple and to continuously improve the design as we go. So without further ado, we're going to jump right into the code. Now, I'm not going to be uh strictly live coding today because I've been making lots of mistakes and you'd all get very bored. But what I've done is I've got a git repository with every little step as a separate commit. I'm going to step through the commits and we'll talk about uh each step as we go. So that git repo, I'll have a QR code at the end. You can check it out if you'd like to. So the way we step through it, I have a script here um that steps forward through the commits. And the very first thing we're going to do is create a test. Very straightforward test. I'm doing this in Java today. Okay, I do have a demo in .NET, but I figured is Java safe for this audience? I figured it probably would be. Um, I'm writing a first test.

**[4:14](https://www.youtube.com/watch?v=o3--D6ydS4o&t=254s)** Given I'm a student, when I register, it should register a new student. Pretty straightforward. Uh, I'm using sort of the BDD syntax without the whole cucumber thing just because I like the framing of the tests. Uh, we're writing the test at the level of an API. So, we're sending in a post request to the students path. uh and the it should register asserts that we get an is created uh status code response. Now, of course, if I run these tests, they are going to fail because I haven't even got a controller yet or any kind of handler of this. So, there's a failing test. You can see here that I've really added almost nothing to this test. It doesn't assert on the payload. It doesn't assert on sort of any error scenarios or anything. It literally just says, do I get a created status code? So, the only thing I need to do to make this test

**[5:01](https://www.youtube.com/watch?v=o3--D6ydS4o&t=301s)** pass is to add a controller that returns a created status code. That's it. The simplest implementation to make the thing work. So now rerun the test. It is actually on auto run, but it only auto runs if the files that are open change. So if the files are just getting created. So now that test goes green. Obviously this is not enough for the whole system. So let's add another test to start asserting on another thing. So we said it should register but also it should allocate an ID and we have a look at the payload of the body um pass uh expect that there's a body there and we make sure that the ID is not the empty good and it's not null which of course fails. So let's [snorts] have a very simple implementation where we create a student

**[5:50](https://www.youtube.com/watch?v=o3--D6ydS4o&t=350s)** with a random UID and now it's passing. You can see this style of TDD is very very small tests, very very small implementation. I really like this style because it makes me feel safe. I often think that writing code is like writing walking a tight rope. And uh if you're practicing TDD, when you write a test, you balance a little bit in this direction. And when you make it pass, you balance a little bit in this direction. Whenever I write too much code without a test, I feel like I'm wobbling off to the side, and I kind of want to get back into the middle. But similarly, if I write too many too much of a test before I start making a pass, I have the same experience. So, it's all about this kind of gentle sway from side to side, little test, little implementation, and back and forth. So we're passing which is lovely. Let's

**[6:40](https://www.youtube.com/watch?v=o3--D6ydS4o&t=400s)** move forward. We do a little bit of refactoring. What I've done here is start to introduce a little bit of what I call DDD light thinking. So domain driven design is all about letting our understanding of the domain drive the design of our code. Um if you didn't catch what the change was there, just go back for a moment. What we had was a student uh class and we initialized it with a random UID right here in the controller. But the refactor pulls that out and puts it into this register method which is just a kind of a static factory uh method. So I really like static factory methods. They give you uh the way an opportunity to effectively kind of name your constructors. And the nice thing about that is I can start to use the language of the domain which in this case is that students register. That's what we had on that um model before students register.

**[7:29](https://www.youtube.com/watch?v=o3--D6ydS4o&t=449s)** You'll notice that in the controller um the method is register new student. Uh sometimes by default we find ourselves writing the method called post or a method called create. The student doesn't get created when they access our system. They were probably born 19 years ago. So create doesn't really make much sense to me. But register does. It's the operation that our users expect to happen. So let's use that in our code. We use it in the controller method. Uh and we put a static factory method on the class. Uh and now I just find this code a little bit more readable. This is you'll notice if you know anything about DDD that this is not like full-blown DDD. This is and and as I said at the start, we're trying to keep it simple. We think that this is just a simple CRUD application. We don't need all of the fancy DDD stuff. There's nothing stopping you using the language of the domain in your code and just starting to sprinkle that language in to make the code a little bit more

**[8:17](https://www.youtube.com/watch?v=o3--D6ydS4o&t=497s)** readable, a little bit more connected to what our customers talk about when they talk about the system. We're going to add another test tower because we're doing a REST API. So, of course, there should be a location header if we've used if we've had a created uh status code. So we're looking for the location header. We're expecting it to be students and the student ID which of course it is not yet. So let's make that pass. We compute the new location. Uh we pass that into the created method which sets the header and then we send back the body of the student. So you can see gradually building up this thing one tiny little step at a time. We add another test to insert that uh we confirm the details. So, we're going to put the name of the student on the payload of the response, which is not there yet because at the moment we were only putting the ID on the response. One

**[9:06](https://www.youtube.com/watch?v=o3--D6ydS4o&t=546s)** thing that you might notice here is that the student class inside the application doesn't even have a name field, but somehow there's a get name on this student class in the tests. And this is deliberate because what I've done is I've added um this kind of uh class in the test uh side of the application separate from the student entity in the application side. The reason for this is quite important. We're writing this test at the public interface of our system. So our AP API consumers are going to be interacting with our API. Our test should act like an API consumer. So if we use the same class from the application in des serializing our our response in the test and we made a mistake and we changed that in some way that could break all of our API

**[9:54](https://www.youtube.com/watch?v=o3--D6ydS4o&t=594s)** consumers but our tests would keep passing. So by creating a separate class on the test side uh we can effectively assert the des serialization into the contract that's expected and it protects us from accidental changes on the application side that might inadvertently break our API consumers. So we've now able to say, hey, there should be a name on here. And of course assert that the name matches to the name that we passed in on the request, which we're not doing yet. So it's failing. And now we can update that. And because we already had a really nice register method that encapsulated the allocation of the ID, we can pass that name into that method. That becomes the place where we set up new students. Um, and it's and it's quite an easy thing to do at that point. The next thing I'm going to do is

**[10:41](https://www.youtube.com/watch?v=o3--D6ydS4o&t=641s)** actually something that uh little bit of a diversion from the code, but I hope you'll indulge me. Uh I really am passionate, as you can probably tell, about this concept of the ubiquitous language. So I've been working on a tool to help us manage a ubiquitous language in our code bases. We have here a a glossery in a YAML file um where we start to define terms like student and register. And the nice thing about it is there's an extension for IntelliJ and for VS Code where now if I hover over that word I get to see that definition and that works also in the code. Hover over the class I get the definition. Hover over the controller I get both the word register and student because it's not matching on anything in the syntax. It's just matching on the word and it knows that in this context this is what these words mean. So when you use these

**[11:28](https://www.youtube.com/watch?v=o3--D6ydS4o&t=688s)** words, this is how you should be thinking about it. And my hope is that this is something that will help developers connect with and understand the languages in the domains that they're working in. It actually goes a little bit further than that. Um if you go to the documentation site for this tool, it's called contextive. Uh you can see all about how to set up these glossery files. And I've just recently added a new browser extension. So if I turn this on, this is actually the university I went to, the University of New South Wales in in Australia. Um, and this word student and enrollment, they're all over the place. If I activate contextive on this page, um, of course it doesn't work during the demo, does it? It I swear this was working just [laughter] before we came on the thing. [gasps] I might give it another go

**[12:16](https://www.youtube.com/watch?v=o3--D6ydS4o&t=736s)** later. Anyway, let's go. Let's go. The point is what you start to get I can show you a picture in the uh in the documentation is you get the terminology on the page gets um highlighted like this and you can hover over it and see those definitions even in the browser. So you can start to see the same terminology in the browser as you do in the code and your whole business can start to align around the way that you speak about um the domain that you're working in. It's not really the point of this talk but it's just a little aside there but check it out if you find it interesting. It will definitely work for you. I promise. So let's get back to our tests here. We're going to add a test for uh actually checking a registered student details because so far we've just actually done the registration and asserted on the response. So now we're going to introduce a um a get request. So the first thing we do is register the student. We post the details in and then

**[13:06](https://www.youtube.com/watch?v=o3--D6ydS4o&t=786s)** we do a get request to get it using the new location that was provided back to us and it should find that student. But of course it doesn't because in our controller we've currently still only got our register method. So let's add a simple um response method which at the moment returns nothing because all we put into the tests was that we expect an okay status response. We're not asserting on the payload yet. Very small tests to do the minimal amount of thing. We just assert on the status code and then we go and make the status code come back as we want it. Then we update the test to um start to uh um h sorry we're starting to what I'm trying to do here is actually do some refactoring on the test side. You probably noticed before that the test was getting quite unwieldy with lots of um code required to execute the API

**[13:55](https://www.youtube.com/watch?v=o3--D6ydS4o&t=835s)** call. Um so we start to uh put like a register student like method that encapsulates some of the functionality of actually calling the API which allows us to express the test a little bit more concisely. One of the things about that refactoring is that it only ever refactor the tests or refactor the code in separate steps, never at the same time because we want to make sure that whatever the refactoring you're doing, it's keeping things in check. It's that balancing act again. So now we can actually assert on the student details uh in the get response. We're checking that the name is there, which of course it isn't because our controller is still just not really returning anything. And then we can add in a response that returns a hard-coded student. This concept of returning a hard-coded value is one I use all the time. There's some real value in it because one of the

**[14:43](https://www.youtube.com/watch?v=o3--D6ydS4o&t=883s)** whole reasons that we write the test first uh is and then we write the code is because we want to see that test go red. When we make the test go green, what we're doing is we're actually checking that our understanding of the test is correct. One of the things about TDD is that we often say it's red, green, refactor, but what's most important is it's the thing I expect. It's the it should always be the color that you think it should be. And so if I had implemented this hard-coded response and the test was still red, that would tell me that I didn't actually understand how the test was working. So by doing a very simple implementation, I build my own confidence that the test is checking the correct stuff. and then I can move on and start to implement more tests that require me to build up more complex implementation that is actually the real thing. In this case, we haven't

**[15:31](https://www.youtube.com/watch?v=o3--D6ydS4o&t=931s)** even got any persistence yet. You'll notice that the registration method is just literally returning the payload with a new ID, but it's not storing it to a database or anything. And so, it's easy to do a get response that just has a hard-coded value. But if I start to add uh some parameterization of the test, so we parameterize it and add a value source. Still just passing in test student at the moment. So that's just a refactor that keeps it all the same. And now we can add a second uh parameter, [snorts] another student uh which is going to fail because now it doesn't match the hard-coded value. And now I have a test that actually justifies implementing a little bit more complexity into the controller. And so this step is actually a little bit larger because I have to add a student repository. I have to save to the repository when the student is registered and then I have to check the

**[16:19](https://www.youtube.com/watch?v=o3--D6ydS4o&t=979s)** repository to load it on the way out. But one of the things about this is that that it feels like a big change. I've just added persistence because but because the whole structure was already there, it was actually still a very small step in the flow. Um and it was just a JPA repository, very straightforward um implementation just extends the standard repository and I get that sort of functionality. So again, very lightweight, very small step uh very incremental change to what we already had. going to be doing some more uh refactoring on the side to sort of uh simplify the way that we're constructing these um API calls. So we now have a student API class that encapsulates the common operations like registering a student and getting a student. And so we're kind of building up an SDK or an API on that

**[17:08](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1028s)** side. And then on the controller, a little bit more refactoring to get the student URI um in a separate method so we can uh reuse that in a few different places. Now we add a test for another scenario which is that we are requesting an unknown student ID. So if we request a random good we should get a 404. Um but we don't. What we actually get is a 500 because we end up with um or else throw here because there's nothing was loaded from the repository. Uh so we want to make sure that that's actually a 404. So we replace or else throw but we respond with a response status exception of not found. So now it gives us the 404. Just a final little bit of refactoring around the students. Move it all into a

**[17:55](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1075s)** subfolder that's starting to make room for the next phase which I'm going to be implementing for adding courses uh and adding rooms. Now the workflow for that is pretty straightforward. It follows exactly the same thing. So I'm not going to bore you with it again. And I'm going to step forward uh a whole bunch of steps pretty quickly now where uh but you can if you follow along the the git commit you can see um you know allocating new ids uh you know doing some refactoring uh inserting on the location header all the stuff that we did earlier. uh adding the room definition, taking the room details, hardcoded room parameterization should all be quite familiar by now. The thing after rooms that we're going

**[18:42](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1122s)** to get on to is including courses in the catalog. So, I'll do a couple of steps here, but this actually does to start to get a little bit different because when we add a course in the catalog, one of the requirements that we had uh was that we nominate what room it's in. So, that's going to require a little bit of extra validation because we want to make sure that the room already exists when we allocate the course uh when we include the course in the catalog. Just noting here as well, of course, that uh the method name here is include course in catalog. That's actually the operation that we're doing. So it's not a post, it's not a create. It is [clears throat] technically that, but we're using the business rule, business terminology um at this level. Uh moving that uh construction into a static factory method as we did before and bit of refactoring on the tests [snorts] location headers. I'm just

**[19:31](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1171s)** jumping ahead here to where we start to assert that the um room exists. Okay, so now we're saying adding a test. So, let me clear all of this stuff out of the way and come into the rooms tests. uh when I set up a room now. Uh oh, sorry. It's a it's a course. Uh the course can be linked to a room. Sorry, I was in the wrong spot there. Um what we're doing here is we're using the

**[20:19](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1219s)** room API to set up a room. Uh and that's our room. And then when we include the course >> [snorts] >> uh do the include course request operation, we include the get ID of the room. Uh so in that process we're linking the uh the the course to the room and now that test is failing um because we're asserting it should confirm the course details and uh in confirm the course details we're checking the name and the room ID which we were not handling just yet. We only just got up to the point of handling the name field. Sorry I know I jumped through a lot of steps there but hopefully that made sense. Now we just add a very simple implementation. Uh we are storing the uh just immediately saves it because we know for sure in our test that the room

**[21:07](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1267s)** does exist. You'll see here in this test we did create the room and then we included the course in the catalog uh and we linked it to the room that did already exist. This is interesting because we know that we're going to have to handle a scenario that we try and link it to a room that doesn't exist. Um, but we don't we're not handling that yet. So, we haven't because we haven't written a test that asserts on that situation. But it's important that even though this test would have passed if we had not created that room first, when you create a test, even the simple test, you want to build it in such a way that it's going to continue to pass into the future. So, it needs to be correct, just a simple version of correct. Okay? even though you're building an incorrect implementation based on what you know the final result is going to be. So now we just have a very simple implementation that just immediately

**[21:54](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1314s)** saves it. And now we're going to add a test for when the room doesn't exist. So um there's another test down the bottom here. Given I have not set up a room when I include a new course in the catalog, then it should uh give me a 4x client error, which it doesn't do. It actually just returns a 200 um because we didn't have that check yet. So now we'll go back and add a very simple did not mean to do that. Um course controller uh we're going to check that the room ID is not null um before we do that. So that's the first step and then we're going to add uh you know refactor to use spring boot validation around that property in the

**[22:43](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1363s)** request object and [snorts] then we're going to um actually test for an unknown. So the first test I did was for a null room ID. Uh, and now we're going to test for a random um room ID, which still isn't pass is now um once we let the test catch up to all my jump forwards is not passing. So, we we did add a check that the room ID is uh not null. We ended up refactoring that into the spring validation. Um, but if it's not null, we're still just saving it. So now finally we can do the thing that we probably you know otherwise might have just jumped straight into which is to check the room repository to say does this room actually exist and here we're saying if it doesn't exist respond with this bad request. Uh otherwise we can go ahead and add the room. We're starting

**[23:30](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1410s)** to build something here which looks a little bit like business logic. It's kind of at that boundary point between input validation and business logic. Um so maybe there's a better way to do this but the moment I'm kind of comfortable just sort of leaving it in the controller here. Um, but the next refactor that I'm going to do is actually going to start to think about how could I include that in my domain model. Um, because we've got an including catalog and uh if there's a rule that says, well, the room must exist, how can I express that as a technology agnostic expression in the business layer? So, I've chosen to go with an approach here that might be a little controversial for some of you Java experts, which is that the repository returns an optional object, and I'm just passing that optional object in. And so, then inside that method, I do a map over the optional, which if you're not sure

**[24:18](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1458s)** what that means, it just says only execute this if the [snorts] if it's not um you know, if it is actually defined, which means that if it's not undefined, it immediately passes back um a not specified optional. And so what that allows us to do is to say including catalog or else throw bad requests. The nice thing about this construction is that if more rules arrive, if there's more things we have to check, they all go into this method and they all end up resulting in the same status response at the HTTP level. So it's done to separate between technical concerns, which is like what's my HTTP status code, and business concerns, which is like what are the rules around how to do that? And we're effectively representing the technical concerns using this optional type. uh so that the business rules can be expressed without care for whether it's a um you know how it's represented

**[25:06](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1506s)** at the HTTP level. This is starting to get a little bit closer but it's not technically the same as what you might have heard of as the ports and adapter pattern um or hexagonal architecture where in that model the controller would be a an adapter between the HTTP interpretation into the application. Um and some people would say well then you know interacting directly with your domain models is not correct. Um I think there's some wiggle room and argument about that because the main point is that when you get down into the course thing that that is our application. So hexagonal kind of nests the things and says on the outside you deal with the technical concerns and you put your application inside in such a way that sort of um your the adapter translates into how the business needs to uh be expressed in in sort of technology agnostic code. So, we're kind of doing

**[25:54](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1554s)** it, but kind of not. Um, the other thing that's a little bit different to traditional uh hexagonal or ports and adapter is that I've got these folders here which are about the domain concepts, courses, rooms, and students. What you might see in a typical hexagonal architecture is that you would have a controllers folder and a domain models folder. Who's working in an environment with that where you've got controllers domain um maybe repositories or persistence or something like that. So that's classical uh hexagonal architecture where your primary organizing principle is around those layers of your application. What I'm doing here is what's known um as vertical slice architecture because our primary organizing principle is actually the domain concept and within the folder then we're still using the ports and adapter segregation to an extent um to differentiate between the technical

**[26:43](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1603s)** level of the controller and the domain model of the course. So you can see here that by doing this, even though I said at the start I'm not really going to be doing full-blown DDD, I'm starting to introduce some sort of DDD like concepts using the ubiquitous language, trying to express my domain logic in a way that's agnostic to the technical concerns. And we're getting something that I think kind of makes sense. It's reasonably easy to read and very happily is protected by all of these robust tests. So let's get u back onto our flow here. uh do a little bit more refactoring asserting that the course is an unknown course is 404 very similar to what we did before and now we get to actually the really interesting stuff we've got all our basics we've got students courses and rooms and now we're going to handle enrollment because remember what I said at the start is that the key

**[27:30](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1650s)** requirement here is that when we go to enroll the student we need to check the capacity of the room that the course is allocated to and block the enrollment if uh it's full. Okay. So let's again start very simply. We're just going to implement uh an enrolling controller um which once again just returns sort of nothing. This is again building up slowly step by step. So I won't go through that in detail cuz it's very similar. Uh adding tests are confirming new enrollments etc. Um adding the method a little more uh structure to it the new ID of the enrollment. Adding a test to confirm that the student ID is confirmed in the response. um adding to test to confirm that the course ID is confirmed in the response. So we're gradually building up this enrollment object with a student ID, an enrollment ID, and a course ID.

**[28:19](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1699s)** All of these tests are built in such a way that even once we do check the capacity, they will continue to pass. Um tests around unknown student IDs and then we start to add a little bit of more validation. So checking the student repository uh if it's not found we respond with a not found because the student ID is actually in the URL it's in the path and then when we add a test for the unknown course ID and an implementation of that we do a similar thing but we respond with a bad request because the course ID is in the payload. So this is some HTTP status code formalisms for us there. Uh checking for the location header. I know I'm not jumping back and forth between the tests and the implementation right now because we're sort of, you know, it follows very similar patterns

**[29:06](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1746s)** to what we've thought before and I'm building up to the the crunchy bit because what we find is when we get to um the test that uh starts to actually check the capacity, this is it. So, let's add a test for if the course is full. Given I have the wrong student ID, wrong course ID, uh given the course I want is full. When I enroll in a course, it should not enroll me and it should give me an error. Now, we haven't checked the capacity yet. So, when we do this, let's talk through this test. We set up a room with a capacity of three. We enroll three students. Student one, student two, student three. Then we enroll our

**[29:55](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1795s)** fourth student who should fail. But they don't fail. They just get added because we haven't yet put the capacity check into the code. But the nice thing now is that adding the capacity check is a relatively simple addition to what we already have because the whole structure is already built up around it. We've got all of this kind of logic around checking the student, checking the course, and we got all the things that we need. So now that test is failing over here given the course I want as full when I enroll in the course. So let's make it pass with a very simple implementation. We just go and load uh the room that's associated with the course. So we get course get room ID and load that. Uh we count the number of enrollments that are related to that. So this is using uh sort of a JPA approach uh criteria builder. I think we'll refactor that shortly. Then we check if the number of

**[30:43](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1843s)** enrollments plus one exceeds the room capacity then respond with a bad request. So now the test is passing which is great. So simple implementation in the first instance but of course looking at this hopefully you agree with me this is starting to get not quite how we would want to leave things. We want to clean it up uh straight away. Which leads me to an interesting point about the refactor step in test-driven development. Sometimes people kind of are ending up on a little bit of a rush. They've got to hit a deadline. Maybe they say, "Uh, we'll write the test later. We got to get this stuff live." And so they start not doing TDD and they just get the thing working. Create, maybe create another ticket to add the tests. Or maybe they do TDD, but they get to the end and they say, "Ah, it's working now. I've just got to get it live. I'm going to skip the refactor step. Everything will be fine." So, I want to share a story with you uh about my daughter who

**[31:31](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1891s)** was five at the time. And uh we were having all sorts of challenges getting her to keep her play area tidy. the I don't know if what's going on these days with kids toys, but she seems to have about a million toys this big and they scatter all over the floor, plastic and metal and wood and all sorts of things. And uh you know, we're trying to help her learn how to keep her space tidy and it's quite challenging. And so one day after much weeping and wailing and nashing of teeth and working together to get this room tied, she said something tidied up, sorry. She said something very profound to me. He said, "Daddy, next time I'm going to tidy it up when it's just a little bit messy because tidying up when it's really messy is just too hard." I said, "You've just explained TDD to me. Thank you." [laughter]

**[32:18](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1938s)** Because that's exactly what we're doing. The reason I'm going to refactor this immediately is because if I wait until I have a 100 controllers that look like this and someone says, "Oh, you need to change the business rules." and I have to go and change it in 50 places and I have to go back and understand these complex rules that are sort of scattered and embedded everywhere, I'm going to have a very bad time. But right now, it's incredibly easy. I've just done it. I understand it. It's right there. I know what it's doing. I can clean it up. I've just written the test. The test is passing. And so, the cheapest time to clean this code up is right now. And it only gets more expensive over time. And if you let that build up across your codebase, you end up in that kind of technical debt quadm that a lot of teams are struggling with where change becomes very very hard. And this is what led me to become so passionate about TDD. I

**[33:07](https://www.youtube.com/watch?v=o3--D6ydS4o&t=1987s)** think there's a commercial imperative to it. Keeping the code base clean keeps the team efficient and there's just no two ways about it. Now there are judgment calls around when you do this. Do you you know Kent Beck has this great book tidy first which is an examination of do we clean things up before we make a change? Do we clean it up immediately afterwards? And there's some great analysis in there, but I think what he definitely agrees is that there's, you know, tidying needs to be part of your workflow. So, what are we going to do here to clean it up? First thing I'm going to do is start to do some extraction. Uh, we had two repository calls here. So, I'm going to extract the room repository. So instead of going and getting the course and then getting the room, I'm going to add a find for enrollment method on my room repository which is going to in the first instance just a hacky

**[33:53](https://www.youtube.com/watch?v=o3--D6ydS4o&t=2033s)** implementation that uses some embedded sort of uh JPA SQL. Um but you know we can clean that up over time but it starts to clean this controller up. Uh then I'm going to use a derived query count. So, I had a more complicated uh method of finding the number of enrollments, but now we just have count by course ID, which is a great sort of JPA method that that cleans that up. And then we're going to encapsulate the enrollment method check on a method on the room. So now instead of having that calculation that formula right here, I've got a method would enrollment exceed capacity and it's just the same logic. I've just by putting it on the domain model, I've now given it a name. In the DDD parlance, we call out an intention revealing name. It tells us not just what the formula is, but why. Why are we checking this? Well, we're

**[34:41](https://www.youtube.com/watch?v=o3--D6ydS4o&t=2081s)** checking would the enrollment exceed the capacity? And so now we're asking that question here. What I would really love to be able to do is sort of hide a bunch of this stuff and be able to just write in the ubiquitous [snorts] language, my new enrollment equals course.enroll. Maybe it returns an option, you know, maybe not. But this doesn't really compile and it doesn't pass um because there's some dependencies on loading the extra data from the database. Um so an alternative approach is to use uh a concept from uh DDD called a domain service. So we're going to add this domain service called an enroller. Let's have a look at our enroller. uh and it has it encapsulates the loading of the room uh and the uh um and the enrollment details and it says

**[35:32](https://www.youtube.com/watch?v=o3--D6ydS4o&t=2132s)** [snorts] enroll in enough capacity and it says find the room for the enrollment uh and then within that I can go and load the count by course ID uh then call that room would enrollment exceed capacity method which is great and now return an optional and so now we've got a nice method that sort of is a enroll if enough capacity and so a domain service exists to help you write some and and encapsulate and business logic that kind of deals with multiple domain models doesn't really belong in either. And I find it also helpful if I want to encapsulate some of the data access as well. So now a controller um doesn't really have too much to do. Uh and we've got this nice enroller method, do a bit of refactoring, simplification. Now we hit an even more crunchy point because we start to get some feedback um from our uh stakeholders that our system isn't working properly. We say, "What do

**[36:20](https://www.youtube.com/watch?v=o3--D6ydS4o&t=2180s)** you mean? I've got all these tests. They're all green. it definitely works. Don't you tell me there's a bug in my brilliant code. And they say, "No, we definitely had some courses where we had 20 seats in the room and 21 students showed up and when we checked the records, there were 21 enrollments for that course. So your system's broken." And you go back and you check it and you say, "Well, this this rule, where is it?" No, I'm definitely checking. Number of enrollments plus one, don't allow the enrollment. Who who has a guess about what might have been going on in this system? Yeah. >> Concurrency. Yes, I think few people nodding their heads. So >> capacity has changed. That's an interesting one. Um that's definitely a possibility and we could explore that another time, but um in this case the concurrency is the thing that we're going to take a quick look at because what we're doing here is we're loading

**[37:07](https://www.youtube.com/watch?v=o3--D6ydS4o&t=2227s)** this data and then we're checking it and then we're resulting it. So what if we had two enrollments come in at the same time? The first enrollment would say, okay, there's 19 enrollments. The second one will come in and load that data and say there's 19 enrollments. So they both get we're good to go and they commit back and we end up with 21. So how do we solve this problem? What have people done in their systems when you hit this kind of concurrency issue? Resource lock. Yeah. So if you didn't catch that at the back, a resource lock or some kind of database transaction lock. So at some level of the system, we need to detect that there's concurrency. And if we've got two people trying to interact with that same data, then we put a lock around it and we say no, one of you has to wait until the operation is completed and then you can go

**[37:54](https://www.youtube.com/watch?v=o3--D6ydS4o&t=2274s)** through. Now, we're only talking about low volume here. But in a high volume system, this is absolutely an recipe for a whole bunch of users watching a spinning wheel for a very, very long time. But it is something that we could do. But before we get into the solution, I want to show you the TDD thing to do in this case, which is that we've now written a test um which is uh where is it enrolling tests um given the C when I enroll in a course concurrently. So we're going to have that situation where we set up three enrollments and then we have two extra enrollments. And what we're doing here is using this Java feature of completable future to sort of allow them to like fire and forget. So we're not waiting for the enrollment to complete. And then what we do is we check the enrollment response of one of

**[38:43](https://www.youtube.com/watch?v=o3--D6ydS4o&t=2323s)** them and we assert that it should not have been enrolled. Now in this case the test passed and we're like okay well we don't seem to have a concurrency issue. What's going on? Um but this is the thing with race conditions is that they're unpredictable. They sometimes happen, sometimes don't. It's all down to little timing intricacies in the middle of the code. So one of the things I like to do is to set up uh a test case well like a test uh execution approach called test and field failure. Intelligj has this thing um where you can say repeat the test until it fails. And so if everything is really good and you run repeat until fail, it will just keep running forever. Green green. But if you do have a race condition and you run test until fail, what you'll find is that within a couple of loops, you hit the race condition. So, let's see that happen here.

**[39:31](https://www.youtube.com/watch?v=o3--D6ydS4o&t=2371s)** Yep, now it's gone. And we can see here actually that it had 50 pass tests before it hit it. I've only got 21 tests. So, this actually took three loops before it hit the failure case. And so, this idea of loop until failure is a very powerful one if you hit a race condition. But at this point now I actually want to step away from the whole concept of the race condition because you know we might have spent some time digging into this whole you know how we going to put a lock around it and everything. But thankfully someone on our team uh said you know feel like if we're starting to think a little bit about DDD maybe we should talk to our users first and understand what they actually wanted to do. So what we're going to do is go and do an event storm. Uh and this is a thing from DDD. If anyone came to Jackie's talk before she talked a bit about event storming. Um the nice thing about this is it allows us to visualize the business process flow that's going on here. So we

**[40:19](https://www.youtube.com/watch?v=o3--D6ydS4o&t=2419s)** say okay well we set up our room we include the course in the catalog and we have a rule there that it must be allocated to a room. Uh the student gets registered. Uh then the student gets enrolled in the course and we say we can't overfill the room. And the great thing about this is it's such a simple visual exploration that when we show this to our business stakeholders they look at it and go sorry you're doing what? Wh why why would you do that? that's not what we wanted you to do. You say, "No, it it definitely is. I didn't you didn't you look at this diagram? That's what it says we're going to do." And they go, "What? [laughter] How are we supposed to make sense of that? This doesn't make sense any sense at all." Because this doesn't tell you about the process. It's a static representation of the data model. The nice thing about the eventtorm is it gives you the process. And what they explain to you is, "No, this wouldn't make any sense. Why would we allocate

**[41:06](https://www.youtube.com/watch?v=o3--D6ydS4o&t=2466s)** the rooms to the courses before we even know how popular they're going to be? We've got one really big room and one really small room. I'm not going to decide which course goes into which room until I know how many enrollments I have. Because if I have, you know, introduction to programming, which gets five people because no one's learning programming anymore and everyone's vibe coding, and I have, you know, some other interesting course like uh ancient myths and legends of Greece. Um, you know, if I put them in the wrong room, we're going to have a bad time. So, let's redo the event storm, engage with our business stakeholders, and they're going to tell us actually what we expected is that we're going to set up the room or not actually because we could do the setup of the rooms at any time, but we do want to list the courses. We want to do want to get students registered. We want to start to monitor enrollments. And then as we monitor enrollments, we can make some choices. Maybe we go and

**[41:54](https://www.youtube.com/watch?v=o3--D6ydS4o&t=2514s)** get some extra rooms if we need them or extra instructors because we don't want students to get turned away. Once we hit a deadline date, we do like auling algorithm that maps courses to rooms based on actual demand and actual capacity. And then we end up with our course schedule for the semester. And you're like, "Yeah, that kind of makes a lot of sense actually. I wish we'd done that." But you didn't. But the good news is if we go back to our code, it's not too hard to fix it because most of what we've done is still the same. It's just this operation of uh the checking the the level at the enrollment. So let's step forward a little bit and I know I'm getting close to time so going to wrap it up very quickly but the first thing we do is rem remove that concurrency test because we don't need it anymore. Uh we're going to remove the functionality associated with checking the enrollment capacity. This is a

**[42:41](https://www.youtube.com/watch?v=o3--D6ydS4o&t=2561s)** really interesting point with TDD. If you are removing functionality this is one of the few cases where you actually change the application first rather than changing the test because if you remove the test first then your test will go green but you've still left the logic in the application. If you remove the logic first, your test will go red and then you can make it go green by removing the test. So it's kind of a reverse operation and it means that we're still following that rule of red green refactor which is actually far more important than test application refactor. So red green refactor. So we make it go red by removing the functionality. We make it go green by removing the test. Continue through a few of these steps and then we're going to start testing theuling process. First thing we're going to do is add an API for it. But here we get to an interesting decision because theuling process is quite

**[43:28](https://www.youtube.com/watch?v=o3--D6ydS4o&t=2608s)** complex and starting to write tests around it is going to get quite tricky. Um because we have to construct so much data into the thing. We actually got a pretty meaty DDD domain problem here now. And so for the very first time I'm going to do what probably a lot of people thought I was going to do which is actually write like a classical unit test and not an API integration test. We're going to write a test around the class that contains that business rules. I didn't do that before because my classes were nothing burgers. They were doing nothing. They just had getters and setters and storing properties and I'd much more interested in the resiliency of the application at the public API level. But now I have to write some business rule logic and I want to make sure that those business rules are correct. And I want my tests to be fast because there's lots of different scenarios. And so we're going to add a bunch of tests. Now I can see that I've got one minute left. So we're really

**[44:17](https://www.youtube.com/watch?v=o3--D6ydS4o&t=2657s)** down to the wire here. Apologies friends. Um but we're going to start adding some tests in our umuling folder uh which start to allow us to construct our object with the actual number of enrollments and just pass that into the algorithm. So now when we assert on that the scheduleled course size is equal to a value um and we're calling schedule courses this is just a a classic domain service that has no uh infrastructure dependencies and I can assert and test on it uh directly so classic kind of you know unit test and we're going to just keep building up on this with some more scenarios some more uh complex things and I'm kind of rushing through this I do apologize we're getting close to the end um and some refactoring so now we end up with got kind of a nice thing

**[45:05](https://www.youtube.com/watch?v=o3--D6ydS4o&t=2705s)** here where we say we load the courses by the most popular. We load the rooms by the smallest. We assign the courses to the rooms uh by mapping through uh assigning each course, finding the smallest room that will suit the enrollments and removing that from the available rooms and then eventually ending up with a nice uh simple algorithm for that. The very last thing that we do after uh wiring up the APIs is that we go back to our domain models and we realize that well one of the ways we know that we're not doing DDD is we've got all these public setters. If you know anything about DDD it's kind of a cardinal sin. You don't allow other things to modify your entities. So the very last thing is I'm going to remove all the public setters and make them private. And the nice thing about that is that our tests will continue to pass because we've been kind of pushing logic down into meaningfully named methods the

**[45:53](https://www.youtube.com/watch?v=o3--D6ydS4o&t=2753s)** whole way along. So that's kind of it. There's going to be a uh I'm going to uh if you want to check out the code, this is the GitHub for that. You can check out all the commits. There's a link to that to the .NET version if that's your preference. Follows the same kind of story. Um, if you want to check out that contextive tool for ubiquitous language documentation, um, check that out here. It's all free. There will be a paid tier soon if you want to integrate with like notion and other things, but this is the open source version. And these are my details. Love chatting about this stuff. Please do connect at uh, anytime and uh, hit me up online. And I'll be waiting outside if anyone wants to talk about this stuff. Thank you very much. [applause]

---
id: 1ec_kSiAuwc
title: "Squeezing More Juice Out of Your LLM API: Performance Optimizations and How to Leverage Them"
slug: squeezing-more-juice-out-of-your-llm-api-performance
conference: mlops-world-tmls
conference_name: "MLOps World / Toronto Machine Learning Summit"
category: "Practitioner AI conferences"
edition: "MLOps World / TMLS"
year: 2026
speakers: []
channel: null
duration_min: 30
published_at: 2026-08-11T13:09:28Z
video_id: 1ec_kSiAuwc
url: https://www.youtube.com/watch?v=1ec_kSiAuwc
youtube_url: https://www.youtube.com/watch?v=1ec_kSiAuwc
tags: ["machine learning", "artificial intelligence", "data science", "machine learning simplified", "automated machine learning", "developers", "Automated ML", "ml", "machine learning operations", "mlops", "education"]
topics: ["Inference, serving & GPU infra"]
transcript: true
---

# Squeezing More Juice Out of Your LLM API: Performance Optimizations and How to Leverage Them

**Speaker not identified**

`MLOps World / Toronto Machine Learning Summit` · `MLOps World / TMLS` · `2026` · `30 min`

`#machine learning` `#artificial intelligence` `#data science` `#machine learning simplified` `#automated machine learning` `#developers` `#Automated ML` `#ml` `#machine learning operations` `#mlops` `#education`

[Watch the recording](https://www.youtube.com/watch?v=1ec_kSiAuwc) · [Conference site](https://mlopsworld.com/)

## Description

Hagay Lupesko, Senior Vice President of Engineering, Cerebras Systems

About the Speaker:
Hagay is Senior Vice President of AI Inference at Cerebras Systems, where he leads the development of the world’s fastest AI inference service powered by the Cerebras Wafer Scale Engine. He brings over 20 years of experience across software engineering and machine learning, with leadership roles spanning Meta AI, AWS ML, and Databricks Mosaic AI. His work focuses on large-scale AI infrastructure for training and serving state of the art AI models.

Abstract:
Most developers use LLM APIs like a black box: pick a model, send requests, and live with whatever performance they get. This talk argues that this is the wrong way to think about performance. Modern inference stacks implement optimizations such as prompt caching, speculative decoding, disaggregated inference, and more. But for those optimization gains to be maximized, the application needs to use the API in the right way. I will explain the key optimizations that matter, how they work at a high level, and what API users can do to fully benefit from them in practice. The focus is not on theory or provider internals. It is on helping practitioners get better real-world performance from the same LLM APIs.

## Transcript

*4,743 words · source: supa (en, exact timings)*

**[0:09](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=9s)** Uh my name is Hagay. I lead the inference engineering at Cerebras. And today I want to talk to you about how you can squeeze more juice, more performance out of your inference API. And uh just as a note, this whole uh presentation is actually a web application. It's live. You can load it in your phone on fast-inference.ai. You can see on the top. And it was built entirely with OpenAI Codex, powered by the world's fastest inference, the Cerebras uh inference. All right, let's get going. So, first to establish kind of even why it's even interesting, um why is speed, why is performance interesting for AI? Um I put here a small demo. I actually stole it shamelessly from OpenAI's blog page, um comparing two capable models,

**[0:58](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=58s)** GPT, OpenAI GPT 5.3, the uh Codex uh regular version versus the Codex Spark version. That's the version that is running on the Cerebras inference, the Cerebras engine. And both models got the same task. That's building a simple HTML snake game. Hopefully, you all know snake. And let's just put these two models to the task and let's see what difference performance makes. So go. Both models started working, as you can see. And they're doing a bunch of stuff, using tools, writing code. And boom, the fast version is done in just about 7 seconds. And as a developer, at this point, you can go back to the code, iterate on it. The slower version is still working. You know, still building that. It will take it a while. And as a user of that app,

**[1:47](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=107s)** you're stuck kind of waiting for the AI to complete. Now, you may think that, "Hey, you know, these performance optimizations, they're all inside the engine, they're all inside the hardware, there's nothing for you as a user of the inference API, there's nothing to do. But the reality is very different and what I'm going to share with you is based on all of my team's experience supporting many customers using our inference API. While inference providers like Cerebras and others are building the engine, application developers are shaping the workload and performance is actually what happens when the engine and the workload meet. So you can definitely influence the performance greatly. Now starting with what are we really

**[2:35](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=155s)** measuring here for performance? There is a multiple metrics, but a few key metrics here. The first one is TTFT, the time to first token. That's the time from when you send the request until you got the first token back from the API. That point you can actually start render something useful for your user. Second key metric is generation speed. That's tokens per second and that's the rate at which you get back the tokens when you're doing streaming of tokens. And that's how quickly you can actually render stuff for the user beyond just the first response. Then there's the API end-to-end E2E. That's the time from when you send the request until you got the last token. And lastly, application end-to-end. That's the time from when you send the request until you actually completed the

**[3:23](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=203s)** whole workflow for the user. So those are four key metrics. Now how do they actually these metrics look like? First we need to take a step back and look at typical stages of inference. Starting with the left, we have the application responsible for the user experience, managing the state of the application, calling certain tools on the client side, then of course sending requests to the API provider. Then the next stage is connectivity. Because actually from when you send the request to the API provider, that request has to travel through the wire to get to the other side, to get to the provider. And that's the connectivity stage. It includes things like DNS resolution, TCP connection establishment, TLS, and other stuff.

**[4:13](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=253s)** Once the request is received at the provider, there's typically an admission stage by the provider, which either allows the request or rejects it. Things like authentication, authorization, rate limiting, and there's a bunch of other stuff down there. Once the request is admitted, next stage is orchestration. The provider needs to decide where to route this request to. Because typically there are multiple replicas, multiple data centers. Um things like tokenization, the text input has to be tokenized, so it's something the inference engines can actually handle. So that's your orchestration stage. From there, there are two main stages that are pretty compute heavy, typically done on accelerators like Cerebras or on GPUs. Um the first stage is the prompt processing. It's also called the

**[5:00](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=300s)** prefill. That's when the model processes the entire input prompt and generates something called the KV cache or the KV matrices. That's the state of the model, and that enable the last stage, the decode. That's the autoregressive loop where the model the um accelerator uh is processing the KV cache, doing the attention layers, the feedforward network, and other things to generate one token after the other. It's called the autoregressive loop because you generate one token, you feed it into the prompt, you generate the next token, and so on and so forth, until you hit the last token. Those are the stages. Now, how do they actually correlate with the performance metric we discussed? First token coming back lights up the TTFT. Then as more tokens come, you actually

**[5:47](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=347s)** can measure the gen speed. Lastly, last token is when we have the API end-to-end. And of course, the application end-to-end is actually once the application side use case or a task has been completed. These numbers here are of course just uh representative just to kind of highlight what's going on. Um one important thing to note about performance metrics, typically when you're running an application in an application at scale, right, you have thousands, tens of thousands, millions or more of these requests in every given time. So, there are a lot of samples. How do you make sense of these samples? Um you need to statistically sample um P50, median, which will give you a good indication of the typical user experience. That's what, you know, uh P50, so like 50% of the users will see

**[6:37](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=397s)** something worse, 50% will see something uh better. So, that's a typical user experience. Um and then you have the tail, and it's really important to pay attention to the tail. And the tail you can sample the percentile TP99 or 95 or 90, depending on the application. Um both the typical experience, the median, and the tail matter a lot when you measure performance. All right. Now, we'll get to actually the actual levers you can pull to get the API, the inference API provider, to give you the best performance. We'll start with intelligent routing. Um it's important to understand there is a trade-off uh between model quality and speed. Now, it's not always the case, but it's almost always is the case.

**[7:25](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=445s)** Um you can see here in the chart, we took a few models, some open source, some closed frontier models, and plotted them on a chart. The x-axis is the artificial analysis intelligence index. That's a measure, one measure of the quality of the model. What is the model's ability to handle complex tasks. The Y axis is the token generation speed of these models taken This data is taken from artificial analysis, which is doing um uh benchmarking of multiple models and API providers. Now, we can clearly see that trade-off I mentioned. Models that are lower in the intelligence index are very fast, and as the intelligence index grows, um the speed The performance of the model degrades. Now, the real question you want to ask

**[8:13](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=493s)** yourself is what is the quality bar for your AI task? Cuz once you answer that question, you can actually draw a line on this chart, and you don't necessarily have to pick the best model. You can pick the the model that can satisfy the quality requirement of this task. And here I've illustrated kind of this vertical line. That's the quality bar for an arbitrary task, and if you actually pick a model like Deep Seek before Flash, rather than Opus 4.8, you're going to get a model that can handle that task, but will give you way better performance. How better? Again, I took an example comparing for a given coding task, one that is not that complex, let's say a code review task. As a 10,000 input tokens per request,

**[9:02](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=542s)** about 500 output tokens, and let's say that task requires 10 sequential LLM API calls. With GPT-OSS 120B, um hosted on Cerebras, you're going to be done with this task that I mentioned in 19 seconds. On the other hand, if you pick GPT-5.5 from OpenAI, a super capable frontier model, because of the performance, you're going to have to Your users would have to wait almost 14 minutes for this task to complete. That's huge. By choosing the right model, you can get performance that is 40 or more X faster than just picking right, you know, blindly uh the best frontier model. Okay, let's get to that phase I

**[9:52](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=592s)** mentioned earlier of establishing the connection with the uh API provider. I put here a simple chart showing the different stages of establishing a connection with an inference API. Uh when you think about the cold request, and you just created a connection, first your code or your application is going to do DNS lookup. Now, depending on whether you're going to get a a hit or a miss on the DNS, it can be anywhere from 10 ms to 50 ms, sometimes even more, just to resolve the domain name into an IP address. Then, you're going to have to establish TCP connection. I mean, as an application developer, you just call HTTP, but remember HTTP is running on TCP. That requires at least one round trip to establish that connection. Again, 20 to 80 ms, sometimes more.

**[10:41](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=641s)** After that, you're going to have to do TLS negotiation. That ensures your uh connection into the API provider is secured and is encrypted. Um another round trip or more, depending on the TLS version. Another 20 to 80 or more ms. And last but not least, you have a payload you need to send to that other side. Now, depending on the size of the payload, that can actually take quite a bit of time. In particular, if you're using multimodal APIs, you're sending image data or videos or PDFs, those things can be heavy, and it's going to take time to upload that data to the other side. So, what can we do about it? Very simply, you want to make sure you

**[11:32](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=692s)** create only one long-lived client for every process you're running. I've seen so many times our customers and other developers make the mistake of creating a client and then terminating it for every LLM call. If you're going to do that, you're going to pay that massive tax I mentioned of establishing the connection. When a process starts, you want to create that connection to the LLM provider. And you don't take that down until the process goes down. That allows you to reuse. You don't have to do these all these connection things every time. The other advice is use HTTP/2. If it's supported by your API provider, Cerebra supports it, other leading API providers supports it. HTTP/2 gives you multiplexing, it gives you binary

**[12:21](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=741s)** framing, give you header compression. Everything is faster and better with HTTP/2. So, use it. It's simple two lines of code as you can see here. This is an example of using the OpenAI Python API. Use HTTP/2 where you can. Second is that let's talk about that payload, right? Which I said as I said can be very heavy. Just to illustrate how heavy, if you take an example of again coding application, coding are super popular with LLMs of course. About a 30,000 tokens input, which is you know, a small or modest code base, will take about 123 kilobyte of payload data. That's a lot. That's a lot if you're sending it over the wire, especially if

**[13:08](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=788s)** you're sending it from multiple locations across the globe. What can you do about it? Encode it efficiently with something like MessagePack. MessagePack is an open source library for encoding binary encoding of JSON payload. And gzip it. I know it sounds trivial, but gzip is super effective. With message pack and gzip, you're going to cut your data down by 98% from 123 kilobyte down to 2 kilobyte in this example. Now, the effect this is going to have on performance is pretty significant because when you send smaller payload, it's going to travel over the over the wire way faster with less retries. And in benchmarks we've ran with some of our customers, we've seen that you can cut the median TTFB 32% for these kind

**[13:57](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=837s)** of use cases and you get an end-to-end experience that is 24% faster. Just by doing this simple thing, there is a code example here using message pack and gzip. Just make sure you set the request headers properly and you're good to go. All right, moving to the third lever you can pull. Um As you see here in this illustration, as you iterate more and more with your agentic application calling an LM provider, you tend to accumulate more and more context in your prompt. Maybe the first request only has a system prompt and instruction and maybe a file. And then the second iteration with the user, you're adding another file, you're adding a bunch of tools, you're adding maybe some context, maybe a JSON schema.

**[14:47](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=887s)** You'd be surprised how quickly these things pile up. And the negative effect of this, as you can see in the chart here, is that it's going to increase your time to first token. As your context grows, your time to first token is going to grow with it. So, you want to be really careful about that. What can you do about it? Continuously curate the prompt that you're sending. Summarize prior interactions, deduplicate items. You'd be surprised how many duplicate entries you're going to have in your prompt, and trim stuff that doesn't matter anymore. I can tell you that all the leading agentic coding assistants are doing it for you. Sometimes you will you see them saying, "Hey, we're compacting the context." That's exactly what they do. You want to do the same in your application. And then in the chart here you can see your TTFT will increase a

**[15:35](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=935s)** little bit, but then as you trim it it will go back down. Another important optimization is about reusing. Now, to explain that I want to give you a quick overview of a super important optimization supported by virtually any leading inference provider. It's called prompt caching. Prompt caching is really revolutionary in terms of the performance boost it give you. Now, just to for those that may not be familiar with it there is an illustration here. Look at the original prompt. Think of these shapes as tokens. Imagine you send that request, it was processed by the service and then the service also cache this request. Now, it doesn't actually cache the request itself. What it caches is

**[16:23](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=983s)** the KV matrices that I mentioned earlier that are generated during the prefill stage. Now, it's cached on the provider. Next request sent, let's look at the green request. You can see that all the shapes are matching up until the last two. This is what we call a cache hit because the prefix of this request, large part of it matches the original prompt. So, rather than recomputing these whole tokens, what the service is going to do is going to just pull these KV matrices from the cache, feed them in, and prefill is going to be dramatically faster. You're going to also pay less money by the way in this case because the service is doing less work for you. This is going to result in lower TTFT, lower cost. On the other hand, the cache miss, like the example on the bottom,

**[17:11](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=1031s)** you can see the first token is a triangle different than the first token on the original prompt, which is a circle. So, there's a cache miss here. What happens? The service has to recompute the entire prompt. It's going to take more time, higher TTFT, and also higher cost. So, how can you leverage this optimization to make sure you get get get great performance? Very simply, you want to optimize for as high of a cache hit rate as you can. Cache hit rate, very simple, it's the number of cache tokens divided by the total number of tokens. Um so, how do you optimize for cache hit rate? You put all the static content first in your prompt. It's really important. Things like reusable instruction, a system prompt,

**[18:00](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=1080s)** tools, schemas, all that stuff has to come first and be in the same order. Second, when you have um when you have um content that is changing, such as timestamps or request IDs, uh or if you have to reorder tools for some reason, you put it last. You don't put it first. What this is going to give you is you're going to give you a pretty high cache hit rate. If you make sure you do it well. So, if in your application you have like a prompt template, make sure to put all the reusable stuff first, and then everything that changes last. It will give you higher uh cache hit rate. Um very importantly, always monitor your cache hit rate. Every response of an LLM provider gives you the number of cache

**[18:48](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=1128s)** tokens, non-cache tokens. You can just calculate a metric, store it in your observability store, and always monitor it. And most services like Cerebras also allow you to see that metric in your console. Now, prompt caching, if you leverage it well, if you have a high cache hit rate of say 85% or higher, you're going to reduce your time to first token by up to 80%. All right. Last but not least, optimizing the generation phase. So, if you look at generation time, it's a very simple formula. It's the total number of generated tokens divided by the generation speed. The lower the number of total generated tokens, generation time will be lower. The higher generation speed,

**[19:35](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=1175s)** uh generation time will be lower. Let's start talking about generation speed. It might be surprising, but you can actually influence generation speed quite dramatically based on the shape of your workload. Now, to explain why that is the case, I want to again take a pause and explain one important optimization every leading inference API provider like Cerebras implements under the hood. It's called speculative decoding. The idea is pretty simple. Rather than having just one large target model handling a request, we're actually putting in front of it a small, nimble, super-fast model we call the draft model. When a request comes in, rather than directing it to the large model, we give it to the small model that is super-fast. And that small model races

**[20:22](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=1222s)** ahead, generating a bunch of output tokens auto-regressively, and passes those tokens as we call them draft candidates to the target model. And the target model takes all these inputs and verifies them. It verifies that the draft model answered correctly. In this example here, the draft generated four tokens. The target model verified correct verified all of them. Three of them were correct, so it keeps them. And the last one was incorrect, so the target model would generate that output token by itself. Now, the fact that the target model can verify and do it in parallel speeds up generation significantly. How significantly? You can get up to 4x speed up by using speculative decoding. Of

**[21:10](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=1270s)** course, it depends on the algorithm, the model, the workload, but we've seen massive boosts with speculative decoding. We deployed across all of our models. And important to remember, the higher accepted tokens per step, the more the draft is correct, the higher the generation speed. Also important to note, I've explained this optimization, but actually you don't need to do anything to use it. It's on by default on most leading providers. Now, the key question is, okay, how can you leverage that optimization? How can you influence it, so you're maxing out the benefits? A couple of ways you can do it. First one, choose a lower temperature. Temperature, as a reminder, that's a sampling parameter supported by virtually any inference API provider. What it does, it rescales the

**[21:59](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=1319s)** next token probabilities after the softmax operation, to put things simply, lower value of temperature makes sure likely tokens are more dominant. The model is less creative, it's more giving kind of straight answers. Higher values makes the model very creative, allow more variation, and is less easy to predict. Now, what this means for us, in our case, lower temperature means higher predictability. Higher predictability means the draft model is going to be right more often. And then, draft model right more often, gen speed increases. How much? I pulled some data running an experiment, GPT-OSS 120B open eyes open model running on Cerebras inference.

**[22:48](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=1368s)** You can see that by going from temperature of 2.0, which is fairly high, down to temperature of zero, the gen speed was increased by 1.5X. So again, that's pretty significant. The takeaway, unless you really need the model to be creative, which I'd argue is not a very likely case, choose a lower temperature, it will improve your generation speed. The other optimization that leverage that is called predicted outputs. It's a feature supported by advanced L and M APIs like OpenAI supports it, Cerebras supports it. It's an argument you can use in your request called prediction that allows you to specify the part of the

**[23:36](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=1416s)** response that you kind of already know. Now, to explain what this is, we can look at the code example on the right. Classical example for that, you have a HTML code and you want to change something very specific like the color of a button or the position of some element. You know, the model doesn't have to change almost anything at the HTML, just a couple of specific things to get job right. So, that's a classical usage where you can pass this HTML code as content to the prediction argument. Um and the model will handle that much faster because it already knows most of the answer. Uh when to use it, use it when you're editing a file with uh pretty small difference. Use it when you're revising a document, but you know

**[24:23](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=1463s)** most of the document is going to stay the same. When we're going to regenerate uh a mostly known artifact. How much faster is it going to be? When you use predicted predicted outputs, it's going to speed up generation by 3x. Of course, that depends on the actual task, but that's an average acceleration you can expect out of using that capability. All right, let's look at the other side of our formula. How can we reduce the number of total generated tokens? Of course, while maintaining same quality of the model Couple of tips here. First of all, structured outputs. Structured output again is a API parameter. Almost any leading API inference API provider supports. It allows you to specify a

**[25:11](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=1511s)** JSON schema that constrains the response to a predefined JSON schema. Um As an example for this use case, extraction. You have a task to take an order in free text or in a document, and you want to extract key elements out of that, say order form or email. Order ID, customer name, you know, total US dollars, etc. It's a pretty standard task for LLMs. This would be the output with and without structured outputs. On the left, you can see no structured output. You're going to get raw text and some prose at it, where the model is telling you what it's doing. And you have to actually parse that to get the data out of it, which by itself is not trivial. On the right, you have the JSON schema. So,

**[25:59](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=1559s)** it's very ordered. There's no prose. There's no extra tokens. Super structured. The difference in tokens is significant. 75 tokens on the left versus 45 tokens on the right. If you do more complex extraction task, that difference is going to be even more meaningful. So, with structured outputs, if you benchmark it, you see that you can get about 50 40% fewer tokens generated. You also get the benefits of we don't have to actually verify the schema that is returned because it's guaranteed to be generated that way by the inference provider. And the other benefit is that it's a stable contract. Even if you upgrade the model, you move from GPT 5.5 to 5.6, whatever, you don't have to re-verify that this logic works because it's built into the

**[26:48](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=1608s)** sampling algorithm. With 40% fewer tokens generated, you can expect um token generation time to be uh 40% faster. Second is about reasoning. Reasoning is an incredible capability of models. It allows models to really solve super hard problems. Um of course, everybody's talking about it all the time. However, there's also a cost associated with reasoning. Reasoning generates way more tokens, and I'll I'll show you how many more, but uh maybe to start um for reasoning models, uh you can actually specify an argument in the API, reasoning effort, that controls how much reasoning is the model going to going to do when it's generating the output. There are different values, but almost all

**[27:35](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=1655s)** providers support low, medium, and high. All right, so low generate fewer reasoning tokens, and you can expect, of course, speed to be higher. You can use it for answering questions that mostly exist already in the input, things like extraction routing formatting. Medium means there'll be more reasoning, um and then high means there's going to be a hell lot of reasoning, and you want to use that specifically for things like planning debugging mathematical proofs, and complex tool use. Uh I pulled some data, again using GPT-OSS on Cerebras Inference, uh just to measure how much more reasoning tokens are generated. Baseline is reasoning low, so that's our 1x baseline. You can see that medium

**[28:24](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=1704s)** generated 1.8x the number of output tokens, and high generated almost 3.7x the number of tokens. That is directly going to impact your time to first token uh and your total generation speed, because there's so many more tokens being generated. So, what's the takeaway? Use reasoning effort very carefully. Remember that it's not for free. It can cost you almost 4x more of the tokens. It's going to cost you more price and it's going to cost you a lot of performance both CFT and API end to end. Choose the appropriate effort for the task to boost the performance. All right, we'll recap. We spoke about four main levers you can pull. First, choose the fastest model that clears the task quality bar.

**[29:12](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=1752s)** Second, you want to reuse connections and shrink every payload that you're sending. Third, create the context so you're not sending too much and preserve the cacheable prefixes. And then last, you want to optimize generation by making outputs as predictable as you can and also bounding the tokens generated in reasoning. The takeaways, while you do not control the provider's inference engine, you can definitely shape the workload to let it perform at its best. Thank you for your time and if you're want to see this deck, and like I said, I built it with Codex on Cerebras. It's live at fast-inference.ai. Feel free to use it, share it and hopefully you'll get something out of it and I'm always happy to connect and

**[30:00](https://www.youtube.com/watch?v=1ec_kSiAuwc&t=1800s)** chat. My LinkedIn profile is here. You can chat here or later. I just hope this was useful for you.

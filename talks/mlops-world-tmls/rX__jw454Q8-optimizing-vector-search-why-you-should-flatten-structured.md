---
id: rX__jw454Q8
title: "Optimizing Vector Search: Why You Should Flatten Structured Data"
slug: optimizing-vector-search-why-you-should-flatten-structured
conference: mlops-world-tmls
conference_name: "MLOps World / Toronto Machine Learning Summit"
category: "AI engineering & agents"
edition: "MLOps World / TMLS"
year: 2026
speakers: []
channel: "Toronto Machine Learning Society (TMLS)"
duration_min: 20
published_at: 2026-08-11T13:10:08Z
video_id: rX__jw454Q8
url: https://www.youtube.com/watch?v=rX__jw454Q8
youtube_url: https://www.youtube.com/watch?v=rX__jw454Q8
tags: ["machine learning", "artificial intelligence", "data science", "machine learning simplified", "automated machine learning", "developers", "Automated ML", "ml", "machine learning operations", "mlops", "education"]
topics: ["Inference, serving & GPU infra", "RAG, retrieval & knowledge", "Training, fine-tuning & model building"]
transcript: true
---

# Optimizing Vector Search: Why You Should Flatten Structured Data

**Speaker not identified**

`MLOps World / Toronto Machine Learning Summit` · `MLOps World / TMLS` · `2026` · `20 min`

`#machine learning` `#artificial intelligence` `#data science` `#machine learning simplified` `#automated machine learning` `#developers` `#Automated ML` `#ml` `#machine learning operations` `#mlops` `#education`

[Watch the recording](https://www.youtube.com/watch?v=rX__jw454Q8) · [Conference site](https://mlopsworld.com/)

## Description

Oleg Tereshin, Senior Software Engineer, Independent Software Engineer

About the Speaker:
I am a Senior Software Engineer with 12 years of experience, specializing in AI Infrastructure and Semantic Search. I am currently building a semantic search platform, managing high-throughput embedding pipelines, and orchestrating vector databases on Kubernetes. As an author on Towards Data Science, I share practical, empirical strategies for vector search optimization.

Abstract:
When integrating structured data into a RAG system, engineers often default to embedding raw JSON into a vector database. The reality, however, is that this intuitive approach leads to dramatically poor retrieval performance. Modern embeddings leverage BERT architectures optimized for natural language, which struggle with the high frequency of non-alphanumeric characters found in JSON syntax.
In this session, I will break down the exact failures of embedding structured data—from tokenization and attention mechanism disruption to the mathematical liability of Mean Pooling on syntax tokens. I will then demonstrate a practical, production-ready solution: implementing a simple preprocessing step to convert structured JSON into natural language templates. Backed by empirical testing on the Amazon ESCI dataset, I will show how this straightforward architectural shift natively boosts Recall@10 by over 19% and MRR by 27%.
Note: This article was recently featured as a top weekly article on Towards Data Science, reaching over 9,000 views.

## Transcript

*2,996 words · source: supa (en, exact timings)*

**[0:08](https://www.youtube.com/watch?v=rX__jw454Q8&t=8s)** Today we're going to talk about optimizing vector search and why you should flatten structured data. So, just a quick jump to the agenda. We're going to define the problem, then discuss why embedding models struggle with a JSON, uh then discuss the solution, which is relatively simple, uh flattening, and then wrap up with experiment and results. So, jumping right into it. The problem is a lot of data stored in structured format as a JSON, but it can be XML or anything else you have. Uh it's naturally compelling to embed this data in the same format it is stored to use it for semantic search. So, when you get a request that like, "Let's make this data searchable." They like the first instinct is basically just let's load this data,

**[0:56](https://www.youtube.com/watch?v=rX__jw454Q8&t=56s)** uh embed it through embedding model, and just drop into vector database, and then it will be searchable right away. Although it will work, but the results of embedding structured data are far from optimal. So, as you can see on this diagram, if embed even a simple JSON, uh it will end up very far from related queries in uh semantic uh in a vector space, which is like again, it will it will work, but it won't be most optimal solution. So, in terms of modern embedding models, they based on a BERT architecture, and uh so basically process text as a sequence of tokens, building contextual representation through attention layers. They trained on a large data set of unstructured data. So, basically pre-trained on billions of sentences from books,

**[1:44](https://www.youtube.com/watch?v=rX__jw454Q8&t=104s)** Wikipedia, web pages, and most of it is written in natural language. Uh they also trained to capture semantic meaning. So, they learned that cheap and inexpensive are related, that price connects to cost, and so on. And they're providing credible retrieval performance, but only when the input matches what they were trained on. So, why isn't embedding JSON is not great for retrieval? Uh let's take a look at the main main steps of embedding generation. The first one is the tokenization. So, basically text takes the text and split it into tokens. Uh the second one is attention calculation. Calculation it's a core power of transformers. It allows the model to weight the importance of tokens relative to each other.

**[2:32](https://www.youtube.com/watch?v=rX__jw454Q8&t=152s)** And the last one is the mean pooling. So, it's a final step in generating a single embedding representation of the document. So, let's talk of them one by one. We can start with the tokenization. Two main algorithms are BPE and a word piece that are used to split text on tokens. The algorithms are optimized for the natural language and breaking words into common subcomponents. And they struggle with a high frequency non-alphanumeric characters which are used extensively in a JSON. So, if you take a look at the example I have there is a simple JSON with a price and different currencies. And below that the diagram of that I taken in OpenAI tokenizer. So, basically as you can see up to 50% of tokens are noise that contains zero semantic value.

**[3:21](https://www.youtube.com/watch?v=rX__jw454Q8&t=201s)** It's a brackets quotes, commas, and so on. Now, talking about attention. So, let's take a look at attention in natural language. If we flat the similar data structure and say the price is 10 US dollars or 9 euros attention can easily link the value 10 to the concept of price just because these relationships are well represented in the model pre-trained data and model has seen it probably millions if not billions of times. So, it's clearly understand that like this sentence is about price with a specific values and so on. If we take a look at the attention in raw JSON, so like the model encounters structural syntax that was not primarily optimized to read.

**[4:08](https://www.youtube.com/watch?v=rX__jw454Q8&t=248s)** So, without linguistic connectors, the resulting vector will fail to capture the true intent of the data and as the relationships between the key and the value are obscured by format itself. So, as you can see like intended target for is price, but there's a lot of structural noise that all kind of can affect the results of attention. And the last step is the mean pooling. So basically we have pair token vectors which encapsulates the token value itself plus the relation to all other tokens in the in the document. So, the final embedding basically is a centroid of all of these token vectors. Some of them and divide them and each dimension will basically shows the

**[4:55](https://www.youtube.com/watch?v=rX__jw454Q8&t=295s)** arithmetic mean of the dimension across all token embeddings. As you can imagine, if it's a if there's a lot of semantic semantic noise as we say brackets, quotes, commas which don't bring any semantic value, the document the final vector will kind of drift apart from its true center and as we can see here, 25% of tokens as structural marker markers and document drifts away from the query which is directly harming retrieval. In this example, we have the price, currency and so on. Like all these vectors being in like same cluster. The query being relatively close to the cluster and expected position is basically to be in somewhere nearby

**[5:43](https://www.youtube.com/watch?v=rX__jw454Q8&t=343s)** of the encoded document. But in reality, we have a bunch of uh semantic noise tokens, right? Related to the JSON format itself, which are pulling the like the final vectors away from its expecting position. And uh it again may work, depends on your uh threshold on semantic search, but it's definitely increase uh the distance between a query and a document. So, it's either won't be in your top five, top 10, top one, if will be at all. So, problem is clear. What's the solution here? Uh flattening. This is like uh relatively straightforward uh step we can add to the uh pipeline uh ingestion pipeline. So, generally it

**[6:32](https://www.youtube.com/watch?v=rX__jw454Q8&t=392s)** looks like we have the observed request with the data [clears throat] we want to uh put into vector database, and uh we just embed it and ingest into vector database. After that, it's available, it's indexed, we can search. Uh what the proposal is to add the new step right before embedding generation, which will flat the flatten the data and convert it to natural language that model will show better results on. After that, it should be ingested into vector database and used. So, let's consider the typical product object. This is simple object with some attributes like description, quantity, uh gift cards available, category, and so on. Uh to if I apply tokenizer to it, uh it will look like we'll notice a high

**[7:20](https://www.youtube.com/watch?v=rX__jw454Q8&t=440s)** density of distinct tokens uh related to syntax. Again, braces, quotes, colon, that basically contribute to noise rather than meaning. And uh we can apply the flattening template here. So, the solution is like we define template, substitute JSON values into it. Something easy as like available discounts, I include gift cards available, not available. It has quantity of whatever number is available and we'll get more or less uh natural language document with which will make it much easier for the model to understand. And if I apply tokenization to it, uh the result the resulting sequence is shorter. It's like 14% fewer tokens. And composed primarily of semantic

**[8:07](https://www.youtube.com/watch?v=rX__jw454Q8&t=487s)** meaningful word words as you can see. There's not much of a uh noise here basically. And also want to point it's fewer tokens as all more characters. Uh So, for experiments that I did, uh I measured the recall precision and MRR. I use a Omnila model which is like relatively small. It has three uh 384 dimension. But it's good enough for experiments and allowed to do it uh to do them quickly. For data set I used Amazon ESCI data set with a random 5,000 queries and uh less than 4,000 associated products. And uh spin up two flat files indexes. Uh one for flat index and one for JSON formatted text. I specifically use a flat uh indexes

**[8:56](https://www.youtube.com/watch?v=rX__jw454Q8&t=536s)** to remove any uh any effect of approximate nearest uh neighbor index on the results to make them as clear as possible. So, here on the diagram you can see how uh how the experiment uh look like. So, we have the raw product JSON. For the JSON pass, we basically embed whatever raw JSON there is and then put it into files flat index. And the parallel pipeline is we flatten the uh uh product JSONs, then embed them, and then put into files flat index. Uh flattening function it was relatively simple. It's just saying like a product ID description, from which brand, and so on. Uh this is just uh below that is just an example of one of the products. And uh for results, uh the flattening

**[9:47](https://www.youtube.com/watch?v=rX__jw454Q8&t=587s)** approach consistently yields higher scores across all key retrieval metrics as you can see here. Uh going a bit deeper on the results, so converting the structured JSON to natural language text resulted in significant gains. Uh we've seen about 90% boost on recall at 10, 27% boost in MRR, and uh 19.7% boost on precision at 10. So, as demonstrated, it is like a very useful uh step to add to your pipeline and uh to improve your retrieval uh quality. In terms of takeaways, I would like you to to focus on a few things. So, embedding models expect natural language. They're trained on books, web text, uh and raw JSON is structurally foreign

**[10:35](https://www.youtube.com/watch?v=rX__jw454Q8&t=635s)** to them. Uh structural tokens are silent killer. So, as we've seen, 50% of your context window are just like doesn't have any semantic value. Uh your future centroid for the result vector will drift away from where you would expect it to be. And flattening is basically a free lunch. It's just uh one template function. Uh you get a very significant uh boost in recall, MRR, and precision. So, call to action is check if you're embedding raw JSON. If yes, write a template and try to measure the improvements. Uh it should show you much better results. With that, uh thank you very much for attending. Uh I linked my LinkedIn original Towards Data Science article you can take a look, and the

**[11:24](https://www.youtube.com/watch?v=rX__jw454Q8&t=684s)** experiment is available on GitHub. Uh yep, happy to answer any questions. >> Thank you. Thank you so much, uh Oleg. And I do have like two questions over here. So, the first one goes as, "What does BERT stand for?" >> Uh BERT stand for uh honestly, I don't remember the the full wording on top of my mind, but the idea is that it's basically stands for like transformer based embeddings. >> Okay, I hope that answers the question. And the second question, is there a good way to automatically generate flatten the JSON? >> Uh it's like I don't think there's a good answer. So, it's really depends on your original

**[12:12](https://www.youtube.com/watch?v=rX__jw454Q8&t=732s)** data. If it's relatively straightforward, not versioned, not often changes, then it's totally like it should be very simple. Uh but I'm not aware of many uh standard solutions there. The problem can be if your data is like versioned, if you have a lot of uh different uh structures, so you need to kind of organize templates and maps into original data and so on. Uh this case, I think you may need to take a look into having multiple indexes per kind of data type or category or whatever else is there still kind of simplified to the point when you can map it to maybe one or two templates or very uh relatively easy. And then you can always use a kind of merge of results

**[13:00](https://www.youtube.com/watch?v=rX__jw454Q8&t=780s)** uh after the retrieval itself. So, that probably will will simplify the pipeline uh by quite a lot. >> Perfect. I do have like two more questions if you're ready for that time. Do you colon, commas, and apostrophes uh apostrophes carry meaning in natural language? So, why don't the embedding algorithms account for it? >> Uh they do have a meaning, and it's like totally about like it's a actually great question. So, they do have meaning. The problem is uh, the JSON use them as a structure not a similar way as a natural language do. And uh, again as I say if model was trained on most of the data presented in natural language, it has a perfect understanding of like a

**[13:48](https://www.youtube.com/watch?v=rX__jw454Q8&t=828s)** commas, brackets and so on. But the JSON kind of use it for a structural not like the same way. So, even to find the connections of the keys related to value is is already kind of a struggle after the tokens are there. And the second part is as I showed even for simple JSONs the number like the amount of uh, uh, noisy tokens uh, caused by the JSON format is like too high. If you would have like relatively small number of it, it's it should not cause such uh, long drift. And again, it may have some meaning. But in in JSON there's like not much meaning and it's like just a too much of pulling the vector from like result vector from where it should be.

**[14:37](https://www.youtube.com/watch?v=rX__jw454Q8&t=877s)** >> Thank you. Thank you. And one more question. Uh, any libs or approaches available for flattening the data that is unknown up front aka universal template that fits any JSON in order to avoid writing a template? >> Uh, I'm not aware of any like public uh, such flattening engines and so on. Uh, I would expect a lot of companies doing it uh, kind of internal implementations. I've heard about approaches with using uh, LLM for that, but you need to understand that it's kind of increasing costs uh, significantly and uh, kind of may produce a little bit different results just because it's non-deterministic. Uh, but LLM uh, for flattening is kind of a known approach.

**[15:25](https://www.youtube.com/watch?v=rX__jw454Q8&t=925s)** But again, as I as I mentioned, uh take a look if you actually need to bring all this complexity uh with like a multiple uh different formats and so on. If not, you can try to like If yes, you can try to use multiple indexes and then just kind of merge results uh retrieve results from them. You can do it in parallel, it should be faster. And eventually you might save on uh LLM calls, but again, it depends. If you doing like a a few thousands of uh ingestions per day or months or whatever, then LLM is probably good enough. If it's like if you're talking about hundreds of millions, uh then the cost will be a big factor there. >> Perfect. Thank you so much. And uh we do have like two more questions if you're

**[16:13](https://www.youtube.com/watch?v=rX__jw454Q8&t=973s)** ready to go. Yeah, can you show the slide before attention in raw JSON uh where I believe you were showing uh we do not have same problems of noisy tokens. Why is there no issue in this case? >> Uh can you say it again? Sorry. >> Sure. So, um can you show the slide before attention in raw JSON where I believe you are showing we do not have same problem of noisy tokens. Why is there no issue in this case? >> Sure. Yeah, that's a actually a great question. Thanks for asking it. Uh so I think we need to kind of keep an eye on both of these ones, right? Uh as I say, the model was trained on the natural language. So uh that attention weights a part of the model training. So, they're not kind of

**[17:02](https://www.youtube.com/watch?v=rX__jw454Q8&t=1022s)** hard coded anywhere, etc. It just basically uh encapsulate what model was trained on and what data it's seen. And uh in this case scenario, the kind of the idea is the price is whatever 10 US dollars or 9 euros. The whole kind of linguistic model, the model seen probably millions of times if not billions of times in one form or another. So, figuring out how much weight and uh attention should pay between different tokens in this structure is relatively straightforward for the model. If we look here, this is basically like even for people, you have to kind of discourage half of the uh tokens just to say, "Oh, yes, price is USD and related to 10 and euro and so on." And if we put

**[17:50](https://www.youtube.com/watch?v=rX__jw454Q8&t=1070s)** like more complex JSON here, uh even for people, it will take some time to kind of figure out what's the exact uh what's the exact structure we're talking about. So, basically, as I say, on attention level, it's just hard for the attention mechanism itself to to define the order and figure out that oh, this specific key related to this specific value. And for example, uh related is related to this specific nested structure and so on. >> Perfect. Thank you so much, Oleg. So, one last question. Uh I wonder if the flattening isn't effective for more reasoned LLM-based embedding models like OpenAI's text LLM 3 large. >> Uh

**[18:37](https://www.youtube.com/watch?v=rX__jw454Q8&t=1117s)** I think it's all very kind of it's all depends on like what data you have, how complex your like JSON are you like maybe you're doing that XML, whatever else is there. And uh the best advice is probably test it. Uh it shouldn't be very complex. You can take like a small amount of data and run an experiment and and measure it. Uh in terms of efficiency, I think all these kind of fundamental rules with like attentions, the mean pooling, the tokenizer are still valid for all modern embedding models. So, I would expect it to be kind of the same problem. Maybe in a little bit less uh uh reduction of retrieval metrics, uh but again, it probably depends on like specific data and specific model.

**[19:30](https://www.youtube.com/watch?v=rX__jw454Q8&t=1170s)** >> [music]

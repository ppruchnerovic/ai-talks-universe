---
id: BG_nB5dolKs
title: "Scaling Production-Grade LLMs: Diagnosing Hidden Bottlenecks in Training and Inference Systems"
slug: scaling-production-grade-llms-diagnosing-hidden-bottlenecks
conference: mlops-world-tmls
conference_name: "MLOps World / Toronto Machine Learning Summit"
category: "Practitioner AI conferences"
edition: "MLOps World / TMLS"
year: 2026
speakers: []
channel: "Toronto Machine Learning Society (TMLS)"
duration_min: 25
published_at: 2026-08-11T13:09:26Z
video_id: BG_nB5dolKs
url: https://www.youtube.com/watch?v=BG_nB5dolKs
youtube_url: https://www.youtube.com/watch?v=BG_nB5dolKs
tags: ["machine learning", "artificial intelligence", "data science", "machine learning simplified", "automated machine learning", "developers", "Automated ML", "ml", "machine learning operations", "mlops", "education"]
topics: ["Agents & orchestration", "Enterprise adoption & strategy", "Evals, observability & reliability", "Inference, serving & GPU infra", "Training, fine-tuning & model building"]
transcript: true
---

# Scaling Production-Grade LLMs: Diagnosing Hidden Bottlenecks in Training and Inference Systems

**Speaker not identified**

`MLOps World / Toronto Machine Learning Summit` · `MLOps World / TMLS` · `2026` · `25 min`

`#machine learning` `#artificial intelligence` `#data science` `#machine learning simplified` `#automated machine learning` `#developers` `#Automated ML` `#ml` `#machine learning operations` `#mlops` `#education`

[Watch the recording](https://www.youtube.com/watch?v=BG_nB5dolKs) · [Conference site](https://mlopsworld.com/)

## Description

Deepkamal Gill, Senior AI/ML Scientist, The Vanguard Group
Mehul Soni, Senior AI Research Engineer, Enterprise AI Research, The Vanguard Group

About the Speaker:
Deepkamal Kaur Gill is a Senior Applied AI Scientist at Vanguard, where she builds production-grade LLM systems for high-stakes financial applications. Her work spans data generation, post-training, and evaluation, with a focus on building reliable, low-latency AI systems under real-world constraints.

Deepkamal holds a Master’s in Computer Science from the University of Toronto and is an active contributor to the AI community through research, mentorship, and initiatives supporting women in technology. At TMLS, she brings a practitioner’s perspective on what it truly takes to scale LLMs in production.

Mehul is a Senior AI Engineer at Vanguard, specializing in building enterprise-scale LLM and agentic AI systems that bridge applied research and production impact. She brings over five years of industry experience applying AI/ML techniques to solve complex business problems. Her work spans LLM post-training, multi-agent systems, evaluation frameworks, and AI systems engineering, with a strong emphasis on translating cutting-edge research into scalable, production-ready solutions. Mehul is actively engaged in AI and professional communities, contributing to initiatives that promote mentorship and inclusive growth, such as Women in Data Science.

Abstract:
While recent advances in LLMs emphasize improved model capabilities, many systems fail to scale in real-world production settings. Beyond a certain point, adding GPUs or data yields diminishing returns: training stops scaling efficiently, hardware remains underutilized, and inference latency is dominated by system constraints rather than compute. These failures are often silent, poorly documented, and difficult to diagnose in distributed environments.

In this talk, we share lessons from building enterprise-scale domain LLM systems, focusing on the system-level bottlenecks that limit scaling in practice. We examine failure modes across distributed training and inference—including communication overhead, pipeline imbalance, numerical instability during training as well as memory-bound decoding, KV cache growth, and throughput–latency tradeoffs at inference—and show how they manifest in production systems.

Rather than introducing new modeling techniques, this session presents a practical, symptom-driven approach to debugging: identifying failure patterns, tracing their root causes, and applying targeted mitigations. The key takeaway is that scaling LLMs is fundamentally a systems problem, and attendees will leave with a concrete framework to diagnose bottlenecks and make better design decisions when moving from prototype to production.

## Transcript

*4,110 words · source: supa (en, exact timings)*

**[0:09](https://www.youtube.com/watch?v=BG_nB5dolKs&t=9s)** So today we'll be talking about some of the practical lessons that we learned from scaling uh LLM systems in production. And for those of you who attended Ian's keynote yesterday, this is going to feel like a good follow-up to that where he discussed uh some of the frameworks for distributed training and inference like Ray, SG Lang, and VLM. And while we won't be talking about those specific frameworks, but we'll be talking about more general principles underlying those frameworks and what are the bottlenecks you can face while uh using those and how to resolve those. Uh just a very brief intro. So uh I'm Deep Kamill. I'm a AI research scientist at Vanguard and my work focuses on domain adaptation of LLMs including synthetic data generation, post- trainining using distributed

**[0:56](https://www.youtube.com/watch?v=BG_nB5dolKs&t=56s)** methods and systematic model evaluations and I'm joined by my colleague Mahul. >> Hi everyone. Uh this is Mihul. I'm a senior AI research engineer at Vanguard and I primarily focus on building domain specific LLM, agentic systems and AI engineering at production scale at Vanguard. So whatever examples we'll be sharing today are actually lessons that we have learned the hard way. So to give you a bit of a context about the problem space. So at Vanguard we have more than 20,000 employees globally and we serve more than 50 million investors. So for Genai systems that means that not only do we need to build systems that are uh intelligent but also systems that can scale at that level. And we really care about the reliability of those systems

**[1:44](https://www.youtube.com/watch?v=BG_nB5dolKs&t=104s)** in production and how u we can make them more trustworthy. So the mission on this slide is important here which is giving investors the best chance for investment success and for AI systems that translates into not only just adding large larger models or bigger compute but actually ensuring that those systems scale in production. So to do that well we need a discipline bridge between research productization and production scale and that is where our team enterprise research vision comes in where we explore cutting edge research technologies and translate those into enterprise scale capabilities that are dependable and yeah let's get started. So the

**[2:32](https://www.youtube.com/watch?v=BG_nB5dolKs&t=152s)** agenda that we have today is intentionally organized as a debugging journey. We start with a hardware underutilization because that's often the first surprise that most teams experience. Then we move into memory bottlenecks because when we try to scale systems for training and inference, memory becomes just as important as compute. Then we look at some of the optimization regressions. This is where the system scales faster but the model quality worsens. Then we look at some of the long context issues. Uh long context is very important from a product perspective but technically it actually surfaces a lot of underlying bottlenecks in inference serving systems. Finally we look atuling bottlenecks. This is where the overall system throughput looks healthy but we still have users that

**[3:20](https://www.youtube.com/watch?v=BG_nB5dolKs&t=200s)** experience streaming issues or uh slowness in the system. So the goal today is not to memorize a list of optimizations but for us to build a mental model and a debugging playbook that we can use to scale L&M systems. So let's start with a simple situation uh that's most familiar to us. We start with a single GPU. We increase the number of GPUs because we want to get faster training, larger models and lower latency and everything works fine. Not really. So when we think about real production systems when we actually add more GPUs we start encountering a lot of different issues. We get out of memory we get system crashes performance degrades and so on. So what this means is that adding GPUs does not directly

**[4:07](https://www.youtube.com/watch?v=BG_nB5dolKs&t=247s)** translate to linear scaling and training and inference bottlenecks can be completely different. So production system can often expose such hidden failures and we need to view both distributed training and inference as a systems problem rather than looking at these individual isolated components. So when we think about GPUs, we think that that's all there is. But the actual uh part starts at the data pipeline stage where the data sets are stored, tokenized and processed and then those data sets are passed on to the CPU host which is essentially responsible for orchestrating the entire training process. Uh it's responsible for running the data loaders, preparing batches and then passing on the workloads to the accelerators. Then comes the GPU compute and memory where the compute is essentially

**[4:56](https://www.youtube.com/watch?v=BG_nB5dolKs&t=296s)** responsible for uh various matrix multiplications including attention operations and the memory is where the weights activations and later KVC states are stored. So this is where the primary constraints start appearing especially when we go to multiple devices that's where communication uh becomes an issue where we need to ensure the communication and synchronization of gradients and activations across the devices. So once we have this infrastructure in place this then becomes an optimization problem where we need to tune the hyperparameters to ensure that adding additional hardware actually translates to better systems. And once this model is trained, then finally we move on to the serving stage where the focus shifts again and this time to latency throughput and overall

**[5:44](https://www.youtube.com/watch?v=BG_nB5dolKs&t=344s)** better user experience. So what I want you to notice here is that as we move from single GPU to multiGPU to multi-node systems, that's where the constraints shift to different parts of the stack. And that's why we need to design systems that balance all of these dimensions together. Before we begin, um I wanted to introduce a general framework that we'll use for example throughout the talk. So instead of jumping right into the solution, it's important to first identify that what problem appears at the surface could actually be different than the underlying problem. So we can follow this framework where first of all we look at the symptom then we instrument the system to gather the right metrics to identify the underlying root cause and once we understand the

**[6:31](https://www.youtube.com/watch?v=BG_nB5dolKs&t=391s)** root cause then it's easy to apply very targeted interventions and then validate that all of these changes actually translate to better systems and I hope this can serve as a mental playbook that you can use for debugging your LLM systems as well. So let's start with a simple situation where the model does not fit into a single GPU anymore. So we try and increase more GPUs and we expect that the throughput should improve in the system. But what happens is not the case in reality and the first thing we try to look at is the memory allocation but we see that the model is actually being distributed across all GPUs. So then what we do is we look at the compute utilization and we see that this in fact is low and compute utilization can can be low because of different problems. It

**[7:19](https://www.youtube.com/watch?v=BG_nB5dolKs&t=439s)** can be because of underlying compute bottlenecks or communication stalls etc. So we need to look more carefully and what for this we need to look at both the CPU as well as the GPU utilization over time. And when we look at this we realize that there are periods where there's bursts of GPU activity and there are periods where the GPU is actually waiting idle. So now we need a more granular view to be able to solve for this problem. So we asked the first question what all could the CPU actually be taking log on and that's where we notice that the batch preparation is actually not able to keep pace with the GPU consumption and this becomes the data pipeline bottleneck actually uh hindering the scaling issue. So to increase the first step to increase the rate at which the data is being prepared the first set of optimization that we do

**[8:06](https://www.youtube.com/watch?v=BG_nB5dolKs&t=486s)** is actually at the pre-processing stage where we actually move the uh pre-processing out of the training loop and instead pre-tokenize the examples and optimize the patch packing efficiency so that the data is already ready when the GPU actually needs it. The second set of optimizations we do is around the data loading where we parallelize the data loading process itself by increasing the data loader workers and also enabling asynchronous pre-fetching. The last uh set of optimizations that we do is pipeline parallelism where we actually parallelize and balance both the CPU as well as the GPU workload. So this is where we actually shift the parallelizer parallelism from the data loader to actually the entire set pipeline between the CPU and the GPU. So at this stage of scaling the key takeaway is that in distributed training the entire

**[8:56](https://www.youtube.com/watch?v=BG_nB5dolKs&t=536s)** throughput of the system actually gets limited by the slowest stage in the pipeline and not necessarily the fastest GPU. So let's say now you fix the previous issue and you assume that you'll add more GPUs, your model is getting distributed, everything should work fine, but you immediately hit a CUDA out of memory error even with a model that is loading fine. So, and then you go go through the usual checklist of reducing the batch size, but even with a batch size of one, you're still out of memory. You do gradient accumulation, still nothing. So, going back to the playbook, we take a step back and ask a different question. Instead of asking where why is the the large model is not scaling, we ask where is the memory being allocated. And once we profile this memory usage,

**[9:43](https://www.youtube.com/watch?v=BG_nB5dolKs&t=583s)** the problem becomes immediately clear. So we can see that the model weights are actually a very small part of the memory and most of the memory is being consumed by the activations and the optimizer states especially for like common optimizers like Adam which for every parameter require storing two additional parameters for variance and momentum estimates and also activations are computed for every layer in the transformer and they need to be stored for gradient computation during backdrop. So and we also know that for transformers for every batch activation scale with the sequence length the hidden dimension and the number of attention heads with sequence length having the most impact because of quadratic attention cause. So once we understand this then the targeted interventions become much easier and we

**[10:33](https://www.youtube.com/watch?v=BG_nB5dolKs&t=633s)** can use activation checkpointing where we allow the recomputation of some of the activations instead of storing all of them. We can use flash attention where instead of materializing the entire attention matrix in HBM we do tile computation for attention thereby improving the memory footprint. And we can also use techniques like zero and FSDP uh that allow sharding of the optimizer states and the activations across the GPUs. And finally we can use wherever possible try to reduce the sequence length or introduce sequence parallelism so that the sequence dimension is split across the different GPUs and thereby we can increase uh the context length or do efficient long context training. So the bottom line here is that for LLM

**[11:22](https://www.youtube.com/watch?v=BG_nB5dolKs&t=682s)** training out of memory is not a memory allocation problem but more of a memory management problem where we need to understand where the memory is actually going. So now we fixed some of our earlier issues. We've optimized the hardware throughput aggressively, but the optimization dynamics have actually changed. We do see on the surface that the throughput has improved significantly. And this looks like a success to us. But now what we experience are different kind of failure modes. We start seeing that the validation loss actually plate higher and the model performance actually becomes worse as we move to a more aggressive rescale setup. So now in this case we need to shift from uh the previous setup to the new scale setup of the system. So in order for us to diagnose this the first thing that we should look at is the bat size. Now when

**[12:11](https://www.youtube.com/watch?v=BG_nB5dolKs&t=731s)** we when we try and scale more hardware we can unintentionally increase the large bat size. And what happens is the effective bat size that the G the optimizer sees is not just the per device bat size. it actually scales as we increase the number of GPUs as well as gradient accumulation steps. So now when we increase the bat size even though that leads to smoother optimizer optimizer trajectories but we actually find that some gradient noise is actually useful for the system and it helps the optimizer explore the launch loss landscape better and also helps the optimizer escape sharp minima. And what also happens is an issue with stale gradients. As we increase the bat sizes, there is more accumulation that happens initially during the update. But when the update is actually applied, the

**[12:58](https://www.youtube.com/watch?v=BG_nB5dolKs&t=778s)** system is not set up for the new scale settings. And finally, what can happen is a hyperparameter mismatch. Since the the original training regime still remains the same, but the scale has shifted. We get issues because we have optimized everything including weight, decay, learning rate, warm-up for the original scale and not the new scale. So in this case what we need to do is recalibrate some of our optimization metrics. We need to retune the learning rate schedule carefully retune regularization and also make sure that we monitor the downstream metrics uh so that we can adjust the system for the new scale. So the takeaway at this stage is that maximizing just the system throughput is not the same as maximizing the learning efficiency and we need to monitor both the system as well as the

**[13:47](https://www.youtube.com/watch?v=BG_nB5dolKs&t=827s)** model metrics together. So now let's say that you were able to successfully train your model and it it looks like a success right so you tried that in isolated experiments and everything looked good but things change at inference time because transformer inference usually happens in two stages of prefilling and decode where the pre-fill stage is when the input comes in and the entire input sequence is processed in parallel using attention operations and that makes this operation more computebound. And then comes the decode stage where auto reggressive generation happens and it happens over one token at a time where every token attends to all the previous tokens and that dominates the time per output token whereas the prefill stage affects the

**[14:36](https://www.youtube.com/watch?v=BG_nB5dolKs&t=876s)** time uh the time to first token which is what the user would perceive the response as. So we see that for training while we were heavily focused on better synchronization effective batches better throughput at uh de at the inference stage that is more dependent on the request shape and uh and let's see an example of how this actually happens. So yeah, let's say your model was loading fine, but in production systems, you rarely have one-off requests. you're usually dealing with thousands of concurrent users and all of those users can send in variable context uh length requests which need to be served efficiently and in this case the bottleneck becomes the memory because for inference we need to store the KV cache states and which are essential because we don't want to

**[15:24](https://www.youtube.com/watch?v=BG_nB5dolKs&t=924s)** recomputee the attention scores for all the previous tokens again and again and this KV cache size grows with both the context length and the concurrency. So we need to ensure that whenever the new request comes in what could happen is that initially the throughput looks smooth and the requests are being served but as we approach the memory capacity instead of a smooth degrade we could start seeing something like a concurrency cliff where the user's uh throughput declines sharply instead of smoothly. So now how can we avoid this and how do we identify this concurrency cliff and how do we stay away from it. So the key is first of all to get visibility into what's happening. Instead of treating the GPU as a whole we should ideally treat it into separate buckets of model

**[16:13](https://www.youtube.com/watch?v=BG_nB5dolKs&t=973s)** weights KV cache and temporary buffers and that will help us uh to tell whether the issue is coming from the model weights or the context length or the concurrency. And then next step could be to identify request shapes because the users could be sending either tens of thousands of rag kind of workload requests uh tokens or they could be shorter single uh chat requests. So identifying that can help us understand our traffic. And finally we should try to understand the memory pressure and fragmentation especially for variable length requests. It's possible that some of the requests are being served but then it leaves some unusable gaps in the memory which are not enough to serve any new requests efficiently

**[17:00](https://www.youtube.com/watch?v=BG_nB5dolKs&t=1020s)** and once we understand that then there are several optimization levers that we can utilize. The P a pedag KV cache is usually the biggest win and that's also what Ian discussed in length yesterday where instead of allocating a huge block of memory we allocate smaller chunks uh where that can help us like schedule the requests better and then we can also use flash attention or KV quantization to reduce the memory footprint and to enable the KV states to be quantized and occupy less memory. Prefix caching can help in cases when we know that the requests will have a lot of uh repetitive context for example uh system prompts or retrieve documents. So we can avoid recmputing those again and again. And then we can use things like L1, L2

**[17:49](https://www.youtube.com/watch?v=BG_nB5dolKs&t=1069s)** tiered caching where specially we can use the GPU to keep the high access and the warmer KV blocks in L1 cache and then we can offload some of the colder states or the inactive KV blocks to the L2 storage which is cheaper or like a CPU. So this will essentially enable us to extend the context length without actually adding more GPUs. So the bottom line here is that long context inference is fundamentally a memory management problem. So better memory aware scheduling allocation and caching strategies can help us get more juice out of those GPUs instead of just brute force overprovisioning. >> Um now let's look at a very subtle but

**[18:37](https://www.youtube.com/watch?v=BG_nB5dolKs&t=1117s)** important production failure. uh when we start thinking about serving a lot of different users, we can see that aggregate throughput of the system which is tokens per second seems completely healthy but a lot of users start experiencing streaming stalls or even high time per output token and in order for us to diagnose this we need to really see what happens and what are some of the nuances we need to be careful about. So let's look at an example of a bad scheduleuling. So in this case what happens is we have a user A who sends a long context and we have users B and user C with shorter context. So what happens as we've seen previously is that this long context actually blocks the the the long prefill and actually starts interfering with the active decode streams. What happens is decode essentially requires frequent

**[19:25](https://www.youtube.com/watch?v=BG_nB5dolKs&t=1165s)** compute small compute slices and such a long prefill can actually monopolize the memory and the compute bandwidth of the system. So for us to be able to diagnose this what we need to do is break down the end to end latency path of the system. We have different components which give us different indicators of what the system tells us. Q time tells us about the the the time the system is waiting before even a request starts. Prefill time gives us an indication of how expensive the input is. Decode time and typer output token actually tells us whether the system will stream smoothly or not. So before we fix this, let's look at an example of a good scheduleuling. So in this case, we see that instead of letting the entire long prefill block the decode, we have jumped the prefill and interled that activity

**[20:13](https://www.youtube.com/watch?v=BG_nB5dolKs&t=1213s)** with decode. Now this actually helps us optimize the workload characteristics because we've seen the nature of prefill and decode is completely different. Prefill is more compute heavy and decode is more memory bandwidth uh bound. And this is actually the fundamental principle behind chunk prefill and an SLO aware shoulduling. Another thing that we can do is disagregate prefill and decode resources so that we can optimize for the workload utilization of the system as a whole. So the main lesson here is that often for in inference systems for large scale we need to optimize for an SLO compliant goodput rather than simply looking at an aggregate tokens per second because we need to really ask the question can we serve a lot of different users while still keeping the system level latency requirements

**[21:02](https://www.youtube.com/watch?v=BG_nB5dolKs&t=1262s)** and to close our talk today I want to briefly highlight the key takeaways that we have across all the different failure modes that we've seen So the first thing is always starting with the symptom and measuring before we optimize. So when we think about scaling both training as well as inference, we need to look at both the metrics at a system level as well as the model level to be able to diagnose and solve for the for the underlying little bottleneck. The second is that we need to find the limiting stage in the end to end path. As we've seen in different stages of training and inference, the bottleneck can become a different part of the pipeline entirely. So whether we are optimizing for compute, GPU utilization or inference serving, we need to find what stage is causing the issue in the end to end pipeline. And the last is treating every fix as a hypothesis.

**[21:51](https://www.youtube.com/watch?v=BG_nB5dolKs&t=1311s)** Instead of going directly to a brute force solution, we need to first identify where the bottleneck is. And often a bottleneck moves across the pipeline as we scale. So we need to rep-profile after we make a fix and then reme-measure the system to make sure that the fix that we've made actually works. So the main lesson that we want to close today is with production LLM scaling is not a single objective objective optimization problem but rather system and model metrics must be analyzed together and if you're able to build a clean mental playbook we can help serve users more reliably and effectively at scale. Thank you. Thank you. >> Thank you so much for that talk. Um I

**[22:41](https://www.youtube.com/watch?v=BG_nB5dolKs&t=1361s)** actually want to ask you a question first. So most of us outsource a lot of this thinking about LLM inference to LM providers like OpenAI, Anthropic and and the rest. What are some of the applications you see at a place like Vanguard or or T a financial firm large organization where it's actually worth it to go through all this trouble of doing the LM inference ourselves? Why don't we just rely on external providers? >> Yeah, I think once once we start looking at the token cost which is like growing exponentially, I think things become immediately clear. And the other thing is that every day there's like new models coming up. So how do we keep up with that? So instead of that if we if teams are willing to just invest like one time on building this pipeline then

**[23:29](https://www.youtube.com/watch?v=BG_nB5dolKs&t=1409s)** you can like quickly uh fix or replace any model and then specifically for use cases uh at Vanguard where we seeing the most value is places like um call summarizations. So at Vanguard we receive more than 100,000 phone calls every day and at that scale if you are to summarize like each of those phone calls even let's say like in 250 tokens that cost like really scales. So I think it's like balancing the cost versus the um usage uh equation there. Yeah. >> Are there any other questions in the room? Did you use any libraries to uh manage

**[24:17](https://www.youtube.com/watch?v=BG_nB5dolKs&t=1457s)** this or do you have a few more custom names? Um so we have experimented with different open source libraries but one of my personal favorites is the model scope swift library that handles like the training specially and uh then so yeah that that handles like all the SFT RL all of those. I've also experimented with VRL or TRL libraries but I think model scope has very good like documentation and stuff and then for inference I think always VLM for the win.

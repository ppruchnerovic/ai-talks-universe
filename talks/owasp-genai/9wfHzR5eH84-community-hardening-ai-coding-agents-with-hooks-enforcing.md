---
id: 9wfHzR5eH84
title: "Community | Hardening AI Coding Agents with Hooks Enforcing Least Privilege on Autonomous Developers"
slug: community-hardening-ai-coding-agents-with-hooks-enforcing
conference: owasp-genai
conference_name: "OWASP GenAI Security Project"
category: "Security conferences"
edition: "OWASP GenAI Security"
year: 2026
speakers: ["Karan Bansal"]
channel: "OWASP GenAI Security Project"
duration_min: 26
published_at: 2026-07-20T00:25:12Z
video_id: 9wfHzR5eH84
url: https://www.youtube.com/watch?v=9wfHzR5eH84
youtube_url: https://www.youtube.com/watch?v=9wfHzR5eH84
tags: []
topics: ["Agents & orchestration", "Coding assistants & agents", "Evals, observability & reliability", "Security, safety & red teaming"]
transcript: true
---

# Community | Hardening AI Coding Agents with Hooks Enforcing Least Privilege on Autonomous Developers

**Karan Bansal**

`OWASP GenAI Security Project` · `OWASP GenAI Security` · `2026` · `26 min`

[Watch the recording](https://www.youtube.com/watch?v=9wfHzR5eH84) · [Conference site](https://genai.owasp.org/)

## Description

AI coding agents like Claude Code execute shell commands, read and write files, and make autonomous decisions -effectively acting as developers with broad access to your codebase and system. As adoption accelerates, securing these agents at runtime becomes critical. But how do you enforce least privilege on an agent that needs wide access to be useful?

This talk presents a practical, hook-based approach to securing AI coding agents. Using Claude Code's event-driven hook system, I'll demonstrate how PreToolUse and PostToolUse interception points can enforce security policies mapped directly to the OWASP Top 10 for LLM Applications: blocking dangerous commands before execution (LLM06 - Excessive Agency), detecting prompt injection patterns in tool calls (LLM01 - Prompt Injection), and generating full audit trails for every agent action (LLM09 - Overreliance).

The session includes a live walkthrough of open-source hook scripts -block-dangerous-commands, protect-secrets, and an auto-audit-logger -along with performance benchmarks comparing Node.js and Python hook implementations. Hooks run synchronously, meaning every millisecond counts; I'll share real-world data on keeping security controls under 100ms per invocation.

Attendees will leave with a ready-to-use, open-source hook toolkit, a framework for applying defense-in-depth and human-in-the-loop principles to any AI agent that executes code, and concrete patterns for building custom security hooks without degrading agent performance. No slides-only theory -everything demonstrated is running in production and available on GitHub.

🔗 Learn more: https://genai.owasp.org

Speakers:
Karan Bansal
Global Head of AI and Security Innovation, ArmorCode

## Transcript

*3,732 words · source: supa (en, exact timings)*

**[0:03](https://www.youtube.com/watch?v=9wfHzR5eH84&t=3s)** Hi everyone, I am Karan Bansil. Over the next 30 minutes, I want to share a pattern I've been using to keep AI coding agents from doing dangerous things on developer machines. It's called hooks and honestly, it's the most underrated feature in the entire agent ecosystem. Everything I show today maps to the OS top 10 for LLM applications and every line of code is MIT licensed and on GitHub. Let's get into it. Quick context on me. I run AI at Armor Code. Today 10 years in cyber security before that I was a founding engineer at Avid Secure which got acquired by Soos. But the reason I'm here is the open source work you see on the right. Clot code hooks is the toolkit we will look

**[0:52](https://www.youtube.com/watch?v=9wfHzR5eH84&t=52s)** at today and the patterns work for any agent framework not just clot code. I started writing about hooks back in January because the docs existed at that point but practical examples did not. This talk is what I wish someone had told me back then. Here is the problem in one slide. AI coding agents are not assistants anymore. They run shell commands. They read and write file across your entire machine. They fetch from the web which is a data expiltration vector and a prompt injection vector at the same time. They call third party tools through MCP servers that you may not have audited. They push to get and critically they spawn sub agents that have all the same

**[1:40](https://www.youtube.com/watch?v=9wfHzR5eH84&t=100s)** powers recursively. Every one of those moves is the model's call based on its reasoning. And the loop runs at machine speed, dozens of tool calls per minute. So whatever your security posture you had for human developers, it was already insufficient. For an autonomous agent making decisions at machine speed across the surface, it's nowhere close. So what do we do? That's the whole talk. Now every modern AI agent runs the same fourstep loop. think, act, observe, repeat. Clo does this, cursel does this, copilot agent, codec cla, all of them. And the act step is where everything

**[2:28](https://www.youtube.com/watch?v=9wfHzR5eH84&t=148s)** dangerous potentially happens. That's a shell command, a file write, a package install, a web page, or it could be a lot more. So the key insight here is that security has to happen between steps in the gap between act and observe not before the agent starts not after it's done. The gap is where hooks live. Okay, quick reality check before we get into the architecture. I'm not going to tell you that sandboxes are wrong. If you can run your AI agent in a VM with lockdown network, no real credentials and a clean copy of your code repo, that's a great answer. It's also the answer almost nobody actually runs. So this is what I see in practice. People would either launch cloud code

**[3:17](https://www.youtube.com/watch?v=9wfHzR5eH84&t=197s)** with the flag dangerously skip permissions or if we look at codeex there is a yellow flag which basically gives all the permissions to the agent or they will click allow all on every prompt because by the third dialogue they have stopped reading. They run agents in their main dev environment with real git credentials and real cloud access because that's where their code is. They skip the sandbox because the sandbox breaks their package installs and their CI integration. The right answer exists. Nobody runs it. And that's the gap hooks fill. Think of it as defense in depth. If you have a sandbox, great. Keep it. Add hooks inside it. If you don't, hooks become your primary lines of defense. And honestly, that's much better than what

**[4:05](https://www.youtube.com/watch?v=9wfHzR5eH84&t=245s)** most teams have today, which is nothing. Now this is the claw code hooks life cycle and honestly the structure looks pretty much the same across every modern agent and while there are a lot more triggers for hooks I have covered only a couple of them in this diagram for simplicity and ease of understanding. The dashed orange box is the agentic loop around it are session level events. Now every single box on this diagram is a hook point. Now a hook can be of five types. The one you will use 95% of the time is a shell command. Any executable in any language, NodeJS, Python, Bash, Go, Rust or any language of your choice. The other four are escape hatches, an

**[4:53](https://www.youtube.com/watch?v=9wfHzR5eH84&t=293s)** HTTP endpoint if you want centralized policy across machines, an MCP tool if you already have an MCP server doing policy work, a prompt if you want an LLM to make the decision. That would be slow but more flexible. or a sub agent is spawn if you need full reasoning power for everything I will show today it's shell now how does a hook actually decide two ways either it will do exit with code two to block the action anything else is non-blocking or it will exit with zero and print a structured JSON to allow deny ask or modify multiple hooks for the same event can run in parallel and by default the hooks are meant to be synchronous that is they block the loop until they finish but you can add async true flag

**[5:42](https://www.youtube.com/watch?v=9wfHzR5eH84&t=342s)** and they will fire and forget and the critical part is that model never sees the hook it cannot be social engineered around it this is policy enforcement outside the LLM and hooks are deterministic whereas LLMs are non-deterministic by nature the full cloud code hook system has grown to 29 events But you don't need all 29. For security work, six events carry the heavy lifting. Pre-tool use, it runs before any tool call. It can block it. This is your enforcement workhorse. Post tool use runs after a tool call. Can scan the results before the model sees it. User prompt submit fires when the user types something. Useful for

**[6:31](https://www.youtube.com/watch?v=9wfHzR5eH84&t=391s)** sanitization of input. Permission request lets you autodeny rescue tool calls without bothering the user. Sub agent start. It fires when the agent response a sub agent. You're ceiling for parallel work. And instructions loaded. This one is newer. It lets you audit when cloud MD or rule files load into the context. It is very useful for catching prompt injection through dropped files. These six are the building blocks for the OS risks. We will cover next. Here is a hook's entire job. Two JSON blocks on the left. The input cloud code hands the hook this JSON on the standard input. When the agent tries to run R minus RF, which is one of the most

**[7:18](https://www.youtube.com/watch?v=9wfHzR5eH84&t=438s)** dangerous commands, it tells the hook the tool name, the actual command string, the working directory, the session ID, everything you need. on the right. The hook's response permission decision deny. It tells clot code to block the command and the reason gets passed back to the model so it can try a different approach. Now notice what's missing here. No SDK, no agent framework, no model. It's just a child process that gets JSON and prints JSON. Which means you can write hooks in Node, Python, Go, Bash, Rust, whatever you want. or you point at an HTTP endpoint and let your security service decide. And because the model never sees the hook code or the decision logic, prompt injection in the user message cannot bypass the hook.

**[8:10](https://www.youtube.com/watch?v=9wfHzR5eH84&t=490s)** Okay, this is the big table. The OS top 10 for LLM applications map to hook strategies. Honest framing here. Hooks don't address every risk. They address six of them cleanly and I want to be precise about which six and why others don't fit. The six hooks address well LLM1 prompt injection, LLM2 sensitive info, then LLM 5 output handling, LLM 6 excessive agency, then there is LLM 7 prompt leakage and LLM 10 unbounded consumption. All six have a clear hook shaped answer because the dangerous action flows through an interceptable tool call. Three don't apply to coding agents at runtime. And I want to be honest about

**[8:59](https://www.youtube.com/watch?v=9wfHzR5eH84&t=539s)** why. LM3 supply chain when the OS fract talks about LLM supply chain it means the model supply chain poison weights compromised model registries. If you're using hosted cloud or GPD that's upstream of you there is nothing a hook can do. LLM4 data and model poisoning that is a training time risk. By the time you are running inference with a hosted model, the training is frozen. So this is not addressable at the agent layer. LLM09 misconfiguration is about the model hallucinating false content, false facts, fabricated library names, madeup API calls. Hooks can't catch that because the model is just generating tokens. There is no tool called to intercept. LLM8 vector end embedding weakness is conditional. It only applies if your

**[9:49](https://www.youtube.com/watch?v=9wfHzR5eH84&t=589s)** agent does semantic search via tool calling. If it does a vector DB query goes through pre-tool use like any other tool and you can apply the same patterns. So six clean fits plus one conditional. The next seven slides go deep on the ones that matter. Let's move ahead. Let's start with LLM 6 excessive agency. The risk here is that agent has more power than it needs and it tries to use it. The hook block dangerous shell commands at the pre-tool use event for the batch tool. Now here is the shape. A list of patterns each tagged with a severity. Critical means catastrophic and unreoverable. High means real damage but maybe

**[10:37](https://www.youtube.com/watch?v=9wfHzR5eH84&t=637s)** survivable. strict adds cautionary patterns that might have a legitimate use. Now at the time of configuration of the hook, you can pick a threshold via single constant. High is the recommended default. Critical alone is too permissive. A strict catches real workflows and frustrates the developer. High is the sweet spot. The reason field is important. When the hook denies a command, that reason goes back to the model. So the agent understands why and it can try a different approach to get the job done instead of retrying the same destructive command in a loop. Notice what's not there. No LLM, no remote service, no network latency, just a reax array and a fast process. About a 100 lines of node end to end.

**[11:29](https://www.youtube.com/watch?v=9wfHzR5eH84&t=689s)** Now this one covers two OS fisks at once. LLM1 prompt injection and LLM2 sensitive info disclosure. They overlap when injection asks the model to read and excfiltrate secrets which is actually the most common attack pattern. Product secrets registers for four tools read, edit, write and bash. For read, edit and write, it checks the file path. For bash, it checks the command string against a second set of patterns. And the bash patterns are where it gets one. We block not just cat env. We block curl uploading env to a remote server SCP copying it, source loading it into the environment, find with exec cat base 64

**[12:19](https://www.youtube.com/watch?v=9wfHzR5eH84&t=739s)** encoding it, all those different scenarios and more. These are real excfiltration patterns we have seen in the wild. And of course there is an allow list for false positives. So NB.ample is fine. And again no LLM no network pure rejects sub millisecond. LLM3 in the OSP pack is specifically around the model supply chain not the software supply chain of a project. But the agent installing a malicious npm package is still a real risk worth covering. And that falls under traditional software supply chain. Adjacent risk same face adjacent risk same fix.

**[13:08](https://www.youtube.com/watch?v=9wfHzR5eH84&t=788s)** Here is the thread. The agent installs a malicious package because the model misspelled the name or because it was told to install something by a poisoned readme. It just tried the shape pre-tool used for bash reax matches install commands across npm pip cargo uv yarn etc. pass the package names. Check each one against the top 10,000 registry packages. Using lemon spin distance to catch type of squads like using LOD AS instead of L O D A SH you flipping the HS order. Um checking minimum downloads or the age of the package and optionally calling certain APIs which give a deeper score for the dependency. a straightforward policy logic. I impact

**[13:56](https://www.youtube.com/watch?v=9wfHzR5eH84&t=836s)** one of the hooks I would most like to see the community ship next. LLM05 improper output handling. Now this one is different. It's about the code the agent writes not the command it runs. The classic vulnerabilities XSS, SQL injection, command injection, eval dangerously set inner HTML, etc. They are there. The hook fires on post tool use for edit and write. It scans the file content with a handful of patterns. If it sees a bad smell, it returns decision block. Now block in post tool use does not undo the write the file is already on the disk but it does prevent the next model call until the agent

**[14:47](https://www.youtube.com/watch?v=9wfHzR5eH84&t=887s)** acknowledges and fixes the file. So you get this clean handshake where the agent rewrites the file before continuing to the next step. For a deeper scan, the same hook can shell out to SIM grip or bandit. That costs you a couple hundred milliseconds, which is fine on a file write event, but not fine on every bash call. Llm 7 system prompt leakage. There are two attack shapes here for coding agents. One where an external attacker tricks the agent into reading clot. MD and excfiltrating it. The product secret hooks we just saw already covers a chunk of this by treating dotcloud config as sensitive two and this is the more interesting

**[15:34](https://www.youtube.com/watch?v=9wfHzR5eH84&t=934s)** one. The agent itself drops a new rules files into dotcloud/ rules via write which then loads on the next traversal and rewires the agent's behavior. Think about what that is for a second. That's the prompt injection via the file system. The new instruction loaded event makes this trivially detectable. Every time a clot.md or a rule file loads, the hook logs who loaded it from where it was loaded and why it was loaded. If a rule file appears that the user never wrote, you raise a flag. This was not possible few months ago, but it is possible now. Now, this one isn't strictly LLM 9. The OS spec for LLM9 is about the model

**[16:23](https://www.youtube.com/watch?v=9wfHzR5eH84&t=983s)** generating false content. Hallucinated facts, madeup library names, hooks can't catch that. The model is just generating tokens. But there is a related problem that hooks do solve. And I want to cover it. The agent tells you that it ran the tests and they all passed. Did it? You can't tell from the conversation transcript alone because the model can rate two outcomes that didn't actually happen. The fix is observability log every actual tool invocation outside the model's storytelling. An asynchronous hook on every pre and post tool event captures a structured record. Time stamp, session ID, tool name, redacted input, the decision outcome, working directory. Because it's async, the agent never

**[17:12](https://www.youtube.com/watch?v=9wfHzR5eH84&t=1032s)** waits. Your workflow never gets slowed down. You can ship the records off to S3 or open search on a slow path. Now you can reconstruct any session. Diff what the model claimed against what actually happened. Beat that into a code review or use it as a forensic evidence after an incident. This isn't preventive. It's the audit trail you will want when something goes wrong. LLM10 unbounded consumption coding agents have a specific failure mode. The agents get stuck in a loop. It may spawn sub aents that is spawn further sub aents burn hundreds of dollars of tokens before anyone notices. Two hooks together could solve this. A

**[18:01](https://www.youtube.com/watch?v=9wfHzR5eH84&t=1081s)** rate limiter on pre-tool use. It will count the tool costs per session per minute per hour. denies once the threshold is set and a sub agent cap on subent start counts active children denies when there are too many. It can also pattern match the sub agent prompt against the parents prompt to detect recussion. Simple and honestly this pays for itself the fastest because every block runway loop is a real dollar saved. Now one thing about hook performance that really matters. Hooks support two execution modes and the right choice depends entirely on the what the hook is for. Synchronous hooks block the agent loop until they finish. Clot code waits for them to finish and this adds up fast. 60

**[18:50](https://www.youtube.com/watch?v=9wfHzR5eH84&t=1130s)** tool calls in a session 200 milliseconds each. That's a two full minutes of pure wait time. So use sync only when the hook has to make a decision. Block this command. Deny this file read. I refuse this package install cap this sub agent for these every millisecond counts because the developer is sitting there my budget for sync hookix under 100 milliseconds. Asynchronous hooks are different and forget the agent keeps running while the hook works in the background. Use async when the hook only observes audit logging telemetry cost tracking scheduled scans notification etc. For these the runtime does not matter much. You can use Python, shell out to a heavier scanner, even make a network call. Same hook system, two policies

**[19:39](https://www.youtube.com/watch?v=9wfHzR5eH84&t=1179s)** pick per use case. The mistake people make is treating everything as sync because the most prominent examples happen to be enforcement. Now looking at some concrete numbers tied to the sync versus async distinction. Pure bash hooks 10 to 20 milliseconds fastest opinion but limited to what reax can express. NodeJS has a cold start 50 to 100 milliseconds the sweet spot for real policy logic. Python 200 to 400 millconds which is your whole latency budget gone if you are synchronous. So for synchronous enforcement hooks, pick bash if your check fits a reax node if you need a real logic or go even faster with a compiled go or a rust binary under 10

**[20:27](https://www.youtube.com/watch?v=9wfHzR5eH84&t=1227s)** milliseconds when the bar is really tight. For asynchronous observers, audit loggers, telemetry, cost trackers, Python is fine. Nothing is waiting on the result. And there is a third option I would flag HTTP hooks. You point cloud code at a local endpoint. keep your ping your interpreter warm pay around 5 millconds roundtrip any language that's how I would build a heavier policy engine that needs database access or shared across multiple session rule of thumb pick the runtime per hook based on what the hook actually does not per repository a single project can mix bash node enforcement and python observability hooks happily Okay, let me show you what this actually

**[21:17](https://www.youtube.com/watch?v=9wfHzR5eH84&t=1277s)** looks like. Two screenshots from real sessions. On the left, I asked CL to do something really inocus and at some point in its plan, it decided to read product secrets denied the read and passed back the reason. Claude saw the denial, understood it, and asked me how to proceed without reading the file. No prompt engineering, no LLM in the middle, just rejects. On the right, an agent during a refactor got confused and tried to force push to main. Block dangerous command stopped it cold. Both of these run in under 5 milliseconds, invisible until they matter. All right. So, a quick note before I

**[22:21](https://www.youtube.com/watch?v=9wfHzR5eH84&t=1341s)** close. The reason this pattern matters beyond cloud code is it's not just cloud code anymore. The four biggest agentic coding tools all of them are shipping hooks and the event names are nearly identical. Clot code has 29 events including pre-tool use, post tool use, etc. OpenI codeex CLI ships six events, the exact same pre-tool use, post tool use, permission request, user prompt submit, plus session start and stop. Get up copilot ships 13 events. Pre-tool use, post tool use, permission request, sub agent start, sub aent stop, agent stop and more. Cursor uses before shell execution, before MCP execution, before read file, after file edit and a dozen others. Different names, same shape. Now

**[23:11](https://www.youtube.com/watch?v=9wfHzR5eH84&t=1391s)** this pattern has converged across enthropic, open AI, GitHub and cursor independently. That tells you that the design is right. So three universal principles to take away intercept at the tool call level run policy outside the model and log everything. If you take one thing from this talk, it's that the vendor specifics will keep evolving. Those three principles would not. Whatever agents you use, ask the same questions. Where are the hooks? What events do they fire? And what policy can I write? Okay. So, a community asked five folks I would love to see merged. Each one is a weekend project. Each one closes another real world risk for coding agents. Pick

**[24:00](https://www.youtube.com/watch?v=9wfHzR5eH84&t=1440s)** anyone. Open aable request. You're on the read me. Core are green field dependency validator for typos packages. Code wellness scanner for the OASP LLM 5 output handling risk instructions audit for LLM 7 prompt leakage and rate limiter for LLM 10 unbounded consumption. The fifth, audit logger pro builds on event logger. py that's already in the repo. It turns the debug utility into a real production audit log. Redact the secret rate, redact the secrets, capture outcomes, ship asynchronously to real storage, critical observability for incident response. If you're new to security, these are honestly some of the lowest barrier

**[24:48](https://www.youtube.com/watch?v=9wfHzR5eH84&t=1488s)** contributions out there. The interception points are documented. the test scaffolding exists. You would need just the policy logic. And there is another 12 hook ideas in the repos readme that go beyond this things like branch card, testdriven development guard, cost tracker, auto format. If none of these five grab you, that list might. All right, so the closing thoughts and four takeaways. One, hooks are runtime policy enforcement outside the model. So they cannot be jailbroken from inside the prompt and hooks are deterministic whereas LLMs are nondeterministic by nature. Two six of the OS prop 10 for LLM applications have a clean hook shaped answer. The other four either

**[25:36](https://www.youtube.com/watch?v=9wfHzR5eH84&t=1536s)** don't apply at runtime or need a different layer of defense. The pattern is the win where it fits. Three or four are the ones I really want you to leave with. Every block dangerous command saves a potential disaster. Every producted secret prevents a leak. These hooks are not glamorous. They fire silently a thousand times a session and they are invisible until they matter. The repo is on GitHub, MIT licensed. All the production code shown today is there. The blog is on my site. The cloud code book docs are at code.cloud.com and OS LM top 10 at jenni.os.org.

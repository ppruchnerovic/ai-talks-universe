---
id: 1EGh_ShqWHc
title: "Systematic LLM Prompt Optimization with DSPy and Databricks"
slug: systematic-llm-prompt-optimization-with-dspy-and-databricks
conference: ai-council
conference_name: "AI Council (formerly Data Council)"
category: "AI engineering & agents"
edition: "Data Council / AI Council"
year: 2026
speakers: []
channel: "AI Council"
duration_min: 41
published_at: 2026-06-16T18:45:17Z
video_id: 1EGh_ShqWHc
url: https://www.youtube.com/watch?v=1EGh_ShqWHc
youtube_url: https://www.youtube.com/watch?v=1EGh_ShqWHc
tags: ["AI"]
topics: ["Data engineering & MLOps", "Evals, observability & reliability", "Prompting & context engineering"]
transcript: true
---

# Systematic LLM Prompt Optimization with DSPy and Databricks

**Speaker not identified**

`AI Council (formerly Data Council)` · `Data Council / AI Council` · `2026` · `41 min`

`#AI`

[Watch the recording](https://www.youtube.com/watch?v=1EGh_ShqWHc) · [Conference site](https://www.aicouncil.com/)

## Description

[2026 - DAY 2 - WORKSHOP] Sustainable prompt engineering is a challenge. Every time we change a model, update the distribution of input data, or extend a pipeline, we often find ourselves manually editing LLM prompts without fully understanding the consequences of doing so. DSPy takes steps towards addressing this by treating prompts as learnable parameters, not hand-crafted strings.

This workshop introduces DSPy's building blocks - signatures as semantic contracts, modules as composable pipeline steps, and optimizers as the training loop - then demonstrates how they combine to produce pipelines that improve themselves systematically.

During the workshop, we will demo a pattern that is still underused in practice: optimize the evaluator first, then let the evaluator drive the optimizer. Starting from a retrieval and generation pipeline, we’ll explore how a small set of human-generated labels can be used to align an LLM judge to expert judgment. DSPy tools can then use that aligned judge’s feedback signal to iteratively restructure and improve the pipeline itself.

The workflow runs on the Databricks technology stack: We’ll use Databricks Foundation Model endpoints as the LLM backend, instrument everything with MLflow tracing, and inspect the before-and-after prompts to build intuition for what optimization actually does under the hood. We'll close with a look at recent Databricks research on program evolution and meta-optimization, and the benchmark results showing why chaining optimizers compounds their individual gains.

SPEAKER:
Robert Martin-Short - Forward Deployed AI Engineer, Databricks

👉 Sign up for our "No BS" Newsletter to get the latest technical data & AI content: https://aicouncil.com/newsletter

ABOUT AI COUNCIL:
AI Council brings together the brightest minds in data to share industry knowledge, technical architectures and best practices in building cutting edge data & AI systems and tools.

FIND US:
X: https://x.com/aicouncilconf

## Transcript

*7,876 words · source: supa (en, exact timings)*

**[0:04](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=4s)** I'm excited to talk about systematic LLM prompt optimization with the SPI. So, how is this talk going to work? We'll talk about the SPI, what it is, look at some fundamentals, and then we'll look at an interesting problem where we can use it to optimize some prompts both for an LLM judge and then also for a main program. So, before we start, brief introduction. I'm Robert. I'm a forward deployed AI engineer at data bicks. So that means that I spend a lot of time with data bricks customers trying to understand uh their AI workloads and bringing PC's uh from development to production. So with me here today are three other um AI FTEEs from my team. So Intai, Priyanka and Michelle and the four of us will be here at the end or afterwards to answer any questions that you have. So let's motivate this topic we with a problem that we often have as

**[0:53](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=53s)** builders. So let's say we have a business problem that we want to solve with an LLM powered system. This might be something simple like a classification in a batch process. Maybe it's going to be a chain of thought. Maybe a rag or a react or some complicated agentic system. So building the first version of this system is typically um fairly easy. It's straightforward. It's fun. But then the more difficult thing comes to like how do we actually iterate on this system to bring it to in alignment with the business goals. So we would like to have a systematic approach to maybe changing the prompts within the system and we also would like a more rigorous approach to make sure that when we do make these iterations we're not running into unforeseen errors or unforeseen issues. And commonly one thing that we see is that when customers have a a baseline system uh maybe the loudest person in

**[1:42](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=102s)** the room will say oh here are some edge cases that we found. then the engineering team will go and try and solve those edge cases but without having understood what the impact of solving those things are on the on the grand scheme of the system. So we can borrow some ideas to help us from the world of classical ML. So in classical ML we have models and we might want to retrain the models if we discover that something is wrong and retraining the models is just changing the weights to minimize a loss function on a training data set. So the beauty of the DSPI library is that it can take some of these concepts and apply them to prompts. So we can automatically optimize prompts on a validation data set to improve our system. Here's the outline of the talk. We'll first discuss DSPI fundamentals. You know what it is, what the motivation

**[2:30](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=150s)** behind this open source library is. As was mentioned, there's a connection with data bricks because of the academic connection, but it's an open source library. You can run it anywhere. So why exists? Uh why it exists, why it matters. We'll look at some core concepts of the DSPI library. So signatures, modules, and optimizers. These are uh classes that you will use if you use DSPI. And we'll walk through notebook one, which is just uh some fundamentals looking at how we set up uh DSPI programs. Then we'll move on to parts two and three, which are about prompt optimization. So first we'll learn how we can align an LLM judge uh using this optimizer called MI Pro V2. that stands for multi-prompt instruction proposal optimizer V2. It's just a statistically um guided grid search basically over both uh instruction candidates and few shot examples. Um and

**[3:20](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=200s)** that's going to be really helpful for building an LLM judge that's aligned with feedback. And next we're going to learn how we actually use that align judge to optimize uh the main prompt for our problem with an algorithm called jeer which stands for genetic parto algorithm. Um, and these are just two of the many optimizers that are built into DSpay that we can use. And there's notebook 2, which we'll walk through. Take a look at that. And I'm not going to run these notebooks for the sake of time, but you'll see them in data bicks free edition. Uh, and if you're interested in getting access to the notebooks, just message me afterwards. I can send you links uh, and we can connect and you can run them in data bricks free. But first, why prompt optimiz prompt optimization? What is it? Why does it matter? So prompt optimization is just the process of making prompts for our LLM system most suitable for a given task. And the most the simplest way of

**[4:10](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=250s)** doing this is just by manually editing strings. Um and of course if we have an evaluation data set we can have some example representative inputs run them through the baseline model generate outputs uh and then we can compare those outputs to uh kind of gold standard. And you can imagine every time you iterate on the system, you can run this sort of hold out data set through and make sure that it's working well. But it still takes time to do these manual iterations. And as we all know, prompts are very brittle. LMS can be sensitive to small wording changes. And we don't know in advance how our edits are actually going to impact the the quality of the entire system. And of course, agentic applications compound this problem because they have multiple LLM calls. We have multiple prompts. we have maybe tools that have um dock strings and prompts inside them as well. So it

**[4:58](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=298s)** becomes very complicated to know what a manual edit might do. And along these lines we have to worry about prompt drift. Prompt drift is just uh a change in the relationship between the prompt and the output. And that can sometimes come about just because the fun the uh the model has changed. Like maybe there's been a model version update. And if we've optimized our system to be very good at version A of some foundation model, we update to version B, it's going to take time to re-optimize all these prompts. And I've actually seen situations where teams are reluctant to switch to an either better or cheaper version of the model because they don't want to invest the time in reoptimizing all the prompts in their system. So ideally we can do this automatically. Another issue is that subject matter expert time is very valuable. Now if we had a abundance of subject subject matter experts we would just have them

**[5:47](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=347s)** look at the outputs of every type every system that we iterated on and give their expertise. But obviously that's not possible especially in for example legal and medical fields. So this is why we reach for LLM judges. Now LLM judges are supposed to encode the information that subject matter experts use uh when they are making their evaluation. But of course LLM judges need prompts too and they need prompt optimization. So if we're not careful, we run into a situation where we have kind of an explosion of prompts in our system and we don't know how to iterate on them. So that's where DS DSPI can be helpful. So DSPI is an open- source LLM orchestration framework. It's designed to keep prompts as parameters. So we can think of them as kind of the analogy to uh weights in a a classical machine learning problem. So the whole

**[6:35](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=395s)** philosophy of this tries to stop you from hand editing strings. In addition, DSPI has lots of these composable modules. It's inspired by um by PyTorch, so you can basically chain various different modules which make LLM calls together into quite complicated programs that don't take a lot of lines of code. So that's helpful um when we're explaining code to to someone else. So for example, we have uh predict chain of thought and react. These are uh sort of these are modules that exist inside DSPI and you can chain them together to make complicated programs. And finally, as I mentioned, we have built-in optimizers. Uh, these can be trace driven, they can be metric driven. We'll talk a little bit more about that later on. Um, MER Prov2, as I mentioned, is a good one for creating LLM judges because it can create few shot examples which are going to be used in the

**[7:24](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=444s)** prompt. And then, generic predator algorithm uses reflective evolution. So, it actually uses the textbased feedback from an LLM judge to enhance uh the the main prompt. So that can be very powerful too. And we'll see that in action later on. Let's take a brief look at a composable module just uh to sort of dive into a little bit of DSPI code to kind of show you what I mean when I say um it's powerful and it's easy to create in modules. So in this code um what this is doing is something very simple. It's just drafting an article. So it takes in a topic which in this case is World Cup 2002 and it's producing an article and it does this in two steps. First step is it needs to build an outline which is defined here by this chain of thought module. And the next step is it uses the outline to draft a section. And what's

**[8:13](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=493s)** nice about DSP is that you can define the inputs and outputs from each of its each of the modules um using these signatures. So we can see our outline signature. It takes in a topic. It's going to output three fields, a title, a list of sections, and a dictionary of subheadings. So you can immediately see that you can use this for structured output, which is really nice. And you can easily see where the inputs and outputs are. So that's our outline. And then when we run our our program in in forward here, we build our outline. And then you can see we're looping over section headings and section subheadings. And we're calling uh draft section. And draft section is just taking in our topic, our section heading, our list of subheadings, and it's generating some content for that heading. So within a few lines of code, we can have quite a complicated system

**[8:59](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=539s)** going on in here with two different chain of thought commands um and an output where we're generating this article. And we'll show you a few more examples in the notebook later on. So that was a brief tour of DSP. Now let's dive in a little more detail in some of the fundamentals. And this will be repeat of what we saw in the slide just now. So DSPI has signatures. These are classes which declare input and output fields. um you can define doc strings in there and you can define descriptions on the fields and those doc strings and descriptions are incorporated into the prompt that DSPI is actually sending to the LLM behind the scenes. And then we have these composable modules. The simplest one is predict which is just one LLM call with a structured input and output similar to what we just saw on the screen. Then we have chain of thought which is the same

**[9:47](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=587s)** as predict but it's just forcing the LLM to generate a reasoning field which is useful for several reasons. Sometimes that can actually improve the result of the the the main task. But it's also helpful if we just want to display the reasoning to the screen for example. Uh and again important thing is that we don't need to make edits to strings when we uh create these modules. We can use the optimizers to help us to do that. Okay. So as I mentioned we have custom modules. We can chain predict and uh chain of thought together in as way as in the way we showed in this previous slide. We also have a built-in uh rag module. So we can provide that with a generator lm and also a function for retrieval which is typically just a call to a vector database and then dsy can do rag for us. So we can put that into a custom module if we wanted to do

**[10:35](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=635s)** something with the output for example. Similar thing for react. We don't need to write the thought loop that happens inside a react agent. We have a built-in module. we could just provide an LM and we provide a set of tools as functions and the LM can then call the tools um in a react loop and the prompts for this can be optimized. So evaluation and optimization is also built in to DSPI. Uh we have DSPI metrics where which you can define as functions. They take in uh examples and predictions and output scores. So the thing that we'll see today is how we can use an LLM judge inside one of these metrics to produce a score which we then use uh to optimize a program. Uh we have a class called evaluate which is kind of a convenience method. It just allows us to um input uh a metric a data set and a

**[11:25](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=685s)** program and it will run the metric against the data set or run the program on the data set and evaluate with the metric in a multi-threaded way. So it's just convenient for us. And finally we have optimizers. As I mentioned u these take in the program which is the module with the signatures as we just saw. They take in a metric they take in a training data set and their task is to optimize the prompts in the program to uh maximize the metric value on the training data set. So sounds a lot like classical machine learning. Let's take a brief look at notebook one uh where we'll dive into some of the fundamentals and see some of these things um kind of in action. So here is the notebook. Hopefully this is big enough for everyone to see. Uh so here we are inside data bricks free edition. As I

**[12:12](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=732s)** said these notebooks can be downloaded as uh it's not the right one. These notebook these notebooks can be downloaded as ipython and run anywhere. Uh data bricks free is a great way of running them because you know you can connect to a cluster here and you can also connect to any of these open- source LLM endpoints that are available via data bricks free edition. Um, so what I'm doing right here is I'll show you some signatures and then I'll show you predict chain of thought a custom module and react very quickly before we move on to the next section. So we can define the LLM endpoint that we want to use which is going to be GPOSS120B here. And then here we are just setting up DSPI. We're going to use MLflow for uh for logging the traces. This is helpful when we have a more complicated program and we want to understand what the flow

**[12:59](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=779s)** of information is. Uh we can use ML flow for that. uh DSPI configuration is pretty straightforward. You can define the configuration here. This is also where you would enter your API credentials if you were using say OpenAI or something where you needed to use uh credentials. And then signatures as we mentioned are just semantic contracts. So here's a very simple example. A basic QA is going to inherit from DSpot signature. It's going to have a dock string. We don't have to give it that but that's kind of the baseline uh prompt that it can start to use. um we give it a question as an input and its output is going to be an answer. Same thing here for this sentiment classifier signature. Its input is a sentence. Its output is a is a sentiment. So very simple structured output. Predict is the simplest module. So to create a predict we just um have our

**[13:47](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=827s)** signature. We wrap it inside predict to make a callable which is called predict v1 here. And then we can just run it. So here's my question. The largest city in Brazil predict v1 is answering with sa Paulo. this is the model actually making the answer. And then if you want to add additional instructions, you can do so. So maybe you already have a prompt that's been optimized or written by an you can just add um some instructions to your signature like this. And then you can ask the model to generate a result with those instructions. Here's the MLflow trace UI uh which is is helpful again for more complicated tasks. What happens if you want to run different LMS inside your program? This is straightforward. You can just do with dspi context and then provide it with a second lm and then you can run a module that you've already written with that

**[14:35](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=875s)** second lm. So right now we're using um gemma 3. There's also uh an open source model that's available as an endpoint in data bricks free. So we could run that and generate the result using this this model. Where is the prompt actually coming into the picture here? So this is a way of inspecting uh what information has been actually sent to the LLM in this process. So here is the system message which DSP has made for us for our simple QA program. So it's basically saying to the LLM, here's a question. Um here's you must produce an answer which is I must produce an output which is called which is an answer. Here's the structure that you expect. Here's the user message which um was input by uh by me in the notebook just now and then uh asking it to respond with the corresponding output fields. So these prompts are what we are

**[15:23](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=923s)** used to handwriting perhaps with other orchestration frameworks but DSP is taking care of writing them for us under the hood. Let's look at chain of thought. This is pretty similar. We can define a chain of thought uh module like this with our basic QA uh signature and then we can have it answer a question and it automatically outputs some reasoning. We can also give instructions to this. So what I commonly see people doing is they want to change the reasoning that the chain of thought produces. They can just add some instructions here. Make your reasoning as concise as possible and then rerun it. And you can see the reasoning is now more concise because that's been incorporated into the prompt too. Skip over this. This is just the system message from our chain of thought.

**[16:12](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=972s)** This is a little more interesting. So this is custom modules. So this is just a very simple example of how we might build a uh a rag um module inside DSPI. So here's a very tiny corpus uh that just contains some information and a simple search function for uh returning some of the elements of this corpus given an input query. So this would be replaced in practice by your uh your call to a vector database. So we can set up dspi configuration for rag. We just give it our language model and we give it our retrieval method which is wrapped inside this class here. So you can see it calling the function that we just defined simple retrieve. And now DSPI is set up to do retrieve augmented generation. It's going to use an LM uh to do the generation and it's going to use this retrieval uh class to retrieve

**[17:00](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1020s)** the data given an input. And now we can define our signature. So given our question and some context which has come out of the DSPI retrieve the thing that's been uh using that retriever just that I defined just now we can generate an answer and we can define this as a chain of thought module right here. Here's DSpi retrieve which is just going to be calling uh the retrieval meth retrieval method that we defined here. And then we run forward again. We take in a question. We get the context using retrieve and then we generate the prediction using just gener selfgenerate with the context and the question and we output an answer. So again within a few lines of code we have a simple rag system with prompts that can be optimized by automatic optimizers later on.

**[17:49](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1069s)** skip over that. One final one to draw your attention to is React. It works in a very similar way. We can define functions. This one is called search Wikipedia. You can see I'm calling this um retrieval module. We define a signature takes in a question, outputs an answer, and then we can just give it to DSpi.react. And that's going to take care of the um memory, the loop under the hood, give it some tools, give it max iterations, outputs an answer. So this is just a very brief tour of some of the things that we can do inside the spine. Let's move back to the slides and we'll take a look at perhaps a more interesting topic. Now we have a fundamentals. Let's look at LLM judge optimization and how we can do that. So firstly, let's back up a little bit

**[18:35](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1115s)** and try and understand the problem that we're trying to solve. So let's say we have our baseline main program to optimize which is defined as a DSPI module in much the same way as we define those modules in the code that you saw just now. We first need a small labeled data set of examples which contain representative inputs that we as developers hopefully have. Uh we run them through our baseline model and then we really need to have some subject matter expert guidance to label these these examples. Maybe we only need perhaps 10 to 100 labeled examples, but this is be going to become our gold standard data set which we use to align our LLM judge and then we're going to use our aligned LM judge to optimize our program. So ideallymemes will be labeling these with a numerical score like yes or no.

**[19:24](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1164s)** For example, this output is good, this output is bad. A like score also works well. Typicallymemes will need to work together with the developers to come up with a score that makes sense for the given problem. So once we have that and our small labelled data set, we then can align an LLM judge to these labels, which means that we can be confident in that LLM judge's ability to act like a subject matter expert. And then we use that LLM judge to align our or optimize our main program. Here's a little more detail about what we're going to do. First, we'll start with aligning the judge using MER Prov2. So, we're going to build our judge module. And I'll show you in the notebook the simple uh program that we're going to use for for that. Um, and our metric will be human versus judge alignment based on our theme labelled data set. Then we're going to optimize

**[20:12](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1212s)** the task. The align judge becomes the metric. And then we run Jeffer on the Asian prompt to actually improve the baseline prompt for our main system. And in reality, we should expect prompt drift or theme disagreement. And this is okay because now we have this system set up. If we ifmemes initially disagree with what the output of our um aligned judge is, we can have them reabel the data set and then we can just rerun the optimization on the reabelled data set and realign the LLM judge and that prevents us from having to tweak prompts and yeah take core takeaway as we'll see is that merov2 is a good algorithm for building the judge and jer will measure with it. So what is MIROV v2? As I mentioned that stands for multi-prompt instruction

**[21:00](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1260s)** proposal optimizer v2. It's just a basian statistically guided search over uh example few shots and instruction candidates. So it jointly optimizes a module's instructions and few shot examples. When I say instructions, I mean system prompt and fshot examples are just examples from the training data set that are can be used by the model to understand what the problem actually is. And this fits the judge alignment well because typically this uh algorithm needs a small labeled data set. So between 30 and 100 labels, it helps to have a clean numerical score and for LLM judges, few shot examples matter a lot and merov2 can generate those few shot examples. So what it's actually doing behind the scenes is an optimization process in two steps. So the first step

**[21:50](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1310s)** is it's going to generate some candidate instructions and some candidate fshot prompts or few shot examples. It will generate the candidate examples from a training data set that you give it and it will generate the candidate um instructions from having a proposal LLM look at the code that you've written and also look at the structure of the training data set and come up with a bunch of different ways of asking the same question basically. So now we have this big configuration space that contains both a bunch of examples and a bunch of um instructions and we're going to run the main program on mini batches of our uh training data set which and then we're going to basically sample from that configuration space. run the main program or many mini batches of our training data set and we're going to

**[22:37](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1357s)** send those results to the optimizer and the optimizer is actually going to learn that there are areas in this configuration space that have higher probability of being of giving us correct results on the metric and over time the optimizer will learn to sample from those parts of the configuration space more and more and the hope is that that will end up giving us an optimal set of instructions and an optimal set of fshot examples that will be used in the prompt. So let's take a look at how this actually works inside the notebook. First, there's going to be a little bit of context to explain what the problem we're trying to solve is. Great. So here's our notebook. So this is a very simple problem that um I've set up to help us understand me v2 and also Jeeper and it's taken from an example in the DSP tutorials. So you can

**[23:27](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1407s)** go online to DSpi website and see a version of this uh example too. So we have some enterprise facility maintenance messages. These are messages that come from maybe emails. They might come from sort of inspectors of some sort of u facility and they need to be classified into low, medium and high urgency. So again very simple but we're saying that as an engineer we don't really understand what low, medium and high means. that's in the data set. But we want to have the algorithm actually learn um this information and put it into the prompt for us so that we don't have to go and try and learn what these means from the data set or have to talk to a lot of uh busy people to understand what these things mean. So this data set is actually coming from uh Llama Prompttops open source library. So you can download it here. um it's just has 200 examples and there's some

**[24:16](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1456s)** ground truth available too which is helpful for us because we can use that to simulate uh labels. So I downloaded the data set and I'm splitting it into train validation and test. Okay. So now I'm setting up the model that's actually going to be used for this classification. Uh for this example problem I'm using data bricks uh meta lama uh 3.18b. Why am I using that simple that model? Um that's because for a demo it's very fast. Uh if you actually run this on data bricks free you're unlikely to run into rate limits which is helpful for uh testing. And also frankly a larger model is so good at this problem that we can't actually see a very good um change in the in in the prompt. So that's why I'm using um llama 3.1. But of course in re reality you would use whatever model you wanted. Setting up with setting it up with DSPI

**[25:06](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1506s)** uh with some uh parameters here. This is just an example of one of the messages in this data set. So it has a subject. It has um some description. It's been truncated here. And then there's an there's a gold answer associated with it in the data set. So there's a lot of information that we can use. Um in this problem, we're actually only taking care of the the urgency, but we have categories, we have sentiment, um and we have urgency. Next thing we're going to do is define our baseline urgency classifier. So again, the idea is this thing is going to take in one of these messages and it's going to output an urgency uh response which is either going to be low, medium, or high. And our baseline prompt is super simple. Classify the urgency level of a facility support message. We don't know anything about these messages. We're actually going to use DSPI to fill in what this prompt

**[25:54](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1554s)** should be. We set up our module, which is just going to be our urgency classifier. we define a chain of sort um which is the the the module we're going to use to actually call the LM and then here's our forward method where we're going to run classify and generate the prediction. This just a quick check to make sure that it works. And now we're going to do a baseline evaluation. This is um important to do when we have a baseline model. We have our uh gold standard data set. We want to see, you know, how good is this baseline classifier? Can we improve on it? And we can do this using DSP evaluate. So we're going to give it our validation data set, our urgency match uh function which is just this. And then we can uh tell it to run and it's going to generate a baseline score which is about 70%. So 70% accuracy of

**[26:42](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1602s)** the baseline question is can we improve that? First thing we need to do to try to do that is to build our judge alignment data set. So again just to recap why do we need an LM judge? We're going to use the LM judge to uh decide whether or not the output of the main program is correct. So the LM judge's result is just going to say like yes or no, this classification is correct. So we can build our alignment data set like this. We take all of our examples in our training data set. We run them through the classifier. We need to extract the gold standard answer which is helpful to have in this data set that's simulating SM labels that we already have. um we generate this human score which is just one if the predicted urgency matches the gold urgency or zero otherwise and we put that into our

**[27:31](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1651s)** alignment data set. So now we can actually run um our judge on the alignment data set to see how accurate our judge is because of course ideally our judge would be fully aligned with the data set. They' be 100% match between the judge's output and the human output. But that's not always the case when we've built a judge from scratch. So we need to first align the judge to the human output. Let's define the judge. Usually with LLM judge, we use a more powerful model than the the model that's running the main program. So right now we're using data bricks gemma 3 uh 12b. Here's our judge signature. So again, this is a very naive judge prompt, but you know, you're evaluating whether a predicted urgency level is correct for a support message. Output one if it matches what the message warrants. output zero if it's

**[28:20](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1700s)** wrong. Takes in a message. Uh takes in predicted urgency. Outputs a score. Here's our custom judge module which is also doing chain of thought outputting the result. So let's evaluate this against our judge eval set. And again the whole purpose of this is to try and see how well aligned the baseline judges. So the baseline judge has about 81% accuracy. So again very small data set here but this is just giving an example of how what we would do in practice to see the alignment of a baseline judge. So question is can we improve on that with mro v2? Meo v2 is easy to set up. You just need to give it this judge alignment metric uh function takes in example takes in prediction outputs uh whether or not the

**[29:07](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1747s)** prediction is equal to the human score and then here we are setting up the merov2 algorithm. There are some other hyperparameters that you can play with in here if you're running a more uh in-depth optimization. For the demo, I kept the auto setting as light, which is just reducing the number of the maximum number of iterations that the optimization has. Uh but if you're running this in practice, you might want to experiment with those. And then to run this, it's pretty straightforward. You give it your module, which we defined as the LM judge. We give it our training data set, and then we run it. And in this example, this should take like 1 to two minutes to run since the data set is really small. And then let's look at the results. So this again is not particularly exciting. Uh we have a lifted about 5% in our result. And we should not be too surprised because data set's really

**[29:54](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1794s)** small. Uh we didn't spend a lot of time tweaking parameters. Um with the with a larger data set, I would expect uh a much more significant lift in accuracy. But what's more interesting is what the ME Pro V2 has actually done to the prompt. Um, which is helpful for our learning about what is going on behind the scenes. So the original judge prompt was this very simple. And then the optimized judge prompt, you can see some of the things that Mev2 has done. So it's quite precisely defined what our score should be. And it's also quite precisely defined the reasoning loop that the LLM needs to go through in order to generate a score. And it's also produced some demonstration examples which are these few shot examples which help the judge perform better on the data set. So there are two reasons why this is powerful. Again for a larger training data set I would expect a much

**[30:45](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1845s)** bigger accuracy lift and also the consistency of this judge is going to be a lot higher. If you were to run this judge on the same data set over and over you would probably get quite a wide distribution of accuracy results. Whereas if you run this optimized judge, especially because it has the few shot examples, the distribution of accuracy you would expect to be a bit tighter. So that helps with reliability of the judge, which is very important. So that's showing how MER V2 can be used to optimize a judge. Let's skip back to the slides and then we can take a look at how we're going to use this judge to optimize our classifier using JPE. So as I mentioned earlier, Jeer stands for genetic parto algorithm. Pareto algorithm or Pareto I guess if you pronounce it correctly. Um and that involves uh candidate prompts over

**[31:34](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1894s)** multiple iterations. So what is genetic PTO algorithm? This is in some ways similar to mero v2 in that it has a pool builds a pool of prompts but the way that it evolves the prompts um is quite interesting. So it has this this pool which is called the Pareto frontier. And the idea of this is that every prompt in the PTO frontier is better on some examples from the validation data set than all of the other prompts but not materially worse than the other prompts. So you can imagine if you have this pool of prompts which have this characteristic, you'll end up with a situation that you have prompts that are very good at particular tasks as defined in our validation data set but not materially worse on all of the other tasks. And the idea of the algorithm is that we're going to evolve

**[32:21](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1941s)** this um this pool forward to get prompts which are better and better. And how do we do that? So we're going to use a reflection LLM which is going to look at the traces from our main program and it's also going to look at feedback that comes from our LLM judge because remember JLM judge can use chain of thought so it can generate feedback and this reflection LLM is actually going to attempt to design new candidate prompts for the par the PTOA frontier that fit um the the characteristics that can actually learn from examples in the training data set that have failed. So we can add these prompts to the PTOA frontier and then we can run the candidates on a validation data set and check whether it has this characteristic of being better at at least one of

**[33:09](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=1989s)** examples and not materially worse on the rest. So that's one of the main ways that uh Jepper evolves prompts. There is another way called an evolutionary loop where we're actually instead of developing a new prompt. We're going to take uh examples from the PTO frontier and we're going to uh merge them in a way that is inspired by a genetic algorithm to try and capture some of the characteristics of these prompts. So there are two methods that um Jeeper uses as it's doing its iteration. And at the very end, we have this pool of prompts and we collapse them down to one single prompt that performs the best on a validation data set according to a metric that's been that we've provided. And this is a very powerful algorithm for uh aentic applications that have large traces because it can make use of all of the

**[33:57](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=2037s)** natural language traces and also any LLM judge results that are occurring on those traces to improve the prompts. So, we're going to finally go back to the notebook. one final time to take a look at what Jeppa does to our uh classification problem and how it's improved or it's going to improve our baseline prompt. So we're going to go back here to step six which is called optimize the classifier with with jeer. So again similar to me prov2 because we're using dspire here it's easy to set up this algorithm we need to define this judgebased metric as I said before it takes in an example takes in a prediction if we have something more complicated we can actually give it the the entire trace that the agent is running for example and then it's going to run our optimized judge so remember

**[34:44](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=2084s)** in the previous step we built this optimized judge it's got that prompt that's been created by mro v2 now we're using that optimized prompt with jeer to update the main program and we're going to use the judge's feedback which is important for Jeeper and we're going to output the prediction which is just the judge score one or zero does the judge think that the output is correct and the feedback what why does the judge think that the output is is correct or not now we're going to set up jer give it our metric again we give it our optimization budget there's more hyperparameters to play with here definitely recommend reading the um API docs if you're interested in playing with it and Then we define our reflection LM. Remember the Jeeper is using this reflection LM to craft the uh updated prompts. In this example, I'm using GPTOSS120B.

**[35:33](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=2133s)** It's common to use a reflection LM which is more powerful than the one that's being used for the main uh the main program. preferable good to set the temperature value a little bit higher here so that we get more variability on the reflection LM because ultimately we're trying to add some examples to this PTOA frontier and then we're just going to run the optimizer and produce the program. So once it's run we can compare the results. So now we can actually evaluate we can run our optimized program and look and compare it to what our initial result was. So our optimized program is really good on our validation data set. So again just recall our validation data set was this very simple classification predicting urgency on um facility messages. So we jumped from about 70% to

**[36:23](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=2183s)** almost 100%. So you might think that's surprising um maybe something has even gone wrong here because this is a a big jump in accuracy. Um, but if we actually look at the prompts, we can see why the accuracy is improved so much. And this is maybe a little bit unique to this simple classification problem, but it gives us a taste of what Jepper can do in practice. So our original prompt was really straightforward and quite naive. But what Jeeper has done is it's analyzed all of the training examples that we have and it's used the LLM judge to decide whether or not the answer is correct from this original prompt. and has actually built this table for us. So it's able to build a prompt where it can understand what low, medium and high urgency actually mean in the context of this data set. Uh and this has been

**[37:11](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=2231s)** built just by learning from the data. Um and so this is probably why the result is so good now is because um our validation data set comes from the same distribution as our training data set. Obviously, it's not the exactly the same um in examples, but it's the same type of example, and we've learned what low, medium, and high urgency actually means. Um, and that is very powerful because we haven't had to go out and talk to people who know what this means. We've learned it from the data. And this is a really powerful uh first prompt that we can then use um to to work on. And of course, if something changes, we can then reoptimize, run it again, and it will generate a different prompt that we can use downstream. Great. So that was our tour of Jeffer and now we can wrap up by just answering a few questions recapping what are the things that we've learned and taking a

**[37:58](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=2278s)** look at some future resources. So I think there are five main things to take away uh from this talk. First is don't reach for prompt optimization without subject matter expert labels. It is very important to build a small data set of labeled examples before you start. You might have this baseline model. run some representative examples through the baseline model, generate some output and then interact with subject matter experts to see how good the baseline model is. And that is where we start. Uh 30 to 100 labeled pairs is often enough to optimize an LLM judge. And then once we have an LLM judge, a baseline LLM judge, we can then use something like MEROV2 to optimize the prompt of that judge and align it to the subject matter expert label data set. And we should expect that that process

**[38:48](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=2328s)** takes some time. Obviously, the alignment is fast, but they may to be may then need to be some discussion with subject matter experts and maybe they'll disagree with the labeling. And if they change the labeling based on what they see in the aligned judge output, that's also fine because we can realign the judge automatically using this this method and we can um develop an LLM judge that we trust. And once we have an NLM judge that we trust, the rest is maybe more automatable because we can plug this in as a metric in our optimizer which is going to optimize the main prompt for us of our of our program and then we can run it and generate uh a main prompt that is uh that is works well with our data set. And this is going to work with aentic applications too. If there are multiple different

**[39:34](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=2374s)** prompts in there, the uh the Jeffer algorithm is actually going to modify all of those prompts to try and maximize the val the the metric on the training data set. One thing that I didn't talk about which is important for teams that are working on this is obviously budget for token cost and latency. These optimizers are using LLMs. Um, and so there is a compromise and a trade-off between the amount of optimization that we that we do versus the amount of manual iteration. Um, and depending on exactly one the problem, we may find that we need to be careful about our our token and latency budget. But just something to be aware of when we're using these optimizers. And again, takeaway there are many different optimizers out there. Meipro v2 is one that I've seen works really well for building LLM judges. There's another one that is called memal

**[40:21](https://www.youtube.com/watch?v=1EGh_ShqWHc&t=2421s)** line which was built recently um at data bricks. I believe it's available in ML flow. So that one could also be useful for building LLM judges. And then Jeeper um is at least in my experience been a really good one for optimizing uh the main task using an LLM judge. So yeah, that's the end. Uh here are some resources. Feel free to check out the notebooks. If you want to actually see the notebooks, just contact me. My email address is there. you can reach out to me on LinkedIn. Um I'll send you the notebooks. You can take a look and then uh these these are helpful documents as well for further research. So thank you again for your attention.

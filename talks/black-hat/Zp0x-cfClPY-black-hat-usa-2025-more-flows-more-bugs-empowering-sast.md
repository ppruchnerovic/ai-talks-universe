---
id: Zp0x-cfClPY
title: "Black Hat USA 2025 | More Flows, More Bugs: Empowering SAST with LLMs and Customized DFA"
slug: black-hat-usa-2025-more-flows-more-bugs-empowering-sast
conference: black-hat
conference_name: "Black Hat"
category: "Security conferences"
edition: "Black Hat"
year: 2026
speakers: ["More Flows"]
channel: "Black Hat"
duration_min: 25
published_at: 2026-03-31T14:54:05Z
video_id: Zp0x-cfClPY
url: https://www.youtube.com/watch?v=Zp0x-cfClPY
youtube_url: https://www.youtube.com/watch?v=Zp0x-cfClPY
tags: []
topics: ["Security, safety & red teaming"]
transcript: true
---

# Black Hat USA 2025 | More Flows, More Bugs: Empowering SAST with LLMs and Customized DFA

**More Flows**

`Black Hat` · `Black Hat` · `2026` · `25 min`

[Watch the recording](https://www.youtube.com/watch?v=Zp0x-cfClPY) · [Conference site](https://www.blackhat.com/)

## Description

Static Application Security Testing (SAST) plays a significant role in modern vulnerability discovery. For example, GitHub uses CodeQL to scan repositories. However, our analysis of over 100 real-world vulnerabilities has revealed that its detection performance is limited by two main factors: 1) incomplete source and sink coverage in built-in propagation rules, and 2) disruptions in data flow due to insufficient support for certain language features. In this talk, we will introduce a framework to empower SAST tools' capabilities to identify previously undetectable vulnerabilities and new CVEs.

First, we will demonstrate how to leverage Large Language Models (LLMs) to automatically identify sources and sinks from open-source frameworks. Second, we will introduce the implementation principles of CodeQL's Data Flow Analysis (DFA). By developing patches for the DFA's QL language library, we have addressed language feature challenges, including Java reflection handling, partial native method support, and value passing model optimization.

Our enhancements support 191 sources and sinks across 18 frameworks. Through comprehensive verification of over 5,000 repositories, we identified a more than 15% increase in data flows when utilizing existing rules, compared to results without the enhancements. Additionally, we reproduced over 50 historical CVEs that were undetectable by the original CodeQL due to a lack of language features support. Our research also uncovered 5 new CVEs (e.g., CVE-2024-45387) that the original CodeQL could not detect. We believe our work will greatly empower the detection capabilities of SAST tools.

By:
Yuan Luo  |  Senior Security Engineer, Tencent Security YunDing Lab
Zhaojun Chen  |  Senior Security Engineer, Tencent Security YunDing Lab
Yi Sun  |  Senior Security Engineer, Tencent Security YunDing Lab
Rhettxie Rhettxie  |  Senior Security Engineer, Tencent Security YunDing Lab

Presentation Materials Available at:

## Transcript

*3,168 words · source: supa (en, exact timings)*

**[0:02](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=2s)** Good afternoon everyone. Thank you for being here. I'm Yuan from Tencent Security Winding Lab and today I will show you how we are enhancing static code analysis using large language models and customized data flow analysis. So first I will introduce what Sust is. Next, how we can use LMS to automatically identify source and sync functions. Then our customized enhancement to data flow analysis. And finally, the results demonstrating the effect effectiveness of our approach. What is SAS? SAS means static application security testing. Using SAS tools, we can analyze source code

**[0:50](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=50s)** without running the application. For example, detecting SQL injection bugs. From the picture on the right, we can see that SAS is now an essential part of DevSec Ops, automating security checks. Popular tools include CodeQL, Fortify, and so on. What is CodeQL? It's now a product under GitHub. queries and the libraries are open source under the MIT license including DFA DFA part but the core engine uh is preparatory. The workflow of code code has four steps. First download the source code of the project. Then create a database using code command and then write a QL

**[1:40](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=100s)** query and run it against the database. And finally the query results show whether there are any bugs. And on the wall of fame of code co we can see all the bugs uncovered by uh code co. It's not a complete list but so far it has found 418 bugs and [snorts] the number is still increasing. Also on GitHub, we can set up our projects to run code L scans either on a schedule or whenever there's a poor request. So does it mean no bugs anymore? We tried using cautical l to detect recent high-risk bugs uh like the ones listed in this table. Uh we found there were a lot of false negatives. We

**[2:30](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=150s)** analyzed the root causes as listed uh in the last column and found two main reasons. First is incomplete source and sync coverage in built-in propagation rules. Second is disruptions in data flow due to insufficient support for certain language features. For the first problem, I will introduce how to use ALMs to recognize source and sync functions. We have noticed that current methods rely heavily on manual work. One approach is manual definition where developers have to manually review the source code to identify sources and syncs. Another way is through community contributions like codeql has default rules. From the picture we can see that key information includes library module

**[3:21](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=201s)** uh function name. uh but we have found both methods are pretty labor intensive. So is there a way to automate this process? First problem is where should we look for sources and syncs? We have found that source and sync functions are actually API functions from thirdparty frameworks. Uh from the picture on the left we can see that uh SSI sync uh actually is an API in resty framework and the implement implementation code for these API functions can be found in open source frameworks. So we can detect sources and syncs by scanning those frameworks. The second problem is how ALMs can help. On the right is the workflow of our

**[4:09](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=249s)** method. It has three AI agents. We first use the discover agent to identify potential source and sync functions in the framework. Next, the judge agent applies expert rules to check the functions and remove results that don't align with expert experience. Finally, the validation agent runs queries to verify that the functions are used in real world projects. First, we run a cog grant detection at the file level. feeding the source code fire and the propumpt to the model. In the propt, we mainly describe the characteristics of the functions we need to identify which are related to the programming language and the bug types. For example, for SSIF bucks, the sync functions are typically those sending

**[4:57](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=297s)** HTTP requests. And on top of that, we can set constraints on the format and style of the response. And you can try different performs to get the best results. And this is just a reference. And for the LM's result, we'll remove results with low confidence scores. And the threshold can be adjusted depending on the prop prompt and the model you use. And in the end, we'll get a list of source and sync functions. To remove the false positives from the previous step, we move on on to function level filtering. We check both the function names and the function code. For example, with iss the sync is a function that sends an HTTP request while for SQL injection the sync is

**[5:46](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=346s)** executing an SQL statement and we have noticed that long thinking models tend to perform better at this stage and again any functions that don't meet the criteria will be removed. uh in the end we collect all the source and sync functions present in each framework. Besides having the ALM directly scan, we also need to integrate expert experience the experience comes from the community's longstanding practice. For example, the function should be publicly accessible. Uh the function should have return values that propagate tented data and the function should uh the return value of the function should not be of the boolean type and there are plenty of

**[6:34](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=394s)** these expert rules and again they can be added or revised based on different bug types or 10 types. uh and we'll use the ALM to apply these expert rules to check the function name and function body here and removing functions that don't match the uh expert rules. The final step is confirming that the source and sync we have noticed uh identified are actually used in real projects. First, we have to head to the framework project homepage and find the dependent page. And this page lists most of the open-source projects using that framework. And next download the source code of these projects sorting them by star count from highest to lowest. And

**[7:23](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=443s)** finally run a sust tool like code with the new source sync rules applied to scan these projects. And based on the scan results, verify if the newly added source and sync is actually used. And any source or sync that isn't used will be removed. And for the second problem, I'll introduce our enhancements to DFA data flow analysis. What is DFA in code L and other SAS tools. Uh on the left is an example of data flow analysis in code l. It works by extending data flow configure sik here and implementing the is source interface at line 10 and a sync interface at line 16

**[8:14](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=494s)** and then calling flow path to perform a global tent analysis at line 27 here. So on the right are the results. From the results, we can see a list of numbered path nodes. And this list represents a complete data flow path. And each path node has two parts, the node and the access path. So how does the whole process work? And this diagram shows the workflow of DFA. First, CodeQL provides the config secret user interface and you can call the flow path API just last uh previous slide and then there are two key concepts node and access path. Node is calculated through

**[9:06](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=546s)** forward flow and reverse flow. Forward flow is the forward propagation from the source point while reverse flow uh goes the other way tracing backward from the sync point and together they form a complete source to sync uh path and access path is calculated in five stages from stage one to stage five. Each stage adds further details to produce the final result. We use forward flow to illustrate how node is calculated. On the right is the diagram of code source code which consists of different steps such as jump, store and details are in the table. First is the source step which sets the starting point for data flow.

**[9:56](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=596s)** Next the local flow step which handles intrarced analysis and then the jump step. It provides the interface for custom propagation from one node to another. There are two interfaces. A user level interface through its additional flow step and a system level interface through additional value step. And then comes the store and load step. Store is writing a tentative node into a field, array, collection or map content while load is reading a tented node from them. And this is the core of access path. And finally, call in, call out and call through steps uh which deal with interprocedural analysis. Call in

**[10:46](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=646s)** handles propagation from actual arguments to formal parameters. Call out and call through deal with propagating the return values. And the difference is whether the return value is propagated from external sources or parameters. And notice that post update note here represents nodes that are implic implicitly updated as a result of a function call. So what is access path? It consists of a content list and a type. Content is basically a way to describe how data can be stored inside an object and details are in the table on the left here. So for a field type, its content is simply the field name. For other types, uh it's

**[11:35](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=695s)** more of generalized result as shown in the table here. and access path keeps track of the content propagation relationships for the four types of nodes field array collection and map during store and load steps and the table here shows examples of access path. So for example in the this row uh the content is name with the type string then how to calculate the access path. So on the right here we write a sample code. Uh from the table you can see how the intermediary value access path changes at each stage. At the beginning at in stage one there's no AP at all.

**[12:23](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=743s)** And at stage two AP becomes a boolean type indicating whether a content operation exists. And at the last we find that the complete access path is a content list here and a type. And after an overview of DFA now we introduce about some of the challenges it faces using co Java as an example. So static analy tools to tools include code face challenges such as cross thread issues reflection and value passing. So here is an example of cross thread analysis. The parameters of the static stat static main method are the source point and the print line method

**[13:12](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=792s)** parameters are the sync point. So on the left here is the code and on the right is the intermediate results of the static analysis. And as we can see in the default analysis we can't find a path from source to sync. It breaks at the constructor call of runnable demo at line 14. So how do we connect it? Remember the jump step we previously introduced. We can use jump step to jump from the runnable demo call to the instance parameter of the run method namely the this parameter and after the patch we get the complete path. So to summarize, when the tent is propagated

**[14:01](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=841s)** uh through the constructor of a runnable uh instance, our approach is to jump from the constructor call to the run method. And this is the implementation using the addition additional value step interface and we complete the jump step here. But what if the tent isn't passed through the constructor uh such as through assignment statement in a function call in at line 8? How do we handle this? Our solution is that if the 10 exists before the start interface is called like line 17, we take the caller of the start method and jump to the

**[14:50](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=890s)** instance parameter of the run method. So from here to here. So in in this case jump from the line 18 to 10 and after the patch we can get the complete path. And what if the 10 comes after the start interface in this case line 23. Our approach is to find a node matches three conditions. First this node is a post update node. Second, this node is a runnable instance. Third, this node uh has a store operation. So we jump from the that node to the instance parameter of the run method. So in this case, jump from line 8 to line 10. And after the

**[15:41](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=941s)** patch, we can see the complete flow is detected. Next we'll introduce the second challenge, Java reflection. and take the invoke method in this code as an example which is line 13. Uh we are facing two difficulties here. First, how do we pinpoint the method that invoke actually correspond to? So [snorts] namely what is the method at line 13? Second, how do we fix the propagation rel relationships for interprocedural cause like call in uh and call through. So from the red line we can see that the parameter propagation logic is different from the normal core process. For challenge one there are two

**[16:30](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=990s)** solutions. One approach is to track the method instance as illustrated in this diagram and the other is to determine the method based on the number and types of parameters in the invoke method. And we have implemented both methods and note that this tracking requires global data flow analysis. The second challenge, how do we make reflection analysis work within CodeQL's data flow? We mentioned earlier that additional value step can define edge to connect to nodes. But here's the problem. You can't use data flow inside additional value step because doing so would cause a non-monotonic recursion issue. Basically you need data flow

**[17:21](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=1041s)** interface in additional value step but the data flow interface depends on additional value step. So as illustrated in the diagram. So how to fix this? We can we can create a copy of the data flow analysis implementation and have our reflection analysis rely on the uh copied version as illustrated here. And then we patch the original data flow here to avoid any dependency issues. And to support the invoke method, we fix the parameter propagation logic for call in and call through. For example, uh for co in argument propagates to parameter and object propagates to the parameter this.

**[18:13](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=1093s)** And here's an example for reflection. Firstly, the data flow breaks at the invoke method at line 13 here. And by adding reflection analysis as shown in steps one to four, we can propagate from tent s here to tent object here and thus we get the complete path. And next pass by value in Java. So first what is uh pass by value? Uh in programming languages parameters are generally passed either by value or by reference. And in Java, the way actual parameters are passed to methods is passed by value, not passed by reference. So if the parameter is a primitive type and what gets passed is a

**[19:03](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=1143s)** copy of the actual value of the primitive type and a copy is created. And if it's a reference type, what gets passed is a copy of the address value in the heap of the object referenced by the actual parameter. And just like with primitives, a copy is created. So what's the problem? And on the right is a case where a copy is stored in a field. But the problem is there are many copies like a person P and D do a here in this example. So the core issue is that when the value is modified in one copy, always copies need to be updated. So in this case it is the par parameter person P uh in the constructor of demo

**[19:54](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=1194s)** field instance here the copies are indicated with the blue line here and here's our solution. So first we have to locate the field which is of a non-peritive type and in this example it corresponds to field A. for this field finance store operations identifying both non-post update node and a post update node. So in this example sorry uh uh it it runs to line seven and line 11. Line seven is a nonpost update node while line 11 is a post update node. And for non-post node store operations use global data flow to

**[20:45](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=1245s)** locate the parameter and the actual argument. So in this example and as shown it in the red dotted line here we located the parameter P here in the demo field constructor. And for another store operation of a post update node, we add a mapping from this node uh to this node. So this is the jump step we have previous introduced and we have to use global data flow because we might encounter scenarios like interprocedural cause uh store and load operations and so on. and then I'll introduce the results of

**[21:38](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=1298s)** our research. Uh we have found about 190 source and sync functions across 18 Go frameworks. We also scanned over 5,000 projects and found that the detected data flows increased by more than 15%. And we use this CVE we found to show how a new sync could lead to a detection. And this is a SQL injection bug in traffic ops uh part of Apache traffic control project. Uh this project has Q code enabled here. So uh uh which means it is a force negative for code. And here's the data flow of the buck. First step is to read the parameters

**[22:26](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=1346s)** from user input here which is a user's comment and then process and validate the requested parameters as illustrated here and this info.go go file and finally when inserting the user's comment into the database here here it calls the query row x function but when we checked code's uh detection rules we noticed that query row x function isn't included in the sync model and we scan the sq x function framework and found this sync as illustrated here and thus we detect the bug and with our enhancement we can detect

**[23:15](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=1395s)** historical CVEes that were previously undetectable and in this case adding the uh source function enables the detection of three CVEEs several open source projects are affected and for this CVE we fixed cross thread and messing sync role and thus we can reproduce this CVE and this CV proves the effectiveness of our reflection enhancement and we also found many new bugs and here are some cases below and finally are our takeaways. So first is that semantic analysis of code in SAS is particularly suitable for LLM

**[24:03](https://www.youtube.com/watch?v=Zp0x-cfClPY&t=1443s)** assisted analysis and their combination is a research direction. Second is that code's data flow analysis mechanism is highly representative serving as a good start for learning data flow analysis. And third, CodeQL's data flow analysis is not perfect and can be studied, modified and improved. So any questions and also please feel free to reach us through the emails here. Okay. Thank you.

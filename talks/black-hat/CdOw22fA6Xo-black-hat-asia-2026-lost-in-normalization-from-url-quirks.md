---
id: CdOw22fA6Xo
title: "Black Hat Asia 2026 | Lost in Normalization: From URL Quirks to Poisoning the Azure Supply Chain"
slug: black-hat-asia-2026-lost-in-normalization-from-url-quirks
conference: black-hat
conference_name: "Black Hat"
category: "Security conferences"
edition: "Black Hat"
year: 2026
speakers: []
channel: null
duration_min: 28
published_at: 2026-08-16T19:00:15Z
video_id: CdOw22fA6Xo
url: https://www.youtube.com/watch?v=CdOw22fA6Xo
youtube_url: https://www.youtube.com/watch?v=CdOw22fA6Xo
tags: []
topics: ["Inference, serving & GPU infra", "Science, healthcare & applied ML", "Security, safety & red teaming"]
transcript: true
---

# Black Hat Asia 2026 | Lost in Normalization: From URL Quirks to Poisoning the Azure Supply Chain

**Speaker not identified**

`Black Hat` · `Black Hat` · `2026` · `28 min`

[Watch the recording](https://www.youtube.com/watch?v=CdOw22fA6Xo) · [Conference site](https://www.blackhat.com/)

## Description

We transformed a single, isolated container in Azure Container Instances (ACI) for GPU into a full tenant-isolation breach and a critical supply chain attack. This talk details the multi-stage exploit chain that broke a seemingly secure, multi-layered design.

It all started with a misconfiguration that exposed a container image for a highly sensitive internal Kubernetes component, effectively handing us its source code. We will demonstrate how we chained two novel vulnerabilities, discovered in this internal code, with advanced Kubernetes techniques to achieve critical service compromise.

First, we achieved full cross-tenant credential theft, allowing us to steal secrets from other tenants' private container registries. More critically, we escalated this access to gain write permissions to a shared, production container registry used by ACI itself. This would have potentially allowed an attacker to poison trusted, official ACI images, creating a platform-wide supply chain attack.

Shortly after, following the disclosure of these critical vulnerabilities, the service was retired. This Briefing serves as a case study in how a single flaw can undermine a robust security design. Attendees will get a rare look into the internal security architecture of a major cloud service and learn to identify and defend against similar platform-wide vulnerabilities.

Nir Ohfeld  |  Head of Vulnerability Research, Wiz
Ronen Shustin  |  Security Researcher, Wiz

## Transcript

*3,687 words · source: supa (en, exact timings)*

**[0:08](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=8s)** Hello everyone and welcome to last enormalization from URL queries to poisoning the Azure supply chain. Today we're going to show you how we were able to leverage a chain of what see were seemingly low severity vulnerabilities to one critical chain to allow that allowed us to escalate a regular customer pod at ACI for GPU to cross tenant access and overtaking the services supply chain. So without any further ado, let's begin. The clicker doesn't work. Let me just check. Great. Okay. So my name is Neld and here with me on stage is Ren. We are both

**[0:59](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=59s)** security researchers from WH. We are based in Israel and we specialize in cloud security research. But actually more recently in the last two years we actually shifted our focus from cloud security research to AI security research. And what does that what does that even mean? It means looking and finding vulnerabilities in the entire AI life cycle. From the training stage where we were able to uncover 38 terabytes of AI researchers training data to the AR services themselves where we were able to gain access to an internal deepseek database to overtaking AI as a service services such as hugging face replicate and SAP AI core to finding critical vulnerabilities in AI

**[1:51](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=111s)** infrastructure open source libraries such as invigoron and invainer toolkit. But after looking and finding all these vulnerabilities, we thought, what if there's a level deeper? Something beneath all of this that is in common with everything related to AI. And thinking about it for just a minute, actually, the answer became quite obvious. GPUs. GPUs are everywhere when it comes to AI. You literally cannot do anything when it comes to AI without having access to a GPU. However, GPUs are expensive and hard to get. And this is the reason why most customer actually opt into GPU as a service services such as core, lambda,

**[2:41](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=161s)** perpace, runod and many many others. But actually most customer go to the flagship cloud services such as AWS, Microsoft Azure and GCP. And for this research we we decided to focus on Azure. [snorts] Azure actually offers a very convenient GPU as a service in the form of Azure container instances. You as a customer can basically spin up a new GPU container in minutes. It's a container as a service, meaning that you don't need to worry about infrastructure or managing clusters. You can strictly focus or your AI or ML work. It's that easy. You can basically run the following command and get yourself a new GPA container. And this is the exact

**[3:29](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=209s)** command that most AI or security researchers will will use. But Rene and I, we're not AI researchers. We are security researchers. So naturally this is the command that we used a very similar command. However this one also invokes a reverse shell. So we ran the following command waited for a couple of minutes and boom we got a reverse shell from the container hosted on ACI. As you can see we are running as root. Okay. So at this point of our research we don't know a lot. We know that we have our container that probably runs in some sort of a dedicated virtual machine. And that's pretty much it. Okay. So, next we inspected our environment variables but

**[4:18](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=258s)** couldn't find anything interesting. Maybe the mount list will have some interesting files. Nope. Okay. Then we try to actually escape our container to the host. However, the container was hardened and had no capabilities that allowed us a straightforward way to escape our container to the host. However, something very interesting did came up during a recon phase and is that we are not running in just an environment. We are running inside of a Kubernetes environment which means that our container is not just a container. It's Kubernetes pod and our virtual machine is not just a virtual machine. It's a node and it runs in some sort of a Kubernetes cluster. However, we still

**[5:09](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=309s)** had more question than answers. We didn't know if we are the only node in that cluster is it shared anything about the infrastructure really. So our next step was to actually scan the internal network for Kubernetes specific ports. And this where things got interesting. We got two cublet ports. Now for those of you who are not familiar with cublet, it's an basically an agent that runs on every Kubernetes node. And this what helps Kubernetes to manage all of the pods and containers on that node. It exposes two main API ports. The read only port that allows us to list running pods and the read write port. And that port can actually be used to execute commands on inside every pod on that node. That port is usually

**[6:00](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=360s)** protected by by us. But in this case, it wasn't. So we listed all of the pods in our in all of the pods in our node and we found out there we are no longer the only pod on that on that node. There are actually other four Microsoft infrastructure pods running on the same node. These are not other customer. These are used strictly for Microsoft in order to manage that node and we are currently running here. So we inspected each parts pack and one became really interesting and it was interesting for two reasons. One it had host network 2 meaning that it shared the networking name space of the host and it had kubernetes credentials.

**[6:49](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=409s)** So naturally we wanted to get access to this port. So using the read write port uh read write port we invoke the reverse shell and once again we were running author but this time we had access to kubernetes credentials. So we begin here now we are here and we have access to the Kubernetes API. Okay with access and credentials of Kubernetes API let's start to enumerate it. First, we wanted to get all of the nodes in the cluster. Got only one outer node. Okay, let's get all of the pods in the default name space forbidden. Okay, let's get all of the pods in the entire cluster,

**[7:37](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=457s)** only the same five pods we got earlier. And running all all of the other commands basically yielded forbidden. we couldn't do anything. So either we have a very very weak service account or something is off. So in order to investigate further, we actually sent a request directly to the API server using CRL. And inspecting the response, we noticed something odd. The server managed Kestrel. What's Kestrel? So apparently Kestrel is a web server written innet. Now, I'm not a big Kubernetes expert, but I know Kubernetes is not written in .NET. So, what's going on? Apparently, Microsoft wrote their own server instead of the real

**[8:29](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=509s)** Kubernetes API. And we in order to understand better how it works, we looked everywhere for the server. We looked on Google, we looked on GitHub, we looked on VU total. we couldn't find any mentioning of this server. Okay, so at this point we actually got back to the podspec and notice that all of the pods are coming from one public Microsoft container registry mcr.microsoft.com and being a public container registry we could just list it and filter out all of the container images related to that service. And looking at the long list, one immediately popped up API server proxy. So we downloaded decompiled it and

**[9:21](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=561s)** looked at its source code and jackpot. This is the castl server we were looking for and it's written in net core. It's a Microsoft propriarity implementation and it implements a limited set of the Kubernetes API and its role is to ensure tenant isolation and restrict direct access to the underlying API server. So this actually doesn't replace the API server. It just proxies to the real API server. So now looking at the source code, we had a much better understanding of how the environment actually looks like. We pod running on our own dedicated node. But alongside our node, there are other customer nodes all under the same cluster. And in the

**[10:11](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=611s)** same cluster, we have the API server proxy that all of the nodes and pods communicate with before reaching the real Kubernetes API server. And with that I want to invite my very good friend and colleague Onen to walk you through how the API server proxy works on. >> Thank you very much Nero. All right. So let's look at the request life cycle. So let's imagine we are a regular user like an attacker or the node. We want to get our ports back. So natural will send the following request using the cublet credentials. Now when the actual API the API server proxy receives the request it will extract the name space. It will also extract the code name and the query parameters.

**[10:58](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=658s)** Now the name the extracted namespace will be matched against our authorized namespace and of course if it will not match will not be able to continue again all done to prevent cross tenant access. Then the API server uh proxy will actually reconstruct the URL again with the real Kubernetes API server. Next, a new request will be issued to the real Kubernetes API server. But here comes the interesting part. It doesn't proxy our cubit credentials, but it uses its own priv privileged service account. Weird. So when we actually receive the results back the API server proxy will get us the results.

**[11:46](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=706s)** Now what happens if we send a global API like an API that will that should give us all the post within the cluster. So when we send the following request to the APS or proxy it will again reconstruct the URI. Then we'll use again the privilege service account and then the real Kubernetes API server will actually give us all the posts within the cluster. However, when the results get back to the API server proxy, it filters the results and give us only the results related to our name space. All done again to prevent cross tenant access. Now if we look back at the request life

**[12:35](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=755s)** cycle, the proxy basically authorize and validates our namespace, then forwards all the requests to the real Kubernetes API server. Now our data passes plenty of components within the code. So we thought can we eject a malicious path or something that basically allow us to bypass the namespace check and access other tenants resources or other infrastructure resources. So our first attempt was a simple path traversal. We basically use the same command to issue to get the pots back but also instead of using our port name space we put a path traversal with a victim namespace or pod. So issuing this command against the

**[13:24](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=804s)** Kubernetes API proxy its URL decoder will decode the dot dot. Next, the normalizer will normalize the path and we will get through the API.net router. When we'll actually get to the pot controller, we'll hit an obstacle because now our name space does not match the victim namespace. So, we'll get for free unauthorized. So at this point me and you were we were were thinking how we can bypass some of those checks and get to the actual URI reconstruction. So we dug deeper in the URI class source code. We also looked at some documentations and we had plenty of ideas. So apparently URI not

**[14:15](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=855s)** always not not only reconstructs URIs but it also normalizes them and because the application is written in net core it has Windows roots. So backslash is also treated as slash. So let's see that in action. So we take the same same example with the patch traversal we did before but here we use backslash instead of slashes. So let's see what we get. So when we send the following request to the APR proxy the URL decoder decodes dot next the normalizer doesn't do anything because the cast because the cast mechanism doesn't fit the back slash session. However, when we get to the API router, we actually match

**[15:07](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=907s)** the route. But when we get to the port controller, our name space actually matches because our path reversal is considered the resource name. So, we actually bypass this check. And when we actually get to the re to the URL reconstruction, can anybody guess what's going to happen? So our path becomes normalized and now we basically control the name space and control the resource name allowing us to basically access other tenants resources other infrastructure resources in the cluster. So if we recap the path traversal vulnerability by only using backslash we can actually bypass all the proxies

**[15:56](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=956s)** restrictions and not only that we can actually invoke any API call against the real Kubernetes API server with the proxies privilege service account. This was a huge achievement on our end. So for for great vulnerabilities there are some limitations. So we could only issue get, patch, put, or delete commands, but we couldn't use post methods. Also, we couldn't list resources in the cluster. We only could fetch them by name. But when using global APIs, we only received our resources because of the namespace filtration. So we had to find a way to actually list all the resources within the cluster. So we found another vulnerability another

**[16:45](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=1005s)** bypass. So looking at the query parameters which are whit listed and actually passed back to the real Kubernetes API server we notice two interesting query parameters. One of them is the limit and the second one is the continue. Now when we issue a Kubernetes API which should return us all the post within the cluster with the limit equals one although we don't receive any results which is a bit expected but unexpected because probably the pods or the pod that returned to us is getting filtered because its name doesn't match ours but interestingly if we look at the remaining count equals 51. It looks interesting because there are

**[17:35](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=1055s)** probably other 51 pods that we still can't see. Meaning, are there other tenants in the cluster, other infrastructure pods? Interesting. So, looking at the continue token, it looks like a regular B 64. So, let's try and decode it and see what's inside. So, can anybody guess what's inside? So it looks like the namespace and the resource name of the return post exactly what we needed. So now that we have a single resource name space and resource name, how can we actually enumerate this request and get all the resources within the cluster? So Kubernetes API has this feature. Basically by sending

**[18:25](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=1105s)** back the continue token we got we actually get the next continue token for the next resource. So we can do that in a loop and basically enumerate the whole resource names name faces anything in the cluster and basically get all those names that we wanted. So here is it in action. We actually wrote a simple web script that lists all the pod names and names within the cluster and those are the results. So again all sensor results but looking at those results we not only saw our pod namespace and pod name but we only saw other tenants name space and names and also infrastructure pods name spaces and names. So to prove cross tenant access, we

**[19:15](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=1155s)** actually created two separate tenants that had two different containers with a goal to run the vulnerability from one tenant on the other and basically look if we can get access using the vulnerability to the other one's resources. So with the enumeration that we had before, we got the pod spec, the pod name and pod name space for the second pod for the second container. Next, we use the vulnerability again to actually fetch its podspec and we got it. Now looking at the potspec, we saw something interesting. It had the image pool secret. For those who are not familiar with the image pool

**[20:02](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=1202s)** secret, basically when using private coten registries, Kubernetes has to have the credentials to actually pull them from the registry and those are stored as secrets within the cluster. So we when we created the second tenant, the second container, we configure it to have credentials to our private container registry. So using our vulnerability our result vulnerability we could fetch this secret then decode it and then we actually got the conditionals for the container registry for our second container. So jackpot we actually proved cross tenant access. Now we can basically do the same and possibly access other

**[20:53](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=1253s)** tenants ports secrets and their private container registries. Now we also had other prospects of infrastructure pods. So we again looked at our image pool secret and noticed that they also have an interesting name and Azure registry. Now fetching this secret and decoding it, we got Applantex credentials to an Azure internal container registry. Now listing the images within this registry, we saw plenty of images we already knew, but those they had also other infrastructure images. Now read access might not be sufficient. So we wanted to test if we had push access to this registry without actually

**[21:42](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=1302s)** pushing an image. So we found an actual cool trick. So by sending the following request with the credentials we could actually see if we get 202 or 43 meaning either we get push access or not. And this time we got 202 meaning we had push access with those credentials. So they were misconfigured. So by getting push access, we could possibly poison the supply chain of the of the ACI GPU service and possibly do that for other tenants as well. With that, I'll pass the mic back to for summaries and conclusions.

**[22:30](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=1350s)** >> Okay. Thank you, Ran. Okay, so this research is actually one of my favorite research that we've ever done in in the team. I think it's very very cool, but I it's also pretty complex and there are a lot of moving parts. So we thought it would be beneficial to do a quick summary slide with what we did and how we did it. Okay, so we began as a regular customer in creating a new GPU container in ACI. From there we found the cublet port and laterally moved from our pod to the shared logging agent pod. From there we gained access to the API server proxy and were able to take leverage of two vulnerabilities that allowed us to basically bypass its uh protections. Using this bypass, we were able to gain

**[23:21](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=1401s)** unrestricted access to customer workloads, customer private registries, and ACI GPU the ACI GPU infrastructure supply chain. The less two impact points are the ones that uh actually increase the blast radius of this attack even further even beyond just the ACI for GPU. Now in terms of takeaways, I think that something that we could really take home here is that ah sorry responsible disclosure first. So of course all of the issues here were reported to Microsoft and we collaborated closely with the Microsoft team to investigate and remediate the issues. Shortly uh after our research the Microsoft

**[24:09](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=1449s)** actually decided to retire the ACI for GPU uh service. Also, something that came up during our work with Microsoft is that some parts of our research, mainly the post exploration parts, weren't fully aligned with the Microsoft rules of engagement. Now, I want to take a moment and to encourage all of the fellow researchers here to read and adhere to Microsoft's rule of of engagement. This way, both you and Microsoft get the absolute most of your work. Microsoft also confirmed that there's no evidence of any power exploitation of uh this vulnerability chain. Now in terms of takeaways, I think that something that we could really take home here is

**[24:57](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=1497s)** that custom API proxies are a very rich attack surface. They re basically reimplement the same security controls and pitfalls that were only already patched in the mainstream project. For example, here m like the idea of making a couple of security barriers meaning creating another API server proxy is a very good mindset. However, in this particular case, it actually reimplemented some of the uh issues that were already patched in Kubernetes. And moreover, as you've seen here, don't publish internal service images to public registries because attackers will find them, download them, decompile them, and look for vulnerabilities. And again, uh reiterating that when

**[25:48](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=1548s)** separating between tenets, always use multiple type of guard rails. Looking at the research here, each individual finding like the anonymous cublet o the host network pods the other private secrets any single one of them on its own is low severity but when chaining them you get critical impact. What I'm trying to say here is that you should uh you should assume that the first line of defense will be always be bypassed. So you have to make sure it's not the only one. And also security posture can make your environment resilient even against zero day vulnerabilities. Virtualization based isolation, strong network rules,

**[26:36](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=1596s)** credential hygiene, least privileges principle can really make a huge difference. Let's take this research for as an example. Microsoft did really had amazing defense in death. had multiple security guard rails. However, because they pulled images by tags and not digest, that's what actually made a substantial impact in the form of our supply chain. If uh in this particular instance, Microsoft would have pulled images by digest and not tags, the supply chain vulnerability basically wouldn't have any impact. Uh for more information on our take on how we think uh companies should implement secure multi-tenant environments, you can head

**[27:24](https://www.youtube.com/watch?v=CdOw22fA6Xo&t=1644s)** over to pitch.wiz.io for a free uh PDF resource where we outline our entire philosophy. With that, I want to say thank you for listening and I believe we have some time for questions.

---
id: nQml4Ng9iVc
title: "Black Hat Asia 2026 | Model Files → Memory Corruption → RCE: The Triple-Stage AI Attack Chain"
slug: black-hat-asia-2026-model-files-memory-corruption-rce-the
conference: black-hat
conference_name: "Black Hat"
category: "AI security"
edition: "Black Hat"
year: 2026
speakers: []
channel: "Black Hat"
duration_min: 37
published_at: 2026-08-30T19:45:36Z
video_id: nQml4Ng9iVc
url: https://www.youtube.com/watch?v=nQml4Ng9iVc
youtube_url: https://www.youtube.com/watch?v=nQml4Ng9iVc
tags: []
transcript: true
---

# Black Hat Asia 2026 | Model Files → Memory Corruption → RCE: The Triple-Stage AI Attack Chain

**Speaker not identified**

`Black Hat` · `Black Hat` · `2026` · `37 min`

[Watch the recording](https://www.youtube.com/watch?v=nQml4Ng9iVc) · [Conference site](https://www.blackhat.com/)

## Description

Amidst the rapid advancement of artificial intelligence technologies, an increasing number of enterprises and individuals are adopting AI solutions. As the core vessel of AI systems, model files encapsulate substantial training outcomes and intellectual achievements from researchers. With the proliferation of large language models and the maturation of open-source communities, leading organizations are actively promoting model open-sourcing and sharing, making cutting-edge models accessible to developers worldwide.

However, in practical applications, the model loading process has emerged as a critical security vulnerability hotspot. Existing research reveals significant security risks in the model loading mechanisms of mainstream deep learning frameworks. For instance, PyTorch's historical use of pickle for model serialization introduces inherent deserialization vulnerabilities, while TensorFlow is susceptible to remote code execution (RCE) through maliciously crafted Lambda Layers. More alarmingly, as these frameworks predominantly employ C/C++ implementations for high-performance computing, they remain exposed to conventional memory safety threats such as buffer overflows. This raises a crucial question: Can these memory vulnerabilities be weaponized into complete and reliable RCE attack chains?

In this Briefing, to the best of our knowledge, we will present the first publicly disclosed study that systematically exploits memory corruption vulnerabilities in AI model files to achieve reliable remote code execution. By analyzing the memory management mechanisms of mainstream deep learning frameworks, we construct a complete, end-to-end three-stage attack chain — from malicious model files to arbitrary code execution — through carefully designed heap layouts and control-flow hijacking techniques. We further validate the practical exploitability of this attack chain across real-world AI inference systems.

Ji'an Zhou  |  Security Researcher
Lei Lu  |  Security Researcher
Li'shuo Song  |  Security Researcher

## Transcript

*5,443 words · source: supa (en, exact timings)*

**[0:08](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=8s)** Hello everyone. I'm Lulay. Today Georgian and I will be presenting model files to memory corruption to RCE recognizing the triple stage AI attack chain. First, let me briefly introduce ourselves. We are both independent security researchers focusing on Java, cloud, and assistant security. Next, here's today's agenda. We'll begin with an introduction to AI models followed by a quick review of historical vulnerabilities in model loading process. Then we will detail a memory vulnerability in model files and show how we exploit it to achieve RCE. After that, we will demonstrate how these vulnerabilities can be used to attack AI applications. Finally, we will conclude with mitigations and a summary. So, first let's quickly introduce the AI

**[0:56](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=56s)** models. With the surge in AI, more and more wrestling models have emerged such as Cloud, Chimney, and so on. So, let's take a look at roadmap of a typical AI and project life cycle. It starts with defining the scope and preparing data. Then we select a model. This is most critical decision. Next comes training, reinforcement learning, and evaluation. After that, we optimize and deploy and finally monitor the model while building application around it. Every step matters, but the model sits at center. It's the foundation everything else cancels on. When it comes to model, Hugging Face naturally comes to mind. It offers a wealth of open-source models that we can

**[1:44](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=104s)** readily use, but when we load this open-source models, have we ever stopped to think about their security? And that leads us to the question, are there any risks in model loading? Next, we will review some historical research and vulnerabilities related to model loading. This is actually a familiar story. Model loading is not inherently secure when users load an untrusted model from platforms like Hugging Face. There's a real risk of RCE. We classify these vulnerabilities into two categories: deserialization vulnerabilities and the memory safety vulnerabilities. First, deserialization vulnerabilities. As we know, most of the issues are related to pickle. There's already a lot of excellent

**[2:32](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=152s)** research on this. Here are the historical vulnerabilities in Keras, many of which are related to model loading. As these issues indicate, even when safe mode is enabled during model loading, vulnerabilities still exist that can be bypassed to achieve arbitrary code execution. Besides the common deserialization vulnerabilities, memory safety issues can also arise during model loading. For example, this is an integer overflow problem in TensorFlow. Similar issues also exist across different model model formats. We won't go through them one by one here. But the question is, we see some memory vulnerabilities in models, but do we hardly ever heard about them being exploited? So, that's

**[3:19](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=199s)** exactly what drove us to explore this further. And next, we will share how to go from a memory vulnerability in model files to RCE. First, which target should we choose? After careful consideration, we select PyTorch. PyTorch is the most popular deep deep learning framework and its model format is also widely used. So, in terms of popularity and traction, PyTorch is definitely a good target. Since we've identified a goal, the second step is determining where to start out our research. As we discussed in the previous section, a key part of AI is training, saving, and loading models. Therefore, we should first focus on the workflow within

**[4:07](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=247s)** PyTorch. What I'm presenting here corresponds to those to those workflows. Model developers training and fine-tune their model parameters, then save the weights using torch.save. For instance, they can save a file file called checkpoint.pt, which contains model weights. Later, if they need to continue fine-tuning or using the model for inference, they can use torch.load to load it. In the earlier day, PyTorch just used pickle.load to load model. As we all as we all know, pickle.load is unsafe. We can easily achieve RCE. So, at this stage, loading an untrusting model directly is extremely dangerous, and it might contain malicious code. Later, the PyTorch team also recognized this issue and added a weights_only

**[4:55](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=295s)** parameter. With this parameter enabled, PyTorch will not load any malicious code. It will only load clean weights data. Meanwhile, in security documentation, they also recommended best security practices practices. First of all, using weights_only equal false is definitely not recommended. And back then, they stated that weights_only equal true is secure. The reason why we emphasized back then is because we have submitted a series of bugs. They have and they have since changed their view on whether weights_only equal true is secure. So, when we discovered this mechanism and underlying security assumptions in PyTorch, we got really excited because nothing is more exciting than challenging established beliefs.

**[5:45](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=345s)** We conducted an in-depth study of its security mechanism and obtained several notable findings. Our work was accepted for presented presentation at Black Hat USA last year. For those interested, I encourage you to review the earlier material. The purpose of this introduction is to provide a brief recap of lighter general background and to clarify the distinction between the two topics. So, now let me briefly walk you through the vulnerability chain we discovered last year. In a nutshell, I think it's more like a logical bug. When loading a model using torch.load with weights_only equal true, it will check if the loaded file is torch script format. If yes, it will internally invoke torch.jit.load to load

**[6:34](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=394s)** model. So, we analyzed torch.jit.load and torch script format. Fundamentally, this model format defines various operators such as arithmetic operators like addition. However, it also supports other operators including the one we discovered, torch.save for writing files and torch.from_file for reading files, and ultimately leading to RCE. So, at Black Hat USA 2025, we mainly shared two key points. The first was our analysis of loading process in torch.load, and in particular, the discovery of clever branch within within torch.jit.load. The second part focused on the serialization and deserialization process of torch script, as well as the

**[7:23](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=443s)** discovery of those high-risk privilege operators that we categorized as logical vulnerability. Of course, we also found several memory corruption vulnerabilities, but due to time constraint and a patch progress, we didn't present them at that time. Now, let's move on to the memory block. Before we dive deep into the memory box, we should have a basic understanding of memory layout of PyTorch. First off, the THP variable tensor function is C++ buying functions for torch.Tensor uh in Python. It first parse the argument we pass in, then checks if a torch function has been provided. And obviously, we didn't pass one here.

**[8:10](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=490s)** After that, it calls Tensor CTR to construct the tensor. Here, it first retrieves several parameters, such as whether to perform whether to perform time inference, and whether gradient information needs to be calculated. It then calls the actual constructor internal new from data to create a tensor, and then it returns tensor. So, our next step is to analyze internal new from data. Since the code is quite long, we will conclude the key steps. First, input check. It checks what type of input we provide. Second, metadata inference. It picks out the shape, the type, and device. Third, tensor allocation. It allocates memory for the tensor. Fourth, data filling. It writes data

**[8:57](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=537s)** into the allocated memory. Next, normalization. It adjusts the tensor to match our settings. Finally, it returns the constructed tensor. Since talking about the code alone can be a bit abstract, let's move on to a live debugging session. Here, new tensor is a lightweight tensor handle that may be half an intrusive pointer to a tensor implementation, where the real metadata and storage implementation live. So, here you can see the tensor implementation object. It has a lot of memory. It has a lot of member variables, like storage, size, and strides, and so on. Next, let's use this table to better visualize this memory layout. First, we

**[9:46](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=586s)** have the weak table PTR and reference count. Then, comes the crucial the crucial storage. It points to a structure that manages the actual tensor data storage, which we will cover in detail later. The subsequent members are not important, so let's skip to the size and strides. As the name implies, it used to record the answer shape. The The first value is size. This is the name used using the source code. Personally, we think it could have been named rank or end dimension instead, because it actually represents the number of dimensions. As you can see, it's set to one here, meaning the tensor is one-dimensional. There's a logic here. If the number of dimensions is less than five, it's used

**[10:35](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=635s)** in line storage, storing the dimension information directly in the object structure. The first five values represent size of each dimension. Here, our first dimension is two. Strides stands for stride length. Since we didn't set it, it defaults to one. Storage offset is used to set the read offset. We can also configure it to start reading from the first, second, and other elements. Numel Numel is the total number of elements in the tensor. Data type indicates the element types. Device specified whether tensor data is stored on the CPU or GPU. Next, let's move on to the storage implementation, which is much simpler compared to the previous one, as you can

**[11:24](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=684s)** see. Most tensor information, such as dimensions, is stored in tensor implementation. Storage implementation here focus focuses on more on managing the actual storage unit. You can see it has a data PDF view which points to real data we input and size bytes field. Here we will still use a table to explain. First we still have the weak table PTR and reference count at the top. Then data PTR points to the actual data. Meanwhile because STD table is implemented with recursive inheritance and C++ mandate the that the base class memory is placed before derived class memory, the actual memory order of element is completely reversed from the

**[12:12](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=732s)** declared order. So you will see the destructor pointer here followed by the data pointer. Next is the device field indicating whether data is on CPU or GPU. Finally size bytes records the total size. Since it defaults to long type, its value here is 0 x 10. >> So you have now mastered PyTorch's core memory layout. And the next question is how to hunt memory vulnerabilities. And our previous approach was to enumerate all operators supported by PyTorch script and identify those with special malicious capabilities such as free and write. And now we can shift our focus to hunting for the operators that contain potential memory bugs.

**[13:03](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=783s)** And back of that, our analysis method was actually quite simple and just have two steps. So first we identify the functions that were likely to contain memory vulnerabilities based on the definitions. And for example, if a function had a parameters involving offset or index operations, then it probably access memory directly and might lack proper bound check. And then we perform manual or AI-assisted code analysis on this Harris function we had filled out. And then, we discuss the problem. And then, so now, let's analyze the vulnerable implementation. And the function named torch.store as stride, and it has four parameters. So, the first parameter is the tensor we want to operate on. And the second is the size, which defines the shape of the output

**[13:53](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=833s)** tensor. And the stride has set the step size for indexing. And the last parameter is the starting offset. And as you can see in this demo here, if we change the offset from 0 to 1, and the value change from the first element 1 to the second element 2. And if we modify the size from 1 to 0 to 2, and the resulting tensor will have two values. And if we adjust the stride from 1 to 2, and the indexing switch from picking elements one after another to taking every second element. And the way I set it up, it has an offset parameter. And I suspect that there might be some flaws in its implementation. So, first, I change the size to 6 and set the offset to 0.

**[14:43](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=883s)** And this works fine and returns all the elements of the tensor. Then, I change the offset to 1, and it will throw an error saying out of bounds. And that is because we only have six elements in total. And starting from offset 1 and trying to take six elements would require seven elements in total, and which we don't have. And this shows that there is some bound check in place. And then, I thought that they support negative numbers, and I set the offset to negative 1, and it threw an error. Okay. So, we know that the negative values were not supported. And next, I want to test if it is support extremely large numbers. So, for example, 2 to the power of 62. And it surprisingly and it returned exactly the same result as when

**[15:33](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=933s)** offset was literal. And it was was very strange. And then I tried to set the 2 to the power of 62 minus 1. And then something strange happened here. And I actually value 65. So, as we all know, 65 in hex is just little 0x41. And this looks exactly like the chunk header of GDPC heap. So, I'm very excited. I tried more offset and I just set the offset to the 2 to the power of 62 minus 1 minus 2 minus 3 minus 4 and minus 5. And I became more certain that this is a heap underflow vulnerability. But why? And let's figure out why this bug exists. So, here exists a function

**[16:20](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=980s)** called check in bounds for storage. As the name suggests, it checks whether you are out of the bounds. And the storage size bytes just represent how long we want. Since we want six element and each element is in a long type. So, 6 * 8 is 48. And then the storage size offset bytes just represent the start offset. Since we set it to 1, so 1 * 8 is 8. And the new storage size bytes just represent how long it actually has. And then it has a check. So, the total length we want plus the start offset must not exceed the actual [snorts] length we have. And then here, of course, we exceed and throw an error. But, what will happen if we set it to

**[17:11](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1031s)** the 2 to the power of 62 minus 1? And you can see here. And then and then we need to multiply it by eight since 2 2 the power of 62 * 8 will overflow, so we get -8 and then 48 + -8 is less than the 48, so we pass the check. And so after the check, and you can see there is another check if the storage offset is less than zero, and it's throwing exception. So that is why our earlier attempt with -1 failed. And if we pass both the check and it saves the auto bound offset into the tensor implementation struct. So to make it easier for everyone to understand, I have also draw a simple

**[18:00](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1080s)** diagram here. And in this diagram, A is the original tensor and B is the tensor we already set the overflow value. And as you can see here, there are two distinct tensor implementation object. And the storage offset of B is the original value, and we set by the storage implementation value stored in this in their structs is identical. And the only difference is that when retrieving values, and A points to the normal one, and but the B here points to the previous memory address because of its storage offset. So let's make a quick summary. So yeah, this is a very nice primitive keep out of bounds read and write. So we can first use out of bounds OOB read to leak

**[18:50](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1130s)** the heap address, leak the lib address, and then use OOB write to overwrite the weak table of function pointer to hijack the execution flow. So here, I would also like to express my sincere gratitude to my good partner and my CTF teammate for their assistance and valuable insights on this vulnerability. And however, there is actually a key question here. Previously, we identified and analyzed vulnerability by directly calling Python API. And so, you receive the relevant data and function to a model file. Well, there be any difference between these APIs and triggered during the model loading loading process? So, we call this API in the forward function and save as as a model file and then load the file with torch.load with

**[19:39](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1179s)** this only go through. And you can see here, there seems no difference at all. And then the auto bound problem was triggered just like before. And then, I tried to perform some hip spray spray method. And as you can see, I can draw a hip spray object like hip one, hip two, and so on. And then you GDB debugging to check the hip function. And however, I found that I couldn't find the hip spray object I had created in memory. And which was very strange. And also, I checked the theoretical separation code saved in the model file. And I discovered that only the out of bound part of our function was present in this forward function. And the hip spray content wasn't the same at all. And this was really odd.

**[20:28](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1228s)** So, I went to check the source code and found that it has some JIT related optimization process similar to V8. And it also implement a dead code elimination workflow. So, if it detects a null object is not used in subsequent operations, it will remove the code entirely. So, this explains that why this several hip spray objects we created earlier were not saved in the final model files. So, add an operation to print the shape of each hip spray tensor at the end. And this ensure they will not be eliminated by dead code elimination. And this time, as you can see here, we were able to search for the corresponding structs in GDB and we could also confirm from the serialized model files that the heap

**[21:17](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1277s)** strip operations were present. And what this was the practically memory issue I encountered while writing and debugging the poke, so I would like to share it here. So, next, we turn to write the poke. So, based on our previous analysis, there are key two key structs, tensor implementation and the storage implementation. Since storage implementation contains a data PTR, points to points to the actual memory where data is stored in and it is ideal for us to implement arbitrary memory read and write primitive by modifying the data PTR. So, I just used our OOB vulnerability to construct a large out-of-bound tensor. Then, we defined several patterns to search memory for the corresponding structs. So, one When the condition are met, and once the

**[22:08](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1328s)** condition are met, then we can confirm that a storage implementation object has been found. So, here are the conditions I used. So, first, I used a pattern for the heap size, typically 0x61. And second, the address of its data PTR and the content PTR must be the same. And additionally, it has a reference counter. And of course, you can also construct other patterns to find example, a pattern matching the lib address of 0x7f. Next, once we confirm that our out-of-bounds tensor contains a storage implementation struct, and then we still need to identify which heap strip object it belongs to. So, I came up with a simple method and then modify the value

**[22:56](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1376s)** of its data PTR. And then under normal circumstance, where the an is two, then the value with hip spread is that they will they will three. And if it had been modified, and then it will be anyways two. So, yeah. This allow us to confirm exactly which object we had a control we have control over, and then making subsequent operations easier. And two leak addresses here, we also rely on the storage implementation struct we have mentioned earlier. It contains a vtable pointer and a pointer to the libtorch python.so and a destruct pointer located in libc.so. And however, there is an issue. And none of these libraries have a system PRD by

**[23:43](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1423s)** default. So, we need to implement the capability of arbitrary address read to perform further leak. And we use a simple approach here. We can leverage the data PRD to perform arbitrary address read, which in turn allow us to retrieve the address of the system function. So, here after we get the system function, and then we can modify the destruct pointer and modify the content of data, and then manually call delete to free the corresponding tensor and achieve RCE. So, here is the demo. Then you can see here we have load.py and save.py, and and then we run save.py to to generate the evil model file.

**[24:31](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1471s)** And then here and then you can check that the file name test.bin. And here we load the test.bin with only equal true. And yeah, you can see that we leak the libc address and then achieve RCE. Next, let's attempt to exploit this vulnerability to attack AI applications. So, first we need to find the AI application that supports hot script. After some searching and investigation, and then we found that OpenSearch is a product that meets the requirements. And let's also briefly introduce it. OpenSearch is an open source enterprise grade search and observability tool that brings order to untrusted data at scale. And it also supports machine learning and AI. So, for example, uh we can use

**[25:18](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1518s)** AI tools to create cutting-edge applications. And then we are reading the document, and I found that it supports AI, and which is a good news for us. So, I'd like to know what exactly is AI feature referred to. And then when I look into it, and I found that it supports loading AI models. >> [snorts] >> And upon further analysis, and I found that it supports Torch Script. So, well, it is great great. And then therefore, uh we want to figure out whether it can be exploited. And here is the deployment duration, and it is very simple. And we only need to use Docker Compose, and then the magic happens, and a very little deploy. And after deploy successfully, and then we can open the dashboard and use the development development tools, and all

**[26:07](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1567s)** subsequent interaction will be operated using these development tools. So, first, we just need to register a model group, and we need to remember the model group ID, which will be used later. And then, we modify the model group ID and official request template, and then sign the request. But here, you can see that it has thrown an exception, telling us telling us that we need to enable the allow registering model via your AI config. And since we are the admin, and we can directly modify the config and set the config to true. And we then send the request again, and it has thrown a new error. And it tells us that we need to disable the setting only

**[26:53](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1613s)** run on machine learning model node. And then since by default the Docker environment don't contain a separate machine learning node, so we can disable it for testing. And then since we are the administrator and we can disable it also. And we try again. And this time we succeed and it return a task ID and we should also remember it. And then we query the task ID and then we obtain it before and it will show the corresponding model ID. And then we send the post request to deploy the model and then we return us a new task ID. And then we can run the model. For example, here it is a text embedding model. So we can input a text we want to embed and then it return the vector it calculate.

**[27:41](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1661s)** And so here let's explore the zip format of model file and then we can unzip the model file provided by the official guide and then check that had three files. One is the license and other is just tokenizer.json and the file with the PT extension is just the model saved in torch script format. So at that time I just want to do a basic test. So first we just define a very basic model file and then it will print okay. And then when call the forward function. And we save it to the folder and replace and then call the S sha256 to calculate the hash and then since we need to set the hash checksum in our register request. So here when we run the Python script then it will generate a new model and

**[28:28](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1708s)** then zip the folder and then return the hash for us. So we need to modify the model content hash value parameter and then modify the URL parameter to our own HTTP server. And then run and yeah it succeed and return us a task ID. When we register the model and get the model ID, deploy it and get state of the model deploy task. And it threw an error. It said that we don't return a valid valid value, so it's not a valid model. And so, how can we make a valid model meets the requirement? And when I think of how to make a model and not and not decide an error message. So, when the product meets error in its back end, it can return the error message directly. So, it is a very good feature, and maybe you can use it to

**[29:17](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1757s)** leak data. And then yeah, we write a new model. Here we just assert we just use assert to trigger an error before return, and we want to check if it can return the test error message. And yeah, then we try to load the model again, and then it just threw a new exception which contains our error message. So, our method works. And so here, you can see that torch.load from file function to read the file. And at first, since we don't know the actual file length, then we can set it to the very large number. So, you can see here when we load the new model again, it is throwing an exception to us the real length of the ETC password is 589. And yeah, we can set the set the length to the real length, and then we try

**[30:06](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1806s)** again. And then yeah, we successfully read the ETC password. And so yeah, our arbitrary file read works. And how about arbitrary file write? And here is a tip. And then if we use torch.save to write files, it will write some dirty data. So, we can only write the SSH SSH key, send the authorized cron tab to achieve RCE, since they don't care about the dirty data. But the best choice is config.fish, and run the cron tab will fail, since this dirty data will affect them. But in our future Docker image, there is no stage, no SSH server, no Chrome tab in the container by default. So, the primitive directory field cannot achieve

**[30:53](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1853s)** RCE in here at all. And at most, you can only inject some garbage data. So, how about the heap overflow bug? So, at first, I'm not sure if the heap overflow works here. I just wrote some basic PoC and saved it. And when I loaded this model and developer tools again, and we can see that the leak tensor data in the error message. So, yeah, very nice. The bug exists. And then, I use the same heap spraying method and the heap phone 3 layout as before, and verify step by step. And I measured my lib C address and system function address were correct. And after that, I saved, uploaded, and then loaded the model file, hoping to achieve RCE exploit, but got damn it, I didn't get a

**[31:43](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1903s)** shell. This confused me for a long time. I debugged it countless times and tested it over and over again. And I watched myself step into the system function in GDB, but the command never executed. And later, I found that it didn't because they implemented this call filter, and they banned execve, fork, all of them. So, yes, my fellow CTF players, your file reading challenges are back. And this really takes me back to those static CTF problems that force you to read the flag using open, read, write. And besides the blacklist, and we still have so many syscalls to choose from. And we can get directory entry syscall to this TR, and then use write syscall to write

**[32:32](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1952s)** files without any dirty data. And since this is a Java problem project, and one possible way to achieve our state is via JVM load options. And since AWS also has a managed open search service, and I try to test the AWS service. But it failed. And as you can see here in the official document, and they said that they currently don't support to modify the allow registry model via URL settings. So, we cannot load our malicious model on the cloud service. So, if you are AWS customers, then don't worry, and you are not affected. And then, I reported this bug to the open search open source program, and actually communicate with them.

**[33:19](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=1999s)** And our security teams don't consider this as a new security issue, since they think that PyTorch has already said that TorchScript is unsafe on their and PyTorch official security statement. So, they add some warnings in the official website to remind user to make sure that the remote model is trusted before loading, and otherwise, if the users load untrusted models, then there will exist security risks. And also, this allow registry model via URL option is disabled by default, so user cannot upload in the model from remote URL by default. And they are also considering implementing more fine-grained security controls, and adding more restrictions on who can

**[34:07](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=2047s)** modify these options. And additionally, and users should also set up proper permission controls, and not grant these dangerous permissions to untrusted users. So, they conclude that So, first of all, and this feature is disabled by default, and they also confirm that PyTorch breaks out of the JVM sandbox via JNI. And I recommend that using OS and booking tools like system and Linux permissions. And additionally, and this data PyTorch has already warned this on this official website that the torch rate and the model is unsafe. So, for those reasons, and they don't think this is a new security issue. So, therefore, and every AI application

**[34:56](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=2096s)** that supports torch scripts will have the same problem. And if the users are allowed to load custom torch script model, and then it will have security risks. And in addition to open source, there is, for example, the media training and inference servers, which also has a PyTorch backend, and it uses torch script to load the models. So, it is actually affected as well. But unfortunately, it is a pity that this product isn't part of the Pwn2Own target this year. And finally, here is a defense and a summary. So, here are some security recommendations. So, first, don't load untrusted models, and as this may be unsafe. And second, for AI applications, be sure to implement proper permission

**[35:46](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=2146s)** control. And this is the issue we have repeatedly emphasized. But still, many people expose the AI applications directly to the public network without any restrictions. And finally, stay alert to security issue and keep your system updated at all times. And here is a summary. So, first of all, and in my opinion, as AI security awareness rises, and simple deserialization flows will gradually be fixed. And then from my perspective, such model deserialization vulnerabilities, which are mostly something like logic bugs, will become increasingly rare in the future. But however, the low-level C C++ operators implemented for high performance requirements will bring renewed focus on memory bugs.

**[36:36](https://www.youtube.com/watch?v=nQml4Ng9iVc&t=2196s)** So, all in all, and thanks again

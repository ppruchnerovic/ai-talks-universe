---
id: Yh9C1Xn_ixo
title: "Black Hat USA 2025 | Kernel-Enforced DNS Exfiltration Security"
slug: black-hat-usa-2025-kernel-enforced-dns-exfiltration-security
conference: black-hat
conference_name: "Black Hat"
category: "Security conferences"
edition: "Black Hat"
year: 2026
speakers: []
channel: "Black Hat"
duration_min: 36
published_at: 2026-04-04T19:00:21Z
video_id: Yh9C1Xn_ixo
url: https://www.youtube.com/watch?v=Yh9C1Xn_ixo
youtube_url: https://www.youtube.com/watch?v=Yh9C1Xn_ixo
tags: []
topics: ["Classic ML & data science", "Security, safety & red teaming"]
transcript: true
---

# Black Hat USA 2025 | Kernel-Enforced DNS Exfiltration Security

**Speaker not identified**

`Black Hat` · `Black Hat` · `2026` · `36 min`

[Watch the recording](https://www.youtube.com/watch?v=Yh9C1Xn_ixo) · [Conference site](https://www.blackhat.com/)

## Description

Kernel-Enforced DNS Exfiltration Security: Framework Built for Cloud Environments to Stop Data Breaches via DNS at Scale

DNS-based data exfiltration via C2 channels and DNS tunneling is a critical cybersecurity challenge, as DNS is a foundational protocol that must remain open on firewalls. Attackers now use DNS not just for exfiltration, but to establish backdoors, execute remote commands, and maintain persistent control over compromised systems. With the evolving scale of C2 infrastructure—leveraging multiplayer C2 modes and botnets—real-time prevention becomes significantly more complex, especially when aiming for zero data loss and accurate process-level implant termination at the endpoint.

Traditional defenses rely heavily on timing and volume-based passive anomaly detection, signature-based filtering, or DPI through proxies and middleware. These approaches are increasingly ineffective against evasive C2 threats. They suffer from delayed detection, longer dwell time, greater data loss before threat removal, and slow response. Most fail to handle DGAs, where attackers constantly mutate domains (L7) and IPs (L3) to evade static blacklists, and they still lack support for
instantaneous implant termination.

This framework is built to disrupt DNS-based C2 channels and DNS tunnelling at scale by moving DNS exfiltration security directly into the Linux kernel. Using eBPF-driven endpoint security enforcement, the framework runs advanced threat intelligence across the entire kernel network stack and mandatory access control layer, performing high-speed DPI by parsing the DNS protocol directly inside the kernel. Aided by a userspace deep learning model trained on diverse DNS payload obfuscation techniques, it enhances detection accuracy and enables dynamic runtime enforcement. It instantaneously prevents DNS C2 channels and tunneling, ensuring that no exfiltrated packets ever leave the endpoint — and precisely threat-hunts and kills malicious C2 implant processes in real time. It inherently supports dynamic domain blacklisting, dynamic in-kernel network policy creation, and threat event streaming, enabling massive scalability for real production cloud environments.

By:
Vedang Parasnis  |  Cloud Platform Software Engineer | Linux Kernel Datapath Security Researcher

Full Presentation Materials Available at:

## Transcript

*6,299 words · source: supa (en, exact timings)*

**[0:03](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=3s)** Hi, good morning. So I'm here to talk about my research on hunting and disrupting DNS C2 and tunneling inside Linux kernel. The stealthiest process and the stealthiest C2 channels at scale in cloud. A bit background about myself. My name is Vang Parnes. I am an independent researcher, a recent former masters graduate from University of Washington. My research interest focuses on Linux kernel security kernel hardening ebpf and its intersection with user space AI and cloud to build endpoint security solutions to that solves emerging problems. I'll strike to structure the talk into four main parts. I'll start why DNS as is a critical backdoor when it comes to command and control and why most of the advanced persistent threats prefer DNS as a communication channel when they exploit your cloud environments. Then I'll talk about some of the attack vectors and the

**[0:52](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=52s)** C2 infrastructure. how C2 IPS are scaling their command and control infrastructure to compromise your networks existing approaches and the challenges for the emerging preventing these emerging attacks and then I'll present a research AIdriven kernel enforced endpoint security and a cloud deployment architecture how that can be deployed in the cloud environment so that you can combat your remote scaled C2 infrastructures a short demo of breaking down sliver C2 DNS C2 well-known adversary emulation framework and K we'll conclude with KT takeaways and future direction. Every time most of the advanced persistent heads DNS is a preferred choice for their communication channel and that's DNS is a preferred uh tool in their arsenal. This diagram shows all the advanced persistent threat over the

**[1:39](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=99s)** last decade. If you could see W Typhoon the recent Chinese power a had DNS beaconing inside it and they it used DNS for comm X rating and maintaining persistent control. There are other APS as well like some of most famous like the cos bear Russian powered AP as well as the ocean lotus which is a Vietnam government powered a compromise the damages to DNSC2 and tunneling are massive. They compromise national defense from infrastructure to hyperscalers mission critical environments breach data as well as uh hardware credentials at scale. As per the unit 42 analysis, 85% of APs leverage DNS for stealthy command and control operations and data breaches as well. Why? But DNS protocol is a major blind spot to compromise the networks.

**[2:29](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=149s)** DNS is a foundation and backbone for internet. I would say it's the it's every cloud environment relies on DNS for service discovery and it's the first packet which every LN protocol sends. But when it comes to command and control and stealthy communication channel, it becomes extremely damaging. It's unencrypted by default and uh basically uh it it has logs are rarely monitored on the firewall. Firewalls always have the ports open for DNS and the most important factor is stateless and because it's majority runs on top of UDP. It can be easily controlled from throwaway infrastructure. This is a diagram how it shows basically uh a standard DNS deployment topology. We have the local firewall and external firewall. Both of them having DNS ports open.

**[3:16](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=196s)** Uh the main attack vectors when consist of DNS are DNSC2, DNS tunneling and raw excfiltration. The main purpose of all of the three are embedding embedding and excfiltrating data inside subdomains of the DNS. DNSC2 being the most damaging. All of them being done with fileless implant. In DNSC2 the attacker em embeds commands and the main focus here is persistence and advanced persistent thread to rely on the system for prolonged period. Tunneling is similar. The only difference is tunneling tunnels the the protocols through DNS which are blocked on the firewall and raw excfiltration is the most simplest. It's a simplex single communication. It just exfiltrates and the malware kills itself once a large amount of traffic is excfiltrated. This diagram shows how it works. It's a multi-stage process. This attacker registers a domain with a C2

**[4:04](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=244s)** server. Once the domain is registered, it generates a fileless implant. That implant gets delivered onto the endpoint. After getting delivered, it asks the remote C2 infrastructure with a query sends a DNS query in a beaconing pattern what next command it should execute. So it's called as a pulling mechanism. The C2 server is it passes through the firewall, reaches the C2 server, the C2 server responds back and the attacker executes that the implant executes that command. But when it comes to it's not just excfiltrating files this commands are most damaging. It's not just data breaches. It's the next channel for delivering zero day attacks. Some of the major damaging attacks command and control can facilitate are remote code execution, shell code exploits and file corruptions. The attacker's purpose here is not to exploit the data but damage the endpoint

**[4:50](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=290s)** and spread lateral in the network. Other are side channel process abuse and persistent backd doors and port forwarding and pivoting are some of the other factors and specifically when it comes to root kits ta is one of the which use this the but the attacker doesn't has a bigger headroom when it comes to amount of data it can put inside DNS DNS has some specification limit limitations when the amount of data it can be put inside the query section usually DNS allows for a UDP basically DNS allows you to have 255 characters max, 127 labels. A label is anything between a dot and 63 characters is allowed per level. The number of questions are usually one per DNS packet because most of the DNS servers don't allow multiple questions. And this is a standard DNS question

**[5:39](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=339s)** format how it looks like it has query name, query type, and query class. But what makes the actually DNS packet contain the C2 commands and extracted data? Usually attackers are trying to embed a command into the subdomains. They always try to encode and encrypt with different mechanisms. Some of net bios, CC, hex. This always adds an entropy in your query name. Longer excessive labels. It's an option basically for the attacker to have multiple smaller chunks of labels or a or a a label which is a longer in length. And then finally, DNS is a restricted set of asai characters. Their words doesn't makes any sense. They have non-dictionary tokens and DGA generated style. But one thing I would say attacker just don't register when it comes to C2 attackers or AP botnet groups just don't register a single

**[6:28](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=388s)** domain on a DNS resolver or an iron provider. They have they they they deploy a fleet of servers for maximizing evasion. This diagram shows how the C2 infrastructure is scaled and the higher the scale the more the evasion is. So you could see on the right you have all your enterprise networks which can be geographically distributed across different regions. You have redirector fleets. A redirector fleets are meant for evasion. They are specifically bought used to hide the C2 servers location for L3 bot. uh it can be thought as a proxy chain and the actual C2 servers are the dedicated C2 servers which register domains across different domain providers or domain registars and uh so that they can have maximum evasion and they categorize themselves as a shortbased C2 or a long B C2 based on

**[7:16](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=436s)** whatever the beginning pattern they exhibit. DGA uh is domain generation algorithm is just not layer 7 mutation. it's paired with IP mutation that makes them more evasive that because one eventually they generate algorithmically thousands of domains one domain got blacklisted they have other domain for that implant to query and each domain usually also has multiple IP address so that the implant can load balance itself when quering sending DNSC2 commands and that's makes your static blacklist evade easily and there's hardcoded and algorithmically generated so that there's that's that's also ads evasion mechanism there There are four main types of domain generation algorithms. Time based and seed based being the most easy to detect while worldly and characters are usually harder because of the pseudo random

**[8:04](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=484s)** generators. Uh uh sorry pseudo generators and the world DGS they use. Some of the example are like once you get a monotonic time clock you can easily detect what's the next DGA will be but seed is the same once you grab the seed you can understand what's the next uh DJ pattern it will be but world list is difficult to guess. Moving on to existing approaches. Right now there are two main existing approaches being used. First is semi-passive analysis and second is passive analysis. Semipassive analysis is less common. It's done with programmable software switches also which use deep packet inspection as a middleware inside the switch and that query uh if it cannot find it clones the packet to the controller. It replicates an SDN topology how virtualization works in the cloud. But majority passive analysis is used and the main factors are anomaly detection and traffic sign

**[8:52](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=532s)** thread signatures and domain reputation. This shows an entire flow of a network intrusion system from start to end. You have all the endpoints. Endpoints are sending all the sensors they have the at running at the end point they are sending all the uh aggregated DNS data. That DNS data is aggregated and passed to as a filter over a specific window. That window then runs to a stateless analysis. stateless analysis then triggers an response which blacklist the domain and response policy zone. It's not always the case but because it's no doesn't has physical access to the wire it aggregates it into a log aggregator or same after that it generates stateful fe stateful feature analysis and classifiers and machine learning models on it and detects outliers and that's where your s pipelines and all that triggers and outliers are blacklisted uh the domains are blacklisted in response

**[9:42](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=582s)** policy zones and alerts are triggered and it basically tells the EDR a region to start blocking that uh domain The main challenges here are it is slow detection and slow response specifically with the evasive nature of the evolving C2 implants because the amount of time it takes to detect something and then reinforce the C2 the C2 implant has already mutated and started using a different domain but if it's a zero day attack it may have already executed that command and started a child process or any other thing that may executing that may already execute and compromise your nodes then again it's less it's kind of a proactive active mechanism rather than a reactive mechanism when it comes to C2 infrastructure attacks and robust protection against domain generation and IP mutations and if it's a passive analysis that's

**[10:30](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=630s)** rarely used it causes latency on the traffic because traffic is been mirrored and that causes unwanted latency and dynamic thread patterns C2s are not just meant for standard way patterns they are used for dynamic like they have different they use different query classes uh text MX null different timing uh stealthy patterns so that all the statics are broken down. So the proposed solution and the research which I will be talking about is rather than being a proactive it should be a reactive. So rather than the network hunting once the C2 activities and executed let your endpoint agent hunt what's happening and that two hunting should be done inside the kernel at ring zero where no userland evasion can hide uh from all the C2 malicious irrespective when they are sending a packet. A bit background

**[11:18](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=678s)** how it can be done in Linux kernel specifically it can be done using kernel modules but I'll specifically done talking about EPF. EPF is a modern way of reprogramming the Linux kernel in a safe way. It is can be thought as a virtual machine running inside the Linux kernel which allows you to inject runtime programs which get attached to the core kernel subsystem. So these diagrams actually show how it works. So you have an APF loader that loaders by gets compiled and then bite code is generated that bite code gets injected through BPF sys call it goes through ebpf verifier just in time compile optimizes it and then it gets attached some of the co- kernel systems are like Linux kernel network stack kernel LSM Linux security models etc. It the main factor here you could see is it has ePF maps and ring buffers or puff events. These are the factors crucial when it

**[12:06](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=726s)** comes to endpoint security. This gives thread and intelligence to the endpoint agent in the user space what's actually happening inside the kernel. So just to summarize it uh they and the user space can actually contact that ebpf maps and read what it's it's a custom bip byte code and it's CPU architecture agnostic. Before moving deeper into the EDR agent I would like to talk about what core kernel subsystems the EDR agent utilizes. Starting off with a kernel network stack which is also referred as a kernel data path. This shows the standard way how packets actually flow from user space downstream to the network device driver. User space system call the TCP IP network stack net filter final traffic control and the network device driver. These are all the hook points which BPF allows you to inject a kernel programs and some of them are K probes trace points crops etc. The main

**[12:56](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=776s)** of them main focus for the EDR here is BPF K probes. K probes and trace points are attached to process scheduulers. This helps the EDR to hunt if the agent in C2 implant is forking itself and send spawning child processes. It should not evade the detection. It should always get the parent process what's tied to it and also helps in process hollowing and other shadowing mechanisms. Uh croup socket maps it's used for premonitoring DNS socket creations and all that events. Traffic control utilizes specifically for uh quality of service in the kernel but this specifically uses deep packet inspection. Here's where it raw passes the DNS questions inside the kernel from the SKB also referred as socket kernel buffer and then it runs enhanced intelligence on top of it. But EBPF allows you to inject all the custom programs EB but the runtime integration

**[13:45](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=825s)** should not compromise the integrity of the kernel and the kernel should remain the most trusted execution environment in your uh system and that's where LSM is used. LSM is Linux security modules. It's a mechanism which helps in order to ensure that every injected program is verified and integrity is ensured and it interacts with kernel keying and Linux security modules as other components for verifying the integrity with with the custom certification authority the EDR agent generates. I'll talk about the main uh what I call it as kernel enforced endpoint security uh and an extension mechanism for DNS. It's an agent based endpoint security is similar to your sensors running at the endpoint but it's a multi-layer security. Starting off with the user space. It has EPF agent. The agent is nothing but a loader which I talked about before which

**[14:32](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=872s)** loads the EPF programs. It has caches. The caches usually hold the top level domains of the most world's reputed domains in the because the C2 server cannot get access to those zone files. Uh then it also holds a quantized deep learning model. model it's used for data offiscation detection mechanisms and some metrics exporters is used to export all the kernel ring buffer metrics uh which the kernel is exporting. Coming down to the user kernel layer all those EBPF hooks are injected in runtime by the EDR agent uh which I mentioned before uh and your kernel is resilient now to tackle that uh and basically you could see all of them are contacting to the same EBPF maps. the EBPF maps becomes your system state store for storing all the EBPF information which each of the program is executing. So

**[15:20](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=920s)** let's uh to walk through how it works. Let's assume that a implant got C2 implant got infected and started sending malicious DNS packets. It send down a DNS query downstream to the access control layer. It hit the sys call layer. Once it hits the sysol layer, the main focus of it is what's the process tied to this packet and go and hunt for the parent process to it and make sure that the lower layers of the kernel gets the parent process only. It goes downstream and then it hits the uh network stack. In network stack what it does it does uh first it does checks that is this packet process coming down the packet coming down is the process back black backlisted if not it does the deep packet inspection on it. It's not always assured that the kernel restrictions will allow you to perform enhanced lexical analysis. That's where it it basically to has two option that

**[16:10](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=970s)** it redirects or clone redirects the packet where packets are redirected live from the kernel to the user space. The EDR agent sniffs it continuously but before doing that you could see it updated the procession telemetry in the maps before doing that. The EDR agent in the user space continuously gets once it gets the packet it knows that the corresponding maps have been updated. gets the process up process level information and does all the inferencing which is data offiscation detection mechanisms for randomness. Post doing that if it founds to be malicious it goes and blacklist that process in the EPF maps. This forces the implant to retry. Uh so the implant retries and then it pass again passes through the but now the process is blacklisted. It kinds of create a trap for the implant and this

**[16:58](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1018s)** for makes it retry again and again but the kernel is exporting every rich telemetry to the user space and this messes up their beaconing pattern. If the implant is beaconing then it beacons again with a jitter or exponential backoff mechanisms and eventually once everything the EDR agent collected rich behavior from that implant behavior it goes and kills it and the implant is terminated. So to summarize, it's a continuous security enforcement loop happening between the kernel and the user space where the EDR the EPF program in the kernel is helping the user space. User space is reprogramming the kernel security layer and that continuous enforcement loop continues until the implant gets terminated or it it killed itself. There are two main modes I would like talk about it works these modes are replicated how C2 real world C2 attack

**[17:47](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1067s)** C2 uh tools exhibits when they their communication is broken down when it comes to C the DNS the first mode I'll talk is active process security enforcement mode this is meant for endpoints which can to which which want absolutely no data loss no command execution but can tolerate some latency this is a big bit brief in uh detail information So eBPF maps I'll talk about EPF maps. Other components are the same which I mentioned before. The first EPF map actually holds the DNS query ID as the key. It's a hashmap and the value it's stored in the kernel memory and the value is the mon system monotonic time and the process information and thread information. The second map holds the monotonic time and the value as a scan. Why I will talk why a second map is used. Same process I'll walk through there. The packet is sent hits there.

**[18:36](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1116s)** raw passes it does that inference now it redirects the packet this only works for UDP by the way if the DNS is trans transferred over UDP uh it redirects the packet and the if it's malicious it gets robbed but yeah after the endpoint agent in the user space reads it it infers through its caches uh which whether there a top level domain or already inferred runs a inference on top of it whatever the verdict is it get make sure that the packet it which came now it gets from the map the first map basically ally process scan time and the process information uh and then that information if it's malicious that telemetry which the EBPF program the kernel updated is used to track the activity of that malicious implant post doing that if it's a banana in the

**[19:23](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1163s)** packet has to be resent and that's where it it sends the packet it authorizes the second map the second map is especially used for zero trust enforcement because when the packet is redirected to the user space the malware should not brute force itself and understand and guess the time okay when next next packet will come then security that's that should not happen. So this monotonic time is system nancond accuracy which kernel uses non-maskable interrupt safe time. So it has precise timing and brute force attack prevention. packet is sent down back and now the kernel again verifies whether this packet coming is authorized from an authorized sender and packet leaves the and this process continues again and eventually once enough telemetry is clear the implant is killed the second mode is it's not always possible to redirect a packet it won't

**[20:11](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1211s)** ever work for TCP will cause unwanted congestion and any redirection to a different Linux network name space or a network interface raises an interrupt causing latency uh so it's not always visible that's where a second mode I want to talk about I'll talk how it works in a state diagram the C2 and tunneling starts the EBPF it hits the traffic control layer the EBPF program starts deep packet inspection on it now it doesn't redirects rather it clone redirects the packet that is internally kernel cloning the entire packet in the traffic control layer and that packet is sent back and also expose the process telemetry uh once doing that the EDR agent does a deep learning model inferencing on that clone packet. Post doing that um if the if the if it founds that like there is some malicious embedded commands inside

**[21:00](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1260s)** it. It keeps on tracking the Xfiltration and C2 tunneling attempts in user space. It's done in the user space memory. Basically once tracking everything the implant again retries same thing happens it clones everything again it finds the uh event tracking in the user space is incrementing. Eventually it found some behavioral analysis that the same implant is exhibiting multiple patterns. The same process by the way it goes and updates the map the EBPF map saying that this process is potentially a malicious one and it should start the kernel should start dropping. So now the kernel transfers the EPF programs basically get pros the network stack and then eventually you could see the EPF kernel program starts hunts if the process is blacklisted because the implant again retrieded. Now and the same thing the

**[21:49](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1309s)** kernel program starts starving or basically starts dropping the traffic. This again same thing I mentioned messes up their uh se beginning pattern but it clones keeps on cloning the packet. So you have richer behavioral analysis implant retries same thing happens same thing is collected and eventually once everything uh rich behavior is analyzed all the analysis it goes and blacklist the domain and the implant is killed everything of this depends upon the accuracy because the model if the model is not accurate can eventually cause service disruption it can kill benign process so the model uses a de dense neural network based data offiscation detection model it first I'll talk about the features the Linux kernel features these are used for deep packet

**[22:37](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1357s)** inspection and they are nothing but limits this limit specifies what is the maximum and minimum range you want and anything above that range is considered as malicious between those minimum maximum is considered as suspicious anything below is benign in kernel they are actually user space features the features are used for an lexical analysis they are uh there are some like again the characteristics which are disc talked about uh like uh about query patterns and lexical measures they are the same lexical features but I want to talk about entropy here entropy is specifically used with the Shannon's entropy with a base logarithmic two because uh DNS only allows as key characters as restricted as I said u I'll talk about the model architecture but before talking about the model architecture I'd like to talk about data

**[23:24](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1404s)** sets it was 60 million domain data set close to And it was synthetically generated utilizing ISP real world traffic and traffic from expediting various files and executing different malicious commands from open source uh adversary emulation tools. This uh is used this represents an ONX graph. Onyx is open neural network exchange. It's a way of serializing deep learning models. It's meant for faster inferencing and lower memory footprint. And you could say it's and again it's contised because floatingoint arithmetic usually takes CPU cycles. So it it is converted to a lower precision for faster to match the speed of the EDR and the kernel processing. Uh this it's a one input layer, three hidden layers, one output layer rail as an activation function sigmoid for binary classification. But each of these modes can be worked

**[24:16](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1456s)** which uh and can safeguard your endpoint from uh the C2 implant. C2s are like kind of a dualsided where you if you want to safeguard a cloud infrastructure the C2 implant also has to be killed as early as possible as well as your firewalls and your cloud infrastructure should be made secure so that this remote scaling C2 infrastructure is broken down. This is a standard deployment architecture I would like to talk uh this assumes it's only in one uh a but it can scale as horizontally as required. So you have the all the data plane and the data plane has set of this EPF agents deployed at the end point and before talking about the actual framework strategy I'll talk about a DNS topology usually looks like you have a local authorative DNS server with a party uh with any backend it is it can

**[25:04](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1504s)** be LDAP provider for your cloud or it can be managed DNS authorative server or it can be a post case if it's done on onrem or any uh generic RDBMS back end it has forwarded to a recursor which is a resolver upstream forwardered to the upstream DNS servers, a DNS firewall connected to a network firewall, uh internet gateway and forwarded upstream to the actual DNS resolvers whatever the upstream forwarders are. But your IPS and ids are connected and you have all the same everything ready. But the now this EPF powered agents now are running and let's assume if I walk through this flow let's assume this agent uh this one data plane node got infected with this C2 server. It started sending malicious packets and let's assume this is the IP it's sending and this is the domain

**[25:52](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1552s)** xmail.com and this some random thing it's asking what or it can be doing a commands running commands for doing that the implant whichever mode the ED edr agent whichever mode it's running in active or passive it it kills it uh but it gathers all the rich telemetry it exports everything uh in a form uh it streams everything collected from the kernel ebpf ring buffers in into into a message broker. A message broker can be a topic. It can be even a queue anything and all managed cloud provider service. Suppose that there are set of controller nodes. These are specifically used for dynamic domain blacklisting to combat DGAS. The they they consume that and then the controller servers host and blacklist the domain in RPZ dynamically. Post doing that it just it goes ahead

**[26:41](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1601s)** and also ask the upstream DNS resolver where is this located? This is specifically meant for reverse hunting to analyze the redirector's nature. What's the redirector IP nature? How is the C2 infrastructure pulling and proxy chains are working when sending different IP addresses for that? It sends back the controller streams back all that and then each of the data plane EPF agents consumes those events and they update their malicious caches which I talked about the user space cache which is and it also injects uh security filters inside the kernel. So your entire data plane now got safeguarded from x-ray.com domain and there was nearly no data loss and you combed the DGA uh specifically and even though that implant mutates and migrates it's not going to survive because that domain is dynamically getting blacklisted but the

**[27:28](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1648s)** benefits of this is it has a dual layer defense for example if one of the endpoint is running a lower version of Linux kernel which doesn't supports EBP features or it's running a different OS you made the DNS server resilient and that DNS server got reprogrammed. So eventually if other oss that same DNS server is going to protect your endpoints and then finally the EB rich telemetry is exported by the EPF agent to your SIM. So everything is exported to the SIM. So I'll show a demo uh right now of of slaver slver or bishop fox sliver tool and for that demo purpose I'll I have a onremise infrastructure and kafka is been used as a message broker and I have already generated and par DNS is used as an DNS server I have already generated all the domains in the with in

**[28:18](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1698s)** the domain DNS server and it's using word this cate strategy so after doing that I start the sl server I'll show some of the attack first and then I'll uh start the agent. So I just start the sliver server and I then generate the implant. I just transfer the implant. It can be done through any intrusion mechan uh in uh infiltration mechanisms. Post that the implant started running and you could see the logs here all the it started quering and you could see the commands are being embedded like this on repartition.com's domain. Now like once it's done I'll start first with uh uh selecting the session. So I'll start the session. I started executing remote commands on it. Some of them like get ur and other remote commands I'm executing.

**[29:06](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1746s)** I'm also showcasing a port forwarding mechanism. So I'm port forwarding engineext server on 32000 port. Uh so yeah I did forward that. So it generated a large volume of DNS traffic because it's a session based uh not a beacon based uh implant. So push pause anyway. So so once that that is done I start a reverse shell on it and once reverse shell is started I start the endpoint agent. Once the EDR agent starts it injects all those EPF programs. Once the EPF programs are injected it immediately started detecting everything and it started asking the deep learning model what should be done for it. post asking it u you could see it will start now the

**[29:54](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1794s)** implant started getting query errors immediately because the kernel EDR agent started dropping it first re so you could see there's a query error then this will again force it to retry uh yeah so again it retries the behavior is captured here and you can see it's 409711 is the process which is actually doing that attempts And eventually you once that there's a enough telemetry the EDR regent goes and kills it. But it captured all the rich telemetry. So you can it's it's exporting right now to Promeththeus and explode visualized to graphana. It can be any sim or elk stack any any cloud provider stack. So it tracks how many packets were redirected live from the kernel. It tracks per process level information like for this process for

**[30:42](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1842s)** this thread it was it was ex accelerating four times how much time the process was alive. So you could see it it shows the process alive time as well as well as each each individual packet it was trying to send was actually seen in the dashboard with with ebpf and rainbow first everything exporting it in real time and everything got exported into Kafka the this is the same thing basically uh and it got black domain got blacklisted as I said RPZ is party post so it got blacklisted so just a metric about response speed how fast is this when it compared as uh uh with and precision. So when I talking about the response speed, it's it's usually for active mode for 10 xfiltration attempts. The x-axis shows the attempts per attempt and yaxis shows

**[31:30](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1890s)** the time it take to respond to it. And on average, it takes t6 microconds for every xfiltration C2 attempt. And this shows a actually this shows uh uh the pre F1 score precision recall graph. The precision for this model was around 99.89% 89% and the uh the main focus here is to reduce the false positives. So 0.7 was a threshold kept as a binary classification. Anything above that is malicious. Uh next steps these are the steps which are actively worked around right now for this project. Uh which is right now it worked for DNS but it I'm currently working on for TLS fingerprinting. Well TLS can be done in two ways. One is user space TLS. Most of the TLS is done inside the user space. But there are other encrypted tunnel mechanisms like wire guard and IPSC. Both of them uh are

**[32:20](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1940s)** done through kernel core kernel component. So attaching TLS fingerprinting mechanisms through APF can help hunting down that process correlation. Well, this is the next advanced mode I would say. For example, this is useful if the implant was initially trying to send uh through DNS and then it started sending through HTTPS. You know the process in the EPF map. You know the deep system telemetry what the process inside happening in the kernel. So that can be correlated continuous model evolution. The model can easily get absolute with a new data offiscation. If a new offiscation comes which looks more very much similar to like normal English words in that case there's there's continuous learning and detection confidence based learning approaches. But I specifically want to talk about GAN plus LSM. GAN specifically for poisoning. uh so that the more offiscation formats the model

**[33:08](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=1988s)** can learn and LSTM is for behavioral analysis specifically. Right now the behavior is just like you have three or four continuous attempts for per process go and kill it. But there can be reach behavioral analysis from the kernel eBPF maps done through LSTM uh and RNN approaches. And finally DOS protection. There's ghost attacks and flood also as an technique being used. So EPF can be injected over the entire kernel network stack. So it has a support for HTTP express data path over the network device driver which is the very lowest layer of the network kernel stack. You can have DOS protection mechanism so that it inherently just not becomes an endpoint security it becomes an endpoint security ingress endpoint security solution as well. So it just doesn't prevent sex federation. Key takeaways when AI is paired with eBPF it matures

**[33:58](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=2038s)** your EDR. Uh because EBPF helps you to reprogram the kernel. It helps you hunting deeper system telemetry and it eventually matches your user space with richer behavioral metrics and it can when paired with AI the dynamic kernel enforcements which the AI can put the model can EDR agent can put makes it resilient. Finally uh then talking about the DGA breaking down DGA kerneldriven EDR uh and cloud firewalls. So I think like it can be when if you want to break down the remote C2 scale it has to be like a bordon of approach where your operating system uh makes the EDR mature. EDR makes your same mature and that same eventually firewalls and that firewalls dynamic blacklisting for L3 and L7 filters on the firewall makes

**[34:46](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=2086s)** your firewalls intelligent and your cloud network is secured from this remote mutative C2 infrastructure. irrespective they change their domains or they change their IP address the still the infrastructure is protected at a scale and finally this worked because DNS is unencrypted and even though for example DNS over TLS is one mechan or other encryption like HTTPS the most preferred one or wire guard for example in that scenario you need behavioral analysis it cannot be done just purely on lexical analysis on to because it may rise false positives that way here also the EBPF and the kernel enforcement can uh help to uh mature your SIM. The more deeper system telemetry your EDR agent exposes, the more faster the SIM matures and your source pipelines

**[35:36](https://www.youtube.com/watch?v=Yh9C1Xn_ixo&t=2136s)** become more faster to prevent this uh encrypted tunnel attacks. Thank you for your attention and listening to my talk. You can reach out to me through this email. The framework codebase is open source and can be found here as well as a white paper. Uh this is a graduate thesis being published. So it can be found here. If you have any questions, I'm open to answer them. Thank you for your time. [applause]

---
id: rZ0Jnu5gtqo
title: "Black Hat USA 2025 | Unveiling the Hidden Perils of the TorchScript Engine in PyTorch"
slug: black-hat-usa-2025-unveiling-the-hidden-perils-of-the
conference: black-hat
conference_name: "Black Hat"
category: "Security conferences"
edition: "Black Hat"
year: 2026
speakers: []
channel: "Black Hat"
duration_min: 37
published_at: 2026-01-02T18:28:30Z
video_id: rZ0Jnu5gtqo
url: https://www.youtube.com/watch?v=rZ0Jnu5gtqo
youtube_url: https://www.youtube.com/watch?v=rZ0Jnu5gtqo
tags: []
topics: ["Classic ML & data science", "Security, safety & red teaming", "Training, fine-tuning & model building"]
transcript: true
---

# Black Hat USA 2025 | Unveiling the Hidden Perils of the TorchScript Engine in PyTorch

**Speaker not identified**

`Black Hat` · `Black Hat` · `2026` · `37 min`

[Watch the recording](https://www.youtube.com/watch?v=rZ0Jnu5gtqo) · [Conference site](https://www.blackhat.com/)

## Description

(PRE-RECORDED)
PyTorch is a machine learning library based on the Torch library, used for applications such as computer vision and natural language processing. It is one of the most popular deep learning frameworks.

However, beneath its powerful capabilities lies a potential security risk. Initially, PyTorch used pickle to save models, but due to the insecurity of pickle deserialization, there was a risk of Remote Code Execution (RCE) when loading models. Subsequently, PyTorch introduced the weights_only parameter to enhance security. The official documentation states that weights_only=True is considered safe and recommends using it over weights_only=False.

For years, the security of weights_only=True remained unchallenged. Our research, however, uncovered unsettling truths. We discovered that torch.load with weights_only=True supports TorchScript, leading us to delve into TorchScript's inner workings. After a period of research, we discovered several vulnerabilities and ultimately achieved RCE. We promptly reported this finding to PyTorch, who acknowledged the vulnerability and assigned us CVE-2025-32434. This revelation overturns established understandings and has profound implications for numerous AI applications. We will provide an in-depth analysis of the impact of this vulnerability.
In this Briefing, we will introduce how we gained inspiration and discovered this interesting vulnerability. Meanwhile, our findings once again confirm the statement, "The Safe Harbor you once thought was actually Hostile Waters."

By:
Ji'an Zhou  |  Security Engineer, Alibaba Cloud
Li'shuo Song  |  Security Engineer, Alibaba Cloud

Full Abstract and Presentation Materials:

## Transcript

*4,634 words · source: supa (en, exact timings)*

**[0:02](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=2s)** Hello everyone and thanks for coming. I'm really honored and excited to be here to give a presentation. So today my topic is safe harbor or hostel waters and viewing the hidden parts of the touchcreen engine in PyTorch. So first let me briefly introduce ourselves. So we are all security engineers for Malibaba cloud and I'm Gro and I completed this work together with my colleague Lis Ros and we were really looking forward to come here but unfortunately due to visa application issue mean I'm able to attend the event on site so we sincerely apologize for this and as a result we will we have to share our presentation through this pre-recorded video and if you have any questions about this colleague feel free

**[0:50](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=50s)** to contact us via Twitter. So here this is the agenda. So first is the introduction and background knowledge and then I will introduce where this story begin and after that I will analize how this only works and the toy screen and finally is the impact of this vulnerability and some defense recommendation and summary. So first introduction. So what is PyTorch? PyTorch is a machine learning framework based on the torch machine learning library and it was developed by Facebook and now it is an open source project and it is very easy to use and has some really great features and pytorch is widely used in the deep learning research and natural

**[1:37](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=97s)** language processing or called NLP and computer vision. And to be honest in the field of machine learning and there are many many popular frameworks and well-known projects like tensflow and cur however according to the official paper with code website and you can see that pytorch has undoubtedly been the most popular machine learning framework and so if someone can find some bugs in such a famous machine learning framework and that will be a really cool thing. So let's come to the second part where it all begin. So in the early days, PyTorch used Pico to load and save models for convenience. But however, Pico is one of the most

**[2:25](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=145s)** well-known security issue in Python. And in fact, the official patent document warns that using pico can be unsafe and especially when loading untrusted data. So in 2021 and someone raised this issue on GitHub and called for a solution to address the pico issue and finally in 2022 and PyTorch released a notable updates that introduced the Wson only parameter. So let's briefly analize the implementation of von and as we can see that when von is not enabled and this is by default pytorch will use p code to save and n modules and which introduce security risk and however when we set a von parameter and it will use something called von m and pico and this is the

**[3:16](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=196s)** restricted pico to load the model and here let's try try to use the vony and you can See that by default the V honing is set to false and we can easily RC we are a evil pico module and you can see that we just use this reduce magic method and we will execute who am I command and we save the module and load the module and you can see that we successfully executed the command and we get without root. However, when they use this only equals true and it get the exception and it seems that the vulnerability is effectively mitigate and also on the official pets document you can also see that they consider withholding because true is safe and at the same time they just warn that

**[4:06](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=246s)** loading and trusted checkpoint withholding equals false must never be done and additionally when similar security issues are discovered in other Python based components they often fix it by using with only equals true and here is an example um initially VM will use torch low without only equals true and which introduce a security risk and then they just patch this vulnerability by adding the V only equal true parameter. So not only v in general whenever a software is found to be using tor low with always equal true and the fix is always the same way just adding this parameter but here comes the question can we blendly trust the security of v only.

**[4:54](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=294s)** So next next we will d deep into how von works and earlier we point out that wony is something like a restricted pickle. So before we analize how vis is implemented, we need to understand how pico works. So ple is just like an interpreter and it reads an interpreter each of code in sequence exact executing the corresponding operation. So here in this case we first encounter the global operate op code and it will execute the global function and here it will just load the target function and so for example here we will load a system function and we will push it onto the stack and you can see here and then since we get a function and we need to construct the parameter. So first we

**[5:44](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=344s)** will execute the uni code up code. We are execute the uni no uni code function and read a string. So here it is one my command and since the command must be the turbo so we need to wrap this string into a turbo format. So we just add a turbo one of code and we will execute the lo turbo function. So you can see here after we execute the load unic code function and we will push the my string onto the stick and after we execute the load turbo function and it will wrap the string to a turbo and push it onto the stick. So finally since we get the function and its parameter and we can execute this function and so here we will use the reduce op code and it will

**[6:32](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=392s)** use the reduce function and you can see here it will get the argument from the stack get the function from a stack and skewed and we can achieve command execution. So how does this hony address this issue and through the above example and we can roughly sense that the most dangerous part of the pleization is load global and load reduce. So the visi and poder just add some restriction to this dangerous op code. And looking at the code we can see that the load global function just add some white list and the blacklist check to prevent a security issue and also reduce function is restricted in a similar way and you can see that it has a white list.

**[7:20](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=440s)** So in general is implemented using a white list and a blacklist. So how can we bypass and a common approach is to analyze the wet list and check if there are any potential dangers of code and I did the same but didn't find anything useful in the end. So we are often limited by the fixed way of thinking. So for example here most of us may only see the obvious red list and the blacklist mechanism and just stop here. However, in reality, the loading process of torch load with with only equal true is quite complex. So, is it possible that there might be some issues elsewhere in this loading process? So, I try to conduct a complete size of the mison workflow to ensure

**[8:09](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=489s)** nothing is missed. So I started analyze the entire workflow and I just stepped through the code and debugging line by line and during one of my exercise I found this something strange and you can see that torch.g download.load. So what is that and I have no idea what is that so I just searched on the Google and you can see that torch.g download.load is used to load the module in the 10 to script format. So now you can understand why we name this topic yet we will analize and exploit the to script engine. Uh hello everyone this part will cover the basics of touchcript and it's cover and it's code implementation. Uh touchcript is used for deploy model in environments without passing

**[8:57](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=537s)** interpreter such as CPP and mobile. It can be compiled from Python code through scrapity or tracing and save as PT field which can be loaded and run in CP and mobile. The save and load process involve serialization. I will explain how present code is compared to touchcript the serialization and the des serialization process the structure of PD fields and finally how it executes. First we can use touch g subri the decator to subcribe the one pass function to touch subscriber. The steps to compile uh first press python source code into a passing j pass pt. Uh second

**[9:48](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=588s)** convert the pass st to a jst. Third transform the jst into an graph. The air graph initially contains many unnecessary notes which are eliminated through optim optimization to make it more consens. Let's take a deep look about the compare process. Before we start, I need to show what is function model in this context. You can see that one is function, the other is a class. The crafting process for these two are somewhat different. So we will introduce them se separately. Uh let's look at the function for the

**[10:38](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=638s)** compile process is handled within the subcrimp mpl function. Uh there are two main functions get j get jt defaf and jt scrap compile. The gadget def function performs two main tasks. First, it use the a library to convert a passing code into a pass and second it calls build dev to transform that pass into a surf employment the JTS. JT subcribe the compel is a function bond from CPB to pass which is a primation in the CP layer. Uh it further converts the GTS to IR within the CP CPP

**[11:30](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=690s)** code. It eventually invokes the two IR function which finish the transformation from the from JTS to IR. Additionally, the two will also cause some optimization functions to simplify the air graph. Now the function compare process is done. A high level understanding of the process is enough for this cherry. If you are interested in the complete source code details, you can use the costic I have provided to navigate through the source code. Then let's look at the model. The overall process is similar to that of functions. It's back there is an

**[12:21](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=741s)** additional step at the beginning uh retrieving all functions from the model. It need retrieve functions and compare functions one by one. The stops fn function is responsible for handling this. Stops fn is analyzed for infer methods to compel in this context. Let's see what the infer methods to compare function does. In the infer method to uh compare function the first use di to get all attributes and methods of the model. After filing uh it saves the methods and uh need to be exported

**[13:14](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=794s)** thereby collecting all functions in the model that require complation completion. Uh make stop from method converts the collect functions into JST using the get jab function which is the same one use during function completion. Back to create a scrap model. The method stops variable stores the jst we just converted and the create methods and properties from stops function takes method stops as input and is responsible for converting it to AR. We can see that uh this cost deck is

**[14:04](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=844s)** similar to the cost deck for function completion. Both eventually finish the conversion in to IR function. Now the model compare process is done. Overall uh some key steps are the same for function. Uh both use uh get JT dev to convert pass to JTS. uh then convert it to AR through to IR. The serialization future can save models and deploy them to other environments. Use touch save to save model as a PT field. Use touch load to load a PT field as a model. The passing code here is converted from IR. uh you can you will not uh you will

**[14:56](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=896s)** notice it's slightly different from the original code. Let's see how scraped function model is serialized. Subscribed function has an additional step compared to secret model. It needs to be converted to secret model first. After that boots call serialize for serialization. During the serialization process some information is stored in the data pair and the constant pair in pico format. Additionally it receives the passing code converted from AR and the debug information to the code folder. Uh to summarize the save process the

**[15:47](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=947s)** touch function first converts it to touch model. Uh the remaining process is the same. First the I value corresponding to the model is serialized in p format as a data p. Second, obtain the code and debug info info via pass python print and write to code directory. Uh third uh save tensor constants to constance pkl. Now the serialization is done. Uh let's look at the detail of the pt. The PD field is compressed compressed the chief using zip compra compression. Uh the field tree after decompression is

**[16:38](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=998s)** show above and we can use ple tools to dis disassemble it. Above is disassembled output of data pri. The meaning of this ple code is create an object named the touch placeholder model and set its training attribute to true and the touch placeholder model object is a secret model. So when interpreting data PKR secret model object is create the constants PKR contains the pico code uh which can create a value object with tensor the touch p contains the passing code strings

**[17:25](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1045s)** uh it's same as the container of the code attribute in circ model because they are obtained in the same Okay, now we understand the three most important parts saved in the serialized field. Data Pare, constant Pare and the source code in the code directory. Let's see how these parts are restored into a secret model. We can see that in the detail in the des serialized function uh the three main parts of the serialized field are converted into a circular model. Let's look at the function information.

**[18:15](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1095s)** D serialize will call reader achieve to read the constant PK and the data PL. Uh then we will get one model in CPP which is C model in Python. Uh during the deserialization process first I'm picking constants VKR to obtain tensor constants then save in constants table. Second uh uh unpickling datab during the process of unpickling datab relevant source code is read from the code directory and pressed as needed and the rel related constants are also read from constants table.

**[19:02](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1142s)** The code pressing process includes converting passing code to custom tokens and then transforming tokens into JSD and finally into IR. This process differs sign uh sign uh significantly from the earlier pass to touch subscribe the complation completion. To summarize the load process, first reach main logic via DS serialize. Second call reader chip to read the constants PKR. Convert consents to a values by MP and save them to consent table. Third call read to read the data PR restore corresponding values by MP.

**[19:54](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1194s)** False during data pick and pickling uh source importer reads code fields and constant table to restore gra through press tab uh find the name tab uh to import named the tab uh now the d serialization is done let's take a look at how touch is running the graph consists of many nodes And every note contains three parts G output and OB code. OB code is the key to execution. The process of executing touch is to iterator through the AR and the

**[20:42](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1242s)** converter it into instructions then interpret the instructions. Every instructions have one OB code. There is a switch table here that finds the corresponding process logic through the op and then execute state. Now we have a basic understanding of the touch book flow. We're reading the code. I discover something interesting. Uh there is one OB code names OP and it can do a call on operator table. The operator table contain many functions corresponding to operators.

**[21:30](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1290s)** I'm curious about uh what operators they are. Some built-in functions register themselves as operators requiring OP OP instructions to call corresponding functions and the register operators class manages this operators and register them by register operator function. I pitch the passing code and add one function to output all operators. You can see there are over 2,000 operators here. Uh are all these operators safe? During our code audit of these operators, we discovered two interesting

**[22:20](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1340s)** operators uh save and from field. Uh here we can see the corresponding implementations of these two functions. The C function will receive a specified uh value as a model to specify the field. Uh this can lead to arbitrary field rate. Uh but the save the field contains a lot of dirty data which model information. Uh the from field function will read the field which I specified and convert the field container to tensor then return the tensor. Uh we can get the field contained from the tensor. Now we know these two operators have vulnerabilities

**[23:11](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1391s)** but there is still a problem. How to call them from touch script? To get the answer, we need to first understand the operator registing process. The get builtin table function registers some object member functions to builtin ops and during this process it also adds perfect to the member functions for rename. Finally, the address of these functions and their modified names will be placed into built-in table during the touch compilation process. The two sugar uh the two sugar value

**[24:03](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1443s)** will be triggered uh where it checks whether the pass object exists in built-in table. If it exists, it return a built-in function. Finally, this built-in function will be inserted into the air graph through emit a built-in call. Uh back to the built-in register register registering process. We now understand that as safe may come from one of these objects in models containing builtins. Um by testing each object individually for the uh process of the of a save function we will find that the save

**[24:54](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1494s)** function exists in the touch object. Item from field can also be obtained in the same way. Uh so we just need to call touch save or touch from field to get arbitrary field read write ability in touch script. Now we can try writing a field to get. But since there are some dirty uh characters only specific uh specific uh special fields will work will work. The Ubuntu the Ubuntu contact failed because of a permission mismatch. Our subscriber use touch save which writes fills with uh

**[25:48](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1548s)** six 44 permissions but the column diamond requires 600. Uh here is one poke video. Little kids. You can see we get a CE.

**[26:44](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1604s)** Uh the vulnerability the vulnerabilities we have discussed so far might be more similar to logic vulnerabilities. Uh on the other hand some hackers may prefer the memory related vulnerabilities. Uh we have also discovered a few overflow issues. Here you can see that uh when we load this model field it triggers a hip overflow. As a result as a result we can observe that hip address and the loaded address of the library are leaked. Uh this is one p video um but due to time limit we won't go

**[27:44](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1664s)** into them in great detail here. We hope to share them in the future. Okay, this is the full details of this CVE. The fix is also relative simple. It prevents the use of touch transcriber when you when only is enabled. So next what is the impact of this vulnerability and the entire AI ecosystem is just like this house and the PyTorch is the fundamental components and if there is an issue with PyTorch and just like we have removed one of the bottom level components and the security of the entire structure is then compromised and we did a simple

**[28:32](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1712s)** search on GitHub for projects using VO code true and you can see that a lot of project use it in this way and here we select two well-known projects as examples to illustrate the impact of the vulnerability and one is VM and the other is transformers. So what is VM? And VM is a high performance library for large language model inference and optimized for speed and marriage usage. And so why did I choose it? because in fact it happened to have a CVE and the CV just said that it used to load function without only equals true and which has a security issue and they just patch this vulnerability by adding the Wing parameter and this seems to solve the problem but because of the Ptor

**[29:20](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1760s)** issue and all mitigations end up being ineffective. So the safe harbor you once thought was actual household or waters. Also at the same time I noticed a very interesting phenomena and you can see that in the requirements CPU. Txt and you can see that it just hardcoded Pytor torch version and also in the GPU version and you can see that it auto hardcoded the PyTorch version. So what does this mean? It mean that even if a pettor released a new patched version and users of VM cannot update timely because VM has headcoted this version as a result the they will remain exposed to this vulnerability for a long period and first let's set up our environment and since the latest version of VM has

**[30:09](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1809s)** already fixed this issue and we need to install an old version exactly 0.7 7.3 and you can see that when we install VM and it will invol in install the old version of PyTorch and so here we will use this function as an example to demonstrate the existence of the vulnerability and at first I didn't think too much about that I just want to take the poke from the previous vulnerability and use it right away hoping for one should succeed and you can see here when we enter the logic we want it will definitely load our model so we succeed did but in fact just field. So why and let's take a look at the previous poke and you can see here after loading the

**[30:57](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1857s)** model and we need to invoke this model in order to trigger the forward logic but here after we loaded the model and you can see that it doesn't invoke the model it just gets the atoms attribute and then delete this model. So is this really the end? But however at that point I noticed the exception message. It just said that the model object don't have the attribute named atoms. And this error message just provided me with some inspiration. So atom is essentially a keyword in Python and commonly used to it iterate our dictionary type objects. As you can see here in fact atom is a function. So can we spoof the function name? And so I

**[31:47](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1907s)** made my first try and however when we write the code in this way I just change the function name from forward to items and I run the code but you can see that we still get this error message and why it is still saying that we don't time the item attribute but however I try to write it in this way and we just call the items function in our forward message then you can see that when we run the code and then we successfully trigger the logic And so this are very very strange and the reason is that when we retrieving the model functions it defends whether this function needs to be exported for function not decorating as export and there is a lazy export strategy. So meaning that they are only exported if

**[32:37](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=1957s)** called within export functions. So here you can see that in the left picture and you can see that we just decorated the item function with torch.g.export. So here the object the the model will have the items attribute and in the right window we don't decorate it with tog.export. So we need to call it in the exported function. So in this way and the object will have the atoms function. And if we don't do that our object will not export the ATM function. So it will definitely not find this attribute. Then in the end we report this vulnerability to VM and they highly acknowledge our work. Then you can see that the team member just

**[33:24](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=2004s)** said that this is very interesting because PyTorch document just claim it safe and the page is very simple. You can see that it just change the version of PyTorch. And the next is transformers. So transformers is a very popular open source library made by hugging face and provides pre-trained models and tools for natural language processing tasks using transformer-based architectures. And also so we can see that transformers also implemented some security hardly measures for to load and you can see that it's by default will use only equals true and first let's set up our environment since the latest version of transformers has already fixed this issue. We need to install the old version 4.51.3

**[34:13](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=2053s)** and this is the official usage guide for transformers and you can see that we need to load the module and create a pipeline and then we can start having a conversation. So for example we can ask the secret to backing a really good kick is and here I got the answer and to use the right ingredients and for the recipe. Oh, and here you can use my demo report. And you can see that my report only has three field. One is read me and another is configur ptor model.bin and this is a test pytor field. And if we run the code and YouTube will finally call the torch.g.load to load our eo model. And let me introduce the code. And you can see that

**[35:02](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=2102s)** when we're loading a remote report and it will load remote model and by default fields in zip tensor format or in pyarch formats are allowed and like the previous vulnerability and it will invoke the keys function. So here we can just spoof the function name to keys and save the model in our local report and and then just load the local report and you can see that of course we can explore it and then I just report this vulnerability to to the trans to hugging face and you can see that it just fix it and they fix it. The patch is very simple and you can see that it has just checked the pytor version must be greater or equal 2.6 and finally is some defense

**[35:52](https://www.youtube.com/watch?v=rZ0Jnu5gtqo&t=2152s)** recommendation and summary. So if you are still use the own version of pytorch please update now and here is also some recommendations. So for the model format perspective and you can see that we should use more secure format like tensors and also from the model community perspective we need to scan and flag malicious models and also hugging face has already done the relevant works and from the user perspective we should not know and trust this model and it is better to know this model in the second box. So any questions and thanks again for listening and if you have any further questions please feel free to contact us at any time.

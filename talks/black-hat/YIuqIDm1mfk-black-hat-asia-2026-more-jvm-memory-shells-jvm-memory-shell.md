---
id: YIuqIDm1mfk
title: "Black Hat Asia 2026 | More JVM Memory Shells: JVM Memory Shell Auto Searching Program"
slug: black-hat-asia-2026-more-jvm-memory-shells-jvm-memory-shell
conference: black-hat
conference_name: "Black Hat"
category: "AI security"
edition: "Black Hat"
year: 2026
speakers: []
channel: "Black Hat"
duration_min: 28
published_at: 2026-08-23T13:30:07Z
video_id: YIuqIDm1mfk
youtube_url: https://www.youtube.com/watch?v=YIuqIDm1mfk
tags: []
transcript: true
---

# Black Hat Asia 2026 | More JVM Memory Shells: JVM Memory Shell Auto Searching Program

**Speaker not identified**

`Black Hat` · `Black Hat` · `2026` · `28 min`

[Watch the recording](https://www.youtube.com/watch?v=YIuqIDm1mfk) · [Conference site](https://www.blackhat.com/)

## Description

A Java memory shell is a fileless backdoor that resides entirely in JVM memory, leaving no trace on disk. Attackers exploit code execution vulnerabilities—such as ScriptEngine injection or deserialization flaws—to use Java reflection to replace legitimate objects in web frameworks with malicious classes. Once implanted, specially crafted HTTP requests (mimicking normal traffic) trigger arbitrary command execution within the JVM, with results exfiltrated via standard HTTP responses. This stealthy technique blends seamlessly into legitimate traffic and bypasses firewalls that only allow ports 80/443, rendering traditional reverse shells ineffective.

Over the past eight years, common variants have included Tomcat Filter, Tomcat Listener, and Spring Controller memory shells—all dynamically injected at runtime. However, the discovery of new types has largely stalled in recent years, relying almost exclusively on manual source code audits.

We have developed an automated framework for discovering Java memory shells, integrating SAST (Static Application Security Testing), Java Agent–based hooking, JVM runtime memory introspection, and AIpowered PoC generation and validation capabilities. This framework dramatically accelerates the discovery of novel memory shells: in a very short time, it expanded the number of known Spring memory shell variants from just 2 to 9. Moreover, it is adaptable to any Java web framework for uncovering new memory shell techniques, significantly enhancing the efficiency of Java memory shell research and surpassing years of manual efforts.

Litong Wan  |  Cyber Security Engineer, Alibaba Holding - Risk & Security Dept
Fanghai Yu  |  Independent Security Researcher,
Yang Jing  |  Cyber Security Engineer, Alibaba Holding - Risk & Security Dept
Dongyan Zhang  |  Senior Security Engineer, Alibaba Holding - Risk & Security Dept
Huan Zeng  |  Senior Security Engineer, Alibaba Holding - Risk & Security Dept

## Transcript

*3,856 words · source: supa (en, exact timings)*

**[0:10](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=10s)** Good afternoon everyone. Thank you for being here. Today we want to talk about Java membersh this area has been shaped by a small number of millia techniques and a lot of manual research. But as the ecosystem gets more complex that approach is becoming harder to scale. In this talk, we will share how we start how we started rethinking about that problem not just as a single exploit challenge but as a broader research and discovery problem. The goal is to move from isolated findings to a more systematic way of understanding what is actually possible. Next, a brief introduction to the speakers. I'll start with myself. I'm Wanong, also known as Miu, a cyber security engineer at Alibaba and former CT player of Chi team. My work focus on

**[0:58](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=58s)** Java application security offensive research and AI agent security is a privileged speaker at blackhead area today. This is our other speaker. He is Euonghai also known as elate. He is an independent security researcher. His work focus on application security and vulnerability research. He has a lot of experience in SAS and RASP and he has worked on building security systems for real world use. He also studies low-level vulnerabilities and has found several high-risk issues in web logic, JDBC and distributed protocols. There is a plan for our talk. In the first three parts, I will introduce what memory shares are, how this field has developed and the current situation looks like and the overall design idea of our project.

**[1:48](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=108s)** After that, we later will present the technical details. He will explain how our system works step by step including start java inter tracing j memory search and AI based poke generation. Finally we will share the main results of this work including seven new memory shell types and talk briefly about the future work. Next I'd like to start by explaining what a g mesh shell is. In simple terms it's a fileless backd dooror that lives entirely in J memory. Unlike a traditional web shell, it does not drop a malicious file onto disk. Instead, it injects malicious logic directly into a running Java application. It is usually implemented through a code execution vulnerability such as expression injection, Java derialization or other Java C issues. After getting code

**[2:36](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=156s)** execution, an attacker can use reflection to modify key parts of a Java framework replace existing objects or logic and attach a malicious class. After that, the attacker does not need obviously malicious traffic. In many cases, a normal looking ATV request is enough to trigger it. The malicious code can then execute commands inside the JVM and return the result through the LTV response. So J memory shells have three main features. They are fileless. The traffic is still and they do not need an extra outbound connection like a reverse shell. So you can think of a ge memory shell as a ste in memory back door for modern Java applications. Next I will introduce a classical memory shell the Tomcat filter memory shell.

**[3:24](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=204s)** This type is classic because it uses a very cool part of Tomcat which is the filter chain. While request enter Tomcat, it first goes through the future chain before it reaches the sublet and the real bin logic. So the CAD is simple. If the attacker can replace a malicious filter into this chain and make it run before the normal filters, then later requests will pass through the malicious filter first. This gives the attacker a global point to intercept request. Under normal conditions, filters can be rejected through standard APIs. But after the web application has finished the initialization, this IPI usually cannot be used anymore. The classic Tomat field memory sh bypasses this limit by using reflection to directly change the internal structures

**[4:12](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=252s)** inside a standard context. After that, Tomcat treats the malicious filter like a normal component and puts it into the request chain. If it if it's uh if it is placed at the front, it can handle requests before the normal screw logic and before the business code. So in the end, this memory shell does not create a new entry point. It hijacks Tomat existing tomcad mechanism and turns the normal component into a new memory back door. That is why it's still and effective. On this slide, let's take a quick look at the history of J memory shares. We can roughly divide it into four stages. The first stage is from 2014 to 2016. This was the early idea stage. At that time, the Java time repack door shows

**[5:00](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=300s)** that even if even if a JSP file was deleted, malicious malicious code could still stay alive in memory. This was an early sign of fireless attacks in Java. The second stage is from 2017 to 2019. This was the initial development stage. Researchers began to introduce classic tomatan memory shells such as filter subert and the listener types. Memory shells become more systematic. The third stage is is from 2020 to 2022. This was a fast growth stage. Tools like behind and gozilla added memory shell support and new types appeared in spring websocket and other areas. The last stage is from 23 to now. Memory SHs are now basic knowledge in Java security but manual discovery is getting harder and the new tabs have become much less

**[5:47](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=347s)** common. On this slide I want to explain the basic idea of how RASP defend against memory shares. From the defender view the key point of Rasp is to stop the injection process at runtime. It usually starts from the original code execution vulnerability and then keeps tracing the costic to see whether some high-risk actions appear later. For example, it checks whether sensive method like define class are called, whether dynamic bad code injection happens or whether a new malicious component is being added into the container. Once these actions are detected, RASP will further analyze the in the injected back code, especially its class type and behavior pattern and then decide whether to block it. In other words, it's my ideas not to wait until the memory shell is fully in

**[6:35](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=395s)** place. it tried to stop it during the injection stage. But there's also a clear limitation. In many cases, both injection blocking and letter detection and letter detection still depend on no memory share features. So when the attack when the when the attack uses a new type of memory shell that has not summarized before, this kind of defense can still be quite passive. This slide explains how defenders detect and remove memory shells. The basic idea is to scan the classes already loaded in JVM memory and check whether they have matching classes fired on disk. If a class is running inside the JVM but no related file can be found on disk, it might be it might been generated or injected at runtime. So it becomes a key target for inspection. After that

**[7:22](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=442s)** defender usually dump these suspicious classes from memory and analyze their back coding inheritance and behavior patterns to decide to decide whether they match no memory shares. A typical example of this kind of tool is coins Java me Java membershare scanner and you can search it on GitHub. So in ence this is a post infection detection method. It means the memory shell is already inside the JVM and then the defender tried to find it. But this method also has a clear limitation. It still depends a lot on non-membershes. If the attackers uses a new type of membership, the effectiveness of this approach can be limited. This slide shows that the manual discovery of J of J membershares has reached a clear bottleneck. In the past,

**[8:11](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=491s)** the euro way to find a new member shares was to manually review the source code of Java frameworks. Earliest classical types such as filter based and listener based of memory shells were discovered from native token mechanisms. These components naturally sit in the request pro uh in the request processing part. So they were relatively easy to turn to back doors. But the problem is that after this obvious entry points were gradually studied continuing to manually read large frameworks like spring and Tomat started to cost much more time while the results become smaller and smaller. In other words, researchers spend a lot of time reading code by finding but finding fewer and fewer new memory shell types over time. This also leads this also leads to a fixed midset. People are more likely to keep extending

**[9:01](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=541s)** no types instead of systematically looking for new injection points. That's why in the past two years there have been very few new memory shell types in mainstream Java frameworks. This shows that relying only on manual source code auditing is no longer enough for efficient discovery of new memory shells. Because my work, I often study new J memory shells and review the source code of Tomcat and Spring. At the end of 20 20 and 24, I found a new type in spring is the flash memory shell. It works in spring MC and you can think of it as a interface hijacking memory shell. Unlike the tomat filter membership, it does not target a native tomat content. Instead, it targets this part inside the spring muc. The part I want to highlight most is is injection

**[9:49](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=589s)** ID. It does not depend on the current requested context. Instead it starts from the uh it does it it starts from thread objects already present in J memory follows object references by reflection reaches key tok and spring objects and finally locates the dispar instance after that it replace the freshman manager field with the malicious implementation. This means the whole injection process can be completed fully in memory without writing a fire to disk. And because of a uh because a key fragment manager method is called during normal request processing. The trigger is also very natural. It does not need a fixed UI extra port or a reversed connection. It can hide inside normal business traffic which makes it a

**[10:39](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=639s)** very sty. This also shows that in spring MC long live interfaces on the request path can also become new memory entry point. on this slide. The key point is not the f map uh is not the frame memory itself but the syncing process that come after I found it. When I was reviewing the spring source code I found that f manager could could be used as a memory share point but after I found it my my first reaction was not just now we have one more new type. I I started ask myself why this one what would make the stable and usable memory share point. As I thought more about it, I realized that fetch map was not just a special case in spring. There are many other classes that could also become candidate points. So the real goal is not to remember one class

**[11:27](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=687s)** name but to summarize the common features behind this point only then can reach move from experience and luck to a repeatable and scalable process. Later I summarize this into three key features. First, the target should be a standard spring component or at least implement a standard interface so it can be replaced without easily breaking bending logic. Second, the target class itself must be replaceable. For example, plus uh for example public and non-final. Third, it must actually be called during request processing. So the trigger is stable and the point has real value. So for me, the value of the fl me memory sh not just one more poke. More importantly, it pushed me to move from manually finding one point at at a time to summarizing features and searching for candidate

**[12:16](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=736s)** classes in batches. From there, I started to think seriously about turning J turning J membership discovery into a structured or even automated method not only for spring but also for other Java IPC frameworks. On the last slide, on the last slide, I said that I said that I after I summarized the common features behind the f memor shell, I want to use code to search for similar points in batches. But after I run it, I found a problem very quickly. The amount of data was huge. In spring, many classes matches match this static features, but that does not mean they can really become stable memory shell points. So I realized that static analyzes alone was not enough. Uh memory shell was at runtime.

**[13:03](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=783s)** So this problem should also be checked at runtime. That's why I introduced the the Java agent instrumentation since colure had already given me a group of suspicious candidates. Next step was to hook them at runtime and see whether they were really triggered during request processing. This idea is similar to IST. We added hooks to candidate method then send a test request or replay traffic. In this way the question changes from does this class looks look suspicious to is this point really called at runtime. For me this step was very important because it quickly removed a lot of noise and left a much smaller set of candidates for deeper analysis. Uh by this slide the early ideas can already be brought together

**[13:51](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=831s)** into one complete system design. Our goal is not just to find one p one specific memory shell. We want to turn the discovery of a new memory shell in into a repeatable process. By combining static feature filtering with runtime hooks, traffic replay and tracing we can narrow the result down to highly suspicious candidates to make the process complete and able to produce full pokes. added two more parts runtime memory pass search and AI based information sorting and poke generation. The Jware memory search step means that after we confirm the candidate can can really be triggered, we continue to check whether it has real usable condition at runtime. For example, can we reach key objects and can we reach the target instance

**[14:39](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=879s)** that can be replaced? Then we pass the collect then we pass the collect context to AI to help generate posts and the human researchers do the final verification. In this way the process moves from manually reading source code for ideas to a more complete chain of static filtering dynamic validation memory memory analysis and generation. AI is moving very fast. In this project, I used the earlier version of our RM in my experiment and now the model has already gone through several runs of iteration with much better performance in P generation with the I believe future security research will become more even more interesting. I have always believed that just like the line from freing beyond the journey's end the

**[15:26](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=926s)** world of magic is a world of imagination and security search is the same. Next the detailed technical implementation of each stage will be presented by you late. Thanks one. I will take take over from here and go into the details. In this part I will explain how we turn the idea into the working play. We will start from SA then movie to Java agent checking J memory switch AI poke generary and final verification the P point is simple we do not want to no ideas uh we want a process the can find and from new memory sh point at this

**[16:18](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=978s)** stage or first the problem is not exported code or first the problem is scale Spring has me many class callbacks and internal components. If we read all of them by heart, it takes a very long time and most of the worker will not lead to useful result. The phone on this side. So the my idea we start from a laser spring framework through face uh then we use symmetrical filing at the end we get a similar candidate set at this step we are not seeing a classes pendable we are only asking which class

**[17:08](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=1028s)** was checking at the runtime. Oh sorry uh this side show what our future is looking for. We are not looking for and older memory shop pattern. We are looking for a useful class should a good candidate should be public uh no final and attribute is should also be inside the spring framework. More importantly it should be created by the framework. Sorry as f in class and car later by framework in the second query. We also perfect method close to request or response handling. This help us move more close to real web traffic not just

**[18:00](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=1080s)** source code that's looking interesting. uh static furing is useful but it's not us the table show that we can already get a meaning for candidates their name are not random they are hit by important framework class and they are called in real framework logic but after uh fing we were we still have many runtime entries so s is not the final OS way it's give us a similar list and better starting point after this these new runtime data to uh decided which

**[18:49](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=1129s)** candidates are really useful and which is once are only losses uh this is we are at the Java agent checking as start as state candidate is only because with we need to see viz is ready calling when the application uh hand has httpical call the picture show the flow we pass the s the result into the agent the we send HTTP test request to the application the agent records which candidates methods are really hit after at KP set write the use forceful results into the event dis fair

**[19:41](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=1181s)** if a method is together by real tical we keep it if it's never together we drop is noises the agent is a beauty ground ruler is is not a set of hard cs as a share it and SL and the GBM start or it can attach later to a running GM the center transfer read hooks ruler from the input resource. Uh it can also read break point ruler from the list little memory switch step. This design is important because we can change the import list without changing the agent

**[20:31](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=1231s)** code. Uh in order words the pipeline stay fl fant the data changes but the aging logical can say the same the aging output is stis. So we turn into the cleanensis fair. This slide show the data flow from uh runtime events the pure hand record and then to started evidence for each hit. We record the class limb mass limb on a field pal sweet and cost. This fire is the b to nest stage is not just a lock and it is not a pocket. It's a

**[21:23](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=1283s)** stretched runtime last step can release and continue the ns in a st. Now we know a method is ready ready call but we still need to know whether is can help up build a memory store. The key question is simple. Uh when is break point in this hint? What object can we reach from this moment? The memory memory switch to start from the current street and the work source object file in looks for use for object switch as request response s or framework context object. This giving us a real object graph from the running TM.

**[22:15](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=1335s)** This is important because memory short need a reachable object without a useful object pass method may still be useless. The project graph give us a injection part in the left part of the side. We have the part formed by memory s is made star from a s more source server object and final rework object in the right part. We transfer the pass into reference code step by step. We get the f make it accessible and read the next object. So the hard part is not guessed by AI. is uh found by runtime memory

**[23:08](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=1388s)** usage. It can help vary code later but uh the pattern uh set of come from the real GM state. This is where AI become useful. The waring approach is to ask AI to find a memory short in hunger st space. The giving the model to sweet too much freedom and the result is hard to trust or operation in diff different. We give AI a trusted individate information or message information runtime and source code and the memory part. The the automat worker flow helps

**[23:59](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=1439s)** generate a pock candilate. So AI is not exploring f is woring waring code inside a clear bounded user face counted by the early stages. The AI staple is counted by prom contract. This model must read the real target source code before writting code. This helper over fake API and worrying message them. The output also have a clear text targeting source code in your logic and verification hints. This make the result for hmer research to reverse. So this is not open and cheating with AI

**[24:51](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=1491s)** is a conting state based on private and this model have with coding but pipeline controller is not input and the output generated code is not a prof of so we also run a verification loop the side so the fuel loop in simple way. First the workflow query create the poke then we rebuilding the J from source after then we deploy it and injest uh it into the target application. Final we send a request and check the uh call output if fail the fing goes back

**[25:41](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=1541s)** into the loop. This is important because AI code can look current but still fair or final stud and simple rebuilding it run it and covering it. Uh with with this pipeline we covering this spring me down on this side. Uh the key point is not only the count the resource shows that memory s logical can leave in more place the older common entry point has many inter parts s as even handing page fair handling uh request handling

**[26:31](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=1591s)** content choose and states fair handling some of them and also become sh becomes short. This means the defenders should not only look at the old entry point that's possible surface is uh the bigger value is uh changing research method many different local uh for long memory s or no signature or to return about right a point can be used in reachable. reachable way as shown here. We can run the same pipeline on new frameworks. Uh we can also try all Java framework and

**[27:20](https://www.youtube.com/watch?v=YIuqIDm1mfk&t=1640s)** miniware the don't not replace researcher still need to review and verify the the results. But uh instead of finding one point by luck uh we can test the many possible point to in a more statement way. If you have a question after take please use on X we many releases for this project as open source later on GitHub. Uh after that you can report possible or send ideas of source ins. Thank you. [applause]

---
id: ox2lq9vsC8Q
title: "SecTor 2025 | How Adversaries Beat User-Mode Protection Engines for Over a Decade"
slug: sector-2025-how-adversaries-beat-user-mode-protection
conference: black-hat
conference_name: "Black Hat"
category: "Security conferences"
edition: "Black Hat"
year: 2026
speakers: []
channel: "Black Hat"
duration_min: 43
published_at: 2026-04-19T13:30:52Z
video_id: ox2lq9vsC8Q
url: https://www.youtube.com/watch?v=ox2lq9vsC8Q
youtube_url: https://www.youtube.com/watch?v=ox2lq9vsC8Q
tags: []
topics: ["Security, safety & red teaming"]
transcript: true
---

# SecTor 2025 | How Adversaries Beat User-Mode Protection Engines for Over a Decade

**Speaker not identified**

`Black Hat` · `Black Hat` · `2026` · `43 min`

[Watch the recording](https://www.youtube.com/watch?v=ox2lq9vsC8Q) · [Conference site](https://www.blackhat.com/)

## Description

Following the largest global IT outage in history in July 2024, many took to the public stage advocating to prohibit endpoint security vendors from deploying kernel-based components, even prompting regulators to weigh in. That launched an effort to evaluate the impact of the proposed design shift, as many endpoint-oriented security solutions, from different malware analysis tools to various commercial products (like AVs, EDRs and sandboxes), already include user mode-based engines.

The research started with examining open-source projects and publications such as SysWhispers and FireWalker, and continued by analyzing and reverse-engineering malware families of all types in the wild, including infamous names like Emotet, SmokeLoader, HijackLoader, FormBook, DarkGate, Hive ransomware and Winnti, among others. Over 55 different data sources were ingested, all in all, mapping the entire threat landscape and tracking the evolution of adversaries for more than a decade. Curating the ultimate collection on the subject yielded in-depth understanding and insights into attackers' tradecraft and made it clear that this is the most prolific post-exploitation technique yet, surpassing even code injection methods.

This session will explore all 27 unique methods which security researchers and malware authors have developed to beat user mode-based protection engines, cataloged under 3 main tactics: Hook Evasion, Argument Forgery and Engine Disarming. The trade-offs of the various methods will be highlighted as well. In addition, the session will include detection schemes, focusing on runtime and forensic indicators, to aid malware researchers, incident responders, threat hunters and detection engineers tackling these issues.

By:
Omri Misgav  |  Security Researcher, Independent

Presentation Materials Available at:

## Transcript

*6,068 words · source: supa (en, exact timings)*

**[0:02](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=2s)** Thank you, everyone. Um so, my name is Omri. I'm currently an independent researcher, um mostly doing reverse engineering of operating systems and malware. I was uh previously uh head of uh security research group at Fortinet. And I'm also past speaker in previous uh uh venues such as Def Con, Avar, and uh it's my first time here at Sector. So, what we're going to talk about today? Um basically, we'll discuss what is even a user mode uh based protection engine. Then, we're going to cover three major tactics that attackers actually use it in order to beat them. And then, uh we'll give you some conclusions at the end. Um just before we begin, let's talk

**[0:50](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=50s)** about what even what have even what are the evasive evasive strategies that attackers actually go for? Um basically, when they want to evade an attack, they try at least when they are based on the endpoint, um either to obscure the context so you as a defender won't see them won't see them coming. Um they try to blend in uh in regular execution chains. Um this goes for all the masqueradings, code injections, DLL side loadings that you guys know. Or they try to break apart the execution chain. Um either try to somehow um use a scheduled task or a auto run key, something that will uh trigger a later execution that won't be uh

**[1:40](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=100s)** connected to the current state. And lastly, they try to um hinder the visibility and hide the actions that each process takes. Um so, this is this is uh our major um area that we are going to discuss today. And let's see how it translates into more specific. >> [snorts] >> So, now that we understand the general uh strategy, now let's try to connect some uh technical details here. How even user mode protection based engines uh are are built? So, this is a high-level architecture. And basically, the component we are talking about is the DLL that is being placed inside

**[2:29](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=149s)** um the monitored processes. It gathers the telemetry, and it's the one that enforces the uh the response, whether to block an operation or not. But, it's not the component that actually uh makes the decision if an an operation is good or bad. Um why is even user mode monitoring being uh deployed by defenders? Um so, first, because it's user mode code is more stable and uh more simple to develop. There is no patch protection in user space, unlike in the kernel, at least in the case of Windows. And also, you get the full context. Um the most simple example of it is if you have an application that uses SSL or TLS sockets, so all the all the traffic is

**[3:19](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=199s)** encrypted um in the OS and on the line on the line, but uh the user space at the application layer, it will be plain text and available. How the monitoring is even possible? Um basically, with instrumentation, uh meaning that some libraries are pre-baked by their developers uh to have some uh ability to report back. Or when the uh security solution uh hooks specific function and intervenes um on runtime uh with the code itself. One other uh term that we have to cover so the rest of the talk will be more clear is call stack. Um basically, call stack is a kind of a data structure that holds

**[4:08](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=248s)** information on um what parts of the code actually took place uh when uh an an operation happens in an inside an application. This is a an example uh of a code flow of a specific function, and all the functions and the libraries that are uh being um used while uh while calling it. Um on native native languages, native binaries, the call stack is something more logical in more in other uh um other languages that are managed, call stack is also part of the runtime, uh but that's a different topic. So, in case we're talking about native applications, which is how the OS is built,

**[4:56](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=296s)** um defenders need to find a way to monitor um basically, the functions. They do that mostly with inline hooks. Inline hooks modify the code as as we mentioned before uh on runtime. And they do it in memory, okay? They don't touch the disk, they just patch the different instructions on the uh memory itself. It's the most common type of uh um hooking uh mechanism that actually used uh by most security vendors. I included a link for a an example. And also, open source project uses if you guys know Cape or Cuckoo and the Frida toolkit, which defenders usually and malware analysts usually use. So, just to understand the the timeline

**[5:46](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=346s)** here, um this is a a graph that I I made of uh all the the dates that the new techniques that we are going to cover were published. And we can see that around 2018, uh there is a starting trend and uh growing growing trend of more and more novel techniques that are being used by uh attackers, which are malware developers and red teamers in order to beat those uh uh those uh security products. All right. Um couple of notes here. Even though you saw you saw the graph that is linear over time, all of the techniques that I'm going to cover are uh ordered are being placed in a not in a chronological order, but

**[6:33](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=393s)** uh in a manner that it will be more easy to understand them. And uh there are a lot of references along the talk. There are slides at the end uh for the links for them, and you can see square brackets uh on the slides for the relevant reference. So, let's deep in let's go inside the the the first technique, the first tactic, sorry. Um which is called hook evasion. Its name is kind of self-explanatory, but we have to understand just how to be able to do that. Um how can we evade the hook? Well, we have to if we are an attacker, we have to execute original instructions and of the uh target uh function, and then be able to somehow continue with the rest of it. There are roughly 20 techniques to do that, um which are

**[7:21](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=441s)** clustered into four into four classes. Uh secondary DLL mapping, binary restoration, uh direct system call invocation, and code splicing. So, let's let's start with the first one. Second uh secondary DLL mapping is quite self-explanatory. The attacker uh will try to create a second instance, which won't have any hooks installed on it. And then, uh since it's set up it uh set it since it set up its own instance, it will just go and use it from that time being. Uh so, how can it be done? There are three methods to do that. Um with reflective loading is the first one. If you know it, it's very famous, very well-known for attackers to use it in order to load their their own payloads, not necessarily system DLLs,

**[8:10](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=490s)** but in this case, they will use it with for system DLLs. They can the second method is to clone the DLL on disk and load it. And the third method is to remap the section um and use lower level uh NT APIs in order to do that. >> [snorts] >> So, we're also going to give you some tips um and directions on how to detect those kind of methods. Uh so, let's let's begin uh with the detection opportunities. Um as every good uh black hat style talk, we have some a lot of screens filled with text, numbers, and we'll try to sort them out a bit. Um here what we can see on the left side of the screen is

**[8:59](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=539s)** a a call stack that I took using a debugger uh for a malware that uh used reflective loading um for a secondary DLL DLL mapping and tried to call the create process uh function. On the right, you can see uh the same uh call stack, but without any uh changes uh any malicious changes. So, it's pretty obvious that we can easily spot that there's there is some kind of code region that is well, not that related to the program, and it's kind of like a floating memory and floating executable memory, and if we compare, as I mentioned, to the regular call stack, the difference is quite clear. Also, um because it's a the the DLL was reflectively loaded, can see the size

**[9:49](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=589s)** will match of the will will match the normal DLL size, and there are other attributes like the headers that you guys can uh try and compare and those would be the same as well. So let's uh talk about detection for cloning DLL. So in case you haven't been able to monitor file access file operations, um it's quite easy to see that uh in this case as well, there will be some module on the call stack that won't be clear on why it's there. Um and you can compare all the rest of it of their metadata um that will be one by one identical to the regular DLL that you expect to see in that uh execution flow. So the detection for section remapping

**[10:41](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=641s)** is similar as well. Um but now uh we'll have a different base address for the that we expect from the same DLL. And we also can see that the debugger picks on it in this case and also attaches a different um a different label to the module. And again, all the other stuff will be the same. Uh >> [snorts] >> all the metadata, the headers, um one thing that will be missing is uh a PEB loader entry for the secondary loaded DLL. >> [snorts] >> Just to recap everything, I put up this table. I'm not going to spend too much time on it because it pretty much clear covered all of it.

**[11:31](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=691s)** And we go to the second class under this tactic, which is pretty self-explanatory as well. Basically, unhooking. Um Once uh if we look at the the modules life cycle, the DLLs life cycle, uh we can see that the code change after it is installed. So if we can somehow restore it back, basically we evade hook. The If we think about it, first of all, everything we just discovered discussed on the secondary DLL loading provide us with the original code. So we can just use those techniques and use them in order to restore the code instead of just using them the section that the instance that was loaded

**[12:20](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=740s)** the secondary instance that was loaded, sorry. The second method that we can use here is if we don't want to have direct access to disk, is we can use basically another process and have a different process do the disk access for us and get the code from its memory. Either we run as an attacker, we run create a suspended child process and then that that will be only applicable to NTDLL and that we can use it in order to load the the to obtain the original code. Or we can use a debugged child process and then wait for the specific data that we need to be loaded in it. Or we can just look it in some other existing process um for the reason that not always all processes are being monitored by the

**[13:08](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=788s)** security solution. So you can just rip it off their own their other process memory. The third method is called section refresh. Um it's similar to remapping, but instead of adding another instance, we'll just remap the same module in place on the exact same base address. We capture uh as an attacker, we capture the current state of the data sections. We get a handle to the section. We unmap and then map the code again at this exact base address. And then just restore all the data section that we captured, so the DLL will continue to run when we need to use it. The last method in this class is called

**[13:56](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=836s)** short circuiting. Um if you think about it, for the hook itself to work, the original instructions have to reside in memory somewhere. So what an attacker can do is basically find those instructions, find this stub, find this memory region by searching, by scanning the process address space for specific memory with specific attributes and then search in in those regions for the specific uh call influence instructions that goes back to the hooked function. Once we find it, we have two things that we can do. Either uh redirect the hook, overwrite the hook itself to the original to the original stub that was copied. Or modify the trampoline.

**[14:48](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=888s)** In this case, um because the hooks were removed, basically there won't be any differences that you can see that we can see in the call stacks. Um but in still in runtime we we'll have some indicators that something malicious is goes on. Um if we don't see any anomalies in the call stack, um at least for a few of the methods. So when you clone a DLL, as we mentioned, the sizes are the same even though the path is different. Um and also as we mentioned before, the headers are similar, so we can check them. In section remapping, we can see that the same system DLL was loaded twice uh just with different base addresses. We talked about it before. And with

**[15:36](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=936s)** section refresh, we see it happens at the same base addresses I mentioned. Um what we can see here when we talk about the again on the detections aspect is at least forensics-wise, uh it's quite easy to pick up on. And well, because the hooks are removed, so if we capture a memory snapshot of a process or a system, it's quite easy to analyze. Um somebody actually released a volatility plugin last year for this. Um so you can go check it out. And also for short circuiting, if it's quite easy to >> [snorts] >> um have some automation that picks up on the hook itself and figure out the hook was modified afterwards. Um

**[16:25](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=985s)** All right. So there are with all of the techniques we mentioned so far, there is one big issue. They all require system calls. They all require system um system services from the operating system. So even if we uh even if attackers like don't want to be monitored, they have to use the they still have to use those functions. So they they have no way to get around it. So for this, they came up with a very specific with a very specific class of methods to bypass hooks specifically for system calls. So we need to understand first the API of a system call. It has a very specific API different specific calling

**[17:14](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1034s)** convention different from regular um functions. Basically, for the kernel to know what the user wanted to to execute, to perform, uh you have to provide it with the index. Um this on Windows, it's done on the it's being set on the EAX register before transitioning to the kernel. And you have two instructions that perform this transition from non-privileged to privileged mode with the syscall instruction and the INTOE instruction. Um what's interesting here is that the INTOE uh instruction, it was the legacy it was the legacy used uh in Windows, but with newer OS versions, it it was brought back.

**[18:02](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1082s)** Um if you are familiar with VBS, uh virtual basic security mechanisms, so actually originally it meant to improve performance. At some point with VBS, they understood that the performance is even better with INTOE and they brought it back. And now if [snorts] you have VBS enabled on your system, the default instruction that will be used by the OS is INTOE. So how can you perform direct syscall invocation? Well, you have to find the index somehow. Uh the basic way to do that is to pass the the library that is in charge of it. Uh pass NTDLL. Basically, get it from disk or from memory as we talked about before. And pass patterns or disassemble instruction

**[18:51](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1131s)** and get the value. Even if the memo if the NTDLL um in memory is being hooked. Once you do that, just call directly for the syscall. There are two other ways to pass NTDLL. One is by counting and the second is by by checking the neighbor. I'll explain it. Um as you can see, I put uh two versions of the the DLL. One the hook one is hooked and one is without hooks. And there there are offsets next to the functions. Those are exported functions and they are sorted in memory uh in an ascending order according to the index value. It's not by chance. It This is how Microsoft

**[19:40](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1180s)** actually compiles and builds the the binary. And it goes like this for years. It's not like just It's not like a one-off or something. >> [snorts] >> So if you sort out the list of exported functions yourself, you can just count the functions without parsing the instructions. Or if you want to go if you're a bit more lazy and you you can go directly to the function you want to use, and then just check the index in one of its neighboring function and increase or decrease depending depending on the direction you took. The second method for direct system call invocation is called Event Gate. Um basically, it's a method it's a way it's a technique to run uh

**[20:27](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1227s)** for 32-bit applications that run on 64-bit operating system to run 64-bit code. Um it was initially meant to interact with 64-bit native processes, but it also presented some opportunities to bypass the hook itself. The first variant here basically skips the whole uh 64-bit emulation layer. Uh once the malware uh does the transition to the 30 to the 64-bit context, it just goes through the 64-bit native DLL. And the second uh variant here um basically uses all the other uh previous methods that we discussed to parse NTDLL and then just uh do the

**[21:16](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1276s)** syscall directly. The third method uh if we parsed uh NTDLL in user space, we can also parse the kernel uh in user space as well. The reason for it if we don't want to touch the NTDLL file is that uh the reason that it's possible is because uh the kernel has to have the same uh translation table for the indexes in order to actually um work correctly with the user space component. So, uh in the kernel in the kernel binary, we can load it up into user space. We don't need kernel read access at all. Um basically, we can we need now we need to look for a different kind of functions. They are the ZW exported functions that they are the one

**[22:03](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1323s)** uh that hold the index themselves, and we can search for the same pattern for the same EAX assignment pattern. Other functions that don't have their uh ZW function exported, we need to go through through some more hoops. Um we need to find the syscall table itself. Um it is a private uh and not exported uh data structure, uh but there are there is a way to find it. If you take pairs of ZW function and anti functions that are exported and extract the index and the offset, um you can scan using those values for the base address of [clears throat] the table itself. And then take the offset

**[22:52](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1372s)** of the anti function you want that is not exported and search the um search the table for it for the for the offset for it, and then get the index out of it as well. Um there is some limitations to this uh to this method. Not all the functions are usable. Not all the syscall functions are usable, but if you write a malware, there is pretty large uh API set that is available to you and you can do pretty much whatever you want. The next technique is called Bring Your Own Index. Um some of you might be familiar with Bring Your Own uh vulnerable driver. This is Bring Your Own Index. Um basically, here the an attacker has the has the system call

**[23:43](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1423s)** index numbers hardcoded in its code. He has to support They have to support all the target OS they want and basically um per each version of the OS, have to know the specific index that they of the each uh function system call function they want to use. Uh which is pretty pretty complicated things and cause a lot of maintenance work. >> [snorts] >> The last method uh for dynamic system call invocation is called dynamic resolution. If up until now we did everything statically, um where it's parsing or pre-compilation and stuff like that, uh now we're going to try and do it dynamically. Basically, uh we use an exception handler

**[24:30](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1470s)** to control the execution of a thread, and then uh on that same thread, we find the address of the system call uh instruction. Uh we place a hardware breakpoint uh on the address, or we can just start single-stepping to that uh address. We call the the function that we want to extract the system call for it. The breakpoint will hit or the single-stepping will reach the uh the specific uh instruction address. And then the uh exception handler will have the the context structure and we can extract the EAX value from it. So, detection for this on runtime is

**[25:20](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1520s)** pretty pretty simple. On the left, we can see that the call stack is really really really small. Um and if we look at the return address, we can actually see the syscall instruction inside of the uh of the malware binary, which is not something that should happen uh usually. >> [snorts] >> Um and we can see on the right how a normal system call happens. But that won't cover Event the case for Event Gate. Um in Event Gate, there are two there are two stack calls. So, there are two call stacks, sorry. Um one for the 32-bit context as we can see on the right, and uh and and the one for the native context, the native 64-bit context. Um so, that will

**[26:09](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1569s)** look different. >> [snorts] >> Uh if we look on the left uh for the skip whole 64-bit layer variant example, we can see really really weird uh call stack on the 32-bit context. And we can see the exactly where the uh direct API was call was done on the 64-bit context. >> [snorts] >> And in case of the completely native, we'll see the transition on the call stack goes directly to the kernel. Okay. Um so, we talked about the the runtime detection. Forensically, it's very hard to detect this technique

**[26:56](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1616s)** um unless you don't count um any [snorts] any option for attacker to obfuscate their code uh because in order to detect this, you have to scan the code instructions and really look for a syscall or into instruction outside of uh system binaries. Um and uh one one major drawback this technique has it it's not uh generic, so it's only limited to syscalls. Um As I mentioned before, um the case of VBS in case of VBS is used, the syscall instruction won't be used anymore. Uh so, that's also another indicator that we can pick up on runtime. Uh if a syscall instruction is being

**[27:44](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1664s)** seen on the on the call stack, it will allow us to detect the more sloppy implementations of these techniques. So, our last class for this tactic is called code splicing or byte stealing. The concept for it is basically rebuild the original stub elsewhere. Um And it has two variants. One, we load this the uh the attacker will load the um the original stub from this call on its own. It's pretty similar to what we discussed before with loading the with using reflective loading to get the code. And uh once it does that, it just extracts the stubs and leave them in

**[28:32](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1712s)** memory floating in memory without the entire uh DLL around them. And then even if functions are hooked, the malware never go through the hook themselves. >> [snorts] >> The second variant is called uh is named is uh stub reuse. Basically, as we mentioned before, uh the stubs are already in memory in case the functions are hooked. So, we don't really need to touch disk. We don't really really need anything to do um other than finding them. And once we do, we can just call them directly. Now, if you want to detect this, the situation will be much harder. There are no real real real runtime indicators or forensic artifacts that we can use,

**[29:23](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1763s)** um but still this method has some some issue with it because it doesn't take care of any hooks, so lower [snorts] level APIs can still be hooked and monitored. So, just to summarize everything we talked about and give you some general notes, um there are additional uh variants and additional uses for these techniques. For for example, um the all of the methods for direct system call invocation can also be used for binary restoration, specifically for NTDLL. Um code splicing uh can also be considered as indirect system call invocation when the the target function is a system service, is a function in NTDLL.

**[30:12](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1812s)** Um and almost all of the techniques are effective against hardware breakpoint as well if you try to do some dynamic analysis for malware um with debuggers or certain emulators. >> [snorts] >> So, we covered most of the content, most of the methods, but we have two um small uh tactics that we're going to cover. It won't be long. We're getting uh forward quite nicely. So, in uh if up until now we tried to evade the hook, attackers in this case will try a different approach. Instead of basically go go around the hook, they'll go this they'll try to go straight for

**[31:01](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1861s)** it and still won't get caught. And to do that, basically they can feed the hook um fake arguments and at some point change them in order for the uh for the target function that they want to use to actually do what they uh to achieve what they want. Um so, basically this is a type of vulnerability that is known as uh time of check to time of use. Um and we need to find a way to stop the execution without touching the without manipulating the memory um at a certain point that will allow us to do the swapping. Uh we can use hardware breakpoints or single stepping as we mentioned before. That's pretty great. Um doesn't require APIs, doesn't require

**[31:48](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1908s)** any actual um code modifications in memory. And there's [snorts] also another option, but it's not really reliable. An additional uh option is to use some fake argument that will cause the protection engine code to have some uh some error, to have some to cause to generate some exception on its own. But the issue with it is well, it highly on the specific code of the defender's product. So, it might not be possible to use it all the time. So, this is implemented again just to cover it quickly. We set up an exception handler. We talked about this before. We find the address of the

**[32:36](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=1956s)** instruction the protection engine returns to in the target function. And we set a breakpoint on it or start or start single stepping. Uh once we do that, we call the function with fake values. For instance, uh in this call the second argument is the URL. Um we can see that it was a benign URL. And then we pass through the protection engine which called at some point will call back again to the original instructions. Then the breakpoint will hit, the exception handler will trigger, and according to the calling convention, we'll swap the arguments. In this case, we change it to the we change the URL to a malicious malicious

**[33:24](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=2004s)** domain. And just continue execution and profit. >> [snorts] >> How can this technique be detected as well? Um on runtime, uh the protection engine can check for hardware breakpoints or a trap flag, which indicates a single stepping that is enabled. >> [snorts] >> Um when when those are present without a debugger attached, it's quite conspicuous. It's not something that happens in regular process operation. And because the protection engine is still invoked, there is a good chance for the defender to actually pick up on it on runtime and prevent it, not just detect it. The next the last tactic we're going to

**[34:12](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=2052s)** cover is engine disarming. Um in this method in this class of methods, basically instead of evade the hook or go directly through it, um attackers will try to leverage some mechanisms in order to disable the protection engine itself. So, they can either try to use a free library um API in order to to unload the DLL. Uh they can unmap all the DLLs. Uh they can try to do that, but it will leave them in a pretty um difficult situation that they won't have any system libraries available to them and no other functionality. Uh so, it's pretty bad. Um they can try to create a new process and leverage the process uh mitigations

**[35:02](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=2102s)** that allow only Microsoft signed code to be loaded, but that will limit them to not able to have their payloads um being their payloads won't be able to be DLLs because that's exactly what they those mitigations are blocking. It's not likely that the attacker's DLL will be signed by Microsoft. Or they can an attacker can preload in an in a new child process and then use certain APIs in order to control the process life cycle and when exactly um other DLL and other codes are being loaded and monitor them um and prevent in case of in case of detecting um

**[35:49](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=2149s)** the DLLs that are for the security product preventing them from being loaded. There are also other vendor-specific implementations like flipping flags inside of the engine themselves, um but those are specific implementation. Uh vendor-specific implementations and we won't get into that in this talk. Um just to quickly explain how can we detect them those those techniques. In runtime, it's pretty simple to if especially if you are the one who are building the security solution, to monitor for the state of the of the user mode component uh that you inject.

**[36:37](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=2197s)** And if it's sud- if it suddenly appears, uh suddenly disappears, sorry, or if it's kept in an uninitialized state, it's quite easy to understand that somebody tried to fiddle with it. Um and in case for in case of the forensics investigation, it's quite hard to detect whether or not uh uh there was some um uh some misuse, some an attempt to disarm the engine if you are not aware of the initial the initial state that the engine should have been in the first place. Cool. Now, let's talk about the conclusions. Um Before we get to them, just one other

**[37:26](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=2246s)** issue. Um If pure user mode products have additional limitations that defender should take account for, they can't provide boot time protection um because at boot time only the OS only the kernel is being executed and the user space components are not available yet. Um they can't provide themselves with sufficient self-protection as long as you're running with the same privileges um as your attacker, uh you can you can't really defend yourself against him against him. And as we covered quite briefly in this talk, there are many ways to bypass user mode hooks. Um you won't have access to where all is

**[38:16](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=2296s)** critical to all the OS areas and critical processes, which means that you have it harder to monitor for other issues like for other things like RPC calls, privilege escalations and stuff like that. And also um you if you use only user mode um user mode capabilities such as ETW or event viewer and stuff like that, you won't be able to provide real-time response or quick enough response because those mechanisms are are actually deferred. So, we have one last slide. I'll try to make it briefly because it was quite long. Um All of the things you saw here are

**[39:04](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=2344s)** really easy to implement and use. If you want, you can find source code for them for most of them at least online. Um Many of them share a lot of commonalities, a lot of uh primitives. If you felt that I'm being repetitive, it's because I it's because I was and not by choice. This is actually how the attackers operate. Um and basically they just take some primitives with and do some minor changes to them. Um at the result of the research, it's quite easy to understand that this is the most prolific uh post-exploitation technique. There are even more techniques to bypass user mode hooks than there are techniques to inject code.

**[39:53](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=2393s)** Uh I didn't mention MITRE ATTACK so far, and I think it's a challenge that I managed to do that managed to do so for 40 minutes without uh uh bringing it up. Um so, the ATTACK framework uh covers uh all of the subject that I uh presented here, but only like in one paragraph. Um and only very uh in very limited manner. It only mentions uh uh the binary restoration methods or the engine disarming. Um I did submit uh new techniques to MITRE, and I hope to get some uh response at sometime. And also, these these techniques are uh and these evasion methods are the uh prerequisite for other evasion methods

**[40:42](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=2442s)** like ETW and AMSI bypasses um and others. And in case uh we talked a lot of about detection using call stacks. In In case uh attackers are quite aware quite well aware of that, and they try to spoof the call stacks themselves. There are ways that they we as a we we as defenders can cope uh can cope that with that. Uh we can leverage uh CET shadow stacks. Um if you have a relatively new newer hardware. Um or if you build your own emulator or uh or sandbox, uh you can implement your own uh transition transition-based code uh tracing if you control the hardware. One last slide, I promise.

**[41:33](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=2493s)** Um very funny very it's quite funny to know that uh user mode uh code can still cause stability issues. Um if you ever uh heard about critical process, if you try to kill one of them, um well, you get a blue screen. And as we mentioned uh user mode uh hooks uh using user mode hooks for security is just not enough. They have a very uh integral flaw. Uh the the fact that they rely on the same environment that they want to protect. And that would be all. I'm just showing the references that you can uh want to take some pictures if you want for a few seconds. And we have time for questions. If any anyone has some.

**[42:26](https://www.youtube.com/watch?v=ox2lq9vsC8Q&t=2546s)** I also mentioned that uh I'm I'm planning to release a white paper uh that will be more complete and will have more references uh to all the malware families that actually uh use those techniques. There are a lot of more uh from references from the list here. Um and it will have more information uh about the techniques themselves and detection opportunities, etc. And that's it. >> [applause] >> Thank you.

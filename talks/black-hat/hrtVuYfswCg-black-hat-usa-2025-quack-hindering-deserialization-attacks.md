---
id: hrtVuYfswCg
title: "Black Hat USA 2025 | QUACK: Hindering Deserialization Attacks via Static Duck Typing"
slug: black-hat-usa-2025-quack-hindering-deserialization-attacks
conference: black-hat
conference_name: "Black Hat"
category: "Security conferences"
edition: "Black Hat"
year: 2026
speakers: []
channel: "Black Hat"
duration_min: 35
published_at: 2026-01-02T18:28:00Z
video_id: hrtVuYfswCg
url: https://www.youtube.com/watch?v=hrtVuYfswCg
youtube_url: https://www.youtube.com/watch?v=hrtVuYfswCg
tags: []
topics: ["Security, safety & red teaming"]
transcript: true
---

# Black Hat USA 2025 | QUACK: Hindering Deserialization Attacks via Static Duck Typing

**Speaker not identified**

`Black Hat` · `Black Hat` · `2026` · `35 min`

[Watch the recording](https://www.youtube.com/watch?v=hrtVuYfswCg) · [Conference site](https://www.blackhat.com/)

## Description

Managed languages facilitate convenient ways for serializing objects, allowing applications to persist and transfer them easily, yet this feature opens them up to attacks. By manipulating serialized objects, attackers can trigger a chained execution of existing code segments, using them as gadgets to form an exploit. Protecting deserialization calls against attacks is cumbersome and tedious, leading to many developers avoiding deploying defenses properly.

We present QUACK, a framework for automatically protecting applications by fixing calls to deserialization APIs. This "binding" limits the classes allowed for usage in the deserialization process, severely limiting the code available for (ab)use as part of exploits. QUACK computes the set of classes that should be allowed using a novel static duck typing inference technique. In particular, it statically collects all statements in the program code that manipulate objects after they are deserialized, and puts together a filter for the list of classes that should be available at runtime. We have implemented QUACK for PHP and evaluated it on a set of applications with known CVEs and popular applications crawled from GitHub. QUACK managed to fix the applications in a way that prevented any attempt at automatically generating an exploit against them, by blocking, on average, 97% of the application's code that could be used as gadgets. We submitted a sample of three fixes generated by QUACK as pull requests, and their developers merged them.

By:
Neophytos Christou  |  PhD Candidate, Brown University
Andreas Kellas  |  Security Researcher, Columbia University

Presentation Materials Available at:

## Transcript

*5,780 words · source: supa (en, exact timings)*

**[0:03](https://www.youtube.com/watch?v=hrtVuYfswCg&t=3s)** Hello. Uh, thank you everyone for being here with us at the last briefing session. The title of our talk today is Quack, hindering digitalization attacks via stag typing. So, let's start off with some introductions. I'm Neo. I'm a PhD student at Brown University. I do research in system security including program analysis, speculative execution attacks, and more. And I'm presenting today with Andreas. Hi, I'm Andreas. Um, I'm a PhD student at Columbia University and like Neo, my research applies program analysis techniques techniques to solve security problems. Um, and this presentation today will be an example of that. Um, but this work was done collaboratively with um, Yaniv, who led this work but can't be here today and our advisers Vasilis and Juong. So over the years we've seen numerous

**[0:55](https://www.youtube.com/watch?v=hrtVuYfswCg&t=55s)** talks about the dangers of deserialization within our applications and passing untrusted inputs to them. But what we'll notice is that many of the talks with few exceptions are about attacks. And our defensive advice tends to be a warning to developers not to do this um resulting in CVE over the years regardless. And so we set out to design a mitigation that can be applied as a defense in depth to help prevent the exploitation of these attacks. And this resulted in a system that we called Quack um specifically implemented to solve this problem in PHP applications which we'll use throughout this presentation. And it was published in an academic venue um called NDSS. And so the goals for our talk today are

**[1:44](https://www.youtube.com/watch?v=hrtVuYfswCg&t=104s)** to one introduce Quack to the security community and try to help it escape the lab and be put to more practical uses and two just continue to raise awareness about the the risks of deserialization attacks and hopefully give you a new way of thinking about uh how they might be mitigated and defended. So to get into the details of what Quack is and how it works, we'll begin with some background on how these PHP deserialization attacks work and then we'll get into Quack and we'll describe how it uses static program analysis techniques to harden applications against these attacks and then we'll end with some takeaways about where Quack can go and the future of decalization exploits. >> Cool. So let's start off um with the

**[2:34](https://www.youtube.com/watch?v=hrtVuYfswCg&t=154s)** exploits. So most people here have probably at least heard about ditalization vulnerabilities. They're still a pretty common type of vulnerability. Multiple CVS issued every year. These are just some recent examples of um PHP digitalization vulnerabilities which is what Quag is concerned with. Um so PHP provides this function called un serialize takes in a string representing a serialized object and then it just reconstructs the object and returns returns it back to the running program. And as you can see at the bottom there's a big red warning that at some point says that a malicious user may be able to exploit this. So let's assume we have a PHP application has a call to unz serialize somewhere and the input to it comes it's trans data coming

**[3:22](https://www.youtube.com/watch?v=hrtVuYfswCg&t=202s)** from the user uh and let's say that when the programmer wrote this application they expected this call to un serialize to return an object of this class called app and this would look something like this. So on the at the top you can see what the input string would look like. You don't need to worry about the exact sy syntax of it. The important part the important part is that it encodes information about the type of the object and its properties. So when this is this runs with this input, it would create something like the object at the bottom. So it would be of type app and with one property with the value my web app. However, if this input is controlled by the attacker, they can first of all change the properties of the DC serialized object. So for example, they could just change the value to some different value.

**[4:11](https://www.youtube.com/watch?v=hrtVuYfswCg&t=251s)** But beyond that they control the whole type of the dialized object. Uh so instead of uh dializing the type of the object that the developer intended they can just uh make the app dialize a complete different type. And they can even go a step beyond that and they can actually cause it to dialize a whole object hierarchy consisting of nested objects by just setting the properties of other objects to new objects. So they can visualize a bunch of different objects. However, their actual goal is not to just dialize and inject random objects in the runtime. They want to actually divert control flow and execute something malicious. So let's see how uh the ability of injecting ar arbitrary objects allows them to do that. So continuing with this application that

**[5:00](https://www.youtube.com/watch?v=hrtVuYfswCg&t=300s)** Neo's introduced, imagine that it also has a class shown on the screen named command exeutor which has two very very convenient properties for an attacker. One, it has a a method that um directly overlaps with a method that's called on the desialized object. And two, it's it has a trivially malicious implementation such that uh an attacker that controls the command property of this object invokes system on these objects. Um and so when an attacker provides a string like this, the attacker achieves their goal of invoking some new method that the programmer did not intend and executing some malicious functionality. But this is quite a contrived example um

**[5:48](https://www.youtube.com/watch?v=hrtVuYfswCg&t=348s)** and trivially exploitable if it exists in the application. So making this one step more realistic. Imagine that this run method doesn't exist. There's no direct overlap with something that is invoked on the D serialized object. What is the attacker to do? Well, PHP has a feature named magic methods, which are methods that begin with a double underscore and have special purposes that are automatically invoked in certain circumstances. In this case, the wake up method is invoked automatically when an object that invokes this method is deserialized. Um, and other such useful magic methods um will be called when the object is destroyed or when it's converted to a string. And so these are useful for an attacker to identify locations and events and um invoke new methods.

**[6:39](https://www.youtube.com/watch?v=hrtVuYfswCg&t=399s)** But still this class has a pretty trivial um trivally useful for an attacker implementation of this magic method um that probably won't exist in a real application. And so making this again one step more realistic. Um what we are more likely to see are classes that implement these magic methods but with some more realistic implementation that doesn't directly hand an attacker the ability to implement uh calls to system. So what is an attacker to do in this scenario and how does the the PHP um uh dialization exploits proceed from here? The tech technique that's been used for years is called property oriented programming or POP. And the general idea here is that the attacker will instantiate or create a

**[7:28](https://www.youtube.com/watch?v=hrtVuYfswCg&t=448s)** string that when D serialized creates some top level object that is strategically chosen because it it has a a magic method that will begin executing um as the attacker needs it to. And then the attacker will nest objects within this within the properties of this tople object that are chosen such that their methods are executed in sequence after the um the initial tople magic method is. And this kicks off a chain of um attacker chosen functions that will eventually reach a malicious function of the attacker's choosing. And this is a high level explanation. And I'm about to illustrate it with an example. So if this, you know, if it's not clicking right now, don't worry. Um, but we've known about this technique since at

**[8:14](https://www.youtube.com/watch?v=hrtVuYfswCg&t=494s)** least 2010 in a black hat talk by Stefan Esser. So again, just to illustrate this, um, we've taken some classes from a an application with a known or with a a real CV that is exploitable um, with a dialization attack. And we'll we'll step through an example of how this pop chain would work. And so each box here represents a class within or a small subset of classes within this application. And within one of them there is a a method that has a vulnerable call to un serialize. The programmer really only intended for one class to be um deserialized at this program point and that's this program uh highlighted on screen. and every other class within the application should

**[9:03](https://www.youtube.com/watch?v=hrtVuYfswCg&t=543s)** should not be represented in the serialized string. But the attacker is able to recognize that there's a another class within the application that has a useful method for them. It's this method named current. And within the implementation of this method, it invokes some callback that the attacker could potentially set for themselves with some argument that the attacker can control as well. The problem here is that this current method isn't uh directly invocable by the attacker. It's not a magic method and there isn't a convenient invocation of current on the D serialized object. So the attacker's question is how do I invoke that method? And what they do is they'll strategically identify some other classes within the application and have create a serialized string that

**[9:54](https://www.youtube.com/watch?v=hrtVuYfswCg&t=594s)** encodes one of these classes that has a um a magic method like this lock class that has a destruct and then set the properties of that initial object to objects of types of these other classes and invoke invoke the methods of those classes through the detailization process after that destruct is uh is invoked eventually reaching the current method um with attacker uh controlled values invoking a call back here that system with a the command of the the attacker's choosing. Um, and so the key takeaway here is not necessarily how this works, but that the attacker is able to pull off this attack by having access to more classes than the programmer intended to be instantiated at this derialization point.

**[10:46](https://www.youtube.com/watch?v=hrtVuYfswCg&t=646s)** Cool. So now let's uh go and take a look at what Andreas just explained, but let's see it in an actual demo. Um so the what I just just described is a slightly simplified version uh of an exploit for a real CV against an application called Moodle. It's basically like an e-learning platform. Uh and it contains this snippet. As you can see, there's a call to un serialize and this is the vulnerable part. And basically, it's a part it's in a part of the codebase where a user can upload an XML and then a field of that XML is a serialized object that's going to get passed to this un serialized call. So, let's see how that's going to work. So

**[11:36](https://www.youtube.com/watch?v=hrtVuYfswCg&t=696s)** uh sorry. So first uh let's take a look at the actual XML that contains the exploit. It's very similar to what Andrea showed. Uh it's slightly more complicated. There's just a few more classes, but the basic idea of how it works is the same. And as you can see, the attacker has set the call back to be called to system and then the arguments to it. They're going to just echo something nice. So we have running instance of the model application. So the way like the vulnerability is in a part of the code where the user can upload a quiz. So we just select the vulnerable format

**[12:23](https://www.youtube.com/watch?v=hrtVuYfswCg&t=743s)** and then upload the XML file we just looked at. And now when we click import the exploit chain Andreas described should run. And if it runs successfully we should see something printed on the screen. And indeed as you can see the exploit run successfully and it printed a nice logo. Um so we've seen how the attack attacks work, how the vulnerabilities work. So now let's talk about mitigations. There's a few different ways uh recommended ways to mitigate these types of attacks. First one is to just not use un serialized and instead use a syntation like JSON to store your data. And this can work pretty well, but it only works if you want to store simple data like arrays and primitive types.

**[13:12](https://www.youtube.com/watch?v=hrtVuYfswCg&t=792s)** and it doesn't really work very well if you actually want to store complex PHP objects. Another recommendation is to uh ensure the integrity of the serialized data using cryptographic caches. Again, again, this can be a good solution if the application itself produce the data. However, again, that's not always the case. And the last recommendation actually is provided by the un serialized function itself. And it's an optional argument you can pass to it called allowed classes. And the way it works is it takes in a list of class names and when that specific call to unialize happens, PHP will only dialize objects uh if they're uh instances of one of these classes that was were provided to allowed classes. And if it if it encounters a different object,

**[13:59](https://www.youtube.com/watch?v=hrtVuYfswCg&t=839s)** it's just going to replace it with a dummy object that has no methods and no no properties. Uh so if we go back to our earlier example uh where uh we assume the developer wanted to only dialize uh an object of the app class. Uh this time they provide the allow class argument and they set the list to only that class. So the attacker's input that was trying to dialize a different type of object is not going to get instantiated anymore. So their exploit is not going to work because no method calls would h will happen and in this specific instance eventually the application will will crash because um there's going to be a method invocation on it like the run method uh which doesn't exist on the dummy object that was created but still

**[14:47](https://www.youtube.com/watch?v=hrtVuYfswCg&t=887s)** the attackers exploit is going to be prevented. Now, unfortunately, even even though this uh mitigation works pretty well as long as you provide a small set with only the classes you wanted to dialize, it's not really used very much. We found that only around 0.1% of calls to un serialize in GitHub repositories even use this argument. There's probably two main reasons why. First of all, developers probably are just not aware of the dangers of serialization and how they can be exploited. The second reason is that it's probably pretty tedious to manually go through your code, go through every single code to un serialize and figure out what classes you actually meant to dialize. Especially if you've already written your application and you weren't aware of these, it's very hard to go back and figure out what you meant to do. So there's two main takeaways we want

**[15:37](https://www.youtube.com/watch?v=hrtVuYfswCg&t=937s)** you to remember from all this. First is that these types of vulner vulnerabilities are exploitable because attackers have access to and can create objects of more classes than what the developer actually intended. And second, that there are some mitigations, but they either don't always apply or are not really used very much. Okay, so let's get into what Quack is and how it works. And we'll begin with its overall design goal. So after studying how these attacks work and the existing landscape of u mitigations for them, what we decided was that if we could build a system that could automatically infer that programmer's intent for the classes that should be allowed to be deserialized at

**[16:24](https://www.youtube.com/watch?v=hrtVuYfswCg&t=984s)** a given point, we'd be able to restrict the application using that allowed classes argument to um break these exploits that the attacker is using in pop chains. Um now inferring a programmer's intent perfectly is a challenging to impossible task. But what we do instead is try to use program analysis techniques to learn about um what this programmer may have intended through the way the details object is used. So at a high level what quack does is given an application with some set of classes declared within it. It first uh determines a set of the available classes that are loaded at that d serialization point or could be loaded. Um intuitively this is effectively the the classes that are included in all the files that are part of an include statement within the points in the

**[17:12](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1032s)** program that this un serialize uh is invoked. It's a bit more complicated than that because of dynamic nature of loading in PHP but that's the idea. Then uh Quack will statically analyze the program and observe how the object the D serialized object is used within the program to infer a set of possible classes that the programmer may have intended um the object to represent. And then Quack will take the intersection of those two sets leaving just a set of the classes that are candidates uh to pass as allowed classes to restrict the behavior of that unserialized call. And as a system um quack looks like this. You input the PHP application source code. It analyzes it in two passes to create these two sets and it will output a set that can be passed as

**[18:02](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1082s)** allowed classes. And as an implementation detail, um Quack is built on the Jurn static program analysis framework which if anyone in the audience uh does program analysis, you may be familiar with. Now let's uh look at some details about u about how quack actually works. So as we mentioned the first thing quack needs to do is identify the set of available classes at each un serialized call. By available we mean the classes that can be imported at that specific uh point in the program. So to do that um quax starts by analyzing the file that includes uh the actual contour call to serialize. It adds all the classes included in that file h to the set of available classes and and then it visits

**[18:50](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1130s)** all the files that are included by the current file adds the classes defined in those to the set as well and just continues doing that recursively until there are no more files to visit. After doing that, it goes back to the original file and then it does the same process but backwards meaning that it visits the files that themselves include the current file and add the adds the classes are defined in those to the set as well. And the intuition behind this last part is that we could have reached un serialized starting from any of these files. So we have to consider all the classes defined in these available as well. Uh class loading in PHP is slightly more complicated than this. There's something called autoloading uh where class can be imported without explicitly being included in the file but quack takes that into consideration as well.

**[19:37](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1177s)** So uh for the next component quack needs to uh figure out the possible types of the object based on how it's used in the code. to do that. Uh it's based on a concept called duct typing which is a concept in dynamic languages that basically says that an object is of a given type if it has the methods and properties that are required by that type. So in this simple example we have here there's a there's um an object called animal and there's two method invocations on it swim and fly. And what duck typing tells us is that this animal object can only be used as a type duck because duck is the only class here that actually defines both of these methods. So the way quack works is it statically analyzes the source code of the application sees how the object is being used and it

**[20:26](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1226s)** collects evidence about what its possible types are by using type inference rules that are based on this uh duct typing concept. So let's take a look at some examples uh of those rules. First, basically the example we just saw. So in this case, Quag would look at the first use of the D serialized object. It would say that this can either be of type quack or whale because both of these classes define the swim method. Then it's going to take a look at the second use. It's going to see that only the duck class has the fly method. Uh and because both of these uh uses were on the same variable, it's going to say take the intersection of them and it's going to say the only possible class that should be allowed to to be dialized here is the class duck. Quack also looks at um property accesses

**[21:15](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1275s)** on the object. Same concept as before. Here it's going to look that the feather color properties accessed on the dialized object. It's going to say that only the duck method has that. So this object should be of type duck as well. Quag also uh looks at whether the object is being passed as an argument to uh function calls and deduces the type of the object from that as well. So here um the object is being passed to that function which has a type annotation. So Quag can just say this again is of type D. If there was no type annotation, Quack would just continue looking at the code inside the body of the function and collect more evidence uh for the use of the object inside that as well. And our final example uh quack takes into consideration known PHP operators that

**[22:04](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1324s)** indicate type information. For example, here it's going to see that the object is passed to the instance of operator and it's going to say its type should be duck because that's a type on the other side of the operator. And these are just few examples of our rules. We have a bunch more rules in the paper which you can go check out if you're interested. Now uh beyond just looking at the type of the outermost object, quag also takes into consideration uh any possible nested objects that could be set as properties of other objects. So in this case here uh quag would look at the first two uses of the digitiz object. So it's going to there's going to be a method colon and a property access. So it's going to say this object is of type human because that's the only class that defines this method in property. But then it's also going to see that uh there's a method

**[22:51](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1371s)** call on one of the objects properties. Uh so it's going to collect more type information about the type of that property. So it's going to say that should be of type dog. So the final set of possible classes here contains both human and dog because both of these needs need to be part of allowed classes when we call and serialize. To put it all together, quack has collected the set of available classes and the set of possible classes based on typing typing inference rules. It's just going to take their intersection and say that this is the final set of allowed classes. So we run quack on the M application we looked at earlier. Uh as you can see the serialized object is being used twice. There's two property accesses on it. So when we run quack uh this is part of its

**[23:40](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1420s)** actual output. On the left you can see the type evidence it has collected based on the type inference rules. Uh there's one for each property access. As you can see there's only one uh class that defines both of these properties. And that class is also part of the available classes set. So Quag will just set the allowed class argument to only that one single class that contains both properties. Okay. And so to see Quack defending this Moodle CV in action, um let's return to it. Um so here's the exploited instance. We'll reset it. And now we'll come and we'll take a look at the this output that after analyzing this application, Quacka's Quack has produced an output for us which uh Neo's

**[24:28](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1468s)** uh Neo has showed us. And it looks like this. It's a JSON file that tells us that within this application there is a file that has a call to un serialize on line 93 whose type should be restricted to the QT type DD-WS choice. And so we can take this type now come into the running instance of this application um and we will go and modify that call to unserialize and update its allowed classes. And so let's do that. Hopefully I remember where we're going. Okay. Um on line 93, we'll see this vulnerable call to un serialize that Quack has identified and we will add ourselves this um allowed allowed classes optional parameter.

**[25:18](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1518s)** Now um you'll notice I'm manually making this change that Quack is recommended. Um, we could also add a post-processing pass uh after the fact quack to, you know, analyze a and make these adjustments uh itself as well. Let me just make sure the parentheses are matching and I think we're good. We'll save this. Need to add class. Oh, thank you. Yes. Good. We'll get the class of that type. Um, save it. And now when we come back into our application, we will load the exact same exploit file. Sorry. Okay. And before when this um when the

**[26:09](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1569s)** application was vulnerable, when this exploit file executes, it executes system and we saw the ASI art or the black hat logo. What we expect to see this time is for Quack to have prevented the execution of that system command and the rest of the XML file will parse and the functionality here is that it imports a quiz for uh into this learning platform. And so we import and we do not see that as art uh print out the um the exploit chain did not execute and now we can learn from this quiz. Cool. >> Cool. So let's close out with some takeaways. First of all, um how effective is Quag? We evaluated Quag on 11 more applications other than the one we just looked at that had actual CVS. And among

**[26:58](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1618s)** them, they had 15 total vulnerable calls to unserialize. And we also run an automated exploit generation tool that was able to generate exploits for five of the CVs. And then when running quack and applying the allowed classes set that it deduced, we were able to block all methods for 12 out of these 15 vulnerable calls. What this translates to effectively is that there there were no classes that should be allowed for the for these 12 calls. So the developer didn't actually even need to use un serialize. And overall across all 15 calls uh were able to block 97% of the methods that the attacker had available to them. uh and the automated exploit generation tool was no longer able to generate any exploits for the 5CBs it used to be able to

**[27:50](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1670s)** quack still be improved. Um well Quack's improvements I think fall into two broad categories. One is improvements to the correctness of its outputs. Um, as with any program analysis tool and especially those created as research prototypes to demonstrate an idea, um, there are some warts to smooth over. Um, PHP is a large language with many features and we don't necessarily know like which features we we still fail to um, to accommodate. And so what we really hope from this talk is to build perhaps an interested community of folks who um, have problems and real PHP code that could be protected by this. And we'd love to get your feedback on um you know if you try this on code that you care about does it work does it not please raise some issues on our GitHub repository so we can identify those gaps in in what we're in the

**[28:38](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1718s)** implementation right now. Um and then uh this other category is usability. Um, we would love to get get to a point one day in like an ideal deployment where Quack could be perhaps directly integrated into an ID such that as a developer is typing on serialize and using the object, Quack can make on the-fly recommendations uh right at that point so that the developer um locks down their application before it's deployed and and has uh the opportunity to be exploited. And so where does this leave us with deserialization? Does Quack solve the the problem entirely? Um what we've shown today is Quack can effectively restrict the number of classes available to an attacker during um in an exploit chain. Um what quack is not designed to do and cannot do is prevent what we'll call data only attacks which would be

**[29:26](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1766s)** one where the attacker can um perfectly um uh respect the classes of the intended programmer but manipulate the properties in a way that um violates security issues or has security issues. Um simple example might be a user class that has a is admin property and if an attacker creates uh an object of this and sets that is admin to true that's clearly a security problem that Quack can't necessarily uh mitigate here. Um so Quack kind of joins a um a set of mitigations for these types of attacks that uh has a use case but leaves us with us still needing to be deliberate about um what our use cases are when we write our applications. um is our use case one that an HMAC could apply to um is it one where we can restrict the the

**[30:15](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1815s)** set of classes within um available to an attacker and so again just trying to add a defense and depth um tool here to to help u manage these problems. Um I think another fair question is um so we've shown an implementation for PHP can this work for other languages um and while we didn't study the the d serialization APIs of these other languages as closely as we did with PHP um our understanding is Java and C# have similar um dialerization APIs that could allow Quack to like the idea behind Quack to um extend to. And in fact at the exact same venue where we first published Quack, a very similar paper was published kind of demonstrating this idea for Java applications. Um so I think there is some um uh evidence there

**[31:04](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1864s)** that that we can extend there. But Python is Python's pickle module in particular is notably much more expressive than what pickle than what PHP provides. And so a quack technique would not apply um to to a decalization interface like that. So that concludes our presentation introducing quack. Um what we've spoken about today are how these dialization exploits work and in particular pointing out that they they tend to use more classes than the programmer intended. Um, we've introduced Quack and shown how it works by trying to infer that programmer's intent to um, identify just the classes that are needed um, through the static program analysis techniques that we call um, static duct typing. And again, if nothing else, we hope to leave

**[31:54](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1914s)** you, especially developers, with the reminder that you should always be deliberate about your calls to unserialize. Um, and if you are using them, please think about using allowed classes and Quack can help um, identify the classes to to set in that policy. Um, if you're interested, please check out our GitHub um, and further our paper. Um, and we'll also be available for uh, questions in the rap room South Pacific D. Um, yeah, thank you for your time and your attention and that's Quack. Guess I should also invite you to ask questions now I believe if if you have any. >> Hello. Um I had a question. Um it looks

**[32:47](https://www.youtube.com/watch?v=hrtVuYfswCg&t=1967s)** like you built it on like SML and O camel's kind of type inference but you took it way further with more modern static analysis. I'm not familiar with the latest and greatest, but is there some way you could do like a type checker that isn't actually enforced where you just I don't know something to make it restrict more classes of errors. So the question as I understand it is um like we we have type checking from Okamel and and you know like we we have literature on how to do type checking well in these other languages. We've updated and implemented our own kind of type tracking rules for this problem. Could we have done more to I missed that last piece like >> well to to have the program automatically figure out that there's

**[33:34](https://www.youtube.com/watch?v=hrtVuYfswCg&t=2014s)** certain types of transformations deserializations that are just invalid or that maybe you have to explicitly say yes I want to do this in the allowed classes or something like that. Um, and again like correct me if I'm understanding this wrong, but like basically could we add annotations in the program to like to to provide some hints that like yes, this is what I intend versus >> Yeah. versus not >> to double check just to get rid of some extra errors that might pop up. >> Yeah. Um, definitely. And and Quack does. So, so one of the challenges with like static analysis of PHP as a very dynamic language is like you you can't soundly type check it the way that you can with Okamel, right? Like it is a is an undecidable problem to do this

**[34:21](https://www.youtube.com/watch?v=hrtVuYfswCg&t=2061s)** perfectly, right? But PHP like Python has things like type annotations where you can as a developer um basically say like even though this is a dynamic language um this should be of this type, right? and just kind of leave these sprinkled throughout the code and p uh Quack does use these type annotations as hints within um with its own analysis to to infer that as well. So yeah, Quack is using that and uh it really does help when developers do do that and thankfully I think um we are seeing more of that happening in like modern code bases as especially in the Python world as people are adding type annotations to things. Um yeah, it definitely does help and um yeah, Quack Quack does use that. Well, thanks. A great project. Thank you. >> Great. Thank you. Thank you everyone.

---
id: WVjnipkKp4U
title: "Black Hat USA 2025 | LLM-Driven Reasoning for Automated Vulnerability Discovery Behind Hall-of-Fame"
slug: black-hat-usa-2025-llm-driven-reasoning-for-automated
conference: black-hat
conference_name: "Black Hat"
category: "Security conferences"
edition: "Black Hat"
year: 2026
speakers: []
channel: "Black Hat"
duration_min: 24
published_at: 2026-04-08T14:15:04Z
video_id: WVjnipkKp4U
url: https://www.youtube.com/watch?v=WVjnipkKp4U
youtube_url: https://www.youtube.com/watch?v=WVjnipkKp4U
tags: []
topics: ["Security, safety & red teaming"]
transcript: true
---

# Black Hat USA 2025 | LLM-Driven Reasoning for Automated Vulnerability Discovery Behind Hall-of-Fame

**Speaker not identified**

`Black Hat` · `Black Hat` · `2026` · `24 min`

[Watch the recording](https://www.youtube.com/watch?v=WVjnipkKp4U) · [Conference site](https://www.blackhat.com/)

## Description

Vulnerability discovery traditionally relies on two primary approaches: manual auditing and fuzzing. Each method possesses distinct strengths and inherent limitations. Manual auditing is good at identifying complex logic flaws due to its reliance on deep contextual understanding and expert insight, ensuring comprehensive analysis; however, this method is labor-intensive, time-consuming, and heavily dependent on specialized knowledge. Conversely, fuzzing offers automation, scalability, and efficiency, yet it may overlook vulnerabilities that require intricate semantic comprehension or encounter limitations in scenarios where fuzzing is infeasible.

Recent advancements in artificial intelligence have created opportunities to bridge the gap between the precision of manual auditing and the scalability of fuzzing, paving the way for more sophisticated vulnerability discovery tools. In this presentation, we will introduce our LLM-powered automated binary vulnerability discovery tool, which integrates LLM reasoning capabilities with established static analysis and dynamic debugging methods. Despite its experimental approach, our tool demonstrates exceptional efficiency and effectiveness in identifying vulnerabilities.

We will illustrate the effectiveness of this approach through our application to Samsung's remote attack surface, successfully uncovering multiple sophisticated memory corruption vulnerabilities. This significant achievement secured us the Rank 1 position in the 2024 Hall of Fame for vulnerability research.

By:
Qinrun Dai  |  Independent Researcher,
Yifei Xie  |  Independent Security Researcher/Student

Presentation Materials Available at:

## Transcript

*3,222 words · source: supa (en, exact timings)*

**[0:02](https://www.youtube.com/watch?v=WVjnipkKp4U&t=2s)** Hi everyone, great to be here. Uh today we will talk about whisper a language model based tool that automatically finds vulnerabilities and some phones. I will cover the motivation the design and some real bugs found. I will also leave the sufficient time for Q&A. So this is a small team. I'm turn my research company focuses on the finding vulnerability and writing explainer on this project. She's currently an undergraduate student and interested in finding vulnerabilities using AI and she developed this tool under my guidance. So large function to large models are becoming essential tool for anyone who writes codes these days. I use chgp and cursor almost every day and about half

**[0:51](https://www.youtube.com/watch?v=WVjnipkKp4U&t=51s)** of the lines come the AI directly and due to l's excellent performance security teams are integrating large junk models into their workflows static analysis penetration testing bug patches and so on and some pioneers are pushing even harder Google product zero and see lens research have shown that the lumin model can find bugs in real world projects like the link kernel and our own research kicked off around the same time as Google and the result yeah it helped us achieve the hall of fame in the Samsung mobile security 2024 and we believe all of these facts show that large model for security could be the future trend to the community and here's the plan for our next first I

**[1:42](https://www.youtube.com/watch?v=WVjnipkKp4U&t=102s)** walk through a bug we found by hand and we show the detailed steps of the auditing journey. Next, I will show the large language model strength in code review. Then I will explain how we combine human expertise with large models into one pipeline to discover bugs in binary files. And finally, I will share some bugs that our tools found and show the models reasoning steps so you can see how it reached high confidence results. First, let's develop into the CV. So last year we successfully exploited this bug and achieved a RC on Samsung phones and this function pares RTCP a package from the remote. The bug is a single M copy that trusts the lens parameter and this field is fully

**[2:31](https://www.youtube.com/watch?v=WVjnipkKp4U&t=151s)** controlled by the attacker and its type is a word. So it could be up to hex FFM. So both the source and destination buffers are much smaller. The bug looks simple but reaching this point took hard work. We had to trace the costex and analyze the data structures across uh different modules to verify this OB size. And in theory checking a buffer overflow is simple. Find where the buffer is allocated record size and review every memory excise to it. But in practice, it's a heavy work. Even with full source code, you may spend days figuring out the classes and pointers relationship. So given the script binaries, it's get

**[3:20](https://www.youtube.com/watch?v=WVjnipkKp4U&t=200s)** harder. So comments, type names and symbols are gone. You have to do reverse engineering, track offsets by hands, resolving in calls and using the debugger to write data structures in the robots. And this risk reconstruction jobs often take longer than proving a bug. And this page shows the entire call stack and the data structure of this bug. So first the C transport manager start receive starts the packet receive logic. Then the RTP create function it allocates the primary RTP context and the sock notify function invokes the callback functions when an RTCP package arrives. Then the two function are the RTCB core handle routines.

**[4:11](https://www.youtube.com/watch?v=WVjnipkKp4U&t=251s)** Then the following functions start to parse RTCP packets. I highlighted some important pointers accordingly to show their relationships among the complex data structures. The same color indicates that the pointers are pointing to the same buffer. All above has show what we did to find the bug. Well, auditing the code, none of these steps are really hard, but they are time consuming, and that's what inspired us to have a large model handle this part instead of us. So, until last year, uh, vulnerability hunting was almost a human only work. Experts disclosed the attack surface, reverse binaries, and try to find bugs with hardcore techniques. and early chat bots hallucinated too often to trust for

**[5:00](https://www.youtube.com/watch?v=WVjnipkKp4U&t=300s)** critical analysis or reasoning. However, since I've changed, new reasoning models from OpenAI and other renders can now handle longer context and show precise reasoning steps and like a human expert, a large model can be copied and run in parallel. So with a clear pipeline and prompts, they can read code at a fast speed and never need a break. Let's see where the large language models help most in code reasoning. This page shows how an large model with code. So we use an addup plugin Jetto. I select the vulnerable function from the previous bug and use the built-in prompts explain this function and remove the variables.

**[5:48](https://www.youtube.com/watch?v=WVjnipkKp4U&t=348s)** Now compare the before and after. The model assigns clear names in first each variable's row and as a short summary. Then the code becomes easy to review. Then I pasted the vulnerable function to the chatbot and asked is it vulnerable and why? So it returned three points. One matched the real bug and the other two were false alarms because of the simple prompt. However, even the correct answer added a may be condition. If the A data lens is large enough, me copy may overflow and that's not helpful. As we all know, big sizes can make overflow. So, here's the problem. When you are auditing hundreds of functions, you need

**[6:35](https://www.youtube.com/watch?v=WVjnipkKp4U&t=395s)** from yes or no answers, not may be that still need further triage. So, the solution is straightforward. Give large chunk models enough and clear preconditions. Then the large chunk models act like a set solver take conditions and concrete data then output precise answers. For example, if we state the written value is 1,024 bytes, the RTCP packet buffer is 1600 bytes and a data length is an attacker controlled 16bit field, then the model will flect the overflow with high confidence. And someone may say it looks too deliberate or artificial, but this introduces the

**[7:23](https://www.youtube.com/watch?v=WVjnipkKp4U&t=443s)** necessary step data structures reconstruction. So using the journey of how we found the CV 224 34587 as a case study, I will show how data structure reconstruction makes our large models analysis precess on stripped on 64 binaries. And this is our tools architecture and it has a core controller environment interfaces on the right that interact with the binaries and mobile device and human and agents on the left handling different tasks. So it might look familiar and I had the same reaction when I saw product zero's

**[8:11](https://www.youtube.com/watch?v=WVjnipkKp4U&t=491s)** project net pan and they have also have the large model back end agent controller and tool site but the key difference is project zero works at the source code level and we are working on the six stripped ARM 64 binaries that forces us to do additional reverse engineering works and have a different solution. So we fit a bunch of DOS files into a decompiler equipped with our own custom plugins. Then we attach the Freda to a real device to resolve in their cause and verify memory layouts. Once we have all necessary data, our large model agents take over, define the package receive and parse routines, trams the global call graph, reconstructs the data structures and performs the

**[9:00](https://www.youtube.com/watch?v=WVjnipkKp4U&t=540s)** vulnerability analysis. And here's our road map. Human does only two things. Pick the target process at the very beginning and verify the results at the end. Everything between is taken over by the agents. So the first identify the target process. Last year I shared how we exploited bugs in the s video engine service. So the service has the following features we are interested in. It runs on a hyperage. It can be excited from remote directly and it is installed and runs by default on the Samsung phones and it's the triggering condition is simple. Just need a package center. So these features contributes to the higher value of bugs from the in the video engine service

**[9:49](https://www.youtube.com/watch?v=WVjnipkKp4U&t=589s)** and we didn't build a fuzzing harness for this service because the codebase is small. So the cost to design and maintain a harness won't pay off. After we evaluated the real workload, a large model based auding tool was a better try. So the tool decompiles the targets and builds a global core graph that every later step depends on. The decompiler produces readable pal codes so that the large language models can index and review. And to resolve the problem that static analysis would break on indirect calls, we take a practical method run app attach frea hook every call and read the

**[10:38](https://www.youtube.com/watch?v=WVjnipkKp4U&t=638s)** real jump from the register. Next, locating the packet receive and parse routines in our attack scenario is important because uh the receive routines represent the attack surface and disclose how to interact with this service and the parse routines are where the bugs often appear. These functions are easy to spot. For example, like um have function name like RCP receive packets or have a big switch statement or a nested if or else statements in the code on magic numbers for classifying protocol type or version. So we don't need a detailed prompt here. And the prompts in the blue background are what we are using in practice. We pass the global call graph and ask show where messages from the

**[11:27](https://www.youtube.com/watch?v=WVjnipkKp4U&t=687s)** remote are received and where they are processed. So as the agent walks through the global call graph, it indexes and reviews each functional pedal code, marks potential receive and parse functions and returns the result in JSON format. The previous step also aims to reduce the search space. It cause related branches and keeps the pipeline focused on the path. And here's the core step. Data structure reconstruction. The goal is to know every buffer sets before any memory sets. If we get this wrong, we will miss all the bugs or get tons of false positives. First, we need to find every election set like melo or a class constructor.

**[12:18](https://www.youtube.com/watch?v=WVjnipkKp4U&t=738s)** Our add plug-in takes each set and stores the decompile function to the database. It has also parses the a at allocation site to build an initial data structure layout. based on how the code initializes the fields like the code name on the right. Then the data structure analyzer agent scans the codebase with its functions that satisfy the policy like match the field type name of site or by a relevant function or class name. So aside from the policy and personal routines the rest is done by the agent. It adds missing fields in first types, renames members and takes controlled fields. So by the time the vulnerability analysis agent starts, the reconstructed

**[13:07](https://www.youtube.com/watch?v=WVjnipkKp4U&t=787s)** structures are expect to be as complete as if we had the definitions in the source code. And next I will show how this works on a concrete example. Take the C transport manager class from CV 2024 345 87 as example in the constructor. This pointer represents the object. So arrow red to an offset becomes a candidate field. For example, the offsets hex 303 and hex 1072. uh they are later normalized to hex 978 and hex 2180 based on the tab and furthermore the agent renames the hex 978 to PC Samsung buffer because for the

**[13:58](https://www.youtube.com/watch?v=WVjnipkKp4U&t=838s)** cast in the code now look at the C transform manager start receive this is the packet receive function and I picked some of these colleies on the right the first function the PSI meree X it is not a member function and it doesn't touch any fields of this pointer. So the agent just drops it. In the next function start RTCP send it is a member function. This matches the class name rule and inside it we see new accesses. So the agent will add two fields to see transport manager class and for the offsite hex 2 310 the agent follows the

**[14:48](https://www.youtube.com/watch?v=WVjnipkKp4U&t=888s)** written value of psi create event x inverse it is name is event handle. Next the function rtp rtb create the agent follows this function because it return value points to crown manager. So guided by our prompt the agent thinks this would help to complete related data structure. Assume the unknown query strct is unknown one and based on the written offsets the agent adds three fields and infers the name of the first field and this explains how the data structures relation map grows step by step. And then in the last case the function

**[15:36](https://www.youtube.com/watch?v=WVjnipkKp4U&t=936s)** create a buffer it takes uh this plus 303 as the argument which satisfy offset match policy and in satisfaction create buffer the argument is treated as the class this pointer. So if the offsets five and 14 are not in the layout yet the agent will add them and infer their names. So to summarize the data structure analyzer agent first completes the missing fields of C transport manager that are size outside it constructor. It then infers and fields in related data structures that may contain pointers to C transport manager and finally it completes a subass that is referenced by a field within C trans C transport

**[16:24](https://www.youtube.com/watch?v=WVjnipkKp4U&t=984s)** manager. So once all of these necessary data structures are ready, the vulnerability analysis agent begins at the packet receive functions then follows the data into each parse function like the tent analysis from the same from the source to the sync. The agent enters every function with the value ranges, buffer sizes and any necessary constraints. So look what the agent exactly needs a prompt template, the code context and a bug model. The code context includes the argument list types reconstruct data structures and which fields are attacker controlled and this info act as the preconditions like what I mentioned before. So with this

**[17:14](https://www.youtube.com/watch?v=WVjnipkKp4U&t=1034s)** the L model can tell if the vulnerable if the code is vulnerable or not and explain why. Like human experts after knowing what specific variables and their potential values then give a code snippet. It is easy to spot the vulnerabilities and during the development we made a problem. We store everything in the JSON format, but the logical language models didn't always return the valid JSON strings. They would drop the codes or failed to escape characters on the first try. So our fix is fit both error report and the bad JSON strings back to the model and on a second run the large language model usually fixes the problem itself.

**[18:02](https://www.youtube.com/watch?v=WVjnipkKp4U&t=1082s)** Resident models do this job well, but they are too slow and costly. And lighter models are less accurate, but good enough for the format repair task. The accuracy is 90% or 95% doesn't matter. So we built a model router send heavy tasks to open AIO3 or deep R1 and send the light tasks like the fixed JSON strings to offer mini or V3. So when the run is finished, the pipeline finally hands over what we have been waiting for. The report shows how many vulnerabilities it found, a confidence score and for each case and a clear description for of this bug and we also include logs of reasoning history

**[18:53](https://www.youtube.com/watch?v=WVjnipkKp4U&t=1133s)** so we can trade the bugs quickly. So now let's apply the pipeline to the real world target starting with a simple buck pattern and here's the buck pattern is very simple and index goes beyond the buffer and in the demo it is the we go grows beyond the ses of buffer A and buffer B. So R and rise go out of bounds. To make the agent focus on this case, we give it a short prompt to describe this exact pattern. And here's the first bug font, the SVE 2024 1490. The green blocks are the buffer that gets corrupted and the red blocks

**[19:41](https://www.youtube.com/watch?v=WVjnipkKp4U&t=1181s)** indicates the bad index and code on the right is the raw data structure reconstructed by the agent. Matching colors means those fields are the same and the text lower right is what beam whisper reports and as the human expert after analyzing this bug I summarized the root cause. So each time entering this function the counter at V13 of at zero increases by one but the right buffers are allocated at a small sizes and is fixes. So when the counter grows pass to those sizes autobound rights are generated and this is what the agent agent exactly said. So this function contains an autobound

**[20:29](https://www.youtube.com/watch?v=WVjnipkKp4U&t=1229s)** right and the processing loop increments the packet count without the bounces check and it's a tricky condition is by crafting a packet with multiple NAL units and buck two is the same pattern and uncheck the counter keeps increasing when re-entering this function and eventually overflow this buffer. and B three to five shares the similar cause. So I will skip the repeats and go to the benchmarks. And I have to say the s patterns might be the simplest you have ever seen a blackhead and I guess why the large models can find them and we tested different models from four

**[21:20](https://www.youtube.com/watch?v=WVjnipkKp4U&t=1280s)** vendors. First cost matters to both the independent researchers like us and two big teams only considering the token price the open A4 mini cipstick R1 and 7 plus are the best deals but the bugs detection and confidence are the key metrics. The calculation of competence is also dedicated to the language model itself based on the uncertain variables number and a missing code context. Check mark in the box row means all the reported bugs were found but confidence may differ. In our experiments the 03 from open AI was the most stable one. It found all the bugs with high confidence in our runs and of MIDI and the DPC R1 did well but sometimes missed

**[22:11](https://www.youtube.com/watch?v=WVjnipkKp4U&t=1331s)** a few bugs during the testing and as you can see any cases with a confidence score less than or equal to 80% will be dropped. This is because after our verification reported cases with higher confidence were all confirmed as the true positives. However, things get worse below that threshold. Massive numbers of false positives start to mix in with the results. And finally, 03 and 04 mini finish in a reasonable time window, but dipstick R1 takes much longer up to 21 hours on the same job. And here's our takeaways. So first this is not a rigorous academic study and we

**[23:01](https://www.youtube.com/watch?v=WVjnipkKp4U&t=1381s)** are not here to prove or define assesses. Instead our work provides a practical evidence that large language models play an important role in our tool for discovering the real world vulnerabilities there. We believe this direction will become a trend. And next a recent concern is will large models replace security researchers. My answer is no. And on the contrary, they take the boring parts and we get much more efficient than before. And the last one, yes, the tool is still reporting new bucks and I have to look at some tonight. Okay. Uh, thank you for listening. I hope this was helpful and I will upload it to the website for anyone who is interested to refer to. Thank you.

**[23:50](https://www.youtube.com/watch?v=WVjnipkKp4U&t=1430s)** [applause]

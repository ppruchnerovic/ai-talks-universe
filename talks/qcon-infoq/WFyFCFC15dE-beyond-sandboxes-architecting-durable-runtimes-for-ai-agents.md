---
id: WFyFCFC15dE
title: "Beyond Sandboxes: Architecting Durable Runtimes for AI Agents"
slug: beyond-sandboxes-architecting-durable-runtimes-for-ai-agents
conference: qcon-infoq
conference_name: "QCon / InfoQ Dev Summit"
category: "General software conferences"
edition: "InfoQ"
year: 2026
speakers: []
channel: "InfoQ"
duration_min: 26
published_at: 2026-06-24T09:00:37Z
video_id: WFyFCFC15dE
url: https://www.youtube.com/watch?v=WFyFCFC15dE
youtube_url: https://www.youtube.com/watch?v=WFyFCFC15dE
tags: ["AI Agents", "Agent Orchestration", "Agentic AI", "MLOps", "Software Architecture", "Generative AI", "AI Engineering", "Distributed Systems"]
topics: ["Agents & orchestration"]
transcript: true
---

# Beyond Sandboxes: Architecting Durable Runtimes for AI Agents

**Speaker not identified**

`QCon / InfoQ Dev Summit` · `InfoQ` · `2026` · `26 min`

`#AI Agents` `#Agent Orchestration` `#Agentic AI` `#MLOps` `#Software Architecture` `#Generative AI` `#AI Engineering` `#Distributed Systems`

[Watch the recording](https://www.youtube.com/watch?v=WFyFCFC15dE) · [Conference site](https://qconferences.com/)

## Description

Sandboxes protect. Durable runtimes survive.

This talk argues that production AI agents need more than isolation - they need orchestration-layer infrastructure that persists state, checkpoints every step, and recovers from crashes without losing context.

Learn how treating agents as late-bound sagas unlocks retries, human-in-the-loop pausing, multi-agent coordination, and auditability at scale.

## Transcript

*4,952 words · source: supa (en, exact timings)*

**[0:03](https://www.youtube.com/watch?v=WFyFCFC15dE&t=3s)** [music] >> Hi everyone. My name is Viren. I'm one of the founders at Orquest. Um and what I'm going to spend the next few minutes talking about is, you know, how do you run agents beyond just, you know, putting them in a sandbox. Um and focusing more on the durability aspect of agents and the runtime. But before we get started, right? Let's do this, right? Let's run an agent and see, you know, what does it mean to have agents surviving the crashes and what uh when we talk about durability, what does it mean? Um so, I'm going to switch it for a minute. Um So, I have got like a bunch of sample agents. Um and I have an agent that essentially demonstrates this very well. So, you

**[0:50](https://www.youtube.com/watch?v=WFyFCFC15dE&t=50s)** know, here is an agent. Um You can see, you know, pretty similar uh setup here. I have an agent that gets the weather. And um yeah, um I'm asking agent to get weather for multiple cities. That's basically the setup. I'm going to run this agent. Um and we'll take a look at how the agent is running. So, there you go. As you can see, you know, I have my agent running. Um The LLM started the call, decided to call a whole bunch of tools. They are all running in parallel. And what I'm going to do is I'm going to kill the agent halfway. Um Sending the signal to kill the agent. Uh

**[1:39](https://www.youtube.com/watch?v=WFyFCFC15dE&t=99s)** most likely the agent is probably finished by now. Not yet. Okay. Too late to kill the agent. Let's give it another try. I'm going to start the agent execution and kill the worker right away. The network is a bit slow and it's going to take some time, but we'll keep trying. All right, there you go. So, we successfully managed to kill the agent that that is running. Um and as

**[2:27](https://www.youtube.com/watch?v=WFyFCFC15dE&t=147s)** you can see here and we'll dive deeper into this um agent is kind of stuck here right now right? Um I scheduled like 10 tool calls, out of which only eight of them succeeded. The remaining two are kind of stuck right now because, you know, the agent essentially died. And then when we restart an agent um it continues forward and and completes the crash. Uh so, what just happened here? Right? Uh we started the agent. Uh we killed the agent halfway. Um what happened with that was that, you know, it stuck getting into the running state. Um after some time, we resume the agent and it continues processing. Pretty straightforward, uh but that kind of demonstrates the need for durability because what's going to happen when you put agents into production is there are going to be failures, either

**[3:14](https://www.youtube.com/watch?v=WFyFCFC15dE&t=194s)** temporary or uh long-term failures, and you want to continue where you left off. And not every agent is a coding agent where you can start from the scratch. Sometimes you have to preserve the state and you have to continue where you left off. Um so, that begs another question, right? Like and the main topic of the session, which is the sandbox. So, does it mean that like, you know, it's not the right thing to do when you think about sandbox? Absolutely not, right? Sandbox are right thing to do. Uh but where you put sandbox is really matters, right? Where you where do you kind of wrap the sandbox? So, there are two aspects of running an agent if you think about it, right? There is the whole aspect of LLM making decisions about what to do next. If you think about an agent, agent is typically an LLM running inside a loop as we all think about it.

**[4:02](https://www.youtube.com/watch?v=WFyFCFC15dE&t=242s)** Every time an LLM runs, it is looking at the state of the world. So, it's context. What has happened so far, the output of the tools that were called earlier in the previous iteration. How many tools are there? Guardrails and whatever not. And it decides what to do next. And then >> [clears throat] >> what what it proposes kind of gets done, right? So, when you think about sandboxing and you put tools into sandbox that kind of works very well because you are kind of separating out those two concerns, right? Your tools are running in a sandbox environment because in a way you are running untrusted code. The code written by the developer depending upon your infrastructure, this might be sensitive workload. This might be tools that could have bugs and things like that. So, it kind of separates it

**[4:50](https://www.youtube.com/watch?v=WFyFCFC15dE&t=290s)** out versus you know the actual orchestration that is happening could be happening somewhere outside. Um which is basically to say that like you know you separate out the reasoning from the actions, right? Agent or the LLM is only responsible for proposing what to do next, not actually doing it. So, in case of for example weather agent and if I ask hey, what's the weather in Boston today? What LLM should do is LLM should look at what capabilities do I have to determine the weather for example, right? And let's say if it says that hey, I can call this tool instead of calling the tool, it should propose that I'm going to call the tool and then somebody else calls the tool. That somebody else is your runtime. Um and so why you don't want to put the entire loop inside the

**[5:38](https://www.youtube.com/watch?v=WFyFCFC15dE&t=338s)** the sandbox, right? When you think about sandbox, you know you are putting the entire loop into an executable process, putting that process into a container or a micro VM and running them. Now, what happens when your agent itself is long running? Think about an agent that could be running for hours, days, months, right? Um let's say agent has a human in the loop or it's a long-running deep research agent that is supposed to run for multiple days looking at the current situation and and doing research and and running those things. Now, your VM is running for that longer period of a time um and especially like if you have a human in the loop and and the person who is responsible to take an action is backlogged, is on a vacation, is sick, whatever. Now, you know, you are wasting important CPU cycles.

**[6:25](https://www.youtube.com/watch?v=WFyFCFC15dE&t=385s)** Second thing is when you think about multi-agent orchestration and when you the agents have to communicate to each other, how that communication is going to happen because now that brings another challenge which is that, you know, an agent has to talk to another agent. How do they identify, you know, where that agent is running? What's the IP address? Those things brings in challenges. Not to say that you can't solve it. The question is should you solve that or not? And the third part is sharing the relevant context. And you know, there are ways around this one, right? Like we have seen people using shared file system, for example, to share the context. Or using some sort of RPC to send the information and share the context. But what happens when the remote agent fails? Then who's going to be retry doing a retry? Who's responsible for making sure that that communication happens? And the moment you start putting everything

**[7:12](https://www.youtube.com/watch?v=WFyFCFC15dE&t=432s)** inside a single process, this starts to become challenging, right? And you start to put a lot of glue code to make sure this thing all works. Um and this is why, you know, when you are running agent in the loop in the box, the entire state is living inside the memory because, you know, it it retains the memory. Uh when you have to restart, you need to understand what's the session ID that you want to restart from. It goes from there and and continues forward. Um all of those things brings in comple- complexities. But it's not just about the state, Like if you think about the context, um when the agent restarts, the context also is gone unless you are able to restore those things. The third part is that, like you know, once the agent dies, the only thing that you have captured is the output of the agent, but you know, being able to explain, you know, why certain things happen, that becomes critical and now

**[8:00](https://www.youtube.com/watch?v=WFyFCFC15dE&t=480s)** you have to kind of sift through logs to find out how many times the agent ran, um what were the number of iterations, what were the inputs, outputs, latencies, all of those things. So, think about the case, right? Um if you are running agents in production, um and how many times it has happened that, you know, this agent lost the context because, you know, there was a crash, you had to restart, uh or there was a timeout. And this is where this becomes important to kind of separate out how your agent runs versus how the agent orchestration happens, right? The runtime versus the framework. So, traditionally we model the agent as

**[8:47](https://www.youtube.com/watch?v=WFyFCFC15dE&t=527s)** a LLM in the loop, which is technically not wrong. Um agent is >> [snorts] >> you start an agent with the initial state, um and then you are running a loop where LLM is looking at the state, uh state of the world, maybe calls the tools, um updates the state, and keeps running in the loop until the job is done. Um now one thing that the loop cannot answer is that, you know, when what happens if something goes wrong, right? If if my process crashes somewhere right here, before calling the tool or after calling the tool, how do I know where, you know, it crashed? Now, you might say, you know, hey, how about we do checkpointing? So, the question becomes is, where do I take a checkpoint? So, is it before the loop, at the end of the loop? What happens if somewhere something happens in between the loop? Um and who is doing the checkpointing on top of checkpointing, right? So, that starts to become an issue, uh because the failures are not deterministic,

**[9:36](https://www.youtube.com/watch?v=WFyFCFC15dE&t=576s)** right? Failures can happen anytime. Um the other part that loop just cannot answer very easily is, you know, why did the agent decide to pick that particular tool? Um you have to capture everything that went into the thinking of an LLM in terms of, you know, what were the inputs given. Um if it's a thinking model, you know, what were the thinking output that were produced by the LLM, what were the tools that it proposed to call, and more importantly, you know, if you have a guardrail, um you don't want LLMs to be deciding that guardrail should be called. Um LLM should only talk about which tools to call, and a separate framework identifies which guardrails are attached to the call and calls it. And the reason why that is important is that if LLM hallucinates and does not call the guardrail, then you have a problem where my agent, you know, ends up deleting a database, right? Um so that problem gets

**[10:25](https://www.youtube.com/watch?v=WFyFCFC15dE&t=625s)** solved by, you know, making guardrails being more deterministic rather than, you know, driven by LLM. So that brings to an another important point, right? What I would like to call out is that if you think about agents, agents are workflows. Um but workflows are deterministic, right? You would like to say. Um and which is true, right? When you define a workflow, you are defining the workflow execution graph up front. That, you know, this is the sequence in which you are going to call certain tools or microservices, and the graph is defined up front. The agents essentially are workflows. The only difference here is that, you know, the graph is built dynamically as it goes. Um I would like to call them as like late bound sagas. Um just like workflows are sagas, agents are sagas, except that they are created as they go.

**[11:13](https://www.youtube.com/watch?v=WFyFCFC15dE&t=673s)** So here is an important thing, right? How that plays out in real world. Um when you think about an LLM, as I mentioned, right? Instead of LLM, if you think about LLM as a planner for a workflow uh that is being built as it goes, uh then this starts to make sense. Uh Uh, you have an LLM that looks at the state of the world. So, you know, if we go back couple of slides here, uh, LLM has a state. Um, it knows what the current context is, what was the initial context, how many tools were called, what were the inputs and outputs given to it. And then it uses that information to, uh, you know, propose a plan in terms of what should happen next. And based on that, you do the actions. Uh, that is exactly, you know, what late bound sagas are. You know, late bound sagas are constructed as they run. Um, they are durable by construction

**[12:01](https://www.youtube.com/watch?v=WFyFCFC15dE&t=721s)** because they are running on a workflow engine. So, you know, every time a tool is about to get called, when it gets called, and after it gets called, all of those things are recorded into a database, which means that, you know, when you want to restart, it can kick off exactly where it left off and continue forward. And other advantage of something like this is that they are replayable, which means that you can go back, you can look at it what happened, and then we can continue where it left off. And this is why, you know, durability alone is not enough. Like, you know, if you put checkpointing, it gives you durability. What durability does not give you is, um, the being able to explain, being able to do a crash recovery, which is first class, um, and the separation of concern between the planner and the executor. But, with that aspect, right, there are four important things, right? You have LLM calls that are recorded.

**[12:50](https://www.youtube.com/watch?v=WFyFCFC15dE&t=770s)** You have tool calls that are recorded. Uh, we have state transition, right, in terms of what happens next, why certain things happen, and you can start to build a graph around it, right? Um, and the final piece of things basically is the human inputs. Uh, once you have human in the loop, it starts to capture those things, what, um, when did the human in the loop was triggered, what was the kind of decision made by the humans, and that all starts to kind of feed back into your LLM's decisions. So, this is where I want to kind of introduce you to Conductor, which is, you know, an open source workflow engine that we built it at Netflix, and now, you know, as Orkes, we maintain the open source project. We provide um, the enterprise version of it. But this is exactly what conductor does, right? Conductor is a durable substrate for your agents. And the way essentially it works is it provides that

**[13:38](https://www.youtube.com/watch?v=WFyFCFC15dE&t=818s)** durability. It can execute a workflow either statically defined or dynamically defined workflow. It can do parallelism. It can do human in the loop. Or it can take other workflows as sub workflows and run them. And this is this is a kind of the important execution part of the overall agents runtime. But then you might ask like you know, conductor operates on deterministic workflows. What about the agents which are non-deterministic? So this is where you know, we built another layer on top of conductor. We call it agent span. And essentially that's an agent runtime that is built on top of conductor. And the way it works is it you declare an agent. So you know, you can write an agent using any of the popular SDKs out there. Last time I counted, there are like 11 of them.

**[14:25](https://www.youtube.com/watch?v=WFyFCFC15dE&t=865s)** It gets compiled into a a semi-deterministic workflow, right? Which is basically an LLM in the loop. And I'll show you in a bit you know, how that looks like. And once the workflow is done, when you run an agent, you are actually running that workflow. So now you get the benefit of running a durable workflow through an agent. You can actually inspect and see what's going on. And if you wanted to, you can actually modify that workflow as well. This is a very high-level architecture of agent span. As I mentioned right, like underneath all of those things, the durability comes from the underlying database. The open source supports pretty much all the popular databases. I've seen the most popular one being Postgres and Redis. And it has supporting SDKs that are out there.

**[15:13](https://www.youtube.com/watch?v=WFyFCFC15dE&t=913s)** And agent span essentially brings in all the agentic concerns to it, right? Things like tool calling, MCP. We have A2A that is work in progress and and things like that. And by the way, the all of these things is completely open source. Now, another advantage with conductor is that conductor kind of retains all the executions. So, if I switch back to the conductor runtime, as you can see, you know, if I go back to my conductor um instance, I can see every agent that ran, right? This is my local. I have like 102 instances of any agent. I can pick up one from the past. I can search for it. Um, I can take a look at it and see what was given as an input, output, how many tokens it were used. So, instead of trying to kind of go through logs and different systems, conductor basically maintains all this stuff. And by the way, this conductor

**[16:00](https://www.youtube.com/watch?v=WFyFCFC15dE&t=960s)** has been doing it for microservices for now years, almost like a decade now. Um, and the way we think about agents are agents are microservices, tools are microservices, and what conductor was doing to microservices is now does it uh for agents. Um, and this is the workflow that it creates behind the scene, right? So, this is a very simplistic view done by turn what really happened every turn. Um, if you want to really see the workflow itself, like as you see it, right? You start by setting up the context. Um, you do some processing. Here's the LLM. As you can see, right? Um, it uses Anthropic. Um, I think it's using uh Sonnet here, yeah. Uh, this were the messages that were passed. The output of the agent was like, if I look at the first iteration,

**[16:49](https://www.youtube.com/watch?v=WFyFCFC15dE&t=1009s)** uh, as you can see, right? Instead of calling a tool, the agent essentially proposes that I'm going to call this tool and here are the inputs given to it. So, you know, agent is not really calling the tool. The tool call is done by the system. Um, so here, as you can see, right? It schedules a tool. Now, this tool can itself be running on the same environment, on a different environment, in a different cloud. If you are building brand new agents, it could be running on your laptop. It could be running in your sandbox, right? And this is what we want to put it in a sandbox. So, now your tools itself can be fully distributed, can be running across multiple machines, um and and you get full visibility into what's happening, right? You know which host it ran on, um how long did it take to run, and whole bunch of things around it. And also, if this tool fails, I can

**[17:37](https://www.youtube.com/watch?v=WFyFCFC15dE&t=1057s)** rerun from where it left off. Um or it can do automated retries and all of those things. So, that comes completely out of the box. The other part is that, you know, if I need human in the loop, um I can also put a human in the loop. So, I can build an agent, and most of the agent allows you to put um guardrails, um and you can specify guardrails as human guardrails. When that happens, the workflow will basically, you know, yield. Uh One important thing with something like Conductor is that, you know, there is no process running for waiting, right? So, the the the workflow itself pauses, your workers can basically, you know, get undeployed if you're running in a micro VM, for example. Uh it can free up the CPU and memory for other agents to run. And it can continue running for, as I mentioned, right? Days, months, um even longer than that if you wanted to.

**[18:26](https://www.youtube.com/watch?v=WFyFCFC15dE&t=1106s)** Um And then, once the human input comes, it can continue where it left off, and then that process might be agentic. It might be a very deterministic workflow after that, but you know, that can just comes out of the box. And this is the important part, right? Like, as I mentioned, like, not every agent completes in few seconds. Some agents are going to take longer period of a time. You know, you could have human in the loop. You could have events that are coming in that might come later on. Let's say, for example, you have an agent that is onboarding a customer. And customer is supposed to provide the documents. That might take a day to a week or more to provide. So, until then, the agent is basically waiting for it, right? Uh in case of something like Conductor, really no process is running there. It's just waiting, and it's fully event driven, right? So, when the next event comes, it wakes up and it continues where it is supposed to continue from

**[19:16](https://www.youtube.com/watch?v=WFyFCFC15dE&t=1156s)** and that next event can come 6 months later also. Uh but here is the important piece, right? Which is that you know, agent failure can come in two ways, right? One is, you know, you have system failures. You know, my tool call failed and I want to do a retry. But let's say the agent can also complete successfully and then some time later, like could be next day, could be few seconds later, could also be 6 months later, right? Compliance wants to know, you know, my insurance processing agent made some decisions and I want to find out why, you know, I want full auditing report. Where are you going to get the data from, right? So, this is where something like conductor works very well is every execution is captured. You can go back, you can look at it and and you can reason about it, right? And and that's there. You may not be able to change it, but at least you know exactly what went into making the decisions.

**[20:05](https://www.youtube.com/watch?v=WFyFCFC15dE&t=1205s)** Um I'm going to skip this for a minute. Um Now, with that, it gives you that debuggability. It gives you audit trail in terms of what really happened. Um the other part is that, you know, you can take that run uh agent execution run, you can convert that into a workflow and try a different um conditions, right? You can you can take a look at it and say, "What if my LLM provided a different output?" Like you can essentially run kind of evals on it to see, you know, if if LLM produces a different output, how does the whole thing work? So, you know, it makes your testing also a lot more deterministic, more confident. The other part is, as I mentioned, right? Like because um the agent itself is not calling other agent, that part is left. So, this is an important distinction, right? In a multi-agent scenario, when you want to do a hand off to another agent or you want to call

**[20:52](https://www.youtube.com/watch?v=WFyFCFC15dE&t=1252s)** another agent as a sub-agent, instead of the agent calling it, agent is only agent is really not doing anything, right? Every agent is essentially a stateless entity. The agent is only doing what it is supposed to do and that the work is left off to somebody else to do it and that handoff is taken care by the orchestrator layer. The orchestrator calls this another agent depending upon the strategy it decides which one to call. So, you know, um there are about like 10 plus different strategies by which you can, you know, coordinate the work, right? You have handoffs, you have router agents, you have human routers. Um you have decision-making routers based on the rules and things like that. But, you know, all of those can be taken care by the orchestrator and and conductor supports pretty much all of those possible scenarios. Um

**[21:39](https://www.youtube.com/watch?v=WFyFCFC15dE&t=1299s)** Which means that, you know, the coordination between agents also do you don't have to worry about how am I going to call the other agent? All you do is I'm done with my job. Somebody else based on the my definition is going to call that agent. And pass the relevant context. Now, is that all? Does it like a magical thing? Probably not, right? Uh you still have to take care of few things, right? Which is, you know, tools which are not item potent like, you know, if you have a tool that does, for example, money transfer. You want to make sure that like, you know, if the retry happens of a tool, uh it's does not have a side effect. And one thing that conductor does very well is conductor behind the scene uses the queuing systems um with at least once delivery guarantees. Um and when there is a redelivery of a message, you have enough metadata, right? A retry count. So, you know, if your retry count is, for example,

**[22:26](https://www.youtube.com/watch?v=WFyFCFC15dE&t=1346s)** greater than one, you know, it's a redelivery and you should probably put some extra checks. Second part is, you know, how do you ensure that your prompts are going to work? I have seen examples where my agent runs completely fine on Anthropic. The moment I switch to Gemini, it just keeps running in the loop. Uh it does not stop. Uh and I have to fine-tune the prompt and, you know, put some extra messaging to it, right? So, testing agents across different um models is kind of important. Um And then third PC that like, you know, your agent itself could be pretty large. Let's say imagine an agent that could be doing, you know, 10,000 loops. Um it's going to start growing the ledger, right? So, you know, you have to kind of think about how do you distribute it, right? One way is if you break down the agent into sub agents, then every sub agent has its only relevant context

**[23:14](https://www.youtube.com/watch?v=WFyFCFC15dE&t=1394s)** because, you know, the context route is for real. And then larger the ledger, larger the context, and you know, those things are going to kind of play a role, right? Even if you summarize the context or truncate the context, you know, they're are not always the perfect situation. But that really depends upon the kind of the agent that you are building and how we are going to handle those things. One other thing that we made an early decision was that with conductor, um, it's just a runtime, right? Which means that you can still use LangGraph agents that you have built, your OpenAI agents or Google ADK or Vercel AI SDK, and then they all run in conductor. No code changes are required. It takes care of all of these things. Think of that as a transpiler that takes your agent definition and converts it into a workflow. We're getting close to the end of it, but yeah, three important things. Right, so one is you want to sandbox the

**[24:02](https://www.youtube.com/watch?v=WFyFCFC15dE&t=1442s)** actions, and you want to orchestrate the reasoning. That's what kind of gives you the scale because, you know, you can orchestrate separately, you can scale those two things very independently. In the end of the day, agents are sagas, but they are really bound sagas, right? And reply or or replay rather, sorry. Replay is how you answer, right? Six months from now, a year from now, when you want to know why a certain agent did something, you should be able to replay. You should be able to replay. The LLMs might have moved on. That same LLM may not be available now, the model, but you should be able to kind of mock that part and replay the rest of the things. But here's the most important thing, right? Um, every time an agent runs and makes a decision, that's the context that is now available to you. This is your proprietary

**[24:49](https://www.youtube.com/watch?v=WFyFCFC15dE&t=1489s)** context. That's not available to OpenAI or Anthropic. Which means they can they can rewrite and they can model your processes. One thing that they don't have is your context. And as you start capturing more and more of those things, this starts to build up your context. Which you can then use it to, you know, further enhance the context. And that is what I think, you know, is going to be our moat. Um people who own the processes, their moat is going to be their context, not necessarily the process itself, because the processes can be recreated, not the context. And yeah, um so here is a QR code. You can, you know, scan uh both Agent Conductor are open-source projects. So, feel free to give it a try. Um both the communities are also pretty active. Uh There are constant updates to both of them. We personally maintain both the

**[25:38](https://www.youtube.com/watch?v=WFyFCFC15dE&t=1538s)** repositories. And um yeah. Absolutely. Thank you. >> [music]

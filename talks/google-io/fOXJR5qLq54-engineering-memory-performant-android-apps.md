---
id: fOXJR5qLq54
title: "Engineering memory-performant Android apps"
slug: engineering-memory-performant-android-apps
conference: google-io
conference_name: "Google I/O"
category: "Vendor & platform"
edition: "I/O 2026"
year: 2026
speakers: ["Alice Yuan", "Ajesh Pai", "Fung Lam"]
channel: "Android Developers"
duration_min: 23
published_at: 2026-05-21T16:30:43Z
video_id: fOXJR5qLq54
url: https://www.youtube.com/watch?v=fOXJR5qLq54
youtube_url: https://www.youtube.com/watch?v=fOXJR5qLq54
tags: ["Android", "pr_pr: Google I/O;", "ct:Event - Technical Session;", "ct:Stack - Android;"]
transcript: true
---

# Engineering memory-performant Android apps

**Alice Yuan, Ajesh Pai, Fung Lam**

`Google I/O` · `I/O 2026` · `2026` · `23 min`

`#Android` `#pr_pr: Google I/O;` `#ct:Event - Technical Session;` `#ct:Stack - Android;`

[Watch the recording](https://www.youtube.com/watch?v=fOXJR5qLq54) · [Conference site](https://io.google/)

## Description

Learn about critical tools like R8 for code shrinking and best practices for bitmap optimization to slash native memory usage. We’ll also explore how to use the new ProfilingManager triggers for identifying OOMs and memory anomalies in the wild.

Resources:
Enable App optimization → https://goo.gle/4f4sZQl
How to add keep rules → https://goo.gle/4nclKIh
Best practices on keep rules → https://goo.gle/3PplkBI
Optimization for library authors → https://goo.gle/4nhrcK2
R8 Configuration analyzer → https://goo.gle/4nbIEiI
Agent skills → https://goo.gle/4tlJvPA
ProfilingManager → https://goo.gle/3Rfh6NR
ProfilingManager Triggers → https://goo.gle/4nKAsXa
Overview on how to optimize memory on Android → https://goo.gle/3PBe2ec
Overview of memory management → https://goo.gle/4nIe0xG
Android 17 App Memory Limits → https://goo.gle/43jvveu

Speakers: Alice Yuan, Ajesh Pai, Fung Lam

Watch the Android sessions from Google I/O 2026 → https://goo.gle/Android-at-IO2026

#GoogleIO

Event: Google I/O 2026

Products Mentioned: Android

## Transcript

*3,311 words · source: supa (en, exact timings)*

**[0:00](https://www.youtube.com/watch?v=fOXJR5qLq54&t=0s)** [MUSIC PLAYING] ALICE YUAN: Hi. I'm Alice. And my colleagues and I will be sharing how to engineer performance applications. When we talk about performance, we usually think of the visible metrics, a smooth UI and a fast startup. But memory is the silent foundation those metrics are built on. If your memory usage is unoptimized, your app's performance and your user experience will be negatively impacted, too. Think of your phone's physical memory capacity similar to this finite grid to illustrate a simplified version of memory page usage on a device. Every screen, high res video buffering, or on-device AI model claims a chunk of these pages. As long as there are empty pages, everything is smooth.

**[0:51](https://www.youtube.com/watch?v=fOXJR5qLq54&t=51s)** But what happens if all the memory pages are used and there are no free pages left? Behind the scenes, the system scrambles to find more space. And that scramble can result in costly performance impacts, such as UI jank, battery drain, ANRs and sudden app kills. For example, it could trigger frequent garbage collection to scan the heap for objects that are no longer being used. This occurring frequently can lead to visible stutters in the UI. When the free memory drops below a certain threshold, a kernel process reclaims and compresses memory pages. This can take significant processing power on the CPU and result in the unpredictable app ANRs and heavy battery drain.

**[1:39](https://www.youtube.com/watch?v=fOXJR5qLq54&t=99s)** If free memory is critically low, the low memory killer steps in and starts terminating entire processes based on a strict priority hierarchy. This is the primary cause of cold starts, which forces users to have a prolonged loading screen while the app completely rebuilds its states. It can also cause highly disruptive, sudden kills of visible processes, like a music player abruptly being shut down in the background while the OS runs out of background processes to kill. Not optimizing for memory consumption can also result in your app exceeding the bad behavior threshold for ANRs or crashes, which may impact your app's discoverability on Google Play. Finally, Android is developing new methods of system level

**[2:28](https://www.youtube.com/watch?v=fOXJR5qLq54&t=148s)** optimizations to ensure stronger memory management and device stability across the ecosystem. In this session, my colleagues and I are going to share three solutions for developers to optimize your memory, which will ultimately improve your app's user experience. AJESH PAI: I will share how to best configure your R8 tools. And Fung will explain some memory best practices to keep your memory usage lean. ALICE YUAN: Finally, I'll be sharing observability tooling so that you can catch memory issues in the field. By the end of this talk, you'll have a proactive roadmap to ensure your app continues to be performant. AJESH PAI: One of the most impactful changes you can make to improve your app's performance is to enable the R8 optimizer. While R8 is widely known for shrinking APK sizes,

**[3:19](https://www.youtube.com/watch?v=fOXJR5qLq54&t=199s)** it also helps reduce memory usage. When less code is loaded from APK into RAM, the app consumes less memory. R8 removes unused code and resources from your app, reducing the memory needed to load your app. You want to do this because, if you don't, unoptimized heap memory usage triggers frequent garbage collection, causing UI jank. R8 minifies classes, methods, and fields into shorter names, reducing memory usage. This is important because stack memory issues contribute to a complex, less efficient runtime. R8 performs optimizations such as method inlining to replace a method called with the actual code and merges multiple classes, reducing the heap size and stack memory. For your users, this means faster startup time,

**[4:09](https://www.youtube.com/watch?v=fOXJR5qLq54&t=249s)** improved rendering, and fewer ANRs. Now that we have seen the benefits, let's enable it in your code base. In your app build.gradle file, set isShrinkResources is equal to true. This enables R8 to remove unused resources and code. isMinifyEnabled is equal to true allows R8 to optimize the code, remove the unused classes, and rename the classes to reduce the code size. And finally, define the default ProGuard file, which contains the optimization rules. A key thing to make sure is to use the correct default ProGuard file. This legacy file prevents R8 from applying optimizations to your app. Note that from Android Gradle plugin 9, the legacy file proguard-android.txt is no longer supported, so replace the incorrect default file with

**[5:00](https://www.youtube.com/watch?v=fOXJR5qLq54&t=300s)** proguard-android-optimize.txt that is bundled with Platform Tools. This seemingly small configuration update can significantly improve several performance metrics. If your Gradle properties file includes a flag to disable R8 full mode, remove the flag to unlock R8's full potential. Make sure to thoroughly test your app after these changes as it enables powerful optimization. Making these changes can have a major impact on several performance metrics. Take the example of Monzo, a UK digital bank with 15 million customers and growing. As the app scaled, the engineering team identified app startup time as a critical area for improvement but worried it would require significant changes to their code base.

**[5:47](https://www.youtube.com/watch?v=fOXJR5qLq54&t=347s)** They sought a solution that would enhance the performance without the overhead of a complete rewrite. By fully enabling R8 optimisation, Monzo achieved a massive 35% reduction in their application not responding rate. And you know what? The team also saw a 30% improvement in slow cold startup time and reduced their overall app size by 9%. To summarize, enable R8 shrinkResources, correct default ProGuard file, and in full mode to get the maximum optimizations. When R8 optimizes your app, it can remove code that's used indirectly, such as code which uses reflection. This can cause your app to crash. In order to handle this, you can use keep rules. Keep rules are instructions that prevent R8

**[6:36](https://www.youtube.com/watch?v=fOXJR5qLq54&t=396s)** from optimizing parts of the code that relies on reflection. To add them, create a new file and mention it in the ProGuard file DSL. Keep rules are at the core of our configuration, so let's take a moment to talk about some best practices. Our first and most important tip is to make sure you are not using any global options as these limit optimizations for your whole app. One such global keep rule to avoid is dontoptimize, as this disables optimizations for the entire app, resulting in a significantly larger and slower app. Dontshrink prevents the removal of unused code and resources, forcing your users to download a bloated app. Dontobfuscate prevents renaming of human readable classes, methods, and fields into shorter names, preventing R8 from reducing memory,

**[7:24](https://www.youtube.com/watch?v=fOXJR5qLq54&t=444s)** especially in large apps. Another common pitfall is using broad package-wide keep rules with wildcards. A rule like this one will keep every single class, any fields and methods inside that entire package. R8 can't remove unused classes or methods within that package. It can't optimize the internal logic. And it can't minify any of the names. Having broad and wide keep rules like this can result in a non-performant app so consider removing them. Because AGP and R8 work together to identify fragments, views and activities that need to be retained after optimization, you should not write keep rules for these Android components. To recap, only add rules that prevent R8 from optimizing code that relies on reflection.

**[8:14](https://www.youtube.com/watch?v=fOXJR5qLq54&t=494s)** Rules should be scoped narrowly. They should not include global options or broad package-wide keep rules, or keep rules for Android components. If you are a library developer, you have two places to define keep rules, the consumer rules file and the internal rules file. The rules that you need to provide to the app that uses your library must go into the consumer rules file. These rules are automatically bundled and applied to any app that includes your library, ensuring that any code accessed with reflection or other indirect means is preserved. Adding rules like dontshrink, dontoptimize, or dontobfuscate in consumer file is a critical mistake. Because these rules are merged into the app's configuration, you would be forcing every app that uses your library to completely disable R8's optimization.

**[9:04](https://www.youtube.com/watch?v=fOXJR5qLq54&t=544s)** In order to prevent this scenario, from EGP 9, we have made sure that libraries can no longer publish if their code includes this global keep rule. Proguard-rules.pro file should contain the key rules that are applied to your library. The file is not passed on to the app that consumes your library. It's used to protect your library's public API implementation from being removed by R8 during your library's own build process. For more information about how to write keep rules for your library, visit the updated guide for library authors. To help you maximize R8's optimization and write specific keep rules, we have introduced the R8 Configuration Analyzer with a new version of R8. You can generate it by running a build enabled with R8. R8 Configuration Analyzer shows you

**[9:54](https://www.youtube.com/watch?v=fOXJR5qLq54&t=594s)** what portion of your app code is available for optimization. It provides you three metrics. Obfuscation score shows how much code of the app is renamed to save memory. Optimization score tells you the percentage of code that is optimized. Shrinking score shows the amount of resources that are removed. You can look at these three metrics to monitor, inspect, and improve your app's configuration. With Analyzer, you can see how many classes, methods, or fields are affected by each keep rule. You can also see the percentage of the app that is unoptimized due to a single keep rule. The detail page for a keep rule has information on which classes, methods, or fields are prevented from optimization. If you see that a keep rule is preventing optimization

**[10:44](https://www.youtube.com/watch?v=fOXJR5qLq54&t=644s)** in a large number of classes, methods, or fields that could be optimized, replace it with a specific and narrow keep rule. With R8 Configuration Analyzer, you can also see which keep rules are subsuming others, meaning a broader keep rule is making a narrower key pool redundant by already covering the same code. Make sure to remove the subsuming keep rule that is preventing optimization in a larger number of classes to get the maximum benefits from R8. Android X, Kotlin libraries, and many standard open source libraries are fairly optimized and already include specific annotations, so make sure to remove any keep rules that subsumes these libraries. With Configuration Analyzer, you can also see which libraries are introducing broad keep rules. Consider filing issues with the library developers

**[11:34](https://www.youtube.com/watch?v=fOXJR5qLq54&t=694s)** or using an alternate optimized library that serves the same purpose. For more information, check out the link on screen and try out the R8 Configuration Analyzer today. To summarize, R8 is a foundational tool for building performant Android apps. Optimizing with R8 is the first step to a smaller, more performant app. To maximize the benefits, enable R8 with the correct configuration, define the keep rules to unlock maximum optimization, inspect and improve the configuration by R8 Configuration Analyzer. In order to speed up your development process, we have added an agent skill that you can use with Android Studio Agent or other AI tools. This new agent skill streamlines your existing R8 setup by resolving misconfiguration.

**[12:22](https://www.youtube.com/watch?v=fOXJR5qLq54&t=742s)** Then, it leverages the configuration analyzer to clarify how keep rules affect your code base and, finally, offers actionable advice for optimizing those rules. This agent skill allows you to improve your app's performance with minimal effort. You can find more details about agent skill and how to use them here. Next, Fung will show you how to further optimize your app for memory. FUNG LAM: Let's talk about one of the most impactful thing you can do to optimize your memory usage-- bitmaps. In this multimedia world, users spend a lot of time with images on their phone, browsing social media, sharing photos, finding restaurants. Bitmap images are everywhere.

**[13:10](https://www.youtube.com/watch?v=fOXJR5qLq54&t=790s)** It is the place you should put your focus. Bitmaps are the largest common memory objects your app will have to deal with. Bitmap decoding and other operations that scale with bitmap dimensions are often on the critical path to drawing frames . And bitmap size continues to grow as displays are larger and higher density. So bitmaps are the first thing you should consider optimizing for memory improvement. Let's look at four best practices you can adopt today. First scaling. Never load a 4K image into a tiny thumbnail image view. Doing so with massive amount of RAM for detail that user can't even see. Use in sample size in bitmap factory options to load a smaller version or use a library like Glide or Coil

**[14:02](https://www.youtube.com/watch?v=fOXJR5qLq54&t=842s)** which does this automatically. Cropping. You don't have to add a padding into a bitmap for letterboxing or framing. That uses much more memory. Config. The default config argb4x uses twice as much memory as RGB 565. If you are loading a thumbnail and you don't need transparency, use RGB 565. In other cases, consider whether RGB 565 provides sufficient quality. Lastly, we use Bitmap Pool can help you reduce bitmap and reduce the number of memory allocations. Again, use libraries like Glide or Coil to help you. Talking about reuse.

**[14:49](https://www.youtube.com/watch?v=fOXJR5qLq54&t=889s)** Do you know Android Studio can help you to find duplicate bitmap usage? In Android Studio, you can profile your app in the Profiler pane, choose Analyze Memory Usage, and start profiling. After a few seconds, you can see the details of the heap dump. Can you see the yellow triangle? Yes. Duplicate bitmaps are now highlighted. But wait, how do I know which one is the duplicate bitmap? Don't worry. Click on the detail and you can see the preview of the image, which gives you a better idea where is this happening. What is the next step after optimizing bitmap usage? Memory leak. Don't underestimate memory leaks. These will accumulate and use up a huge amount of memory over time.

**[15:39](https://www.youtube.com/watch?v=fOXJR5qLq54&t=939s)** And again, we have something for you in Android Studio. In Android Studio, pain-free, you can now find memory leaks with LeakCanary. The leak analysis is now within the IDE and integrated with your source code so it is providing features like Jump To Source and other helpful connections. You can also copy the entire leak analysis for further processing with Gemini. This could reduce the friction and time required to investigate and fix memory leaks. Let's have a quick recap. Bitmaps are the largest common memory objects. Optimizing bitmap usage can bring huge memory improvements. And Android Studio can help you to find duplicate bitmaps and fix memory leaks. Here's Alice to show you advanced memory profiling tools.

**[16:31](https://www.youtube.com/watch?v=fOXJR5qLq54&t=991s)** ALICE YUAN: Now that we've learned some foundational memory best practices and tools, let's talk about advanced tooling that you can use to level up your memory debugging and analysis. Profiling Manager is an advanced performance observability API introduced in Android 15. It's the only API that lets you collect real user Perfetto profiles programmatically. The Profiling Manager API supports two types of profile collection. In app-driven profiles, the developer must explicitly define the start and end of the collection process. In this session, we'll focus on the event-driven profiles that are automatically collected by the system. Since Profiling Manager's launch, we've scaled up the number of event-based performance

**[17:20](https://www.youtube.com/watch?v=fOXJR5qLq54&t=1040s)** triggers significantly. In addition to triggers introduced in Android 16, such as ANRs, app_fully_drawn, and force_stop, in Android 17, we're launching four new event triggers, cold_starts, kills due to excessive CPU usage, Out of Memory, and Anomaly. For this talk, we'll be focused on the memory related triggers Out of Memory and Anomaly. The anomaly trigger identifies performance anomalies detected by the system. Memory and binary spam are the first two types it supports. When memory usage exceeds a threshold that the system considers a violation, a heap dump is triggered. This allows you to use the anomaly trigger to examine resource allocations and the application state

**[18:09](https://www.youtube.com/watch?v=fOXJR5qLq54&t=1089s)** to identify the root cause of the issue. To support the Android 17 memory limit enforcement, the Anomaly trigger provides a heap dump prior to app termination. With the Out of Memory trigger, Profiling Manager monitors for out of memory errors. When one occurs, the system automatically collects a Java heap dump. This captures the exact state of your heap object allocations at the moment of the crash, providing significantly more actionable debug information than a standard stack trace. Let's explore how to create a profiling manager trigger. We'll use Out of Memory events as our example, though you can replace this logic with any other event trigger you wish to collect. To start listening for system triggers,

**[18:57](https://www.youtube.com/watch?v=fOXJR5qLq54&t=1137s)** you should do the following. First, import the Android X tracing and core libraries. Once the application launch has completed, initialize profiling manager by fetching it from system services. Register for all performance triggers you're interested in debugging. In our scenario, it would be the Out of Memory trigger. Then register for profiling results by providing a results callback listener. The result listener is defined as a consumer listener that listens for any profile completion. For Out of Memory crash triggers and other triggers where the app is killed, this data would return upon the next application launch. Once the Out of Memory trigger result has returned, the profiling result will provide the local file path.

**[19:49](https://www.youtube.com/watch?v=fOXJR5qLq54&t=1189s)** You can then upload the profiling result file to a server using Work Manager for either manual or bulk analysis. Let's see how we would manually inspect the heap dump collected using Profiling Manager and identify the root cause. I'll demo an example using a sample file parsing app that received an out of memory crash. To inspect this data, I'll be using Perfetto UI, a tool to visualize performance, instrumentation, and analysis. You can use this tool to inspect recorded heap dumps by going directly to UIPerfetto.dev and drag and drop or open the trace file. Here's what a heap dump looks for the sample app I've written that received the Out of Memory crash. Perfetto displays the root Java object alongside the memory

**[20:40](https://www.youtube.com/watch?v=fOXJR5qLq54&t=1240s)** allocation hierarchy. We can see that the root memory allocation of this heap dump is significant. It's past the maximum heap size of 256 megabytes defined by the manufacturer. Also merges many smaller objects into combined slices called merged within its flame graphs. Developers can generally focus the debugging efforts on larger objects as the merge smaller objects are not typically critical towards Out of Memory failures. You can click on a section of the stack and isolate the stack by using the Show Stack feature. Finally, I traced the root cause of the excessive heap usage to the ParseLargeFile in our class in Main Activity. I was unnecessarily holding references

**[21:31](https://www.youtube.com/watch?v=fOXJR5qLq54&t=1291s)** to string objects created during the file parsing, which prevented them from being garbage collected. I can now clear these references to resolve the Out of Memory issue. Profiling Manager also supports collecting of heap profiles, which provide deeper insight into native memory usage to help you optimize allocations like bitmaps. Check out this Perfetto documentation to learn more. To recap, Profiling Manager is a performance observability API that provides actionable, in-field performance data. Profiling Manager can record event-driven profiles like Out of Memory and memory anomalies in Android 17. Finally, use the Perfetto UI to analyze heap dumps to better understand memory object allocations.

**[22:21](https://www.youtube.com/watch?v=fOXJR5qLq54&t=1341s)** That's all from us on engineering performance applications. By applying the strategies discussed today, you can shift from a proactive memory roadmap that delivers a smooth experience for your users. Keep app size small by configuring R8 with narrow, specific keep rules that maximize bytecode optimization. Keep your memory usage lean by optimizing bitmap loading. Also, catch memory leaks early right inside the IDE using Android Studio's LeakCanary integration. Finally, leverage Profiling Manager's API's new event-driven triggers in Android 17 that allow you to automatically capture heap dumps for out of memory crashes and memory anomalies. You can check out all of our additional resources in the video description.

**[23:11](https://www.youtube.com/watch?v=fOXJR5qLq54&t=1391s)** Happy learning. [MUSIC PLAYING]

---
id: ExXmZBlu8KA
title: "Scaling LLM Inference With Tiered Caching: Extending LMCache With Amazon... Yihua Cheng & Ziwen Ning"
slug: scaling-llm-inference-with-tiered-caching-extending-lmcache
conference: lf-ai-dev
conference_name: "AI_dev / Open Source Summit (Linux Foundation)"
category: "General software conferences"
edition: "Open Source Summit + ELC NA 2026"
year: 2026
speakers: []
channel: "The Linux Foundation"
duration_min: 41
published_at: 2026-06-03T18:23:10Z
video_id: ExXmZBlu8KA
url: https://www.youtube.com/watch?v=ExXmZBlu8KA
youtube_url: https://www.youtube.com/watch?v=ExXmZBlu8KA
tags: []
topics: ["Inference, serving & GPU infra", "Prompting & context engineering"]
transcript: true
---

# Scaling LLM Inference With Tiered Caching: Extending LMCache With Amazon... Yihua Cheng & Ziwen Ning

**Speaker not identified**

`AI_dev / Open Source Summit (Linux Foundation)` · `Open Source Summit + ELC NA 2026` · `2026` · `41 min`

[Watch the recording](https://www.youtube.com/watch?v=ExXmZBlu8KA) · [Conference site](https://events.linuxfoundation.org/ai-dev-europe/)

## Description

Join us at the premier vendor-neutral open source conference, where developers and technologists come together to collaborate, share knowledge, and explore the latest innovations and advancements in open source technology. Learn more at https://events.linuxfoundation.org/

Scaling LLM Inference With Tiered Caching: Extending LMCache With Amazon SageMaker HyperPod - Yihua Cheng, Tensormesh, Inc. & Ziwen Ning

LMCache supports tiered KV caching with CPU memory offloading, extending inference beyond GPU memory limits. But what happens when local CPU memory isn't enough? This session introduces the next tier: offloading KV cache to Amazon SageMaker HyperPod managed storage, expanding cache capacity for large-scale LLM inference.

We'll cover the technical design of the SageMaker HyperPod connector contribution to LMCache. Hot entries stay in GPU memory, warm entries spill to CPU memory, and cold entries persist to HyperPod's managed storage. This three-tier architecture lets organizations cache far more context than local resources allow, reducing redundant computation for repeated prompts and long-context scenarios.

The session demonstrates the integration in action, showing cache hit rates, latency across tiers, and how the connector handles transitions between local and remote storage. We'll discuss key engineering decisions, including async prefetching and failure handling.

Attendees will leave with practical knowledge of how managed cloud storage can extend open source caching frameworks for LLM inference infrastructure.

## Transcript

*6,221 words · source: supa (en, exact timings)*

**[0:00](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=0s)** All right, it's 4:30 now, so let's start. Uh my name is Ziwang, open source contributor to M Cash, and this is uh Yihua. Tencent vice CTO. And our talk today is scaling uh and and inference uh with tier caching. And the sharp issue is that uh when your um serving infrastructure is bottlenecked by repeating the same expensive profile work over and over, where do you put that data instead? And how big can you make that uh somewhere else? We'll show you a path that goes all the way down to manage the tier storage and what it gets you. Uh quick road map. Yihua will start by walking you through M Cash and its tier cache uh architecture, where L1 and L2

**[0:48](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=48s)** mean, and why both of them are needed. Then I will take over to talk about the Amazon uh SageMaker HyperPod tier storage, how we integrate it with uh M Cash as an L2 back end, and the benchmark numbers. Okay, Yihua, over to you. All right, thanks Ziwang. Thanks for the introduction. And before I start, just want to do a very quick small survey. Uh like, how many of you guys have been playing around, trying, or deploying like serving engines, uh LM serving engines? And maybe how many of you heard the word of KV Cash? Great. Seems like we've got a great set of audience here. So, probably let me start with some like well-known fact. Uh think about this, you probably build

**[1:35](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=95s)** some awesome AI applications which works very well in development uh environments, but when it hits production, there there are actually a bunch of like challenges you're facing. So, the first one is uh rising uh the rising of GPU cost, which means that every single user, every single task, every single context you're putting to your application is is actually adding to your GPU bill, which is not going to be cheap at all. And then there's the second challenge is latency bottlenecks. So, long latency not only undermines the user experience, it actually is pretty frustrating to users, and which will also make your product, your AI application become less competitive.

**[2:23](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=143s)** And third challenge is redundant compute, which means that in many different real like real-world workloads, your AI application, your LM is actually processing the same context again and again, and wasting the GPU compute, wasting the GPU cycles on the work they have already been done before. And finally, the there is a challenge on scaling the the inference, because when it comes to production, the GPU memory, the single GPU memory actually becomes a bottleneck of how many contexts you can fit and how many concurrent you can serve in your in all environment. And those are not just small inconveniences. These are actually major roadblocks for you to scale AI applications. And to address those challenge,

**[3:12](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=192s)** there is actually a key data structure which which is called KV cache. And just for quick background, the KV cache is a data structure, it's a big tensor that that is generated during the inference of large language models. And when the request is coming to the large language model, it actually generates this KV cache, which captures the model's understanding of the context. And since it actually embeds the knowledge of this context inside the model, by reusing the KV cache, you can actually cut the GPU cost by fact 5 to 10 x where also reducing the request latency by a lot, because the KV cache can help you skip the redundant GPU computations by skip the processing of the long documents.

**[4:00](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=240s)** Also, by reusing the KV cache in your distributed environment, you can have a better scalability because suddenly you have your contacts being able to being accessible to all your inference engine instances in the cluster. And also, all the instances, no matter no matter which instance sees this context previously, other instance can reuse the previously computed KV caches and skip the GPU computes. And these days we are actually seeing people want to do more and more things on KV cache to improve the inference. So, very interesting story. At the beginning of we when we start the M cache project is around 2 years ago and we are talking to people and people will just feel like,

**[4:47](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=287s)** "Okay, KV cache is interesting, but it's just some internal data structure generated by the inference engines and can be totally fit into the GPU memory." But since probably in the middle of last year and also early this year, we do see a lot of trend, a lot of interest, a lot of people want to do a lot of things on the KV cache including like for example, people want to share uh save the KV cache to the remote storage because they are really big and GPU [clears throat] GPU memory cannot simply hold the cannot simply hold enough KV cache. And people want to also observe the KV cache life cycle saying, "Okay, when is my KV cache generated? When is it evicted from the GPU memory? When is it stored into the remote uh remote storage?" And as I said, people want to share the KV caches across different serving

**[5:35](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=335s)** engines because they would like to reuse the context not only in this local engine, but also in the all the different engines across the cluster. And finally, there's a lot of interest uh especially this is for example turbo cons like people want to do some advanced advanced transformations on the KV cache including compress the KV cache or quantize the KV cache or even added the KV cache to have the better I'm sorry I'm also quality. However, the challenge here is that those KV cache operation if we want to land it in those KV cache operations in real production it becomes really really painful. It becomes pretty difficult. So here are the reasons main reasons. The first of all the KV cache operation it actually incurs a lot of overhead in

**[6:25](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=385s)** inference. So think about this the inference engines they're they need to run the model on the GPU which is pretty compute intensive tasks. And if you want to do deal with KV cache storing saving you're actually play with a huge amount of data and transferring it between different storage layers which is the IO intensive task. And if you're mixing the IO intensive task with the compute intensive task it actually incurs a lot of overhead on both ends. So you cannot get a very high highly concurrent IO IOPS or and the inference engine's performance is getting impacted. And the second one is the inference engine is actually very difficult to hack. So the model in modern inference engines like VLM or SC line they have some basic functionalities to let you

**[7:14](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=434s)** offload the KV cache to to the CPU RAM. But it's just a a flag you can turn it on or turn it off. You cannot do much more than than that. But if we're talking about for example turbo cons for example KV cache compression or more advanced KV cache like I want to manage the KV cache is across different memory tier. There's no place for you to implement that logic. And finally there's it's it's pretty connected to what I'm just uh I just said The the interfaces for KV cache operation is super limited. So, uh So, if you want to implement different logics, or if you want to implement different algorithms, there's actually no place for you to to do this in the serving engines. And to address the those pain points or those challenge, our insight is actually

**[8:04](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=484s)** uh that the KV cache is not only just some internal internal data inside the serving engine. It actually needs a separated totally separated soft software stack to manage, to uh do the transformation, to do the IO operations, and to do the management operations. And that is why we build the AMCache project. So, on a very high level, AMCache uh project it it's a open-source project which pro- provides most mature open-source solution that connects the inference engines with uh tier storage uh and the tier storage with the KV caches. So, um more like fine-grained uh not fine-grained, but a more a high high-level overview of the today's AMCache, where at the top highest level we we've got this uh distributed serving

**[8:53](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=533s)** framework, like NVIDIA Dynamo or AMD from Red Hat and IBM, and also this KServe project, uh which is another uh another popular open-source project. And those serving engine frame- uh distributed serving framework is directly orchestrating on the serving engines, which is VM, as we all know, TensorFlow, as we all know, and PyTorch, as we all know. And AMCache is sitting uh under lying it, providing the uh management, uh IO, and also transformations on the KV cache. And as of now, we've got uh the AMCache project has already been uh has already got like 200 more contributors from the global open-source community, global AI community. And it's also a PyTorch ecosystem project. We have joined the PyTorch ecosystem since uh PyTorch Foundation since 2025.

**[9:44](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=584s)** So, uh then let me just give a give some uh ideas about what are the key features in AMCache. Uh The first one is uh the first uh set is the basic functionality. So, what are the basic functionality pro- provide by AMCache? So, uh in AMCache, we actually support the uh uh distributed uh CPU RAM offloading and key cache sharing, which means that you can uh store a key cache into CPU RAM and access it into uh from another node uh using the RDMA networks. And uh from the serving engine's perspective, AMCache is actually a distributed uh memory pool that has uh uh that is uh provides a much larger capacity than a single than a single

**[10:32](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=632s)** node uh memory. And regarding storage, we have implemented the purely async opera- uh optimizations for the for the storage uh for not only local, but also remote and even more advanced uh storage techniques like GDS, GPU direct storage. And finally, we also have this uh multi-tier memory management, which means that if you have uh multiple different storage devices and you want to customize what kind of uh key caches you want to store in the hot hotter tier and what kind of uh key cache you want to store in the slower but larger tier, uh you can embed your local like uh logic here. And another very important part is about the production support. Basically, how to land this all those uh components

**[11:21](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=681s)** into production. So, in AMCache, the first thing we do is we have this uh cloud-native deployment, which we call AMCache operator. Uh AMCache operator is a Kubernetes operator includes a controller, includes a CRD, uh, that can allow you to easily deploy in your, uh, Kubernetes or OpenShift or whatever production clusters. And we also implement this, uh, enhanced fault tolerance, uh, which is we call it bidirectional fault tolerance. Uh, so by bidirectional I mean, for, uh, on on the first direction, if the VM restarts due to any reasons, the KV caches generated by the VM can still persist in M-Cache. Not only persist in the storage layer, but also persisted in

**[12:09](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=729s)** the the CPU RAM layer. So if even if the VM is, uh, our serving engine is getting restarted, you still have all the KV caches in the, uh, hot KV caches in the CPU memory. And when the serving engine starts again, you can directly load them with a very fast, uh, very high bandwidth. And on the other direction, uh, if the if M-Cache, uh, failed or restarted due to any reasons, the serving engine, uh, we have logics to prevent serving engine, uh, being impacted. So basically, the serving engines can still serve the request, uh, but probably slightly at a lower speed until M-Cache is getting restarted. So, uh, the third point is, uh, is, uh, related to observability. I think it's very important to to production because

**[12:58](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=778s)** when people putting things into production, we always want to know that what are the potential a problem in this component, or is it working well, or is it providing real values? Uh, we have this KV cache observability, KV cache tracing, and KV cache life cycle analysis. And we also have example like dashboards for people to get service in the open source, uh, repo. And the next item is, uh, we call it advanced features. It's not just only like storing or loading KV cache, but it's going a little bit beyond that, uh, where we actually touches the values in KV cache. So, one example we call it is, uh, non-prefix KV cache sharing. So, as a background, if you just, uh, load or store KV cache, uh, what you can do for now is actually, uh,

**[13:47](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=827s)** we call prefix caching, which means that if the document you change a few words at the very beginning, then the KV cache of the later part cannot be reused. But, what we have here is, uh, feature we actually call it cache blend, but what it does is even if the, uh, the reuse part is not a prefix, or even if you make changes to your system prompt, or make changes to, like, timestamps, uh, you can still use the KV cache of the later document. And also we have the, uh, interfaces for people to implement, uh, the KV cache compression and the KV cache quantization inside AM cache as well. And I think previ- uh, just recently there is a PR in AM cache also implementing the the turbo quant. Uh, and finally, there is, uh, ecosystem

**[14:35](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=875s)** support. So, AM cache is not only, uh, just, uh, small project that works with, uh, some a subset of the, uh, different projects, but we actually also have a pretty wide, uh, like, ecosystem support, where we support, like, a lot of mainstream serving engines, including vLLM, including SGLang, including TensorRT-LLM. Also, there's another, uh, serving engine called Modular. I'm not sure, uh, many people heard of that, but we're also making progress on supporting that. And in the meantime, we also support a a lot of different hardwares, like, not only NVIDIA hardware, but also, like, AMD GPUs, as well as Ascend and some, uh, Intel XPU and HPU. And we are building this platform module so that if there is new chips, like, uh, Amazon or Meta, they're building their own chips.

**[15:23](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=923s)** Uh we can we can uh uh integrate those uh those new chips by just implementing a few platform support uh functions. So, uh that's a feature part, and let's go back to the KV cache uh for for a little while. I think regarding store and load the KV cache, a lot of people may wondering, "Okay, what are the real KV cache workload look like?" Looks like. So, what what do we really need to do regarding all those KV cache uh generated KV cache? So, let me answer this question probably in from two different perspectives. The first one is how large the KV caches are. So, here I'm getting a few numbers from uh

**[16:09](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=969s)** some analysis benchmark uh about how fast the the uh the GPUs can uh can serve uh for different models and their KV cache sizes. So, for the full attention model like Minimax M2.5, the KV cache is actually really big. They are for 1 million tokens, they are 256 GB of the KV caches. And uh recently, like we have we have been seeing this uh super condensed or super compressed KV cache uh in this DeepSeek-V2 model, where you got a much much smaller, but still like 10 around 10 GB of the KV cache uh for 1 million tokens. And combining with the previous speed, which means the speed of the GPU to process the uh process the input

**[16:57](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1017s)** context, we've got the number of this. As a single like B300 node, it will generate like from 10 to 500 TB of the KV caches every day. And think about if you have multiple uh B300 nodes, it will it will go to like petabyte level of the KV cache. Uh and although this sounds like a very large number, actually in real world, probably not all the KV caches we need to save. So, the next question we want to answer is like how many KV caches we should save in like real like real world workloads. So, what what we do is we actually con- conducted some uh massive analysis on massive trace capturing uh for some uh from some like public coding agents.

**[17:47](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1067s)** And uh we did some trace analysis. Like uh in real world, the context lengths is will be ranging from usually be ranging from like 300 to 800 K, which is a super long context. And to to serve uh those contexts, probably a typical uh concurrent number of user sessions for each node is around 100 to 300. So, here the concurrent user session does not mean that the concurrent requests running in the serving engine, but it's like the active sessions, like people are chatting with the uh with the coding agent, and people maybe reviewing the code and then uh talk to the coding agent again. So, the the session is active. And those numbers are translated to a uh to to what I put down here. So, probably

**[18:37](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1117s)** in order to serve a workload like that, we need to keep uh around like 1 to 10 terabytes of the KV caches inside the system at any time to in order to achieve a good cache rate in order to avoid the uh avoid the the the the recompute. And as we may know, the CPU RAM, the CPU RAM capacity of the modern like uh uh servers are just uh 1 terabyte or 2 terabytes, which is not enough for so much uh that much GPU uh that much KV cache. And that's why uh we're saying like storing and reusing KV caches require multi-tier storage system, where we are not only storing and reusing KV caches from CPU, but also from the uh, from the lower-tier storage. And here's what we're doing M Cache. So,

**[19:26](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1166s)** in M Cache, we do have this hierarchical memory management, uh, but from the serving engine's perspective, it's pretty simple. So, serving engine, well, just uh, from serving engine's view, M Cache is just a pretty big pool of KV cache. It does not know the serving engine does not know any details about, okay, how M Cache do the distribution, or how M Cache do multi-tier memory management. What they do is just require KV caches from uh, M Cache. And inside M Cache is uh, actually a little bit more complicated. So, at the very beginning, the KV cache will land in the no no local CPU RAM. And then we have this low local storage, uh, where uh, if the no local CPU RAM is full, we can uh, put the put put the KV caches into like local

**[20:14](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1214s)** SSDs or local hard drives. We also have this remote shared storage like remote NFS or maybe even S3, uh, to store and share the KV caches across the whole cluster. And since we're in a distributed setup, there is another node duplicating the setup. And what we have also implemented is this RDMA based intra internal sharing internal sharing, uh, where uh, different instances can get KV caches from other instances uh, CPU RAM and other instances SSD uh potentially. And finally, uh, small but very important part is the we call it memory controller. Basically, this part implements the logic of how you manage the memory across different tiers. Like, where I should put the KV

**[21:02](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1262s)** cache to, or when should I evict the KV cache to lower tier. So, all the logics are here. We have interfaces for people to plug in their own customized logic as well. So, basically this is a architecture of the of the multi-tier memory management in the AM cache. And after the architecture, we also have a bunch of storage optimizations. The first one is I think I mentioned it before is like purely asynchronous IO. Like the IO operation is happened purely in the background with zero overhead adding to the serving engine. And we are actually in the new new architecture of AM cache, we're actually separating the AM cache into a different process than the serving engine process, which eliminates the potential GIL contention caused by Python. And there is

**[21:52](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1312s)** I I would like to talk about these two points at the same time, the distributed look up and concurrent loading. Which means that in real world, the KV caches of a single request may from different storage tiers. Like some part of them, for example, system prompt may be stored in in CPU memory, and some in local SSD, and some even colder KV cache will be stored in remote remote storage. And what we can do here is rather than just doing look up from a single tier, we can do a concurrent look ups in multiple different tiers. And build the real load plan to before the execution. And once the load plan is built, we can AM cache can concurrently load the KV caches from multiple different storage

**[22:40](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1360s)** tiers to accelerate the loading time of the of the KV cache. And finally, I I think I also want to emphasize that the AM cache is not just pre-built solution, it's actually ecosystem model. The L2 party is actually a public interface that anyone can easily extend. So, we're abstracting the the L2 interface to the between Memcached and storage vendor into three key primitives, which is store, load, and lookup. Or you can think of this as put, and get, and contains. And for the async organization, uh we're implementing this io_uring-like interface. Uh basically, you can submit the store, load, and lookup tasks uh in a non-blocking manner. And later, you can

**[23:28](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1408s)** check whether the load and store and uh and lookup tasks are finished and pop the finished uh items. With this context, we're actually integrating a lot of uh different storage solutions from the community, including S3, including Mooncake, Redis, All Key, and also a bunch of uh NVIDIA NVSwitch, of course, and also a bunch of like storage vendors like Weka, IBM Scale, local storage. And uh today, Zhaohui will uh uh I will hand over to Zhaohui. He will talk about the experiences and how do how does uh uh Amazon SageMaker HyperPod lands in uh Memcached. So, Zhaohui, hand over to you. Okay. Uh thanks. Uh let me take over. Uh so, we know the shape of of the

**[24:16](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1456s)** problem. We want a L2 that is bigger than CPU memory, shared across the cluster, and uh persistent. Let me walk through how we use Amazon SageMaker HyperPod to integrate it. So, quick context uh on Amazon SageMaker HyperPod uh for those who have not worked uh with it. Uh HyperPod helps you provision resilient clusters for running machine learning workloads uh and developing state-of-the-art models, large language models, diffusion models, or foundation models. It removes the undifferentiated uh heavy lifting of building and uh maintaining a large-scale computer uh clusters. And when hardware fails, the residency features automatically detect and replace it on the fly. So, teams can focus on machine learning instead of cluster ops. Hyper quality integrates with the Amazon

**[25:04](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1504s)** EKS. That's how we deploy cache on it as a container riser workload. And uh And the critically for our talk, every Hyper board node also runs a managed tier storage service. It's there where you provision the cluster. That's what we are using as our after backend. Let me describe what the tier storage looks like. On every node in your cluster, there's a tier storage service running in the back end background. It manages two tiers. The first tier is a shared memory on host, basically RAM. And the second tier is a no local SSD, which gives you a lot more capacity. Uh the demand moves in ter-

**[25:53](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1553s)** Uh the demand moves the entries between the two tiers behind the scene. What clients do see are two interfaces. The first is a control uh API that serves over HTTP. This is how clients ask things like uh do you have this object? Please reserve it for me. Where is it located? It's small structured uh message at low frequency. And the second interface is the data path. And this is a part that's actually a neural. Uh the bytes themselves don't go over the network. They don't even go over a Unix socket. They're served over uh shared memory on host. When a client uh requests an entry, uh the demand makes sure it's in shared memory and points the client uh at right region.

**[26:41](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1601s)** So, the client process and the storage service end up reading and writing uh the same physical pages of RAM. Why does this matter? Uh for something like a multi gigabyte query cache transfer, the cost of going over a network long store would be huge. But with shared memory as the data pass, every read is essentially a memory copy. Orders of magnitude faster, no serialization, no network hop. And then let me describe how the Hyperion memory tier storage fits L2. So first, uh larger. Yes, the capacity uh tier on each Hyperion node is 100 gigabytes. Much bigger than CPU RAM. And it's cluster shared. Yes. Every node

**[27:32](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1652s)** has the storage service and in production the routing layer in front of M-Cash. Um pins returning users to the same to the same node when possible. So most of the cache hits stay node local. And persistent across engine restarts. Yes. Uh the storage service is a part of the platform, not part of the engine. Uh we can crash, restart, redeploy, and the cache is still there. And there are two bonus properties on top. Uh one, the local data pass means a cache hit is a memory copy, not a network transfer. That's where most of the latency improvement uh comes from. And second, the operational story is clean. Uh there's nothing to deploy or operate. We just open a connection. So how does M-Cash actually uh talk to

**[28:24](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1704s)** this thing? Uh let me put the whole picture on screen and walk through it from the top. So first, uh the user request uh comes in. Uh here's the inference engine, uh such as a VM, uh which already keeps the active prefix the cache on GPU. And the engine talks to M-Cash, which as an L1 uh in CPU memory. And uh then the new piece is And then the new piece I built a hyper port connector. The connector is the bridge between memcached pluggable L2 interface and tiers and tier storage service on the local node. From there, there are two channels, a sync a sync HTTP control channel for metadata and a sync shared memory channel for the

**[29:12](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1752s)** actual data into the tier storage two tiers, shared memory and SSD. The connector mirrors the two interfaces of the storage service. The control plane handles the metadata. Does the entry exist? Please reserve it or release it. Those are RCP requests on small number of endpoints, asynchronous, low frequency, low payload. And the data plane is the heavy lifter. When the storage service tells the connector, your data lives at offset in shared memory, the connector reads it directly from there. No copy through user space, no kernel mode round trip in the common case. Just pointer math and a memory copy into memcached tensor buffer.

**[30:00](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1800s)** And from a user's point of view, turning this on is just a one-line change, a URL with the sys mem hyper port scheme in your memcached config. Then let's walk a full request through the system end to end. And for write for write request on the right side, as the engine generates new key value state, memcached put it into L1 right away. That's faster than in process. And independent and independently, a background process picks up entries from L1 and shifts them down down to hyper port through the connector. The engine doesn't wait for any of these, it's already moved on to the next batch, the next request. For the reader request, imagine a

**[30:49](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1849s)** returning user shows up after their L1 entry has been evicted. M cache checks the GPU first. Miss, checks L1, miss, then it then it asks HyperPod through the connector's control plane. Do you have the user's key cache? If yes, the storage service points us at a region in shared memory. And the connector copies the bytes into a tensor and hands it back to M cache. M cache hands it back to the engine, the engine resumes the generation as if the cache has been in tribute all along. So two things were worth highlighting. One, neither one neither past blocks the engine on network.

**[31:35](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1895s)** And second, the connector internally prioritizes reads over background writes. So even when the system is under heavy network workload, say lots of new sessions filling up, and interactive user's read is not waiting in line behind them. That priority handling is what protects the tail latency. >> [clears throat] >> And let's talk about how to set up this in a real cluster. Set up this on a real cluster is boring kind of. You know, you just need to add one line to your M cache config. A URL with the sigman HyperPod scheme pointing at a at a local storage service. You mount the shared memory directory into your M cache container so the

**[32:24](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1944s)** connector can read from it. And that's the deployment. Sure, there are some tuning knobs, connection limits, time out durations, but the but the defaults work for of the workloads. You don't need to think about them all day long mostly. Um and because the storage service is already running on every HyperPod node, there's nothing else to deploy. No new credentials, no actual cost line on our bill. It's part of the platform. Okay, then let's go to the part you've been waiting for. Does it really help? So, we ran a realistic multi-round chat workload to find out. And here's the setup. Uh we used the Llama 3.1 70B model. Um and for hardware, we used the two 2P5 uh

**[33:16](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=1996s)** 48x large nodes. Uh that's eight H100 uh per nodes, uh 16 GPUs total with a tensor parallel of eight per server. And for workload, we have a 100 concurrent chat sessions. Each carrying uh 12,000 tokens of history. Um and new requests are arriving at eight uh QPS. L1 inside at uh 20 GB per server, enough to hold some sessions, but nowhere near all of them. L2 was 100 uh GB per node of all HyperPod managed tier storage. And we compared the two configurations, L1 only uh using CPU cache and no remote here, and L1 plus L2, same same setup with the um

**[34:04](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=2044s)** with the HyperPod connector enabled. And here are the numbers. Uh we we saw the inter-token latency at a P50 drop from six 63 milliseconds uh to 38. Uh there's a 1.67 times improvement. At P90, uh 165 down to 17 117. Um about 1.4 times better. And the output throughput went up from 844 uh to 1071 tokens per second. And for same hardware, same workload, we see uh 27% more throughput. And in the same window, we served uh 18% more total requests. Uh and success

**[34:53](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=2093s)** rate was 100% in both. Uh we see the time to first token tells a more nuanced story. Uh the the P50 is slightly higher with L2. There's more cost on cache misses for the extra check. Um but the P90 is actually better with L2. It's about 1.2 times. That's because we cache That's because we catch the long tail of evicted sessions where the L1 only setup would have triggered a full multi-secondary recompute. Um and one important thing, L2 is not free and it's not always win. Um you see the biggest benefit uh where recompute is expensive and L1 is under pressure. Uh that means

**[35:41](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=2141s)** large models where prefill is expensive per token. Oh, also long contexts where prefill costs uh grow super linearly or high concurrency where L1 is consistently evicting um old sessions and workloads where users return like a chat um agents rag pack pipeline. But if you're serving small model with a short single turn request, uh the gain modest and you might even see a small penalty on first token token latency. Uh but the production but the production reality of LM serving today is increasingly the first case and not second. Uh With big models, long contexts, lots of concurrent users, returning

**[36:29](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=2189s)** sessions, Redware L2 earns its keep, yeah. And overall three takeaways, first Amazon Sagemaker pod managed tier storage is available and even ideal L2 for replication. The combination of capacity, local data path, um bandwidth, and zero operational overhead is hard to match with a bring your own infrastructure. And secondly, it's all open source. The connector is optional in ElastiCache one config line, you're running. And third, the performance wins are real and measurable and measurable production shipped to workloads. 1.67 times better unit of work latency P50 and 1.27 times more throughput on the 70B model

**[37:21](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=2241s)** under concurrent load. And just to to add, what we hope you take away is that architecture generalizes. The L2 abstraction in ElastiCache is not tied to any vendor. We did work for Aeropod because the local data path properties are particularly nice, but the same pattern applies to others too. And okay, thank you and the links are on the side on the slide here. The ElastiCache report, the post has the results and the instructions yeah. Feel free to ask any questions if you >> [clears throat] >> if you have. Oh, oh, go ahead. I have a question about real world

**[38:08](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=2288s)** maybe customize policies how static versus how dynamic can Like for example, like inspect a session and decide, oh, based on the session, I see somebody turning maybe off that session. I can't have it on ElastiCache for that long if I can't have it Or is it >> Really good question. So the question is about like catching policy when this running like real world workloads when will we cache the work even the cache to the lower tier or when will we persist the cache? I think first of all this really depends on the workload like there are multiple different workload like like rag or Chad or agentic they they are sharing very

**[38:58](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=2338s)** different like read and write patterns. And I think by default is memcached provide very simple or straightforward caching policy which is LRU like when there is basically we evict the least frequent used caches and when the cache is hit from the lower tier we can bring it up to the to the to the hotter tier but we also support some configurations. For example, you can config that config the CPU memory just as a staging buffer where no cache is stored in the in the L1 but it's just as the staging buffer we load from your local disk. So this will be super useful when your CPU

**[39:46](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=2386s)** memory is also under pressure because you won't you don't want to use a super large CPU memory buffer. And sometimes we also want to just when we for example load from the disk or lower tier we do not want the KV caches to be persisted in CPU memory because in CPU memory there is usually a more useful KV cache for example the system prompt or the like more frequently used KV caches and we what we do is you can also enable a flag to just a don't store the caches uh into CPU memory where it's loaded from the lower tier. But these are the the things we have right now. Oh, there are actually more than what I just described. This is a subset of the things. But more importantly, you can actually

**[40:33](https://www.youtube.com/watch?v=ExXmZBlu8KA&t=2433s)** craft your own like logics and plug it into the M cache. So we have some interfaces called store policy and prefetch policy. Basically and also eviction policy. Basically it allows you to determine okay when storing to the to the lower tier, what kind of key caches I need to store and when prefetching from the lower tier, should I persist it into CPU memory or should I just discard it after reuse? So all those kind of things you can you can customize.

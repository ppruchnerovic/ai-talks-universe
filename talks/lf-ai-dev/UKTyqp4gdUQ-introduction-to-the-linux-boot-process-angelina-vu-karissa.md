---
id: UKTyqp4gdUQ
title: "Introduction To the Linux Boot Process - Angelina Vu & Karissa Sanchez, Microsoft"
slug: introduction-to-the-linux-boot-process-angelina-vu-karissa
conference: lf-ai-dev
conference_name: "AI_dev / Open Source Summit (Linux Foundation)"
category: "Software dev with AI tracks"
edition: "Open Source Summit + ELC NA 2026"
year: 2026
speakers: ["Angelina Vu", "Karissa Sanchez"]
channel: "The Linux Foundation"
duration_min: 37
published_at: 2026-06-03T18:15:11Z
video_id: UKTyqp4gdUQ
url: https://www.youtube.com/watch?v=UKTyqp4gdUQ
youtube_url: https://www.youtube.com/watch?v=UKTyqp4gdUQ
tags: []
topics: []
transcript: true
---

# Introduction To the Linux Boot Process - Angelina Vu & Karissa Sanchez, Microsoft

**Angelina Vu, Karissa Sanchez**

`AI_dev / Open Source Summit (Linux Foundation)` · `Open Source Summit + ELC NA 2026` · `2026` · `37 min`

[Watch the recording](https://www.youtube.com/watch?v=UKTyqp4gdUQ) · [Conference site](https://events.linuxfoundation.org/ai-dev-europe/)

## Description

Join us at the premier vendor-neutral open source conference, where developers and technologists come together to collaborate, share knowledge, and explore the latest innovations and advancements in open source technology. Learn more at https://events.linuxfoundation.org/

Introduction To the Linux Boot Process - Angelina Vu & Karissa Sanchez, Microsoft

From the moment you press the power button to the instant the login prompt appears, a complex sequence of events happens behind the scenes to get your Linux system up and running. This talk aims to demystify the Linux boot process through a deep dive into each stage.
Starting from the role of system firmware, we compare legacy BIOS with modern UEFI and see why there has been a shift to UEFI. From there, we move on to the bootloader stage, discussing its function in loading the kernel and passing control over to it. Using GRUB as an example, we show how to view and customize bootloader configurations. Next, we explore the kernel initialization stage, including the role of initrd/initramfs, how the real root filesystem is mounted, and how the kernel initializes essential system components and launches the first userspace process. From there, control transitions to the init system. We examine SysVinit and its more modern alternative, systemd, and their roles in bringing the rest of the system online.
Finally, we cover practical debugging techniques, such as viewing boot logs, analyzing boot performance, optimizing boot up time, and improving security with features like Secure Boot.

## Transcript

*5,664 words · source: supa (en, exact timings)*

**[0:00](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=0s)** Hello everyone. Thank you for coming to our talk today. Today we will be giving you an introduction to the Linux boot process and I'm very excited to see that seems like a lot of people are interested in learning about that today. [laughter] So I'm Carissa. I have been working at Microsoft for almost two years now and since starting at Microsoft I have been working in the Linux systems group where I work on Linux virtualization based security. Hi everyone. I'm Angelina and I've been at Microsoft for over three years now and I also work at on the Linux systems group of Carissa. So just to give a rundown of what we're doing with this talk or I guess a motivation. So in our workflow we're working on a custom kernel. So every day we're doing this multiple times where we or modify the

**[0:47](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=47s)** kernel, we rebuild, we reboot and then rinse and repeat. But when we do that whole process of rebooting, it works pretty smoothly and it's easy to take a lot of it for granted and just those like maybe 15 seconds or so between hitting the power on button and then actually seeing the login prompt. So today we wanted to share share what we have learned because we were curious to see well what actually happens in between and so we're going to show you what we have researched ourselves and basically take you from power on to firmware to the bootloader to kernel net then user space in it and then finally to the login prompt. Okay, so when you press your power on button your CPU is going What you're in the wrong be sorry. Oh.

**[1:35](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=95s)** You do it in presenter mode. Presenter mode? Oh. All right, sorry. So when you hit the power button your CPU it starts executing at a fixed address that's called the reset vector and the reset vector is mapped to your firmware. So the CPU will then read and execute that firmware boot code from ROM. And we're at the first phase of boot which is the firmware phase. This is where firmware manages the early hardware initializations including detecting and checking the hardware. The end goal of this phase is to set up your boot environment and hand control over to the boot loader. The firmware performs POST which stands for power-on self-test. This detects and initializes your CPU memory and your essential hardware components and then

**[2:23](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=143s)** it checks them. Things it does checking for CPU basic startup, RAM training, checking that the keyboard input is working and thermal checks and so on. And if any of the checks fail or you're missing components, then your boot will stop and sometimes these errors are reported as a beep code which if you've experienced that, they're really loud and annoying. Historically, the firmware was something called BIOS which stands for basic input output system. As name suggests, it's pretty simple but that also makes it more limited. For instance, BIOS doesn't have direct access to the boot loader. Instead, it's tied to the MBR which is the master boot record. The master boot record is

**[3:11](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=191s)** a disk boot structure and it's 512 bytes. The last 2 bytes of the MBR are the boot signature and this indicates that that sector contains bootable code. So that doesn't necessarily mean that you can boot with that code but just that the boot sector format is recognized. The MBR also has 4 16-byte partitions. So BIOS only supports four primary partitions. And that leaves 446 bytes which isn't quite enough to fit the boot loader code itself. So instead, this section contains bootstrap code and it kind of acts like a first stage boot loader that its job is to find and load the actual boot loader. Other limitations are that BIOS only has a maximum disk size of about 2.2 terabytes. It runs in 16-bit

**[4:00](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=240s)** real mode, so there's no memory protection. And it also only has access to 1 MB of memory. In comparison, UEFI is used in more modern day systems. That stands for Unified Extensible Firmware Interface. It um uses something called a GPT. I'll go over that in the next slide. So, it supports much larger disk sizes as well as many more partitions than BIOS. It's also faster because it has direct access to the boot loader um through accessing the ESP. And it's more secure because it supports something called secure boot, and Chris will go over that towards the end of this talk. It also uses a CRC, which is a checksum that makes sure your boot variables and boot loader and firmware

**[4:48](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=288s)** settings haven't been corrupted. And UEFI runs in 32-bit or 64-bit mode and has access to full RAM. Okay, so how does UEFI get you to your boot loader? So, in your non-volatile RAM, there are boot variables that define your boot entry. So, UEFI will first read those boot variables from your NVRAM to determine your boot order and your boot entry. So, you can see here the boot order first priority is 0002, and then if you go to that boot entry, it's the name of the Ubuntu, and then the part that follows that is the device path. So, where it says HD 15, that means uh it's on partition 15, and then you have the partition GUID. And at

**[5:38](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=338s)** the end, where it says shimx64.efi, that is your EFI file path. So, we'll come back to that file path a bit. So, after reading the boot and determining the boot entry to use, um UEFI reads your GPT, that's the GUID Partition Table. The first part of this uh table is the protective MBR, and this is used so that legacy tools that are expecting an MBR don't end up overwriting your GPT data. The GPT header contains metadata and a signature, that just shows that it's valid. And then you have the partition table with partition information like the type and the start and end addresses. And then there's a partition area

**[6:26](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=386s)** where your partitions themselves are, and this is the EFI system partition. And then the GPT header and partitioner are backed up uh for recovery. So, your firmware parses the GPT and it finds the matching partition GUID from the boot entry, and then it mounts the EFI system partition. The EFI system partition is a FAT32 formatted partition, so UEFI EFI knows how to read this. It stores your UEFI applications, your boot loader, diagnostic tools. Um in this example, you can see where my ESP is mounted on SDA15, or that SDA15, and you see it's mounted at /boot/efi.

**[7:15](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=435s)** And then when I expand that folder, you see the entry for Ubuntu, which is going to be our boot entry. And if you remember from before, the EFI um file path given was shimx64.efi, so I will run that executable, and that will take us to the boot loader. Okay, yes. And so, now we have reached the boot loader stage. So, the boot loader is responsible for locating and loading the kernel and then transferring execution to it. When we talk about the bootloader, probably the most common example that comes up is going to be GNU GRUB 2, where GRUB stands for Grand Unified Bootloader. And I would say it's a default because it has a lot of cool features and it's pretty flexible. So, one of those features is that it

**[8:03](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=483s)** supports multiple images to boot. Um so, when you look at the GRUB menu, you can see like many different entries and you can choose any of them. Um and then it's also not limited to Linux kernels. So, you could also boot Windows using GRUB just by doing like chain loading. So, instead of booting the Windows OS directly, you can boot like the Windows boot manager, which will then boot Windows OS. Um GRUB is pretty flexible because it supports UEFI and BIOS with MBR or GPT partitioning. Um and GRUB also comes with a command line. So, if your boot fails for some reason, maybe um one of the files it needed was corrupted or it could not be found, then GRUB has a command line that you can use to try to um look around in that pre-boot environment and scope out what happened.

**[8:52](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=532s)** Um And so, GRUB is also pretty flexible because it allows users to modify a few things using the file at c default grub. Um crucially, you should know that you cannot update the grub.cfg file itself. Um and that is what GRUB actually reads and interprets to determine what settings to apply. Um [snorts] So, instead, you need to write to sc default grub and then use update grub or grub-mkconfig depending on the system. Um and grub.cfg will let you know in all caps at the top, do not edit this file. So, you should listen to it. >> [laughter] >> Um But yeah, some things you can modify. You can modify the grub timeout style. Um you can

**[9:41](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=581s)** um also modify the amount of time that you're going to spend in the grub menu before it just chooses the default option. And you can also modify your kernel command line parameters. So, there are a lot of built-in command line parameters. As you can see, that's an example of a command line. Um and you have the parameters at the end. Uh there are quite a few like quiet splash that are already there, but custom ones can be added as well as long as you add the logic for parsing it and then actually behaving according to what that parameter says. Um so, now I want to show a demo of interacting with this file in grub and actually seeing those changes applied to the next boot. Okay, so here I have an Ubuntu VM on Hyper-V. And when I boot it,

**[10:30](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=630s)** we're going to see the logo, but not actually the grub menu, which is kind of a bummer cuz this slide is called grub demo. So, I'm going to put in like my username password login, and I'm going to open the file that I mentioned before, etc. default grub. And then we can see the different settings that we can change. Okay, so here we have grub timeout style is hidden, which is why we did not see the grub menu. So, I'm going to change that to menu. I'm going to set up the set the timeout to more than 0 seconds. And I also see that there's command line

**[11:19](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=679s)** parameter for app armor being set to 0. Um which I want to enable app armor, but for the sake of this demo, I disabled it right before just to show you guys how I can re-enable it. Um and yeah, I just want to check. Yeah, there are no logs showing that AppArmor was run. And if I go to my command line, yeah, it was definitely disabled. So, I am Oh, and then I need to update grub, of course. Okay, and then once I update grub, I want to check the grub.cs-cfg file to see that my changes were actually applied. So, I can open that file, go to one of the entries,

**[12:06](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=726s)** and see, yeah, that is the command line that I want, and we are no longer disabling AppArmor. And so, with that, I can reboot the system, and see that my changes are applied. And so, when I do this, there should be two changes I notice. The first being, I can actually see the grub menu this time, and there it is. That's wonderful. I can choose one image from the list of options. >> [snorts] >> And the second change that I will hopefully see is that once I finish this reboot and log into the system again, I can see that AppArmor was actually enabled, and hopefully the logs will demonstrate that. Okay, so, once again, I'm going to check

**[13:03](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=783s)** dmesg to see if we have any logs for AppArmor. And then, yeah, so we see AppArmor was initialized, file system enabled, hashing enabled. So, that's great. We can see that the changes that we just implemented on /etc/default/grub were applied. We can see we actually saw the grub menu and now app armor is enabled. Um and with that we have chosen a kernel and now start the next phase. Yeah so once grub is done it's noted to critical file items into RAM. One of them is decompress kernel image and then the other one is the initramfs archive. And the reason the kernel image is compressed is to save the space as

**[13:50](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=830s)** well as reduce the bootloader loading time. So we're in the kernel init phase now and this is where the kernel will decompress itself and do more early CPU memory and core kernel subsystems initializations and then it will prepare mount the root FS using initramfs. So we're still really early in the kernel boot process. So the kernel is in a very minimal environment. It will run some architecture specific code and this code is extremely low level. It's pure assembly and the purpose of this code is to just prepare the CPU enough to run C code. And this includes setting up the stack and clearing the dot BSS section as well as detecting CPU features, initializing early memory management, and then calling decompress

**[14:37](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=877s)** kernel. Um once decompress kernel is called the real kernel is decompressed into memory and will jump into the fully unpacked kernel. So start kernel is the first C kernel function. It's um in /init/main.c. This is where it does a bunch of architecture specific initializations and initializes all the core kernel subsystems including memory management, interrupts um exceptions schedules timers file systems, and the device and driver framework. And then at the end of that it unpacks the initramfs into RAM in this function called populate So, towards the end of

**[15:25](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=925s)** the start_kernel function, uh there's a call to kernel_init and kernel_init does um do_initcalls. And what this does is it runs all the registered kernel initialization functions in the staged order. So, you can see um they're kind of ordered by what by dependencies. So, you start with pure init calls and then core, postcore, or so on. And this is the part that does that brings up the subsystems, the drivers, uh file systems online before we can start running user space. And one of these init calls of those is rootfs_initcall and that is what our populate_rootfs function is registered at. So, once we reach that point in do_initcalls, we unpack the initramfs.

**[16:17](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=977s)** So, why do we need initramfs in the first place? Well, at this point the kernel doesn't have everything it needs to access the real root file system yet. For example, we need to be able to read files from the root partition, but to do that you need a file system driver module and that module is stored on the root partition. So, then you need to read the root partition and you're stuck in the cycle. Um initramfs solves that by serving as a tiny temporary RAM based root file system and it's used to finish bootstrapping the system. And the initramfs will launch your early user space, which is /init, that loads any necessary drivers and modules you need and then you're able to mount the real root file system. And then there's an operation called

**[17:04](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1024s)** switch_root, which takes you to the real root and starts your real init, which is normally /sbin/init. So, without initramfs, you would need all these drivers and modules to be built into the kernel, which is small space. You may have heard of an initrd, that is what was used before initramfs. It's initial ramdisk, it's a compressed file system image, which means it needs a built-in file system driver, making it more complicated than initramfs. Um because initramfs is a CPIO archive, which um the parsing and tailing of that is already built into the kernel, so it doesn't need a separate file system driver.

**[17:53](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1073s)** So, it's faster and more simple. Uh after initramfs work is done, we are at the point where we want to launch our first user space process, which will be PID 1. So, first the kernel will check for this kernel command line called init. And here you can set where you want your first user space process to be. And this is particularly beneficial uh for recovery because you can set this in GRUB during boot, and it will override whatever it was set to elsewhere. So, if you don't have this set, then the kernel will check the config_default_init, which is the kernel config where you can

**[18:39](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1119s)** also set your first user space process. And if not, it has some fallbacks. The first one being /sbin/init, and that will take you to your user space. Okay. And the init system marks the start of user space. So, the role of the init system is to initialize processes on the system and prepare prepare the environment to achieve the desired state. So, we're getting the operating system to actually be the usable system that we need. Um and so in doing so, it's going to configure things like networking, mount file system, hand off to login prompts prompts and do I'm sure much much more. Um and it is going to be the first process to begin on startup and the last to end on shutdown. Um since it manages all the processes, it is always going to

**[19:28](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1168s)** be there and all processes are going to be in some way a descendant of PID 1. So since processes have uh parent-child relationship, like any process, if you trace it back enough parents, you will get to PID 1, which is the init system. Um so, to go into some history, sysvinit was the primary init until it was replaced by systemd in the early 2010s. And now I will go into what those two things are. So, to start with sysvinit, um it is the early Linux default init used on a lot of main major systems. Um and it runs using the concept of run levels. So, run levels are essentially states that you can set your system to achieve. Um so, you have something like power off, single user, multi-user

**[20:16](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1216s)** non-graphical with no network either. Um you have multi-user non-graphical, so that's something for like servers to use. Um run level four can be user defined. Run level five is multi-user and graphical, so like a desktop setup. And six is reboot. Um And the way that these run levels would work is that they would all have a set of scripts attached to them. And so, when you told the system to achieve a certain run level, it would go through and sequentially run all those scripts that are attached to it. Um This, however, can take quite a bit of time. So, since it's all running sequentially, if one thing um if one process gets hung up or runs too slowly, then everything else is kind of waiting on that before it can proceed. Um and so, we want to or there was a need for a init system that could run

**[21:06](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1266s)** parallel processes to speed things up a bit. And that is where systemd comes into play. So, systemd is the modern mainstream init process. It has more capabilities than sysvinit and it's more dynamic. So, instead of using the concept of run levels, it bases its design on something called units. And so, units can have different types. So, we have sockets, service, path, target, and some more. Um and targets essentially replace uh sysvinit's concept of run levels. So, targets are going to have a set of units that it is waiting for in order to be achieved. Um and since systemd has this concept of or allows um

**[21:53](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1313s)** requirements to be declared in these unit files, then it has some awareness of what these dependencies and conflicts are. And as a result, it can run things in parallel. So, if process A does not rely on process B, then we can start them both at the same time. Um and then So, one main concern with systemd though is that it does a lot. Um so, one of the main criticisms is that there can be feature bloat um and maybe raises some security concerns. Um but I do want to give or actually just to address this image here. So, this is a screenshot from the boot logs and you can see like systemd starts and it starts um doing everything. Um so, here it's like creating slices, uh listening on sockets, reaching targets, and mounting file systems. So,

**[22:41](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1361s)** which is related to the different unit files. Um but I do want to give an example um of a systemd unit file. So, bringing things back to AppArmor. Um So, if I run systemctl status AppArmor, then I I see the status of the AppArmor service Um, and that is the output in the top image. You can see that it's active exited. It says or it gives a description of it. Um, it shows the unit file. Um, and it just gives a bunch of information including the logs. So, we can see how systemd made the first call and then afterwards the calls were made by the AppArmor system itself. So, 501 and then it spawned off some more processes and then once it finished systemd was back saying, "Okay, it finished." Um, something kind of

**[23:30](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1410s)** interesting is that we have active exited here and that corresponds to under the service header how it says remain after exit, yes. So, that means it's kind of it ran once, um, but we still want it to show up, um, for some reason. And then yeah, so under the service it also shows things like exec start. So, that's once we start AppArmor, um, what are we going to run? And so, it gives us a file path and a parameter. And if you actually follow that file path, you can see, yep, that um, program does exist there and it is going to parse the command reload, which then gets passed to rc.apparmor.functions and runs some logic.

**[24:18](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1458s)** Um, and here in the install you can see it's creating some sort of dependency. It's saying wanted by sysinit.target. Um, so yeah, that's an example of an AppArmor unit file and kind of or sorry, yeah, yeah, an AppArmor unit file in systemd, um, and just what it looks like. But once systemd, uh, or at some point when systemd is running, it will reach the graphical target, at which point you will see this lovely login screen. And so, with that you have reached the end of the Linux boot process. Um, hooray. And >> [laughter] >> now we are going to go into some bonus content that's not exactly the Linux boot process or like the stages of it, but still pretty related to it as well.

**[25:07](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1507s)** Okay. dmesg is a command you can use and it will show you the logs from the kernel ring buffer. Um this is particularly useful for debugging kernel panics. dmesg supports multiple log levels so you can filter it based on which log level you want. And I'll show that in an example. Okay, so I run dmesg. I'm going to scroll to the top. And and here you can see it shows you which Linux kernel version image version you're on as well as your kernel command parameters and pretty much all the prints from boot will show up here.

**[25:54](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1554s)** So, you can see has the memory and the info secure boot status which is disabled, which is not good. Um You can see NUMA info. And at this point we're bringing up This Oh. Okay, never mind. See a warning here or this is an error cuz it's in red. And if I keep scrolling down here we're bringing up secondary CPUs and then you get to this print that says slash init and that is where There it is. Okay, run slash init and at that point we're at the initramfs stage. And you see initramfs does more

**[26:45](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1605s)** driver registration. And if we scroll down a bit more you see your first systemd print. So, at that point we've reached the real user space. And if I want to filter these logs, so I don't have to see all of that, I can use {dash} O. So, I'm going to use {dash} O and give it error, so it only shows the error. So, there's that error from earlier. And if I also want warnings, then I can just add warn as well. The message is super useful. Okay. There's another demo. So, in this demo, um there's going to be a kernel panic and we're going to fail to boot.

**[27:34](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1654s)** So, I tried to boot into the 6.8.1 image and nothing prints and nothing loads. So, I'm going to just restart this VM. And this time I'm going to edit the grub boot entry at boot time. So, you do that by going to that pressing E. And here's the entry. We have quiet and splash as the command line parameters. Well, I'm going to remove that and add these other parameters so that we can see the prints. And then I boot into that kernel. And this time we have some logs coming through. Uh you can see at the end here you did

**[28:23](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1703s)** have a kernel panic. And if you look at the stack trace, it seems like we got to kernel init. So, that kind of gives you an idea of how far in the boot process you made it to. So, this time I'm going to reboot again. And you'll see in the grub entry that quiet and splash are back cuz when you edit it this way, it's only temporary and just for that boot. So, I you those again And add back the other parameters for logging. This time I'm also going to add an init call underscore uh debug. And this will give you a lot more prints uh related to the init call phase. You can see here you have more information. And at this point you finally want to connect to a console so you can actually scroll up

**[29:12](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1752s)** and see the rest of the messages. But from there you kind of get an idea of where you've made it in boot and hopefully you have a good image that you can boot into and you can start debugging from there. Um yeah, that's the end of that demo. That was just to show how to add a grub during boot time and how dmesg can be pretty useful. Uh we have some boot time optimizations and best practices. Uh if you let's look at that systemd-analyze, this would show you how long it took to boot. And it splits it between the kernel and the user space booting. And then if you add blame to the end of that, it will show

**[30:00](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1800s)** you which services are taking the most amount of time. So, we'd recommend disabling any services that you're not using. Just make sure you're not using it before you do that. Uh you can optimize the kernel command line so like before I had quiet and splash so it wasn't printing anything. Uh if you do want it to print, then that can slow down your boot. So, if you don't need prints, you can uh quiet the output. You should also um install your root and slash boot on a fast storage device so that would boot the load the kernel and the initramfs faster. And enable secure boot as well as password protect your grub. There are two ways you can do that. One of them protects uh from modifications of the grub entry and the other protects

**[30:48](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1848s)** users certain users from booting certain entries. Okay, and finally we're just going to very briefly cover secure boot. Um so, secure boot is something that ensures that the binaries that we loaded during boot, so like the boot loader and the kernel are actually something that we can trust. And it does this by establishing a chain of trust. Um And so, if we were to work backwards, so let's say now we are loading the loading the kernel. Well, how do we know that we can trust the kernel? Well, the kernel is signed by a UEFI key and that UEFI certificate is embedded within the shim loader and it is able to validate the signature on the kernel. But then, how can we trust the UF- UEFI

**[31:37](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1897s)** cert that exists within the shim loader? Well, the shim loader is signed by Microsoft and since x86 hardware comes pre-loaded with a Microsoft key, the firmware is going to trust anything that is signed with that key. And so, that is how we can trust the shim loader. Um and this is an assumption that we really do have to trust because at some point you do need to put your root of trust somewhere and yeah, so when So, since shim is a binary signed by Microsoft, um that UEFI cert which is embedded within it is going to be a trustworthy certificate for validating signatures of the boot loader and the kernel. Um and when secure boot is enabled, if you attempt to load binaries that are not validated, boot will fail. And with that, that is the end of our

**[32:28](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1948s)** talk. So, we thank you all for coming here today and we welcome your questions. >> [applause] >> Uh yes, you in the front. Me? Yeah well. >> [laughter] >> All right, so first, I I I've often noticed I EFI and UEFI being used like nearly interchangeably. Like for example, how the um ESP is the EFI system partition, whereas the former itself is the UEFI. What is the difference between those terms and why are they used in different places? I see. I don't know the answer to that. Does anyone know? >> [laughter] >> Um EFI was the original uh specification designed by Intel and others. And with

**[33:18](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=1998s)** the um now what we call the Itanic or Itanium. And when x86-64 became a thing, they adopted EFI as UEFI as the Universal Extensible Firmware Interface. So, it's just a super set of the original EFI. And then a Thank you. And then a second question, uh I So, the initramfs loads all the file system drivers. Uh where does it get those file system drivers from? Is that from the initramfs image that it loads up the boot partition or what? It doesn't. They I think they should be in the initramfs. Okay. Okay, in the doorway. I was hoping you would you you would touch on touch up on UEFI stubs and and

**[34:08](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=2048s)** possibly UKI Universal Kernel Images. Could you comment on either on both of them? Okay, I have not come across the term UKI before. Um Is that Essentially, it's it's the kernel and the initramfs image merged as a as a single bootable image. A single bootable executable, more or less. >> [laughter] [gasps] >> Yeah. we're we're still newbies here. You you mentioned um speeding up the boot process by removing certain processes or services you might not need. Could you give the give some examples of services people might pull out or remove um that that would speed up the boot

**[34:56](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=2096s)** process? Like services. Um Would would that be things like I don't know removing like uh you know, networking that you might not need or something like that? I'm just curious kind of specific things people might pull out. I'm not sure. I think it depends on what you're using your kernel for. Okay. I think on their screenshot they have plymouth in 4 seconds, which is a graphical uh renderer for grub, so you don't need the graphical stuff on boot. Okay. Thank you. Yeah, uh at the beginning you mentioned something called Hyper-V. Uh what exactly it is? Oh, that is a virtual

**[35:44](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=2144s)** machine manager. So, since this is a Windows machine, then I like have Hyper-V running on it and I can spin up virtual machines on it. Yeah. Uh you mentioned I don't know exactly remind me which file that you cannot change. Um is that a cannot or should not? I actually know from experience because I tried. >> [laughter] >> Um I yeah, very early on the job I saw the big caps do not edit this file and I was like, "I'mma edit the file." >> [laughter] >> And then I very quickly saw that my changes did not go through at all. So, what file is that again? It's uh boot/grub/grub.cfg. Yeah. So, it just won't let you. Um I think it gets rewritten Okay. during boot anyway. Okay. Yeah. I broke

**[36:34](https://www.youtube.com/watch?v=UKTyqp4gdUQ&t=2194s)** it on my machine editing before. Don't touch it. >> [laughter] >> Can you recap how you did the update? Did you You modified one file then you ran something? Yeah, so I modified like {slash} etc {slash} default {slash} grub. That's where you have all the configs for grub listed and then I run Since this is Ubuntu, I'm running update-grub and that will then take all the edits that I made and apply them to grub.cfg. So, I guess it it safely changes grub.cfg for you. Yeah, I guess that's it. Okay, thank you everyone. Thank you. >> [applause]

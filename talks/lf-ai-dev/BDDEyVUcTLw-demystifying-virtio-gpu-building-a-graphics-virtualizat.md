---
id: BDDEyVUcTLw
title: "Demystifying VirtIO-GPU: Building a Graphics Virtualizat... Yung-Tse Cheng & Sheng-Wen (Colin) Cheng"
slug: demystifying-virtio-gpu-building-a-graphics-virtualizat
conference: lf-ai-dev
conference_name: "AI_dev / Open Source Summit (Linux Foundation)"
category: "Software dev with AI tracks"
edition: "Open Source Summit + ELC NA 2026"
year: 2026
speakers: []
channel: "The Linux Foundation"
duration_min: 30
published_at: 2026-06-03T18:23:09Z
video_id: BDDEyVUcTLw
url: https://www.youtube.com/watch?v=BDDEyVUcTLw
youtube_url: https://www.youtube.com/watch?v=BDDEyVUcTLw
tags: []
topics: []
transcript: true
---

# Demystifying VirtIO-GPU: Building a Graphics Virtualizat... Yung-Tse Cheng & Sheng-Wen (Colin) Cheng

**Speaker not identified**

`AI_dev / Open Source Summit (Linux Foundation)` · `Open Source Summit + ELC NA 2026` · `2026` · `30 min`

[Watch the recording](https://www.youtube.com/watch?v=BDDEyVUcTLw) · [Conference site](https://events.linuxfoundation.org/ai-dev-europe/)

## Description

Join us at the premier vendor-neutral open source conference, where developers and technologists come together to collaborate, share knowledge, and explore the latest innovations and advancements in open source technology. Learn more at https://events.linuxfoundation.org/

Demystifying VirtIO-GPU: Building a Graphics Virtualization Bridge From Scratch - Yung-Tse Cheng, National Taiwan Normal University & Sheng-Wen (Colin) Cheng, The University of Texas at Austin

Additional Authors/Contributors: Jim Huang, Assistant Professor, National Cheng Kung University

VirtIO is the standard interface for device virtualization, enabling guest systems to access host resources and powering platforms such as QEMU and ACRN, which provide virtualized block, network, input, and graphics devices.

This talk takes an implementation-focused approach to virtio-gpu. We add virtio-gpu and virtio-input support to a minimalist RISC-V Linux emulator, building a graphics virtualization bridge from the guest framebuffer to host GPU acceleration. We examine the architectural decisions and trade-offs required to make the system function end to end.

Although VirtIO simplifies abstraction, virtio-gpu remains one of its most complex devices. Enabling 3D acceleration goes beyond the specification and requires coordination with Mesa 3D and virglrenderer, as well as compatibility with OpenGL and Vulkan. We highlight practical gaps between specification and implementation, including memory management, command submission, and synchronization.

Attendees will gain:
* A clear mental model of virtio-gpu architecture, including 2D and 3D paths
* Practical insights into integrating Mesa 3D and virglrenderer

Reference implementation: https://github.com/sysprog21/semu

## Transcript

*3,925 words · source: supa (en, exact timings)*

**[0:00](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=0s)** And hi everyone, my name is Chen and he is calling. Today I want to talk about Vertile GPU. Recently we implement Vertile GPU support for Samuel, which is small risk five virtual machine. And the 2D part is now close to complete while the 3D part is still in progress. And this is today's outline. I will first explain the motivation for this talk. Then introduce several ways to think about GPU virtualization. With that class- classification in place, we will briefly look back at a few attempts before Vertile. Then we will move into Vertile GPU itself. The main focus will be Vertile GPU 2D because it is a good way to understand the infrastructure.

**[0:49](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=49s)** At the end, we will talk about what changes in 3D and a few implementation details that are easy to miss. Okay, so let's start with the motivation and background. Before pre- preparing this talk, we noticed that there are already many excellent talks about Vertile GPU implementations. There are also talks dedicated to Mesa itself. Those implementations are obvious- obviously more complete than ours. So I was thinking about what this talk can aid. I later realized that most talk naturally focus on acceleration because the 3D part, more precisely Mesa and Vertile renderer, is more interesting and more difficult.

**[1:37](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=97s)** So for this talk, I want to go back to the basic infrastructure of Vertile GPU itself. The goal is to give you a big big picture so that if you want to implement Vertile GPU for your own virtual machine, you know what pieces are involved. I will also include a bit of history along the way. So, this talk is an overview for people who have not worked with virtual GPU before. If you have already worked on it, most of the material today will probably be familiar. I also summarize part of the several good talks in this presentation. The purpose is not to replace those talk, but to give you a pointer so that if you there is a topic sound interesting, you know where to start digging deeper.

**[2:26](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=146s)** One direct inspiration is Dave Airlie's talks about the virtual. But, I will not spend much time on the full history up front. Instead, I want to put the motivation from let's talk there. And then come back to virtual virtual and related direction when we reach the 3D part. Okay, so now let's first look at the main ways to provide a virtual GPU. For this part, I use Rob Clark's talk as a reference and add some extra background. I will include the link later, and this is not a strict classification, but it's useful enough as a mental model. To save time, I will summarize the four common direction with one table. The first one is device emulation.

**[3:16](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=196s)** Here, software fully emulates the behavior of particular GPU. This is more common in old hardware emulators. And the second one is API remoting. The guest doesn't directly control the physical GPU. Instead, it sends some graphics protocol to the host, and the host translate it into work that the host graphics stack can execute. And all virtual virtual IO GPU belongs to this category. The third one is a fixed pass-through. Where an entire physical GPU is assigned to one emulator. The guest loads the native GPU driver and uses the GPU driver itself. A typical example is QEMU KVM VFIO GPU pass-through.

**[4:04](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=244s)** The fourth one is media pass-through. This is the ideal approach. One physical GPU can be can be split across multiple guest while still providing um nearly native performance. But it is difficult to implement and it requires support from the hardware, firmware, driver, and platform. And the example includes NVIDIA vGPU and SR-IOV. Okay, with those categories in mind, the attempts before vGPU become easier to read. The goal here was not to pass an entire GPU through the guest. The goal was a reusable virtual GPU design that could fit into QEMU and bring up the Linux graphics stack. Before vGPU,

**[4:53](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=293s)** VMware, VirtualBox, and Wゲット them had already shown that the virtual GPUs were useful and each of them tried a different interface boundary. VirtualBox tried to use OpenGL as a boundary, but the problem is that OpenGL is large state machine with capability rules, extensions, and corner cases. If it becomes the device API between the guest and the host, the cost become very high. And the Wゲット them was closer to dev direct direction because get them was low enough to avoid the complex upper level OpenGL state, but high enough to avoid exposing the underlying hardware details directly. But it still required the host driver to expose a special get them interface, so it was not general enough. So the missing pieces was a reusable

**[5:42](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=342s)** QEMU-friendly design that could keep a get them like guest model without requiring uh, special host driver API. As long as the host side could connect to existing OpenGL stack, the Linux guest stack could be brought out first. Okay, next we move into VertIO GPU itself. VertIO GPU can be placed in the API or protocol remoting category, but more precisely, it's more like a virtual GPU transport. And because VertIO GPU 2D doesn't provide acceleration and uses software rendering, so its architecture is relatively simple. So, we will start from 2D to talk about the VertIO GPU architecture and comments.

**[6:30](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=390s)** At the end, I will use one example to walk through what happened when a GLX application starts. The examples later use XORG and mod mod stating with Mesa SWrast and DRI_SWrast reference configuration. The configuration is basically the default 2D setup. If you change the configuration, the behavior you observe may differ from the following examples. And VertIO device use virtqueues for communication between the host and guest. A mini A minimal VertIO GPU 2D device can be treated as a simple display device. It provides two virtqueues, control queue and cursor queue. The control queue carries the main 2D commands, while the cursor queue handle high frequency cursor updates.

**[7:21](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=441s)** The first command is get display info. At At startup, the guest needs to know which display output the host currently provides. For a single monitor, and this is usually one scanned out and one resolution. After the guest kernel driver receive this information, it can build a normal KMS mode setting state. And next, the device needs an object that it can display. So, this is the resource. The guest allocates a resource ID and ask the host device to create the corresponding resource object. In the 2D software path, mhm, you can think of this resource as an image. In a Xorg mode setting 2D scanout path, it's essentially the final pixel data

**[8:10](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=490s)** for the whole desk desktop. Uh, resource create 2D this command creates this resource. It carries the resource ID, format, width, and height. After this set up After this step, the device side has an image object that can be found by resource ID. And later, the transfer scanout flash and cursor path can all refer to the same resource. Once the resource exists, mhm, the guest can configure scanout. And with the set scanout commands, the guest tells uh, display output which rectangle of which resource to show. Okay, so at this point, we have a resource on the host device side, and we

**[9:00](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=540s)** know mhm, which we know which scanout should look at which resource. The next question is where the pixel data lives. And frame buffer memory in the guest is large, and it's usually split across many guest pages. We don't want every display update to describe the whole frame buffer memory again. So, for that reason, VirtIO GPU separates the relationship between a resource and guest pages into attach backing API. So, basically, the resource attach backing commands will attach a list of guest physical address and length entries to a resource. And these entries describe where the resource guest memory lives. The device remember this backing, so

**[9:48](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=588s)** later transfer commands can simply refer to the resource and the region to copy. And for 2D device, the command that actually moves pixels is transfer to host 2D. The guest produce pixels in its own memory, then uses a transfer command to tell the host that the content starting at this backing offset and covering this rectangle should be synchronized into the host resource. After receiving the command, the host device copies the specified region from guest backing into host side resource storage. And after the transfer, the guest uses resource flushed command to publish up publish the update.

**[10:36](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=636s)** And flush is meaningful for resources that are going to be displayed. The guest can first synchronize data into the host resource and then use flush to tell the display backend which region should become visible. Okay, so let's now summarize the basic command flow. And first, the get display info, that's the guest discover outputs. And resource create 2D creates an image object on the host device side. And resource attach backing connects guest memory to the resource. Transfer to host 2D synchronize pixels and settings set scanout chooses which output displays which resource. And resource flush publish the update to display backend.

**[11:25](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=685s)** Okay. Now, we put the 2D display infrastructure back into the XORG and Mesa path and look at how Vert IO GPU correct cooperate with the graphics stack when the GLX application renders and update the screen. So, before going into the application example, let's first look at which objects are prepared before rendering starts in this 2D software rendering path. This can help us locate Vert IO GPU inside the graphics stack. In this path, there are three main kind of objects. The XORG front buffer object and the window system object such as window, drawable, and pixmap. And the client-side current buffer managed by Mesa. So, the first one is the screen pixmap,

**[12:15](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=735s)** which is the storage XORG used to represent the contents of the whole screen. In the Vert IO GPU 2D double buffer path, the screen pixmap is backed by the front BO created by XORG mode setting. On the kernel side, there is a corresponding gem or GBM buffer object. The Vert IO GPU driver creates a 2D resource for it and on the host device and attach gets back into the resource. Then, XORG registered this BO as a DRM frame buffer that KMS KMS can use. The KMS plan and CRTC select its through and a frame buffer ID. Inside the Vert IO GPU driver, this scan out configuration is translated into set

**[13:03](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=783s)** scan out command. Okay, the second kind of object is the X11 drawable like window and pixmap. The window here is the window that the application creates inside the X server. A drawable is the X11 API abstraction that a client uses to specify a drawing target. A pixmap is a image object that on the X server side that can receive pixels. In this path, its backing is software memory allocated by the X server with calloc. This object describe which X11 target the pixels should be presented to and how the image should be clipped and which part of the screen storage should be finally update.

**[13:51](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=831s)** And the third object is the client-side color buffer. It stores the render results produced by the application. Mesa X SW and DRI SW allocate a software display target. Its backing is created by the DRI software being assist and it's also memory in user space. So, among these three kinds of object prepared before rendering, only the front buffer setup involves the virtual GPU DRM driver in this 2D path. It's mainly mainly because the front buffer is backed by the GBM. So, the other two don't go through the virtual GPU DRM driver. Okay. After these object are prepared, the application starts drawing a frame. We can simply simplify the whole rendering

**[14:41](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=881s)** flow into four actions. The first action is rendering. The application uses Mesa to render its into its own client-side color buffer. In this path, OpenGL draw draw calls are rasterized by the guest CPU. And then pixels first land into a software buffer on the application on and Mesa side. And the second action is present. The application needs to hand the render result to the window system. And when the application swaps, Mesa DRI SW sends the pixel back to the X server through PutImage style request. The third action is composition. After the X server received the pixels, it used the window position and clip region

**[15:29](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=929s)** to update the ready content into the screen pixmap and front BO. The core of this step is deciding which parts of the result are actually visible on the screen. The fourth action is scan out update. The front BO has already been wrapped as a DRM frame buffer and the KMS plan and CRTC use frame buffer ID to select it as a scan out resource. As after XORG hands the update region to DRM, the virtual GPU driver sends transfer to host 2D command and resource first command for the resource corresponding to the front BO. Okay. So, let's use a GLX application as an example and put our comments back

**[16:17](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=977s)** into the Linux graphics stack. This should This should make the path easier to understand. The first step is device probe after the VM boots. The Linux with virtual GPU driver creates the DRM device, KMS object, control queue, and cursor queue. It's then sends get display info commands to obtain the scan out information currently provided by the host. At this point, the host emulator already has the SDL window and display mode information and it's waiting for the guest to provide a scan out resource. The second step is display setup during start X. The XORG loads the most stating driver and create the front BO and cursor BO used by the X screen.

**[17:07](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=1027s)** In the virtual GPU 2D dump buff buffer path, the kernel creates a gem object for the front BO on the guest side and it also creates the corresponding 2D resource on the device side. Then, it will use the resource attached backing commands to attach guest pages to that resource. Then, Xorg wraps the front BO as a DRM frame buffer. The KMS primary plane and CRTC select it through frame buffer ID. Inside a virtual GPU driver, this KMS scanout configuration is translated into set scanout command. After this set step, the host X window knows which resource to look at. Later frames are usually update the

**[17:55](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=1075s)** contents of this resource, while the scanout configuration stays the same. The third step is application rendering. The application creates an X window, a GLX drawable, and a GL context. Then, Mesa GLX uses DRI SW and the driver side use the SW raster. So, the actual rasterizer runs on the guest CPU. And OpenGL draw calls are converted into pixels and first written into the client side color buffer managed by Mesa. And the last step is client present and display display update. When the application calls swap, Mesa DRI SW sends the pixel from the client side buffer back to the X server. This becomes a pixel upload such as

**[18:44](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=1124s)** XPutImage function. After the X server receives the pixels, it uses the window position and clip region to update the visible region into the screen pixmap and front BO. Finally, Xorg mode setting hands the dirty region to DRM. The front buffer object corresponds to the K KMS frame buffer and frame buffer ID. And it is the scanout source of the primary plan. After the virtual GPU driver receive the dirty update, it send transfer to host 2D to synchronize dirty pixel from guest backing to host resource. Then sent resource flush command to notify the display backend to publish the update.

**[19:31](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=1171s)** So, after the host is emulator receives those command, it update the host side image storage and the SDL window shows the next frame from the application. Okay. So, this summary puts all the operation together. So, for this 2D cases, the application's rendering operation doesn't use the virtual GPU at all. It's handled entirely by software rendering. So, basically it's all in the guest and guest the CPU. And the parts that use virtual GPU are concentrated in the display pipeline. Okay. Next, I will use two issues we ran into during implementation as example. The problem is not the virtual GPU commands themselves are complicated. The problem is that the commands do not

**[20:20](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=1220s)** fully mirror the state in Linux DRM and the emulator display backend. So, the first issue is related to the cursor alpha. The issue The issue give us a simple simple way to see the relationship between Linux DRM object and the host 2D resource. And in this diagram, the left side is the host 2D resource. While the right side shows the guest DRM frame buffer and virtual GPU objects. The problem is that when 2D resource is created, the DRM frame buffer frame buffer has not been created yet. So, the resource format is fixed to XRGB. Later, even if the upper DRM frame buffer correctly describe the cursor as ARGB. The format stored on the host side

**[21:10](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=1270s)** is still XRGB. As a result, the host interprets the cursor pixels using an XRGB format and it ignores the alpha value. So, what you can see is a black square behind the cursor. Yeah. And the QEMU such solution is very very straight straightforward. The QEMU cursor structure doesn't have a format field at all. Yeah. And then in the SDL2 backend, the cursor format is hardcoded through the ARGB channel mask. Instead of using the XRGB format carried by the host resource. Okay, the second issue is related to stride. For a similar reason, the image stride is stored in DRM frame

**[21:59](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=1319s)** buffer and in the host 2D resource. The problem is that the 2D commands doesn't carry it a stride parameter. So, this means that when implement the host side, we need to check the Linux kernel implementation to know what stride value the guest is using. So, in our example, the value is width multiple four. Calculated when the double double buffer is created. So, when the host create the 2D resource, even though it doesn't receive the value from the command, it's implicitly used the used the width multiplied four as well. So, these two examples shows the same point. Vert GPU 2D commands are small transport protocol. They do not fully reproduce every state

**[22:48](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=1368s)** carried by the Linux DRM. So, when implement it, you often cannot rely on the command structures and definitions. You also need to look into the implementation implementation detail in Mesa and the Windows system, and uh Linux kernel. So, this is a relationship diagram between a virtio-gpu 2D resource and KMS related objects. And I will only want to show this very briefly here, and not going to expand this part. Okay, next, let me hand off to Colin. Hello. Finally, we would like to comment on virtio-gpu 3D and uh some supplementary topics. Also, at the end, we will show some

**[23:36](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=1416s)** demos quickly. In this page, we summarize the virtio-gpu 2D and 3D into two diagrams as showing on the screen. For virtio-gpu 2D, the guest rendering the graphics on its own memory. And the host only receives pixels that the guest has already computed. In contrast of the virtio-gpu 3D, the host no longer receives pico pixels. It receives graphics commands or operations for rendering on the real hardware. Inside the guest, the Mesa virgl driver processes operations such as draw, clear, and state changes into command stream, and then send it to the host through virtio-gpu.

**[24:25](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=1465s)** The host renderer back end such as virgl renderer then offload the work to the host GPU. Here, we want to bring back some term terminologies that we haven't mentioned clear earlier. Nowadays, we refer virgl general as a general idea of virt virtual 3D GPU virtualization. But in some cases, virgl also implies rendering via OpenGL. While Venus targets Vulkan. In this speech, we introduce VirGL, i.e. OpenGL, for the historical reason and as initial attempt. However, it is also worth noting that Mesa 26.1 announced that VirGL is no longer considered maintained.

**[25:15](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=1515s)** If no new coming maintainer takes it over long-term, the related code may gradually become abandoned. However, the idea already extended to other graphics stack like Vulcan. Venus, a standard that is able to serialize Vulcan works, serves as a new backend in VirGL renderer. Also, DRM native context or VDRM move the boundary further down toward the native driver protocol. By using a device-specific protocol, it can have a thinner host component and fewer high-frequency guest-host round trips. Also, it is also worth noting that Rob Clark had a very good talk about this. After showing the difference between 2D

**[26:07](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=1567s)** past and the 3D, it is clear that if we were writing an emulator from scratch, we would not start with VirtIO GPU 3D. We would first ensure the VirtIO GPU 2D is stable. This involves feature negotiation, queue setup, this prefetch chain parsing, used ring handling, interrupts, and all essential steps that needed to be made. The second step is to implement minimal 2D display, starting with the basic 2D commands. At this point, if the guest should the guest should be able to bring up KMS and X11 apps. The third step is then obvious to add VirtIO input for mouse and keyboard for human interaction human interface

**[26:55](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=1615s)** interaction. Only after all that we think it will be meaningful to move into the vertio and 3D acceleration pass. During testing, what important is not only check whether something appears on the screen since the guest may fall back to software rendering and still showing an image. We want to ensure that the vertio GPU is actually running with 3D mode. At minimum, checking rendering that's actually being selected is also crucial which can be done by using GLX info {dash} B command. At the end of this talk, we have three demos to present. The first demo is direct FB2 which is built upon frame buffer instead of DRM

**[27:44](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=1664s)** making it not able to be accelerated. So, the demo will appear not to be quite smooth. Nevertheless, we can still see the mouse and the keyboard are working validly input vertio input. So, the mouse is dragging the window. And we now drag the green box and we'll press R button to rotate the window. Next, the second demo is KMS cube. On the left side, we have KMS cube running on

**[28:31](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=1711s)** an unaccelerated vertio GPU 2D environment. And on the right side, we have the one running in the 3D accelerated environment. So, actually the left side is not static. It is just too slow. Similarly, the third demo we have X11 running in 2D environment on the left hand side and the accelerated 3D on the right side. We can clearly see that the difference after introducing VertiLit with 3D for hardware acceleration. And this is all for our today's share. Thank you for all your listening and we will be appreciate to hear your feedback. Thank you. >> [applause] >> Is there any questions?

**[29:31](https://www.youtube.com/watch?v=BDDEyVUcTLw&t=1771s)** Okay, thank you. Feel free to reach us later later. Thanks.

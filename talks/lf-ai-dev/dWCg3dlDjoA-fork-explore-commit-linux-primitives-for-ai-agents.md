---
id: dWCg3dlDjoA
title: "Fork, Explore, Commit: Linux Primitives for AI Agents Exploration - Cong Wang & Yusheng Zheng"
slug: fork-explore-commit-linux-primitives-for-ai-agents
conference: lf-ai-dev
conference_name: "AI_dev / Open Source Summit (Linux Foundation)"
category: "Software dev with AI tracks"
edition: "Open Source Summit + ELC NA 2026"
year: 2026
speakers: []
channel: "The Linux Foundation"
duration_min: 42
published_at: 2026-06-03T18:19:28Z
video_id: dWCg3dlDjoA
youtube_url: https://www.youtube.com/watch?v=dWCg3dlDjoA
tags: []
transcript: true
---

# Fork, Explore, Commit: Linux Primitives for AI Agents Exploration - Cong Wang & Yusheng Zheng

**Speaker not identified**

`AI_dev / Open Source Summit (Linux Foundation)` · `Open Source Summit + ELC NA 2026` · `2026` · `42 min`

[Watch the recording](https://www.youtube.com/watch?v=dWCg3dlDjoA) · [Conference site](https://events.linuxfoundation.org/ai-dev-europe/)

## Description

Join us at the premier vendor-neutral open source conference, where developers and technologists come together to collaborate, share knowledge, and explore the latest innovations and advancements in open source technology. Learn more at https://events.linuxfoundation.org/

Fork, Explore, Commit: Linux Primitives for AI Agents Exploration - Cong Wang, Multikernel Technologies & Yusheng Zheng, eunomia-bpf

AI agents don’t execute a single path, they explore many. They try different code changes, commands, or configurations in parallel, then keep the one that works. Today, running this safely on Linux requires fragile combinations of temp directories, git tricks, containers, or VM snapshots.

This talk introduces branch contexts, a new Linux execution model built for AI agents. A branch context gives each exploration path an isolated, copy-on-write filesystem and coordinated process group, with a simple lifecycle: fork, explore, commit or abort. The first successful branch commits atomically; all others are automatically invalidated.

We present two Linux implementations: BranchFS, a FUSE-based branching filesystem with O(1) branch creation and atomic commit without root privileges, and branch(), a proposed Linux syscall that composes filesystem branching, namespaces, and process lifecycle management into a single atomic operation.

We’ll explain why existing Linux mechanisms fall short for agent workloads and how fork/explore/commit semantics fit naturally into the future of Linux process and filesystem design.

## Transcript

*5,321 words · source: supa (en, exact timings)*

**[0:00](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=0s)** Uh, my name is Kuang, and uh, I also have a co-speaker, Yushan. He is not able to travel here. However, he recorded uh, a video for his part. So, we will present this together in this way. So, thanks for coming to our session. So, today we are going to uh, uh, present our work, which we already uh, uh, submitted to the workshop at ASPLOS conference earlier this year. So, this is the the work based on our own paper. So, in this talk, we are going to introduce a new Linux some new Linux primitive for the AI agent exploration. We call it a fork,

**[0:50](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=50s)** explore and commit. So, in in this talk, we are going to first uh, uh, uh, introduce the back the background about the AI agent exploration. And then, we will introduce the problem of the current AI agent exploration. And then, we will define some requirement for the to solve the problem. And we will also present our design and solution and also implementation to solve the agent exploration work as well. So, this is the agenda of the talk today. So, and because Yushan is not

**[1:38](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=98s)** here, he recorded the video for the first part. So, I'm going to play his video here. Hi, I'm Yushan. So, let me start with what I mean by AI agents here. For this top air agents controlling loop including reasoning with the LM and with in a local workspace with tool calls and the agent will observe the results and do a and repeat the process in a loop. We have seen many agents like Alpha Codex and I'm sure I think people are very familiar with this concept. So here

**[2:29](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=149s)** we are mainly targeting one pattern that motivate rest of all talks. Air agents are trying to study to try several passes at the same times in the to solve one problem. For example, they can try three candidate bug fix on the same report and commit on only the one which can pass all the tests. And this is like trying to do parallel software works. You can also do like job source is exploring a chill of recent different reasoning passes. You can You can do a IR rows in parallel. You can also do in speculative on execution which means study start likely passes earlier.

**[3:19](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=199s)** And does not need to wait for LM to response. Here, in linear system, we can see agents are processed with effects. You can see from S trees agent looks like a developer and it can run a bunch of commands and has bunch of sys calls. Uh however, it's uh this agents can produce unpredictable side effects. They can create dirty keys and install packages. but I first which is the OS only see the as the normal units work well but aliens intense invisible. Before we look at the city making bomb

**[4:15](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=255s)** some let me try to see what this alien one times or sandbox requirements. First you need to isolate parallel execution and this is core concept of sandbox. You also need to have hierarchical nesting. You need to have parents and children's for experimenting different variants. You need to coverage the full file systems, capture all the modification or just check files to ensure you have consistency. You the then you also want a sandbox to be lightweight and privileged and portable. In so it just doesn't require root privilege.

**[5:03](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=303s)** In multi-agent systems you you may want different agents to coordinate together for some specific tasks but here no existing Linux mechanism can satisfy all these requirements. Let's walk through while this happens. So current approaches for the file systems including like you can just copy all your work space. You can also like using Git or like Git work space. You can try to use Docker containers. You can try to like your trend root or by months which is part of the dark darker features.

**[5:53](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=353s)** But, they all have problems, like you can You need to create a lot of duplicate files. It's heavy weight and require privilege. Or But, we actually What we actually need is like one name space agent living. It has multiple branch of it. It has nested branch for recursive exploration, but that don't require like root access. Um Uh let me see. Is there any Is this a mechanism Linux gives us today? And where we can build on this existing mechanism? And why it's not enough? For example, we have file system branches in Linux system, like overlay

**[6:41](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=401s)** FS or BTRFS. However, tools like overlay FS require pseudo mounts, and like you cannot commit change change back. There's no automatic clear for losing branches. And also, nesting is composition easy to to break. For a BTRFS, it's another solution, but it requires like specific file system. It also cannot commit changes back. And a lot of existing file system formatted didn't support that. So, we have a lot of file system that can come closer, but mix committing mix certain features, like committing

**[7:28](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=448s)** changes back, like discarding loose branches. We also have process isolations in Linux. So, therefore, isolate a bunch of process like we can use this process to build sandbox space down low enough. For example, we have PID namespace. We But it requires it has already have we have no namespace, but still we have Cgroup Cgroup we have clone three. We have set PID. But it's still like limit required access or is is capable. So, we have a lot of right ingredients.

**[8:17](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=497s)** Kernel is process, but there is no one operation that can combine them safely. When we say safely is important because composition of them is not straightforward. If you want to make one branch in user space, you you need to have a lot a lot of steps like mounting and share clone, move from PID into Cgroup, install a lot of things. Each step can fail. Each step between each layer has a race window. Making clear reliable as overhead. For example, if uh there is a fork between different different patterns, the fork grandchild can establish Cgroup. Reliable clear up

**[9:06](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=546s)** means like uh PID babysitter or a single atomic primitive. So, So, what we are keeping finding is that every single OS primitive does one thing well, but ideally is exploration of them composed autom- autom- atomically. Like, you require first on branch and more news namespace PID namespace. Each step in user space? I mean, if you are just putting space in use space, it means you have a race window and maybe you can create like safety or security risks. So this is similar to what clone exists

**[9:57](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=597s)** in Linux today. Before clone, stress you can you can build stress from forks and shared memory and signals, but Linux and clone because their composition need to be atomic. Uh we are trying to introduce the similar concept for agents. Branch context make the same argument for file system states and process isolation. Including a global process. So the missing pieces here is not a single sandbox, it's the OS level mechanism. Branch. We have a lot of piece of mechanism issues, but Linux lack one operation they can combine all of them safely. Uh so as you showed and demonstrated

**[10:51](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=651s)** in uh first part, actually Linux operating system already provide a very rich primitive for different ingredients we need here. However, as he mentioned, there is no one single primitive could combine them together to provide the atomic operation. So because of that, actually we want to propose our solution. So we define a new concept called a branch context. So this this concept is a combination of the copy-on-write file system uh combined with a combined process group. So, together we put it we name it branch context. So, there are four

**[11:40](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=700s)** properties in this concept. The first one is the before the parent process needed begin to branch children process, the the parent process needed to become read only because otherwise the parent would race with the children process. The second property is parallel isolated execution. So, for each of the branch, each of the children processes they needed to be completely isolated. And they their file operation their file operation their file system view needed to be isolated as well. And the third

**[12:27](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=747s)** property we define here is the first commit no one will win. So, why we define this property is because we don't want to deal with the conflict. Without this property, we would need to deal with merge between different branches like the how the Git conflicts handled in a really complicated way. This could This is not an easy problem to solve. So, this is why we decided not to solve this problem at all. So, instead we define a new property here called first commit wins. >> [snorts]

**[13:12](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=792s)** >> And the last property here is the branch context must be nestable, which means if we have a if we already have a branch, that branch could also uh, continue to branch in a natural way as well. And the diagram on the right side could give you a very, uh, high-level picture of the, uh, design of our solution. So, on the top you could see there there is a the parent process could branch into uh, different uh, children processes. And each of the children processes you could actually, uh, have isolated, uh, file system branch review and they could actually, uh operate

**[14:01](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=841s)** yeah, isolated way uh, uh, in the file system as well. >> [snorts] >> And at the bottom we could see there is some file system layer solution which could provide the isolated branched, uh, views for each of the, uh, process. So, uh, the first part let's take a look at the easy part. So, the the file system part actually is relatively easy because for the file system we could actually realize the file system with a user space file system instead of, you know, writing a lot of, uh, Linux kernel code. So, we implemented we realized and implemented this idea, uh, with the, uh, with a file system in the user space. We call this, uh, file system branch FS.

**[14:49](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=889s)** So, this this project actually provides the, uh, copy on write branch with a virtual, uh, pass over the, uh, any, uh, on top of any, uh, existing, uh, directory. And the first, because this is, uh, copy on write, so that means the first write to the file will copy the file will copy the entire file into the branch there data, uh, area. And for the commitment the uh, it is guaranteed to be a atomic so there is no risk condition on the file system operations. And for the two aboard the branches the cost rate is

**[15:37](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=937s)** also relatively cheap because all what we need is just to delete the data we created we created for the for each of the branch. And what an important each of the branches is also could be nested. So inside each of the branch we could also create more branches as well. And this entire file system does not require root privilege unless you of course you want to use on top of any privileged directory that's different story. >> [snorts] >> And it could also run on any Linux file system and in fact it also support Mac OS recently as well. And on the right side I do have some very quick command line demo to show you

**[16:27](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=987s)** how this work. So to start to get us started we need to use the branch FS command to mount a directory to our work space and [snorts] then we need to create a branch on top of the mount point. And the the branch actually is also each of the branches is has a virtual path to in the file system so you could actually locate the branch with the virtual file system path as well. So and once you enter that virtual path you could actually do the file operation at pretty much as

**[17:17](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1037s)** the entire file system the entire directory is owned by your owned by yourself. And after all the operation is done, you could actually commit all the work to the file system and all the operations will be committed back to the disk. So you would not lose any data. And specifically for the this is designed specifically for the branch and agent. So each of the agent actually they should use each of the branch or the virtual file path. And to get the isolated view actually each of the agent they should actually use the bind mount use the bind mount to mount this virtual

**[18:07](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1087s)** file path to their own mount name space so that they would not see other branches. >> [snorts] >> And the inside this project the most important part is the copy on write because this is how we save the storage because otherwise we would needed we would waste a lot of storage space to implement this. And this copy on write is implemented on top of the user file. So it's different from the kernel file system like butter FS because butter FS pretty much implements the block level copy on write. So here we implement this

**[18:56](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1136s)** in the user space. So it's the file level. So that means each when each of the file get modified it's the entire file file get copied into the data directory of each uh And if the file is not modified, you know, everything will be uh passed through to the base directory. >> [snorts] >> And uh fortunately, we believe this could uh uh uh work for the AI agent because typically, especially for the coding uh uh uh AI agent, they typically just touch some uh uh uh source repositories, not a a lot of files. And the the the the source source file typically is not very uh huge. And with that, actually, I

**[19:46](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1186s)** think this is uh suitable for the AI agents, especially the coding agent, to use. >> [snorts] >> And uh we also did some uh performance benchmarking as well. So, for the 1 MB copy, it's about 200 microseconds. So, it's still not a of a very big deal compared with the real bottleneck of the AI agent, which is the RM call. >> [snorts] >> And uh because we we support the branch and also actually uh nested branch. So, that means the branch resolution is not a uh an easy task for this uh project. So,

**[20:35](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1235s)** for example, when we look up the file, actually, we needed to take the branch hierarchy into uh consideration as well. For example, in this case, we want to look up whether for example, uh uh to uh to read the file uh uh this source file from the branch called uh B3. So, first, we actually needed to uh check the the uh uh inner branch B3. And then, we walk up the uh, branch hierarchy until we reach the, uh, the, the, the main branch or the base branch. And once we hit the, uh, the base branch or the base directory, we needed to check whether that a file is, uh, uh, uh, still exists there or otherwise may it could be a newly, uh, created file. Or if it's, uh,

**[21:25](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1285s)** or in any of the step, it it it it if it is sees any, uh, tombstone in one of the, uh, uh, uh, branch, it means you know, that branch already, uh, uh, completely deleted this file. That means that means we should return some error for, uh, this this, uh, uh, system call. >> [snorts] >> And the, the tombstone actually is, uh, uh, uh, encoded in the, uh, virtual file path in each of the branch virtual, uh, uh, uh, directory as well. So, this is how we implement the, uh, file operation together with the, uh, uh branch uh hierarchy. And the commandment is also becomes complicated because of the, uh, uh, branch, uh, hierarchy. And we needed to

**[22:15](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1335s)** collect all the modi- uh, modified files, uh, from the, uh, inner branch and also collect the, uh, uh, tombstone. And then we needed to be, uh, uh to uh, carefully apply the, uh, the file operation orders in a very, uh, uh, dedicated uh, uh, sequence to make the whole command, uh, uh, commandment, uh, uh, uh, work with on top of the, uh, branch, uh, uh, uh, file system. And, uh, the, the, the abort, uh, operation actually is relatively easy because all what we need is pretty much just to delete the, uh, uh, third, uh, directory in each of the branch. There is much nothing we

**[23:03](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1383s)** needed to do. And the cost of the abort is also very cheap because that's literally just the cost of the directory deletion. And we also actually performed some performance benchmark to measure each of the operation, especially related to commit and the abort, which which are the right operations supported by this operating system. And the branch creation is for the branch creation, the the performance is excellent. So, it does not actually increase that much when we even when we increase the number

**[23:52](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1432s)** of files in the directory. And for the commit commit and abort, the commit actually is a little bit of complicated because it's highly related to the size of the modified file. So, when like I mentioned, so when the file size grow to 1 megabytes, actually the commit means overhead, the the operation time actually becomes like for the abort, actually it stays in microseconds level, just increased slightly with when the size increased. >> [snorts] >> And we also measured the regular file system read

**[24:40](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1480s)** throughput for this file system as well. So, the baseline is native EX4 file system, we could actually read like 8 gigabits per per second. And the uh the uh branch FS actually because this is built on top of the file system in user space, so the over the the overhead actually is pretty large. So without optimization, we could only ask actually get less than about 20% of the throughput compared with the native. However, there is an optimization provided by Linux kernel called pass-through. So with pass-through actually we got very good performance. We are almost we already achieved more

**[25:31](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1531s)** than 80% of the native throughput with this pass-through. However, there is some problem with this pass-through optimization. First it actually is relatively new, so it's only available since the kernel 6.9. And also actually there is another disadvantage is for some reason this this optimization requires privilege. So if you want to use this actually this privilege requirement is enforced by the kernel is not that our project really wants this privilege. So actually we don't want this one and we don't even understand why we require why it requires privilege unless you know of course the the directory itself is privileged.

**[26:21](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1581s)** So this is the limitation of this optimization. And because of that actually as we can see the file system side actually is relatively easy and the complicated part as you showed actually is the process part. So we want to confine the process. We want to branch. We want to isolate each of the children process. So that's the hard part. So although Linux already provided different primitives, we could actually in some some way could combine them, but like you said mentioned, there's no atomic guarantee between all the operations. So, with that actually we believe

**[27:09](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1629s)** it is necessary to introduce a new system call to realize this uh uh branch semantics. And this this system call actually could guarantee the all the atomics and could save the all the implementation uh hassles from the user space. So, what we follow file system, we already have all the semantics, all the requirements ready for uh in the file system layer. However, with the the last piece of that is still missing is the file system

**[28:00](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1680s)** part of still need the coordination with the process. It's not that only the file system part is sufficient. The process is execution also matter. So, they they together actually needed to uh uh make the whole solution complete. So, this is what we believe is the missing piece and is the connection between the file system and the process or the execution. So, this is what we what we believe the kernel needs to do to solve all the exist all the problems of the existing primitive primitive. The first thing is the atomic. So, everything must be atomic. Otherwise, we needed to deal with the race condition in the user

**[28:47](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1727s)** space in a very painful way. And the memory branching actually this part is impossible uh in the user space. This part has to be in kernel. I don't see any uh feasible way to implement this in the user space. And of course there is some work around for this because for example if we could save all the memory into the file system. Actually we don't need this frankly speaking. However, sometimes it's not a practical because sometimes it's expensive to save everything you know from memory dump everything from memory to the file system. So we might want to you know avoid this expensive operation. And the the reliability for the process termination you know of course we have C group. We also have uh the PID name

**[29:35](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1775s)** space available but none of them is atomic. >> [snorts] >> And because of that actually we want to propose the a new system call called branch system call to the Linux kernel community. And with this system call we hope it could solve all the problems and all the missing parts we required you know specifically for this agentic exploration scenario. And this is the design and the high level interface of this system call. We pretty much get inspired by the BPF system call. Because BPF system call is a very very flexible and we want to make this new system call also as flexible as possible because otherwise

**[30:23](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1823s)** in the future we we could we want to extend this in the future without breaking all the existing APIs. And we provide three core operations you know like pretty much like the file system rights. We want to create a branch you know, for bio both file system and the process. We want to also implement the command uh the command and the abortion for both file system and the process as well. And on top of that, we also provide some uh more uh granularity to control each of the operation. For example, we might want to the user might want to to opt in, for example, the memory uh a branching if they need. So, that's why

**[31:12](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1872s)** we provide additional flags on top of the operation as well, you know, for user to control what they uh want with their own agent. >> [snorts] >> And this is how the system call is supposed to use. So, it's a little bit uh uh uh complicated because of this design, and we needed to wrap up a lot of different things, especially related to the uh file system. And for the file system, we needed to uh first we needed to get the mount point, and we assumed the the underlying file system uh uh a file system uh behind the uh uh mount point already supported the branching operation. Uh for example, if you you use a branch FS, that's already

**[32:00](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1920s)** available. And with that, uh the this uh system call will work together with the underlying uh uh file system to uh perform all the uh uh operations supported by uh the And this is the interface between the uh this system call and the uh uh file operation. So, this is how the process and how this uh system call got connected, for example, with the uh branch FS. Because this system call does not actually intentionally does not want to directly handle the uh file system operation. So, uh so indirectly it's just a call some file system operations through the IO control. And with these three

**[32:48](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=1968s)** different IO control, this is how it interact with the for example the branch FS file system. So, that means that if you want to implement a new file system to support this branch system call, that means you also needed to provide these three core IO controls in your file system as well. And with that actually on the right side let's use the create as an example to show you know how this system call works together with the file system operation. So, this file system this system call will invoke the VFS layer operations the IO control and it will pass the

**[33:35](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=2015s)** IO control command to the underlying file system which is supposed to be available in the file system behind the mounting point. And for the kernel part actually we also implemented the working prototype in the Linux kernel as well. However, it's not based on the latest kernel so Yushan created a patch and all the work based on the 6.17 kernel and the currently there are three patches available and we only implemented the very minimum prototype. For example, we did not actually implement the

**[34:26](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=2066s)** memory branching support which is the most complete part yet. However, with the rest part is pretty much ready. And you should actually also release this in his repository. You know, feel free to check out this repo and there is here are also some instructions for you to build the kernel if you are interested in this part. And this is a benchmark mark performance by Yuchen. So, for the for this system call the the this system call is pretty cheap because all the operation pretty much like could have been done within about like tens or hundreds of microseconds. So, compare with the real bottleneck of AI

**[35:16](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=2116s)** agents the which is the arm call, this is pretty much like a a [snorts] very like a fraction of the bottleneck. And on top of the last part actually is is another project because even if we have the the system call, we have the file system ready. It It is still not easy to use. So, as we can see from the the the the example uses of the the system call, it's not easy to directly use by the developers. And because of that actually we implement a in the user space new library in Python for developers to use. So, the the AI agent developers, they don't need to deal with any, you

**[36:05](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=2165s)** know, Linux system call or Linux file system. They just want to actually implement their high-level logic. They just want to try something in parallel and probably like, for example, pick the better solution among all the parallel solutions that AI agent tries. So, because and for that actually we implemented the a very high-level library in Python called Branch Context. And in this project actually we realized the seven agentic exploration patterns for the developers to use. So, we wrap up everything and so that is the developers will not not needed to worry about you know all the Linux related stuff. For example, one of

**[36:54](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=2214s)** the uh uh um one of the most useful patterns is the battle of. So, in this pattern actually we intentionally to run several solutions in parallel. And we we let the developer to implement each of their own logic in each of the branch. And we also let the developer to implement their own evaluation for each of the branch so that they could decide they could score each of the solution. For example, they might pick the the one with the better performance just for a quick a quick example. And with that actually >> [snorts] >> this library deal with all the Linux all

**[37:43](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=2263s)** the low-level stuff under under under hood. For example, there is not even as a branch not even any branch directly exposed to the developers. And you don't need to worry about you know the the system call, the the the the process group. Everything is transparent to the developers. And we also have some other speculative and also some hierarchical patterns like tree of thoughts which is popular for the uh, AI agent as well. So, the example on the right side actually demonstrated how this could be used on the very high levels. As you can see, there is no Linux

**[38:33](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=2313s)** uh, operating system detail at all. And I also have a very quick demo available. Actually, it's in the uh, GitHub GitHub repository as well. So, you can actually use a command line to play with this as well. For this one, actually, we just released with three different solutions and find the best one. Find the the the first one passed all the tests. So, this is a very simple scenario and everything is the successful uh, solution is committed committed uh, uh, automatically and everything is handled by the library. And this is pretty much what we have today. We have a file system, we have the branch content library for the

**[39:22](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=2362s)** high-level developers, and we also have a prototype for the Linux system call as well. And this is pretty much what we have for today. And this is our instructions if you are interested in, you know, trying our project. This also has the link for our original paper published on the archive as well. So, feel free to uh, check out. And this is the repository we have and everything is open source. So, if you have any question, you know, feel free to reach out to me or you send directly. Uh, that's all we have. Thank you for joining. >> [applause] >> Yes, yes.

**[40:10](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=2410s)** Uh, I have two kind of connected questions. Yeah. Um, you mentioned Well, my first overall question, I'm not really an agent the person, but some of this sounds like it would be useful for people too. Like it would be useful for regular human developers too. I had wondered if you thought about that. And then the other thing was one of the things you mentioned was package installation, but it doesn't sound like this would quite be applicable for like whole system access for an agent. Yep. Oh, yeah, yeah, yeah. Thank you. So, you you have two questions. So, the first question is this one could be helpful for humans, too. So, this is very accurate. Actually, when the we do actually mention it, but I actually skipped this slide. So, when we have one

**[41:01](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=2461s)** we only have one when we only have one branch to try. Actually, this this this work will turn into a try and roll back primitive, which could be helpful for the CACD, for example. You want to try some fix and realize that oh, something go wrong. Just go back. So, yes, yes, this is also helpful for for human. Yes. And for your second question, you know, for the package installation, that could be literally the entire file system. So, that's also true. So, with that actually I do hope the branch IFS could be run on top of the entire file system to provide the the the the the the branching support. Thank you. You have a great question. Thank you.

**[41:52](https://www.youtube.com/watch?v=dWCg3dlDjoA&t=2512s)** Any other question? Okay, thank you. Thank you. Thanks for joining. >> [applause]

---
id: vM_Gbx-c2Qs
title: "Building the Simplest Possible Linux System - Rob Landley, Hobbyist"
slug: building-the-simplest-possible-linux-system-rob-landley
conference: lf-ai-dev
conference_name: "AI_dev / Open Source Summit (Linux Foundation)"
category: "Software dev with AI tracks"
edition: "Open Source Summit + ELC NA 2026"
year: 2026
speakers: ["Rob Landley"]
channel: "The Linux Foundation"
duration_min: 53
published_at: 2026-06-03T18:14:22Z
video_id: vM_Gbx-c2Qs
youtube_url: https://www.youtube.com/watch?v=vM_Gbx-c2Qs
tags: []
transcript: true
---

# Building the Simplest Possible Linux System - Rob Landley, Hobbyist

**Rob Landley**

`AI_dev / Open Source Summit (Linux Foundation)` · `Open Source Summit + ELC NA 2026` · `2026` · `53 min`

[Watch the recording](https://www.youtube.com/watch?v=vM_Gbx-c2Qs) · [Conference site](https://events.linuxfoundation.org/ai-dev-europe/)

## Description

Join us at the premier vendor-neutral open source conference, where developers and technologists come together to collaborate, share knowledge, and explore the latest innovations and advancements in open source technology. Learn more at https://events.linuxfoundation.org/

Building the Simplest Possible Linux System - Rob Landley, Hobbyist

Once you've done enough of them, putting together a simple Linux system is easy. The hard part is working out what "simple" means in each new context.

We'll start by building and booting an example minimal Linux system to a shell prompt, first under QEMU and again on real hardware. Then we'll explain the theory: build environment (dependency management, reproducibility), native vs cross compiling (toolchain selection, libc selection, static vs dynamic), board bringup theory, kernel configuration, initramfs creation (and other root filesystem options), installing and booting, the init process and system bringup, hardware resource management and I/O categories, and running "your app" on its own dedicated device.

If there's time we'll go into software dependencies AGAIN (on target this time), add an example server (sshd), add a native toolchain to compile "hello world" on the target (build vs development environment), and some perspective on the online book "Linux From Scratch" for further reading (plus "what is a container".

## Transcript

*7,982 words · source: supa (en, exact timings)*

**[0:00](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=0s)** Hi, my name's Rob Landley. I agonized over whether to give you the how or why versions of this talk and given the tiny amount of time, I'm going to be doing the how version with why explanations sprinkled in because I want to demonstrate to you something that I got to work. So that we won't be missing, you know, here's a large pothole that you will not be able to cross because it's very simple to get to the other side, but you're doing it completely blind. So the the big problem with board bring up on a new piece of hardware is until you have output from it, you don't know what's wrong. You have no visibility into the system until it actually works. So, um Oh, I have

**[0:50](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=50s)** way too many. Where is my Okay. So, um few quotes at the at the start, you know, Dijkstra's quote quote about simplicity, uh couple of them and uh Saint-Exupéry is the author of The Little Prince. I had as my email sig for years, "Perfection is achieved not when there's nothing more to add, but when there's nothing left to take away." Uh and here are I will be uploading this and sending them in of here are all the tabs that I have open that I'm mostly going to be walking you through, you know, here's here's real world stuff, primary sources out there. What I promised in the in the description is that I would build and boot an example minimal Linux system

**[1:38](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=98s)** to a shell prompt under QEMU and on real hardware. We're not going to have time on real hardware, but I can I can show you an image. Um and then explaining the theory of all these things. We'll We'll go to those and then stretch goals, which unfortunately in 40 minutes we won't have time for. the thing. But fundamentally, building and booting the tiniest system, I spent many years working on. I I first encountered Tom's root boot back in the '90s where, you know, a a floppy disk booted Linux to a shell prompt and seemed like a pretty good system. And then the first time I built the Linux from scratch project, which was build 25 or so GNU packages to assemble a standard Linux system, the

**[2:26](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=146s)** result was 110 megabytes and I went, "What?" And the trick Tom's root boot did is was based on busybox and uClibc, so I started going, "Let me take Linux from scratch and replace packages with the busybox implementations of those things." And the first time I tried it, just, you know, put busybox in the path in front of the standard path and try to build Linux from scratch, awk seg faulted, sed hung, it just it it didn't get it out get out of binutils autoconf. And it's like, "Okay." And I started fixing stuff. And over the next few years I fixed so much stuff that the busybox maintainer handed the project over to me. >> [laughter] >> My my mistake is when I cut a release because he hadn't cut a release in a

**[3:15](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=195s)** while, so I went, "Here's a release candidate." And it's like, "Don't do that." You will wind up inheriting the project. It's like, or at least accidentally forking it. Um my first attempt at making this work I called firmware Linux, which is a name that was profoundly ungoogleable. Which as I mentioned down in here, and yeah, it's mirrored up at that URL. I'll give you the URLs later. But I managed to replace 22 GNU packages um with the busybox equivalents. Um, both by testing and by fixing a BusyBox. What I did is in addition to having a BusyBox directory at the start of the path

**[4:04](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=244s)** that you know, it would run the BusyBox command before finding any of the other commands, I created a command interposer that would look for the second instance of the command in the path. You know, it would run first, it would print out the name it was called under and all of its arguments. And then it would call the second instance in the path it would execute to call the real one, but it would have appended to a log what got called. And the first directory was just every command in the path simlinked to that one executable. So, I had a log of here's everything that's actually being called. And I could see, you know, what failed. And

**[4:50](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=290s)** after several years of that, I started over with Aboriginal Linux. The uh in 2005, the big jump there was the discovery of QEMU because Firmware Linux was based on User-Mode Linux, which only ever worked on x86. They never ported it to any other architecture. I don't know what the hardware dependencies were, but it was Linux compiled as a normal elf executable that would then run processes as threads. So, you could you could actually stick printfs into the kernel and see how the system call handler worked because it was just a normal process. QEMU let me boot not just x86, but ARM and SPARC and MIPS, so I got into cross-compiling.

**[5:39](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=339s)** And there's a lot of documentation on here because I document compulsively that I don't have time to get into, but I did that for for several years and then end of life did in 2017. The third one I did and and I wrote up a history of, you know, why I did all that stuff that is also one of the things that, you know, we're not going to have time for. The when I left BusyBox, I started a new version ToyBox for reasons we again don't have to get time into, but make root is a is a system builder I made in a subdirectory of ToyBox, which is a 400-line bash script that

**[6:26](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=386s)** boots Linux to a shell prompt and uh is that Yeah, that's the current one. It that thing that I just booted uh that just did here. I just built the uh system 390 uh 60 S390X is the 64-bit version of IBM zSeries mainframe stuff. I just built that as, you know, to have one to play with here. The run QEMU instance uh the run QEMU script it makes is basically snapshot what directory the command we were called under is in in case we're called out of the directory, but we need to tell it where the kernel is and where the root file system is. QEMU system S390X 256 MB of memory pass through any other arguments that

**[7:15](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=435s)** were on the command line, run it in no graphic, no reboot mode, tell it its kernel is this file. Um tell it that it's initrd, which is the old The difference between init ramdisk and initial RAM disk is that RAM disks were a fixed block of memory that you then formatted as EXT2 or something and it acted as a block device with a file system in it. An init ramdisk is an archive that's extracted into the disk cache. So, you know, as long as you have the page cache and everything anyway caching the contents of the files, Linus just went, "We can mount that as a file system." And so it it dynamically expands and contracts rather than being a fixed-size block of memory. It's a lot more convenient. But the old initrd loader is here is a

**[8:05](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=485s)** blob to extract into that block device. Well, if the signature at the start of the blob says that we're a CPIO or Gzip CPIO image, it will extract it into the thing. It's the same loader. So, it's the initrd loader but being used for initrd ramfs. So, here's where your root file system is. And then dash append is QEMU speak for here is the kernel command line. On most architectures, you're not actually appending. You this is the whole command line and you can't specify it twice because of course if it's called append, you can only have there can be only one. Lot of legacy stuff in here. So, the important one is that I have to tell it its console is on the serial device cuz that's almost never the default.

**[8:55](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=535s)** And then if I if I have an environment variable k args, it'll pass it through so I can go, you know, k args equals init or rdinit equals bin sh to tell the initrd ramfs to run a different init. And then just run QEMU and it would inherit that. Or because of this dollar sign asterisk thing, I can go run QEMU HDA {dot} {dot} {dot} {dot} readme and it will boot up a virtual mainframe. You know, cat etc. Um CPU info. Uh no, proc CPU info. And it goes, "Look, I think I'm an S390 and I have because I told it {dash} HDA, it

**[9:44](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=584s)** attached that readme file. Uh let me head dev VDA. It attached that readme file to the virtual block device, you know? So, it's it's reasonably flexible, and then when I exit, it will shut down the emulator. Since this is only a 400-line script, I'm going to try to walk you through it really fast. So, you go, "Here's what it actually does." You don't have to use this script. Learn from it how to do your own things. And then if there's time left, which I really hope there will be, I can get into more of the why stuff, which I think is important, but the problem is there's at least 8 hours of why stuff, and we don't have time. Okay. So, at the start it it's in three parts. Part one is

**[10:34](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=634s)** throat clearing. Part two is create a root file system. And also as part of the root file system, it will build toybox, uh my toybox project, to populate the bin directory. You can also use uh busybox. You could theoretically do this approach with the GNU tools, but why? And then part three is building a kernel. And the fiddle This supports a bunch of different architectures. Uh make root make root cross equals help. These are the cross compilers I currently have installed that I built with a different script

**[11:23](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=683s)** that I'll briefly link to. Uh there's cross and native compilers in there because what my Aboriginal Linux project did, remember how it was trying to replace all the Linux from scratch packages with uh busybox, but still be able to build it could rebuild itself under itself and it could build Linux from scratch under the result. So, my regression test was configure and build all the Linux from scratch packages in the standard Linux from scratch build. And along the way I made busybox good enough to do that, and projects like Alpine Linux took that and ran with it off in their in their own direction. So, they can be busybox based because of years of work of extending the busybox commands to actually be

**[12:11](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=731s)** properly load bearing. And I'm trying to do that with make root, but you know, ever since I turned 50, I don't have the caffeine tolerance I used to have anymore. I need way more sleep. So, there's a question of how much I'll get done on that, and there's a new law in California that if you put up operating system images and they don't do the age verification thing, you get a $7,500 fine per download in the state of California. So, I'm probably not putting OS images up after the 1st anymore because I just I that I would say there's no reason that there's no possible way that would have survived a court challenge, except

**[12:59](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=779s)** that the Roberts Court is still in charge of the Supreme Court, and I can't predict what they'll do. So, eh But anyway, just in terms of making it work, that's why I'm focusing on the how rather than the why, because some of the why has actually gotten kind of sad in the past few years. Don't get me started on vibe coded rust. It's a bad thing. Just no. All right. So, what you basically do is you run make root make root from the toybox directory cross equals whichever target you want to do and the list I had um name one. Call one out. PowerPC PowerPC 32 or 64-bit or 64-bit little endian?

**[13:48](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=828s)** 64-bit endian 64-bit little endian PowerPC 64LE power Okay, and then I have to tell it where the Linux source is. So, Linux Linux and presumably that will take about a little over 9 minutes on this laptop. This laptop was very very high-end in 2012. I have upgraded it as far as it will go. Half the time I SSH to a real machine to actually, you know, there's a there's a couple of special targets. There's an uh there's an all non-stop target. I can go cross equals all that will try to cross compile all those targets it listed, but it will stop at the first failure and all non-stop will just build them all and there's a there's a log

**[14:37](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=877s)** file uh under build uh sorry, under root build log Uh you'll notice it it it has some support for building targets in parallel, but I I didn't quite make that go all the way through. But, um the build log, so uh PowerPC LE uh .n is it is teeing the output of the build to this file. So, uh cat you know, tail -f you can watch new things show up in it. We Uh but, the other one that's in there is Remember how I said I had that wrapper that told you every command the build was running?

**[15:26](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=926s)** It's set up by default here. So, it's just, you know, tail {dash} f. That file, well, here's all the commands that are actually being run behind the scenes, including all of their arguments with quotes and everything. Um which is useful if I'm, you know, if something goes weird and I'm debugging okay, what happened? Because All right, let me go through Let me go through the 400 lines of the script. Um so, there's the setup, there's the creation of the root file system, which includes building toybox, and then there's building and packaging the kernel for QEMU, or depending on the config it um I've got a couple in here. I've um I I have one that builds for uh the turtle board that the This is not a Raspberry Pi. The brain of this thing is an FPGA. It's Raspberry 2

**[16:16](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=976s)** Pi B form uh 2 Pi B form factor, but you load a bitstream onto the FPGA and all the IO devices of a standard Raspberry Pi 2 B are wired into FPGA pins. So, that your VHDL or Verilog is implementing your HDMI device and so on. And and your CPU. It's uh it's fun stuff and you know, I do well, I did development on that. Uh let's just say that the most recent administration has had foreign companies less enthusiastic about having American developers in their supply chains. Uh complications. Anyway. So, this this script the first thing it does is I have had many people over the

**[17:06](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1026s)** years run my stuff and break it in all the most fascinating ways. Uh the U-Boot main the old U-Boot maintainer Wolfgang Denk, who unfortunately died a few years ago, um the first time he ran Aboriginal Linux, he had environment variables set I had never seen before and stuff broke in just the strangest ways. So, I had an environment variable white list in Aboriginal Linux. And what I do this time is I just exec env -i run myself under it and manually set a few environment variables plus this no clear thing makes it so that it that's not an endless loop. So, this is environment variable sanitation in two lines. Now, note, small and simple are not the same thing. And a simple result does not mean it's immediately obvious what it's doing until it's explained. So,

**[17:57](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1077s)** on the Y side, the the the opening line is define simple. Because unfortunately, simple can mean a lot of things. Um, and then if you're not running it in the toybox source directory, it goes, you know, sanity test. Um, this has package support. There's There's documentation on on how to use it. Um, I have way too many of these things open. Um, I'll worry about that. Worry about that in a bit. But, it has package support. I- if you list a keyword equals value on the command line, it will set it as an environment variable. Otherwise, it will add it to a list of packages to try to build out of the packages subdirectory of makeroot,

**[18:47](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1127s)** which is basically just source this script, you know, run this other script so it can do things like, you know, build dropbear, which is um, okay, there's a plumbing thing in there that if you build any I- if you don't build any packages, none of this stuff gets pulled in. The those 400 lines are completely self-contained. That was a design goal. But, if you do build packages, the first thing it sources is this plumbing thing, so you get a download function that will W get your file for you, but it will also have an SHA1 sum to make sure that the file downloaded properly, and it will keep the old one. It won't download it again as long as you have one that matches that SHA1 sum, and it does it in this many lines of bash. So, you know, normal. Um setup four is basically tar extract

**[19:36](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1176s)** and CD into the directory, except it remembers what you did so that you can later call clean up and it will rm-rf the right directory without you having to say which one. And that's just all the plumbing thing. And then, you know, so download from this URL, expect that hash. Download from that URL, expect that hash. Setup for zlib, here is how you how you build zlib. This one is a little weird because Dropbear wants zlib as a component. So, if I want Dropbear to have zlib support, I build zlib for the target first. Remember, this is potentially cross-compiling. So, I I way too much stuff. You you can read this if you're interested. It it actually it writes it appends to the etc shadow

**[20:24](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1224s)** file on the image to give you a root user with password root and a guest user with no password because if you're SSHing in, it kind of matters. And uh here is a file that it adds to the host output next to that run QEMU thing so that you can call this and it will dial into your VM image, and then this gets added to that QEMU to that run QEMU invocation, which says, um take this port on loopback of the virtual system and forward it to a port on loopback of the host system so that on the host, I can SSH to that port on loopback and it will actually go into the VM. You know, just

**[21:11](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1271s)** normal straightforward things. Because it was useful to me. Anyway, so you can add packages. You don't have to. And one of the packages is called dynamic that once upon a time hasn't been regression tested in forever instead of static linking it would copy the dynamic libraries on there and set a flag so that it did dynamic linking instead. There was one that tried to substitute busybox in for toybox and I hit the fact that busybox had had a regression where if you don't have the bzip2 compressor in the host uh path the busybox that's no longer optional for busybox. It if you have bzip2 compression enabled in busybox the help text must be compressed with bzip2.

**[22:01](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1321s)** And you can't really easily chop that out. You know, fun stuff. Anyway, um as I said, this is this is throat clearing most of this conceptually can be skipped and you don't really care so I'll try to breeze through it. Um so that is is grabbing the any extra environment variables I want to specify on the command line or package names. This is colon is a synonym for the true command. And this is the bash syntax to say resolve this environment variable but if it doesn't have a value set assign a default value to it. So this will actually write to a variable if the variable is not already set. Meaning this is a very compressed way of saying if top isn't set assign top to to dollar sign

**[22:50](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1370s)** PDBD/root. If build isn't set assign build to top/build. And so on. So this is set this is just assigning default values for six variables. It's very simple once you understand it. If nobody's explained it to you, it's not simple. So again, simple is is a non-simple concept. Um dumb little command that uh you'll notice how this uh well, it it you'll notice how this says powerpc64le-linux-powerpc. This is the magic escape sequence, the zero the the octal 33 and the octal 007 to change the title bar of the current terminal window. So when I say announce, it change the title bar of the current window and then

**[23:38](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1418s)** it prints uh triple equal sign and that same thing so it goes into the log as well. And then die is a command that will just spit its arguments to standard out to standard error and then exit one. So if I call die, there was no, you know, you specified a cross compiler and I couldn't find the cross compile-cc. You know, it you specified a cross compiler doesn't there. Um it it will both exit with an error and give me an error message just real simple. So if you set cross compile as a prefix to where a cross compiler lives or just a cross compile, you know, it can be a a path or it can be just a cross compiler prefix out of dollar sign path, then it will set cross from it, which is the short name that is used in hey.

**[24:27](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1467s)** Um you know, like that is that is the short name for that target, that is the short name for that target. So it can it can figure out the short name from a cross compiler prefix. Basically, it just chops chops up the first dash and everything after the first dash. That's what the short name is. Um and if you set the short name, then it will look in the ccc subdirectory, which is a symlink to that directory with lots of cross compilers in it. And again, it does the, you know, here is, you know, prefix asterisk cross, these are your available cross compilers. So, all of that is basically just find the cross compiler and then set some more default values based on what target we're building for and then

**[25:16](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1516s)** just do a really quick smoke test of can you build a statically linked binary and if no, by default we're building statically linked binaries and on some systems like Red Hat which Ulrich Drepper used to work for and he hated cross compiler, sorry, he hated static linking conceptually. So, he broke it in glibc as many ways as he could while maintaining glibc and also convinced Red Hat that all the static libraries should not be installed by default. They should be extra packages you have to go out of your way to install. You know, on a real operating system, a Debian-based one or, you know, Alpine or basically not not pointy hair Linux, um, you get the static linking support by default, but I have had to check, is it there? Because this build tries to be portable and it will break

**[26:05](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1565s)** much less expressively later on if you don't do that. So, and then we do what Google likes to call a hermetic build environment and I was calling it airlock for years and years. Um, Linux from Scratch, like the 3.0 days, had chapter five and chapter six. Chapter five created a forward {slash} tools directory that it populated with temporary builds of packages. And then chapter six was change root into that where you have an empty directory except for forward the forward {slash} tools directory that everything lives in and then run all the package builds again to populate the normal locations and when you're done, delete forward {slash} tools and this would act to prevent host stuff from contaminating

**[26:55](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1615s)** the target. You would go through an airlock that would seal, and then you would go from there into the final thing. And I thought that was a really, really, really good idea, so I stole it. And so what I was doing in Aboriginal Linux is chapter five is busybox. At busybox plus I needed to grab a few things from the host because I need a compiler, I need an assembler. Busybox didn't have a make, so I needed gmake. There were There was a set of binaries and um uh I I I carefully open tabs and now I can't find them. So, what I'm going to do is go back here

**[27:46](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1666s)** and go to scripts. There There's a slight cheat here in that we're asking toybox to set up an airlock. It has a make airlock target, which means it's install script down around here knows how to set up an airlock. Specifically, you do a make install flat, so instead of having sub you know, bin and sbin subdirectories, you should install all the all the symlinks in the same directory alongside the toybox binary. Busybox can do this too, it always could. And then I need to grab these symlinks out of the host, plus these two that the Linux kernel needs to build, but I have patches that remove that. But if you're building without my patches, then you also need

**[28:33](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1713s)** these two. And then um busybox isn't at 1.0 yet, so I need some commands that um I either need to enable them in that they're not in defconfig, that I have half-finished versions that work for some of this, or just grab them out of the host. And right now, by default, it's just grabbing them out of the host. So, these are the extra commands that Toybox doesn't provide. I believe BusyBox does provide all of them now, uh except except it still hasn't got a make. Um So, basically, we make uh we make install airlock, and then we set the path to be just that new directory.

**[29:20](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1760s)** So, if you run autoconf, if you add an extra package to like build Python or something, and it checks to see if you have Pearl, or it checks to you know, it won't find it out of the path. So, when you're cross-compiling, this this was a big section of the Y material, configure, make, make install, all three are wrong. Autoconf is useless can be sung to every sperm is sacred from Monty Python. Autoconf asks questions about the host system, and uses those answers for the target system. So, more than half of cross-compiling is lying to autoconf.

**[30:10](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1810s)** The other part is when you are cross-compiling, you have two sets of headers, two sets of libraries, two different kinds of object file binaries, and other problems. So, there's only ever been a thousand or so packages that cross-compile, and there are tens of thousands of packages in a distro like Debian. So, what Aboriginal Linux was trying to do was we build the a minimal native development environment, the smallest, simplest system that could rebuild itself under itself from source code, we would then fire it up inside the emulator and build Linux from scratch under that, which I got building for a dozen different architectures, you know, including ones I don't support anymore like Spark, because when I moved from uClibc to musl libc, musl doesn't

**[30:58](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1858s)** have Spark support. Um and the trick to making that feasible, especially back when QEMU was single-threaded, was I'd already set up a cross-compiler in order to do that much. So, I built distcc for the target and I configured it so that distcc was running on host's loopback and inside the emulator, um 10.0.2.2 is the magic QEMU address to say connect to the host's loopback. So, I would have distcc call the cross-compiler not knowing it was a cross-compiler. And the way distcc works is it runs the file It runs the C file through the preprocessor to get one self-contained blob. It then sends that to the remote system to be turned into a .o file, takes the

**[31:47](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1907s)** .o file back locally, writes it down, and eventually it will link it. Meaning, even though I was using the cross-compiler and I I could get about J3 before running the preprocessor repeatedly under the emulator, you know, and marshaling data through the virtual network card where the the thing I could get some level of SMP build, but as far as the build was concerned, it was entirely native cuz there was only one set of headers it had to deal with. There was only one set of libraries it had to deal with. If autoconf ever asked the question, it got the right answer because it was all running under the emulator. And that's why I could build the whole of Linux from scratch in a host-agnostic way. I just build for whatever you are. None of my Linux from scratch builds ever were configured to know your arm,

**[32:38](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=1958s)** your MIPS, your PowerPC. They just built I tried to get Gentoo to do that and it was fundamentally every single ebuild is tagged with every single architecture that that ebuild supports. So, if you add a new I I was working a contract for Qualcomm years and years ago where we were we were porting Linux to the hexagon for the first time and I naively suggested that Gentoo, since it builds everything from source, would be a great, you know, way to do that. And we would have had to touch every single ebuild in the entire tree to add hexagon and what they wound up doing is convincing Gentoo that hexagon was a variant of 386. Because it was crazy to do it and anyway, tangent. [snorts]

**[33:27](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2007s)** So, this is the hermetic build. Now, notice these large blocks of code all start with if this variable isn't set, then do all this stuff. If you uh if you do not set cross compile and you do not set cross, it will build for the host. And down here it will go cross equals host and put it in a directory called host. If if here if it will only do this if no airlock isn't zero, zero bytes. So, if I set no airlock equals one on the command line, that gets skipped. Most of this stuff is optional and can drop out. Um so, basically do a make dir to create the output directory we're going to be doing into. Um this sets up that logging wrapper. Um I

**[34:16](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2056s)** can explain if you're interested later, but we don't have time. And then you know how you can do exec redirects? Um this is an exec redirect to the special bash syntax that says this looks like a This looks like a file argument, but it's actually going to be {slash} dev {slash} file handle for we ran a child process and did a FIFO to it. So, the output is actually redirected to T that is going to be writing it to a log and forwarding along to standard out from now on. So again small, simple and immediately understandable are adjacent.

**[35:04](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2104s)** So, and then it says building for cross compile. Okay, so that that was all throat clearing. It's I explained that so you wouldn't be so confused by it, but in your own builds you almost certainly don't have to care. Now we create the root file system. We start with a big make dir {dash} p. Are you familiar with bash curly bracket syntax? Where where this turns into um This expands to user {slash} bin space user {slash} sbin space user {slash} lib except it's nested. So, all of these are paths down under where we're creating the new root file system. It just is one big make dir {dash} p to create all those directories. Then it change mods some of them because the temp directory needs the sticky bit. Um and it creates three symlinks so that user bin is a symlink to bin at the top

**[35:54](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2154s)** level rather than having a separate bin and user bin. Yes, that was my fault. I explained on busybox where that came from in the first place. The the the split between bin and user bin happened because Ken and Dennis ran out of space on their first PDP-11 and leaked to the operating system into their second mount point. And then when they got a third disk, they plugged it in and called it {slash} home and moved all the user directories to there and let the original user directory location {slash} usr be more operating system space. And then over the years people went, "Oh no, no, this is deep. You know, five-dimensional chess that they were playing." And it's like, "No, it's grandma's roast. She cut off the end of the roast cuz it didn't fit in the pan." And you won't find out unless you ask her. It doesn't make it

**[36:42](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2202s)** taste better. It's just history. Anyway, I I feel bad that the systemd people picked that up and went, "This is a good idea." And I'm going, "Yes, it's a good idea but the fact that you're endorsing it makes me reconsider." Anyway, the next thing it does is it writes an init script. Because when you launch a kernel, it's it's going to need to mount a root file system, and it is going to need an executable out of there that it can launch as PID 1. And by default, it will do /init. So, there are two executable file formats that we feed into the kernel config. And we'll get to the kernel config later. Um that

**[37:30](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2250s)** there's the script kind with the pound sign exclamation point path to interpreter as the first line, and there's elf, good old elf. Except for the no emu targets where it's uh it's fd pick instead of elf. But fd pick is just it's elf with four base pointers instead of one base pointer. It's 95% the same thing as elf. But anyway, um because I am giving it a pound sign exclamation point bin bin sh, and then at the end here I'm setting the executable bit, chmod -x, it can run this as PID 1 even though it's a shell script. So, what this is going to do is it's going to export home and path so that we have our basic environment variables. If dev is not already mounted, then mount dev tmpfs on it. Um redirect standard in, standard out,

**[38:20](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2300s)** and standard error to dev console using the exec redirect thing again if we didn't already have a standard in because there's a kernel bug with statically linked initramfs that is 20 years old that they started there's they started fixing the wrong way earlier this year that initramfs has a CPIO extracted into it. You can point the kernel at a directory of files and it would the build will CPIO and gzip it will archive up and gzip that directory. So I can point it at this root file system I've made but all of this build is running as a

**[39:09](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2349s)** normal user. It does not run as root. So in order for me to make nod a dev console in there I would have to be root. There's sort of a way to feed in extra information and they're saying that that that way moved but still exists and I I'm going to try to update it to see if I can get it to do that. But if you didn't do that you will feed it a CPIO GZ that that has an empty dev directory. Yeah, I'm out of I'm very long time. Um empty dev directory but when you use the external loader if it thinks that the built-in ramfs archive is empty it will create dev and dev console for you before launching init so

**[40:00](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2400s)** it has something to open to point standard in standard out and standard error at. If you have an external one it doesn't do that. So we launch with no standard in no standard out no standard error. It's another example of I'm I'm going blind until I fix it I don't get any debug messages to tell me what's wrong. So, this is a workaround for that. That I may be able to package things a little differently so that that isn't necessarily needed, but it it's another thing that, you know, will bite you if you don't know. And then um this makes uh there's a there's a dev standard in dev standard out dev standard error that is not in devfs. And this just is a little two-line thing that will create the simlinks to proc

**[40:49](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2449s)** self FD the appropriate file descriptor. And then we uh create a dev shm if we mounted devtmpfs because you don't actually have to mount anything on dev shm because it's already a tmpfs, so it's like it's good enough. It just has to be there. And then some more things, if we don't already have a dev pts, then mount dev pts on it. Uh if we don't have proc mounted, mount proc. If we don't have sysfs mounted, mount sysfs. And then the kernel added a new echo API, so you don't have to be root with the ability to send raw packets in order for the ping command to work. But they didn't enable it by default. You have to write into proc to enable it. And I went this is insane. Why?

**[41:40](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2500s)** And never got a clear answer to that, but you have to do that in order for the ping command to work unless you want the ping command to be root. Like, meh. Um and then we just bring the network up, bring up loopback, bring up the static IP that QEMU uses, add the routing, uh the default route, and then check and see if our timer is set because date percent S is number of seconds since uh the epoch, and if it's very close to zero, that means your clock was never set. So, uh if not, then use the sntp command, which is just a It It's a simpler version of NTP that is actually compatible with an NTP server to ask the host and then if asking the host didn't work, ask time.google.com, which is just hard-wired there cuz it was I I should move that to somewhere

**[42:27](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2547s)** else now that Google is um different. And then remember earlier that I said there was package support? Well, if the package support added any thing to etc/rc, run it. Most of the time it won't have. And then right echoing three to kernel printk just tells the the console to be less chatty about kernel messages from then on cuz I was If I have a shell, if it boots to a shell prompt, and then it's spewing crap over it, you know, that that mostly makes it stop. And then if we didn't specify a handoff, because remember when I run uh You were saying PowerPC. Okay, so if I run

**[43:15](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2595s)** um those kexs I mentioned, anything ju- the kernel command line is something the kernel command line has always done. Any keyword equals value that the kernel doesn't recognize gets set as an environment variable in uh no, not the I. It gets set as an environment variable. So uh the PowerPC 64-bit little endian QEMU BIOS is very slow. It is open firmware. Um it does a lot of Okay, now the kernel's running. And it is unhappy probably because of

**[44:05](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2645s)** the 70 Yeah, no init, but I mean, that's not going to be it, but Um I was in the middle of putting together a release, and I moved from the 6.17 to the 7.0 kernel, so I may have, you know, something may broken. Always when you demo. I I I wanted to get the new release out before this talk, so I would be showing you the release version, and I did not. Um next couple of days. But All right. So, if if you didn't specify what command to hand off to, which you can do by feeding it

**[44:52](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2692s)** uh environment variables through the kernel command line, then it will check to see if the file mount init exists, which will only happen if we mounted mount up here. If I fed it a dash HDA, it will try to mount that that first device on /mnt. And if mount init works, then uh let me actually SSH to Come on. There we go. What's current? Yeah, to docs. Okay, I built all this stuff 64 LE. I built all this stuff last night. Uh

**[45:42](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2742s)** install bin path because I don't have root on that machine. So, I had to install it in my directory. Okay, so here's Pac-Man, cat, etc. Uh cp or proc CPU info. But so I can actually CD um make root test root. I have a little test harness here. What I use make root for is I launch all of these targets in parallel and I give them a squash FS image that has an init in it that will

**[46:31](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2791s)** run some basic smoke tests of does the network work? Does the Does that HDA work? Uh is the is the clock set properly? And of course, could it run its init program? You know, could it run executables? And so if I run make root test root, it will be unhappy because I didn't put QEMU in the path again. QEMU install bin path. Okay. It will Each period says one of the QEMU instances it launched exited. And then at the end it will say which architectures passed all of their tests, which failed at least some of their tests, and which failed to build. And so it's saying all these

**[47:19](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2839s)** architectures passed. Um these architectures did not pass for various reasons and each of them has a different reason for not passing, mostly to do with QEMU and the kernel not being able to pass all the data to each other. And then these two didn't build These two didn't build right. Um as I said, I'm I'm fixing up stuff. So that is an example of that mount init thing doing a completely automated thing behind the scenes where I'm doing something expect like of grabbing the output of the emulator and searching it for lines of interest for the automated regression testing. Um and if 10 minutes over Oh, sorry. Um anyway, skip all that. Okay? Skip all that. That's setting up the rest of the file

**[48:06](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2886s)** system. You can probably figure that out real quick. The The fact that it can build all these different targets in there is because I have a compressed format for the kernel config. This is the base kernel config that is common to all architectures. You'll notice the uh brace expansion syntax again? That gets fed through uh this function BE to CSV brace expansion to comma separated values up here. Brace expansion to comma separated values, and then comma separated values to mini config format turns each of those things it adds capital config underscore to them, and if they don't already have an equal something on the symbol, it will add equals Y. So, one of

**[48:56](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2936s)** those per line, and then it sets it through the mini config thing with make all no config Kconfig all config equals this file to go start with all no config and switch on these symbols and let the dependency resolution do its thing for each symbol as if you went into menu config and made these changes, and that expands it to a full config. So, that's why um you know, MIPS64, sure. Uh in the docs directory, there's Linux micro config, which is those comma separated value versions, and then there's Linux mini config, which is, you know, the two blocks of architecture specific and architecture

**[49:44](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=2984s)** independent. These are all the symbols you have to set from all no config. And then there's Linux full config, which remembers what the kernel actually spit out and used. And each of these architectures, the get target config takes that cross thing and does an if else staircase of if it's one of these, this is the arch equal you feed to the kernel, this is the extra thing I need to feed to the kernel command line to tell it what its serial port is, and I can actually add more stuff if I need to. This is the file you find out of the kernel build that is actually what QEMU will load because even though VM Linux is generic, QEMU isn't set up to load it on all architectures, so sometimes you got to grab this file.

**[50:31](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=3031s)** Um there's no path to it. It just does a find. It's a it's unique on all architectures so far. This is the QEMU-system-what-to-call to load QEMU. Here are the architecture-specific config symbols, and you know, you'll notice as I said, you can add your own equals and it won't do equal Y. And then if this needs a device tree, here's where you find the device tree binary. And then all of that gets fed into there's an init ramfs generator, it creates the run QEMU script, um and it builds the kernel. And that's what the rest of this is doing. Okay? Does it get network drivers? Um you ask for the network drivers. Um they're usually architecture-specific. Um

**[51:19](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=3079s)** let's see. Um in the case of risk five, uh this is RISC-V 32 and RISC-V 64, so ISA, PCI, block dev SD, scuzzy, blah, virt uh virtio net here. virtio_net. Yeah, so in this case it's virtio. Some of them are um serial 8250, power reset, net vendor AMD. Um I think that's a menu symbol that you need to open the menu and then it's going to be under there. Um, M68K is using Uh, I think their their one is actually called Mac. No, it it Mac Sonic. So, it's configure underscore Mac Sonic. Um,

**[52:05](https://www.youtube.com/watch?v=vM_Gbx-c2Qs&t=3125s)** Spark was an Aboriginal Linux. Spark is not in make root because I switched from UC UC lib C to muscle and muscle doesn't support Spark. I can look it up in the Aboriginal Linux files to tell you what it was, but we are out of time. Sorry. Um, I I can stay around after to answer questions if you have it. Um, you you see why I was saying we would not have enough time. Sorry. Thanks. >> [applause]

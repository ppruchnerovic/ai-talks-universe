---
id: fL_wod4MVuw
title: "Zero Trust AI Agents: Securing MCP in Private Kubernetes Networks - Mithil Patel, Equinix"
slug: zero-trust-ai-agents-securing-mcp-in-private-kubernetes
conference: lf-ai-dev
conference_name: "AI_dev / Open Source Summit (Linux Foundation)"
category: "Software dev with AI tracks"
edition: "Open Source Summit + ELC NA 2026"
year: 2026
speakers: ["Mithil Patel"]
channel: "The Linux Foundation"
duration_min: 39
published_at: 2026-06-03T18:15:12Z
video_id: fL_wod4MVuw
youtube_url: https://www.youtube.com/watch?v=fL_wod4MVuw
tags: []
transcript: true
---

# Zero Trust AI Agents: Securing MCP in Private Kubernetes Networks - Mithil Patel, Equinix

**Mithil Patel**

`AI_dev / Open Source Summit (Linux Foundation)` · `Open Source Summit + ELC NA 2026` · `2026` · `39 min`

[Watch the recording](https://www.youtube.com/watch?v=fL_wod4MVuw) · [Conference site](https://events.linuxfoundation.org/ai-dev-europe/)

## Description

Join us at the premier vendor-neutral open source conference, where developers and technologists come together to collaborate, share knowledge, and explore the latest innovations and advancements in open source technology. Learn more at https://events.linuxfoundation.org/

Zero Trust AI Agents: Securing MCP in Private Kubernetes Networks - Mithil Patel, Equinix

The transition from passive RAG to autonomous agentic workflows forces a dangerous trade-off: to be useful, agents need access; to be safe, they need restrictions. Giving a non-deterministic LLM distinct permissions to your Kubernetes cluster is a security nightmare, yet agentic tool execution demands real-world access to be effective.

This session introduces a battle-tested architecture for Zero Trust Agents. We will demonstrate how to secure Model Context Protocol (MCP) servers within private networks, replacing risky static credentials with a dynamic control plane that enforces strict safety guardrails.

Attendees will learn:

Identity for Autonomy: How to integrate OpenBao (OpenSSF) to issue Just-In-Time (JIT) credentials, ensuring agents only hold permissions during active tool use.

Bounding Agency: Implementing "Read/Write Separation" at the protocol level, preventing stochastic errors or misinterpretations from causing deterministic outages.

Secure Orchestration: A blueprint for deploying MCP servers as secure bridges between AI reasoning and internal infrastructure.

Stop building toys. Learn how to deploy autonomous systems that your security team will actually approve.

## Transcript

*5,697 words · source: supa (en, exact timings)*

**[0:00](https://www.youtube.com/watch?v=fL_wod4MVuw&t=0s)** Hello everyone. Thank you for joining my session. I'm going to be presenting about zero trust AI agents securing MCP and private Kubernetes networks. A bit about me. I'm Mithil Patel. I'm a principal engineer at Equinix and I lead the site reliability engineering and DevOps for the interconnection organization. I have about 11 plus years of experience primarily in site reliability engineering and DevOps managing complex infrastructure, distributed systems, and vast 11 teams. I'm an open source enthusiast and I have been a maintainer of open source project open switch in the past and I've been an avid user of open source tools and technologies throughout my career. And and I try to integrate it I try to integrate it in my

**[0:50](https://www.youtube.com/watch?v=fL_wod4MVuw&t=50s)** work as well as in my past time and I try to contribute on open source any innovations that I do at work or in my past time. A quick glance of the agenda and what you will learn from this presentation. You will learn what are the issues with deploying AI agents and MCP servers in private enterprise networks. You will understand why with AI it has become increasingly important to establish zero trust. Identity for autonomy. So as AI systems become more autonomous, they require their own unique verifiable identity. This is essential for controlling behavior at run time, managing risk, and maintaining audits. Bounding agency with AI. So this refers to the practice of deploying autonomous AI agents within defined constraints,

**[1:39](https://www.youtube.com/watch?v=fL_wod4MVuw&t=99s)** guardrails, and specific scopes to ensure safety, reliability, and regulatory compliance particularly in enterprise networks. All of this will show you a blueprint for deploying MCP servers as secure bridges between AI reasoning and your internal infrastructure. Now, to understand why we take zero trust so seriously, you have to understand our scale. Equinix is the world's digital infrastructure company. We operate 280 plus data centers all over the world across 70 different global metros. Equinix is where networks, clouds, and enterprises meet. Thousands of providers and hundreds of thousands of interconnections are present in our ecosystem. To manage this, we have hundreds of

**[2:27](https://www.youtube.com/watch?v=fL_wod4MVuw&t=147s)** microservices and thousands of pods running in our Kubernetes infrastructure that provides and supports software-defined connectivity in our ecosystem. We operate at a scale where a single misconfiguration doesn't just take down an application. It can impact global network traffic. So, when we decided to introduce AI agents into our development functionality as well as into our infrastructure to manage this massive scale, we knew standard perimeter security isn't going to cut it. We need zero trust. Now, before I talk about the experiment, how many of you know what an MCP is? Okay, almost all. And how many of you are building MCP servers? Okay.

**[3:14](https://www.youtube.com/watch?v=fL_wod4MVuw&t=194s)** A lot of you. Uh so, we're living in the wild west of AI agents. The model context protocol MCP has made it incredibly easy to connect large language models to our infrastructure. So, I started with a simple experiment to help my SRE team stay ahead during P1 incidents. I built a custom Kubernetes MCP server that acts as a bridge translating an AI agent's natural language request into actual Kubernetes API calls. And it was magical. It could detect root cause of a crash loop in seconds and could suggest ways on how to fix it. It was like a Kubernetes expert as a companion next to me. Now, these are a few of the capabilities in the MCP server. As you can see, these are the function tool calls which I

**[4:01](https://www.youtube.com/watch?v=fL_wod4MVuw&t=241s)** wrote. So, it could check logs, it could troubleshoot pods, nodes, it could restart deployments. It could also run custom kubectl commands based on the LLM's intelligence if there are unique requests coming in from users. So, to reduce toil from developers on requests on why their applications are failing and repetitive tasks and requests, I created this MCP that could provide a self-service way for them to ask the agent in natural language. And it could also assist us as SRE team during P1 incidents. So, this is the anatomy of the Kubernetes MCP server. So, when a user sends a natural language prompt or a request to the AI agent, the AI agent does some massaging orchestration of the task and forwards

**[4:50](https://www.youtube.com/watch?v=fL_wod4MVuw&t=290s)** it to the LLM. Now, the LLM is already integrated with our Kubernetes MCP server, so it knows the different function tool calls that are available for it. So, if you ask what are the pods running in our infrastructure or in my name space right now, it would translate to a JSON payload, something like a kubectl get pods. It would send it to the MCP server with which acts as a protocol bridge. And eventually this translates into actual Kubernetes API calls happening on our clusters. So, the result, it worked remarkably well. Too well, actually. I gave an LLM maintainer level rights. If the model made an incorrect assertion, it could potentially harm applications and eventually impact anything in production. I immediately realized the potential

**[5:39](https://www.youtube.com/watch?v=fL_wod4MVuw&t=339s)** security issues that could happen with natural language misinterpretation or due to the confused deputy problem. So, the confused deputy problem, if you guys don't know, it's a security vulnerability where a privileged program is tricked by a less privileged user into misusing its authority to perform unauthorized actions. As you can see, that can easily occur with natural language user inputs. So, I had to find a way to make it safe. But, why couldn't we just use standard Kubernetes RBA? We tried, but we hit a fundamental wall. Kubernetes RBA is binary. You either have permission or you don't. It doesn't understand context. It doesn't know if the agent is deleting

**[6:26](https://www.youtube.com/watch?v=fL_wod4MVuw&t=386s)** a pod because it's unhealthy or because a malicious user asked it to burn everything down. Standard pods mount long-lived service account tokens at var run secrets. Even projected tokens default to ours and they auto renew. So, an attacker who exfiltrates one has substantial dwell time. Worse, many legacy clusters still issue non-expiring secret-based tokens. We realized that permission isn't enough. We needed ephemeral identity. We needed a system where the permission doesn't even exist until the exact second it's needed and vanishes the moment the job is done. Oops. So, in my presentation, layer one, two,

**[7:13](https://www.youtube.com/watch?v=fL_wod4MVuw&t=433s)** and three refer to a defense-in-depth strategy. They represent the three walls an attacker must break through in the specific order they would encounter them. So, in a standard setup, you might rely on VPC or private subnets. But, inside Kubernetes, the network is often flat. If I compromise a web front end running in Kubernetes, I can usually curl your internal tools. So, we solve this with Istio. So we inject an Envoy sidecar into our agent pod as well as into our MCP server pod. Now this changes the game. The agent no longer talks TCP to the MCP server. It talks mTLS. Now this gives us two massive wins. First encryption.

**[8:01](https://www.youtube.com/watch?v=fL_wod4MVuw&t=481s)** Deep packet inspection on the network sees nothing but garbage. Second, and more importantly, identity. The MCP server doesn't just see the IP of the agent. It sees a cryptographic ID. It sees something like spiffy.cluster.local, the namespace, the service account. So spiffy, if you guys don't know, it stands for secure secure product production identity framework for everyone. Uh I'll talk about spiffy a bit more on the coming slides. So we then apply an Istio authorization policy that says deny all. We write a single exception saying allow traffic on port 8080 if and only if the source identity is the spiffy ID of the service account of the agent.

**[8:50](https://www.youtube.com/watch?v=fL_wod4MVuw&t=530s)** You also use service entries in Istio to maintain strict internal routing so the agent never accidentally traverses the public internet. So this is an example of the authorization policies which I talked about. So Istio automatically assigns a spiffy ID to every pod based on its Kubernetes service account. When the AI agent sends a request to the MCP server, the Envoy sidecar injects the identity into the mTLS handshake. The MCP server sidecar then validates the identity against this defined policy. So the above configuration here implements a zero trust model. It explicitly allows the spiffy AI agent and the specific AI agent, sorry. Uh and then relies on global namespace

**[9:38](https://www.youtube.com/watch?v=fL_wod4MVuw&t=578s)** wide deny all policy to catch everything else. Now, even if an attacker gets into our cluster, they can't even open up a TCP socket to our MCP server unless they have the agent specific SPIFFE certificate. Now, you can also further restrict uh any API paths if you want to with {slash} MCP star as an example here. Uh so, Istio uh in the last year or two also has come up with uh Istio ambient mesh architecture, which is a sidecar-less approach, but the same fundamentals and principles can be applied there as well. So, on SPIFFE, right? So, how does SPIFFE uh ensure security? So, first thing, cryptographic proof. The identity is encoded in the X.509 certificate used for mTLS. It cannot be

**[10:26](https://www.youtube.com/watch?v=fL_wod4MVuw&t=626s)** easily spoofed by other pods in the cluster. Granularity. So, unlike namespace level isolation, SPIFFE identities allow you to restrict access to a specific service account. Even if another tool is running in the same namespace as the AI agent, it will be blocked unless it shares that specific identity. And automation. You don't need to manage the certificates manually. Istio via the Istio daemon handles the issuance and rotation of these identities automatically. Okay, layer two identity. Now, let's talk about the keys to the kingdom. Right? So, we follow the secret zero principle. Our MCP server starts with zero credentials. No environment variables, no config

**[11:15](https://www.youtube.com/watch?v=fL_wod4MVuw&t=675s)** maps. In a zero trust world, we never assume a workload is who it says it is just because it's running in our cluster. We need cryptographic proof. There's many open source secret management tools out there, and Open Bar is one of the most popular ones being maintained by the Open SSF. And it's a primarily a fork from the popular tool vault. The Kubernetes authentication method of Open Bow works great for this. So, how does our MCP server authenticate itself with zero pre-provision credentials? It uses the Kubernetes projected service account token. It's a standard JWT mounted into every pod in our cluster. Unlike legacy tokens, it's it's time bound. The kubelet handles the rotation of of this in the background, so our

**[12:04](https://www.youtube.com/watch?v=fL_wod4MVuw&t=724s)** application stays application stays rotation aware without a single line of refresh logic. Now, when let's say as an example, when the agent asks to restart a deployment, right, our our MCP server takes its own JWT and sends it to Open Bow. Open Bow calls the Kubernetes token review API to do a delegated check. Is this pod actually running? And is the token valid? Now, that's machine identity at the platform level. Now, once verified, Open Bow doesn't just hand over a static password. It issues a brand new scoped Open Bow token for this one request with a 60-second TTL. Now, that 60 seconds is is the agent's authorization window.

**[12:51](https://www.youtube.com/watch?v=fL_wod4MVuw&t=771s)** How long the agent can ask Open Bow for credentials before having to re-attest with its pod identity. Now, under that lease, uh Open Bow mints a fresh Kubernetes service account token. The agent uses it immediately to restart the deployment in this case. And the token drops out of the scope when the function returns. After 60 seconds, no new credentials can be minted without a fresh round of cryptographic proof. Now, think about the security posture here. If you dump the memory of the Kubernetes MCP the you find nothing persistent. If you steal the dynamic credential, you have less than a minute before Open Baow revokes it. By the time an attacker uses it, his access is already gone. Now, let's look at the block of the

**[13:42](https://www.youtube.com/watch?v=fL_wod4MVuw&t=822s)** code. Now, this is the exact pattern we use to solve the confused deputy problem. There are three things happening here. So first identity. Notice that there are no API keys hardcoded here. We read the service account token directly from the pod's file system. This is a standard short-lived JWT signed by Kubernetes. We use this to prove to Open Baow that I am the MCP server pod. The role MCP restart here, it's it's nothing but the Open Baow auth role, making sure the agent can talk to Open Baow and for how long. Second, the lease. We exchange the pod's identity for an Open Baow token

**[14:29](https://www.youtube.com/watch?v=fL_wod4MVuw&t=869s)** scoped to a single capability and bounded by a 60-second TTL. 60-second Okay. So, this 60-second TTL is set on the Open Baow role here. The role deployment restarter, as you can see here, is the secrets engine role. It controls what Kubernetes credential Open Baow gives me. Now, before this line, no authorization existed. After this line, there's 60 seconds of operating authority. Most workflows complete inside this 60-second TTL for us. There are a few edge case scenarios where it genuinely cannot be completed in 60 seconds, where we tune it to about 3 to 4 minutes. And that is configured per role. Third, the scoped execution.

**[15:20](https://www.youtube.com/watch?v=fL_wod4MVuw&t=920s)** Now, under the 60-second Open Baow lease, the agent calls the Kubernetes secrets engine, which issues a fresh short-lived Kubernetes service account token via the token request API. Now, the token is used immediately to patch the deployment, so it would restart the deployment here, drops out of the scope when the function returns. So, no persistent state, no environment variables, no disk writes. Finally, I realized that code-level permissions were not enough. There is a need of architectural separation. I call this the two-brain system. Some of the lessons we learned from this experiment and the proposed architecture from our POC uh is here. You should separate your MCP servers

**[16:08](https://www.youtube.com/watch?v=fL_wod4MVuw&t=968s)** tools into two different sets of functionalities. So, in my case here, the first one that can only call tools that just need to read data. So, it has tools like get get logs, list pods, describe pods. Now, LLM can use this as much as it wants. It's fast, it's autonomous, and it's safe. For calling this, it uses a read-only service account and executes everything synchronously. The second set of tools, call it the right functionalities. So, these are state-altering actions. Now, it has edit, delete, restart, scale kinds of action for my Kubernetes MCP server here. These should be handled via the human in the loop middleware, and these require a

**[16:56](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1016s)** higher privilege service account that the agent only unlocks after a human approval. Let's talk a bit about uh the asynchronous human in the work human in the loop workflow and why it is critical. Now, for SREs, it is a nightmare uh to think that you are allowing AI to allow write operations on your Kubernetes applications. But with a human in the loop protection and controlling what it can or cannot do, it can be turned into a safe operation. With user input need or due to some misinterpretation, if the LLM tries to call a tool with a right functionality on the server, the request hangs. With the human in the loop middleware setup, you can send a ping to SREs or

**[17:45](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1065s)** any authorized users here. Maybe a Slack button or a UI pop-up asking for an approval and having a message what the agent is trying to do. Now, why we had to do the two or two accepted pattern, I'm going to talk about a bit in the next next slide. So, one of the things which is essential to have this workflow is a state store like Redis, Valkyrie, or or another any other durable key value store. This is where the pending workflow with key value information of the user ID and the Kubernetes action in my case are cached until a human reviews and approves the action. Only when the human clicks approve is when the webhook triggers the right tool functionality with the cached information.

**[18:34](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1114s)** This means even if the AI goes completely rogue and tries to delete something on the cluster, it physically cannot execute the command without a human co-signer. Now, this effectively solves the risk of destructive actions. Uh so, I have a diagram for a workflow of the human in the loop. Let me bring it up. Okay. I can't really see from here. Okay. So, when a request from the AI agent or the LLM ends up at the MCP, uh and it's a mutating call, so if it ends up at one of our read

**[19:24](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1164s)** functionalities, it is safe. It is synchronous and it can immediately be executed on the target infra. In my case, this is the Kubernetes cluster. But, if it uh But, if it is a one of those right functionalities, then this is where the human in the loop comes into play and this is an asynchronous function. So, it stores it hydrates the context in Redis as an example for the key-value store here with the information of what is the Kubernetes action it's trying to do and other information like the UUID. So, as you can see here, uh this does a non-blocking 202 accepted call back to the LLM.

**[20:14](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1214s)** Why we had to do that, uh I'll talk about uh in detail on the next next coming slides here on why we had to send a non-blocking call back to the LLM. Then the right functionality is then uh triggered for an approval to a Slack Slack block kit or a UI or a pop-up and asking the human for an approval here. Can't really see much. I'm sorry. Okay, so when the human approves it, sorry, it's not too visible from here. Okay, it's already reached the background location. Okay, so when the human uh clicks an approval, uh it picks one of the background workers get gets a

**[21:01](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1261s)** webhook callback. It picks the context from Redis on what is the action it needs to do. Goes to OpenBao for the just-in-time credential that we talked about. And eventually does the action on the target infra. After this, it sends the information back to Slack as a message on what it tried to do and back to the LLM. Okay. Okay, so last, observability. Right? So, security isn't just about prevention. It's also about visibility. One of the scariest things about AI agents is the black box problem.

**[21:48](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1308s)** If a production application vanishes and you ask why, you cannot accept that the AI agent misinterpreted as an answer, right? You need engineering-grade logs. It is important to build a structured audit pipeline. Every single time the agent touches your infrastructure, you should log three things. The user who asked, the tool arguments the agent chose, and the OpenBao lease ID in this case it used to execute it. This lets us trace a request from a chat message all the way to a Kubernetes API call. We can see exactly why a decision was made. We can even set up some anomaly detection if a bunch of destructive actions are triggered in a short period of time. Uh LangChain is a popular open-source AI

**[22:37](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1357s)** agent framework that we leverage, which also supports OpenTelemetry. So, you can plug into any standard observability tool. All of this turns AI magic into engineering observability, which is extremely important in today's world. Okay, so key takeaways. Giving AI agents operational capabilities creates powerful automation, but requires network isolation and specific permissions based on cryptographic identity. Just-in-time credentials for zero security leaks. Strict guardrails and approval workflows with human in the loop for any kind of dynamic or destructive actions. And of course, comprehensive observability and audit logging. The future of AI-assisted operations

**[23:25](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1405s)** requires solving the safety challenge first. Now, I have given an example of the Kubernetes MCP that we have built here, but the same blueprint can be applied to any agent and any MCP server out there, especially when it has access to manage your infrastructure. Okay, so is all of this architecture worth it? The data says yes. First velocity. By giving AI agents safe read-only access to logs and events, we reduce the our mean time to resolution for P2 P3 incidents by 60%. The agent triages the issue reported by developers even before reaching out to our SRE team or even before opening tickets.

**[24:12](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1452s)** Second, the security posture. That was our primary primary goal here. So, we eliminated 100% of standing privileges. There are no API keys waiting to be stolen. If an attacker breaches the agent container, it would find an empty room. And finally, compliance. Because every action goes through our proxy, we have a perfect audit trail. We don't just know that a pod was deleted, we know who approved it and why the agent suggested it. And the exact second the permission was granted. Okay, what broke? So, naturally, not everything just miraculously worked as I developed this MCP and integrated into our Kubernetes environment.

**[24:59](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1499s)** There came several issues uh that needed to be dealt with. Some of the most troubling ones which I can remember are the paginated lock timeout, right? So, the 60-seconds TTL trap. Now, the problem we set our just-in-time tokens to expire in exactly 60 seconds. So, what broke? Um So, when I asked the agent to diagnose a crash loopback in a Java application, to do this the agent initiated a tool called to pull the logs. The pod had gigabytes of logs. Now, so the Kubernetes API responded with a paginated stream. After 60 seconds, while the agent was still processing chunk, right? OpenBow revoked the token token lease. The connection severed abruptly and the

**[25:47](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1547s)** agent panicked and just responded saying with a false outage saying the pod is no longer existing. And kind of reported a false outage. So, the fix I had to do is I had to rewrite the MCP check logs tool functionality to strictly use {dash} {dash} tail 500 or {dash} {dash} since the past 10 minutes of logs, right? So, that so that the Kubernetes API can satisfy the request well within the OpenBow 60-second lease. Now, for a few tool calls and scenarios that genuinely cannot be completed within 60 seconds, we configure a higher TTL window of 3 to 4 minutes at the OpenBow lease level. Uh yeah, the second one, the thundering herd self DDoS. Now, our our MCP server

**[26:36](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1596s)** was designed to be perfectly stateless. Every time the LLM tool made a LLM made a tool call, the server requested a fresh just-in-time token from OpenBow. Now, we had a cluster-wide node failure, uh the agent attempted to audit hundreds of pods together in a tight loop. Now, at machine speed, it fired hundreds of simultaneous tool calls. The MCP server hammered our OpenBao with concurrent login requests. And because Kubernetes OpenBao also does a Kubernetes token review API, uh Kubernetes authentication method, it also delegates the JWT verification and populates the Kubernetes API server as well. So, the herd hit both OpenBao as well as our Kubernetes API servers at the same

**[27:25](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1645s)** time. Now, we kind of did a self-DDoS uh attack on our own control plane and tripped the uh rate limiting alerts for us. So, the fix, we knew we had to introduce token caching at the MCP middleware layer. Now, if a just-in-time token for the for the agent specific role has about 15 to 20 seconds of TTL remaining on its OpenBao lease, then we just reuse the token that is existing. Uh the third, the asynchronous slack timeout. Now, this is the 202 uh accepted non-blocking call that we had to introduce in our architecture. So, for state-changing actions like, let's say, restarting deployments, the MCP server uh sends a slack block kit message for an approval to SREs or or a human,

**[28:12](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1692s)** right? Uh so, it pauses the execution. Now, SREs are busy. The dedicated humans are busy. They're not going to approve it within a few minutes. Now, LLM agent frameworks enforce tool call timeouts measured to a couple of minutes, now depending on the provider and configuration. Now, by the time uh it took about 15-20 minutes for someone to approve it, the LLM had already given up on the tool call. It dropped the response and just moved on. The human approved an action a few minutes later, the agent no longer cared about it. So, the result never made it back to the conversation. So, the fix there was we had to decouple our architecture. The MCP tool now immediately sends a 202 non-blocking accepted pending human

**[29:00](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1740s)** approval back to the LLM so that it doesn't time out. We moved it to an event-driven authorization. So, the actual execution is handed off to a background worker that waits for the Slack webhook approval uh and then executes the action. So, none of these forced us to abandon the just-in-time model. Right? It forced us to deploy it correctly. The pattern survived, but but our initial naive implementation did not. So, tuning the AI. Now, LLM is just like a junior SRE. It is an expert in knowing commands, but not quite how to operate in production environments. It doesn't know the environment and the existence of network policies or

**[29:50](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1790s)** or pod disruption budgets or that or that the environment is maintained via GitOps. So, system prompt is how you can put some instructions to tune it better. With our learnings and experience, we had to add a list of things for it to just for it to suggest what is acceptable and what is not. With more time of using it, you will have edge cases and scenarios that you can learn and maintain in the system prompt. But system prompt is just there to help the AI and the LLM to make a call. It still doesn't guarantee anything. Okay, so to wrap up, uh we are entering a new era. We are moving from chatbots that talk to agents that do. But you cannot build the future on the

**[30:38](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1838s)** security patterns of of past. Static keys and flat networks will not survive in an agentic world. So, remember the three layers we went through. Isolate the network with MTLS. Ephemeralize the identity with Kubernetes authentication on the fly. And gate the action with human in the loop uh and the architectural separation. So, my challenge for you is simple. Go back to your org and find that one server with a with a hard-coded or long-valid API key or token and replace it with a pattern you trust. Don't just build agents that work. Build agents that survive. Lastly, if you've not heard of uh Agentic AI Foundation, I would highly

**[31:25](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1885s)** recommend to scan this and go through it. MCP, as we discussed, is a leading project being maintained here. In this new AI world, it is becoming increasingly important to have a neutral open foundation to evolve all of these AI projects transparently and collaboratively. Equinix is excited to be a gold member here and is actively participating in working groups to define, adopt, and advance these AI standards. Thank you. >> [applause] >> Questions? Hey. Hi. Thank you so much for your talk. Yeah. I had a question. When it comes to identity, Mhm. you know, most of these examples covered one hop

**[32:11](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1931s)** of identity. How does it work when you have multi-hop agents? Do you propagate identity from the parent all the way down to the leaf node, or how does that work? Have you seen examples of that? Yeah, so we haven't seen multi-agents here. Uh what we've done in our enterprise, we have multiple MCP servers. Now, each of those has its own just-in-time credential configuration there. So, we put the just-in-time credential as I showed you in the block of code, it's integrated into the function tool call itself. Uh we haven't uh done multiple AI agents approach, but I think each each AI agent has its own OAuth mechanism to uh go. But this was just specific to an agent calling an MCP server. Thanks. Any other questions?

**[33:02](https://www.youtube.com/watch?v=fL_wod4MVuw&t=1982s)** Yeah. How are you managing the context that you would send to the model? So would you When you're getting logs or metrics, would you consolidate that and then send it to the model or were you sending all the logs all at once to the model? Yeah, so we had to tune it uh based on each request. Now uh in terms of logs or all of this for Kubernetes functions, right? The context really doesn't matter much. It just wants to know key information about what was the previous pod information, right? But in general, in terms of context, I think it's a popular popular challenge right now happening in AI agent. Uh I think this week itself Anthropic has launched something called memory states. You can uh maintain memory states uh for a particular time window across your LLM model or in your uh case, and you can also do KV caching

**[33:52](https://www.youtube.com/watch?v=fL_wod4MVuw&t=2032s)** for this. Uh but in terms of specific logs uh I don't see much of a value to have historical context other than just the pod information. Uh hope that answers your question. Yeah. Yeah, go ahead. Would you uh so you mentioned that [clears throat] you would send a a Slack message to a human Mhm. for approval of command that could be multiple minutes between the approval. Yeah. Would you do a a revalidation of the the scenario to make sure that things are in the same state as they were whenever you fired off that that action for approval? Uh great question. So uh uh what it has what the human has to evaluate is something that is existing in the cache for us. So, it's always

**[34:41](https://www.youtube.com/watch?v=fL_wod4MVuw&t=2081s)** there for it to refer whether that particular action is allowed to do. Uh so, the reference there would always be the uh key value cache that is there, which is Redis in our case. So, that is where it always refers it. So, if a human had a function like delete a pod, Mhm. they would have to go with the already the Yeah, so it still in that state and that they actually Correct. So, if it's not in that state, let's say the pod something happened to the pod, right? The just-in-time verification by the Open BaW will fail. It will say that the pod is no longer existing. So, the JWT service account Kubernetes verification that we were doing so that it gets the Open BaW token, that will fail at that point saying that the pod's no longer there. Right? So, before any dynamic action, still verifies whether the pod is

**[35:28](https://www.youtube.com/watch?v=fL_wod4MVuw&t=2128s)** actually existing or not. Yeah. Yeah. So, you said you're using Redis as like the cache Mhm. for actions that need to be approved, but that's also where all of your logging is for the entire history of actions performed by the LLM and No, so once any action is performed, it's it's no longer existing in the cache. It's it's it will be available in the observability tools that this action has happened. Oh okay. Yeah. Any other questions? Okay, going once, twice. Yeah, go ahead. What was What was triggering

**[36:18](https://www.youtube.com/watch?v=fL_wod4MVuw&t=2178s)** the investigation to begin with? Was it wired up to alerts? Was it Was there something autonomous triggering the the actual mission investigation? Yeah, so it started with and I took a safe action to only check logs, troubleshoot pod. I had a custom functionality for troubleshooting pods and nodes that it would do multiple different coop CTL calls to understand what what it's doing. So, initially I had to only do read capabilities uh something safe capabilities. But, as the more you develop it, you have to make it more autonomous and to do understand more details and allow your developers power your developers to do more things. Right? You don't need an approval from uh let's say if you have Kubernetes deployments and you have configured it to be a rolling restart, right? A zero

**[37:07](https://www.youtube.com/watch?v=fL_wod4MVuw&t=2227s)** downtime restart. You don't really need an SRE there, right? You you can just safely ask it that, "Hey, uh I just want to restart my deployment." And it would make sure it just does it uh in zero downtime. But, the whole the whole workflow was manually invoked then? Uh like let's say the who's having issues with the Yeah. Yeah, so we have a kind of a SRE agent and a bot where it could just ask natural language requests and it would execute everything Kubernetes applies. Yeah. Yeah, hi. I'm assuming the user's credentials along with the agent request and then authorizing not only the agent but also the user writing the prompt. Right, so we do log that and this is just internal to our enterprise network,

**[37:56](https://www.youtube.com/watch?v=fL_wod4MVuw&t=2276s)** so we only information we have is which user asked it, right? We don't need to have more information about passing his credentials. We're going to actively actively use the MCP server's different function tools and it would check the service account of it, right? So, we don't really need to push our credential of the user here. Yeah. How robust is this flow that you're doing? Assuming that there's like bad actors with injection like sometimes >> Yeah, so the agent and MCP set up for us it's it's it's in our private Kubernetes network. So uh we only allow it to execute functions which are available on our MCP servers.

**[38:46](https://www.youtube.com/watch?v=fL_wod4MVuw&t=2326s)** We do not allow it to take even actions or inputs like hey, send me send things like this to an email. We we have tuned it so much that it won't take any such requests which are not which are not something that it can satisfy from its different uh tool functions.

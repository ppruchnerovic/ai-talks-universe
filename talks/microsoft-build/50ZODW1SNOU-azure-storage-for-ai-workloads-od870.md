---
id: 50ZODW1SNOU
title: "Azure Storage for AI workloads​ | OD870"
slug: azure-storage-for-ai-workloads-od870
conference: microsoft-build
conference_name: "Microsoft Build"
category: "Vendor & platform"
edition: "Build 2026"
year: 2026
speakers: []
channel: "Microsoft Developer"
duration_min: 33
published_at: 2026-06-12T16:31:50Z
video_id: 50ZODW1SNOU
youtube_url: https://www.youtube.com/watch?v=50ZODW1SNOU
tags: ["Azure Storage for AI workloads​ | OD870", "Cloud Platform & Data", "OD870", "OD870_v3", "Saloni Sonpal", "Saurabh Sensharma", "Vishnu Charan TJ", "build", "build 2026", "m9z7", "microsoft", "microsoft build", "microsoft build 2026", "ms build", "ms build 2026", "msft build", "msft build 2026"]
transcript: true
---

# Azure Storage for AI workloads​ | OD870

**Speaker not identified**

`Microsoft Build` · `Build 2026` · `2026` · `33 min`

`#Azure Storage for AI workloads​ | OD870` `#Cloud Platform & Data` `#OD870` `#OD870_v3` `#Saloni Sonpal` `#Saurabh Sensharma` `#Vishnu Charan TJ` `#build` `#build 2026` `#m9z7` `#microsoft` `#microsoft build` `#microsoft build 2026` `#ms build` `#ms build 2026` `#msft build` `#msft build 2026`

[Watch the recording](https://www.youtube.com/watch?v=50ZODW1SNOU) · [Conference site](https://build.microsoft.com/)

## Description

Learn how Azure Storage powers AI inference at scale. This session explores how to securely bring enterprise data to AI models, accelerate AI workloads with high-performance storage, and reduce GPU idle time through faster model loading and optimized data access. See how Azure Storage integrates with Microsoft and open-source AI frameworks to improve performance, lower costs, and enable scalable agent-based applications.

𝗦𝗽𝗲𝗮𝗸𝗲𝗿𝘀:
* Saurabh Sensharma
* Vishnu Charan TJ
* Saloni Sonpal

𝗦𝗲𝘀𝘀𝗶𝗼𝗻 𝗜𝗻𝗳𝗼𝗿𝗺𝗮𝘁𝗶𝗼𝗻:
This is one of many sessions from the Microsoft Build 2026 event. View even more sessions on-demand and learn about Microsoft Build at https://build.microsoft.com

OD870 | English (US) | Cloud platform & data

Pre-recorded

#MSBuild

Chapters:
0:00 - Introduction to Azure Storage for AI workloads and session overview
00:01:20 - Framing the connection between Azure Storage and AI workloads
00:03:57 - Azure Storage integration with AI frameworks and infrastructure
00:05:20 - Understanding agentic inference and storage requirements
00:07:11 - Optimizing inference with prompt caching
00:10:10 - Three tiers of prompt caching in Microsoft Foundry
00:13:05 - Deep dive into Azure Context Cache design and enterprise benefits
00:17:47 - Fast model loading and distribution using Azure Blob and Streamer
00:25:05 - Bringing enterprise data into AI inference with integrated tools
00:29:00 - Connecting Azure Storage to Foundry IQ and Storage Center announcements

## Transcript

*4,849 words · source: supa (en, exact timings)*

**[0:06](https://www.youtube.com/watch?v=50ZODW1SNOU&t=6s)** Hi everyone and welcome to Azure Storage for AI workloads. I'm Sourabh Sen Sharma, a Product Manager on the Azure Storage team focused on AI workloads. And today I'm joined by my colleague Vishnu Charan from Azure Storage and Saloni Sonpal from Azure Open AI. Today we are going to walk you through how Azure Storage powers AI inference at scale, how we securely bring enterprise data to AI models, how we accelerate inference with high performance storage, and how we reduce GPU idle time through faster model loading. Whether you're building on Microsoft Foundry on Ask or directly on Azure Infrastructure as a Service Services, this session is built for you. This talk is focus on Inference, the dominant AI workload

**[0:57](https://www.youtube.com/watch?v=50ZODW1SNOU&t=57s)** today, and the unique capabilities Azure Storage brings to make agentic inference faster, cheaper, and more secure, including turning your enterprise data into agent ready knowledge. This session is broken into chapters that map to each agenda item that you see here, so feel free to jump straight to the topic that matters the most to you. Before we dive in, let's quickly frame how Azure Storage and AI connect. We typically think about these two in two categories. First, storage for AI storage as a foundational element of every AI workload, from training and fine tuning to agentic inference, which is the focus of today's talk. This is where speed, scale, and security have to come

**[1:48](https://www.youtube.com/watch?v=50ZODW1SNOU&t=108s)** together. Second, AI for storage. Using AI to make storage itself smarter, from better data management and classification to day-to-day operations. Today, we'll focus on the first category, storage for AI. Before we go deep on storage for AI inference, let's set the stage on how Azure Storage plugs into the AI stack today across training and inference, and how it gives you the freedom of choice to build applications on Microsoft Platform as a service, services on Kubernetes, or directly on Infrastructure as a Service or IAS. With open source and partner tools at the infrastructure layer. The AI workload stack looks quite familiar. Physical infrastructure, GPU's, NICs, local NVME, the same hardware story,

**[2:42](https://www.youtube.com/watch?v=50ZODW1SNOU&t=162s)** virtualization on top VMS and Kubernetes nodes. Again the same playbook. But the differences emerge in storage, where AI workloads demand high throughput and low latency at massive distributed scales across three patterns. In fact, data prep, high throughput checkpointing during training and model and data retrieval during inference that starts with Azure BLOB as the durable foundation access through purpose built storage clients beginning with BLOB views for POSIX like file access, AD LFS for FS spec workflows and the Pytorch connector for high throughput data loading for HPC class throughput. Azure Manage luster with the AMLFS POSIX client and Azure Container storage that presents local NVME as persistent volumes for

**[3:36](https://www.youtube.com/watch?v=50ZODW1SNOU&t=216s)** stateful Kubernetes workloads with Azure files, disks and Elastic Sans support on the road map. All of these are orchestrated across VMSS and ask we've optimized these clients to plug into the AI run times and orchestrators the developer community is using today. This includes Ray, Pytorch, Tensorflow, VLLMSG, Lang, Triton, and ONNX. And the same foundation powers Microsoft Foundry, the Ask Kubernetes AI Tool Chain Operator, or as we call it Kaito, and the broader Azure AI infrastructure. Building on exactly this foundation, Azure gives you 3 intentional paths to run AI workloads, and storage is a critical

**[4:27](https://www.youtube.com/watch?v=50ZODW1SNOU&t=267s)** element across all of them. Managed Pass with Microsoft Foundry as the managed AI platform with access to Microsoft Partner and Open models. Great for teams who want abstraction. Managed Kubernetes with Ask for teams who want more control. Bring your own models and frameworks and orchestration. Still deeply integrated with Azure Infra Direct, IIS with VMS, storage, networking and AI optimized silicon direct. This is where hyperscale and mega scale AI customers operate across all three. Storage delivers the same outcomes, bringing enterprise data into the agent stack and powering fast, cost effective, governable inference at

**[5:18](https://www.youtube.com/watch?v=50ZODW1SNOU&t=318s)** scale. Let's look at agentic inference from a storage centric view. Any agent that leverages enterprise data is built on a data pipeline anchored in object storage or Azure BLOB with two stages. Stage 1, bringing your enterprise data to AI services and agents. This is the data preparation, pipeline ingestion, cleaning, chunking, embedding and indexing into a vector database so that the agent can do grounded retrieval with the right context. Intermediate representations, namely the embeddings, indexes, process chunks get stored on Azure BLOB so they can be retrieved with milliseconds latency at query time without rerunning the entire pipeline. Stage 2 Retrieve, store, and manage AI data at runtime.

**[6:13](https://www.youtube.com/watch?v=50ZODW1SNOU&t=373s)** Every turn of the agent loop multiplies, load on storage, more retrievals from BLOB and the vector index, more test time, compute, and an exponential explosion of logs, conversation state, tool outputs, and agent traces that all have to be persisted for fast retrieval downstream at scale. Distributed inference also swaps models in and out across GPUs. That gives us three critical storage requirements for agentic inference. 1, making it easy to bring enterprise data securely to agents and converting it into agent accessible knowledge. 2, distributing and loading models onto AI compute fast enough to keep expensive GPUs busy. And three, optimizing infrastructure utilization to drive up tokens per

**[7:05](https://www.youtube.com/watch?v=50ZODW1SNOU&t=425s)** Watt per dollar. The rest of this talk maps directly to those three. Let's start with optimizing inference itself. And the biggest lever we have there right now is prompt caching. Now that we've set the context for top storage requirements for agentic inference, let's go one layer deep into where the next big wave of inference cost and performance pressure is showing up. Prompt caching. As enterprises move from single turn chat to long multi turn agentic workflows, the way prompt tokens flow through the GPU is becoming one of the largest drivers of inference cost and latency. My colleague Saloni Sonpal from Azure Open AI and I are going to walk you through what's happening and what we are doing about it at Microsoft across Azure and

**[7:55](https://www.youtube.com/watch?v=50ZODW1SNOU&t=475s)** Microsoft Foundry. To set the stage, let's look at how inference works today without caching. Modern AI agents run on long static enterprise context, system instructions, tool definitions, retrieval context, and they invoke these prompt context across millions of invocations. In most production agents, the static unchanged portion of the prompt represents more than 60% of inference cost. Here is why. On every turn, the GPU has to recompute the model attention over the entire prefix, the static system prompt, the tool definitions, and all of those prior turns. Turn 2 adds even more history on top of that, and then turn 3 adds even more. By the time you're a few turns in, the GPU

**[8:44](https://www.youtube.com/watch?v=50ZODW1SNOU&t=524s)** is burning expensive cycles rereading the same tokens it has already seen before. That recompute is what drives up time to 1st token, or as we call it TTFT and the end to end inference latency. And for the what makes long context agents so expensive to operate. Now let's apply from caching to the same picture. The idea is simple but very powerful. Instead of recomputing attention on every turn, we cache the KV state of the repeated context tokens and reuse it for the next turn. On turn 1, the GPU still processes the full prefix once, but as it does it, it also writes the attention state out as a context cache object. Turn 2 the GPU doesn't redo that work, it just loads the cache state and only computes the attention on the new tokens for that turn.

**[9:35](https://www.youtube.com/watch?v=50ZODW1SNOU&t=575s)** Turn 3 and every turn after that looks exactly the same. The result is a dramatic reduction in GPU compute per turn, and customers can get inference cost savings up to 90%, along with significantly lower time to 1st token. In fact, this is so impactful that model providers like Open AI already offer implicit prompt caching. AP is out-of-the-box. That's a great starting point. But for enterprise scenarios our customers are building, implicit cashing alone is not enough. And that's exactly what Saloni will talk about next. Thanks Aura for grounding us in the core problem of prompt caching. At Microsoft Foundry we think of prompt caching as an evolution. 3 distinct tiers, each designed to address different scale of workload.

**[10:24](https://www.youtube.com/watch?v=50ZODW1SNOU&t=624s)** Let me walk you through them. First is implicit prompt caching our fast by default option? The KV cache sits directly in the GPU memory alongside the inference engine, making it ideal for real time agents, multi turn conversations and other short lived workloads. It supports data sizes from kilobytes to low megabytes, with TTLS typically between 5 to 10 minutes up to about an hour. The key advantage is speed and simplicity. The model provider owns the cache and the API. The customers simply benefit from improved performance with 0 changes to their application. Implicit prompt caching is already supported in Foundry. The second tier is Extended Prompt Caching. As workloads scale, whether longer conversations, richer media or larger data sets, the limits of GPU memory become apparent.

**[11:15](https://www.youtube.com/watch?v=50ZODW1SNOU&t=675s)** Extended caching moves the cache onto the GPU nodes local SSD, enabling hundreds of gigabytes of capacity and TTL of up to 24 hours. This is the cost efficient scale tier, still managed by the model provider, but allowing customers to retain context significantly longer. Extended prompt caching is generally available in Foundry. Starting May 2026. Customers can set the prompt cache retention option to 24 hours or leverage it by default on GPD 5.5 or newer model deployments. Both implicit and extended prompt caching, however, are node bound. If a request is routed to a different node, a different cluster, or even a different region, or if the TTL expires, the cache is lost and the full pay for compute cost is incurred. Again, this becomes a limitation if it's a long running

**[12:05](https://www.youtube.com/watch?v=50ZODW1SNOU&t=725s)** agent, global workloads, and scenarios where context needs to be reused across the enterprise. That's where explicit prompt caching comes in. Foundry addresses this gap with Azure Context Cache, a first class Azure resource purpose built for storing preprocessed context in durable storage within the customer's own tenant. Built on Azure BLOB Storage, Context Cache removes these size constraints TTL extending to days or weeks or even months, and operates independently of the nodes, working seamlessly across sessions, deployments, and regions. The API is owned by the inference layer, making it portable across models and frameworks. This is the ship we are driving. Prompt caching as a core system capability of the AI platform, not tied to where your request lands, but delivering

**[12:57](https://www.youtube.com/watch?v=50ZODW1SNOU&t=777s)** consistent guaranteed savings wherever the reusable context exists. Thanks Saloni. Now let's go one level deeper into the Azure context cache. At its core, it's durable storage of Model KV prompt objects inside the enterprise's own Azure tenant. The design of Azure context cache has three properties that make it enterprise ready. First, a provider only endpoints writes the encrypted KV state into the cache, the models IP or intellectual property. The attention representation is never exposed to the customer in clear text. Second, a customer or developer only endpoint that gives the enterprise full control over time to live or TTL retention, invalidation and metrics.

**[13:48](https://www.youtube.com/watch?v=50ZODW1SNOU&t=828s)** As an enterprise AI developer, you own the life cycle of your own cache. 3rd, it's no diagnostic and highly performant, delivering milliseconds latency and several tenths of 1,000,000 per minute of token throughput at global scale with a neutral API that integrates cleanly with Microsoft Foundry at the time of Microsoft Build 2026 and in future with ASK or Azure Kubernetes services and 3rd party frameworks as well. Thanks Saurabh. Let me bring this home with the outcomes that matter most to our developers. First, cost and efficiency. Explicit prompt caching improves cache hit rates and delivers durable cost savings on the cache portion of your prompt. For GPT 5 class models, this enables inference workloads to

**[14:38](https://www.youtube.com/watch?v=50ZODW1SNOU&t=878s)** scale economically. Second throughput. We support multimillion tokens per minute throughput for GPT 5 class models, providing the headroom needed to run high concurrency agent driven workloads without any bottlenecks. Third latency. Retrieving from context cache is orders of magnitudes faster than recomputing the full prefill translating directly into a step function reduction in TTFT or time to 1st token in multi turn scenarios. This also improves observed end to end latency as the system spends less time on the repeated prompt ingestion across the concurrent users. 4th security and control. This is a bring your own resource model with purpose built protection for your model IP and secure access all running in your own Azure tenant.

**[15:28](https://www.youtube.com/watch?v=50ZODW1SNOU&t=928s)** This also preserves the enterprise level access controls our customers are used to in Azure. And finally observability. You get rich cache diagnostics and fine grained TTL control so you can tune the cache behavior per workload and clearly understand its impact. We're opening this up for Gated Preview at Build 2026. If you're building long context or multi tone chats, long running agents, or even globally distributed inference workloads on Microsoft Foundry, we'd love to partner with you. Use this link on the slide to share feedback & up for the preview. We're excited to see what you can build with Microsoft Foundry and Azure Context Cache together. That is fantastic. Thank you, Saloni. Now let's go to Vishnu to learn how customers building

**[16:17](https://www.youtube.com/watch?v=50ZODW1SNOU&t=977s)** agents with their own open source tool chains or their own models can leverage Azure Storage for prompt caching. Thank you, Saurabh. Hi, I'm Vishnu Sharan, a product manager in the Azure Storage team focused on AI workloads picking up on explicit caching. The reason that tier works is the cache no longer lives on the GPU node, it lives on durable Azure storage, and once it's on Azure storage you get the full scale of Azure BLOB behind your KV cache, effectively unlimited capacity, configurable retention, and multi region reach. That same idea is also how we are meeting the open source community. Nixel is Nvidia's inference Transfer library, a unified API for moving model weights and KV blocks across memory tiers with a pluggable back end underneath. You can see those backends along the bottom of the

**[17:06](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1026s)** diagram. We've contributed a first class Azure BLOB back end into that room. So any framework that already speaks in Excel like VLLM, Dynamo, Tensor, RTLM array can now read and write KV blocks to Azure BLOB with 0 custom adapter code. The most immediate payoff is an LM cache. The open source KB cache layer widely used with VLLM to our plug in LM cache offloads KV blocks straight to BLOB, so the picture is consistent across the stack. Manage customers land on Azure context cache. Oasis developers land on Azure BLOB via Nixel and LM cache. Both parts put KB cache on durable Azure storage that travels with the workload, not the node. And with that, let me show you what this looks like in practice. Here's what we've got. Running an inference app on VLLM serving LAMA 3.370 billion tensor panel across all four GPUs on this VM.

**[17:59](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1079s)** Loading the model from a BLOB storage container. LM cache sits alongside VLLM. Building the KB cache. Now to drive it, I'm using a simple Python client that posts an open AI compatible request to that VLLM endpoint with a fairly punishing prompt. Roughly 100,000 tokens of context. And there it is, 52 seconds for the first token. That's the GPU's doing prefill from scratch. The long context goals start everyone's familiar with. Now let's run Excel again. Here's the config chunk size 256, local CPU tier off because I want Nixle doing the work, not a CPU cache layer. And here's the new piece. Nixle storage on back end set to Azure BLOB async puts so writes never block inference, plus the storage account and container we're targeting. Now I restart the LLM with the LM cache connector

**[18:49](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1129s)** enabled and fire the same prompt as token stream out. Nixle writes the KB chunks straight to BLOB in the background. We pay GPU prefilled once, but the cache is being populated. Quick look in the portal and there they are, around 1500 chunks, about 20 megabytes each sitting durably in the container. The full KV state persisted. Now for the real test. I restart BLM wiping GPU and CPU memory for a genuine cold start. Same prompt this time. Nixel streams those KB chunks directly from BLOB into GPU memory. Watch the network. Here's end load. We are pretty close to the line trade on this 80 gig Nic. The bottleneck just moved from GPU compute to network bandwidth. Exactly the trade we want and the time to 1st open about 8 seconds.

**[19:38](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1178s)** Just by streaming the KV out of BLOB, the model loads roughly 6 times faster. That's the Nixle BLOB plugin turning 100K token cold start from a 52 second wait into an 8 second stream. The Nixle demo you just saw was the inference optimization story. KV cache streamed out of BLOB, so cold start don't pay prefilled twice. But the KV cache is only half of the cold start story. Every cold start also has to load the model weights themselves from storage onto the GPU. For small models that's seconds, for the really large ones it's minutes. Now I want to focus on that half fast model loading and distribution. Same problem, same fix. Saturate the Nic straight out of blobs. The Run EI model streamer is an open source loader for large model weights.

**[20:26](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1226s)** It plugs straight into the engines people actually run in production. VLLM and SGLAC drop it in, point it at a storage account and it replaces the default loader. Gold starts on the tax every GPU base when it comes up. Auto scaling out, recovering a failed node, rotating a model, or loading a fine tune on demand with the default loader. That tax is unpredictable and minutes long on the big models, so the bigger your fleet, the more capacity you're burning just waiting for waits to land. Here's what Streamer does differently. It splits the model file into chunks, pulls them from object storage in parallel, stages them in CPU memory, and pushes those chunks to the GPU memory concurrently. The reads and copies overlap, so the nick stays saturated and the GPU is fed as fast as the network

**[21:15](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1275s)** allows. And here's the piece we built the Azure BLOB plug in for the running Ice Streamer waits flow straight out of BLOB through CPU into the GPU. The BLOB container is the source of truth and the Nic is the only bottleneck and we get 3 to 6 times faster cold starts than the default loader consistently. Let me show it running same VM as before ND 96 hedge 100. It has eight hedge hundreds on board with an 80 gig Nic on the back. The model I'm loading is GPD OSS120B, about 60 gigs of weight, pre staged as safe tensor shards in BLOB container. Same storage account, same plumbing as the Nixle demo, just a different artifact this time. Now I start VLLM standard VLLM serve command with one switch load format set to run EI streamer.

**[22:05](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1325s)** That's the entire integration. Point it at the BLOB container and the streamer takes over chunk parallel reads from blobs, CPU staging, concurrent CPU to GPU copies all overlapping. Now watch the Nic, about 86 gigabits a second on an 80 gig link. The Nic is fully saturated, the streamers pulling everything BLOB will give it, and the GPU's are eating it as fast as it lands. This is what loading at network speed looks like. And there it is. All 8 GPU's loaded. Application startup complete 60 gigs of weights into eight hedge hundreds in roughly the time it took to say this. One more cut at model loading, this time at AK scale. Everything I have shown so far has been a single part, a single GPU box. But production isn't one part, it's 100, and every one

**[22:54](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1374s)** of them has to pull the model down before it can serve a single token. If you run the numbers, A70B model is 140 gigs. 100 pots coming up together is 14 terabytes coming out of BLOB all at once. You hit throttling, the nick saturates and cold start stretch 10 minutes. Everyone of those minutes is GPU you're paying for and getting nothing back. So we build a distributed cache inside the a case cluster itself. Zero changes to the workflow on top, hugging face loader, BLOB fuse, the runny streamer, they all sit on top of it unchanged. Underneath every GPU node is both a cache and a client. Local Nvme raid holds the hot data served at Nvme speed with 0 copy send file. Anything missing comes in over the network from a peer with ingress acceleration on the fan inside.

**[23:45](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1425s)** First bod warms the cache. The next 99 read from inside the cluster. 2 1/2 times faster. Model loading on AKS. Let me show you. Here's the life comparison. 12 gigs of LLM waits loaded on a real cluster direct from BLOB. 1500 megabytes a second. 8 seconds to load with the distributed cache, 4000 megabytes a second. Three seconds. 2 1/2 times faster. And when 100 bots come out together, they all read from the cache. BLOB gets hit exactly once. That's Azure container storage. New distributed cache built for AI on ask. Distributed cache is an early access right now. Nvme class latency, throughput to local network, saturation, cubilities native and minimum to 0 changes on your site.

**[24:33](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1473s)** If you're running AI on ask, this is the one to try. Scan the QR, sign up for the early test, and we'll get you on it. That's the inference half of the story on compute. KB cache going out, the BLOB waits coming back in, both outline trade. But none of that runs without data. The model itself, the cache, the context windows, the retrieved documents, all of it lives in storage. And for inference, the bottleneck is in whether you have the data, it's how fast you can feed it into the GPU. So the question for this last section is simple. How do you get your data sitting in BLOB in front of an inference engine as fast as possible? That's other pillar, bringing enterprise data to AI inference and converting it to agent trading knowledge. So how do you actually get to that data from

**[25:23](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1523s)** inside your AI stack? That's what this slide is about. And this revolves around one key idea. We meet developers where they are with native integrations across the entire AI ecosystem. If you look at that list there BLOB, fuse and Arizona copy on the data movement side, ADFS, the Pytouch connector, the land chain data loader on the framework side, plus the two ecosystem integrations you just saw demo run AI model streamer and NVIDIA and Excel effectively whatever stack you're on, Python, Spark, Ray, Pytouch are an agent framework. You get the BLOB without friction. A few tools worth calling out here. Blobfuse gives you a POSIX like mount so you can treat object storage like a local file system. Same experience across VMS, containers and a case. ADFS is the Python native fspec compatible interface which makes

**[26:15](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1575s)** BLOB instantly usable from desk, pandas and rig for deep learning. The Pytouch connector plugs directly into data set, data loader and checkpoint APIs. No custom code 0 configured with Azure Identity. Then the performance critical parts for agentic and inference workloads we saw run a model streamer cutting cold start times, loading models from BLOB 6X faster. Nixel drops us straight into Nvidia's data movement stack with KV cache offload and high performance model transfers across Dynamo, VLLM, Ray and the rest. And the last one, the Langshain Azure BLOB loader. Faster ingestion and load memory footprint when you're working with large document corpora, 5 to 9 times faster lazy load, 7 to 10 times faster a lazy load against the community loader.

**[27:01](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1621s)** So BLOB isn't just scalable infrastructure underneath, it's deeply integrated into the AI developer ecosystem with high performance, secure, seamless access across training, inference and agent workflows. And that's the OSS layer. Saurabh is going to take it from here and show how Microsoft Manage Infrastructure brings all of this together so customers get there without having to wire it up themselves. Thank you, Vishnu. Fast model and enterprise data loading is exactly the kind of thing that takes GPU idle time off the table and improves the quality of agent take inference. Now let's come back to the other half of the agent take inference picture, bringing your enterprise data into the agent as knowledge.

**[27:49](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1669s)** In the past, we have shown how BLOB Storage plugs directly into Microsoft AI services like Azure AI Search and Microsoft Foundry to turn enterprise data into agent ready knowledge with just a few API calls. But many of you have unstructured enterprise data on Azure Files as well. File shares, SMB workloads, lift and shift estates, and you need the same ease of getting that data into AI. For that, we've made Azure Files a data source for Azure AI Search with two ways in which you can bring this data over to agents #1 the Azure Files Logic Apps Connector, a local path to connect a file share and create a vector or hybrid index. It, by the way, also handles reindexing as your data

**[28:39](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1719s)** changes #2 the native Azure Files indexer in Azure AI Search for teams that want finer control over enrichment and indexing. Either way, the path from a file share to a RAG or Retrieval Augmented generation Ready index is now only a few clicks or a few lines of code. And building on that, many of our customers are going a step further with Foundry IQ. Foundry IQ is Microsoft's unified knowledge layer for agents. Instead of every agent reinventing its own retrieval stack, Foundry IQ gives you a single governed knowledge layer that any agent in Microsoft Foundry can ground against. What makes this developer friendly is that Azure BLOB Storage

**[29:28](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1768s)** is a first class knowledge source for Foundry IQ. Just point Foundry IQ at a BLOB container and your enterprise data becomes agent accessible knowledge with security, lineage and governance of Azure Storage already in place and no glue code required. Foundry IQ also supports Azure AI search indexes with Azure BLOB or Azure Files data as well, and this is how we close the loop between your data plane and Azure Storage and your agent plane in Microsoft Foundry, going from raw enterprise data to grounded governed agents in just a few steps. To bring it home, we wanted to give AI developers and storage admin a single place to discover the AI

**[30:17](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1817s)** capabilities of Azure Storage and wire your data into agent inference. Fast Meet Storage Center, a centralized hub for file, block and object storage all in one place. You get a unified view of your storage landscape, usage performance, redundancy tiers, and tools like life cycle management and Storage Mover with rich visualizations that make it easy to stay on top of your Azure storage estate. What we heard loud and clear from customers, partners, and developers is that this is the best place for both admins and developers to land their data. Starting with BLOB and then connected to AI from Storage Center, you can launch into storage for AI experiences we covered today.

**[31:07](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1867s)** We are excited to announce a limited preview of this experience. Scan the QR code now on this slide to sign up. We'd love to have you in and try this out. So to bring it all together, we started this session with three storage requirements for agentic inference #1 bringing enterprise data to agents as knowledge #2 distributing and loading models fast enough to keep GPUs busy, and #3 optimizing infrastructure utilization for better tokens per Watt per dollar. Across the session, you saw how Azure Storage delivers on all of those 3 Azure Context cache for explicit prompt caching in Microsoft Foundry, now in Gated Preview as well the Nixel plug in that enables attaching BLOB storage to open source model run times for prompt caching #2 fast

**[31:59](https://www.youtube.com/watch?v=50ZODW1SNOU&t=1919s)** model loading with ASK and IAS with container storage, distributed cache and early Access. And finally, Azure AI Search and Foundry IQ to turn your enterprise data into agent ready knowledge and to bring it all together Storage Center as your unified entry point to leverage storage for AI applications. Here are some helpful references that you can refer to and supercharge your AI inference workload journey with Azure Storage from Saurabh Saloni and Vishnu. Thank you and we can't wait to see what AI applications you will build with Azure Storage.

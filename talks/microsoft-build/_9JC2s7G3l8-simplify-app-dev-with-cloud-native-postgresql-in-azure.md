---
id: _9JC2s7G3l8
title: "Simplify app dev with cloud-native PostgreSQL in Azure HorizonDB | DEM364"
slug: simplify-app-dev-with-cloud-native-postgresql-in-azure
conference: microsoft-build
conference_name: "Microsoft Build"
category: "Vendor & platform"
edition: "Build 2026"
year: 2026
speakers: []
channel: "Microsoft Developer"
duration_min: 26
published_at: 2026-06-04T13:25:22Z
video_id: _9JC2s7G3l8
youtube_url: https://www.youtube.com/watch?v=_9JC2s7G3l8
tags: ["17b83627-30f4-4447-9093-6446eef5d675_M9Z7-DEM364-1", "CP&D", "DEM364", "Data", "Maxim Lukiyanov", "Simplify app dev with cloud-native PostgreSQL in Azure HorizonDB | DEM364", "build", "build 2026", "m9z7", "microsoft", "microsoft build", "microsoft build 2026", "ms build", "ms build 2026", "msft build", "msft build 2026"]
transcript: true
---

# Simplify app dev with cloud-native PostgreSQL in Azure HorizonDB | DEM364

**Speaker not identified**

`Microsoft Build` · `Build 2026` · `2026` · `26 min`

`#17b83627-30f4-4447-9093-6446eef5d675_M9Z7-DEM364-1` `#CP&D` `#DEM364` `#Data` `#Maxim Lukiyanov` `#Simplify app dev with cloud-native PostgreSQL in Azure HorizonDB | DEM364` `#build` `#build 2026` `#m9z7` `#microsoft` `#microsoft build` `#microsoft build 2026` `#ms build` `#ms build 2026` `#msft build` `#msft build 2026`

[Watch the recording](https://www.youtube.com/watch?v=_9JC2s7G3l8) · [Conference site](https://build.microsoft.com/)

## Description

Enterprise developers creating AI‑driven applications face growing complexity as vector search, models, and retrieval pipelines sprawl across services. In this session, see how Azure HorizonDB streamlines the stack by embedding AI and search directly in the database. Learn to run hybrid vector queries, apply BM25 relevance, call managed AI models from SQL, and prototype agentic workflows—shipping faster with less architectural overhead.

To learn more, please check out these resources:
* https://aka.ms/build26/DEM364

𝗦𝗽𝗲𝗮𝗸𝗲𝗿𝘀:
* Maxim Lukiyanov

𝗦𝗲𝘀𝘀𝗶𝗼𝗻 𝗜𝗻𝗳𝗼𝗿𝗺𝗮𝘁𝗶𝗼𝗻:
This is one of many sessions from the Microsoft Build 2026 event. View even more sessions on-demand and learn about Microsoft Build at https://build.microsoft.com

DEM364 | English (US) | Cloud platform & data

Demo | (300) Advanced

#MSBuild

Chapters:
0:00 - Introduction to the demo: Simplify app development with Cloud Native PostgreSQL in Azure Horizon DB
00:01:31 - AI integration: Built-in models, pipelines, and functions for AI agents
00:05:00 - Provisioning Horizon DB with AI model bundles and vector search extensions
00:06:41 - Introduction to using embeddings for AI reasoning on furniture styles
00:07:20 - Overview of available models: chat, text embedding, and semantic ranker
00:13:43 - Simplified vector generation pipeline supporting incremental updates
00:19:05 - Visualization of query plan to understand internal execution and joins
00:20:08 - Applying semantic re-ranking for top 10 relevant results
00:20:57 - Transition to knowledge graphs as the next step

## Transcript

*3,683 words · source: supa (en, exact timings)*

**[0:22](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=22s)** Thank you, Chadora. Hi everyone. So this is a demo session called Simplify up dev with cloud native PostgreSQL in Azure Horizon DB. So welcome everyone. So today we'll talk about AI features of Azure Horizon DB. If you missed the announcement by Satya yesterday, let me give you a quick refresher. So Horizon DB is our new Postgres SQL database and just yesterday we announced public preview of it, which we are very excited about. So what we did we take, we took open source Postgres QL engine and connected it to cloud optimized scalable storage back end. And that allows us to achieve high resiliency, larger scale and better performance for Postgres workloads. So with Horizon DB, you'll find 3 times higher throughput

**[1:14](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=74s)** on transactions and three times faster vector search. You can also scale it to a very large databases, enable up to 15 read only replicas. So it gives you a very scalable, powerful engine. And on top of that engine as well, we've built AI features. So Horizon DB is designed for this new era of AI agents. So it comes with built in AI models, AI pipelines and AI functions. And this is what we are going to focus on today. And we'll test this claim of like having AI functions and AI features that simplify development of apps by building this agent today on the stage using Horizon DB features. So let's start from this demo application.

**[2:03](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=123s)** So this is the end state. This is the app that we are going to build today, or at least some key components of it. So what do you see? Here is a Zava room designer app that helps me design my room. What you what we have here is a photo of my Brooklyn loft with the first piece of furniture that I have this gorgeous Scotch. And I'll use this app to help me pick other furniture that fits this style and color palette of this room. So all I need to do is click design my Room and this app that we are going to build together, it's going to analyze the folder, determine the style and color palette, right? And scan the product catalog and pick the furniture that actually fits into the into my room, right?

**[2:53](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=173s)** And voila, the room is furnished. So no more kind of this very difficult choices, each piece of furniture to put, right? Everything is done for me and under the nice small budget. So fantastic. So you can see here all of the pieces of furniture selected, right? So on the surface this may seem a little bit intimidating or a little bit sophisticated, right? Seems like a complex app, but this cannot be farther from the truth. This app actually is intentionally designed to be very simple and we'll show it by building components of this app 1 by 1 to show you how this app was actually built and how we simplify development process with Horizon DB.

**[3:43](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=223s)** So the first step in in our journey would be connecting models, right? So if you want our agent to be able to reason right, the first step always is provisioning models, connecting them. All of this infrastructure setup, this Horizon DB. That's the first step that we get to skip. So instead of doing that, I will just show you how you provision Horizon DB. And as part of that process, we'll actually bundle models into the database. So the database comes already with all of the model connections provisioned. So what I'm going to do, I'm going to click this button, and throughout this demo, I'll actually use this Postgres extension for this code. So this is our product.

**[4:30](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=270s)** All of the experiences that you'll see is actually based on that. I highly recommend you use it for your Postgres work. And in this new server screen, we have a new entry provision Horizon DB, right? So I'll click it. I'll log in into my Azure portal using my credentials and then I will specify details of my cluster, right? So I will I will name it my Horizon DB2. I will provision a little bit more compute cores. I will select my user and this is where I can bundle my AI models and AI related features. Here just a simple check box which activates model management and PG vector and disk and N vector search extensions

**[5:23](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=323s)** for Postgres, right? So just like that I am ready to provision my Horizon DB with all of the AI enablement kind of models already provisioned together with it. So we want to wait for this process to complete. For this demo, we will use and it will run in the background. But I will and I just almost clicked it just in time something didn't go as well. But I have my pre provisioned Horizon DB for the demo ready. So we can just jump right into this one, right? So the first step or the next step in our app development journey, right? So now we have Horizon DB with AI models enabled. So second, let's prepare data right for for our from

**[6:14](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=374s)** our product catalog for AI consumption. So for this, let's first take a look at at the data that we have. So here is a product sample table. You can see it has all of the kind of product catalog related details here, right? But what it doesn't have, it doesn't have embeddings, right? And the embeddings are important for our model, for our model, for our app to be able to reason which pieces of furniture semantically kind of fit into the style right of the room. So we need embeddings almost always when we build AI agents right? So let's add embeddings to this table. Actually we will do a little bit more sophisticated than that.

**[7:01](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=421s)** So what I do have though in the database, as I mentioned, we have pre provisioned the models, right? And in SQL I can actually inspect the model registry and see what kind of models I have here, right? So now you can see 3 managed models were provisioned automatically with the database. I have a generic chat completion model here. I have text embedding model, right? And I also have a very powerful model which is semantically ranker model that we will use later that allows us to improve quality of search results. We also support if you have your own enterprise models, right? We also support bring your own model feature where you can connect your existing models to the database as well. OK.

**[7:52](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=472s)** So now we have the models and the next step we'll we'll create this AI pipeline that takes our product catalog data, right, and generics embeddings for them and all in the database, right? So this is a full definition of the AI pipeline. So we say, hey, from this product sample table as a source performs these steps and this is a simple sequence of steps, right? First we'll chunk the data, which is important for accuracy of our vector search later, right? So we need to split it into little pieces. So this is a chunk function. And then we'll we are going to for each chunk, you are going to generate embedding, right? So this is how I specify that step. And that trigger is important element here as well. This pipeline not only will process my existing data and

**[8:40](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=520s)** product sample table, but it also connect a trigger to it. So any changes in my product catalog will automatically go through this pipeline. So it will incrementally keep updating the data and generate new embeddings. So let's run it. OK, pipeline created successfully. I can see the status of my pipeline using SQL as well. So here is the status and if you look here at this total processed items, if I keep running this status query you will see it's increasing or at least it should increase. Or maybe not.

**[9:35](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=575s)** OK, let's pretend that it's increasing. If not, it will be interesting to see what's happening. But in in addition to the SQL status of the pipeline, I can also use UIS that we've developed for this in this extension. And I can look at the pipeline status here. So I can I can just open the pipeline window and see visual representation. So that's a backup embedding. Let's see this is the green still. OK? And this is our new embedding pipeline that we've just created. And here is a kind of a diagram of everything

**[10:23](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=623s)** that is happening. OK? So you can see that we have. So let's close this one. Stand this a little bit. You can see what's going on here, right? So we have our source table which has 60 rows, right? So this is all of the rows that we have here. Then chunking process splits each row into a few or more chunks, right? And the generates 116 chunks. And then embedded embedding step talks to the Microsoft Foundry model, embedding model that we pre provisioned with the database, right? And generates 116 vectors, right? So all of that is pretty good. So if you look at the output of this final sync step, right, we can see the table here, the output table is embedding pipeline output, right?

**[11:16](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=676s)** So let's see what that table has if our pipeline succeeded. So this is our pipeline output of the pipeline table. So let's see how many rows. Yeah, so it actually did succeed. So our status now if you run it again shows 116 rows process. So it was just a little delay in our demo. So that is awesome. And now we can see the output of this MBD pipeline right table. So here you can see the chunks that were generated and more importantly, the embeddings, right? So now we have our product catalog fully vectorized.

**[12:07](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=727s)** So it's ready for our vector search to find relevant items. So let's do, let's do just that and demonstrate a very simple query a little bit bigger that performs vector search. So if you're not familiar, in Postgres, this is vector search operator, right? So we are performing vector search across our output table and trying to find best chairs that is comfortable. Comfortable right? And this is the results. So some chairs were returned. So that's great. But what if you want to change the data, right? So let's add a new item into our product catalog product sample table.

**[12:55](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=775s)** So we'll just insert this new row. And very quickly, if you look at the count of the rows in the embedding pipeline, we see that it's already 117, right? So the pipeline was watching in the background and asynchronously without blocking my actual insert, right? Because we want to keep our database responsive, all of the inserts happen very quickly and pipeline asynchronously bug processes all the new data. So it did process that very quickly. And now in our output table, we have 117 items. So now if you repeat that vector search, you can see, you can see that the newly added item is already in the results of the vector search, right? So that gives you a very simple way to build

**[13:46](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=826s)** this vector generation pipeline without any kind of steps covering all of this complicated use cases like incremental updates, all of that. So all of this complexity is taken away. Great. So how are you doing on time? You're doing great. So that's first step, right? So we prepared our data. Now we can start searching over it and we'll search in a very scalable manner and in a very sophisticated manner as well. So let's do a retrieval step. So in order to enable retrieval of data right from our now vectorized product catalog, what we want to do, we want to be we want to achieve high accuracy of the results and we want to make the search

**[14:38](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=878s)** very fast, right? So in order to achieve both, we will enable two types of searches. So we will do hybrid search. So first we will enable full full text search extension on our database and we'll create full text search index on our product catalog. So this is the new PGFTS extension that we've enabled on Horizon DB, which basically brings a Lucene type functionality to Postgres. So now in Postgres we can do full text search. That's awesome. And we also provision here our disk NN vector index that gives us better performance compared to PG vector, right? So it's a very fast vector search. So now we have created this indexes, we can perform vector search and to simplify it even faster, we created

**[15:29](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=929s)** this simple function that allows you to perform pretty sophisticated operations with just this very simple syntax, right? So what we are searching here is this string that kind of describes the style of the photos that you saw right? So you can see here we in the prior step in the app from the photo, we kind of deduced the description of the room, right? So we are skipping that for the sake of this, for the sake of time. But this is the output of that step. So now we have textual description of the room, right? So how to describe the style? Then we specify the source table where we are going to search, right, the column and an important parameter, a type of search. So now we are specifying hybrid search. We can do vector search, we can do full text

**[16:18](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=978s)** search. But hybrid combines both. So it kind of gives you strength of both. So we are doing hybrid search here and then just join some details. So we have a nice output. So let's run this and here you can see the results right? So some results were returned and interesting part of this about this result is the influence of hybrid search brings into our search results. So if you look at that search string, right, you can see here the term mid century modern, right? And the top result in the hybrid search results is mid century model. So it's a keyword match, right? So that's what you get when you do a hybrid search.

**[17:05](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=1025s)** When you combine full text search results with vector, you get kind of the relevance of the exact match of the keywords kind of brings the results up, right? So if that's what you want, the hybrid search is really good for you. And there are use cases where keyword matches are important, right? But in our scenario, I think more important is kind of a style and semantic matching, right? So let's improve on that. So to do it better, we are actually going to enable semantic re ranking. And to do that it's just another parameter on our search function. We'll say, hey, do re ranking equals true, right. So let's do that. And semantic re ranking will will significantly prove kind of relevance of our results from the semantic point of view, right.

**[17:54](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=1074s)** So it uses as a here semantic ranking model behind the scenes. And now as you can see, the the results are slightly different Coffee tables here are kind of found more relevant to this query result, probably because our search stream includes wood tones and dark vibes, right? So coffee table has kind of a more surface, I don't know. So that's how you easily you can enable semantic re ranking and improve accuracy of your query results, right. But that was that simplified function may seem like kind of a pack or like a black box, right? So how do we understand what's happening inside of this search function?

**[18:42](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=1122s)** So this is another feature that we can demonstrate. So we have here visualize query plan button and that allows us to look inside of any queries that we are running, right? So I'll do that and create a visualization of these queries that we just run. And here we go. So let's zoom out a little bit. So this is pretty large query, right? But we can safely ignore this part of the query. This is just our joins where we format the output, right? So let's ignore that part. So this second part is actually our search function. And if you zoom in on it, we can see exactly the structure of what is going on behind the scenes right?

**[19:29](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=1169s)** So let's further zoom in. So we can see that the query starts with two steps. So first it does a full text search and it performs vector search in parallel, right? So that gives you 2 lists of items with different priorities. And then what we do as a hybrid search, we do reciprocal rank fusion. So that's basically a formula here, very simple actually mathematical formula that kind of averages out the 2 results, right? So it allows you to take 2 ranked results and merge them together, averaging the out the rankings. And after that, because we enabled the semantic re ranking, right? So we perform a call into a semantic re ranking function to take the 30 candidates and reorder them now

**[20:20](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=1220s)** in the order of semantic relevance, right? And finally, that allows us to take out of the 30, we take the top ten that are the best matches right? And because we requested top 10 results, we return the top 10 results. So that's kind of the interesting way how our Postgres tooling that kind of has this query plans allows us to understand what's going on behind the scenes. So that's how this intelligent semantic ranking search was done and we are doing good on time. So the last step I wanted to show is a very powerful step. Knowledge graphs, right? So the, the heart of the intelligence of this agent of this app is being able to match styles, right?

**[21:10](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=1270s)** So look at the photo and then determine what is the stylistic kind of matching between this piece of furniture. So what we did, we created a knowledge graph of styles for this product catalog, and in the interest of time, I'll skip that step, but you're welcome to look at the repo. It has yet another AI pipeline and SQL scripts that actually builds a knowledge graph, and it stores this knowledge graph again in Postgres in a real graph manner. So we use Apache AGE Extension for Postgres, which basically brings a graph capability, graph database capabilities on top of your relational Postgres SQL. So that allows you to run this cipher query. So if you look at these queries, so this is how a cipher query looks like.

**[22:00](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=1320s)** So it has a bunch of matches. It allows you to traverse relationship in your graph, right? So very cool. But let's run it and see the results. So if we run this graph query, what we see is visualization of the graph because that's what we have in our tooling as well. And if we zoom in a little bit more than for, then in the middle of this graph you can see a style mid century modern style, right? And that style connects coffee tables. So here is this one coffee table, right?

**[22:49](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=1369s)** So that's a category coffee table. So it connects this coffee table to various other items, right? So it connects to chairs that have similar style, and it connects this coffee table to bookcases, right, of the similar mid century modern style. So that's kind of the basic idea of this style graph right? It allows you to connect furniture, these styles together. But it's kind of simple, right? What happens if there is not enough furniture of particular style, right, that matches your own well, styles actually similar to each other. And that knowledge we expressed in the knowledge graph as well. So we can actually go better do better than just

**[23:40](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=1420s)** showing connecting styles, connecting furniture based on styles. We can also introduce this relationship similar to between two styles right? So our mid century modern style is similar to Bahamian style right? And that knowledge expressed in our knowledge graph allow us to again traverse from this chair and connect it to a similar area rugs to a similar area rugs and basically fulfill our room desired room with a much broader variety of furniture options, right? So that's kind of how knowledge graphs work. They are very powerful tool applicable to almost any domain, right?

**[24:27](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=1467s)** And they frequently kind of encapsulate this domain knowledge that you have, right? So they are very powerful for enhancing intelligence and quality of operation of your agents. Cool. So that's all I had to conclude. Let's go back to the to our app and see what what was actually what it did right, what we did. So if you look at the agent details in this demo, we were able to prepare our data right for agent consumption, for AI consumption using AI pipelines. We were able to perform powerful hybrid search with all kinds of sophisticated accuracy options. And we were able to use semantic re ranking, right to pick the the best rooms related to our to

**[25:19](https://www.youtube.com/watch?v=_9JC2s7G3l8&t=1519s)** our room. And of course, we find related products using knowledge graphs, right? So all of that functionality we were able to achieve just using built in Horizon DBAI features. So no external services, no complexity, right? And very few lines of SQL if you prefer that. If you're interested in trying it out yourself, here is the repo with all of the source code and sample queries right? It has only 60 rows as a sample data set, but it's enough to kind of illustrate how this how simple actually behind the scenes this app is. And that's it.

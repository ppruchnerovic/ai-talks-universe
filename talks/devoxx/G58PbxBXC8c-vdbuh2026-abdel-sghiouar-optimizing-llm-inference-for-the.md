---
id: G58PbxBXC8c
title: "[VDBUH2026] Abdel Sghiouar - Optimizing LLM Inference for the Rest of Us"
slug: vdbuh2026-abdel-sghiouar-optimizing-llm-inference-for-the
conference: devoxx
conference_name: "Devoxx"
category: "General software conferences"
edition: "Devoxx"
year: 2026
speakers: ["Abdel Sghiouar"]
channel: null
duration_min: 38
published_at: 2026-05-13T03:37:51Z
video_id: G58PbxBXC8c
url: https://www.youtube.com/watch?v=G58PbxBXC8c
youtube_url: https://www.youtube.com/watch?v=G58PbxBXC8c
tags: []
topics: ["Inference, serving & GPU infra"]
transcript: true
---

# [VDBUH2026] Abdel Sghiouar - Optimizing LLM Inference for the Rest of Us

**Abdel Sghiouar**

`Devoxx` · `Devoxx` · `2026` · `38 min`

[Watch the recording](https://www.youtube.com/watch?v=G58PbxBXC8c) · [Conference site](https://devoxx.com/)

## Description

Not every organization operates with the hyperscale resources of Anthropic, Google, or OpenAI. For the majority of businesses integrating Large Language Models (LLMs) into their critical paths, the high costs and scarcity of GPU/TPU accelerators present a significant challenge. Striking the balance between performance, availability, scalability, and cost-efficiency is a must.

While Kubernetes is a ubiquitous runtime for modern workloads, deploying LLM inference effectively demands a specialized approach. This session dives deep into practical strategies for optimizing your Kubernetes clusters and LLM Inference workloads to run efficiently and cost effectively. We will explore:

– Container and Model Optimization

– Accelerator Management

– Data & Storage

– Network & Load Balancing

– Observability

Attendees will leave with practical techniques for maximizing cost/performance for LLM inference for their AI-powered applications on Kubernetes.

## Transcript

*7,579 words · source: supa (en, exact timings)*

**[0:09](https://www.youtube.com/watch?v=G58PbxBXC8c&t=9s)** Hi everyone. Thank you for coming. I would normally say this is the end of the day. This is the before last session. I know that you are all tired, but you don't have an excuse because you had a coffee break. So I have you have enough coffee to keep you awake until you know, 45 minutes from now. My name is Abdell. I'm a developer advocate. I was I was told that during the keynote or the opening session they talked about some dude coming from Las Vegas. That's me. I don't actually live in Las Vegas, but I was in Las Vegas last week and I flew back on Sunday back to Stockholm where I live and then I flew here Tuesday. So I'm still adjusted. So if you if you hear me snoring during the session, maybe throw something at me. I'm just kidding. So I work at Google and at Google Cloud specifically. And one of the things we do is we do have a cloud provider which has quite a lot of unlimited resources.

**[0:58](https://www.youtube.com/watch?v=G58PbxBXC8c&t=58s)** I like to say that I have unlimited access to GPUs virtually, which is not the case for everyone. Hence why we named this session or I named this session optimizing large language model inference for the rest of us. So if you're not using Google Cloud or if not AWS or Azure, but you still need to do large language model inference on prem for whatever reason or on your laptop, what are the things that you need to look at in terms of optimizations? And this talk is mostly focused on the open source side of things. Although Kubernetes is both an open source project and also a managed solution in a lot of cloud providers, but not everybody uses cloud and most people actually might use on prem with something like OpenShift. So pretty much all the learnings from this talk should be applicable to anything that has to do with the cloud native ecosystem. Um

**[1:44](https://www.youtube.com/watch?v=G58PbxBXC8c&t=104s)** Before that, can you raise your hand if you used any of these tools to run large language models like Ollama, Docker model runner. I don't know if you are aware of Docker model runner. Okay. Hugging Face maybe. Ollama Cloud. All right. Few people Stefan mostly. But the joke here is that if you did thank you, you don't need me, but clearly you don't because only one person raised their hand, so I'm just going to continue with this with this. So I don't need to repeat this, but models are getting more complex, right? And by more more complex it it it it means mostly two things. They're getting more complex in terms of the size of the neural network and the size of the neural network of a model has a direct correlation to the size of the model itself. Mainly the size of the weights, but for the purposes of simplicity, we're just going to say the size of the model,

**[2:31](https://www.youtube.com/watch?v=G58PbxBXC8c&t=151s)** which is the file itself. But they're also getting more complex in terms of how they function. They are supporting more than just text. They are supporting multiple types of inputs multiple inputs. They have sometimes very complex architecture. I don't know if you know, but there is if you have heard there is this new architecture now which is very popular called MoE, mixture of experts, where you have a single model that orchestrates inputs and then split that into multiple small models running inside the same big model. It's if you want the details, it's mostly one big neural network that can be activated and deactivated depending on the task, right? The thing with this kind of architecture with mixture of experts is that to make the mixture of expert models run fast, you still need to load the entire model into the GPU memory, right? You can just like load and unload because loading is

**[3:20](https://www.youtube.com/watch?v=G58PbxBXC8c&t=200s)** the thing that takes actually most time. And I'm going to get into details. These are some examples of the models that exist today that are quite huge. I have to update this slide because clearly deep seek there is newer versions. They just released version four last week. Gemma four is also getting big llama Kimi 2.6 which is recently seems like what everybody swears by for coding specifically ignoring the fact that you need a pretty big GPU actually to run it if you want to run it locally. But but the models are just getting bigger and bigger. Before I move on, I want to spend a little bit of time explaining some concepts that I'm going to be talking about during the rest of this presentation. And mostly because few of these terms, a lot of people seem to just know them because they have heard them before, but they don't really know what they mean. So everybody knows what

**[4:07](https://www.youtube.com/watch?v=G58PbxBXC8c&t=247s)** large large language models are. Inference, I'm going to talk about it in detail, but inference is actually a lot of people say inference is the is when you send the model data and then you get back a response. It's slightly more complex than that. I'm going to explain it in a second. Serving or serving engine. Serving engine is the piece of software that loads the model into the memory and gives you a standard interface. So that's Olama, vllm, hugging face, all these tools that you probably have to interact with. That's the thing that basically understand rest API and then translate that into something that the model understand and then return the results, right? Accelerators are basically GPUs or TPUs if you're already in Google Cloud. Quantization is a very important um aspect. Quantization is basically a floating point that defines the precision of the model. Every model gets released at normally full quantization, which is typically 16 for 32 for the

**[4:55](https://www.youtube.com/watch?v=G58PbxBXC8c&t=295s)** most complex model, and you can actually quantize the model to drop the precision. The lower the quantization of a model, the lower the precision of model becomes, but also the lower the memory footprint. So a lot of times actually when you're using one of these large language models, if you don't need it to do very complex task or you don't want it to be very precise, you can still run the model but at a lower quantization, which means it will require less memory to run. Weights are the numerical values that the model downloads. Those are the neural network definition essentially for the transformers architecture. Context window is is the important part. That's essentially how many tokens a model can ingest in one go. So why does it that that's important? Well, for coding tasks, you want a model with a relatively good context window so it can ingest your prompt but also a bunch of like all the context, all the files it might need to look at to make

**[5:43](https://www.youtube.com/watch?v=G58PbxBXC8c&t=343s)** the code suggestion. Um And then there is multi-modality, which basically means that the model itself can actually uh run can understand more than just text. It can understand text, audio, but maybe video. These two concepts are to each other. If you want a model that can process video, you need a model with a relatively high context window. Roughly, when you are doing model that can understand video as an input, roughly every frame in the in in your video will translate into like 100 tokens, roughly, right? Um so, there is multiple techniques for using models to process videos without using too much context, but I'm not going to go into details here. And when you run large language models, you are probably going to be in one of these three situations. You're either going to run on a single host single accelerator, so one VM or one laptop, one GPU. So, for most of you who run on Ollama, that's basically where you are.

**[6:31](https://www.youtube.com/watch?v=G58PbxBXC8c&t=391s)** You're just running single accelerator on your laptop with a single host, which is your laptop. Single host multi-accelerators, where you might have a server with multiple GPUs, and then you have to shard the model across all the GPUs on that particular server. But there is actually physical limitation in which you will get at some point where you can't just get more GPUs or more accelerators into the same host, which means you have to split the host. But the splits the model across multiple hosts, and that's where things start getting more complex because if you want to shard the model across multiple hosts, you need high-speed network connectivity between the hosts, right? For context sharing between the the the different hosts. Um so, if you probably have seen there is this uh open-source project on the internet called Axo, and Axo is what it allows you to do, it allows you to run multiple MacBooks or Mac minis or MacBook Studio or whatever, connect them over

**[7:17](https://www.youtube.com/watch?v=G58PbxBXC8c&t=437s)** Thunderbolt, so daisy-chain them, and then split the model across um all these these these nodes. Um on a physical server infrastructure, that might be something like Mellanox for those of you are familiar with infrastructure, which is like high-speed network connectivity between the between servers. So, I'm going to spend a moment to to explain what inference is. Um you can you can actually visualize inference as sort of like a pipeline within the model. When a model is performing inference on an input, it's actually doing multiple things. It's not just a single operation. And the two main categories of processing that the model executes is what we call pre-fill and then decode. So, pre-fill is when the model takes your input and load it into memory. And then decode is when it processes the output based on the inputs that you passed it into memory.

**[8:04](https://www.youtube.com/watch?v=G58PbxBXC8c&t=484s)** These two operations have actually completely different compute characteristic characteristics. The pre-fill phase is usually compute-bound. The CPU is the factor that limits how fast your model can perform pre-fill. And then decode is memory-bound, right? And so, why this is important? Because in certain situations, actually splitting these two phases of the pipeline into specialized hardware, one can do pre-fill very well because it has the hardware that is specifically designed to do pre-fill and one that can do decode very well is actually ideal. Um and how do you actually identify which one is the bottleneck if you're running inference on on a large language model is the metrics you see on the bottom. Pre-fill, the the the faster the model can be can load the tokens into memory,

**[8:52](https://www.youtube.com/watch?v=G58PbxBXC8c&t=532s)** the faster it can start producing the first tokens. That's where we look at metrics like time to first token. And then the memory side, um the the bigger is the memory, the more tokens can be output. And that's why we look at the time per token output, TPOPT, right? And then there's other metrics, but these are usually the two big metrics that you will want to monitor to look at where a bottleneck might be [snorts] coming from your system that you're performing uh um inference on. Okay? Um So, probably you have heard of something called KV caching. You probably have heard of this before, like key-value cache. So, key-value cache is essentially the when a model performs pre-fill, it loads all the tokens into memory and that becomes a cache. So, that's both used by the decode phase to to generate the output tokens, but it's

**[9:40](https://www.youtube.com/watch?v=G58PbxBXC8c&t=580s)** also sometimes used by the model to not reproduce the same um uh output again. Or it's used sometimes by the model for context purposes. So let's say you are doing a chat with the model, you send the first request that is pre-filled, the request is loaded into memory into memory, then the decode phase, right? Then the next time you ask a follow-up question, the model doesn't have to perform pre-fill again cuz it already has the previous question cached in memory. So it will only pre-fill the new the new inputs, the new prompts, and then from there perform the the the the the the the decode phase, so producing the outputs right? So, to run models, basically you're going to be in one of these three situations, right? All the way on the left, you're going to be using a virtual machine, a bare metal server, your MacBook, your laptop, your brother's MacBook, a a

**[10:28](https://www.youtube.com/watch?v=G58PbxBXC8c&t=628s)** Raspberry Pi, whatever. Like something that you run yourself. Uh or something you run in the cloud. On the right side, you might be using like a a managed solution by one of the cloud providers, Vertex AI, Bedrock AI, Azure AI, one of these solutions. Or now one of what we this this new cloud providers that are specially called neo clouds or uh specialized cloud. So that would be like CoreWeave, Inno Scalar, these are all cloud providers that are specifically targeting AI workloads, right? The middle is where Kubernetes kind of fits. That's where you get like the most control, but also the most operational overhead. So that's where you have to do everything yourself, but you also get the most kind of ability to tweak. And I was having a conversation earlier about uh about this. Um from my perspective, the way I see it when people say I'm running models locally, um I'm usually

**[11:17](https://www.youtube.com/watch?v=G58PbxBXC8c&t=677s)** putting it in one of the two categories on the left side. Where local might actually not necessarily mean on your laptop, on on or on a a gaming server that is runs on your in your home, right? It might be local as in it's local to my company. So it's centrally run by my company somewhere in the cloud or somewhere in on prem, but then it's only accessible by the team that is part of that company. So, that's what mostly when people say local. As opposed to remote, which is you're already using a remote API like Gemini or Anthropic or Claude or whatever, right? So, just keep these two two things in mind. And then why? Why would you want to run models locally, right? Um I think Brian touched on some of the stuff earlier. Privacy and security is a big one. You don't necessarily want to just send a bunch of sensitive data to a remote model because you don't trust the cloud.

**[12:03](https://www.youtube.com/watch?v=G58PbxBXC8c&t=723s)** You don't trust Google or you don't trust Amazon or you don't trust any of these big US companies. Regulations is another one. There is There is a regulator forces you not to be able to do that. Latency. It's actually something that we don't really think about that much. It's kind of weird to me that that we have to repeat this again in 2026. Couple of years ago, we used to tell people you have to run your database as close to your application as possible to avoid latency. Now, you have to make sure that your application and your large language model run as close to each other as possible. The latency sensitivity in this particular case is not as big as the the example of database with application because models are slow by definition. So, probably the latency between the app and the model is not going to be perceivable by the end the end user. But in lot of cases, when you're running really small models that are very fast, then the latency might be apparent if it

**[12:51](https://www.youtube.com/watch?v=G58PbxBXC8c&t=771s)** takes like a long time for the app to send requests to the cloud and back, right? Special use cases, the cloud hosted models just doesn't fit your use case. They're mostly trained to be generic. Um but they might not be trained on health care or, you know, you might need to take a model and then fine-tune it to your particular use case. Privacy, we talked about it. Cost is another one. This seems to be the defining criteria today in 2026 because most of the cloud hosted models, you're just paying uh token well, cost per token. Um um or you are paying a subscription, but you are basically under the mercy of the cloud provider changing the terms and just removing the the the the model from that particular subscription you're paying. I mean you you all see Anthropic started experimenting with removing Claude from the $20 subscription. Now it's only available in the $100, but who knows in

**[13:40](https://www.youtube.com/watch?v=G58PbxBXC8c&t=820s)** 1 month from now they will realize oh this is not actually um commercially viable for us and then they will remove it from that tier and then make it available in the highest tier. Um virtually every single AI provider today runs inference at a loss. No one is making profit from inference. So either you are the case of big companies like Google or Amazon where you have the money to afford the loss or you are burning VC money. No one is making is making money from this today, right? If you look if you are a developer using Anthropic like extensively, they're probably losing more money than the $20 they're paying them per month. And that's good. Keep using Keep doing that. It's good, right? But like I'm just saying that like cost for me is probably the the the the main defining factor for why people might want actually to run models themselves instead of just you using a cloud-hosted model. So where does Kubernetes fit in all of

**[14:28](https://www.youtube.com/watch?v=G58PbxBXC8c&t=868s)** this? Well, it's it's a flexible platform. Um it it's basically an orchestrator on top of computes. So yeah, you can do VMs and you can do Ansible and Chef and all this stuff, right? And if I wasn't working for Cloud and uh Google Cloud I wasn't like um doing what I'm doing right now, I would probably be happily doing virtual machines with Ansible and Chef because that's what I came from. But here we are. We live in 2026 and we have to care about Kubernetes. Um and then efficiency is the other one. You know, the the system will like Kubernetes gives you automation. So automatically restarting workloads when they fail, automatically migrating workloads to to other parts of the system when they fail. Uh scalability is another one. I get more requests, I run more instances, I have less requests, I'll have less instances. It's a single configuration way. You don't have to do anything. Um and then there is Kubernetes as a project have initially been designed 11

**[15:17](https://www.youtube.com/watch?v=G58PbxBXC8c&t=917s)** years ago really to run back-end systems, right? But over the last 3 years the ecosystem or developers on the open source side have been actually working toward supporting AI workloads more and more. And I'm going to talk about a couple of features in a second. One of them is actually advanced device management, which is like managing the accelerators within the node. So, today actually with Kubernetes, you don't really have to do extra automation steps. You can just install a driver, and then Kubernetes will automatically run the model on top of the GPU. You don't have to do anything manually yourself. And then it's multi-cloud. That's probably the main advantage, which is it's a single abstraction layer on top of infrastructure. It doesn't really really matter what this infrastructure is. Could be on prem, could be your laptop, could be Google, Azure, Amazon, whatever, right? You can just run it on top because you're using standard API. So,

**[16:05](https://www.youtube.com/watch?v=G58PbxBXC8c&t=965s)** a lot of So, one statement I will I will say now, I'm going to talk about it now. Large language models are not your typical kind of workloads. If you're thinking about typical workloads, which are web workloads, right? Like web applications, backend API driven applications, they have a very specific characteristics. And as an industry, we have been working over the last how many years to optimize those things for production. So, we know how to run them fast, we know how to monitor them, we know how we know how to observe them, we know how to, you know, optimize them, etc., etc. And one specific thing that is kind of mind-blowing to me is when containers showed up about 12 years ago, we've been working toward making the container sizes as small as possible, right? Multi-stage build and all these like crazy mechanisms and GraalVM native Java applications and all this stuff. And then models showed up and they blew

**[16:54](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1014s)** all of these techniques out of the water because none of this stuff works anymore. Why? Um I'm going to go I'm going to jump ahead a second and then go back. Here is a snapshot of image size for some of these popular uh uh engine like a serving engines. And on the right side, you see the average image pull time if you're running this thing as a container image. So, how long it takes you to pull that container image. And you can see on the image size column that roughly the smallest one is roughly like 4 5 GB. So, just the image size. That doesn't contain the model, just the runtime. Just the Think Think like you just imagine if if the first line was your JVM. Just the JVM was like 6 GB. And then you still have to dump the binary of your application inside. Imagine how long it

**[17:41](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1061s)** will take that particular image to be pulled. And the reason why this is important is because for large language models, there is actually multiple layers. There is the model itself, but there is the server engine it runs on, and then there is the the the the accelerators it runs on. And each of this layer could be optimized separately. And then on the other side, you also have the horizontal optimizations. So, things like that like how fast can I scale out and scale back when I have, you know, more traffic toward my application. And I'm going to talk about each of these layers separately. So, these are some of the common models I talked about server engines I talked about it like vLLM, Hugging Face, Ray, Ollama, Deepstream. No Names are not important. All you have to care about is that they are quite huge. And so, in a Kubernetes context, what you're going to be typically doing is

**[18:28](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1108s)** you're going to be running one of these container engines, and then you're going to be giving it a model ID to run. So, it will spin up. The container will start, and then it will have to download that container that model from somewhere, typically Hugging Face or Kaggle, and then run it for you. And as you can imagine, if one of these things is like 5 GB, and the model is let's say 10, that's like couple of minutes to start the container for a couple of minutes to download the container, start it, couple of minutes to download the model, load it into the GPU memory. We're talking about maybe 5 to 10 to 20 minutes end-to-end between I start a workload and then the workload becomes live and I can talk to it, right? So, how do we solve this? And uh yeah, these are um this this table basically shows you

**[19:16](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1156s)** GPU memory needed depending on which model you are running and then on which quantization you are running on. And I just want to show you an example here, which is DeepSeek. So, at full quantization, this is DeepSeek version one. At full quantization, you need roughly 1.37 TB of memory. If we look at the right side, what you see over there is an Nvidia A100. So, that's couple of week couple of months ago, I would say a year ago, that was the biggest GPU you could buy. Now, you could buy something which is like twice that size. That's like roughly 80 GB. But you can clearly see, I cannot fit this model at full quantization on that GPU. I cannot even fit it on a higher GPU, right? So, what I need is multiple GPUs and then maybe multiple hosts. So, so hence why I was talking earlier about like multi multi host multi GPUs,

**[20:04](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1204s)** right? So, couple of solutions. There is a one of my favorite projects, it's pretty cool. It's a project called Spiegel. So, Spiegel, what it does, it's a Kubernetes caching um uh tool which does distributed caching within Kubernetes itself. So, typically in a Kubernetes scenario in a Kubernetes architecture, you have your nodes and then you run your workload. Your workload gets scheduled on a specific node and that that node has to download the container image, download the model, and all this stuff. But with Spiegel, you can actually cache all of this on the cluster itself. So, that's the node that has to pull an image, instead of going to pull it directly from registry, which is usually hosted somewhere else in your system or in the cloud or whatever, it will first check if other nodes have it. So, it's acts as a distributed cache. Right?

**[20:51](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1251s)** A second one is this new thing that is available now in Kubernetes 1.35. And I'm I'm I'm talking specifically about Kubernetes features, but like if you don't know Kubernetes, it's fine. Like the the the whole concept is the whole idea here is to learn about the optimizations, right? So, at the storage layer, there is actually you have the possibility to use a container as a volume. So, what you could do is you could package the model itself as a container and then attach it to the container that runs the server engine. So, you're running vllm and then you just say, "Okay, mount this other volume." But, our volume is just a different container. And if you combine this with Spiegel, you basically have a distributed cache for both the container engine which runs Sorry, the container which runs the server engine, but also the container which has the model. And that could speed up speed up the time it takes to load the models. Right?

**[21:38](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1298s)** Just think about this as if you have a cached um copy of all of Ollama on your laptop. So, instead of you doing Ollama pull model, which takes a couple of minutes because it has to download the model from Ollama Cloud, imagine if all of Ollama Cloud was cached on your laptop or cached somewhere in a file system, right? Doesn't really have to be on your laptop. It could be in a server that you run under your desk and then you have a full cache of Ollama. So, the time the difference between the time it takes you from get getting it from the cloud and then getting it from your locally could be substantial. Make sense? So, that's like the storage optimization. Accelerators, Kubernetes for a very long time had something called device plugin. A device plugin is a pretty simple tool that's essentially a thing that you install your Kubernetes, you install your device plugin and then you tell device plugin, "Hey, I have these GPUs

**[22:25](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1345s)** available on my cluster. Here is the driver." And then whenever Kubernetes spins up a node, it will automatically install the drivers and then make the GPUs ready. And then from your perspective as a developer or as a operator, you just write I don't know if you If you're not aware, Kubernetes is YAML driven. I call that YAML YAML driven engineering, which is amazing. It's my favorite programming language. Um and then I say, "Hey, give me three GPUs." Right? Normally, you would have another line that specify which model of GPUs you have. And so, this means actually that in order to achieve this, there is two things you need to do. As a platform administrator, you need to actually install the cluster, have the GPUs available, make the drivers available. You know, you have to prepare the platform. For a developer to be able to claim resources with a simple line that says, "Give me three GPUs

**[23:14](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1394s)** of type Nvidia L4." And then the Kubernetes scheduler, it will take this and it will go, "Okay, the user is requesting three GPUs, so I'm going to make sure to put this pod, this container, on a on a node that has those GPUs available of this particular characteristic. The problem with this is a very static way of doing things, right? You you you you you are you have to statically define the resources available in the cluster first, and then you have to statically assign the containers to the to the to the to the nodes right? Now, my some of you some of you might look at me and wondering, "What's the problem?" Because that's what we've been doing with CPU and memory, right? To run things on top of servers, you need to configure the servers first, and then, you know, make sure that the the the workload requires or ask for the resources it needs, and then we assign

**[24:02](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1442s)** the workload to the particular node that has those resources. In a cloud-based environment, or maybe even on prem, that's not always the case. One of the main things that is happening right now is resource scarcity. So, even if you have deep pockets, you might actually not be able to even get the GPUs in the first place, because they're not available, because they the cloud providers doesn't have them, Nvidia doesn't have them, there is memory shortages, all sorts of problems. So, to make things a little bit more dynamic, Kubernetes introduced this thing called dynamic resource allocation. I'm going to just show you an example, and hopefully it will be easy to to to to to uh understand. So, you still have the two roles, platform admin, developer. As a platform admin, I define or describe which devices I have in my cluster. So, this is an example, I say, "This is an Nvidia GPU, it's an A100, it

**[24:51](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1491s)** has 40 GB of memory, and then it has how many number of cores." Right? So, you describe these are the available resources in this particular cluster. Then, as a developer, I would deploy it with what we call a resource claim. Don't don't worry too much about the name of the resources, it's not very important. But, basically, what the resource claim allow me to do is I describe my need in a more generic way. I say, I need an NVIDIA GPU with 30 GB of memory and at least that number of cores. And this way, my cluster will automatically assign that workload to that particular node that has that particular accelerator. Now, notice a couple of things. I did not have to specify which model I need. Right? Which means I can actually, as a platform admin, I can make sure that my cluster has a certain number of models available.

**[25:38](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1538s)** And then, whoever needs some sort of characteristics can get their their workload deployed there. It doesn't They don't have to say, "Give me this particular GPU, blah blah blah." They just say the characteristic of the resource they they need. And this API actually have been created really to be generic in the sense that using this API, we can describe all sorts of resources. We can describe storage. We can say, "Hey, the cluster has SSD storage with this uh um uh this kind of throughputs characteristics. And this kind of IOPS characteristics." And then, a workload says, "I need a SSD disk with this size and this kind of IOPS." And the cluster will just match them together. We can do it for networking. We can do it for all sorts of things. So, this is This API have been introduced couple of years ago, and this is one of the most actively worked on

**[26:26](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1586s)** project right now, which is helping with both dynamic aspects and then resource scarcity. And then, what you could do actually, if we go a little bit further, I don't have a slide, but I can I can um um just describe it high level, is we could combine this with actual infrastructure auto scaling mechanisms, where let's say, in this particular case, there is no GPU that has 30 GB of memory and 3,000 cores available. There is none available in the cluster, what will happen to this workload is that it will be pending because it doesn't have the resources needed to be running. And then another mechanism will trigger and will go, "Okay, I have a workload running that is waiting for something to happen. I will just go to the cloud provider called API and provision that automatically." Add it to the cluster. Kubernetes is all You can think about it as like an all of

**[27:14](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1634s)** like a a combination of multiple controllers running at the same time doing control loops. And then one is responsible for one thing, another one is responsible for a different layer, and then they can be complementary with each other. So, using these mechanisms, we can actually make the cluster quite elastic, quite like fast to scale out and scale down um dynamically without having to be very specific. And then the last thing I have is one of the reasons I mentioned earlier about large language models not being typical workloads is actually the network characteristic profile of the workloads. Now, on the left side, you see how a typical microservice network profile looks like. Microservices have these characteristics: very small, very uniform, very cheap request to serve. Right? Most microservices are HTTP,

**[28:03](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1683s)** HTTPS. Ideally, if you are doing something cool gRPC. But most of the requests coming from your front end to your back end or from user to your front end are small requests. It's an HTTP with a specific method and a specific JSON payload. And that JSON payload a lot of time have more or less a fixed size. Right? Because it's just like random text and random um uh values right? But with large language models, that's not the case. Large language models might have an uniform or non-uniform requests because some requests could be text, some requests could be pictures, some requests could be um uh uh uh videos. Some requests could be streaming. A lot of times actually, when you are talking to a model and the model is answering you right away, you basically have a web socket from your clients all the way back to the model.

**[28:50](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1730s)** And then you are getting back those tokens streamed back from the model as they are generated. Right? So, there's no buffering on the server side. Which means that some requests might be small, some requests might be big, some might take longer time to conclude, some might take shorter time, etc. So, using a standard typical load balancer that we use for web traffic doesn't work for large language models. Right? Because what a load balancer designed for web traffic will do, it will just round robin, right? It will assign request number one to back end one, request two to back end two, blah blah blah, and then go back. And then do this. If you do that for large language model, you will end up with uneven distribution of processing of requests. So, one request might be assigned to a model which keeps that particular model super busy. The other one will be assigned to another model which is short. And then because the the the the load balancer is not aware of what's

**[29:40](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1780s)** happening inside the request, like the size, the type, the kind, etc. etc., you will have uneven distribution of traffic. Another important thing also, if you are running large language models yourself, is imagine you're trying to optimize your infrastructure. So, you want to run a set [clears throat] of large language models for your entire business to solve all your business uh um uh needs. One of them might be your developers are using a model to code. Another one might be your chatbot in your website is answering user requests. Another one might be your CRM system is talking to a model to get like customer data from multiple locations. And in Imagine you are an airline, and then suddenly there's a strike somewhere, which means a lot of users will flood into your website and try to talk to the chatbot to get answers, right? So, you need to be able to prioritize traffic.

**[30:29](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1829s)** Where you can say, "Oh, my CRM people can wait. I want to prioritize the requests coming from my website because that's more business critical for me." Right? Hopefully, your company is not going to say, "My developers can wait." because developers cannot wait, but like you get the idea, right? You might be able to do like some prioritization. So, how do we solve this? Uh so, uh this is essentially what was described. A round robin can waste GPU cycles. Um major generation can take longer connections. Chatbots um might want to prioritize versus summarization or something. Um uh a large language model that supports streaming requires sticky connections. Uh blah blah blah. So, these are some examples. I can skip through this. So, the without again going too much into details, but since Kubernetes is a resource-driven system, which is you

**[31:17](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1877s)** don't really configure things, you just create an object and then Kubernetes does things in your behalf. From a load balancer perspective, there is this API called the Gateway API. This API exists for like 6 years and we've been using it to do load balancers. It works great. But because of these non-uniform characteristics that large language models have, the Gateway API itself had to be modified to support this. So, now we introduced a new type of objects. Again, I'm not going to go into details. I'm actually going to jump straight into an example. If you have used Ollama before from the command line, one thing you will notice is you run Ollama with a model on your laptop and then you want to curl it. You don't just curl Ollama on a specific path. You have to pass pass a JSON, right? You have to send a post request with a JSON payload. And inside the JSON payload, you have

**[32:04](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1924s)** your user requests. You might have system instructions, you know, all the typical stuff that the large language models support, but one important thing is you have the model ID. And that's because Ollama, like any other inference engine, uses an API which was actually designed by OpenAI. So, the spec is designed by OpenAI. And so, it respects that spec. And part of that spec is that you have to have the model ID in the body of the request. That's it. That's how it works, right? So, um this creates a problem. The mainly because most load balancers or most API gateways cannot actually route based on the body of the request. They can only route based on the URL. So, when you're designing a load balancer or designing API gateway, you say, "Okay, my company.com/v1/api goes to this back end. My company uh

**[32:52](https://www.youtube.com/watch?v=G58PbxBXC8c&t=1972s)** /v1/ um API/flights go to this other back end. /users go to this other back end." And that's like standard load balancer stuff. We can do that, right? But, not with the body of the request. And one of the things this API have introduced, what you see on the top side, that's actually the path on which all of OpenAI spec works. So, if you curl any OpenAI standard interface, that's actually the the path. And then inside the body, you pass your authorization token, blah blah blah, and then the model ID. So, what we did is we introduced something called endpoint picker extension. So, what this thing does is will basically uh register itself to the gateway, and it's will tell gateway, "Hey, I can do routing based on this particular characteristics." So, when the user hits the gateway, which is your load

**[33:40](https://www.youtube.com/watch?v=G58PbxBXC8c&t=2020s)** balancer, then the gateway will send that request to the endpoint picker, and then the endpoint picker can read the body of the request, and then make routing decisions based on the body of the request. This is an extensible API. As of now, it only supports base routing routing based on model ID. But, the idea here is that in the future, you can think about it as this could integrate into the back end and read um uh metrics from the back end. And it can be aware of which back ends are busy and which back ends are not busy, and then make routing decisions based on that, right? Or, you can actually configure it specifically to say, "If the media type coming through my load balancer are of type um pictures, send it to this model. If it's of type video, send it to this model. If the user is asking for streaming, send it to this model."

**[34:29](https://www.youtube.com/watch?v=G58PbxBXC8c&t=2069s)** So, this gives you as a platform administrator a central way to manage routing based on very specific characteristics that large language model traffic has. Make sense? All right? So, we talked about that. So, if you are curious, there is this project, you don't have to look into it, but it's just if you're curious. It's an open source project called LLMD. This is a combination of a combination of like best practices, architectural patterns, and then tools that Google, IBM, Nvidia, we all worked on this stuff. And this is today open source, and it's part of the Linux Foundation. So, it's not under the control of any cloud provider. It's within it sits within a neutral organization. So, the idea of this is this will become the the the standard in terms of how we

**[35:18](https://www.youtube.com/watch?v=G58PbxBXC8c&t=2118s)** implement the patterns. So, this documents the patterns and documents benchmarks for testing those patterns. Now, how do you do the implementation under the hood? It's really up to you. And the reason why I'm mentioning this is because it's completely open source, and it's Kubernetes native. So, you can actually run it locally on your on your on your on-prem clusters or on your laptop for fun. Um so, it defines this thing called we lift pads. You remember earlier when I talked about prefill and decode as part of inference? It's actually has a built-in architecture for that. It has a built-in implementation for how do you do prefill decode implementation across multiple nodes in a cluster. Um this is the architecture if you are curious. So, it has it has like a it uses a a proxy called Envoy. It uses vLLM behind the scene, and it has like all these stuff that you see on the

**[36:05](https://www.youtube.com/watch?v=G58PbxBXC8c&t=2165s)** right side. Um so, all these like intelligent routing that I've been talking about. I'm just going to skip through it. I don't want to spend too much time. If you're curious, we have built actually this GitHub repository. This runs on our platform, but you can actually replicate it. If you are If you just never seen how a YAML file for running a model looks like, this is a good starting point. You can just like it uses we use vLLM, and then we use a bunch of like we run 10 models. Just for fun we decided we're going to run 10 large language models on a Kubernetes cluster and then built a single interface UI. We can type a prompt and then we send it to all the models and then get back the results and we show some some metrics. So we show how much it takes time for the first token to show up, how much time it takes for the for the complete answer to come back, latency stuff like that. This is really cool experimentation if you're trying to

**[36:53](https://www.youtube.com/watch?v=G58PbxBXC8c&t=2213s)** evaluate multiple models at the same time. So to recap, running large language models is not as easy as running web applications. It has a lot of complexities due to the nature of how large language models are, the size, the complexity, the fact that they need extra hardware to run and then also the the network characteristics of these models. And so if you're approaching this idea of I want to run large language models myself, you can optimize all of these things at different layers and it's not a single layer of optimization, it's multiple layers of optimization that combined can lead into um something good. Hopefully for you. Everything I talked about is open source. Everything you can find it on the internet. And with that, I would appreciate it if you give me feedback. This is a link to the companion app. Um

**[37:41](https://www.youtube.com/watch?v=G58PbxBXC8c&t=2261s)** Hopefully this was either it was useful or I had no idea what I was talking about for the last like 30 minutes. And um with that, thank you so much Multimask.

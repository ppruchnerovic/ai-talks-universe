---
id: oRVtTEbtt_Y
title: "TornadoVM: Supercharge your Java with GPU acceleration - Christos Kotselidis, Mary Xekalaki"
slug: tornadovm-supercharge-your-java-with-gpu-acceleration
conference: devoxx
conference_name: "Devoxx"
category: "Software dev with AI tracks"
edition: "Devoxx"
year: 2026
speakers: ["Christos Kotselidis"]
channel: "Devoxx"
duration_min: 46
published_at: 2026-03-30T17:53:08Z
video_id: oRVtTEbtt_Y
youtube_url: https://www.youtube.com/watch?v=oRVtTEbtt_Y
tags: []
transcript: true
---

# TornadoVM: Supercharge your Java with GPU acceleration - Christos Kotselidis, Mary Xekalaki

**Christos Kotselidis**

`Devoxx` · `Devoxx` · `2026` · `46 min`

[Watch the recording](https://www.youtube.com/watch?v=oRVtTEbtt_Y) · [Conference site](https://devoxx.com/)

## Description

#VoxxedDaysCERN26

In this talk we will present TornadoVM (www.tornadovm.org); a state-of-the-art programming framework that enables native GPU acceleration of Java programs. TornadoVM allows developers to program GPUs from within Java while the system handles compilation, execution, and optimization completely transparently.
TornadoVM is an open-source project (https://github.com/beehive-lab/TornadoVM) and it is being used by the European Space Agency GAIA mission to accelerate its data processing pipelines resulting in significant reduction of execution times. In addition, it offers a rich toolset ecosystem to help developers throughout their journey (coding, debugging, performance optimizations) and it recently added to its ecosystem GPULlama3.java (https://github.com/beehive-lab/GPULlama3.java); the first GPU-accelerated native Java implementation for AI inference on a plethora of LLM models which has been integrated with Lanchain4J and Quarkus.

The target audience is developers who wish to make a first transition into the GPU programming world, or just practitioners who wish to use the high performance Java AI libraries that TornadoVM provides.

The key takeaways are:
1) Java is AI ready since we can harness GPU power natively for LLM inference
2) Programming GPUs from within Java is easier with the mature ecosystem that TornadoVM provides
3) Having a native Java platfrom for AI and general purpose GPU acceleration enhances both productivity and integration

## Transcript

*7,403 words · source: supa (en, exact timings)*

**[0:00](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=0s)** that I'm still holding on to something. You should go and love yourself. For all the times that you made me feel small, I fell in love. Now I feel nothing at all. start. >> Okay, I hope you can hear me. Uh, hello and good morning everybody and thank you for being here. Uh, we are very happy because every time we talk about GPUs, more and more people uh are showing up

**[0:48](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=48s)** which means that GPUs are becoming a thing in our world. Today we will uh together with Mary uh we will introduce to you our project that we have been working on it for the last 10 years called tornadovm uh which helps Java developers to use GPUs completely seamlessly from the programs. So uh Mary is an associate at the University of Manchester. I'm associate professor at the University of Manchester and today we will talk about we give a bit of an overview of tornado VM. So how what it is and uh how it came into existence. Um we will we will motivate a little bit about GPUs and why they are so different to use compared to CPUs and why we need a new programming model for it. And then we will go on to more practical like how

**[1:38](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=98s)** to install it to run some programs show you some performance numbers and then we're going to switch gears and we will talk about um the maturity of the system and how it's currently being used in production. So what is tornadovm? Essentially it is a plugin to JDK distributions. So you can download your favorite JVM from your favorite vendor and then on top of it you can plug in to VM and by you by doing that you you get access to APIs and runtime system that allows us to develop uh code for GPUs. Uh it has been around for 10 years. It has been open source in 2018. Since then we have 23 releases. We're currently at 2.2.0. We have a rich uh community of Java developers who like GPUs and currently

**[2:28](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=148s)** uh it's in production at the European Space Agency Gaia mission. We will discuss about it a bit later. So tovm it is a a plugin to JDK distributions and it's part of a rich ecosystem of tools, frameworks and other Java goodies that we like so much that we can together use to enable uh harnessing GPU power. So if you like Open JDK or Grav VM or Eclipse distribution or Red Hat, you can download them and then install Tornado on top of it and then you can get access to GPUs. uh despite Java uh which is the primary programming let's say language of uh tornadovm if you use galvm and you use a truffle framework uh which you can execute on

**[3:16](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=196s)** top of the jvm language like javascript python r you could um do a a foreign call from the python wall to the java API of turno VM and have GPU acceleration so if you make decision to write some GPU code on VM by using RAV VM M you can transfer this code across different programming languages without having to rewrite this code at all. We currently support three backends. So we target OpenCL uh Nvidia CUDA and Spearv. And now we're working on a metal back end to have native uh acceleration on Apple devices. And of course because it's part of Java we'll get all the Java ecosystem that we like like a dockerization running Kubernetes um installation through Maven central uh

**[4:03](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=243s)** SDK man for installation and everything. And through our journey of developing toVM we realized how difficult it is for Java developers for any developer to be honest to make this jump to program GPUs. And for this we have developed a number of tools uh that helps developers from the development of the code debugging and um deploying the code and then do performance analysis on the code. We will expand on them a bit later. And of course you have integrations with quarkus uh lchain forj for AI workloads and soon a spring boot. So let's talk about Java the the strength of Java and uh how GPUs come into uh the picture. So uh Java it's a really powerful language when it

**[4:52](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=292s)** comes to uh scalable production systems. uh throughout the years the the capabilities of Java through the APIs in the runtime to exploit different kind of um uh multiore CPUs or vectorization or vector units um has given birth to many APIs like the vector API the streams visual threads uh which is they're very powerful in order to scale application on the CPUs which is great but in the new AI world that we need GPUs for inference uh this API I do not particularly map very well on this kind of GPUs because the GPUs they have a I wouldn't say a unique programming model but they have some elements in the programming model that do not currently supported by Java for

**[5:41](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=341s)** example in GPUs we have to switch our mentality to think about the parallelism as a mess a 3D mess where we have to allocate per thread which work has to be done on this 3D mesh of uh the working group of the GPU. Also, developers have to do explicit data management. So, you have to copy manually data from the CPU memory to the GPU memory. Copy back and forth uh maintain consistency uh or coherency between the different memories. And on top of that, which is a different shift than doing CPU multiple programming, uh we also have the the problems or the challenges of which code to offload on GPU because not all the code can execute on the GPU. how to do it, how to install the drivers, how to call the drivers. So it it has a lot of

**[6:30](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=390s)** let's say under underlying work that we as Java developers we were never asked to worry about and this is the motivation where we developed VM. So what we want to do is a high levels of API that allows developers to express the computation for GPUs and all the rest are handled completely transparently by torrent VM. For example, we transparently offload the code on the GPU. We transparently compile the code for the GPU. We automatic we automate data management how to copy data back and forth. And because it's Java of course we like the right ones run everywhere which means that as soon as we write the code on trend VM and it runs on CPU then we guarantee that if you change GPU if you change your system it will also run the

**[7:19](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=439s)** same code uh and try to get the maximum performance of the system installation. Now you're going to ask me why do you have slides about installation? because installation is a big pain point when it comes to kind of systems and we have iterated a lot over how to install a system that enables GPU acceleration. Um we have two options. The first and the easiest one highly recommended is to use SDK man. So assuming you have a Java distribution then you can actually use SDK man to install tornadovm and then it works out of the box or you can download clone the repository from the GitHub and then we have a a big script that does everything downloads all dependencies compiles the system links the system and after we do that then we can actually invoke the system. So we have um you can call the system like normally like uh Java and

**[8:08](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=488s)** you uh give some arc files with libraries that have to be linked for the drivers. And to again to simplify that we have a Python script called tornado where it does the same job essentially you just call it and then um you can actually hide all those arguments underneath the script. Of course we have we are releasing uh the artifacts on baby central. So if you have an application, you can just uh put a dependency and automatically download the system and then you can actually use it from your system. So let's talk a little bit about the programming model um in general. So how we should think about GPUs uh and then how to VM helps us navigate this problem. So essentially when we try to program GPUs, we have to

**[8:58](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=538s)** think about two things. The first one is I have my Java program, the normal Java program and then I want some code that fits the description and the requirements to be accelerated on the CPU to run there. So semantically and in some programming models it's also explicitly stated we separate the code between the host code which is the code that runs on the CPU and the kernel which is the accelerated code. And our job then as developers is to define how to write this kernel. So how to write a kernel that can be compiled run on the GPU. Then how to offload data. Okay. Now we have the kernel. Now how to define somehow a way to copy this data and this code to the GPU. And third how to launch it. how to actually call the

**[9:48](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=588s)** GPU uh but then let's say interrupt the CPU launch the code get the results and continue my execution and um for each one of those let's say semantical problems or whatever we have to solve uh in order to express dispatch and launch we have some APIs that helps us to do that and we will uh um expand a little bit later so to name them for expressing computation we have the loop parallel API and the canal API for dispatching code and data we have the task graph and to launch we have the tornado execution plan and specifically for writing the code which is to be honest the the biggest pain point uh when you you transition from CPU programming to GPU programming we have also developed a plug-in for IntellJ

**[10:37](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=637s)** that helps Java developers guide them through how to write the code on for the GPUs so here this is u some code snippets Mary will demo it a bit later. Uh how to use these APIs. So we have the simple API which is the one on the left called the loop parallel API and the kernel API which is more advanced and exposes more elements low-level hardware primitives from the GPU Java developers. So you can use one the other or both at the same time. There is no restriction how to use APIs. So the loop parallel API essentially it is very useful if you have a loop already imagine now you have a a parallel loop that you use multi- threading now you can just add

**[11:25](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=685s)** annotation at parallel and then the tornado catches this annotation and then understand that this code has no dependencies and then it creates the GPU code which is open or cuda or spv and if you have a reduction which are really important for machine learning workloads you put the reduce annotation and And the tornado VM handles all the code for the reduction. And these are the only two annotations we have for the loop parallel API. Now although this is very easy and powerful, it doesn't cut it for all the cases. Some cases, especially when we do AI inference, we need really fine grain control over the GPU code because the performance that a simple parallelization of a loop does not uh yield at least compete with PyTorch or the other frameworks. So with the k API

**[12:14](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=734s)** now we switch our minds from Java developers to CUDA developers essentially especially if you have done CUDA OpenCL the kpi is very similar now we do not think of our loop as the whole body which you have to parallelize now we think about which is the work that each thread in this thousands of threads that the GPU launches uh has to do. So it's our job to partition the the data and the code between let's say the threads and then we actually utilize the code in order to achieve this the results and because we fine grainly define the code per thread we also have to do uh synchronization like putting uh barriers between the

**[13:02](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=782s)** threads to um uh synchronize them and data which is I mean to be honest after so many years of uh code development for GPUs uh the majority of the frameworks can reach very good performance whether it's PyTorch or any other frameworks they can actually get very high performance when it comes to the code that generated the one of the largest pain points is data uh even in AI workloads uh the biggest problem is data how to optimize data movement how to parallelize accesses and this data it's a big problem for the JVM because the JVM has the heap has the garbage collector. So we cannot just use data from the heap because as soon as the data leave the heap go on the GPU you create a copy and then the garbage collector doesn't have

**[13:52](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=832s)** a view of what's happening on the GPU you ended up with corrupting your data and check fault. So you have two ways. The first is either to lock the GPU the sorry the garbage collector so the garbage collector cannot do anything until the GPU code executes and comes back the result or to use out of heap data types like through project panama and the foreign memory API. We love it. Uh this is our f amongst our favorite latest Java features. We have been from the early adopters because it allows us to create to create our own data structures that are GPU um native and then because they're out of the heap we can copy them back and forth freely and it's our responsibility

**[14:40](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=880s)** to manage the consistency and because we control the runtime then we can guarantee that this data will be consistent among different GPU executions. So you see on the right hand side we have um our own types our own arrays like interate float arrays and for machine learning workloads we have quantized type arrays uh which developers can use and then u accelerate on the GPU very easily after we have our code and then Mary will show how we use the plug-in to help us uh write this code then we have to define uh the dispatch which means how to create a graph of kernels to offload. So in pure CUDA or punopl somebody would

**[15:28](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=928s)** say okay here's my kernel create the buffer copy the kernel run it and get back the result. Uh this is great but in workflows in very complex workloads normally the kernels they create they create chains of one operating some data giving the results to the second task and then you have a pipeline of complex tasks that execute all in the GPU. So to express that we built a graph which is we call a task graph. So essentially the task graph it is a chain of tasks uh which we pass input a lambda function and now this lambda function essentially it is our kernel that want to accelerate. So as soon as we do we build this graph then tornadovm can analyze this graph and find which task shares

**[16:16](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=976s)** data with what other task and optimize data transfers uh between these tasks. And this is very powerful because you you are able to minimize uh data copies between executions and to persist data on the GPU as much as you can until the execution finishes. And then when it comes to input and output data from the GPU, then we have to put two methods for transferring the data from the CPU to the GPU and back and forth. And there we can also define the frequency that we need to copy the data. Sometimes imagine we have a an array which is read only and we do many operations on on it. Then we can just copy this array only one time in the beginning persist it and then all the tasks can operate it. But

**[17:05](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1025s)** if it's a readr array, so we have we want to to refresh it in every execution, then we can actually define how to copy the data in every execution in order to have a consistent memory view. And finally, all we do is launch. So we have this graph now and now we have to take a snapshot of this graph, create an immutable task graph and then launch it on the GPU. So why we need this to be immutable? Because a task graph can mutate. So we have a task graph of some operations. We do something and then we can change it. You can put tasks, remove tasks. So whenever we are ready to launch an execution, we would take a snapshot of this task graph and then we launch it through the executor. And the exeutor essentially it

**[17:54](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1074s)** is um an entity in our system where we can actually create plans how to execute them which device to run maybe you have 10 CPUs or FPGAAS you want to swap them and then we launch now for the kernel API because it's more fine grain we have to help the system also to know the dimensions of our problem of our problem we need to create some grids because now we need to help the system understand what type of GPU it is that we have underneath, what is the how big it is, what are the working group size is and create this grid and pass it on the exeutor in order to optimize the schedule of the execution uh to the maximum extent and then all we do we call the plan execute and with this I

**[18:42](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1122s)** will pass it to Mary who will actually demonstrate all this boring theoretical stuff that I have been explaining. >> Hi everyone. So let's have a look at some practical examples of the concepts that Christos just described. So here as you can see I have my IntelliJ open. The code that I will show you is all written in Java. So as you can see I can use a Java environment to demonstrate this. And let's look at this function this matrix multiplication here. So if you look at it, it's a simple Java function. There's nothing very strange about it. Uh the only difference is that we have this at parallel annotations that I have put here because I know that the code inside these loops uh can be parallelized on the GPU and also we have this uh float array type which essentially stores float data of heap by taking advantage of a memory segment and

**[19:33](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1173s)** it uses uh get and set functions to do that. So this is the function that we want to execute on the GPU. Let's see now how we can do this with tornado. So uh as you can see here I have a main function. We initialize the data as we would normally would and then we create the task graph object. So as Christo mentioned this task object essentially encapsulates uh the deployment that we will do on the GPU. So as you can see I have defined the input data the data that will be transferred uh from the CPU to the GPU which is the two matrices that we will multiply matrix A and matrix B. I am defining here uh the function the lambda function that will be executed and uh finally which data I

**[20:22](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1222s)** want to copy back which is actually the result which is stored in matrix A and that's it. we have defined uh our deployment and now we're ready to launch it. So since we're ready, we take a snapshot of it. We pass a snapshot to the executor and from this point on we're actually ready to launch. So here this is written in benchmark mode. So as you can see I'm running it several times to warm up and then I'm getting some m metrics and um running the sequential to compare against and this I can run this from intelligj directly. The only thing I need is uh some JVM flags. So as Christos mentioned before we have this tornado command which essentially it's Java with some flags that are necessary in order to invoke tornado VM at

**[21:11](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1271s)** runtime. So I can just do tornado dash print java flags. As you can see it's the java plus the flags that I need. So I can copy this and put them in the configuration of uh intelligj. I already have them here as you can see. Plus I have also flag that we will use to see uh on on which device the computation was executed and with uh how many threads. So I can simply run this and you can see it was launched several times because of the warm-up iterations. So you can see it run on my Nvidia GPU on my machine. You can see that tornado automatically also decided um the number of threads that will launch on global

**[22:00](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1320s)** and local memory. This is based on some metrics from the drivers. And uh you can also see the speed up that we get compared to the sequential which uh is quite impressive I believe and that uh the results were actually correct of them of the operation. Okay. So now let's look at a different implementation of the same uh computation the matrix multiplication written with the kernel API. So this implementation essentially was based on an open CL implementation that was very efficient and because it uses some typical GPU uh optimizations like utilizing local memory and uh tiling. So by using this kernel context here, this this object, we're actually able to take

**[22:49](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1369s)** a very fast implementation on koda or of open CL and replicate this all in Java. So as you can see, I'm using this kernel context object here to get the thread information and um this looks uh I think will look very familiar to a low-level developer. We also have the barriers here. And now in order to launch this essentially the process is very similar as before. The only difference is that we have this griduler. So essentially the kernel API is all about control. So you want to control everything. You want to control the optimizations. You want to control how you will actually execute the program. So if you noticed before uh when we ran uh our previous implementation with the loop API tornado

**[23:37](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1417s)** VM uh based on the driver metrics decided on the number of threads to launch in global and local memory. However, now that we have written our own optimizations, we know that uh the local size should not be 16 to gain the most performance. It should be 32. So we use this grid uh scheduleuler to actually define this and we declare the task graph just like before. We just simply pass the griduler to the tornado execution plan and then we simply invoke the execution through the execute and run it several times. And once again this is a Java program. So I can also run this from IntelliJ and see uh that actually the configuration that we defined is used.

**[24:27](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1467s)** Okay. So also as Christo mentioned uh because u this is a new programming model and we know that uh there is a learning curve uh to anything new any any new APIs or anything we developed tornado insight. Tornado inside is an intellig plug-in. You can directly get it from Jet Brains Marketplace. I have it installed here. You can see it. And uh since I have it downloaded here, I have this icon, the tornado icon. So what tornado inside does, it actually has two main functions. The first is that it performs a dynamic inspections on the functions. So when we have a file open it analyzes uh which uh methods use uh either of the tornado VM APIs. So here we have the matrix multiplication with the loop API and you

**[25:16](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1516s)** can see it identifies that we have this function. If I move to this file we have the matrix multiplication with the kernel API and you can see that it finds this function and uh it performs a static analysis on it. So essentially it analyzes the code to see uh if we have written anything that's not supported uh by tornadovm um sedactically let's say. So for example if I try here to let's say I try to throw an exception exception handling is not supported on GPU. So tornado does not support it either. And you can see that this has turned red here. I can hover over it and you see uh that we get a message that informs us that this is not supported. And also the task is removed from here. So we cannot actually see it as a tornado task anymore.

**[26:05](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1565s)** So that's one function. The second and I believe the most important function is that it helps us uh debug our our programs to see if they will produce uh lo if they will produce legal code. So let's say now I have this code. Okay. And I want to see if this is uh if this is legal to VM code or if we'll have a compilation error. What I need to do in order to do that normally is what we do here now. So write a main method, initialize some data, create a task gra object and run it with inside. We don't need to do that. We can simply select the task here. Click on the run and if the code is actually legal oop sorry this is stupid. uh intellig uh let me

**[26:59](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1619s)** we will get the code if not we actually get uh the full error stack which helps us de debug our programs. Okay. So, so far we have seen some trivial examples, matrix multiplications, vector additions, but tornado VM is actually a powerful tool that's very mature. And um as a proof of concept of that, we developed GPU Lama 3. So, GPU Lama 3 essentially is a llama 3 implementation that's written purely in Java that uh can be executed on the GPU by taking advantage of tornado VM. So uh it allows also switching between pure CPU and pure GPU inference. It

**[27:50](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1670s)** supports the GDUF model and uh we currently support the llama 3 mistral 53 quen IBM granite and we're also working on demo. This is an open source project so you can clone it and try it and run it and give us a feedback. Feedback is always welcome. And now let's see let's see it in practice. Let's see a bit the code. Let's run it. Let's get a better idea of it. So again, it's a Java program. So as you can see here, I have my IntelliJ open for this as well. And uh now we're looking at the code of the logit uh layer, which uh simply put is a layer essentially that transforms the text representation uh into scores in order to decide the token that will be generated. So if you

**[28:39](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1719s)** look at the code here, you can see that uh it utilizes the task graph object in order to define the data that will be persisted on memory consumed from memory of the device of the GPU. Uh you can see uh that we also define the tasks the functions that will be executed and actually the data that we get back from the GPU to the CPU. So the same logic now is applied to a very complex use case. And if we look at this file here, this file actually has all the methods uh of the inference that are deployed in tornadovm. And you can see from the tornado inside that there are quite quite a few. So I think now at this point uh it's very easy to understand the value that tornado inside provides because if I wanted to debug each one of

**[29:26](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1766s)** them I I would need essentially to build a main method for uh all of them which is not very very easy to do. So instead I can choose whichever click on the run. Oops I think I have somewhere an error. See maybe I made a change somewhere and now it informs that there is an error here somewhere. So I can run it, I can debug it. It offers all these possibilities for me. H and now for this implementations here we have used the kernel API. So why the question is because the kernel API essentially h enables us to write a very performant code. So performance matters a lot on GPU lama. So here you can see for instance a matrix uh vector

**[30:14](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1814s)** quantized implementation that's uh that uses row major and this uh is as you can see the code is very complex. It uses some optimization some standard uh GPU optimizations like call list memory accesses uh it uses um loop unrolling and through this powerful uh kernel API we're actually able to express all these optimizations and take the full advantage of them. So to run GPU llama 3 h all I have to do is simply type lama tornado define that I want to execute on the GPU because as I mentioned before we can either run on the GPU or the CPU h define the model uh let's use now the quantized uh llama 3.2 to with one uh one billion

**[31:03](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1863s)** instructions and put a prompt. Let's use the hello world of LLM which is tell me a joke. And here you can see that we actually got our joke. It's not very good but tornado cannot uh do anything about that. I'm sorry. And you see that we get some tokens back. And also we can use some flags to actually say um some informations about uh what to execute and where. For instance, we can use again the print threads. Print is it print threads? Um print. >> It's before the model. >> Ah it's before the model. Uh sorry about

**[31:52](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1912s)** that. So let's put it here. Print threads. That's the one I think. Let's remove also the space because you never know. And uh now it will run. And you can see that it gives us some similar output as the ones we saw before. So we see which functions are executed on the GPU. We see the thread configurations and everything. Okay. So using uh GPU lama 3 is actually very easy. We believe uh it's also provided uh as a model by lang chain for chain. So you can use this dependency here uh to import it in your project. Uh we also have an integration with quarkus

**[32:40](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=1960s)** and lang chain for chain. So if you're using quarkus you can also add the dependency and you will be able to utilize it or you can use it standalone uh through maven central. Now in order to um demonstrate uh the integration of lang chain for chain uh quarkus and gpu lama 3 we wanted to build a demo as proof of concept. So what we did was that we had a look at the quarkus documentation page to see the kind of applications that they run and we found a very interesting one which is about text summarization. So essentially you provide a text and then you get back uh some key points of it some a summary of it. So uh we have uh actually implemented a demo for that. It's available on GitHub. It's open source. Everything is open source. So

**[33:27](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=2007s)** you can download it and try it and see. And actually let's see it now. Uh again I have it downloaded here. You can see the project. We have two demos. We have the chat and the streaming demo. And if you look at the functions at the code that we have, it's actually a boilerplate Quarkus code. So you can see here we have the quarkus made an annotation. We have uh this quarkus run. We define here uh the action that will be performed. We have this uh file here with a prompt. So there's nothing very weird about it. The only thing is that in the pom file we have included the dependency I showed you before this here for the quarkus length chain for uh GPU lama 3. So um I can just run this as a Java

**[34:18](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=2058s)** program. As you can see I can either use the tornado keyword uh or I can run test a Java program with the tornado VM flags here. And uh if I run it you will see that it uses uh GPU lama 3. This is the text for which uh we want to get a summary and uh we will get the key points out of it soon. So here you can see uh that we get uh the full summary of it. So I think this is it with the demos. Now we'll pass the mic to Christos who will continue with the presentation. Thank you. >> Thank you Mary for the for the interesting demos. uh as we um closing the um the end of the presentation I would like to uh because

**[35:08](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=2108s)** we're at CERN so I think it's very interesting to uh actually we're very proud of it to discuss a little bit our production deployments at at the moment so uh at the end of 2025 end of last year uh we did our first production deployment actually not very far from here it is uh at the European presency the Gaia mission which is the department in the University of Geneva Um so for those probably that not don't know about this mission this is uh I think the largest uh European space mission uh where the Gaia spaceship uh was taking pictures of our galaxy uh sending data back to earth and then it was uh the scientists here they were approaching the data to create the 3D mapping of our galaxy. So the idea is that through the photographs and the

**[35:55](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=2155s)** data that the Gaia mission sends back to uh to University of Geneva and the other teams the data uh processing team in the Gaia mission. Um then they process the data uh through complex pipelines and then they create data sets uh which are being released and uh scientists all all over the world can actually use them and create their papers. So uh our colleagues from the Gaia mission they had a lot of algorithms that they wanted to port on on GPUs and uh they have selected a tornado for this uh job. So I'm not pretending to understand exactly what they did because I'm not a physicist but to my understanding they acceler they have chosen specific pipelines that they were actually prime for GPU acceleration. um in detail I think they accelerated

**[36:45](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=2205s)** the periodic test algorithm uh and they did not only GPU porting but also fusion of kernels and cooperation between CPUs and GPUs and they managed to get over 500 speed up acceleration on the kernels and end to end so how much time it takes them to process parts of data from 1 month down to 5 days which created not only like a speed up and they could produce more data and experiment meant also saving in resources, electricity cost because now they could produce the data uh very easily. So in the next data release of the Gaia mission which again if I'm not mistaken it is December of 2026 um it will include data that has been processed by tornado VM and uh also in the next I think the last that list that list five they will also have some data.

**[37:35](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=2255s)** So the um unfortunately I couldn't show you the code because it is proprietary. Uh hopefully in the future they they will they will release it. Uh but it's really complex and it they really helped us tune the system in a ways that is practical for applications in the physics domain. So if people here from CERN they want some GPU acceleration and they have some code they want to accelerate hopefully they can give to VM a chance uh because it's being used already in computing. And besides those there are many use cases uh from different domains like computer vision uh face detection and machine learning and uh rate tracing and then um the computer vision is a is a gift but that doesn't work probably but in general all these codes are open source in our GitHub. So if you have any use case that you can actually get

**[38:24](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=2304s)** inspiration of how we use different domains you can uh check them out and uh use them. Um so to summarize sov is a is a project that has morphed into the last decade and it's trying to adapt and help developers to program GPUs. Uh 5 years ago we're talking about GPUs and everybody said yeah who cares about GPUs and now GPUs is the actually the programming model that everybody wants to use and uh there are discussions about Java losing power to Python and PyTorch and all this ecosystem wars. So um hopefully projects like tornadov and others can help uh create inject this kind of new technologies into the JVM ecosystem. Um which is possible as demonstrated through uh our

**[39:14](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=2354s)** work with Gibblama 3 and integration with L chain 4J. Um we have unfortunately we didn't have time to go through all our tools. We just went through tornado inside for development and deploying of the code. But we have a number of tools that uh can help us uh do also performance analysis. Uh Java is really good in having performance profilers. We can use them in order to understand what are the bottlenecks. Uh in the GPU world, we need different kind of tools and uh tools that bring together the CPU code, the GPU code and then together have a view of the whole system uh as a whole. And for this we have two tools again they're open source uh tornado pulse and tornado visualizer where we uh produce performance metrics and then we have a system that visualize

**[40:02](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=2402s)** execution. So we know exactly in our complex pipeline which kernels are the bottleneck and which kernels can be uh further improved in order to uh reach uh the performance higher and those tools have been developed in parallel with the AI inference engine. So as we're developing the inference, we realize we need to also understand where the performance uh is lost. So um please have a look and of course these are all generous European projects that has helped develop toVM throughout the years. So where are we going from now? So now we have a baseline, we have a system that's mature enough and now we are improving it. So uh our team is working towards um let's say three dimensions. The first one is the to keep improving performance through different GPU vendors um and extend the support

**[40:52](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=2452s)** the platform support through AMD and Apple silicon uh because it's a different thing if you develop GPU code but you cannot run it on your hardware right so now we need uh all the examples that Mary u shown today was running on this laptop the GPU so you know that as soon as it runs here when you deploy it on a cloud GPU it will perform similarly or even better depending on the power of the GPU. But you need to have a support that no matter what you use, what system you use, you have the same guarantees you have for your CPU code that the JVM is really good at providing, we want the same guarantees for the GPU code, which means that we need to extend the platform support and this is what we do. Um we continue because now we are shifting through AI uh and uh

**[41:43](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=2503s)** optimizations for AI workloads. Uh we continue uh optimizing the system adding iterics the compiler um exploiting more low-level GPU hardware details to get the performance uh higher and higher and hopefully to reach native performance like Kublas. Uh, of course that's a not an easy thing to achieve but we're working on that and uh we are monitoring developments from the open CDK uh the same way we were the first adopters of uh project Panama which we really like also we are waiting very eagerly for Babylon Valhalla and Leiden in order to include them in our system and then get uh the system into the next level um that we hope that the JVM ecosystem uh should transition in this new era that with new requirements for uh GPUs. So

**[42:33](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=2553s)** that was the last slide. So thank you very much uh for your attention. Hopefully you enjoy it onto VM and if you have any questions please let us know. >> Yes. >> Yes. uh detect is a potential >> yes yes it can it can detect it so that's a very good story I will say it so we we thought of removing it at some point because to VM can detect it right that is parallel and then we were at GVMLS uh I can't remember 2017 18 I can't remember and then we were discussing with engineers there to

**[43:19](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=2599s)** remove it and told us no you shouldn't remove it why because the JV the JVM specification uh guarantees uh the precision when it runs on the CPU. On the GPU, the precision changes a little bit because now you're on the GPU, you have different precision. It can always guarantee that the result will be exactly the same. By using the parallel annotation, the developer gets a hint that this code will run on the GPU. So the expectations from the Java spec now changes compared to giving the illusion that this is proper Java code that aderes to the JP specification and then you get the result which is not according to specification. So that's this is the only reason we have it. >> Yes. >> Yes.

**[44:06](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=2646s)** >> Is there any specific setup you need to do regarding that like any special pipeline between hardware or something you know so how this pipeline bridges between them? >> So sorry I didn't I didn't have your question. So the question is how how to bridge the gap between the hardware and the software >> right not the hardware you are running your applic you have two choices either either you can actually preconfigure tornado for the the system that you're going to run or Let tornado automatically understand which hardware is on and then rebuild itself and optimize the code dynamically

**[44:55](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=2695s)** for the new hardware. >> Yes. Yes. >> Thank you very much. Yes. >> I I had a quick question actually. So I wanted to ask um are you planning maybe to be able to serialize more let's say complex Java objects and pass them to to the GPU somehow? >> Yes, for this for this we're waiting for the we have been working on that for many years but uh we see that in project Babylon now they're doing a lot of work into trying to do custom uh shapes that will map very nicely on the native memory. So we're waiting for this to become available to port them and use them. That's our our our mission, let's say.

**[45:41](https://www.youtube.com/watch?v=oRVtTEbtt_Y&t=2741s)** >> Okay, cool. Thank you very much. Thank you very much again. >> Thank you.

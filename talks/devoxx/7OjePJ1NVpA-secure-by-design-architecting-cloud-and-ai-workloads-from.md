---
id: 7OjePJ1NVpA
title: "Secure By Design: Architecting cloud and AI workloads from the perspective of infosec by N. Godec"
slug: secure-by-design-architecting-cloud-and-ai-workloads-from
conference: devoxx
conference_name: "Devoxx"
category: "Software dev with AI tracks"
edition: "Devoxx"
year: 2026
speakers: ["N. Godec"]
channel: "Devoxx"
duration_min: 37
published_at: 2026-04-01T08:04:53Z
video_id: 7OjePJ1NVpA
url: https://www.youtube.com/watch?v=7OjePJ1NVpA
youtube_url: https://www.youtube.com/watch?v=7OjePJ1NVpA
tags: []
topics: ["Security, safety & red teaming"]
transcript: true
---

# Secure By Design: Architecting cloud and AI workloads from the perspective of infosec by N. Godec

**N. Godec**

`Devoxx` · `Devoxx` · `2026` · `37 min`

[Watch the recording](https://www.youtube.com/watch?v=7OjePJ1NVpA) · [Conference site](https://devoxx.com/)

## Description

#VDZ26
Secure By Design: Architecting your cloud and AI workloads from the perspective of infosec by Natalie Godec

So you've vibe-coded your Super Smart AI Agentic App and are ready to set it free on the Internet. You are sure it will do well, it solves this amazing use-case, with such clever technology! You pick your favourite cloud provider, decide on a runtime - perhaps, Kubernetes or Cloud Run - buy a domain, ask Gemini to wire up a deployment pipeline. Happy days!

But here's the thing. You can't vibe-code security. Every single thing you deploy to the cloud needs to be secured. And it's a hard task! Even for cybersecurity professionals.

But fret not. Cloud providers, such as Google Cloud, have already put in the work to make your job easier. And in this talk, you will learn the fundamentals of securing your workloads, (almost) end-to-end.

## Transcript

*5,606 words · source: supa (en, exact timings)*

**[0:15](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=15s)** Right. Hello everybody. Welcome to this session. We're going to switch gears a little bit into security and talk about platform security. I'm a little bit of an intruder in the Java community. I don't do Java. I don't do coding. I don't do any software engineering. I'm an infrastructure person. More specifically, DevOps platforms, cloud engineering. I'm currently a cloud architect. At Zenor, we're like a professional services company. We build platforms and data platforms and AI workloads on Google Cloud for other people. This QR code goes to my socials if you would like to stay connected. My specialies are in infrastructure, surprise, data, and security, which is why today we're going to be talking about security. And the title of the talk is a little trick. It contains the words AI, but

**[1:06](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=66s)** actually we're not going to be talking about the security of AI itself because AI needs to run on something. You still have the platforms. You still have the runtimes. You still have the infra your little services and the multi- aent systems that we just listened to in the talk before. And all of the other applications need to run somewhere. And your agents, they need data. The data needs to be stored somewhere. You need to have a data platform. You need to prepare it and you need to secure it. And in general, your cloud infrastructures will need to be protected from all sorts of parameters. So that's going to be our agenda for today. We're going to learn how to secure services, how to secure data, and how to secure global parameters. Now, I'm also a GD, Google developer expert, like an ambassador, like a Java

**[1:55](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=115s)** champion, but for Google Cloud. So I'm going to be using examples of Google Cloud, but most of those except two things you can use or you can do open source or on other clouds as well. and I'm going to be mentioning the technologies that you can use. Let's start with securing services. GE is Google Kubernetes Engine. So, we'll start with Kubernetes. And why do we talk about that? Why is it important? Well, because most people in the world are using Kubernetes in production. About half of those Kubernetes clusters in production worldwide are running insecure outdated software. And about how many% 45% are running in security software. 46 have reported revenue loss, customer loss, lawsuits following a breach of their Kubernetes

**[2:44](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=164s)** infrastructure. We've had some quite famous um incidents like the Tesla one in 2018. Um and a lot a lot a lot of vulnerabilities are coming out every single day regarding Kubernetes and also different components of Kubernetes. Like last year there was a big suite of vulnerabilities in the um engineext ingress controller called ingress nightmare where um they just kept coming and kept chaining and people were exploiting exploiting them quite frequently. So it is widespread and we need to know how to secure that stuff hopefully. And in order to secure your clusters you have to take two approaches from two different sides. You have to secure your cluster itself

**[3:32](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=212s)** following the networks following the infrastructure the node pools the control plane all of that. And you need to secure your workloads as well separately. And I'm not even going to talk about your code which should contain libraries that don't contain vulnerabilities and authentication and all of that. So in terms of clusters, what do you do? Well, you should have your control plane private, your node pools private. In Google, you have something called VPC native clusters, which means that the routing goes out to the VPC itself. You will have similar setup on AWS where you have your um control plane and node pools private. On GCP, you also have a DNS endpoint for that control plane. There is a typo in endpoint if you're paying attention.

**[4:22](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=262s)** What does that mean? That means that there is a load balancer that terminates TLS traffic in front of the Kubernetes control plane and you can connect to it without needing a bastion host. Bastion hosts are VMs. You need to maintain them. you need to update them, you need to open port 22 and essentially to the internet. So that's not that's that's a lot of f that's a lot of hassle. So we have a DNS endpoint that does securely terminate traffic and does IM authentication for um the node pools themselves? What happens if somebody compromises the boot sequence and the kernel components of the nodes that run that are running your workloads? you should think about protecting that on Google Cloud. Again, there is a feature for that called secure boot and shielded VMs. Just enable it. It's free. Just like

**[5:10](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=310s)** literally just enable it and use it all the time. There's no downside to using that. And also you can use things like policy controller which is basically a gatekeeper and like an OPA for your cluster and for configuration of the cluster itself. Then speaking of policies, you should be thinking about how you're configuring your workloads. And I mentioned that in your software you're using libraries. What about the supply chain security of those libraries? What if you are using a very outdated version of a component of a library that contains a vulnerability? What happens then? What happens if you are using a new tool, a new skill for an LLM that was published on the marketplace and it contains a backd

**[5:58](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=358s)** dooror and you will install that package on your cluster and it will download malicious software from the internet. Will you know about that? Will you be able to catch it? Will you be able to remediate that? That is very important. And things like workload identity as well. Are you giving your workloads access to all sort resources that exist in your cloud or are you doing arbback? Are you doing fine grained accesses? And how do you do that specifically? Let's look at that. That's called workload identity on G on EKS. It's a similar feature where you have an identity basically for your Kubernetes workloads. What does it look like on Google Cloud? You have your cluster. So here we have a dev autopilot cluster. Autopilot is a type of Kubernetes

**[6:47](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=407s)** uh cluster on GK on Google that cluster has an identity a cloud identity you don't need to create another one then each nameace has an identity automatically um for Google cloud you would typically have service accounts for AWS you would have the roles that are assumed or you would have AWS users and keys instead of that the namespace itself has an identity And uh additionally, each Kubernetes service account also has an automated identity inside Google's IM system. What does this mean? This means that you no longer need to um mount keys, service account keys or AWS credentials into your workloads into your pods. Nor do you need to create IM service accounts as separate identities.

**[7:37](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=457s)** I particularly like having the name spaces separated and have each app each service deployed into a separate namespace and just using the namespace identity. That way you don't even need to create the Kubernetes service account. You don't need to manage anything. You just take the identity which on the top left you have the format of that identity. Um you just take it and give it IM roles. Cool. and then it works and whatever libraries you're using will be automatically calling that endpoint if you're not providing other credentials. So this would use the GK metadata their standard metadata server and be generating temporary tokens. What does this fight against that Tesla incident from 2018 that I mentioned? Well, they had um an insecure control plane dashboard and AWS credentials were

**[8:25](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=505s)** stored there. Somebody stole those credentials and was running crypto mining software on the AKS clusters of Tesla. Lovely. Don't do that. Use dynamic identities. The next thing I wanted to talk about was policy controller which is like I said basically gatekeeper. Policy controller is a GKE Google cloud specific tool. Gatekeeper is the open source version. You have multitudes of policies and policy stacks that you can download and just install on your cluster. This is the beauty of that. The community has already prepared all of the various um combinations of security controls that you can apply to your clusters and just use them there. And additionally, you can um have the policy

**[9:14](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=554s)** controller running on multiple clusters and have a single git repo where all of your policies are declared and it will apply to all of your clusters. So you have really central control over all of your infrastructure which is quite nice and you can of course create your own custom policies according to the requirements of your organization. Now let's talk a little bit about the libraries and the software components and the supply chain security. There were a couple of major incidents in 2024 I think where the supply chain of software brought down entire systems. I think it was the the petrol station um outage in the US which was caused by a vulnerability that was um exploited in a

**[10:05](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=605s)** library that was used in the software. So what do we typically do? We or you as developers um or now agents you just tell them I want to write blah and the agent will go and look for libraries that do set blah and implement it. Do you even check what's in those libraries? Typically no. Um and those that that is called the supply chain and there are loads of supply chain attacks. There are also attacks into your CI/CD pipeline whether where you're thinking that you're building an image of your own application but actually the CI/CD pipeline is compromised and what is get what gets deployed into your infrastructure is not what you built not what you had in your codebase. So features that make sure that what

**[10:55](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=655s)** you're deploying is what you intended to deploy. Um sometimes use cryptography um and encryption crypto cryptographic encryption of the specific images. Um an open source version of this is for example cosign on G you can and on cloud run you can use binary authorization. What happens is that you have um specific KMS keys, encryption keys that you attach to a testers. So here we have a build a tester and a prod tester. What is an atttor? It is a an an entity that you grant a role. For example, you have a CI pipeline that builds your image and then you'll deploy it to dev. The CI pipeline can validate that the image contains no vulnerabilities and that it

**[11:42](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=702s)** is indeed going through all of your testing suites and create a cryptographic signature that will be stored separately in Google cloud and then can be validated on the deployment type uh at the deployment time. So you have sort of an admission controller in your Kubernetes cluster. Again you can implement that with cosine and other open source tools. Then you have run this image in dev. You've run it through all of your non-prod environments. And if you work for an enterprise, you are well aware of the change release calls um review calls that where a lot of people sit and discuss the new releases, the new deployments, new features that are going out into production. Well, you can replace those calls with buttons in your CI/CD. And each button would be

**[12:33](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=753s)** attributed to a specific team. for example, compliance and security. Each of those teams will have their own encryption key that will sign that this image is ready for production. And then your production cluster will require both build and production testers. Meaning that every image that is getting deployed into Kubernetes is validated against an encrypted signature. So it is it cannot be compromised. And these signatures are not attached to an image tag which can be rewritten. It is attached to a hash the um sha sum of the image version. So it really is a very secure way of making sure that you know what's running inside your clusters. There are of course white list policies and things like that. But

**[13:24](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=804s)** really what this uh works against what this works to prevent is a compromise in your a compromise inside the image. If um like like I said for example a library contains a backdoor you use that library thinking that it parses JSON but actually downloads something from the internet when it's deployed and boom you have a backd dooror running in your cluster. If you have something that scans your images at build time and then validates that what is running is exactly the same image that was signed, you can prevent that from happening. Now, Kubernetes is cool and nice, but we don't always need that. Sometimes we can just use serverless services, and Cloud Run is one of them. If you're not familiar with Google Cloud, it's like you take a container, you throw it to

**[14:12](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=852s)** the cloud, the cloud runs it. Cool. Um similar service would be Fargate. Um Lambda is a little bit different, but Cloud Run um really creates you a service in the cloud with an HTTPS endpoint. That endpoint by default is open to the internet. It's just a URL that is generated and somebody can brute force that URL. And something that people sometimes forget to do is just to set basic security restrictions on their Cloudr run services. So those are restrict your ingress to internal and load balancer traffic only and you can also stick an authentication window before the service called identity aware proxy. So you would have a load balancer an identity aware proxy and any user who is accessing the

**[15:01](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=901s)** service like those chatbot demos from the talk just before that they could have had a login window that is managed by the cloud. You don't have to do anything for that. And please don't use default service accounts and in AWS don't use the default roles. Create your own roles and policies and your own accounts for each service. This is how the whole system looks like. Um you have an ingress setting on the cloud run service and the URL here in in red is the autogenerated URL. So if somebody if a thread actor can derive that URL and the ingress setting is left to be default they can go around all of your proxies DNS cloud armor all of your security and just access the service directly but if you have set the ingress

**[15:51](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=951s)** to the correct setting then you don't have to worry about that they will not be able to access now let's talk about data a little bit and here I will be using examples of again um specific Google services. LOB is a posgress database. So really the same technology that you can use on any other service and data center and cloud. BigQuery is one of those two unique services that you can only use on Google cloud. You will have data whatever you do whether it's operational data which posgress is an example of or it's your data lake which is typically what you would use to train and to give context

**[16:41](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1001s)** to your AI agents. So this is really relevant to most of the workloads that you will be developing for postgress for lobb what you need to do is make sure that your databases are private and I wanted to talk about how that private connectivity works specifically because there's more than one way and the way it works is different. The basic way is VBC peering. So when you create a managed posgress database, it will not be created inside your VPC and inside your project on Google cloud. It will be created in a sort of shadow project, a secondary resource that does all of the black magic of managing the resource for you, the database for you. And the peering basically connects two of your VPCs.

**[17:29](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1049s)** There is a big disadvantage to that is that VPC beering is non-transitive. Meaning that if you have another VPC where you are deploying another workload and you want to access this database, you wouldn't be able to do that because of the peering. It is one to one and typically what I see people do, you will have your normal services running in your main sort of Kubernetes, your main services projects, but then the data science team that is developing the agents will be doing it in separate places. So you how do you how do you access your databases? You do it using PSC. It is a different technology private service connect. Basically the database will have an endpoint in into your project and you

**[18:17](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1097s)** connect to that endpoint. There's a bit of setup that needs to be done. It looks a bit like this. Why did they create it in such a convoluted way? I do not know. But your database has an endpoint. An endpoint is basically an IP address inside your VPC and a forwarding rule. On AWS, I think you can use something like well for for RDS you wouldn't even have that because RDS runs in your VPC but you need to ensure connectivity. Um and basically your pods from your Kubernetes are connecting to the database through the private DNS address that points to that private IP address. Now what do you do with BigQuery? BigQuery for those who don't know is a data warehousing service. So that's where you will have most of your data that you will likely be using to train

**[19:05](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1145s)** your agents and to give your agents con context. Bitquery is fully serverless. It does not have an IP address. It does not have a VPC. It does not have an installation of an instance anywhere. In fact, I have had many conversations with compliance people who would be asking, "Yeah, but which instance of BigQuery are you running?" There is no instance of BigQuery there. It's serverless. It's just a data set. No, no, no. But which instance is the data set in? No, there's no instance. It's just a data set. No, no, but which instance? And it goes in the loop because it's very hard to wrap your head around the fact that your pabytes of data are just sitting somewhere be behind an HTTP endpoint on the internet. So, how would you protect that? Aside from IM, right, you have access controls and that's your main

**[19:53](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1193s)** power. um your IM will usually be applicable to either a project or a data set right but sometimes you need something a lot more fine grained and for that you have row based and column based security for the column based security here I'm using an example of the address and the security number social security number of users of your company for example those are protected pieces of information and no AI agent really needs to know that information right or no analyst data scientists don't they don't need that information so what you do is you create do I have that I do have you create a taxonomy with policy tags where you assign levels of security to pieces

**[20:42](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1242s)** of information to the columns and then in the schema of the table you say that that taxonomy is applicable to these columns and then through specific IM assignments You can say who has access to the high security, moderate security or someone who will only have access to non-tagged data. They will just not see these columns at all. Now what about having the same data set, the same table with mixed data? Here I'm using an example of the regions Asia Pacific and Demia. A different use case of this would be for example you are an insurance company and you would like to do some research some data science on the data of your

**[21:30](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1290s)** consumers. You might have a tick box on the sign up form for your company for your product that asks for consent for the research to be done on the data. that consent might be might go into a a column in a table and you just allow access based on the values of that t of that column. This is done through specific SQL queries. So this is not IM this is done within the the table and your data pipelines like DBT or whatever you're using will be the ones that are assigning that access and then the people who have access only to EMIA data will only see IMIA data. they will not see the other data. And similarly for the consent form, you will only be able

**[22:18](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1338s)** to query the um data that has the consent true in that specific respective column. So you can see an example. Select star from this table. Then the only things that are returned are from the the relevant region to which you have access. On top of that is um you can you can use some something called um DLP data loss prevention which is a method of obfuscating data encrypting fields encrypting encrypting sensitive data reducting it removing it altogether. Um it is a little bit more expensive but it is a powerful tool if you are dealing with sensitive data. Now let's talk about parameters to kind

**[23:08](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1388s)** of take it all together. Your all of your resources regardless of where the whether they are in a VPC or not in a VPC, they will be in the cloud, they are an HTTP endpoint somewhere, right? Any resource is just an endpoint in the cloud. It is running somewhere on someone's VM, but we don't always have control over how that is protected. So for that we have global parameters and we have security controls that can encompass your entire infrastructure. For the security controls you have something called org policy or control tower or SCPs on AWS and these are basically rules global security guard rails that are set on the entirety of your infra

**[23:55](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1435s)** that prevent creation of insecure infrastructure. For example, you may say that nobody can create service account keys because keys are the easiest way to leak access to your cloud. Or you can say that the cloud scale instances or lobb instances should not have public IP addresses. You can also create your own custom constraints. Something that we have seen and done previously was um denying IM roles being granted to an individual user because you should be granting roles to groups not to individual users and something for budget like restricting the types of the machines that you can create because data scientists love to create something with 92 GPUs and leave it running over the weekend and then you come back and you have a giant bill. Well, you can

**[24:44](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1484s)** prevent that from happening and through an org policy and you just sleep well at night. You don't have to worry about that because there is a constraint. They will not be able to create a resource that breaches this constraint. And now to a to the second feature that is GCB specific and I personally really love it. Um it's called VPC service controls. Essentially, it is a firewall that operates at level seven instead of levels three and four of the OSI model and it takes in account all of the metadata that can is contained in each request that is sent to the cloud. So typically when you are doing a query to a bigquery table there is all sorts of information attached to that request

**[25:32](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1532s)** such as your IP address your email address where are you querying from which region uh which browser are you using which laptop are you using which device are you using etc. All of that can be used to protect and to filter the the requests. When you have your resources that are running in a VPC, instances, databases, things like that, you can use firewalls, normal network controls that we are all familiar with. But what about those services that are not in a VBC that are serverless buckets and bigquery data sets and functions and cloud run? What do you do with them? Well, that's where you create those parameters around entire projects and all of the APIs that are in them. You can select the the APIs

**[26:20](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1580s)** that you protect. I personally like adding all of the APIs into a perimeter because that's just one shot done. It's all secure and it all can communicate with each other. Once you've done that, once you've put your projects and the APIs into a perimeter, no request from outside of of the parimeter can enter. Only the resources that are inside can communicate with each other. And then you decide who and from where can access your lovely cloud services. You can do that through different mechanisms, ingress and ingress policies, access levels and bridges between different parameters. And you deal with things like IP addresses. Like I said, if you have a corporate VPN, you might

**[27:08](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1628s)** whitelist that. You have a Terraform pipeline that uses an identity, you will whitelist that identity. If you have an orchestration sort of DevOps VPC that um is deploying services into your Kubernetes clusters, you will whitelist that VPC of the orchestration layer of the DevOps layer. So it's really fine grained filtering of what sort of requests, what sort of sources of the requests are allowed inside of your cloud and it really protects all of the APIs. The way it works is by changing the DNS resolution of the APIs. So instead of bigquery.googleapis.com, you will now have restricted.googleapis.com. And every single request that hits that

**[27:55](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1675s)** API will go through the filtering process and all of the requests that go between the Google services stay within the Google backbone network. That is different from other cloud providers where very often a request from your VM to your storage bucket will go out to the internet and then back into your backbone into the the cloud backbone on Google. Everything stays within the Google cloud network and the DNS has changed so everything just stays nicely protected. It is difficult to implement this at first because all of a sudden all of your pipelines are broken, all of your third parties are broken, nobody can access anything. So there is a dry round mode which is the recommended way of starting out with this and then you

**[28:44](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1724s)** filter the violation logs and um construct your ingress policies accordingly. There's a couple of best practices that I wanted to go through to show how how people architect this um in a way that is nice and manageable. If you have data, if your company is dealing with sensitive data or data is your product or just you have a lot of data, you will likely have a data warehouse or a data lake. The recommendation is to have a perimeter around that data lake. This is your core company corporate data. You don't want it messed up. something needs to create the data and write the data. It's likely your services. So you will have a compute or services perimeter with all of your operational stack including your

**[29:32](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1772s)** operational data. So the posgress databases, the MySQL databases etc. And then if you have like an analytics a data science research space you might want to separate that as well because people do all sorts of things. You don't necessarily want those people in the same place where your golden data set is. So you split them out into a separate space and you allow communication between those parameters using bridges or using ingress and ingress policies. Another option is if you're dealing with like less sensitive things and more sensitive things and services and typically if you are a financial in institution or if you are an identity provider or if you're a security company you will have things like data and services that are

**[30:21](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1821s)** generally fine and then you will have your core stack. So you might want to have that core stack and like KMS keys and secrets in a separate space and again a data parameter to separ separate your data and make sure that it's nice and protected. a couple of examples of how that communication will look like because like I said once you've put things and projects and APIs into a parimeter nothing can access it from the outside then how will the data be written into the data parimeter well you can use eress and ingress rules just like firewalls but you define identities projects VPCs and even APIs that can communicate between the perimeters So

**[31:09](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1869s)** you would have um egress from compute saying from a list of service accounts that are writing data or maybe any service account if you trust all of the services to write that data to specific projects in the data parameter. Um and then an ingress rule again the same list of identities that can um input data. You can also restrict it by the API. to say all of their calls to BigQuery API can be allowed. And similarly, if you have for example a data sharing exercise, you have third parties that you are publishing data to, that is a very common use case. You might be sharing data sets, you might be sharing buckets, you might be sharing even SQL databases.

**[31:57](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1917s)** You will want to separate them into their own space because this is somewhere where third parties can access. And then you will specifically allow a single identity to write that data to publish the data, right? Because this at this point in time we're talking about publishing data that is to be shared with third parties and nothing can go in the other direction. So even if somebody gets into one of the projects that are in the data sharing perimeter, they are not going to be able to traverse into your core data lake. And just an example of what those ingress ingress policies look like in reality. Well, in in Terraform reality, you have conditions like an access level with conditions that allow a VPC and a

**[32:47](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=1967s)** bunch of service accounts. This is one of those examples where I said, let's say you have a DevOps environment, a an orchestration environment that deploys into all of your clusters that does Terraform um rollouts, etc. Those orchestrators will have identities and they will also be running in a specific VPC if you are deploying private private runners of GitHub for example. So you can restrict by identity and by VPC and that is the recommended way because what happens if somebody is breaching your Terraform service account your Terraform identity now suddenly they have super high privileged access to your entire cloud. Well, if you're restricting by a VPC and the identities, then they also have to be inside of your VPC and that

**[33:36](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=2016s)** is two layers of security instead of one and that is the recommended way. Another example of what it looks like in the code logs. If you want to collect your audit logs from all of your projects in a same space and those projects are in different parameters, well, you will have egress policies on those on each of those parameters allowing the logging syncs identities to go into a single project that is the aggregator and only write to logging. So this the roles logging log writer means that only logs can be written across parameters. So that's how you sort of filter these things and that that's what it will look like. You will have a bunch of these eress ingress rules on each

**[34:23](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=2063s)** parameter you will have a bunch of access policies. Again it it is a little bit tricky to implement at first and maybe the starting point should be just having a single parimeter around your entire production. That is also super fine. Um, but it just means that data cannot be exfiltrated and you have this extra layer of protection around all of your services. So to conclude all of that, you need to make sure that your runtimes are secure. You need to make sure that your data is nicely protected. Especially in the era of agents that are free to do whatever they want, you need to make sure that those agents can only access the data

**[35:10](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=2110s)** that you mean for them to access and not your entire gold layer with personally identifiable information and secure data that they have no business accessing. And make sure that your parameters, the whole cloud is secure because something somewhere might leak. Lateral movement is a thing. Impersonation is a thing. Breeding networks is a thing. So, make sure that you are doing like a layered approach to your cloud security always. Thank you very much. Again, you have a QR code to the um to my socials and a QR code to the slides if you would like to have them handy. And if anyone has has any questions, I'm happy to answer. And if no questions, you can go grab a

**[36:15](https://www.youtube.com/watch?v=7OjePJ1NVpA&t=2175s)** coffee. Thank you very much.

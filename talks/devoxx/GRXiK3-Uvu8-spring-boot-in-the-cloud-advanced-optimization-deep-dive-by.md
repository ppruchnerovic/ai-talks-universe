---
id: GRXiK3-Uvu8
title: "Spring Boot in the Cloud: Advanced Optimization Deep Dive by Patrick Baumgartner"
slug: spring-boot-in-the-cloud-advanced-optimization-deep-dive-by
conference: devoxx
conference_name: "Devoxx"
category: "Software dev with AI tracks"
edition: "Devoxx"
year: 2026
speakers: ["Patrick Baumgartner"]
channel: "Devoxx"
duration_min: 96
published_at: 2026-04-09T21:31:07Z
video_id: GRXiK3-Uvu8
url: https://www.youtube.com/watch?v=GRXiK3-Uvu8
youtube_url: https://www.youtube.com/watch?v=GRXiK3-Uvu8
tags: []
transcript: true
---

# Spring Boot in the Cloud: Advanced Optimization Deep Dive by Patrick Baumgartner

**Patrick Baumgartner**

`Devoxx` · `Devoxx` · `2026` · `96 min`

[Watch the recording](https://www.youtube.com/watch?v=GRXiK3-Uvu8) · [Conference site](https://devoxx.com/)

## Description

Please subscribe to our YouTube channel @ https://www.youtube.com/@DevoxxForever

Spring Boot makes it easy to launch cloud-ready applications, but building truly lean and efficient cloud services requires going beyond the basics. In this three-hour deep dive, you’ll immerse yourself in advanced Spring Boot techniques to optimize your applications for the cloud.

We will examine in depth how to reduce memory usage, improve startup times, and streamline your Spring Boot apps for modern cloud environments. Topics include Spring AOT, classpath exclusions, lazy beans, actuator configuration, custom JVM options, and additional state-of-the-art tools.

This session combines short presentations, live coding, and practical hands-on exercises. You will experiment with advanced configurations, observe real-time results, and troubleshoot performance challenges alongside the instructor. Real-world scenarios and best practices will help you immediately apply these techniques to your own projects.

Whether you want to fine-tune your CI/CD pipeline or push Spring Boot to its limits in production, you will leave with actionable skills, a collection of code samples, and a deeper understanding of cloud-native application optimization. Get ready for an in-depth, practical exploration of building lean Spring Boot applications for the cloud.

## Transcript

*15,134 words · source: supa (en, exact timings)*

**[0:01](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1s)** [music] >> Good afternoon, everyone. I'm glad you showed up, actually, because when I looked into the schedule of today, I thought really like I have tough competition in the other slots. So, I'm glad you you found the way here and you're interested how to make your Spring Boot applications faster. Um it's a deep dive, so it takes about 100 minutes or so. Um and I'm going to show you many ways how you can improve your Spring applications. Um you have seen probably the talk by Josh Long or so. Um and what he actually never does is he never tells you like how to make the application faster. That's not bad, I'm saying, but I think actually like Josh does a wonderful job, but I'm here

**[0:50](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=50s)** actually to show you where you can you tweak your application, where you can reduce the image sizes for your Docker containers, how to get more throughput on the applications, how to to show you like the nitty-gritty details by upgrading the JVM, using different JVMs, and so on and so forth. So, this is basically like the talk about how to make your Spring Boot applications faster, or maybe starting up faster, maybe like leaner as well, and just like maybe also that you get more throughput in your applications. And I'm showing you many ways, and I'm pretty sure you will have some takeaways out of the session afterwards. Now, of course, I have a repo where you get all the examples and also the slides, and you just can actually like

**[1:38](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=98s)** get it from there, but I will also upload it later to the Devoxx companion app as well, so you'll find a reference there as well. But just if you are interested in want to have a look at it, I will show the QR code later again at the end. So, I'm going to show you some numbers in my presentation because I do benchmarks, and my goal is actually like to show you in which direction we can optimize our Spring Boot applications, and what kind of effect it will have. Because we know, we are adults, we know that if I measure something on my side, you might measure on your side something else. You just can't take these numbers more like as a educational thingy. So, um that means just like measure yourself, your application you're

**[2:25](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=145s)** running production, because yeah, it will have for sure not the same numbers. So, don't take me on the numbers. I might have even some flaws in my benchmarks, but I can show you some patterns there, and that you get an idea in which direction you can optimize your application. So, my name is Patrick. I'm coming from Switzerland. Um I run a consulting business, but also teach at the University of Applied Sciences in Zurich. And I'm also very much involved in the software development scene in Switzerland, and basically we just ran like last week Voxxed Zurich, which was also another great success, and we did it like the 10th time. So, I'm here now to talk to you about like the Spring Boot stuff, because I think if you want to be guest, you have to be host as well. So, now I'm here guest,

**[3:13](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=193s)** and I'm enjoying the conference and telling you about my topics, and last week I was just like the host and giving space to all the other amazing speakers as well. Um yes. So, that's about me. So, what's the challenge, and actually like why does it matter? I think the challenge is really like that Java is an awesome platform. It has stability, and it's it's really amazing what we can do with Java. I'm in the Spring ecosystem since a long time, so I'm doing Spring applications since around 2007, and I think it's an awesome ecosystem also like to build applications with. So, I really care about those two topics, and I think actually like when we bring our applications to the cloud, we know we have some restrictions, and we just need to make those applications great, right? And therefore, I I'm going to show you

**[4:02](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=242s)** these topics. So, let's dive into it. And um let's talk about optimization goals. Like what would you optimize for? And I'm pretty sure there are like many applications which have a different purpose. If you think of like a serverless application, or if you have a long-running application, or if you just have a task which needs to be executed, there are different kinds of applications, and therefore, we might also have like different optimization goals. Right. So, I'll start with an exercise, and that's for you. So, let's take the next 3 minutes and talk to your neighbor. And the goal would be actually like think about your current project. Ask you the question like what would you

**[4:51](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=291s)** optimize in your current application? What would be the metrics, and how you would measure it? Just talk. >> [snorts] [snorts]

**[6:23](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=383s)** [snorts] >> Very unconventional, isn't it?

**[7:23](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=443s)** During the presentation, but I think actually like you had the chance to meet somebody, maybe somebody new, and also like talking about like what your application does. Of course, um I'm not going now to ask like what you came up with, but um I came up with a list actually like from other sessions, and you could actually say like there are some things in there we we could optimize for. That could be the use case, right? When you are like having a process you're going through in the application, you want to make it faster in a way. You could optimize for build time. You could optimize for image size, for startup time, for CPU usage or memory usage, for example, for throughput or requests per seconds, for latency, response time, and many other things, of course. And it highly depends

**[8:11](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=491s)** on your application. So, my things I'm going to show you, they will optimize like in one or the other direction, right? Of course, and and there is not like one silver bullet to actually like tackle all the problems. Um you have to take it with a a grain of salt, and basically like put it into your context if it fits. So, imagine you are going to write a new application and the infrastructure team, the platform team is going to ask you like how many virtual CPUs are you using? Do you know? How much RAM would you use? Do you know that? Well, how much storage? Maybe that's easier. Maybe you say just like none. We write things into the database, of course. Um which technology stack are

**[8:58](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=538s)** you going to use it? Yeah, probably Java and Spring Boot, that's why you are here. So, that's also an easier application and an easier answer. And then actually like the application you're going to build, you probably know as well. But the thing regarding the resources is always a difficult question, and we know things on our developer hardware runs faster than in the cloud, right? So um yeah, we will have to find out. We just don't know. So, um of course, that means like we have to find out like which application we are going to use for that, and like we want to optimize for. So, my agenda of this afternoon is basically we're going to have a look at the Pet Clinic, and the Pet Clinic is always mentioned as the application which is used for comparison. Everyone can download it. It's super simple, and we just use that for our optimization.

**[9:47](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=587s)** We talk about baselines, like measuring different things there, and then actually we can compare against those. We are going to talk about JVM optimizations, so just like from a JVM perspective, we talk about the few Spring Boot optimizations, some other application optimizations. I have only like one thing you can take with you there um because like there are many things related to your type of your application and I just don't know that. And then we also talk about other runtimes uh and so on. Of course, there will be some conclusions and in the in the end, I'm going to show you what happens if you're going to combine simple optimizations into one thing and how much better the world looks like. Of course, the goal would be also that we have like safe optimizations, so you

**[10:36](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=636s)** don't run into production issues because you take that path. So, let's talk about um the Spring Pet Clinic and I'm pretty sure you have seen that at Josh Long actually like was even showing it um on the GitHub repo um yesterday, I think. Yes. And um it's a simple application. It has some pagination. It has some static HTML which is served. So, um nothing really difficult, but you know, it's just a database behind. It's a crowd application that's actually. So, there is also another thing and that's just nice to know. There is a Pet Clinic community and the Pet Clinic community gives you like the same thing but with different technologies. And this is a nice place because it's maintained by the community to get this kind of like um inspiration how you

**[11:24](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=684s)** could use, for example, Spring Boot with Vaadin together or like with something else like React or like HTMX or like doing it like with microservices and other things. So, um it's just a community project and be aware, it's not maintained by the Spring team. The Pet Clinic here is maintained. It's an official project from the Spring team actually. And I'm happy actually also I'm a contributor because like I am going to fix the Pet Clinic every time I do a presentation, so I have like an up-to-date version which I can benchmark as well, right? Well, that's just like how how you do things. So, be aware of that um the Pet Clinic, as I said, is just like the official Pet Clinic we use for our example and actually like it's based on Spring Boot. We use Caffeine as caching. We have

**[12:13](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=733s)** Thymeleaf for the templating engine. We have Spring Data JPA to access the database. You can choose like H2, MySQL or Postgres behind the scenes. And then actually like you can use Spring MVC for the controllers. So, that's just a typical stack for Spring Boot applications. Be aware, in my examples because it's just easier to do the benchmarks, I used the H2. And that makes my life so much easier. But that means also like when you benchmark things, yes, of course, um your application, I mean, you have to take these kind of ideas in your mind. Now, let's talk shortly about limitations. Um if you are going to measure things and especially if you want to go and read, for example, memory out of your

**[13:00](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=780s)** application, you should not use Docker stats to read like what what you get. It's more important actually like that you use Docker top because you get the right data out of it. So, um if you use Docker stats, it's more like the allocated RAM and if you use Docker top, you will get actually like what the process is actually consuming. By the way, and you see that, I'm using the idea basically like from the Quarkus team and they have written a post about that how to measure properly like resources, memory resources on a Docker container. So, um kudos to them and that's one thing. The other limitation is never do benchmarks on your notebook because like your CPU the CPU gets warm and therefore you run into thermal throttling and that

**[13:49](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=829s)** means like your results will look after a certain amount of time just like really bad. Be aware of that. Get a dedicated machine. Get best real hardware, nothing virtual because then actually like you know exactly like what you get. If it's virtualized, it really highly depends on the hypervisor underneath for the results and that might have some impact on your data. Just that you're aware of this. When I'm going to benchmark my application and this is now like a preset, so you'll keep that in mind, of course. We are going to like build the application with Maven. And the Maven build in the end says something like, "Hey um it took like 72 seconds or something like that to build the application." So, Maven will tell you. I'm also going to use here a thing

**[14:37](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=877s)** called a OHA. It's a low test generator and um that means like I just push hard on the rest endpoints of the Pet Clinic to just like generate some load and to measure throughput and latency. Okay. Another thing is what I measure is I measure the startup time of the Spring Boot application. And as you know, Spring Boot also tells you like how long it took to to um to start the to start the application, not to start the container, but how to start the the Spring Boot application process. And now it looks a little bit weird here. I do some bash scripting here. I like to have my output in colors, so that's why you have these square brackets and other um things in there just to make things more visual. Don't worry about those. Um it's

**[15:26](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=926s)** just made by purpose to have like some colorful marks out there in my in my benchmark suite. Another thing I measure after startup time is like the initial memory I'm using. So, um that's the thing I do with Docker top and basically like get out the RSS. And the RSS is then parsed and I will measure basically like the amount of megabytes I'm using in my application. I will do the same also after the warm-up, but also after the load test to like get a get a feeling, right? So, um I'm using here now this OHA tool where I definitely say like I want to I want to have like a lot of load on the thing on my endpoint here and then actually I measure or like take the thing apart which is returned and we

**[16:16](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=976s)** have a thing here um we use here like no TUI. That's one thing and the JSON output is really nice parsable from this application. So, this is just like the measuring tool I'm using and I know like how many requests per seconds I get on my thing. You will see also um later this in the results. So, I'm just parsing the output. Um I measure the memory again at the end of the load test, so I know like where is basically like my peak before the garbage collector kicks in and and cleans up. Okay. So, let's go into the optimization experiments. So, be aware of that we use OCI compatible containers and I'm using build packs and in a minute I will explain what build packs are, but this is the way I do the containers. Then we

**[17:03](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1023s)** use like the JDK 17 as a baseline because the JDK 17 is still like the lower baseline for Spring Boot 4 applications. And we will compare that also to the others. We use Spring Boot 4 um in the test suite we use for integration test test containers. There are some DB migration scripts also running when you start up the application, which has an impact basically on the startup, of course. And we examine build time, startup time, resources, um container image size and throughput. That's what we look at. Okay. So, let's talk shortly about build packs. Build packs is an awesome thingy. Um it's a specification to how to build containers. And I think it's very important and it's very cool because I don't need to write the Dockerfile. So,

**[17:53](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1073s)** that's a huge advantage. And Paketo is actually an implementation of build packs. So, it's basically like tools in a specific way which are manipulating the output of the Docker container. So, when a Docker container is created, you can imagine that every step basically like um manipulates the container and the build packs are just a step in there. I will show you that um in details in a minute. Paketo is awesome because you can use it for different technologies. So, that means like in your company, you have like one single approach to do containers for all the technologies you have and that's a huge advantage over writing your own Dockerfiles because you can streamline the way how you're doing basically containers, right? So,

**[18:39](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1119s)** um just to have a short look at this before we get there, I jump into my console and imagine I've been running here MVN and I did it before just to make sure we have enough internet now. So, I did that. I did the Spring Boot build image and of course, you're doing a build first. That's just like the regular Maven build and then actually like Paketo kicks in. So, we do build image step and it started to download basically like a builder and the builder contains as well like a build image and a run image. So, we are doing kind of a something like a multi-step Docker build. It's not exactly the same, but we are basically like adding things to the run image in your Spring Boot

**[19:27](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1167s)** application. And we can see here that like six of 26 Paketo build packs are participating in the build. And these kind of things will be executed. So, um you see things like, for example, here adding CA certificates, doing some Java stuff, adding things here. You see also that stuff here. So, actually like Java gets installed also with some helper tools. Uh we have things like here the the executable JAR file. So, basically Paketo unpacks your fat JAR, and it it adds your Spring Boot application exploded because it starts faster by default. That's anyway a good thing you should do by default that you just use the the the tools to basically like separate the layer from from the

**[20:15](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1215s)** layered JAR file, and if you run it exploded, then it runs really faster during startup. So, build packs just do that for you. Then we have some Spring Boot build pack where we will see later AOT is enabled, AOT caching, and other things. And then at the end it's exporting the different layers and creating an application, right? And we have now our Docker container. And what I can do now is I just take the output of the whole thing, I type Docker run, I make it colorful, and we just start that one, and you will see here the Spring Pet Clinic is up and running. And now what you also need to know is that in one of the build steps actually, that part up here has been done as well for you. So, build packs add something

**[21:04](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1264s)** which is called Java memory calculator. You see that up here, memory calculator, and it takes the memory provided to your Java process into account to do the sizing of the heap, for example. It does some calculation behind the scenes according to the classes which are in the class path. So, that's that's what you see here with this thread count which is guessed for a regular web application like Spring Boot. If you would go reactive, it would use actually like 50 instead of 250. Um it counts the classes, and then it calculates basically the X parameter for your application. So, this is just done here XMX max meta space, this kind of thing. The build packs also add things for example like exit memory error. That's awesome because like if your process

**[21:54](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1314s)** really like does nothing, and we have an out of memory error, um the Java process stops, and Kubernetes will just like rerun it again. Huge advantage. And the OOM killer does not need to kill the Java process, right? So, this is just like built in, and Kubernetes will just like reschedule another another pod for you. We have some um diagnostic options in here for native memory tracking and so on. And another thing actually like which is done here for you is the Spring Cloud binding. So, that's basically like the liveness and the readiness probes are enabled. And now actually like if we go even a step further, you could see in the build packs that it's running with a non-privileged user. Here, it's it's the Cloud Native build packs user, which is also awesome

**[22:42](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1362s)** because like in the internet you find a lot of examples how you do like your Docker file, and often they run with root rights, which is super bad, of course. So, build packs actually help you to do the containers the right way. And I think it's really really awesome. And you just saw basically like how they work with the different steps here, and there is much more to it. So, if you don't know about it, you should definitely have a look at it. Let's jump back to the presentation. Now um let's find out together. So, what I said before, the part on the top is basically like build packs related because if you run your Spring Boot application just like regularly with Java minus JAR, it won't show up. And then we said here down here the the user, the CMB user actually like is there in the container.

**[23:32](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1412s)** So, I'm telling you actually that build packs are 1,000 times better than your regular Docker file because you are not doing mistakes um when you're doing build packs because the community is basically maintaining it and does it the right way. On the other hand also, um it's even more secure because of the build packs community and they drive that forward. And you probably know from Heroku, but also from Cloud Foundry, build pack is a technology which is there already like for I would say like 15 years or so. So, it's super stable. It's not on the platform anymore, it just have moved into your pipeline. So, that's basically the difference these days. Good. And that's Josh Long, you know him? And he said, "Friends don't let friends write Docker file." Yes, build packs are so much better, and I totally

**[24:22](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1462s)** agree with that. Great. But let's talk about like benchmarks now. So, imagine now we want to do the benchmarks and try to find out like what is the build time, what's the image size, and so on. What's the startup time, what's the memory usage, what's like the throughput and the latency. And be aware, I did it with two virtual CPUs and 1 GB of RAM for the Pet Clinic. This is usually like more what you have in your production. So, the numbers might look better than the numbers would look at your application, for example. But it will have also some other slides where I lowered the amount of CPU just to show you the difference actually for certain features. Okay. So, let's just do the baseline. And baseline means like no optimization.

**[25:10](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1510s)** I just run a regular JDK 17 build, and you see I'm using SDKMAN. I set the Java version, I do the build of the image, I run it basically like with the things we have seen before, and the build time takes me now 66 seconds. I have like an image size of 280 MB with the Pet Clinic. It takes like 5.3 seconds to start up. I have like 200 um 60 MB of initial RAM, 300 MB of RAM afterwards. I get through about like 235 requests per second, and then you see basically like the percentiles towards the end. I didn't put the 99.99 there because you know the outliers would be there, too. But we get a feeling that like 99.9% of the requests get through in like 1

**[26:00](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1560s)** second. Perfect. With max load basically. I'm really like do a burst load here. Now imagine we move to Java 21. And Java 21 has some amazing features like for example virtual threads. And that means I just do another build, and now we can compare it, right? What we see is like Java 21 is a little bit beefier than actually like the Java 17. Um we will have like faster startup times, as you can see as well. Um RAM usage is slightly higher on both ends. I get more throughput, and I see also like better response times in general. Right? So, now let's think of the next step. Well, if I do Java 21, we also can do

**[26:50](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1610s)** like Java 25. And here we basically see we see even a better um startup time here, but with the drawback that the container image got way bigger. We will have to work on this, of course. And we have a little bit less like throughput here, but we are roughly like with the percentiles, with the response times, in the same ballpark. And I would even say like if it's below 10% a difference to the to the other numbers, then it might be also like something uh the error in the measurement. That could be. So, they're like really really close, which is actually amazing. Now, of course, there is Java 26, and we see that there is even like some improvements. And some colleagues of

**[27:37](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1657s)** mine did they just had a a talk before they say like Java 26 is boring. No, it's not boring. It's boring in terms of the language, but not in terms of the features you get because you can also see like we have higher throughput, we have better answers basically, faster answer times, and yeah, still the the image size is quite a bit big. Now, just that you are aware of like the measurement, Java 17, which I added a clock there because I will always use it as a reference, is not that bad. And we have seen that before because like um Java 17 has the 18th release or the 19th release actually. So, a lot of like back ports of like um

**[28:25](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1705s)** let's say performance updates went into the 17 release. And for me, I'm doing these benchmarks already for quite a while, I see that actually like the 18 makes a huge difference. So, if you would have like 17.0 release for Java in production, change it to the 18, and you will see it's it's performing way way better. With Java 21, we just have like the the 11th release basically. With 25, it's like the third, of course. And with 26 basically like it's the first one. So, I usually see that the the latest release basically like the 26 um since it's not an LTS version, it will not gain so much updates, of course, because um in September there is the

**[29:14](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1754s)** next release of Java 27. But also like you really see that the JDK needs to ripe over time, and that's what we saw for example with 17, with all the 17 releases. So, like really make these like micro updates, it's really worth it. Just telling you. I think that's very important. Now, let's talk a bit about um Java optimizations. And I start with the first one because like I hope you're going to forget about this again because no verify is a feature where you turn off basically the bytecode verification. So, that means like your your classes can be loaded faster. And fun fact, as we can see, we have like 1 second faster startup time just because because we turn off noverify.

**[30:01](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1801s)** And in certain environments, like for example, if you use an IDE or so, and you want to make the IDE starting faster, they will put the argument in there just to make sure you get like faster feedback, right? There are all the features we're going to see later. They are also used in IDE when you start the Spring Boot application. So, I'll tell you there. But, actually, you should not do that in production because it's a security feature, right? So, don't do that, but I'm just still showing you the feature is there and you start up faster. So, if you know what you're doing, maybe you can take it. There is another feature which came actually like with the recent versions of Java. This used compact object headers. And there are a lot of people out there saying, "Hey, this I am compact headers are actually like a game-changer because we can save

**[30:50](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1850s)** memory." The only thing what I see, and you can see that in the amount of requests you are doing, basically, is that that costs because the compact object header has a compression algorithm in there for the references. And because we have to decompress this, it means like the throughput gets worse, right? So, that's like a drawback on one side, but actually like it might help you with memory consumption. It depends also like how much objects you are creating in your application. Right. But, try it in your application. Maybe it helps you, maybe it doesn't help you so much. It really depends on the use case. Another feature which is actually quite interesting is about the cheat. So, the just-in-time compiler. And we can use

**[31:41](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1901s)** something like tiered stop at level one where we deliberately select the C1 cheat compiler, which is made for optimizing startup times. So, if you do serverless or something like this, this can really help. And fun fact, when you look at the three dots when you start the Spring Boot application in IntelliJ, you will see it's done on tiered stop at level one. Because in IntelliJ, you're the only user. They want that actually like your Spring Boot application is starting faster. So, they're adding for you the parameter that you have to feel in. You don't have proper load on those applications which you run through the IDE, right? It's more important that they are faster executed. So, if you are now setting this, for example, for level one, then actually

**[32:28](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1948s)** like startup time actually like gets better. And it it really depends on what kind of application you're doing because if you are on C1, you don't have like optimizations during the runtime. So, this is just optimizing on startup. If you put level two, three, and four, you have like C2 in place, and therefore you will have like monitoring going on and optimizing the hot paths while your application is running. So, this is a thing for applications like serverless or things which just need to start fast and do not have a lot of users. Now, you could say like, "Yes, we could actually optimize with the garbage collector." Of course. And I have seen, for example, that if you're using parallel GC, which is a throughput

**[33:17](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=1997s)** optimized garbage collector, and it's not the default actually like when you use buildpacks, you get really like higher throughput on the things. And that's like what you see on the numbers. And why not just using that instead of G1? Because it's throughput optimized. And when we do web applications, when you have a Tomcat in place, that's really like the nature for this kind of thing. You could also say, for example, "Yeah, let's have a look at another garbage collector like the ZGC." And we know in Java 25, the generational one actually like became the default, but I still put like both parameters there so I can compare it like with Java 21. And that might help you, but here the point is just like we make sure that the garbage collector run does not take more

**[34:05](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2045s)** than 10 milliseconds. So, that should have an impact on the response times of your of your request you're doing against the application. So, you could play with those things if you want to. And therefore, because there are so many options and I don't want to dive into all of those, I found a project from Chris Who Codes, and he has basically like with all the JDKs out there from all the different vendors which build actually like the OpenJDK, they have the parameters in there and you can look them up. And you can find in thousands of parameters the right ones you probably want to use, right? So, awesome page. Have a look at this. And also, I think all of the or almost all of them are also supported by the buildpacks. So, you can basically choose in

**[34:53](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2093s)** buildpacks which one to use. We'll see that later. Now, let's dive into Spring Boot optimizations. In Spring Boot optimizations, actually like we're going to have a look at the lazy beans because there is lazy initialization and we can say like, "If we are going to lazy initialize our application, we might start up faster as well." Of course, the answers should be still at the same level because we warm up basically the JVM, and that means like all the beans are created so you don't will see like um like slower answer times. And that's what you also see like with my results. They're like pretty much the same. Right? So, what we do is I just we optimize for the startup time and I say like spring.main.lazy-initialization=true. And I optimize as well Spring Data and

**[35:42](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2142s)** Hibernate so that Hibernate only initializes when we are going to use it. Of course, there is a drawback with that optimization because you know now when you run a Spring Boot application without the lazy beans, it might fail during startup on the main thread. So, basically, the process is not going to start if there is a misconfiguration. If we do it like with lazy beans, the beans will be created on the first request, and the thread which is going to create those beans is the worker thread of the Tomcat. So, that means like your application is not crashing if there is a misconfiguration. So, there is like a slow a slow or a small drawback in that, but I'm pretty sure you have good integration tests in to make sure that your config is right, right? Exactly. So, but that's an idea if you want to

**[36:29](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2189s)** optimize on faster startup. And if you don't care about so much about the first request right? Another thing actually like you probably know about Spring Boot is Spring Boot when you load application properties and then application.yml files, it looks them up in four different places. So, it looks actually like in the working directory of the JVM. Everything up, there is a folder called config. It could be in there. It could be also like in the classpath on the root level and in the config package, basically. And what I'm doing here is basically saying, "Hey, there is a property spring.config.location. That is the only configuration which you need. You don't have to search for the right one." Right? And therefore, I should have a slight faster startup time because a check on the file system is

**[37:17](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2237s)** not needed, right? Everything else should stay roughly the same as we see as well in the numbers, of course, because it doesn't have an impact on the bean creation, the amount of beans which are created, and so on and so forth. Great. Now, actually like an awesome topic is no Spring Boot actuators if you're brave enough, right? I mean, we love actuators. They add so much valuable things to the application. But, what I want to point out here is if you use the Pet Clinic without the actuators, you have 282 beans in there. If you use the actuators, you have like 457. So, that's that's incredible, right? That's like almost 180 Spring beans.

**[38:05](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2285s)** Crazy. And of course, creating 180 Spring beans takes time, takes effort, and so on. And also, having all these measurements like metrics, like all these observability features in place, it will take some time when you run the application. So, if you can remove it because you don't need it, right? Then just do it. Don't put it there because you are having additional overhead in your application. I'm not saying you should do that because you should always do that. I think there are valuable features in there, but if you don't need something like this, then just like don't put it there because it costs, right? So, I was also testing this with different Java versions, as you can see. And if you remove it, you really have faster startup times, but you also have

**[38:53](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2333s)** like faster response times and more throughput, of course. Great. There is another topic we need to talk about. And it's ahead-of-time processing. Ahead-of-time processing is a feature within Spring Boot where you basically are not relying on component scan and entity scan anymore. So, what happens actually like during build time is this all these things get evaluated profiles conditionals and so on. And then actually like they generate for you code which registers Spring beans in code. So, at runtime, we won't have classpath scanning and entity scanning anymore. By the way, it's a replacement for the Spring Content Indexer, which is not there anymore since quite a while, but I

**[39:43](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2383s)** think it's a good actually option to to have this kind of replacement because it makes my starting faster. Ahead of time processing will be related to AOT and native images from GraalVM, but you can use it without it. And that's actually like a huge win for us. So, what you're going to do is actually like in the Spring Boot Maven plugin, you just can say with one line there, you can say spring AOT enabled equals true. You add in the execution the process AOT part in there, and when you start up the application or the container, there will be a property set which enables basically like the classes to register the spring beans. So, the property is called spring AOT enabled equals two, which is actually

**[40:31](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2431s)** passed, and the build packs will do that automatically for you. So, you don't have to explicitly mention it, and that means like we have way faster startup time. And it's just part of the build process, additional classes get basically like generated and compiled and of course then executed during startup time. If you don't have the property there, and if you would do it manually of course, then actually like it will still do component scanning and entity scanning. But startup is way better better, the rest should be roughly the same. Good. Another thing actually like is JMX, you don't need that. Why do I put JMX here? Because if you run Spring Boot in your favorite IDE IntelliJ, they will turn on JMX to have the tabs open for the health

**[41:23](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2483s)** end points for the spring beans and so on. They will read out of the application which you start within IntelliJ out from this JMX end points basically. Now, when we put our application in production, we don't need JMX because nobody can use JMX in a cloud environment because we you would use actually TCP to connect directly to the application. And since Spring Boot 2.2 it's turned off, and also in build pack by default it's turned off, so you don't have to care. You could enable it if you need it, and that's easy possible with the configuration I'm showing here. But anyhow, don't worry about it, it's just turned off and it makes your application better and faster for your cloud environment. Do you see? Spring Boot is great and

**[42:10](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2530s)** build packs as well, right? So, it's really amazing what you can do here. Great. So now, let's shortly talk about application optimizations. Application optimizations, there is a thing where I can tell you you should use as well. There is actually like a a tool out there which is called dependency cleanup. The thing I usually tell people when I run spring trainings, I tell them starters are here to start, but not to finish. Because starters will pull in transitive dependencies, for example for the web topic, and if you don't need Jackson for example, why should you have it as a dependency in your Spring Boot application, right? So, maybe it's better you get into control of your dependencies yourself, and dependency cleanup can help you with

**[42:59](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2579s)** that. So, dependency cleanup is actually a Swedish product which was done actually at the university in Sweden, and someone I know actually like did the PhD around the topic and so on. There is a Maven and the Gradle plugin, and you just can execute that one to find out which dependencies are maybe maybe not used. So, what you also could do is you can generate a de-bloated pom file, and the de-bloated pom file is a pom file which means like it doesn't have like the the transitive dependencies, it it moves all of them up to the main level, so you know which dependencies are there. You can also use this as a build breaker for example, where you say that if you use unused dependencies in your application,

**[43:49](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2629s)** then the build should fail. So, the dep clean plugin actually, and the command you see at the bottom, that's like how you could execute it if you want to. You want to exclude certain dependencies for test, provided system and so on. These different kind of like Maven scopes. You run it, and then you get the report. And because the report gets quite big and it takes me about like 20 seconds, 27 seconds to execute, you will get an idea which are your used direct dependencies, used transitive dependencies, and then you see others like potentially used also potentially unused direct dependencies, potentially unused transitive dependencies. And of course we know Spring actually does quite a bit of reflection, so it will not catch like

**[44:36](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2676s)** all of them, right? Hibernate does the same, but I'm pretty sure you would know like what's Spring related, what's Hibernate related, and what you could leave out, right? And certain things which are mentioned here also like are the starters because the starters is just a pom file, and the pom file is just 4 kilobytes. If you have dependencies in here with 4 kilobytes, it's highly likely that it's just a starter and it has transitive dependencies. So, that means actually like you could clean up your application, you can get a gut feeling what's maybe needed, what's maybe not needed, and it can do some trial and error to exclude the dependencies and to reduce the application size. And just because of certain new features you and your colleagues are going to add, just to add a full starter, it's sometimes really not worth it. Maybe

**[45:24](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2724s)** just like pull in the dependency you need, and that's it, right? You still can leverage basically like the version management from Spring Boot. So, it's definitely worth it. So, what what I basically did is in my example, because I don't need REST end points, I could remove for example Jackson. I could remove like the database drivers which were not needed, and has anyone done web sockets with Spring Boot? Probably not. There is a embedded Tomcat web socket library in there, and most of you are not using it, so you just can remove it because it's not needed for example. So, you get the feeling like for what it's there and what's maybe not needed. Be aware of there are some challenges because like it doesn't know about auto

**[46:12](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2772s)** configuration imports, it doesn't know about Spring XML files, which are still supported with Spring Boot 4. It doesn't know about web XMLs. It knows about reflection. If you say for example when you load classes with a string name, it could find out those things. Yes. So, that means like if I'm going to clean up things, you will actually realize that like build time get faster, and it looks exceptionally fast. I'm surprised actually here. But also the image size you could reduce by a few megabytes compared to like the others. So, that's something I can tell you about your application optimizations. Because other topics like those I cannot tell you because I don't know

**[47:00](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2820s)** your application. I don't know about your connection pooling you do. I don't know about the caching for example, the database queries, the batch processing, and all these kind of things. So, this slide should give you just another idea what you could probably like improve on your application, right? But what I have shown you so far is just like that's something everyone can do very easily. And I will continue with those ideas so that everyone can actually like improve the applications with some good guidelines. So, let's talk about some more JVM optimizations. So, who of you knows jlink? A few, just a few hands here. Thank you very much for putting your hand up. Jlink is actually an underestimated tool tool in the Java space. You can actually

**[47:50](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2870s)** create a custom JVM for your application. So, imagine there was the idea that we modularized the JDK. And why? Because we can create a custom JVM for the applications. Not that we build like modular applications with the module system from the JDK, but what we could leverage here is definitely we could say like we don't need all the modules actually like from the JDK, we just pull in those we need, and we run the JVM like this. So, so jlink basically as an example here on the command line can be executed, and we can create with the output parameter on the last line a specific Java runtime for our application, and then you can use this one to run your Java code.

**[48:39](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2919s)** And of course it looks a little bit complicated with different parameters and so on. We can make this easier because the build packs enable that for you just by using one parameter. So, if you say build pack JVM jlink enabled equals true, it will just like try to find out which modules are needed, and then it will shrink your image size quite a bit. And as you can see, if we look at the reference, I mean, hey, that's like 90 megabytes roughly, right? For the JDK 17. It's quite a bit. And that means also that the startup times might get faster because of that, and so on, right? So, that means also like less memory usage and so on.

**[49:27](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=2967s)** Now, of course, there is also a tool which is called jdeps, and you could actually go and check out like which modules you really need. And that's also quite powerful because you can actually like use this command. I'm just basically like unpacking my Spring Pet Clinic, my fat jar, and then I'm going to check actually like which modules do we need. And I'm pointing to the class path which is within the Spring Boot INF lib folder, so where all the jars live to find out which modules are needed. And the output is basically like what you see in a a very small print at the bottom. That's like the output of that. And now I could use that output here and I can add it to the Spring Boot Maven plugin.

**[50:15](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3015s)** And I added some additional parameters like compressed two, like no header files, no man pages, no debug stuff, because who is debugging in the cloud? Nobody, right? So, just like get rid of all this stuff and we can actually make it even better. So, we got rid of 10 more megabytes. So, that means like we got rid of 100 megabytes just by shrinking down the JDK. Awesome, isn't it? I think so, it is and it's super easy to add to your project, right? You don't have to go this way, you just can actually like keep that way and it's good enough, right? So, it is super simple. By the way, does anyone has recognized that there is a package in there which is called Java Desktop? We are writing a a server application, not a desktop application, but this is

**[51:04](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3064s)** like the biggest module they have created and they could not clean up further somehow. I don't know why and it's still there actually. And unfortunately, I would love to get rid of this one, too, because like it will shrink down the JVM even more because that's one of the biggest modules. Great. Let's go another step. There was a thing called class data sharing and probably you have heard of AppCDS. And AppCDS is actually an approach where you are starting the application where you dump the loaded classes to an archive file and then you restart from this archive file and you are not needed to search basically like the the classes in the class path. You just pull in this this

**[51:52](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3112s)** file and that's basically it. And AppCDS is really awesome because it's another parameter. It will do a training run during the build and that means like my my build time takes a little bit longer as you can see because a training run is needed. And the training run will be done during the startup of the Spring Boot application. We have there a parameter called Spring Context exit on refresh. So, that means if the Spring Context is fully initialized, there is a file stored in the archive class at exit. So, this application CSA and then during startup, we load it with an additional parameter at the bottom which is called shared archive and it will try to find the classes in the shared archive first and if it's not in there, it will search in

**[52:40](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3160s)** the class path. So, this is a thing which does not harm anyone and it makes your application start faster like hell, right? As you can see here. I didn't put Java 25 there because of a reason. You will see later in a few minutes. But we just like cut the startup time in half with AppCDS. But we know it build time takes a little bit longer because of this training run which is needed. Great. So, you see Spring Boot and Buildpacks are awesome, right? Yes. Okay. Now, let's get a step further. Because of Project Leyden, they introduced in Java 25 a thing which is called AOT caching. It's not the same like AOT processing. You can combine both, but this is basically like the

**[53:31](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3211s)** next version of AppCDS. And in Buildpacks, you cannot do AppCDS anymore with Java 25 because the default is set to AOT caching, which is a very similar approach, but still like slightly different. So, AOT caching basically like is a thing where we do also a training run. So, that means like we start up in our build pipeline the application. We do the same like this Spring Context exit on refresh basically and then we pump the output into this AppAOT file. And then we pass the argument also to the application when she is starting, right? So, this is just like the next version of AppCDS and it belongs to the whole idea of making application faster in the

**[54:19](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3259s)** cloud with Project Leyden. So, that's the thing and you also can see here, man, it's getting so much faster with Java 25, right? That's awesome. There is like a slight drawback because we dump these files basically in the build time into the container, the container image size gets bigger, of course, right? So, this is definitely like one of the disadvantages, but if you can deal with that, if you have a fast network to like move container from A to B, then that's not a problem because you can you win quite a bit, right? AOT caching can go even a step further. You could actually do a training run basically like making your application warm, hitting all the hot paths and do the dump there. What we do

**[55:07](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3307s)** here is just like the easiest way which works with for all the applications which is start the application up and do the dump. There is like one story I need to tell you. I added AOT caching into the GitHub Actions pipeline and I have a NAS at home which can run Docker containers and I think my NAS at home is actually like the perfect place to run an application which I'm the only user. But it's a bit older. It just has a Celeron processor and the thing which was produced here did not run actually like on the Celeron processor. So, the error message which was like written out was like there are like two um um commands for the processor which is which are missing on the Intel Celeron which is on the processor which

**[55:57](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3357s)** is used actually like um on GitHub Actions. So, you have to make sure it's related to the architecture of the processor like what you dump. So, you cannot be too far away um between [snorts] the different versions of the processors. So, be aware of that. That might be an issue. There is an idea at the moment on the mailing list where they try to uh on the mailing list of OpenJDK where they try to make it like agnostic to the processors like we had like with AppCDS because that's really agnostic, but um this one it still needs to develop a bit, right? But Project Leyden is still going on, so I don't see a point actually like why it should not evolve and get more stable. At the moment, um they also have this instruction dumped into the into

**[56:45](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3405s)** the file and that's the way it is. But I think it's a super cool approach actually like to make your startup faster and even if you would script it your own, you could even do a training run, um hit all the hot paths and even like load the application in an optimized state. And this would this will evolve with Project Leyden quite a bit in the future. So, now let's talk about another thing which is called other runtimes because I mean it's not just like the OpenJDK we are using. There might be other versions of it. And one version you probably know is like OpenJ9 or Semeru from IBM which is optimized on better memory usage. So, if you have the issue that you almost scratch at the at the border of the

**[57:32](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3452s)** memory, you just could switch to this one because this one is more optimized for better memory. So, um OpenJ9 has a nicer um website now. For a long time, they always compared to Java 8 because they knew they are like during runtime not so much better than actually like the OpenJDK. Actually not, right? So, with 17, 21, I have seen like way worse numbers. Now, it's getting better actually like if you do it with OpenJ9 and you also see like response times here which are really not so great. But if you look at the memory consumption, if that is your goal, this is definitely a way to go. And what I did here is I explicitly configured the Buildpack for

**[58:21](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3501s)** OpenJ9 because by default, it will use BellSoft Liberica as a default version. What I additionally added is the Java Buildpack because this is a Buildpack which has transitive dependencies and in there, we are basically overriding the Buildpacks for the installation of of Java. So, the last line is really needed to get also the Spring Boot Buildpack, some other step like the CA certificates, like the Spring Cloud bindings and all the other things, the S bomb stuff actually like working. So, you need both lines, but you can easily switch actually like the Java installation there. So, as I said, better memory, we see it here, but it's not really like way better with startup times and response times. But, there are

**[59:11](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3551s)** optimizations for the cloud which you could use and they make it better. So, find your way there. You still can tune it a bit. I just went with the default settings here. Now, you could say actually like GraalVM. I'm not talking about GraalVM native images here yet. GraalVM is actually also a Java runtime. It uses HotSpot underneath, but they have a better cheat compiler. So, the just-in-time compiler is not C1 and C2, it's the Graal one. And that means like it has way more optimization steps for the hot path, and that could be very interesting. When we look at the image sizes here, GraalVM with the Java stuff in there is quite beefy. It's huge, right? We would have to run jlink. I'm not sure actually like if it works here. I hope so. I have not tested it. But um

**[60:00](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3600s)** the the just-in-time compiler is better, um but we don't see much here actually like in the response times. Doesn't matter. You could also go like for an Oracle JDK if you like to. Just switch it to the Oracle buildpack basically. That's it. And it's interesting, right? The OpenJDK is different from the Oracle JDK because they do additional optimizations. Which is amazing, right? So, this is just not what's in the OpenJDK. That's really like the the Oracle version, the the licensed version basically. Um but you can add that via buildpack as well. So, startup times are way better. Great. You can also use the same with Azul Zulu, for example, if you want to.

**[60:47](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3647s)** Another OpenJDK build, same pattern here. They also have some um optimizations as you can see with the startup time, for example. But the rest is not so much different, I would say. Okay. Now, um let's talk about another thing. So, the other thing is actually like, yeah, white. Um we just used the the Paketo builders, the buildpack builders. And that's the way how the container is created. That means like there is an Ubuntu inside. Well, maybe your security team does not like Ubuntu to run in production. Maybe you want to need something like UBI 9 from Red Hat. You want to maybe need a Alpine Linux, whatever. This is possible here too. So,

**[61:35](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3695s)** the thing is actually like if you want to go with an Alpine Linux, um BellSoft, the company behind um um Liberica, the JDK, and basically that's also like the JetBrains runtime and the supported version you get from VMware, for example. So, that's the same same. They also have their own builder. And they have a builder actually like which you can use a different tag. And basically like what we reference here are just like all the Docker containers basically. So, in Docker containers you can just store anything, right? So, we can use the muscle tag and we have an Alpine Linux in there. So, um that means you can switch between muscle version because um BellSoft has a JDK which works with muscle. So, that's the the the the C library basically like from

**[62:24](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3744s)** Linux. Or you can use the glibc version in Alpine. What means is that you get like a way slimmer container image because the Alpine base image is roughly 20 MB. And that makes a huge difference um than if you would use something like um the Ubuntu base image from Paketo, for example. So, they have done that. By the way, the run image um has also changed now. I think they have something in place where you can even get hardened containers as well. So, you would see if you do something and like maybe can show that here. Um Oops. What's that? Just jump out here. Let's go to the console. And let's do something like Docker images. Let's stop the application.

**[63:13](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3793s)** Docker images. Uh sorry. So, here we go. They have here something called that this is our their builders. So, like how to basically install the JDK and so on. And then we have the run images here which are called hardened run, right? With muscle and glibc. So, they do something there. And that means like they're better than than the regular containers you might get. Okay. So, just jumping back here. So, I mentioned basically that um the M means in the list that's muscle and the G means like it's glibc. So, yes. And what we also can see here basically like smaller image sizes, faster startup times, RAM should be roughly the same,

**[64:04](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3844s)** but also faster response times. Right. Great. Um do you actually know like how long my benchmark suite runs? It's now 2 days basically because I have so many different things I'm testing. So, um it's quite a bit of time um which is spent basically like to find out all those numbers. Okay. So, you could actually go and say like the current version of the buildpacks is using Ubuntu Noble. But maybe you say like I want to have Ubuntu like um 2004. No, that's not true. 2204, I think. Um that's Jammy, right? And you can just choose like a builder which is called Jammy Java Tiny, and that's a tinier one than the regular one because the regular

**[64:53](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3893s)** one can do more. And they just like slimmed down basically the builder so that it only works for Java uh and keep it as simple as possible. So, we could actually switch like to an Ubuntu Jammy, for example, by just placing the builder link there basically. Reference with the builder is always the build image, but also the run image, of course. Good. And so, that means like you could switch. Now um I was always looking for something like distroless. And um I have tried it. And I did it. So, um as a pet project, I just came up with the idea, let's do the distroless builder and let's make sure that we have distroless from Google basically because

**[65:41](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3941s)** there you don't have a shell in there, right? So, nobody can jump in and execute things because it's just not there, and it would be even more secure. So, what I did is actually, and this is something I can show you here, is I have done a a project basically where I now published it for the even for the conference as public, um the distroless approach basically, and I can create distroless images. Um this is the one which basically runs with Ruby, Python, JavaScript, and also such things, and also Java. But I also made like a tiny one. When if you are going to look at, first I tried basically like to have distroless in there with the all the C and C++ libraries, and then I found out I could even slim it more down by

**[66:28](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=3988s)** removing the C++ libraries. And then actually like I copied everything into a scratch container, which is basically empty, and with the buildpacks I can install Java and can make it even slimmer. So, you can do those things if you want to, right? And it's not that difficult if you take some time, or maybe if you ask AI to do so. Okay. Just that you get an idea. You can choose. You can even do your own ones, and basically like your your run image can be adjusted because it's it's just like a Dockerfile. So, buildpacks has since a 1 or 2 years the possibility that you can basically like inherit from the run image and even manipulate the run image and do your own one. Okay.

**[67:17](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4037s)** Great. Now, let's get to the real thing. I mean, Alina was here talking about GraalVM and native images because that's a super cool thing. And of course, that helps like in certain cases if you are doing something like serverless, or if you have a lot of application like which need to scale up and scale down all the time. Um but if you would just like start the application once, it's maybe not worth it to spend way more time in the pipeline. So, it has a use case, for example, for serverless and so on. And what I did here is I basically created with the profile native, you might know from Spring Boot, like a native image. And wow. I mean, you see it takes like four times longer, up to like five times-ish

**[68:06](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4086s)** with the Oracle GraalVM distribution, which is not the the one which you usually get, right? That's like the the Oracle GraalVM free edition. So, this one takes a little bit longer, but you see basically like that the image size gets smaller, that the startup is insanely fast. You have way more throughput just by using it, right? Less memory consumption than way better response times. But definitely, this is not the thing which you can do like all the time because you're spending four to five times more in the pipeline. So, it's only worth it if you scale up and scale down your application all the time, and you save this amount of time and resources you're putting into your pipeline right? For the GraalVM the Oracle version,

**[68:57](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4137s)** and that only belongs to the Oracle version, there is like profile guided optimizations. That's a feature where you can basically like instrument the hot paths in your application, and you can even optimize those hot paths. It's not done here. So, we didn't do optimization on the hot path. It could get even faster, right? The community edition doesn't have profile guided optimizations. And some people are not talking about that when they are doing presentations, but I'm telling you. So, um yes. So, be aware of this. And if you use Mandrill, for example, or Liberica NIK, which are basically like based on the community edition, they don't have this profile guided optimizations. There is another thing I came across.

**[69:44](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4184s)** Probably have heard of CRaC. Sounds like a drug, but it's not. So, the idea with this coordinated restore their checkpoint is an idea that you do a dump. You start your application, you dump basically like the whole process and the memory, and you restore restore from there. Basically, you would invent a time machine, right? You can go back to the future back back to the past because like if you start the application, the clock will be for a short time the old time until it gets synchronized. It's only accessible from Azul Zulu and BellSoft Liberica and their separate builds. You have to be aware of that. You need a Linux to do that. It's not working on Windows and Mac OS. For development purposes, they have like

**[70:32](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4232s)** a simulator for this, right? So, that's the thing. So, it only works on on Linux. Be aware that you snapshot on the same architecture, the same version of the operating system like in production. Otherwise, it's not working, right? So, be aware of this. So, basically, it means like you should do the dump in production, basically. There are other challenges with that. For example, there might be credentials which you provide as environment variables in there dumped basically into the file system and other things. Yes. It's very interesting, but they told or the the the the the Java team said it will never make it into the OpenJDK. So,

**[71:18](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4278s)** that's why I just I give you some idea how it works, but more than that I I don't because it's not supported with Bill packs. There is no easy way. There are many challenges, right? But that's the thing. So, Spring is supporting it since a while, and that means also like when you do the dump for CRaC, you need to close all the open connections. You need to close all the file handles. And when you start the application, you need to restore those as well. So, there is something involved, and the Spring and Spring Boot team, they had to support it, right? There is something similar in OpenJ9, which is called CRaC, and they also dump basically like the process to the file system. So, that means also that your image size, if you would do that and if you would script it yourself, will be way

**[72:07](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4327s)** bigger than than the regular ones, but it's amazing fast. So, it's it's comparable to native images, actually. But you have a real JVM, and you take it from there where you basically dumped it, right? You can have the optimizations for the hot paths and so on, and that's a huge advantage here. But I know also in Graal native images, they try to do with the new projects basically like some on-the-fly optimization during runtime. So, they go there somewhere. Now, let's talk about virtual threads. I mean, that's another thing we should automatically enable in our application, and I thought like let's test it with different things. Now, I do by putting basically up these parameters

**[72:54](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4374s)** spring.threads.virtual.enabled equals true, and that means like virtual threads are enabled by default. And that means actually what you can see faster startup times, but way more throughput. And since we got rid of like this um thread pinning issue, um I think it was resolved in Java 23 or so, um it's a no-brainer to actually add virtual threads to your Spring Boot application, right? So, you should just enable it because like your numbers look like way better. I have an advantage here. I need to tell you. I'm running on two CPUs, right? So, that's why they look so great. Now, let's find out together. Because I did the benchmarks for four CPUs, two CPUs, one CPU, a half, and a

**[73:44](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4424s)** quarter. And you see the numbers. And I mean, nobody tells you that you need at least like one CPU that it's really worth it. So, if you look at the quarter CPU, enabling virtual thread doesn't make sense from a throughput perspective because you have some overhead which is needed. But if you go up, you could say like half CPU is okay, right? But if you think now of one CPU or higher, it really makes a difference. Yes. And that's Java 21, right? Don't worry too much about it. Java 24, basically, um you have now on the top one 800 requests 900 requests more, almost. Um so, there is definitely like a sweet spot, right? And you could say basically

**[74:32](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4472s)** like it's more worth it if you have one virtual CPU attached to your application. I did that as well like for Java 25, of course. Numbers are slightly similar, and I was surprised actually like by Java 26. If you look at the lowest one, the quarter CPU for Java 26, we have higher throughput, we have way better response times, and also like for the others. And you might ask yourself what happened here. It's not a mistake in the measurement, I can tell you. They basically like optimized synchronize in Java 26, which has a huge impact on the on the numbers here what we see.

**[75:20](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4520s)** And they did some other optimizations, and I was really really surprised, and I talked to some people who are working basically like on the JDK, and they told me that there was like several spots, and besides that also like the synchronize optimization, which makes my numbers look way way better. Wow. And that means like you need to upgrade to Java 26 in production if you use virtual threads, right? Maybe not yet. Maybe let's wait till till the next LTS release, but then you should definitely, right? Okay. So, I I was blown away when I saw the numbers. I could not believe it that it looks so much better. And also like if you think of like the the other numbers in general. Great.

**[76:08](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4568s)** So, let's go to the summary part. It will take still probably like another 10 minutes or so to go through all the things. So, what I want to show you is like we did quite a bit of things here. We talked about no optimizations, just upgrading the JDK, did some JVM tuning, we talked about the lazy beans, and so on and so forth, right? We we talked about like Spring Boot optimizations like removing actuators removing dependencies, fixing the spring config location, talking about like different Java runtimes, making our custom Java runtime, and so on and so forth, talking about ahead-of-time processing, about AppCDS AOT caching. We

**[76:56](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4616s)** used different versions of of Java actually which are out there, and also like different runtimes like CRaC and for example a native images, and so on. So, we tackled quite a bit actually. Now, there is some conclusions to talk about. We just talked about the amount of CPU. And fun fact is, and also I linked the blog post from Dave Syer from the Spring team down there, is that you might not need a full CPU for your application at runtime. That's like one fact because like often the CPUs are idling if you're not have enough users. But to have fast startup, you should have like four CPUs. That would be perfect, right? That's a perfect world.

**[77:46](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4666s)** If you add five or six, it doesn't get much better, but the sweet spot is really like at four. Until four, it's like more linear. So, that means like if you have two, you have kind of like double the time to start up. If you have one, you have another doubling the numbers. And then actually like at the bottom, it might get even worse. So, like from one to four, you have kind of linear field, and four cores are are perfect if you can afford it, right? Of course, you might have applications which are not sensitive, which are not exposed like to the internet, which maybe are just used like internally, and it's good enough to give it like a quarter CPU or so. But you have to decide. But be aware of like four would be like perfect. Then actually,

**[78:35](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4715s)** the other thing is like your application might differ from my application. So, that means like the requests per second might differ in your use case, and also like the path you're taking in your application. That's definitely also like one thing. And to find the right on the optimal configuration, you should definitely do some load testing to get some feeling for your application, and then also like put the measurements in place. Great. Then actually like consider other runtimes than just like a regular OpenJDK because we have like other vendors optimizing your the JDKs. That means like there are some vendors out there which provide arm support for the JDK. There is like with IBM and OpenJ9, there's another version of the

**[79:23](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4763s)** implementation of a Java runtime, for example, which might help you, too, right? You have like the the crack and the clear support basically like built in in Azul Zulu, in Liberica, and as well in in OpenJ9, you could leverage as well. Graal native images might be also an option for your application, but we know that the build time takes about four to five times longer, and it might be more painful because certain libraries may not have like the the reflection stuff declared in the Graal native metadata repository. So, that means like maybe you're building it natively, but it's not running because certain parts are missing, and you would

**[80:11](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4811s)** have to contribute this information so that you could build it, right? Maybe somebody else does it for you, whatever, but that could be one of the challenges. That doesn't mean that if it works with Quarkus, the library, that it's running with Spring Boot because Quarkus takes another path. The Quarkus team has clearly decided that the information Graal native images is needed is part of their extensions. They do not care about the other Java ecosystem and the Spring users. So, that means basically like the GraalVM team and the Spring team are maintaining this additional metadata for native images for you, right? So, that's really a pity, but there are actually discussions, you will find them in some GitHub issues if you search for

**[80:59](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4859s)** them. If you are scarce on memory resources, maybe OpenJ9 is the perfect solution because as we said, it's optimized on memory usage. It might take a little bit longer to start, but maybe that's good enough for you. And then I want to mention as well, there are some vendors outside which actually like charge for some exciting features, and sometimes it's worth it to like pay money to have some better optimized things in your application. And even if it's just like support for the next 10 or 15 years of the specific Java version, maybe because you need it. I'm just telling you because like you do not always get everything amazing for free. We get a lot for free, but not

**[81:49](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4909s)** everything. Now, I have tried, and that's why I put some other things up there with a star. I tried some things, for example, like crack. I have a solution for me which works. I scripted that basically so that's fine, but I would not actually like provide it to the to the crowd because you can make so many mistakes, and it heavily depends on your application like how you're going to use it. So, this dumping of things like exposing credentials or leaking credentials on a on a container repository, no. So, that that's that's a challenge, definitely. I also had a look at like tree shaking or obfuscation, and I realized actually like that if you use something like

**[82:37](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=4957s)** ProGuard, which is an obfuscator, that it will basically like make a lot of classes with just one letter. And if they're in different packages, and you do component scanning, that doesn't work if the class is called ABC and so on because you will have like many A's which do not fit where they need to be injected in your code, right? So, I tried to like exclude all the cases which were annotated with at component and the other um meta annotations, but then I realized, "Mhm, it's not working. It's not getting better. It's not getting smaller the whole thing." because I thought like you do it in Android, it's going to be executed faster. It could be actually shrink my application, but it was not the case. So, I had to put so many exclusions there that there was no

**[83:25](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=5005s)** difference anymore. So, that's why I put the star there. I recently had an idea about auto configurations. What if I do not need to like load all auto configuration in Spring Boot? You probably have heard that Spring Boot now with the current version of Spring Boot 4 got a way better modularization. There is not one char anymore which contains all auto configurations. They said basically like for each topic, we have an auto configured char, and you only like have these auto configured chars loaded when you add the starters to it. So, the separation is now way better. So, still I thought it would be nice basically like to not load all auto configurations. And

**[84:12](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=5052s)** fun fact, I have added another project which I was playing around with, and I said I have like a Maven plugin created, and I can show you slowly that. I don't have the benchmarks yet for that one, where I basically just do a training run, and where I inject some additional files into the Spring Boot application. Imagine I start the application, I'm going to check which auto configurations are loaded. I dump that into a file. This file is added to the Spring Boot application, and with the next start, I only allow those auto configurations to be loaded. If you go deeper in my project, you will actually like realize that I have benchmarks added to the pipeline, so I

**[85:01](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=5101s)** see what's going on. Of course, the pipeline is not perfect and might vary from time to time also with the numbers, but for me it's just like an indicator in which direction it goes, and I'm just going to pick like one of those builds which are part of the benchmark pipeline, and you can see here that compared to the baseline, and it's not the full-fledged Spring Pet Clinic, it's more like a Pet Clinic-like application, that I basically excluded 52 auto configurations from start, and in this case, it's roughly 3% faster. Just because I do not care about these 52 additional auto configurations. And I have seen numbers while playing around where I had like 4% basically um

**[85:50](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=5150s)** 4% faster startup time, but you can get here in in this example like about you or you can save basically 45% of the auto configurations. It's just another thing I tried to play with to find out how can I filter out and this auto configuration, and Spring Boot 4 has an amazing API which is this auto configuration filter, which is actually also described here in the thingy. So, you can use those filters to basically like exclude the auto configurations which you don't need. If you remember, there was a property in the past, I don't know or don't remember exactly the name, but there was an exclude property. In Spring Boot 3.5, you would have to consider basically the auto configuration twice. In Spring Boot 4,

**[86:38](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=5198s)** they added this additional filter, so it's just considered once, and that's how we can even more things. So, I'm as you see, I'm constantly playing on how to make my Spring Boot application faster. Now actually I could even say like, let's do some more JVM tuning, garbage collector memory whatever string compacting or deduplication or you name it. There will be some more project-laden things coming up in the future which are also amazing to be added to the whole thingy, and which could also make our application way faster. So, this is the conclusions. Now, let's have a look at the simple optimizations applied, and and and the idea is basically like we do more things together, and see like how many things

**[87:29](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=5249s)** we can basically save by summing them up, right? In different cases. So, imagine we do a dependency cleanup, removing DB drivers, actuator, Jackson, Tomcat web socket, and so on. Let's use a different builder. Let's use J link. Let's use still the Java memory calculator, which is there anyway, right? But be aware of it's there. It's not just a regular standard startup. Let's add, for example, class data sharing or AOT caching. Let's generate the configuration to basically register all the Spring beans with code instead of like to component scanning and entity scanning. Let's fix the location of the config. Let's deal with all the the lazy beans,

**[88:16](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=5296s)** for example, and maybe let's also use like virtual threads if you want to. So, let's combine a few things of those, and that's like my first slide where I want to show you how amazing this is if you know like if you apply certain things to make your application faster, and how they sum up. Now I'm using like BellSoft Liberica which has Alpine in there with muscle, and that means like also the Java version is a muscle version, and you can see basically like we we can remove the the memory footprint, the image size quite a bit, depending on the Java version, of course. And then actually like also like we have way higher throughput here. And with the the lower points here with the with the thread thingy there, you see also like there we

**[89:03](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=5343s)** have like virtual threads enabled, which also allows us to have better response times. Now let's even go a step further where I tried even more things. So, think of like virtual threads, then you have AOT caching added, think of like compact object headers for example as the third column in line there and also like the other things on top and how it looks like. And what we basically can say is depending on like in which direction we go, we might even get something like half the startup time we could in some cases but it's not here yet um that would be more like on the other side if you are using Distroless or for example the Alpine Linux, we could like reduce the the image size by half, right? We

**[89:54](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=5394s)** can get like way better memory thing and also like way more throughput and as well also response times. So just by applying a few of those measures depending on the combination of course, you can gain quite a bit and that means like you need to do a little bit of testing a little bit of load testing experimentation, but you can apply the things we have learned today just like tomorrow, you know. Great. So if I missed something or if you have an idea what else I should do next please let me know because I'm on a journey now for almost 3 years on the topic. I have learned so much about Java and Spring Boot while doing this. Of course, I had a huge backpack with me already, but I

**[90:43](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=5443s)** still learned quite a bit. So I would be happy like to extend my presentation and please let me know what you think I could do next to find out like in which direction we could go, right? Um please also share your feedback because feedback is always a gift and you know actually like if you do that in the companion app, tell me about the bugs in my presentation, about the missing docs or what I should refactor next. It would be really awesome, right? And if you don't put feedback into the companion app it's like it works on your machine, right? Okay, great. If you still need the slides and so on, I will upload them afterwards as well like in the companion app, but you can also get the link to the GitHub repo. You will find also the projects and how

**[91:30](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=5490s)** I manipulated them mostly in the palm file and you just can go and play around with that. It's basically like just the the regular Spring Pet Clinic application with some small touches, but you will find the Java 25 versions of the projects and um you can play around with that. And if you have questions or if you have some additional input, please let me know. Question in room? Yes? Yes. So the question was like at the start I said you should not benchmark on your notebooks because of thermal throttling.

**[92:24](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=5544s)** No. So I do the benchmarks actually here on a dedicated machine. So it's just really like hardware with dedicated resources, I run everything there. I use then Docker to start the application to measure the things, but I want to be in control of of this stuff. Imagine if I do it on my desktop at home. My desktop computer at home has water cooling. Super nice, right? It has also a RGB. I'm not a gamer anyhow, but for that is water cooling perfect. I can see basically an email coming in and making ping in the results basically. So you need a very slim operating system where you don't have background processes that you get reliable and reproducible numbers. So really like make sure that you have a

**[93:14](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=5594s)** dedicated machine. If you have like a virtualized machine it can be okay if you have reserved resources. If you can have a physical machine, even better because then you have really guaranteed resources because the virtualized one with dedicated resources might still depend on the hypervisor. So that means like the numbers might be not perfect, right? So really go for a real machine and make sure that it has always the same CPU temperature, right? And that means like you have to put like constant load there basically like to to be there so you don't get into thermal throttling. My notebook over here is like an ultra book and it doesn't take long actually until it goes into thermal throttling. Yes.

**[94:03](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=5643s)** Okay. Yeah. Yes. So I mean you could actually use the Maven daemon to have better build performance, of course. You can also use Gradle. I think Maven is just like simple to more simple to understand for people um because it's a human readable format, of course. And that's why you use Maven. And imagine now we have been adjusting for example the parameters of JVM parameters for the startup of the application. You could do the same actually like for your Maven run because Maven also starts up a JVM and that means like you could actually also add additional parameters to even improve

**[94:52](https://www.youtube.com/watch?v=GRXiK3-Uvu8&t=5692s)** the build times. I just didn't care about that. Yes. So it's just a personal choice and I believe um just because like architects like Gradle a lot, it doesn't mean that the software team needs to use Gradle as well because Maven is in so many cases much simpler for what we are doing. So that's just like personal preference. Great. So um if you have additional questions, come down and talk to me. Otherwise, I wish you a very nice evening. Thank you for being here and hopefully to see you next year again at Voxxed Days Amsterdam.

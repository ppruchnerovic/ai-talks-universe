---
id: 2peFTUktkuY
title: "Write Once, Accelerate Everywhere: GPU-Ready Java with TornadoVM by Thanos Stratikopoulos"
slug: write-once-accelerate-everywhere-gpu-ready-java-with
conference: devoxx
conference_name: "Devoxx"
category: "General software conferences"
edition: "Devoxx"
year: 2026
speakers: ["Thanos Stratikopoulos"]
channel: "Devoxx"
duration_min: 44
published_at: 2026-02-27T13:52:44Z
video_id: 2peFTUktkuY
url: https://www.youtube.com/watch?v=2peFTUktkuY
youtube_url: https://www.youtube.com/watch?v=2peFTUktkuY
tags: []
topics: ["Inference, serving & GPU infra"]
transcript: true
---

# Write Once, Accelerate Everywhere: GPU-Ready Java with TornadoVM by Thanos Stratikopoulos

**Thanos Stratikopoulos**

`Devoxx` · `Devoxx` · `2026` · `44 min`

[Watch the recording](https://www.youtube.com/watch?v=2peFTUktkuY) · [Conference site](https://devoxx.com/)

## Description

In the AI era, accessing GPUs from Java is essential - but it still remains a challenge. TornadoVM bridges this gap by enabling developers to accelerate Java applications on GPUs, CPUs, and FPGAs without writing any GPU code. Originally born as a PhD research project at the University of Manchester, TornadoVM has evolved into a mature open-source framework supported by both academia and a growing global community.

This talk will present the key innovations that are offered by the TornadoVM runtime platform, and will focus on practical ways to optimize existing Java applications through its high-level API. Through hands-on examples, we will demonstrate how to offload computations, manage data efficiently, and achieve significant performance gains - entirely from Java.

We will also showcase GPULlama3.java, a pure Java large language model (LLM) library accelerated with TornadoVM, illustrating how modern AI workloads can run efficiently within the JVM. Finally, a live-coding demo using IntelliJ IDEA will highlight TornadoVM’s integrated profiling and tooling ecosystem for seamless development experience.

## Transcript

*6,630 words · source: supa (en, exact timings)*

**[0:15](https://www.youtube.com/watch?v=2peFTUktkuY&t=15s)** Great. Hello everyone. Good afternoon. Nice to be here. It is a pleasure to have this kind of opportunity to present the software that we have built at the University of Manchester. It is called TornadoVM. It is an open source project that enables JDKs to access GPUs. So yeah, I hope this can be interesting to you as well. A little bit information about me. I am uh Thanos Stratopoulos. I am a research fellow at the University of Manchester. I have been working in the tornado VM project for the past eight years and I have touched many things inside the tornado VM. So I have done work in the runtime in the compiler side on the code generation h I have uh deployed the technology on AWS consoles

**[1:05](https://www.youtube.com/watch?v=2peFTUktkuY&t=65s)** with GPU uh capabilities and um yeah let's see so I have prepared an agenda here are some highlights of today what I'm planning to show you I'll start with a little bit of um history of Tornado VM because it is a project that is running for more than a decade I'll give you some overview about the API. Then uh some overview about some u key characteristics. One of them is uh the ability to use u the to integrate with project panama to use off heap allocations for GPUs. Then I will show you how you can install tornadovm which is something that we introduced I mean the SDK distribution in December. So it's uh quite new. H we will jump into the integration to existing Java

**[1:54](https://www.youtube.com/watch?v=2peFTUktkuY&t=114s)** projects and they will have some more complex use cases including an LLM engine that we have built entirely in Java and we are using tornado VM to offload computation on the GPU. So yeah a little bit of history how it all started. It started as a PhD project back in 2013 at the University of Manchester. It was uh one PhD student student James Clarkson who had this kind of project. Let's find some obstacles. Let's identify what are the key uh parts the key factors that can hinder the integration between Java and hardware accelerators like GPUs and FPGAAS. And back then the project was named Jack. It later evolved to Tornadov and it was originally based on the suit

**[2:43](https://www.youtube.com/watch?v=2peFTUktkuY&t=163s)** framework. So it has changed a lot since then but finally it was uh ended up with a successful PhD defense which is important and with an open source state-of-the-art technology that we are proud to maintain at the University of Manchester. So how is it going? We open sourced the technology in 2018. We have transitioned to Growl which has enabled us to utilize existing um an existing JIT compiler and all the optimizations that are coming from Growl and we have extended the Growl compiler with new faces especially at the back end side where we are actually translating the IR into three different backends so three different formats open

**[3:33](https://www.youtube.com/watch?v=2peFTUktkuY&t=213s)** CLC CUDA PTX and SpearV which is a binary format for accelerators. We have uh 23 releases and the recent the most recent is uh version 2.2.0 and we have uh 30 contributors and uh last year we had our first production deployment uh in the context of the European Space Agency Gaia mission. So this is a work that um it is a collaborative work with the university of Geneva in the context of the EU research funded IRO project and the idea is that um there are data that are captured from our galaxy from a spaceship. So the data are sent to earth for data analysis and for this kind of analysis the University of Geneva has implemented some uh high performance

**[4:21](https://www.youtube.com/watch?v=2peFTUktkuY&t=261s)** computing algorithms and they have parallelized it with the API of tornadovm to offload the analysis on the GPU and they have seen significant performance improvements. For example, I think for the analysis of one data series, uh the execution time has improved from 1 month to 5 days and this is key. Okay, moving on. Um here is a generic picture I would say high level picture of um the ecosystem of tornado VM. So we have placed tornado VM at the center and if we have a look on the right side we see the actual hardware backends that enable us to target specific uh type of accelerators like uh even multiol multiore CPUs ARM x86 and recently as well also risk 5 through sperv then uh

**[5:13](https://www.youtube.com/watch?v=2peFTUktkuY&t=313s)** we have um a dedicated back end for CUDA PTX and we have as well open CLC that enables us also to target FPGAs. On the other side, on the left side, we have a different list of uh JDK distributions. Currently, we are compatible with JDK21. Um, but we are recently we are working on the uh adding compatibility with JDK25 which we believe that will be ready in a couple of weeks. So that uh we will it will come on the next release that we have for tornado. Now in every release we are also um adding docker images in docker hub. So we have dedicated containers for running on Intel graphics and for nvidia GPUs as

**[6:02](https://www.youtube.com/watch?v=2peFTUktkuY&t=362s)** well. And um as I said before we are working and tornado VM extends grow in the compiler side. So this gives the opportunity as well to interact with truffle. So in a sense we have interoperability. we can achieve interoperability with other programming languages that are running on top of truffle. So imagine a scenario that you're using Java because tornadovm has an API for Java. You implement your high performance implementations there and the algorithms and this uh kind of computations that can be invoked at the same runtime from different programming languages like JavaScript, Python and R and R. So this is uh very powerful and uh finally we have implemented some uh key um in-house uh tools that enable us

**[6:52](https://www.youtube.com/watch?v=2peFTUktkuY&t=412s)** to profile um the execution to understand the orchestration when we send data and when we have multiple of methods that we send on the GPU the order and the data transfers how they are handled. So this is very important. So we have pulse and tornado v for that. We have a JDK plugin for IntelliJ uh sorry we have an IntelliJ plug-in for tornado VM tornado inside. So this is a plug-in that we have implemented. It is open source and is available in the jet brains marketplace and enables um programmers to try to understand the tornado VM concept and uh what is valid what is not supported and this kind of uh so it is like a first step for beginner programmers with tornado and finally we can integrate also with

**[7:41](https://www.youtube.com/watch?v=2peFTUktkuY&t=461s)** other tools like Nvidia and side systems or intel vetune to add more profiling capabilities and indirect directly the project it is integrated with Quarkus and Lancing forj and I will emphasize on the final part of my talk today how this is done okay now I will go into this kind of question how much different is GPU programming well this was the initial motivation of the PhD project in fact and uh the reality is that it is way different from the way that we have used to as Java developers so imagine this kind of view which is kind of abstract an abstract illustration of what's happening in our computers nowadays. So we have a multi-core CPU. We have the main memory of the system and then we

**[8:29](https://www.youtube.com/watch?v=2peFTUktkuY&t=509s)** have some u accelerators I would say like GPUs or FPGAAS that are attached into uh our processor and the main goal is to offload computation from the main processor in order to accelerate the execution time. So to improve execution time in sometimes you can achieve also better energy efficiency. So that's the key principle. So as Java developers we have uh some APIs that enable us to express parallelism like vector API parallel streams virtual threads but uh they are mainly in the scope of the CPU. So we can express parallelism in the scope of CPUs. So then the question is what's happening if we want to express something to run on the GPU in parallel. So then we need to change our mindset.

**[9:18](https://www.youtube.com/watch?v=2peFTUktkuY&t=558s)** We need to express the parallelism in a thread level uh granularity. So the idea there is that you have a computation and you express it in a way that all the threads on the CPU will perform the same computation but on different data. And uh in some cases as well you need to think how you will group the threads on the GPU so that they can work together and they can access the same cache memory as it is called in the OpenCL terminology the local memory. So that to improve data accesses and avoid you know any penalties of accessing memory. Here uh we have some key remarks regarding GPU programming and so GPU programming requires a

**[10:06](https://www.youtube.com/watch?v=2peFTUktkuY&t=606s)** different mindset because you we need to start thinking differently to express the computation programmers they need to become familiar with thread level programming and grids which is the groups of threads that will be deployed on the GPU and uh in some cases I haven't shown so far but there's a lot of verbosity because these kind of programming models that are targeting GPUs they require developers to explicitly do everything. So allocate the buffers uh copy the data to the buffers um launch even launch execution through a command q. So this is done uh with a dedicated command that C++ developers they are uh writing. Okay. But all this they break the the promise of Java the slogan that write

**[10:56](https://www.youtube.com/watch?v=2peFTUktkuY&t=656s)** once run anywhere. So in practice uh this breaks this kind of problems and this is what motivated tornado VM as a project. The idea was that we want to implement a Java API that enables programmers who are not familiar with GPU programming to express parallelism for at the method task and then to have a way to launch the execution of that parallel code on the GPU side. And in fact, tornado VM has been um designed to act as a plug-in for existing JDK distributions. So it is like an add-on. It is not a VM, a JVM, but we call it VM because we orchestrate some custom bite code to orchestrate the

**[11:44](https://www.youtube.com/watch?v=2peFTUktkuY&t=704s)** execution on the GPUs and this is done seamlessly from the programmer. So it is open source and the API that Java programmers they are using it is uh it is a platform agnostic. So that means that the same API can target FPGA, GPU, CPU in the same way. It has um automatic code optimization because it jit compiles code at runtime and we have support as well of uh for off heap data types through the integration of uh the foreign function memory API which we were one of the first adopters of project panama. So now I will jump into the last the last part. So the integration with project panama. So in this code snippet

**[12:34](https://www.youtube.com/watch?v=2peFTUktkuY&t=754s)** we can see an implementation of the canonical matx multiplication. In fact it is the main method that will invoke matx multiplication as we will see. So at first we need to create the objects. So these are um the arrays they are created as um primitive types. So arrays of primitive types floats and they are stored on heap. So this means that they will be pruned on uh to kick uh when the GC will be kicked. So then we randomize the data we we initialize the two matrices A and B and finally we invoke the execution of the matrix multiplication. So this is vanilla sequential Java code. Now if we want to migrate to off data types with

**[13:25](https://www.youtube.com/watch?v=2peFTUktkuY&t=805s)** the tornadovm uh API, we can see that the tornadovm uh API exposes some collection of types. One of them is the float array as we see on the right side. So then we just create this new type float array. We pass the number of elements as a argument and um we are using as well the setters and we can use also the getters to get the result from these data types. The interesting part is that uh this is a way that you can express and utilize it from any Java application. So no need to use GPUs or something so far. The idea is that the float array data type abstracts the information of Panama. So like uh you don't need to know about memory segments

**[14:12](https://www.youtube.com/watch?v=2peFTUktkuY&t=852s)** the arenas. So everything is handled uh under the hood and here is the link where you can have a look regarding this kind of examples and um the transition from the off of on heap to off. Additionally, we have uh implemented some u uh key factory methods that enable developers to migrate data from uh on heap to off heap as we see here from array. I'm not sure if it is very clear with the font size. So these are the key factory methods to migrate data to off data types. And similarly there are also some methods some factory methods to migrate data from off heap to on heap and this is also for all the key data types integers longs doubles etc.

**[15:05](https://www.youtube.com/watch?v=2peFTUktkuY&t=905s)** Okay now we move to the fun part. So setting up and installing tornado VM in the systems. Usually before December before the release of uh December we used to say in every talk uh you need to build tornado from scratch in your by source the source code. So you need to clone the repository and build it from scratch. But uh in December we had this kind of u um achievement where we actually shipped the SDK archives. So you have two ways to use them. One is to use the our tornadovm website and go to the downloads where you can actually invoke SDK man command or you can manually install it. So you can extract it and uh start using it. And uh if you are using

**[15:54](https://www.youtube.com/watch?v=2peFTUktkuY&t=954s)** SDK man well that's great because uh we are there. So you can just type SDK installed tornadovm and you can start using uh the tornado API. So here is um in more detail. So the only dependency that tornado VM has is to have already a JDK distribution in your system. And this is because tornadovm is not a JVM distribution. It is a plugin to JDK. So with SDK install Java, we can install tearine 21 and then we can install tornadovm. So if we don't uh specify the version, it will take the latest version and the OpenCL back end by default and then we can just type this kind of commands tornado device and tornado version.

**[16:42](https://www.youtube.com/watch?v=2peFTUktkuY&t=1002s)** Okay, I will jump to the terminal to actually show you how we can do it. So as we see here, I will use the temorine version uh is ah internet connection. Okay, let's do echo. Okay, fine. I had already done the use. So, we are great. So, if you type SDK use java and 21 temorine the SDK man will uh show you will actually install the JDK21 temorine into your system and will export automatically this kind of variable the Java home. Similarly, if you run the command that I show you for tornadovm and you type echo tornadovm home, you will see that SDK man has automatically

**[17:32](https://www.youtube.com/watch?v=2peFTUktkuY&t=1052s)** extracted and exported this kind of variable of the tornado VM home and points to the uh actual uh place where it has been installed. Now if we do a tornado well if we do which tornado we will see that it will point to the tornado VM home and if we try tornado dust version we will see that we are running the latest version 2.2.0 zero. So we can confirm that we this is the version that we are we want to use. And finally we can also run an example. So tornado does devices. Tornado is a Python um utility that we export in the CLI let's say. And devices it is an option that identifies at runtime what are the available devices that we have

**[18:21](https://www.youtube.com/watch?v=2peFTUktkuY&t=1101s)** in the system. And at this stage I am on M4 MacBook and I have this GPU on M4. Great. So let's go back to PowerPoint. So we had uh this look. So we saw Java home, we saw tornado VM home. And now I said a little bit about the tornado command. So it is a Python utility that we expose to users. So it makes um life easier for us the developers. But the it is simply analyze for Java. So so you can replace the tornado command with Java and then pass the tornado arc file which has inside some JVM flags that enable JVM to be able to work to

**[19:12](https://www.youtube.com/watch?v=2peFTUktkuY&t=1152s)** JIT compile code for GPUs and uh work with your Java distribution. and you don't need to change anything. So if you have a an application, you can simply um use this kind of way to execute it. Now I said about the JVM flags, they look like this. So you can simply dump them by running tornado does bring Java flags and it will bring it will uh pop up in the console all these kind of u uh JVM flags. Excellent. Okay. And uh we can run an example with the Java and tornado arc file. So if I jump back to the terminal uh this is the example. So this is a

**[20:01](https://www.youtube.com/watch?v=2peFTUktkuY&t=1201s)** matrix uh vector row major implementation that we have done this kind of oh yes sorry it is a powerpoint yeah I need yeah perfect let's clear yes perfect so this is a matx vector implementation it is one of the key computations that we have found in the llm engine that we built that uh it was critical for performance so what we did it was to actually h implement it. We have it packaged it in the examples of tornado VM. So it is available in the SDKs and is a way to dump the configurations of the inputs and the data that we have and then we are doing some simple let's say comparison between implementations and APIs. So this run

**[20:52](https://www.youtube.com/watch?v=2peFTUktkuY&t=1252s)** but uh we can also run it with a tornado command to show you. So we can replace Java and ARC file and we can just say tornado and this will have the same behavior exactly. So it runs as we see here at the bottom we have um a a way to visualize that we are using the GPUs. So if you see the spikes here on the right side it is the actual energy that we are consuming on the GPU and here you can see the usage of GP on the GPU. So if I run again and I want to well let's do tornado does help we see here all the uh valid options that we have. We have some options to debug and use debug information. Then we have the thread

**[21:39](https://www.youtube.com/watch?v=2peFTUktkuY&t=1299s)** info which will actually show in the console the deployment. So the grid how it looks like when we deploying on the GPU the number of course that we are using in each dimension. Then we have uh the print print kernel which will actually dump the generated code on the console and we have print byte codes enable profiler etc. So I will run the same example and I will run with a print kernel. So this will show us in the console the generated OpenCL code that is generated by the compiler at runtime. And this is the kind of computation that we have launched to run on the GPU. Okay, let's jump back to the slides.

**[22:32](https://www.youtube.com/watch?v=2peFTUktkuY&t=1352s)** Great. Now we will move into the second part of the talk. So the idea is to show you how you can um clone the repository. I mean assuming that you have already cloned it. We will use the branch of tornado inside demos which is available in the public repository and has some demos for the IntelliJ and the JDK plug-in that we have implemented and the idea is to show you how you can build it from IntelliJ and how you can run it through IntelliJ but a little bit more about the uh plug-in. So tornado inside the recent version it is 1.5.0 zero. We released it earlier this week and uh it is um implemented significantly. It has been

**[23:20](https://www.youtube.com/watch?v=2peFTUktkuY&t=1400s)** extended significantly through the PT to code project which is a European funded research project and as I said earlier the the key objective is to familiarize Java developers with what is valid and what is invalid regarding um tornado VM scope and the API. And we released in December a blog where we explain how um users they can actually achieve compatibility with the SDK. So you don't need to actually clone the repository. If you have the SDK man and you have already download the tornado SDK, you can uh start using the plug-in as well. Okay, let's uh jump into the console. So we are here. If I do get remote, we see that we have

**[24:09](https://www.youtube.com/watch?v=2peFTUktkuY&t=1449s)** cloned the behav tornado VM repository and uh we are in the tornado inside demos branch and if we do make intelligj in it we will see that uh uh what it did it was to actually create some uh templates and particularly the tornadovm build and tornadovm test templates. So tornadovm build will be used to build the tornadovm projects and the examples that we have from IntelliJ. So then we jump to the IntelliJ. Great. Yes. So here what we see it is an example of a matrix multiplication. If we see I hope it's yeah it's clear. I hope so. So as we see here the first

**[25:00](https://www.youtube.com/watch?v=2peFTUktkuY&t=1500s)** steps are what I initially described in the code snippet when I referred to the Panama types and the float array example. So in this example we will use float array which is the collection of tornado VM and once we have um h created the data and initialized the data then we will jump into the definition of a task graph. So this is an object that tornado VM API exposes to developers and it is used by developers to define what they want to run on the GPU. So it exposes some methods like as we see here the transfer to device, transfer to host and the dot task. And these kind of uh methods enable programmers to express

**[25:49](https://www.youtube.com/watch?v=2peFTUktkuY&t=1549s)** which data will be the input data, which data will be the output data and what will be the computation that I want to run. And as we see here for the transfer to device, you can um configure the data transfer mode. If you want to transfer data only once in the first execution, assuming that you have read only data, then you can use this. you can uh change it to every execution and uh then you pass just the input data which is matrix A and matrix B. Once you have defined that then then you can define the actual computation. As we see here we use the task uh method. We assign as a first argument a string and uh this combined with a task graph

**[26:36](https://www.youtube.com/watch?v=2peFTUktkuY&t=1596s)** string name will identify the specific task of a task graph that will launch and this is useful especially when you have multiple number of threads and kernels to to be able to identify which threads are deployed for which task for which kernel. So once we have done this we pass the method reference that we want to offload on the GPU and we pass the actual data. So in this case in the order that they exist in the method. So matx A, matx B, C and size. And finally we define which which will be the result. So which will have the actual data that we want to send back to the Java world from the GPU memory. And as I said here, as you see here, uh we can again um configure the

**[27:27](https://www.youtube.com/watch?v=2peFTUktkuY&t=1647s)** data transfer mode. In this case, it will uh copy the result back in every execution. I think I skipped the number of iterations. So I think we have yes here is a warming up iterations constant. So this means that in this program we will run 15 times and um then we will have another execution. So if we go here a little bit lower, we see that we have a for loop for the number of 15 executions and then we will have another execution which will be the execution that we will time and we will report the timing. Okay, with the task graph I showed you how you can express uh which part that you want to uh code and uh send to the date to the GPU. But uh the idea is that

**[28:16](https://www.youtube.com/watch?v=2peFTUktkuY&t=1696s)** this is a mutable object. So if you want you can change again the shape of the task graph. You can append more tasks. This means that if you want to send multiple computation that the second computation may consume data from the first computation. This is the way that you can do it in a task graph. But if you do it in a programmatic way that means that you will have only the latest version of task graph to send to the GPU. So if you want to keep different kind of uh versions of task graphs then we introduced immutable task graph which will actually take a snapshot of the task graph that you have at this point and uh it will create an immutable task graph. It is an object that it is not mutable as it says. So at this stage we we are able to create a tornado

**[29:05](https://www.youtube.com/watch?v=2peFTUktkuY&t=1745s)** execution plan which is the object defined by our API to uh so that developers can use it to express how they want to run on the GPUs. This means that they can trigger it to actually run as we see here with pre-ompilation. This means that it will run the first time it will jit compile the code and will store it in the code cache. So the second time it will be already zit compiled and be ready. But there are more options like you can run with profiler, you can run uh with specific device. Yeah, there are many options here. So people can use specific compiler flags if they know what they want to pass and uh etc. So this is a way to express how you want to run the GPU. And then to

**[29:54](https://www.youtube.com/watch?v=2peFTUktkuY&t=1794s)** actually kick everything and launch them on the GPU, you actually invoke the execute method of the executor of the execution plan. So this is the point that everything will take place assuming that you are not using with pre-ompilation. This is the point where you will actually did compile the code, send the input data to the GPU memory um launch execution and copy the data back. So it is a blocking call in a sess and um yeah I think this is uh pretty much the application. So this example uh I just want to show you a little bit of the task and how um um yeah here we have a canonical implementation of matx multiplication. So assuming that I remove this kind of annotations, it is the Java sequential implementation. If

**[30:44](https://www.youtube.com/watch?v=2peFTUktkuY&t=1844s)** we revert back that parallel annotation, this means that we give a hint to the tornado.jit compiler that this they have the opportunity to be executed in parallel concurrently on GPUs. And if there is no data dependency between these loops, then the the code will be compiled and will run in parallel. Okay, great. So now I think we can uh uh actually run the application. So this is the vanilla code that I run. So as we see here we run for 15 times and for every time we um dump the thread information. So what is running? So first, second, everything and then we jump into the end which this is the 16th execution and then we have the uh

**[31:33](https://www.youtube.com/watch?v=2peFTUktkuY&t=1893s)** invocation um of the um Java sequential implementation which uh refers to this part and then we perform this kind of uh printing and we actually at the end verify the code. So if we see here it says verification true. It means that the data that we run on the Java sequential code and the data that we run on the GPU they are the same. Now if I want to show you an example I have prepared already I think for uh the sake of time. So I have started it. Yes. So yeah here I have changed a little bit the way of uh the class file that I showed you earlier. So I have added a new uh method which is called clear matrix. In a sense I'm planning to

**[32:24](https://www.youtube.com/watch?v=2peFTUktkuY&t=1944s)** run this task after the execution of the matrix multiplication in order to clear the result data. And as you see here it just simply goes over all the elements of matrix and assigns the number zero and does it in parallel. So as we see here I have changed nothing at this point. I run 15 times and um in the last execution after the last execution I just change the task graph object which is the mutable task graph. I assign a new task and uh a new name and the method reference and then the actual input data that I want to have and I create a new execution plan with the snapshot of this version of task graph. So this is a new version and then

**[33:13](https://www.youtube.com/watch?v=2peFTUktkuY&t=1993s)** I launch the execution. So um if I build with uh tornado. So if I go edit configurations I go here select the Python SDK and I run. Now I will kick the build of tornado VM in IntellJ and this builds all the modules that I have including the examples modules that I have just uh changed. And uh yeah now it's ready. So if I go to run again matx multiplication we see that it runs the same thing 16 times. And uh after the last execution of s0 t0 which is the first u task that I I launched for the matrix multiplication we actually cleared the data and the verification has failed because we

**[34:01](https://www.youtube.com/watch?v=2peFTUktkuY&t=2041s)** actually screwed the data. So yeah, this is the a nice way I think to show you to demo the um varability of um the ability to express multiple task graphs. So I just want to fast uh show you that uh if we go to the settings here uh we are able in the plugins to show you the so in the marketplace you can install the JDK plugin tornado inside and once you have done it you can actually use it. You will see here on the right side that there is this kind of logo of tornado VM and what it does it is that um automatically refreshes. So I have opened the matx multiplication class and uh it identifies the tasks that I have.

**[34:50](https://www.youtube.com/watch?v=2peFTUktkuY&t=2090s)** So it means two tasks matrix multiplication and clear. And I can run a dynamic inspection. And we can see that it generates and uh pops here the parallel code that has been implemented. And we have a message that the testing has been completed which is a good indication before you actually compile and build something that this uh task can be did compiled by the the compiler. Okay, let's uh move forward. So more information are available in the DevOps uh presentation that we gave and uh okay to integrate tornadovm in uh Java projects you can actually use Maven dependencies. So we have added the jar files in Maven central. You can uh use the dependency for uh the tornado API

**[35:40](https://www.youtube.com/watch?v=2peFTUktkuY&t=2140s)** and tornado runtime and this will be enough for you to start uh coding the examples or your applications to integrate in your applications. We have an example of the LLM engine GPU Lama 3 Java which is the project that we implemented in our university to offload um computation on GPUs for LLMs for the inference and run inference locally on our GPUs. Here is the GitHub repository and a little bit more information. We support multiple uh GGUF mold models including llama 3, mistral 53 uh when recently we added support for IBM granite models and we're working on GEMA and we have integration with uh lang

**[36:29](https://www.youtube.com/watch?v=2peFTUktkuY&t=2189s)** chain forj and quarkus lang chain 4j as well. So what I want to quickly show you it is um the llm running. So here I have um this this command lama tornado. We have uh I will run without print kernel. Yeah, I don't want to throw the open CL kernels of 9,000 tasks on the console. So and I simply pass um the model that I want to launch which is a llama 31 billion model and the actual prompt says tell me a job. So we run it on the GPU. We see that the GPU usage is increasing nearly 100% and we see the spikes as well on the bottom right and we have actually received the data back. So we'll have a throughput of 17 tokens more than 17 tokens coming per

**[37:19](https://www.youtube.com/watch?v=2peFTUktkuY&t=2239s)** second. So this is quite good. Um yeah let's jump to PowerPoint. So if you want to use uh uh GPU lama 3 you can integrate it in your projects through lang chain 4j. Uh we have integrated with lang chain forj so we are providers there. So if you want you can simply change the dependency and automatically launch the inference on your GPUs locally. Uh we have also um the jar files. We have also you can also add the Maven dependency for the Quarkus implementation. Quarkus lang chain forj GPU lama 3 and if you want to use it as a standalone you can

**[38:07](https://www.youtube.com/watch?v=2peFTUktkuY&t=2287s)** also uh use the maven dependency of GPU lama 3 project and uh we wanted to show you something a demo uh that shows the integration between GPU lama 3 quarkus and glain forj. So we went to the quarkiverse documentation. We found this kind of example of implementing a summarization service. So this example in a sense you give it an input an article and we'll uh send it to an LLM. I think in this case it is open AI if I'm not mistaken and um what it does it is that it creates a summary and uh it creates in a human readable way some key points of this summary of what it has read and what we did it was to take the code and create a

**[38:59](https://www.youtube.com/watch?v=2peFTUktkuY&t=2339s)** new repository in our uh uh space and uh we simply change the dependency to be the quarkus lang chain for JGPU Lama 3 dependency and automatically you can if you want to reproduce the demo that I will show you you can clone the repo you can use uh JDK21 you can use tornadovm and uh you can build it with maven clean install and then just run this um command. So if we go to terminal, oh no PowerPoint. Yes. If we go to the terminal, we can actually run the streaming uh example. So this is the command that I

**[39:47](https://www.youtube.com/watch?v=2peFTUktkuY&t=2387s)** just show you that you can run. It uh launched Quarkus as we see here. This is the information of the original input article. It is about uh the global adoption of electric vehicles. And uh if we see more information here, we see that um here we have some parameters about the model. For example, it uses a a lama 31 billion model that it is available on my system. It has some configurations like the temperature uh the max number of tokens which is 1,024 and the the flag to run on on the GPU it has been enabled so it is true and once this is complete we see that we started receiving back the actual output of the

**[40:36](https://www.youtube.com/watch?v=2peFTUktkuY&t=2436s)** LLM. So we have one sentence summary and some key points that are listed by the LLM. And we see on at the bottom uh right side that uh the GPU has started uh computing inference. So we have more than 9,000 tasks that we offload on the GPU at this point and uh all the data they are coming back from the GPU and they are dumped on the console. And once this has finished, we see that the GPU usage goes back to 1% and the the GPU power also uh drops to zero. More information about the project are available in the DevOps presentation that Mkhalis gave from our team last

**[41:25](https://www.youtube.com/watch?v=2peFTUktkuY&t=2485s)** October. And we jump to the actual summary of um the talk today. Tornado VM during the past decade it has been um matured as a framework through different use cases that we have implemented. We have from spanning from computer vision. We have a rate tracer implementation in Java uh some machine learning models and LNLP and I have just shown you the recent use case which is the GPU lama 3 project and uh the idea is that uh we want to express parallelism in the Java world. we don't want to start coding in CUDA or OpenCL and we can also harness the power of GPUs. So here is a list of uh the road map of our team. So we're working on uh adding more improvements performance

**[42:14](https://www.youtube.com/watch?v=2peFTUktkuY&t=2534s)** improvements across different GPU vendors. So to uh to extend the supported platforms with new backends for example we're working on a metal back end for adding native support for Apple systems and Mac OS and we are working on AI specific optimizations and as I said earlier also we are uh finalizing the compatibility with JDK25 which will come in the next weeks and we are continuously monitoring um future Java features that are coming through projects like uh project Babylon Valh Halan leaden so that to be able to adapt our framework and utilize what's coming from these projects. So in summary, tornadov it is an open source plug-in for JDK distributions. It enables native GPU acceleration in a

**[43:04](https://www.youtube.com/watch?v=2peFTUktkuY&t=2584s)** seamless way and we have shown that it can enables Java to be AI ready for uh different computations through integration with lang forj and quarkus. It is production ready and we have a set of uh tool ecosystem that helps us to optimize like profiling and um for deployment. And uh finally uh this is a work that has been done uh in our group and we have several alumni and researchers that they have worked in the past decade to mature the the compiler. So it is a nice way to acknowledge them and um yeah I'm ready to go to questions. So, thank you very much. Do we have any questions?

**[44:02](https://www.youtube.com/watch?v=2peFTUktkuY&t=2642s)** Okay. If not, I invite you to come and if you want to collect uh some stickers that we have for Tornado VM and GPU Lama. So, yeah, come in the break. Thank you.
